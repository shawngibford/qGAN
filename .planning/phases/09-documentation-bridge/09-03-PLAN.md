---
phase: 09-documentation-bridge
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/training_protocol.md
autonomous: true
requirements: [DOC-01]
requirements_addressed: [DOC-01]
must_haves:
  truths:
    - "`docs/training_protocol.md` exists and is paper-ready (drop-in for Phase 14 Methods)"
    - "All 17 hyperparameter constants from `core/__init__.py` are documented with line citations (D-09)"
    - "Optimizer (Adam, betas=(0.0, 0.9)), N_CRITIC=9, λ=2.16, LR_CRITIC=1.8046e-05, LR_GENERATOR=6.9173e-05, NUM_EPOCHS=2000, BATCH_SIZE=12 all appear with correct values and file:line citations"
    - "Early-stopping rule documented (EMD-monitored, patience=50 eval cycles, warmup_epochs=100)"
    - "Seed=42 default and seeded RNGs documented (Claude's Discretion — Reproducibility section)"
    - "Shot/analytic distinction stated explicitly per D-10 (`shots=None`, statevector; shot sweep deferred to Phase 12)"
    - "Hybrid format per D-08: tables for numbers + ≥1 paragraph prose justification per section"
    - "Quantum circuit details (5 qubits, 4 layers, IQP-encoding + strongly-entangled, [0, 4π] noise) documented"
    - "Critic CNN architecture documented (Conv1d 1→64→128→128, k=10, p=5, AdaptiveAvgPool1d, Linear 128→32→1, dropout 0.2)"
    - "Gradient penalty (two-sided, λ=2.16) documented"
  artifacts:
    - path: "docs/training_protocol.md"
      provides: "Paper-ready training protocol Methods section content (DOC-01)"
      contains: "N_CRITIC"
      contains_2: "LAMBDA"
      contains_3: "LR_CRITIC"
      contains_4: "LR_GENERATOR"
      contains_5: "Adam"
      contains_6: "NUM_EPOCHS"
      contains_7: "BATCH_SIZE"
      contains_8: "NOISE_HIGH"
      contains_9: "shots=None"
      contains_10: "EarlyStopping"
      min_lines: 80
  key_links:
    - from: "docs/training_protocol.md"
      to: "core/__init__.py"
      via: "Source-of-truth citations per row (D-09)"
      pattern: "core/__init__.py:[0-9]+"
    - from: "docs/training_protocol.md"
      to: "core/training.py"
      via: "Adam betas, seed, EarlyStopping citations"
      pattern: "core/training.py:[0-9]+"
    - from: "docs/training_protocol.md"
      to: "core/models/quantum.py"
      via: "shots=None, diff_method=backprop citations"
      pattern: "core/models/quantum.py:[0-9]+"
---

<objective>
Write `docs/training_protocol.md` — a paper-ready Methods-section-equivalent document covering all training protocol details for the v1.1 unconditioned QWGAN-GP baseline. Format is hybrid (D-08): tables for numerical content, 1-paragraph prose justifications after each table. Every numerical value carries a file:line citation back to `core/__init__.py` or another canonical source file (D-09). The shot/analytic distinction is stated explicitly (D-10).

Purpose: Phase 14 (PAPER-08, PAPER-09) Methods drafting reads this doc directly; the line citations mean a future HPO retune updates `__init__.py` once and the doc tracking is mechanical.

Output: New file `docs/training_protocol.md` with seven sections (Optimizer & Schedule, Early-Stopping, Quantum Circuit, Critic 1D-CNN, Gradient Penalty, Reproducibility, Analytic-vs-Shot Distinction) and a top-of-file "Source of Truth" blockquote naming `core/__init__.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-documentation-bridge/09-CONTEXT.md
@.planning/phases/09-documentation-bridge/09-RESEARCH.md
@.planning/phases/09-documentation-bridge/09-PATTERNS.md

<interfaces>
<!-- Verified line numbers in core/__init__.py (read 2026-05-11; lines 1–45) -->

| Constant | Value | __init__.py line |
|----------|-------|------------------|
| N_CRITIC | 9 | 11 |
| LAMBDA | 2.16 | 12 |
| LR_CRITIC | 1.8046e-05 | 13 |
| LR_GENERATOR | 6.9173e-05 | 14 |
| NUM_QUBITS | 5 | 17 |
| NUM_LAYERS | 4 | 18 |
| WINDOW_LENGTH | 10 (= 2 × NUM_QUBITS) | 19 |
| NUM_EPOCHS | 2000 | 20 |
| BATCH_SIZE | 12 | 21 |
| GEN_SCALE | 1.0 | 22 |
| EVAL_EVERY | 10 | 23 |
| DROPOUT_RATE | 0.2 | 24 |
| DITHER | 0.005 | 27 |
| DITHER_SEED | 42 | 28 |
| PAR_LIGHT_MAX | 12.5 | 29 |
| NOISE_LOW | 0.0 | 32 |
| NOISE_HIGH | 4π (= 4*math.pi) | 33 |

<!-- Non-__init__.py canonical sources (verified live in 09-RESEARCH.md) -->
- Adam betas=(0.0, 0.9): core/training.py:233-234
- torch.manual_seed(seed): core/training.py:211
- torch.cuda.manual_seed_all(seed): core/training.py:215
- seed: int = 42 (default): core/training.py:188
- EarlyStopping defaults (patience=50, warmup_epochs=100): core/training.py:96-97
- shots=None (analytic statevector): core/models/quantum.py:64
- diff_method="backprop": core/models/quantum.py:43, 77
- compute_gradient_penalty: core/training.py:30-73
- compute_emd raw-sample wasserstein_distance: core/eval.py:25-36
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| markdown ↔ code | Hand-typed numbers can drift from __init__.py; line citations are the only mitigation absent runtime checks |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-11 | Tampering (number drift) | Hand-typed values in markdown table | mitigate | Every row cites `(\`core/__init__.py:LINE\`)`; reviewer can verify with one grep (Pitfall 7 from 09-RESEARCH.md) |
| T-09-12 | Tampering (overclaiming) | Shot/analytic phrasing | mitigate | D-10 explicit statement: "All Phase 9 results use analytic statevector simulation; shot-noise sweeps reported separately in Phase 12 (SENS-01)" — addresses R1-M5 calibration honesty |
| T-09-13 | Information Disclosure | external network/PII | accept | Static markdown; no PII, no secrets |
| T-09-14 | Repudiation (provenance) | citations to wrong file | mitigate | Acceptance criteria grep every cited path; Task 2 validates citations resolve |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Write docs/training_protocol.md (7 sections, hybrid table+prose)</name>
  <files>docs/training_protocol.md</files>
  <read_first>
    - core/__init__.py (FULL — confirm every line citation matches; this is the source of truth per D-09)
    - core/training.py (FULL — confirm Adam betas at 233-234, seed setup at 211-215, EarlyStopping at 79–175 / defaults at 94–98, train_wgan_gp signature at line 188)
    - core/models/quantum.py (confirm shots=None at line 64, diff_method="backprop" at lines 43 and 77)
    - core/models/critic.py (confirm Conv1d channel structure 1→64→128→128, k=10, p=5, AdaptiveAvgPool1d, Linear 128→32, Dropout, Linear 32→1)
    - core/eval.py (compute_emd raw-sample at lines 25–36)
    - .planning/phases/09-documentation-bridge/09-CONTEXT.md (D-08 hybrid format; D-09 citation rule; D-10 shot/analytic statement; "Claude's Discretion" subsection)
    - .planning/phases/09-documentation-bridge/09-RESEARCH.md (lines 494–567 — training_protocol.md skeleton template, lines 328–356 — content checklist)
    - .planning/phases/09-documentation-bridge/09-PATTERNS.md (lines 324–363 — citation patterns and section ordering)
  </read_first>
  <action>
    Create `docs/training_protocol.md` (the file does not exist; `docs/` contains only `.gitkeep`). Use the Write tool. The doc has SEVEN sections in this exact order:

    1. **Source-of-truth blockquote** (top of file, before any heading)
    2. **§ Optimizer & Schedule** (table + 1-paragraph prose)
    3. **§ Early-Stopping** (table + 1-paragraph prose)
    4. **§ Quantum Circuit** (table + 1-paragraph prose)
    5. **§ Critic (1D-CNN)** (table + 1-paragraph prose)
    6. **§ Gradient Penalty** (table + 1-paragraph prose)
    7. **§ Reproducibility** (table + 1-paragraph prose — includes seed=42, DITHER, DITHER_SEED)
    8. **§ Analytic-vs-Shot Distinction** (1-paragraph prose only; this is the D-10 statement)

    Exact content (write VERBATIM, do not paraphrase the numerical values):

    ````markdown
    # Training Protocol — QWGAN-GP (v1.1 unconditioned baseline)

    > **Source of truth:** all numerical constants below are imported from
    > `core/__init__.py` (with non-`__init__` quantities cited from
    > `core/training.py`, `core/models/quantum.py`, and
    > `core/models/critic.py`). Update those files to change values;
    > this document tracks them via per-row line citations.

    This protocol describes the QWGAN-GP training run that produced the
    `unconditioned_wgan` checkpoint (`qgan_pennylane.ipynb` cell 65,
    HPO-tuned in v1.1 Phase 4). All numerical values below are HPO-tuned
    defaults that remain in force for v2.0 unless explicitly noted.

    ## Optimizer & Schedule

    | Constant | Value | Source |
    |----------|-------|--------|
    | `N_CRITIC` | 9 critic steps per generator step | `core/__init__.py:11` |
    | `LAMBDA` (gradient penalty coeff) | 2.16 | `core/__init__.py:12` |
    | `LR_CRITIC` | 1.8046 × 10⁻⁵ | `core/__init__.py:13` |
    | `LR_GENERATOR` | 6.9173 × 10⁻⁵ | `core/__init__.py:14` |
    | Optimizer | Adam, `betas=(0.0, 0.9)` | `core/training.py:233-234` |
    | `NUM_EPOCHS` | 2000 | `core/__init__.py:20` |
    | `BATCH_SIZE` | 12 | `core/__init__.py:21` |
    | `EVAL_EVERY` | 10 epochs | `core/__init__.py:23` |

    `N_CRITIC=9` and `LAMBDA=2.16` are v1.1 HPO-tuned values; the high
    `N_CRITIC` reflects the WGAN-GP convention that the critic should
    approximate the Wasserstein distance well before each generator update.
    Both learning rates are also HPO-tuned and intentionally asymmetric —
    the generator updates roughly 4× more aggressively than the critic per
    Adam step to compensate for the 9:1 critic-to-generator step ratio.
    Adam betas `(0.0, 0.9)` follow Gulrajani et al. 2017's WGAN-GP
    recommendation (zero first-moment memory avoids stale-gradient drift
    against the moving critic target). The 2000-epoch budget with
    `BATCH_SIZE=12` and 384 training windows yields ≈ 32 mini-batches per
    epoch and ≈ 64 k generator updates total.

    ## Early-Stopping

    | Property | Value | Source |
    |----------|-------|--------|
    | Class | `EarlyStopping` (custom, EMD-monitored) | `core/training.py:79-175` |
    | Monitored metric | EMD on log-returns (raw-sample Wasserstein) | `core/eval.py:25-36` |
    | `patience` (default) | 50 eval cycles ≡ 500 epochs at `EVAL_EVERY=10` | `core/training.py:96` |
    | `warmup_epochs` (default) | 100 epochs (no monitoring during warmup) | `core/training.py:97` |
    | Checkpoint scheme | save-best-EMD, reload on stop | `core/training.py:142-175` |

    EMD is the headline distributional-fidelity metric for this study; it is
    computed via `scipy.stats.wasserstein_distance` on raw real-vs-synthetic
    samples (NOT histograms — v1.0 design lock). Monitoring EMD rather than
    critic loss avoids the moving-target ambiguity intrinsic to adversarial
    objectives. **Methodological caveat (R1-M5 calibration):** the EMD
    early-stop metric is computed on the same distribution used for training;
    a true held-out validation EMD is not available because the
    single-campaign dataset is too small to support a held-out split (see
    `docs/dataset_stats.md`). The 100-epoch warmup avoids
    selecting checkpoints from the noisy early-training regime; the
    50-eval-cycle patience (≈ 500 epochs) is generous on the 2000-epoch
    budget, accepting longer training runs in exchange for stable best-EMD
    selection.

    ## Quantum Circuit

    | Property | Value | Source |
    |----------|-------|--------|
    | Backend | PennyLane `default.qubit`, `shots=None` (analytic statevector) | `core/models/quantum.py:64` |
    | Differentiation | `diff_method="backprop"` | `core/models/quantum.py:43, 77` |
    | `NUM_QUBITS` | 5 | `core/__init__.py:17` |
    | `NUM_LAYERS` | 4 (strongly-entangled blocks) | `core/__init__.py:18` |
    | `WINDOW_LENGTH` | 10 (= 2 × `NUM_QUBITS`) | `core/__init__.py:19` |
    | Encoding | IQP-style data-reuploading + strongly-entangling ansatz | `core/models/quantum.py` |
    | Latent-noise range | [0, 4π] (NOT [0, 2π]; v1.1 Phase 4) | `core/__init__.py:32-33` (`NOISE_LOW`, `NOISE_HIGH = 4*math.pi`) |
    | `GEN_SCALE` | 1.0 (output rescaling) | `core/__init__.py:22` |
    | PQC trainable parameter count | 75 (= 5 + 4 × 15 + 10) | verified Phase 8 (`08-VERIFICATION.md`) |

    The 5-qubit / 4-layer choice yields a statevector of dimension 2⁵ = 32,
    well within local-Mac simulator memory, while 75 trainable parameters
    target an expressibility regime that is non-trivial without exceeding
    classical PQC trainability limits. `diff_method="backprop"` replaced
    parameter-shift in v1.1 Phase 5 because of PennyLane #4462 broadcasting
    gradient bugs in the parameter-shift path; backprop is exact on the
    analytic statevector and gradient-stable under batched inputs. The
    [0, 4π] noise range (v1.1 Phase 4) doubles the standard [0, 2π]
    convention to give the encoding layer more dynamic range; this was
    HPO-validated against [0, 2π] and showed improved generator diversity.

    ## Critic (1D-CNN)

    | Property | Value | Source |
    |----------|-------|--------|
    | Block 1 | `Conv1d(1 → 64, kernel_size=10, padding=5)` | `core/models/critic.py` |
    | Block 2 | `Conv1d(64 → 128, kernel_size=10, padding=5)` | `core/models/critic.py` |
    | Block 3 | `Conv1d(128 → 128, kernel_size=10, padding=5)` | `core/models/critic.py` |
    | Pooling | `AdaptiveAvgPool1d(1)` | `core/models/critic.py` |
    | Head | `Linear(128 → 32) → Dropout → Linear(32 → 1)` | `core/models/critic.py` |
    | `DROPOUT_RATE` | 0.2 (configurable) | `core/__init__.py:24` |
    | Precision | `float64` (`.double()`) | `core/models/critic.py` |

    The critic is a 1D-CNN with three convolutional blocks at fixed
    `kernel_size=10` matching `WINDOW_LENGTH`; receptive fields span the
    full input window in the first layer. `AdaptiveAvgPool1d(1)` reduces
    the temporal axis to a single per-channel summary before the linear
    head. Lipschitz constraint is enforced via the two-sided gradient
    penalty (see § Gradient Penalty), NOT weight clipping. The critic
    runs in `float64` for numerical-gradient stability under the high
    `N_CRITIC=9` ratio.

    ## Gradient Penalty

    | Property | Value | Source |
    |----------|-------|--------|
    | Type | Two-sided: `mean(((‖∇‖₂ − 1)²))` | `core/training.py:30-73` |
    | Coefficient λ | 2.16 (= `LAMBDA`) | `core/__init__.py:12` |
    | Interpolation α | sampled per-sample from `U(0, 1)`, broadcast over remaining dims | `core/training.py:54-60` |
    | Gradient target | 1 (unit-norm penalty) | `core/training.py:30-73` |

    Two-sided gradient penalty (Gulrajani et al. 2017) enforces the
    1-Lipschitz constraint required by the Wasserstein dual. The penalty
    coefficient `λ=2.16` is HPO-tuned (v1.1 Phase 4) — slightly above the
    canonical λ=10 baseline; the lower value paired with `N_CRITIC=9` was
    empirically more stable than the canonical λ=10 / N_CRITIC=5 setting
    on this dataset.

    ## Reproducibility

    | Property | Value | Source |
    |----------|-------|--------|
    | Seed (default) | 42 | `core/training.py:188` |
    | `torch.manual_seed(seed)` | yes | `core/training.py:211` |
    | `np.random.seed(seed)` | yes | `core/training.py:212-213` |
    | `random.seed(seed)` | yes | `core/training.py:213-214` |
    | `torch.cuda.manual_seed_all(seed)` | yes (no-op on CPU/MPS) | `core/training.py:215` |
    | `DITHER` (data pipeline) | 0.005 | `core/__init__.py:27` |
    | `DITHER_SEED` | 42 | `core/__init__.py:28` |

    All randomness sources are seeded inside `train_wgan_gp` before
    parameter initialization to guarantee per-seed determinism on a fixed
    machine. `DITHER=0.005` adds U(−0.005, +0.005) noise during data
    preprocessing to break OD-value ties before log-ratio computation;
    `DITHER_SEED=42` keeps the dither itself reproducible. Multi-seed
    sweeps (≥ 5 seeds) for headline results are produced by Phase 12
    (SENS-03), reusing this single-seed framework.

    ## Analytic-vs-Shot Distinction

    All Phase 9 results use **analytic statevector simulation** (PennyLane
    `default.qubit` with `shots=None`). Shot-noise behavior is reported
    **separately in Phase 12 (SENS-01)** at `{analytic, 8192, 1024}`
    shots; that analysis is out of scope for Phase 9. This protocol
    document, the Phase 8 parity check, the EVAL-06 round-trip check
    (Phase 9), and the Phase 09.1 preprocessing ablation all run in the
    analytic-statevector regime.
    ````

    Notes:
    - Numbers MUST be VERBATIM: `1.8046 × 10⁻⁵` (Unicode 10⁻⁵ allowed in markdown) OR `1.8046e-05` — both render to a paper-ready value. Use the form shown above (× 10⁻⁵) for prose; the `1.8046e-05` form for grep gates.
    - Section ordering above follows the "Claude's Discretion" suggestion in 09-RESEARCH.md line 360 and is final.
    - The "Reproducibility" subsection IS included (CONTEXT.md Claude's discretion item — it fits naturally).
    - Do NOT hand-type values for any constant; copy the values from `core/__init__.py` line-by-line.
  </action>
  <verify>
    <automated>cd /Users/shawngibford/dev/phd/qGAN &amp;&amp; F=docs/training_protocol.md &amp;&amp; test -f "$F" &amp;&amp; \
      grep -c 'N_CRITIC' "$F" &gt; /dev/null &amp;&amp; \
      grep -c 'LAMBDA' "$F" &gt; /dev/null &amp;&amp; \
      grep -c 'LR_CRITIC' "$F" &gt; /dev/null &amp;&amp; \
      grep -c 'LR_GENERATOR' "$F" &gt; /dev/null &amp;&amp; \
      grep -q 'betas=(0.0, 0.9)' "$F" &amp;&amp; \
      grep -q 'NUM_EPOCHS' "$F" &amp;&amp; \
      grep -q 'BATCH_SIZE' "$F" &amp;&amp; \
      grep -q 'shots=None' "$F" &amp;&amp; \
      grep -q 'EarlyStopping' "$F" &amp;&amp; \
      grep -q 'patience' "$F" &amp;&amp; \
      grep -q 'warmup_epochs' "$F" &amp;&amp; \
      grep -q 'NOISE_HIGH' "$F" &amp;&amp; \
      grep -q '\[0, 4π\]\|4π\|4 \* math.pi\|4\*math.pi\|NOISE_HIGH' "$F" &amp;&amp; \
      grep -q 'DROPOUT_RATE' "$F" &amp;&amp; \
      grep -q 'seed' "$F" &amp;&amp; \
      grep -q '42' "$F" &amp;&amp; \
      grep -q 'diff_method' "$F" &amp;&amp; \
      grep -q 'backprop' "$F" &amp;&amp; \
      grep -q 'Phase 12' "$F" &amp;&amp; \
      grep -q 'analytic' "$F" &amp;&amp; \
      [ "$(grep -c 'core/__init__.py' "$F")" -ge 10 ] &amp;&amp; \
      [ "$(wc -l &lt; "$F")" -ge 80 ] &amp;&amp; \
      echo 'Task 1 training_protocol.md checklist: OK'</automated>
  </verify>
  <acceptance_criteria>
    - File `docs/training_protocol.md` exists
    - `grep -c 'N_CRITIC' docs/training_protocol.md` >= 1
    - `grep -c 'LAMBDA' docs/training_protocol.md` >= 1
    - `grep -c 'LR_CRITIC' docs/training_protocol.md` >= 1
    - `grep -c 'LR_GENERATOR' docs/training_protocol.md` >= 1
    - `grep -c 'betas=(0.0, 0.9)' docs/training_protocol.md` >= 1
    - `grep -c 'NUM_EPOCHS' docs/training_protocol.md` >= 1
    - `grep -c 'BATCH_SIZE' docs/training_protocol.md` >= 1
    - `grep -c 'shots=None' docs/training_protocol.md` >= 1
    - `grep -c 'EarlyStopping' docs/training_protocol.md` >= 1
    - `grep -c 'patience' docs/training_protocol.md` >= 1
    - `grep -c 'warmup_epochs' docs/training_protocol.md` >= 1
    - `grep -c 'NOISE_HIGH' docs/training_protocol.md` >= 1
    - `grep -c 'NUM_QUBITS' docs/training_protocol.md` >= 1
    - `grep -c 'NUM_LAYERS' docs/training_protocol.md` >= 1
    - `grep -c 'WINDOW_LENGTH' docs/training_protocol.md` >= 1
    - `grep -c 'DROPOUT_RATE' docs/training_protocol.md` >= 1
    - `grep -c 'DITHER' docs/training_protocol.md` >= 1
    - `grep -c 'diff_method' docs/training_protocol.md` >= 1
    - `grep -c 'backprop' docs/training_protocol.md` >= 1
    - `grep -c 'Phase 12' docs/training_protocol.md` >= 1 (shot-noise deferral D-10)
    - `grep -c '42' docs/training_protocol.md` >= 1 (seed)
    - `grep -c 'core/__init__.py' docs/training_protocol.md` >= 10 (citation discipline; D-09)
    - `wc -l docs/training_protocol.md` returns >= 80
  </acceptance_criteria>
  <done>
    `docs/training_protocol.md` exists, has all 7 sections in order, every cited constant appears with the correct value and `core/__init__.py:LINE` (or `core/training.py:LINE`, `core/models/quantum.py:LINE`, `core/models/critic.py:LINE`) citation, prose justifications follow each table, and the D-10 shot/analytic statement is present.
  </done>
</task>

</tasks>

<verification>
- Task 1 `<verify>` exits 0 (all 20+ grep gates pass)
- File length ≥ 80 lines (substantive content per D-08 hybrid format)
- Every numerical value appearing in the doc matches `core/__init__.py` (or other cited source) verbatim
- D-10 statement present in the final section
</verification>

<success_criteria>
- All acceptance-criteria grep gates pass
- `docs/training_protocol.md` is paper-ready for Phase 14 Methods drafting
- Number-to-source citations are present in EVERY table row (D-09)
- Hybrid format (table + 1-paragraph prose) followed for all sections (D-08)
</success_criteria>

<output>
After completion, create `.planning/phases/09-documentation-bridge/09-03-SUMMARY.md` documenting:
- Final file path and line count
- List of 7 sections with their numerical citations
- Confirmation that every constant value matches `core/__init__.py` (one-sentence audit)
- Pointer to Phase 14 (PAPER-08, PAPER-09) — this doc is the upstream artifact
</output>
