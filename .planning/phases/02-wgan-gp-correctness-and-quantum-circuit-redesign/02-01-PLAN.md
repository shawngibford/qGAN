---
phase: 02-wgan-gp-correctness-and-quantum-circuit-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [qgan_pennylane.ipynb]
autonomous: true
requirements: [QUAL-06, QC-05, WGAN-01, WGAN-02, WGAN-07]

must_haves:
  truths:
    - "WINDOW_LENGTH is computed as 2 * NUM_QUBITS in the config cell, not independently hardcoded"
    - "N_CRITIC = 5 and LAMBDA = 10 in the config cell (restored to Gulrajani et al. paper values)"
    - "LR_CRITIC >= LR_GENERATOR (critic learns faster than generator per WGAN-GP theory)"
    - "normalize() returns a 3-tuple (normalized_data, mu, sigma) and the call site unpacks all three"
    - "GEN_SCALE = 0.1 is defined as a named constant in the config cell"
  artifacts:
    - path: "qgan_pennylane.ipynb"
      provides: "Updated config cell + normalize signature"
      contains: "WINDOW_LENGTH = 2 * NUM_QUBITS"
  key_links:
    - from: "normalize() function (Cell 14)"
      to: "normalize() call site (Cell 15)"
      via: "3-tuple return unpacking"
      pattern: "norm_log_delta, mu, sigma = normalize"
    - from: "Config cell (Cell 27)"
      to: "qGAN instantiation"
      via: "WINDOW_LENGTH, N_CRITIC, LAMBDA passed to constructor"
      pattern: "WINDOW_LENGTH = 2 \\* NUM_QUBITS"
---

<objective>
Update the hyperparameter config cell to restore WGAN-GP paper values and compute WINDOW_LENGTH from NUM_QUBITS, and change normalize() to return mu/sigma for checkpoint serialization and denormalization.

Purpose: Establishes the foundational config and function signatures that all subsequent plans depend on. These are low-risk changes that set correct values before the complex circuit and training loop rewrites.

Output: Config cell with correct hyperparameters, normalize() returning tuple, mu/sigma stored for later checkpoint use.
</objective>

<execution_context>
@/Users/shawngibford/.claude/get-shit-done/workflows/execute-plan.md
@/Users/shawngibford/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-wgan-gp-correctness-and-quantum-circuit-redesign/02-CONTEXT.md
@.planning/phases/02-wgan-gp-correctness-and-quantum-circuit-redesign/02-RESEARCH.md

<interfaces>
<!-- Current config cell (Cell 27) values that need changing: -->
WINDOW_LENGTH = 10          # → WINDOW_LENGTH = 2 * NUM_QUBITS
N_CRITIC = 1                # → N_CRITIC = 5
LAMBDA = 0.8                # → LAMBDA = 10
LR_CRITIC = 3e-5            # → LR_CRITIC = 8e-5 (swap with generator)
LR_GENERATOR = 8e-5         # → LR_GENERATOR = 3e-5 (swap with critic)
# NEW constants to add:
# GEN_SCALE = 0.1
# EVAL_EVERY = 10

<!-- Current normalize() in Cell 14: -->
def normalize(data):
    mu = torch.mean(data)
    sigma = torch.std(data)
    return (data - mu) / sigma  # Returns only normalized data

<!-- Current call site in Cell 15: -->
norm_log_delta = normalize(log_delta)

<!-- Phase 1 patterns to follow: -->
# UPPER_CASE for all hyperparameter constants
# Stage-based pipeline naming: raw_data, log_delta, norm_log_delta, transformed_norm_log_delta, scaled_data, windowed_data
# Instance attributes use lowercase (self.lambda_gp, self.n_critic)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update config cell hyperparameters to WGAN-GP paper values</name>
  <files>qgan_pennylane.ipynb</files>
  <action>
Edit Cell 27 (Hyperparameters config cell) to make the following changes:

1. **WINDOW_LENGTH** — change from `WINDOW_LENGTH = 10` to `WINDOW_LENGTH = 2 * NUM_QUBITS`. Add a comment: `# Output dimension: PauliX + PauliZ per qubit`

2. **N_CRITIC** — change from `N_CRITIC = 1` to `N_CRITIC = 5`. Update comment to reference Gulrajani et al. Algorithm 1.

3. **LAMBDA** — change from `LAMBDA = 0.8` to `LAMBDA = 10`. Update comment to reference Gulrajani et al.

4. **Learning rates** — swap values: `LR_CRITIC = 8e-5` and `LR_GENERATOR = 3e-5`. The critic must learn faster (LR_CRITIC >= LR_GENERATOR) per WGAN-GP theory. Update comments accordingly.

5. **Add new constants:**
   - `GEN_SCALE = 0.1  # Generator output scaling factor`
   - `EVAL_EVERY = 10  # Evaluate metrics every N epochs`

6. **Update the parameter verification section** in the same cell:
   - Remove `iqp_params = NUM_QUBITS` from expected params calculation (QC-01 will remove those params in Plan 02)
   - Update: `expected_params = NUM_LAYERS * per_layer_params + final_params` (no iqp_params term)
   - Update all the print statements to reflect the new values
   - DO NOT change the qGAN instantiation line yet (constructor changes come in Plan 02)

7. **Update print section** to reflect corrected values. The "Critic/Generator Ratio" line should show `1:5` not `1:1`.

Note: The parameter verification will temporarily show a mismatch with the actual model (which still has IQP params). This is expected — Plan 02 will update the circuit to match. Add a comment: `# NOTE: param count will match after circuit redesign in Plan 02-02`.
  </action>
  <verify>
    <automated>python3 -c "
import json
with open('qgan_pennylane.ipynb') as f:
    nb = json.load(f)
src = ''.join(nb['cells'][27]['source'])
assert 'WINDOW_LENGTH = 2 * NUM_QUBITS' in src, 'WINDOW_LENGTH not computed'
assert 'N_CRITIC = 5' in src, 'N_CRITIC not 5'
assert 'LAMBDA = 10' in src, 'LAMBDA not 10'
assert 'LR_CRITIC = 8e-5' in src or 'LR_CRITIC = 8e-05' in src, 'LR_CRITIC not swapped'
assert 'LR_GENERATOR = 3e-5' in src or 'LR_GENERATOR = 3e-05' in src, 'LR_GENERATOR not swapped'
assert 'GEN_SCALE' in src, 'GEN_SCALE not defined'
assert 'EVAL_EVERY' in src, 'EVAL_EVERY not defined'
print('All config assertions passed')
"
    </automated>
  </verify>
  <done>Config cell contains WINDOW_LENGTH = 2 * NUM_QUBITS, N_CRITIC = 5, LAMBDA = 10, swapped learning rates (critic >= generator), GEN_SCALE = 0.1, and EVAL_EVERY = 10</done>
</task>

<task type="auto">
  <name>Task 2: Update normalize() to return (data, mu, sigma) tuple and fix call site</name>
  <files>qgan_pennylane.ipynb</files>
  <action>
Edit Cell 14 (utility functions) and Cell 15 (normalize call site):

1. **Cell 14 — normalize() function:** Change the return statement to return a 3-tuple:
```python
def normalize(data):
    """Normalize the data to have zero mean and unit variance.

    Returns:
        tuple: (normalized_data, mu, sigma) where mu is the mean and sigma is the std
    """
    mu = torch.mean(data)
    sigma = torch.std(data)
    return (data - mu) / sigma, mu, sigma
```

2. **Cell 15 — call site:** Update the unpacking:
```python
# normalize the log-delta
norm_log_delta, mu, sigma = normalize(log_delta)
```
Keep the existing print statements. Add a print for mu and sigma values:
```python
print(f'Stored mu = {mu.item():.6f}, sigma = {sigma.item():.6f}')
```

3. **Verify no other call sites of normalize() exist** in the notebook. If found, update them too. The function is called only once in Cell 15 based on current code analysis.

The mu and sigma values will be used by the checkpoint system (Plan 02-03/04) and denormalization pipeline. For now they are module-level variables accessible from any cell.
  </action>
  <verify>
    <automated>python3 -c "
import json
with open('qgan_pennylane.ipynb') as f:
    nb = json.load(f)
# Check normalize function returns tuple
src14 = ''.join(nb['cells'][14]['source'])
assert 'return (data - mu) / sigma, mu, sigma' in src14, 'normalize does not return tuple'
# Check call site unpacks 3 values
src15 = ''.join(nb['cells'][15]['source'])
assert 'norm_log_delta, mu, sigma = normalize' in src15, 'Call site does not unpack 3 values'
print('normalize() tuple return + call site assertions passed')
"
    </automated>
  </verify>
  <done>normalize() returns (normalized_data, mu, sigma) tuple, call site unpacks all three values, mu and sigma are available as module-level variables for checkpoint serialization</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. Config cell has WGAN-GP paper values (N_CRITIC=5, LAMBDA=10, LR_CRITIC=8e-5, LR_GENERATOR=3e-5)
2. WINDOW_LENGTH is dynamically computed from NUM_QUBITS
3. GEN_SCALE and EVAL_EVERY are defined as named constants
4. normalize() returns 3-tuple and call site correctly unpacks
5. mu and sigma are stored as module-level variables
</verification>

<success_criteria>
- Config cell contains all corrected WGAN-GP hyperparameters per Gulrajani et al. (2017)
- WINDOW_LENGTH = 2 * NUM_QUBITS (not hardcoded 10)
- normalize() returns (data, mu, sigma) tuple and the single call site unpacks correctly
- No runtime errors from the changed function signature
</success_criteria>

<output>
After completion, create `.planning/phases/02-wgan-gp-correctness-and-quantum-circuit-redesign/02-01-SUMMARY.md`
</output>
