---
phase: 13-architecture-introspection
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - core/models/quantum.py
  - core/training.py
  - tests/conftest.py
  - tests/test_ansatz_variants.py
  - tests/test_entropy_purity.py
  - tests/test_cr01_spectral_grad.py
  - tests/test_cr02_es_restore.py
autonomous: true
requirements: [ARCH-01, INTRO-03]
must_haves:
  truths:
    - "QuantumGenerator(num_layers=4, topology='range') (the default) produces byte-identical params and circuit to pre-change (Phases 8-12 reproduce)"
    - "QuantumGenerator(num_layers=8, topology='range') has 135 params; QuantumGenerator(num_layers=4, topology='linear') has 75 params and uses only nearest-neighbour CNOTs"
    - "generator.introspect(noise_vec) returns scalar Von Neumann entropy in [0, ln4] and purity in [0.25, 1] for a 2|3 bipartition"
    - "With spectral_loss_weight>0 a non-zero gradient flows into params_pqc; with spectral_loss_weight=0.0 the branch is skipped (default byte-unchanged)"
    - "EarlyStopping restore is device/dtype-consistent on CPU (and MPS if available); early_stopper=None default path unexercised"
  artifacts:
    - path: "core/models/quantum.py"
      provides: "topology-selectable ansatz + introspect() entanglement/purity helper"
      contains: "topology"
    - path: "core/training.py"
      provides: "CR-01 differentiable PSD loss + CR-02 device-safe EarlyStopping restore"
      contains: "torch.fft.rfft"
    - path: "tests/test_ansatz_variants.py"
      provides: "ARCH-01 selectability + byte-unchanged-default + param-count regression"
    - path: "tests/test_entropy_purity.py"
      provides: "INTRO-03 entropy/purity bounds + API regression"
    - path: "tests/test_cr01_spectral_grad.py"
      provides: "CR-01 grad-present-when-on / skipped-when-off regression"
    - path: "tests/test_cr02_es_restore.py"
      provides: "CR-02 device/dtype-consistency regression"
  key_links:
    - from: "core/models/quantum.py::generator_circuit"
      to: "topology switch"
      via: "if self.topology == 'range' (default, literal existing block) elif 'linear'"
      pattern: "self\\.topology"
    - from: "core/models/quantum.py::introspect"
      to: "_introspect_qnode"
      via: "qml.vn_entropy(wires=[0,1]), qml.purity(wires=[0,1])"
      pattern: "qml\\.vn_entropy|qml\\.purity"
---

<objective>
Land the three byte-unchanged-default `core/` edits and the pytest
scaffold that the rest of Phase 13 depends on:

1. ARCH-01 — add a `topology` selector + an `introspect()` entanglement/purity
   helper to `QuantumGenerator` (V1 default circuit byte-identical).
2. CR-01 — replace the broken zero-gradient `scipy.welch` spectral loss with a
   differentiable, device-resident `torch.fft.rfft` periodogram.
3. CR-02 — make `EarlyStopping._load_checkpoint` device/dtype-consistent.
4. Wave 0 — create the greenfield `tests/` package + the four regression tests
   covering ARCH-01 / INTRO-03 / CR-01 / CR-02.

Purpose: Plans 02 (ansatz sweep) and 03 (introspection runs) cannot start until
the topology arg and `introspect()` exist; the CR fixes are folded todos tagged
`resolves_phase: 13`; the tests are the Nyquist gate for every downstream claim.
Output: Modified `quantum.py`, `training.py`; new `tests/` package with 4 test
files + conftest.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-architecture-introspection/13-CONTEXT.md
@.planning/phases/13-architecture-introspection/13-RESEARCH.md
@.planning/phases/13-architecture-introspection/13-PATTERNS.md

<interfaces>
<!-- Extracted from the codebase. Use these directly — no exploration needed. -->

core/models/quantum.py — QuantumGenerator.__init__(num_qubits=5,
num_layers=4, window_length=10, diff_method="backprop"). num_params formula
(line 59-61): num_qubits + num_layers*(num_qubits*3) + num_qubits*2 →
5+4*15+10=75 (V1/V3), 5+8*15+10=135 (V2). self.dev =
qml.device("default.qubit", wires=num_qubits, shots=None). self.qnode built at
lines 73-78. generator_circuit Steps 1-6 at lines 122-171; the range-CNOT
DEFAULT block is lines 150-155 (literal):
  range_param = (layer % (self.num_qubits - 1)) + 1
  for qubit in range(self.num_qubits):
      target_qubit = (qubit + range_param) % self.num_qubits
      qml.CNOT(wires=[qubit, target_qubit])
idx accounting lines 122-163 must stay untouched (topology touches only CNOT
wiring → param count unchanged).

core/training.py — _spectral_psd_loss(fake, real) at lines 470-507
(scipy.welch + numpy + mse*var/var.detach() proxy — the CR-01 target). Call
site lines 356-360 guarded by `if spectral_loss_weight > 0.0:` — KEEP guard
(D-13-06 default byte-unchanged). real_log_returns_for_psd helper lines 464-467
KEEP unchanged. EarlyStopping._load_checkpoint lines 163-175 (the CR-02 target):
currently un-mapped torch.load + raw .data= assignment. `model` is the
_ESAdapter whose params_pqc proxies generator.params_pqc; .critic/.c_optimizer/
.g_optimizer are direct attrs. Keep the trailing print(...) lines 172-175.
train_wgan_gp(...) signature: keyword-only num_epochs, n_critic, lambda_gp,
lr_critic, lr_generator, seed, spectral_loss_weight=0.0, eval_every=10,
early_stopper=None, callback=None.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0: Create tests/ package scaffold + conftest</name>
  <files>tests/conftest.py, tests/__init__.py</files>
  <read_first>
    - run_baselines.py (how the project resolves ./qgan_env python + imports revision.core)
    - .planning/phases/13-architecture-introspection/13-RESEARCH.md (Validation Architecture section — quick-run command, Wave 0 Gaps list)
  </read_first>
  <action>
    Create the greenfield `tests/` directory (none exists). Add empty
    `tests/__init__.py` and `tests/conftest.py`. conftest.py inserts the repo
    root onto sys.path so `import revision.core...` resolves when pytest runs
    from repo root; expose a session fixture `repo_root` returning the absolute
    repo root Path. No pytest.ini change needed unless collection fails — if it
    does, add a minimal `pytest.ini` at repo root with `testpaths = tests`.
    Tests MUST be runnable via `./qgan_env/bin/python -m pytest tests/ -q`.
  </action>
  <verify>
    <automated>./qgan_env/bin/python -m pytest tests/ -q --collect-only</automated>
  </verify>
  <acceptance_criteria>
    - `tests/conftest.py` and `tests/__init__.py` exist
    - `./qgan_env/bin/python -m pytest tests/ -q --collect-only` exits 0 (collection succeeds, zero or more tests)
    - conftest puts repo root on sys.path (importing `revision.core` from a test does not raise ModuleNotFoundError)
  </acceptance_criteria>
  <done>tests/ package importable; pytest collection succeeds from repo root.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Add topology selector + introspect() to QuantumGenerator (ARCH-01, INTRO-03)</name>
  <files>core/models/quantum.py, tests/test_ansatz_variants.py, tests/test_entropy_purity.py</files>
  <read_first>
    - core/models/quantum.py (FULL file — current __init__, generator_circuit, count_params, forward — implement against actual current state)
    - .planning/phases/13-architecture-introspection/13-RESEARCH.md (Pattern 1 byte-unchanged topology switch; Pattern 3 verified PennyLane 0.43.0 vn_entropy/purity API + bounds; Pitfall 3 byte-unchanged discipline; Pitfall 6 MPS/float64 introspection device)
    - .planning/phases/13-architecture-introspection/13-PATTERNS.md (quantum.py topology-switch + introspect() assignment, lines 26-58)
  </read_first>
  <behavior>
    - QuantumGenerator() with no extra args: count_params()==75; fixed-seed
      (torch.manual_seed(0)) forward pass on a fixed noise vector matches a
      reference vector captured from the pre-change code (byte-unchanged default)
    - QuantumGenerator(num_layers=8) → count_params()==135
    - QuantumGenerator(num_layers=4, topology="linear") → count_params()==75
    - topology="linear" emits only qml.CNOT(wires=[q, q+1]) for q in 0..n-2
      (no wrap-around) — assert via qml.draw or tape op inspection
    - topology="range" (default) emits the existing wrap-around pattern unchanged
    - introspect(noise_vec) with noise_vec shape (5,) returns two python floats:
      vn_entropy in [0.0, math.log(4)+1e-6], purity in [0.25-1e-6, 1.0+1e-6]
    - Invalid topology value raises a clear ValueError (argparse-style guard)
  </behavior>
  <action>
    Add keyword arg `topology: str = "range"` to `QuantumGenerator.__init__`;
    store `self.topology`; validate against {"range","linear"} raising
    ValueError otherwise. Leave the `self.num_params` formula at lines 59-61
    UNCHANGED (topology does not change param count: V1/V3=75, V2=135).
    In `generator_circuit` Step 4, wrap the existing lines 150-155 range block
    verbatim under `if self.topology == "range":` as the LITERAL first branch
    (so the drawn tape is byte-identical for the default), add
    `elif self.topology == "linear":` emitting `for q in range(self.num_qubits
    - 1): qml.CNOT(wires=[q, q + 1])`. Do NOT touch idx accounting (lines
    122-163). Add a read-only `_introspect_circuit(self, noise_params,
    params_pqc)` that clones generator_circuit Steps 1-5 verbatim (including the
    topology switch) but returns `qml.vn_entropy(wires=[0, 1]),
    qml.purity(wires=[0, 1])` (the 2|3 balanced bipartition; record {0,1}|{2,3,4}
    in a class/docstring constant for the JSON metadata in plan 03). In
    __init__, build `self._introspect_qnode = qml.QNode(self._introspect_circuit,
    self.dev, interface="torch", diff_method="backprop")` next to self.qnode
    (lines 73-78 pattern). Add `introspect(self, noise_vec)`: ensure params/noise
    are CPU-resident (Pitfall 6 — `.detach().cpu()` params if on MPS), call
    `self._introspect_qnode(noise_vec, self.params_pqc)` inside the caller's
    no-grad context, return `(float(e), float(p))`.
    Write tests/test_ansatz_variants.py (count_params 75/135/75, default
    byte-unchanged fixed-seed forward vs an in-test reference computed from the
    default-topology path, linear-CNOT-only assertion, ValueError on bad
    topology) and tests/test_entropy_purity.py (introspect bounds with
    torch.no_grad). RED first: write failing tests, then implement.
  </action>
  <verify>
    <automated>./qgan_env/bin/python -m pytest tests/test_ansatz_variants.py tests/test_entropy_purity.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `quantum.py` source contains `topology` in the `__init__` signature and `self.topology` stored
    - `quantum.py` contains `qml.vn_entropy(wires=[0, 1])` and `qml.purity(wires=[0, 1])`
    - `grep -n 'range_param = (layer % (self.num_qubits - 1)) + 1' core/models/quantum.py` still matches (default branch body literally preserved)
    - `pytest tests/test_ansatz_variants.py tests/test_entropy_purity.py -x -q` exits 0
    - Default-topology count_params()==75 and fixed-seed forward equals reference (byte-unchanged assertion present in test and passing)
  </acceptance_criteria>
  <done>V1/V2/V3 selectable via topology+num_layers, default byte-identical, introspect() returns bounded entropy/purity; both tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CR-01 differentiable PSD loss + CR-02 device-safe ES restore (with regression tests)</name>
  <files>core/training.py, tests/test_cr01_spectral_grad.py, tests/test_cr02_es_restore.py</files>
  <read_first>
    - core/training.py (FULL relevant ranges: _spectral_psd_loss 470-507, call site 356-360, real_log_returns_for_psd 464-467, EarlyStopping._load_checkpoint 163-175, _ESAdapter 438-461, train_wgan_gp signature 178-208)
    - .planning/phases/13-architecture-introspection/13-RESEARCH.md (Pattern 5 CR-01 torch.fft periodogram; Pattern 6 CR-02 map_location+recast+opt-state-to-device; CONTEXT-mandated regression-test shapes)
    - .planning/phases/13-architecture-introspection/13-PATTERNS.md (training.py CR-01/CR-02 self-replace blocks, lines 62-109)
    - .planning/todos/pending/2026-05-18-fix-cr-01-spectral-loss-hook-non-differentiable.md
    - .planning/todos/pending/2026-05-18-fix-cr-02-earlystopping-restore-device-inconsistent.md
  </read_first>
  <behavior>
    - CR-01: with spectral_loss_weight>0, after generator_loss.backward(),
      generator.params_pqc.grad is not None and .grad.abs().sum() > 0
    - CR-01: with spectral_loss_weight=0.0 the `if spectral_loss_weight > 0.0`
      branch is not entered (assert via a stub/spy or by asserting the loss
      equals the pure -mean(fake_scores) term) — default byte-unchanged
    - CR-01: _spectral_psd_loss(fake, real) returns a torch.Tensor (not a python
      float), is finite, and carries grad_fn when fake.requires_grad
    - CR-02: after EarlyStopping restore, model.params_pqc.device and .dtype
      match the live device/dtype, and every optimizer-state tensor is on that
      device — assert on CPU; assert on MPS guarded by
      pytest.mark.skipif(not torch.backends.mps.is_available())
    - CR-02: early_stopper=None default path is never exercised by a plain
      train_wgan_gp call (sanity assertion)
  </behavior>
  <action>
    CR-01: replace the body of `_spectral_psd_loss` (lines 470-507) with the
    differentiable torch periodogram: flatten fake; flatten + detach real; move
    real to fake's device+dtype; psd via `torch.fft.rfft(x).abs()**2`; return
    `torch.mean((torch.log(psd_fake+eps) - torch.log(psd_real+eps))**2)` with
    eps=1e-12. Drop the inner `from scipy.signal import welch` import. Keep
    `real_log_returns_for_psd` (464-467) and the call-site guard `if
    spectral_loss_weight > 0.0:` (356-360) UNCHANGED.
    CR-02: replace `EarlyStopping._load_checkpoint` body (165-171) with
    map_location to `model.params_pqc.device`, recast restored params to live
    device+dtype, then push every torch-tensor value in both
    c_optimizer/g_optimizer `.state` to that device, then re-register
    `g_optimizer.param_groups[0]["params"] = [model.params_pqc]`. Keep the
    trailing print (172-175).
    Write tests/test_cr01_spectral_grad.py (grad-present-when-weight>0 via a
    tiny synthetic forward+backward through params_pqc; weight=0.0
    branch-skipped; return-type is torch.Tensor) and tests/test_cr02_es_restore.py
    (CPU device/dtype consistency after restore; MPS variant skipif). RED first.
  </action>
  <verify>
    <automated>./qgan_env/bin/python -m pytest tests/test_cr01_spectral_grad.py tests/test_cr02_es_restore.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n 'torch.fft.rfft' core/training.py` matches inside _spectral_psd_loss
    - `grep -n 'from scipy.signal import welch' core/training.py` returns no match
    - `grep -n 'if spectral_loss_weight > 0.0' core/training.py` still matches (call-site guard preserved)
    - `grep -n 'map_location' core/training.py` matches inside _load_checkpoint
    - `pytest tests/test_cr01_spectral_grad.py tests/test_cr02_es_restore.py -x -q` exits 0
    - CR-01 test asserts `params_pqc.grad.abs().sum() > 0` when weight>0 AND branch skipped when weight=0.0
  </acceptance_criteria>
  <done>CR-01 PSD loss is a real differentiable device-resident torch term; CR-02 restore is device/dtype-safe; defaults byte-unchanged; both regression tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pre-change `core/` ↔ post-change `core/` | Phases 8-12 reproducibility depends on byte-identical default behavior crossing this edit boundary |

## STRIDE Threat Register

(Local-Mac scientific compute: no network, no auth, no PII. Threats are
research-integrity, scoped truthfully.)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Tampering | quantum.py default circuit silently changes (idx/CNOT reorder) → Phases 8-12 no longer reproduce | mitigate | test_ansatz_variants.py asserts count_params()==75 + fixed-seed forward equals reference; range block kept as literal first branch |
| T-13-02 | Tampering | CR-01 fix accidentally activates spectral term at weight=0.0 → headline runs corrupted vs baseline set | mitigate | test_cr01_spectral_grad.py asserts weight=0.0 branch skipped; call-site guard left unchanged |
| T-13-03 | Information disclosure / corruption | CR-02 restore lands params on wrong device/dtype → silent NaN/garbage in any future ES run | mitigate | test_cr02_es_restore.py asserts device/dtype consistency CPU + MPS-skipif |
| T-13-SC | Tampering | npm/pip/cargo installs | accept | RESEARCH Package Legitimacy Audit: zero installs this phase (all deps resident in ./qgan_env, used by Phases 8-12) — N/A |
</threat_model>

<verification>
- `./qgan_env/bin/python -m pytest tests/ -q` exits 0 (all 4 test files green)
- `quantum.py` default path byte-unchanged (count_params==75 + fixed-seed forward reference assertion in test_ansatz_variants.py)
- No `from scipy.signal import welch` remains in training.py; `torch.fft.rfft` present in _spectral_psd_loss
- `map_location` present in EarlyStopping._load_checkpoint
- All four `core/` default-behavior guards (topology="range", spectral guard, early_stopper=None) preserved
</verification>

<success_criteria>
- ARCH-01 partial: V1/V2/V3 constructible via num_layers+topology; default byte-identical (param + forward reference)
- INTRO-03 partial: introspect() returns bounded VN-entropy + purity on 2|3 bipartition
- CR-01 + CR-02 fixed with CONTEXT-mandated regression tests; defaults byte-unchanged
- tests/ greenfield package created and green
</success_criteria>

<output>
Create `.planning/phases/13-architecture-introspection/13-01-SUMMARY.md` when done
</output>
