---
phase: 13-architecture-introspection
plan: 04
type: execute
wave: 3
depends_on: ["13-03"]
files_modified:
  - run_introspect_figures.py
  - results/figures/training_progression.png
  - results/figures/training_progression.pdf
  - results/figures/param_trajectory.png
  - results/figures/param_trajectory.pdf
  - results/figures/entanglement_trajectory.png
  - results/figures/entanglement_trajectory.pdf
autonomous: true
requirements: [INTRO-01, INTRO-02, INTRO-03]
must_haves:
  truths:
    - "training_progression figure shows the generated distribution at epochs {0,250,500,750,1000} for the quantum generator AND all three classical WGAN-GP variants side-by-side"
    - "param_trajectory figure shows PQC param norms across epochs AND angle histograms across epochs"
    - "entanglement_trajectory figure shows Von Neumann entanglement entropy AND state purity across training"
    - "Every figure is rendered purely from the plan-03 companion JSON (no re-training) and exists as both .png and .pdf"
    - "Figures load only the companion JSON — no training/sampling code in the figure script"
  artifacts:
    - path: "run_introspect_figures.py"
      provides: "matplotlib renderer: reads the 3 companion JSON, writes 6 figure files"
    - path: "results/figures/training_progression.png"
      provides: "INTRO-01 figure (quantum + 3 classical side-by-side)"
    - path: "results/figures/param_trajectory.png"
      provides: "INTRO-02 figure (norms + angle histograms)"
    - path: "results/figures/entanglement_trajectory.png"
      provides: "INTRO-03 figure (vn_entropy + purity)"
  key_links:
    - from: "run_introspect_figures.py"
      to: "results/figures/training_progression.json"
      via: "json.load — render-only, no training"
      pattern: "training_progression\\.json"
    - from: "run_introspect_figures.py"
      to: "matplotlib"
      via: "savefig .png + .pdf"
      pattern: "savefig"
---

<objective>
Render the three INTRO figures from the plan-03 reproducibility JSON
(ROADMAP criterion 4: "saved as figure artifacts — each with underlying data
in JSON"):

1. `training_progression.{png,pdf}` (INTRO-01) — generated distribution at
   {0,250,500,750,1000} for the quantum generator AND `wgan_mlp`/`wgan_cnn`/
   `wgan_lstm` side-by-side (D-13-08).
2. `param_trajectory.{png,pdf}` (INTRO-02) — PQC param-norm curve + angle
   histograms across the snapshot epochs.
3. `entanglement_trajectory.{png,pdf}` (INTRO-03) — Von Neumann entanglement
   entropy + state purity Tr(ρ²) across training, annotated with the
   {0,1}|{2,3,4} bipartition (D-13-09).

Purpose: completes the R2-6 "black-box feel" rebuttal as paper-ready figures
Phase 14 consumes. There is NO figure-rendering code anywhere in the repo
(`run_ablation.py:17` "NO matplotlib import") — panel layout/format/styling are
Claude's discretion per the D-13 discretion list; the only hard contract is
that each figure renders solely from the companion JSON and exists as png+pdf.
Output: `run_introspect_figures.py` + 6 figure files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-architecture-introspection/13-CONTEXT.md
@.planning/phases/13-architecture-introspection/13-RESEARCH.md
@.planning/phases/13-architecture-introspection/13-PATTERNS.md
@.planning/phases/13-architecture-introspection/13-03-SUMMARY.md

<interfaces>
<!-- The plan-03 companion JSON contract this script consumes. -->

results/figures/training_progression.json — INTRO-01: per-target
{target, epochs[5], samples[5][N]} for "quantum","wgan_mlp","wgan_cnn",
"wgan_lstm"; metadata pipeline="B", seed=42, snapshot_epochs
[0,250,500,750,1000].
results/figures/param_trajectory.json — INTRO-02 (quantum only):
epochs[5], param_norm[5], param_angles[5][75]; metadata variant="V1",
depth=4, topology="range".
results/figures/entanglement_trajectory.json — INTRO-03 (quantum
only): epochs[5], vn_entropy[5], purity[5]; metadata bipartition
"{0,1}|{2,3,4}", measure string.

run_dualscale_fidelity.py:355-376 — argparse + write_text(json.dumps)
project idiom (the JSON-emit half; this script is the matplotlib half which has
NO in-repo analog — use standard matplotlib idioms + RESEARCH §"Recommended
Project Structure").

NOTE: matplotlib is project-installed (used by prior figure-producing phases
e.g. transform_ablation/figures); import as `import matplotlib; matplotlib.use
("Agg")` before `import matplotlib.pyplot as plt` so it renders headless.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: run_introspect_figures.py — render 3 figures (png+pdf) from companion JSON</name>
  <files>run_introspect_figures.py, results/figures/training_progression.png, results/figures/training_progression.pdf, results/figures/param_trajectory.png, results/figures/param_trajectory.pdf, results/figures/entanglement_trajectory.png, results/figures/entanglement_trajectory.pdf</files>
  <read_first>
    - results/figures/training_progression.json (POST plan-03 — exact key shape to plot)
    - results/figures/param_trajectory.json (POST plan-03)
    - results/figures/entanglement_trajectory.json (POST plan-03)
    - run_dualscale_fidelity.py (lines 306-376 — argparse + main() project idiom)
    - .planning/phases/13-architecture-introspection/13-RESEARCH.md (Recommended Project Structure — figure file names; D-13 figure discretion)
    - .planning/phases/13-architecture-introspection/13-PATTERNS.md (run_introspect_figures.py assignment lines 187-191; no-analog figure note lines 254-258)
  </read_first>
  <action>
    Write `run_introspect_figures.py`. `import matplotlib;
    matplotlib.use("Agg")` THEN `import matplotlib.pyplot as plt`. argparse
    `--figures-dir` default `results/figures`. Load the three
    companion JSON files (FAIL with a clear error if any is missing — this
    script is render-only, never trains/samples).
    Figure 1 training_progression: a grid with one row per target
    (quantum, wgan_mlp, wgan_cnn, wgan_lstm) × one column per snapshot epoch
    {0,250,500,750,1000}, each cell a histogram/KDE of that target's generated
    samples at that epoch — quantum and the 3 classical visually side-by-side
    (D-13-08). Layout/styling Claude's discretion.
    Figure 2 param_trajectory: two panels — (a) param L2-norm vs epoch line;
    (b) angle histograms (one per snapshot epoch, overlaid or small-multiples)
    from param_angles. Annotate variant="V1" depth=4 topology="range".
    Figure 3 entanglement_trajectory: vn_entropy vs epoch and purity vs epoch
    (twin-axis or two panels); annotate the bipartition string from the JSON
    metadata verbatim (D-13-09) and the [0,ln4]/[0.25,1] reference bounds.
    Save each figure as BOTH `<name>.png` and `<name>.pdf` via plt.savefig
    (bbox_inches="tight"). Close figures (plt.close) to keep memory bounded.
    Print the 6 written paths.
  </action>
  <verify>
    <automated>cd /Users/shawngibford/dev/phd/qGAN && ./qgan_env/bin/python -m revision.run_introspect_figures && for f in training_progression param_trajectory entanglement_trajectory; do test -s results/figures/$f.png || { echo "MISSING $f.png"; exit 1; }; test -s results/figures/$f.pdf || { echo "MISSING $f.pdf"; exit 1; }; done && echo FIGURES_OK</automated>
  </verify>
  <acceptance_criteria>
    - `run_introspect_figures.py` exists; contains `matplotlib.use("Agg")` and `savefig`; contains no `train_wgan_gp` / `QuantumGenerator(` / sampling call (render-only)
    - All 6 files exist and are non-empty: training_progression.{png,pdf}, param_trajectory.{png,pdf}, entanglement_trajectory.{png,pdf}
    - Script reads `training_progression.json`, `param_trajectory.json`, `entanglement_trajectory.json` (grep shows the three json.load targets)
    - Re-running the script is idempotent (overwrites the 6 files, exits 0) and requires no training
    - training_progression renders all 4 targets (quantum + 3 classical); entanglement figure annotates the bipartition string from JSON metadata
  </acceptance_criteria>
  <done>3 INTRO figures rendered as png+pdf purely from companion JSON; script is render-only and idempotent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| companion JSON → figure | the figure must faithfully render the recorded data; a render-time recompute or silent fallback would decouple the figure from its reproducibility JSON (ROADMAP criterion 4 violation) |

## STRIDE Threat Register

(Local-Mac scientific compute: no network/auth/PII. Research-integrity scope.)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-12 | Tampering | figure script re-trains or re-samples instead of reading the JSON → figure no longer traceable to its reproducibility data | mitigate | acceptance asserts NO train_wgan_gp/QuantumGenerator/sampling call in the script; render-only; fails loudly if a JSON is missing |
| T-13-13 | Tampering | a missing companion JSON silently produces an empty/partial figure | mitigate | script raises a clear error when any of the 3 JSON files is absent; acceptance requires all 4 targets rendered |
| T-13-SC | Tampering | npm/pip/cargo installs | accept | RESEARCH Package Legitimacy Audit: zero installs (matplotlib already project-installed) — N/A |
</threat_model>

<verification>
- 6 figure files exist and non-empty (png+pdf × 3)
- run_introspect_figures.py is render-only (no training/sampling code)
- training_progression renders quantum + 3 classical side-by-side (D-13-08)
- entanglement figure annotates the {0,1}|{2,3,4} bipartition (D-13-09)
- Script idempotent, fails loudly on missing JSON
</verification>

<success_criteria>
- INTRO-01: training_progression.{png,pdf} — quantum + 3 classical generated distributions at {0,250,500,750,1000} side-by-side
- INTRO-02: param_trajectory.{png,pdf} — norms + angle histograms across epochs
- INTRO-03: entanglement_trajectory.{png,pdf} — vn_entropy + purity across training, bipartition annotated
- Each figure backed by its plan-03 companion JSON (ROADMAP criterion 4)
</success_criteria>

<output>
Create `.planning/phases/13-architecture-introspection/13-04-SUMMARY.md` when done
</output>
