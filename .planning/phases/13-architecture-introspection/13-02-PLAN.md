---
phase: 13-architecture-introspection
plan: 02
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - run_ansatz.py
  - run_ansatz_sweep.sh
  - run_ansatz_comparison.py
  - results/ansatz_comparison.json
  - tests/test_ansatz_json_schema.py
autonomous: true
requirements: [ARCH-01, ARCH-02]
must_haves:
  truths:
    - "run_ansatz.py trains one (variant,seed) per process with an idempotent 5-file bundle under runs/<variant>/<seed>/"
    - "run_ansatz_sweep.sh resolves a 10-run matrix (V2+V3 × seeds {42,43,44,45,46}); V1 is NOT in the matrix"
    - "Sweep uses xargs -P 2 only — zero multiprocessing.Pool anywhere"
    - "ansatz_comparison.json validates against the extended long-form rows[]+models[] schema with ansatz/depth/topology dims"
    - "V1 rows are read from existing transform_ablation/runs/B/{42..46} final metrics (NO recompute); data equivalence note recorded"
  artifacts:
    - path: "run_ansatz.py"
      provides: "single (variant,seed) quantum training driver, 1000 epochs, early-stop OFF, spectral weight 0.0"
    - path: "run_ansatz_sweep.sh"
      provides: "idempotent resumable 10-run sweep, xargs -P 2, atomic sweep_status.json"
    - path: "run_ansatz_comparison.py"
      provides: "aggregator → ansatz_comparison.json on extended long-form schema"
    - path: "results/ansatz_comparison.json"
      provides: "ARCH-02 comparison table (V1 reused + V2/V3 new, dual-scale)"
    - path: "tests/test_ansatz_json_schema.py"
      provides: "ARCH-02 schema regression"
  key_links:
    - from: "run_ansatz.py"
      to: "revision.core.models.quantum.QuantumGenerator"
      via: "QuantumGenerator(num_layers=depth, topology=topology)"
      pattern: "QuantumGenerator\\(.*topology"
    - from: "run_ansatz_comparison.py"
      to: "revision.core.eval.full_metric_suite"
      via: "full_metric_suite UNCHANGED (D-10-20)"
      pattern: "full_metric_suite"
    - from: "run_ansatz_comparison.py"
      to: "results/transform_ablation/runs/B/{42..46}"
      via: "V1 reuse, NO recompute (D-13-01, D-10-15)"
      pattern: "transform_ablation/runs/B"
---

<objective>
Deliver the ARCH-01/02 quantum-vs-quantum ansatz comparison:

1. `run_ansatz.py` — a clean clone of `run_baselines.py`'s WGAN branch that
   trains one `(variant, seed)` with `QuantumGenerator(num_layers=depth,
   topology=topology)`, 1000 epochs, `early_stopper=None` (D-13-05),
   `spectral_loss_weight=0.0` (D-13-06), into an idempotent run-dir bundle.
2. `run_ansatz_sweep.sh` — clone of `run_baselines_sweep.sh` driving the 10-run
   matrix (V2 depth-8 + V3 depth-4-linear, 5 seeds each; V1 NOT in matrix).
3. `run_ansatz_comparison.py` — emits `ansatz_comparison.json` on the extended
   long-form schema; V1 rows reuse existing 09.1/10 Pipeline-B final metrics
   (no recompute, D-13-01); V2/V3 from the new runs.
4. Execute the sweep and produce the JSON.

Purpose: ARCH-02 "ansatz comparison table, identical training budget,
multi-seed, full metric suite" — the R2-5b "why this circuit?" rebuttal.
Output: 10 new quantum run-dirs + `results/ansatz_comparison.json`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-architecture-introspection/13-CONTEXT.md
@.planning/phases/13-architecture-introspection/13-RESEARCH.md
@.planning/phases/13-architecture-introspection/13-PATTERNS.md
@.planning/phases/13-architecture-introspection/13-01-SUMMARY.md

<interfaces>
<!-- Extracted from the codebase. Use these directly. -->

run_baselines.py — the structural template. Key pieces:
- imports/constants from revision.core (BATCH_SIZE, EVAL_EVERY, LAMBDA,
  LR_CRITIC, LR_GENERATOR, N_CRITIC, NOISE_HIGH, NOISE_LOW, NUM_LAYERS,
  NUM_QUBITS, WINDOW_LENGTH)
- DatasetBundle (lines 101-114) + build_dataset_for_pipeline Pipeline-B branch
  (lines 140-171, inverse_kwargs at 152)
- generate_wgan_samples (177-208) — moves generator to CPU + *0.1 (Pitfall 6)
- _save_inverse_kwargs (211-223), _compute_data_hash (226-234)
- _train_wgan (237-282): builds Critic, calls train_wgan_gp(... num_epochs,
  n_critic=N_CRITIC, lambda_gp=LAMBDA, lr_critic=LR_CRITIC,
  lr_generator=LR_GENERATOR, seed, eval_every=EVAL_EVERY) — NO early_stopper
- main() (430+): argparse choices guard, run-dir = out_root/runs/<model>/
  <pipeline>/<seed> (lines 456-463 clean), config.yaml+5-file persist (490-522)

run_baselines_sweep.sh — the sweep template. MODELS/PIPELINES/SEEDS/
EPOCHS constants lines 86-89; --parallel 1|2 guard 146-156 (reject ≥3);
is_complete() 174-184; update_status() atomic tmpfile+os.rename under flock -x 9
199-282; run_one() 290-335; xargs -P 2 -L 1 dispatch; final non-zero-if-
incomplete exit. Header lines 34-43 document the no-Pool rule — PRESERVE.

run_dualscale_fidelity.py — schema clone source. Envelope lines
355-380 ({"schema","model_kinds","pipelines","seeds","data_hash",
"metric_helpers","rows"}); MODEL_KINDS line 102; quantum reuse run-dir path
lines 185-186: rel = Path(f"results/transform_ablation/runs/
{pipeline}/{seed}"); data_hash verification pattern lines 241-281.

core/eval.py — full_metric_suite(real, fake) at line 143 → dict of
emd/moments/acf/dtw/jsd/psd metrics. Reuse UNCHANGED (D-10-20).

Existing V1 quantum 5-seed runs (D-13-01 reuse, NO recompute):
results/transform_ablation/runs/B/{42,43,44,45,46}/ each containing
config.yaml, checkpoint.pt, samples.npy, metrics.json, inverse_kwargs.npz.
Pipeline B is the headline pipeline. These are V1 (depth-4, range, 75 params).
NOTE (from run_dualscale_fidelity.py:30-44): 09.1 quantum configs carry NO
data_hash field — equivalence is by-construction (identical
load_and_preprocess) — record that note verbatim-in-spirit in the JSON.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: run_ansatz.py single-run driver + run_ansatz_sweep.sh 10-run sweep</name>
  <files>run_ansatz.py, run_ansatz_sweep.sh</files>
  <read_first>
    - run_baselines.py (FULL — structural clone source; copy DatasetBundle, build_dataset_for_pipeline Pipeline-B branch, generate_wgan_samples verbatim incl. .to("cpu")+*0.1, _save_inverse_kwargs, _compute_data_hash, _train_wgan shape, main() run-dir clean + config.yaml + 5-file persist)
    - run_baselines_sweep.sh (FULL — clone; copy set -euo pipefail, PYTHON resolution, --parallel guard, is_complete, update_status flock/tmpfile/os.rename, run_one, xargs -P 2 -L 1 dispatch, final exit; PRESERVE the no-Pool header comment lines 34-43)
    - core/models/quantum.py (POST plan-01 state — QuantumGenerator(num_layers=, topology=) signature)
    - .planning/phases/13-architecture-introspection/13-RESEARCH.md (Pattern 4 driver/sweep clone; Pitfall 5 no-Pool; Pitfall 6 CPU sample gen; Open Q1 emit both scales — carry inverse_kwargs.npz for Pipeline B)
    - .planning/phases/13-architecture-introspection/13-PATTERNS.md (run_ansatz.py + run_ansatz_sweep.sh assignment blocks, lines 113-153)
  </read_first>
  <action>
    Clone `run_baselines.py` → `run_ansatz.py`. argparse: `--variant`
    choices=["V2","V3"] (V1 NOT a sweep target — reused), `--seed`,
    `--epochs` default 1000, `--out-root` default `results/ansatz`,
    `--csv-path`. Map variant→(num_layers,topology): V2=(8,"range"),
    V3=(4,"linear"). Build `QuantumGenerator(num_layers=num_layers,
    topology=topology)` (window_length=2*NUM_QUBITS default). Reuse the
    Pipeline-B dataset branch verbatim (headline pipeline) and carry
    `inverse_kwargs.npz` so OD-scale rows are possible (RESEARCH Open Q1 — emit
    both scales). Call `train_wgan_gp(..., num_epochs=1000, n_critic=N_CRITIC,
    lambda_gp=LAMBDA, lr_critic=LR_CRITIC, lr_generator=LR_GENERATOR,
    seed=args.seed, eval_every=EVAL_EVERY)` with NO early_stopper and
    spectral_loss_weight default 0.0. Sample generation copies
    generate_wgan_samples verbatim (CPU + *0.1). Run-dir = `<out_root>/runs/
    <variant>/<seed>/`; persist config.yaml (with `data_hash` per D-10-15 +
    `ansatz`/`depth`/`topology`/`parameter_count` keys), checkpoint.pt,
    samples.npy, metrics.json, inverse_kwargs.npz; idempotent shutil.rmtree on
    rerun.
    Clone `run_baselines_sweep.sh` → `run_ansatz_sweep.sh`. Change ONLY: matrix
    constants → `VARIANTS="V2 V3"`, `SEEDS="42 43 44 45 46"`, `EPOCHS=1000`,
    total_count=10; is_complete() bundle → config.yaml/checkpoint.pt/
    samples.npy/metrics.json/inverse_kwargs.npz under runs/<variant>/<seed>/;
    2-tuple key (variant,seed); run_one python invocation → `-m
    revision.run_ansatz --variant "$v" --seed "$s"`. Keep --parallel 1|2 guard
    (reject ≥3), atomic flock status, xargs -P 2 -L 1, no-Pool header comment.
  </action>
  <verify>
    <automated>cd /Users/shawngibford/dev/phd/qGAN && ./qgan_env/bin/python -c "import ast; ast.parse(open('run_ansatz.py').read())" && bash -n run_ansatz_sweep.sh && ./run_ansatz_sweep.sh --dry-run 2>&1 | grep -Eq 'V2|V3' && echo DRYRUN_OK</automated>
  </verify>
  <acceptance_criteria>
    - `run_ansatz.py` parses (ast.parse) and argparse `choices=["V2","V3"]` present (V1 excluded)
    - `bash -n run_ansatz_sweep.sh` exits 0
    - `grep -v '^#' run_ansatz_sweep.sh | grep -c 'multiprocessing.Pool'` == 0 and same for run_ansatz.py (preserved ban header comment is acceptable)
    - `./run_ansatz_sweep.sh --dry-run` lists a 10-run matrix over V2/V3 × {42,43,44,45,46}; V1 absent
    - `run_ansatz.py` source contains `QuantumGenerator(` with `topology=`, `train_wgan_gp(` with `num_epochs=1000`, and NO `early_stopper=`
  </acceptance_criteria>
  <done>Driver + sweep clone the Phase 10 template; dry-run resolves the 10-run V2/V3 matrix; no Pool; idempotent bundle with data_hash.</done>
</task>

<task type="auto">
  <name>Task 2: Execute the 10-run sweep, then build ansatz_comparison.json + schema test</name>
  <files>run_ansatz_comparison.py, results/ansatz_comparison.json, tests/test_ansatz_json_schema.py, results/ansatz/runs/</files>
  <read_first>
    - run_dualscale_fidelity.py (FULL — envelope lines 355-380, quantum-reuse run-dir resolution 180-186, data_hash verification 241-281, dual-scale row builder; clone the rows[]+models[] shape)
    - core/eval.py (full_metric_suite signature line 143 — reuse UNCHANGED)
    - run_ansatz.py (POST task-1 — run-dir layout + config.yaml keys this aggregator reads)
    - results/transform_ablation/runs/B/42/config.yaml + results/transform_ablation/runs/B/42/metrics.json (the V1 reuse source — confirm metric keys + that no data_hash field exists; record the by-construction equivalence note)
    - .planning/phases/13-architecture-introspection/13-PATTERNS.md (ansatz_comparison.json schema-clone block, lines 195-213)
  </read_first>
  <action>
    Run the sweep: `./run_ansatz_sweep.sh --parallel 2` (10 runs,
    V2+V3 × 5 seeds, 1000 epochs each — local Mac statevector; resumable so a
    stall is recoverable). Wait until `results/ansatz/sweep_status.json`
    shows `all_complete: true`.
    Write `run_ansatz_comparison.py`: emit
    `results/ansatz_comparison.json` cloning the
    `run_dualscale_fidelity.py` envelope — keys `schema` ("long-form rows[] +
    models[] (D-10-16) + ansatz/depth/topology (Phase 13)"), `model_kinds`
    ["quantum"], `ansatz_variants` [{variant:V1,depth:4,topology:range,
    parameter_count:75,source:"reused 09.1/10 5-seed final metrics (D-13-01, no
    recompute)"},{V2,8,range,135,"Phase 13 new runs"},{V3,4,linear,75,"Phase 13
    new runs"}], `seeds` [42,43,44,45,46], `rows`. V1 rows: load samples.npy
    from `results/transform_ablation/runs/B/{42..46}/` and compute
    `full_metric_suite` UNCHANGED on those existing samples (NO new training);
    record the by-construction data-equivalence note (09.1 quantum configs carry
    no data_hash field) following run_dualscale_fidelity.py:241-281. V2/V3 rows:
    from the 10 new run-dirs under `results/ansatz/runs/<variant>/
    <seed>/`. Each row = {model_kind:"quantum", variant, depth, topology,
    pipeline:"B", seed, metric_name, scale, value}; emit both scales
    (log_return + OD via inverse_kwargs) mirroring run_dualscale_fidelity.py.
    Write tests/test_ansatz_json_schema.py asserting: top-level keys present;
    ansatz_variants has V1/V2/V3 with depth/topology/parameter_count =
    4/range/75, 8/range/135, 4/linear/75; rows non-empty; each row has the 9
    required fields; variant set == {V1,V2,V3}; scale set ⊆ {log_return,OD}.
  </action>
  <verify>
    <automated>cd /Users/shawngibford/dev/phd/qGAN && ./qgan_env/bin/python -m pytest tests/test_ansatz_json_schema.py -x -q && ./qgan_env/bin/python -c "import json;d=json.load(open('results/ansatz_comparison.json'));vs={v['variant'] for v in d['ansatz_variants']};assert vs=={'V1','V2','V3'},vs;assert d['rows'];print('OK',len(d['rows']),'rows')"</automated>
  </verify>
  <acceptance_criteria>
    - `results/ansatz/sweep_status.json` has `all_complete` == true with 10 completed runs
    - `results/ansatz_comparison.json` exists; `ansatz_variants` contains V1(4/range/75), V2(8/range/135), V3(4/linear/75)
    - `rows` is non-empty; every row has model_kind/variant/depth/topology/pipeline/seed/metric_name/scale/value
    - V1 source string contains "no recompute" / "D-13-01"; V1 rows derive from transform_ablation/runs/B (no new V1 training dir created)
    - `pytest tests/test_ansatz_json_schema.py -x -q` exits 0
    - Metrics computed via `revision.core.eval.full_metric_suite` (grep shows the call; eval.py unmodified)
  </acceptance_criteria>
  <done>10-run sweep complete; ansatz_comparison.json on the extended long-form schema with V1 reused + V2/V3 new, dual-scale; schema test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reused V1 artifacts → ansatz table | existing transform_ablation samples cross into the new comparison; mis-attribution corrupts the headline circuit-justification result |
| sweep worker processes → atomic sweep_status.json | concurrent xargs workers write shared status; a torn write loses run-completion accounting |

## STRIDE Threat Register

(Local-Mac scientific compute: no network/auth/PII. Research-integrity scope.)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-04 | Tampering | V1 reuse silently recomputes or pulls wrong run-dir → comparison no longer apples-to-apples | mitigate | aggregator reads transform_ablation/runs/B/{42..46} explicitly; records by-construction data-equivalence note; test asserts V1 source string; no V1 training dir created |
| T-13-05 | Tampering | multiprocessing.Pool reintroduced → fork-shared numpy RNG corrupts per-seed reproducibility | mitigate | xargs -P 2 -L 1 only; acceptance grep asserts zero non-comment Pool uses; no-Pool header preserved |
| T-13-06 | Tampering | torn sweep_status.json under parallel writers → false all_complete | mitigate | clone the proven flock -x 9 + tmpfile + os.rename atomic update from run_baselines_sweep.sh verbatim |
| T-13-07 | Tampering | mixed-budget contamination (a stale <1000-epoch dir counts as complete) | mitigate | is_complete() checks the full 5-file bundle; idempotent shutil.rmtree on rerun |
| T-13-SC | Tampering | npm/pip/cargo installs | accept | RESEARCH Package Legitimacy Audit: zero installs (deps resident in ./qgan_env) — N/A |
</threat_model>

<verification>
- `results/ansatz/sweep_status.json` → all_complete true, 10 runs
- `ansatz_comparison.json` validates against extended long-form schema (test green)
- V1 reused (no recompute); V2/V3 from 10 new runs; both scales emitted
- No non-comment multiprocessing.Pool in driver or sweep
- full_metric_suite + eval.py unchanged (D-10-20)
</verification>

<success_criteria>
- ARCH-01: V2/V3 trained 5 seeds each via the topology-selectable QuantumGenerator
- ARCH-02: ansatz_comparison.json — identical training budget (1000 epochs, early-stop OFF), multi-seed, full metric suite, dual-scale; V1 reused per D-13-01
</success_criteria>

<output>
Create `.planning/phases/13-architecture-introspection/13-02-SUMMARY.md` when done
</output>
