---
phase: 13-architecture-introspection
plan: 03
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - run_introspect.py
  - results/figures/training_progression.json
  - results/figures/param_trajectory.json
  - results/figures/entanglement_trajectory.json
  - tests/test_introspect_callback.py
autonomous: true
requirements: [INTRO-01, INTRO-02, INTRO-03]
must_haves:
  truths:
    - "snapshot_cb fires at epoch indices {0,250,500,750,999} and records exactly 5 snapshots with 999 relabelled to 1000"
    - "Each quantum snapshot captures generated-distribution samples (INTRO-01), params_pqc norm + angle histogram (INTRO-02), vn_entropy + purity (INTRO-03)"
    - "One instrumented V1 quantum run (depth-4 range, seed 42) + three instrumented classical WGAN runs (wgan_mlp/cnn/lstm, Pipeline B, seed 42) all snapshot the same 5 epochs"
    - "Companion JSON records the 2|3 bipartition {0,1}|{2,3,4} in metadata (D-13-09)"
    - "Classical generators (no introspect()) are hasattr-guarded — they capture samples without crashing and emit no PQC param/entropy fields"
  artifacts:
    - path: "run_introspect.py"
      provides: "instrumented training driver: callback-snapshot V1 quantum + 3 classical, single seed 42"
    - path: "results/figures/training_progression.json"
      provides: "INTRO-01 reproducibility JSON — quantum + 3 classical generated-distribution snapshots"
    - path: "results/figures/param_trajectory.json"
      provides: "INTRO-02 reproducibility JSON — PQC param norms + angle histograms"
    - path: "results/figures/entanglement_trajectory.json"
      provides: "INTRO-03 reproducibility JSON — vn_entropy + purity trajectory + bipartition metadata"
    - path: "tests/test_introspect_callback.py"
      provides: "INTRO-01/02/03 callback-wiring regression (short run)"
  key_links:
    - from: "run_introspect.py::snapshot_cb"
      to: "train_wgan_gp callback hook"
      via: "callback= kwarg; SNAP={0,250,500,750,999}"
      pattern: "callback\\s*="
    - from: "run_introspect.py::snapshot_cb"
      to: "generator.introspect"
      via: "hasattr-guarded introspect(noise[:,0]) for quantum only"
      pattern: "hasattr\\(.*introspect"
    - from: "run_introspect.py"
      to: "training noise contract"
      via: "rng.uniform(NOISE_LOW,NOISE_HIGH,(NUM_QUBITS,BATCH_SIZE)) float32 .to(float64)*0.1 on CPU"
      pattern: "NOISE_LOW.*NOISE_HIGH"
---

<objective>
Produce the INTRO-01/02/03 instrumented training runs and their reproducibility
JSON (the R2-6 "what is it learning?" rebuttal):

1. `run_introspect.py` — wires a `callback=` snapshot closure into
   `train_wgan_gp` (the existing dormant hook — NO training-loop surgery) and
   runs FOUR instrumented 1000-epoch single-seed (42) trainings: V1 quantum
   (`QuantumGenerator(num_layers=4, topology="range")`) plus classical
   `wgan_mlp`, `wgan_cnn`, `wgan_lstm` (Pipeline B).
2. Each snapshot at epoch indices {0,250,500,750,999} (999 → label 1000)
   captures generated samples (INTRO-01), `params_pqc` norm + angles
   (INTRO-02, quantum only), `introspect()` vn_entropy + purity (INTRO-03,
   quantum only).
3. Emit the three companion reproducibility JSON files (ROADMAP criterion 4)
   that plan 04 renders into figures.

Purpose: INTRO-01 needs quantum + 3 classical side-by-side on Pipeline B
(D-13-08); INTRO-02/03 profile the production V1 circuit (D-13-07).
Output: 4 instrumented runs + 3 figure-companion JSON files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-architecture-introspection/13-CONTEXT.md
@.planning/phases/13-architecture-introspection/13-RESEARCH.md
@.planning/phases/13-architecture-introspection/13-PATTERNS.md
@.planning/phases/13-architecture-introspection/13-01-SUMMARY.md

<interfaces>
<!-- Extracted from the codebase. Use these directly. -->

core/training.py — train_wgan_gp(generator, critic, dataloader, *,
num_epochs=2000, n_critic=9, lambda_gp=2.16, lr_critic=1.8046e-05,
lr_generator=6.9173e-05, seed=42, spectral_loss_weight=0.0, eval_every=10,
early_stopper=None, callback=None). The callback hook fires at lines 395-411
INSIDE the eval branch (epoch % eval_every == 0 or epoch+1 == num_epochs),
try/except-wrapped, passing ONLY a scalar dict:
{epoch, emd, critic_loss, generator_loss, mean, std, kurtosis}. It does NOT
pass the generator or samples — the closure MUST close over `generator` and
re-generate. With eval_every=10 and num_epochs=1000, epochs {0,250,500,750}
satisfy epoch%10==0 and index 999 satisfies epoch+1==num_epochs.

Training noise contract (training.py:304-313 / run_baselines.py:198-207) —
LOAD-BEARING, copy exactly: move generator to CPU (MPS has no float64,
Pitfall 6); noise = torch.tensor(rng.uniform(NOISE_LOW, NOISE_HIGH,
size=(NUM_QUBITS, BATCH_SIZE)), dtype=torch.float32); out =
generator(noise).to(torch.float64) * 0.1 (the *0.1 is the quantum-output
artifact, applied for classical too for consistency with run_baselines).

run_baselines.py — _train_wgan (237-282) for train wiring shape;
build_dataset_for_pipeline Pipeline-B branch (140-171);
generate_wgan_samples (177-208) CPU+*0.1; _WGAN_GENERATORS map (93-97) →
WGANMLPGenerator/WGANCNNGenerator/WGANLSTMGenerator from
revision.core.models.classical (79-83); Critic from
revision.core.models.critic.

core/models/quantum.py (POST plan-01) — QuantumGenerator(num_layers=4,
topology="range") = V1; .introspect(noise_vec) → (float entropy, float purity)
on the {0,1}|{2,3,4} bipartition; .params_pqc nn.Parameter (count_params()==75).

run_dualscale_fidelity.py:355-376 — out_path.write_text(json.dumps(obj,
indent=2)) emit pattern for companion JSON.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: run_introspect.py — snapshot callback closure + 4-target run wiring</name>
  <files>run_introspect.py, tests/test_introspect_callback.py</files>
  <read_first>
    - run_baselines.py (FULL — _train_wgan wiring, build_dataset_for_pipeline Pipeline-B branch, generate_wgan_samples CPU+*0.1, _WGAN_GENERATORS map, classical imports)
    - core/training.py (callback hook 395-411, noise contract 304-313, train_wgan_gp signature 178-208)
    - core/models/quantum.py (POST plan-01 — introspect() + topology="range" V1)
    - .planning/phases/13-architecture-introspection/13-RESEARCH.md (Pattern 2 callback closure + the gap; Pitfall 2 stale-generator/noise-contract; Pitfall 4 snapshot off-by-one; Pitfall 6 CPU/MPS)
    - .planning/phases/13-architecture-introspection/13-PATTERNS.md (run_introspect.py assignment block lines 157-184; training-noise-contract shared pattern lines 226-235)
  </read_first>
  <behavior>
    - snapshot_cb(epoch, metrics): returns immediately unless epoch in
      {0,250,500,750,999}; appends one record per matching epoch; relabels
      999 → 1000 in the stored "epoch" field
    - A short run (num_epochs=30, eval_every=10, SNAP rescaled to {0,10,20,29})
      produces exactly 4 snapshots; each record has keys epoch, samples
      (non-empty list), emd, std
    - QuantumGenerator record additionally has param_norm (float>0),
      param_angles (list, len == count_params()), vn_entropy (float in
      [0,ln4+eps]), purity (float in [0.25-eps,1+eps])
    - Classical generator (no introspect()): hasattr guard → record has samples
      but no vn_entropy/purity/param fields; no crash
    - Generated-snapshot std is same order of magnitude as metrics["std"] at
      that epoch (noise-contract sanity, Pitfall 2)
  </behavior>
  <action>
    Write `run_introspect.py`. argparse: `--target` choices
    ["quantum","wgan_mlp","wgan_cnn","wgan_lstm"], `--seed` default 42,
    `--epochs` default 1000, `--out-dir` default `results/figures`.
    Build the Pipeline-B dataset (headline, D-13-08) via
    build_dataset_for_pipeline. target=="quantum" →
    `QuantumGenerator(num_layers=4, topology="range")` (V1, D-13-07); classical
    → matching `_WGAN_GENERATORS` entry. Build Critic. Snapshot closure:
    `SNAP = {0,250,500,750,999}`; on a matching epoch, under `torch.no_grad()`,
    move generator to CPU, draw noise via `rng.uniform(NOISE_LOW, NOISE_HIGH,
    size=(NUM_QUBITS, BATCH_SIZE))` float32 (exact training noise contract),
    `gen = (generator(noise).to(torch.float64)*0.1).cpu().numpy()`; if
    `hasattr(generator,"introspect")`: `p =
    generator.params_pqc.detach().cpu().numpy()`, `ent,pur =
    generator.introspect(noise[:,0])`, record param_norm /
    param_angles / vn_entropy / purity; else record only samples. Append
    {epoch:(1000 if epoch==999 else epoch), samples, [param_norm,
    param_angles, vn_entropy, purity if quantum], emd, std}. Pass the closure
    as `callback=` to `train_wgan_gp(..., num_epochs=args.epochs,
    eval_every=EVAL_EVERY, early_stopper=None)`. Persist this target's
    snapshots to an idempotent intermediate JSON
    `results/figures/_introspect_<target>.json` (re-runnable). For the
    quantum target also record `{"bipartition":"{0,1}|{2,3,4}"}` metadata
    (D-13-09). Write tests/test_introspect_callback.py exercising the closure on
    a tiny synthetic run (small epochs, rescaled SNAP) for BOTH a
    QuantumGenerator and a classical generator, asserting the behavior block.
    RED first.
  </action>
  <verify>
    <automated>cd /Users/shawngibford/dev/phd/qGAN && ./qgan_env/bin/python -c "import ast; ast.parse(open('run_introspect.py').read())" && ./qgan_env/bin/python -m pytest tests/test_introspect_callback.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `run_introspect.py` parses (ast.parse) and argparse `--target` choices == ["quantum","wgan_mlp","wgan_cnn","wgan_lstm"]
    - source contains `SNAP` with the literal set {0, 250, 500, 750, 999} and a 999→1000 relabel
    - source contains `hasattr(` ... `introspect` guard and `callback=` passed to `train_wgan_gp`
    - source reproduces the noise contract: `rng.uniform(NOISE_LOW, NOISE_HIGH, size=(NUM_QUBITS, BATCH_SIZE))` float32, `.to(torch.float64) * 0.1`, generator `.to("cpu")`
    - `pytest tests/test_introspect_callback.py -x -q` exits 0 (quantum: 5 captured fields incl. vn_entropy/purity; classical: samples only, no crash)
  </acceptance_criteria>
  <done>Callback closure + 4-target driver wired; short-run test green; intermediate per-target JSON idempotent.</done>
</task>

<task type="auto">
  <name>Task 2: Execute the 4 instrumented runs, assemble the 3 companion JSON files</name>
  <files>results/figures/training_progression.json, results/figures/param_trajectory.json, results/figures/entanglement_trajectory.json, run_introspect.py</files>
  <read_first>
    - run_introspect.py (POST task-1 — intermediate _introspect_<target>.json shape this task assembles)
    - run_dualscale_fidelity.py (lines 355-376 — json.dumps(obj, indent=2) write pattern)
    - .planning/phases/13-architecture-introspection/13-CONTEXT.md (D-13-07/08/09 scope; snapshot epochs; bipartition recording requirement)
    - .planning/phases/13-architecture-introspection/13-PATTERNS.md (run_introspect.py persist block lines 183-184; no-analog figure note lines 254-258)
  </read_first>
  <action>
    Run all four instrumented 1000-epoch seed-42 trainings (local Mac
    statevector — the quantum run is the long one; classical are fast,
    D-13-08): `./qgan_env/bin/python -m revision.run_introspect --target
    quantum`, then `--target wgan_mlp`, `--target wgan_cnn`, `--target
    wgan_lstm`. Add an assembler entry-point (subcommand or `--assemble` flag in
    run_introspect.py) that reads the four `_introspect_<target>.json`
    intermediates and writes the three companion JSON files:
    - `training_progression.json` (INTRO-01): per target {target, epochs[],
      samples[][]} for quantum + the three classical — the side-by-side data
      (D-13-08); metadata records pipeline="B", seed=42, snapshot_epochs
      [0,250,500,750,1000].
    - `param_trajectory.json` (INTRO-02): quantum only — epochs[], param_norm[],
      param_angles[][] (the angle-histogram source); metadata variant="V1"
      depth=4 topology="range".
    - `entanglement_trajectory.json` (INTRO-03): quantum only — epochs[],
      vn_entropy[], purity[]; metadata `bipartition` == "{0,1}|{2,3,4}"
      (D-13-09 — verbatim), measure="Von Neumann entanglement entropy + state
      purity Tr(rho^2)".
    Use `out_path.write_text(json.dumps(obj, indent=2))`. Each file is the
    reproducibility companion ROADMAP criterion 4 requires; plan 04 renders
    figures from these (no figure rendering here).
  </action>
  <verify>
    <automated>cd /Users/shawngibford/dev/phd/qGAN && ./qgan_env/bin/python -c "import json; tp=json.load(open('results/figures/training_progression.json')); pt=json.load(open('results/figures/param_trajectory.json')); et=json.load(open('results/figures/entanglement_trajectory.json')); import sys; targs={t['target'] for t in tp.get('targets', tp if isinstance(tp,list) else [])} if False else None; print('files-load OK'); assert 'bipartition' in json.dumps(et) and '{0,1}|{2,3,4}' in json.dumps(et), 'bipartition missing'; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - All four `results/figures/_introspect_<target>.json` intermediates exist with 5 snapshots each (epochs 0,250,500,750,1000)
    - `training_progression.json` contains data for quantum + wgan_mlp + wgan_cnn + wgan_lstm; metadata pipeline=="B", seed==42
    - `param_trajectory.json` has quantum-only param_norm[] (len 5) and param_angles[][] (each len 75)
    - `entanglement_trajectory.json` has vn_entropy[] + purity[] (len 5) and metadata `bipartition` == "{0,1}|{2,3,4}" (D-13-09)
    - vn_entropy values ∈ [0, ln4+eps], purity ∈ [0.25-eps, 1+eps] (INTRO-03 bounds hold on real run data)
    - All three JSON files are valid JSON (json.load succeeds)
  </acceptance_criteria>
  <done>4 instrumented runs complete; 3 reproducibility companion JSON files emitted with correct scope + bipartition metadata.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| metrics dict (scalar) → snapshot closure (re-generates) | closure must reproduce the exact training distribution; a wrong noise contract silently mis-reports "what the model learned" |
| dormant callback hook → instrumentation | the hook is try/except-wrapped — a closure bug is swallowed and silently drops snapshots |

## STRIDE Threat Register

(Local-Mac scientific compute: no network/auth/PII. Research-integrity scope.)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-08 | Tampering | snapshot closure uses wrong noise contract → distribution figure misrepresents the model (R2-6 rebuttal corrupted) | mitigate | copy training.py:304-313 contract exactly; test asserts snapshot std ≈ metrics["std"]; acceptance greps the literal contract |
| T-13-09 | Repudiation/Info | callback try/except swallows a closure exception → fewer than 5 snapshots silently | mitigate | test asserts exact snapshot count on a short run; task-2 acceptance asserts 5 snapshots per intermediate |
| T-13-10 | Tampering | snapshot off-by-one drops the final epoch (index 999) | mitigate | SNAP includes 999 with explicit 999→1000 relabel; acceptance asserts epoch 1000 present |
| T-13-11 | Tampering | bipartition not recorded → INTRO-03 not reproducible (ROADMAP criterion 4) | mitigate | entanglement_trajectory.json metadata must contain "{0,1}|{2,3,4}"; verified in automated check |
| T-13-SC | Tampering | npm/pip/cargo installs | accept | RESEARCH Package Legitimacy Audit: zero installs (deps resident in ./qgan_env) — N/A |
</threat_model>

<verification>
- tests/test_introspect_callback.py green (closure fires correctly, quantum vs classical guarded)
- 4 instrumented runs complete (1 quantum V1 + 3 classical, Pipeline B, seed 42)
- 3 companion JSON files valid, correct scope, snapshot epochs {0,250,500,750,1000}
- entanglement_trajectory.json records bipartition "{0,1}|{2,3,4}" (D-13-09)
- INTRO-03 bounds hold on real run data
</verification>

<success_criteria>
- INTRO-01: training_progression.json — quantum + 3 classical generated-distribution snapshots, Pipeline B, side-by-side (D-13-08)
- INTRO-02: param_trajectory.json — V1 PQC param norms + angle-histogram source across {0,250,500,750,1000}
- INTRO-03: entanglement_trajectory.json — vn_entropy + purity trajectory + bipartition metadata (D-13-09)
</success_criteria>

<output>
Create `.planning/phases/13-architecture-introspection/13-03-SUMMARY.md` when done
</output>
