---
phase: 09.1-r1-m3-preprocessing-ablation
plan: 01
subsystem: preprocessing
tags: [torch, log-returns, lambert-w, min-max, round-trip, preprocessing-ablation, abl-01]

# Dependency graph
requires:
  - phase: 09-documentation-bridge
    provides: "differentiable inverse_lambert_w_transform + verified Phase 9 EVAL-06 full-pipeline round-trip ≤ 4.8e-9 (revision/02_eval06_roundtrip.ipynb template re-used here)"
  - phase: 08-core-module-extraction
    provides: "revision/core/preprocessing.py module with 4 NotImplementedError stubs reserved for Phase 09.1; load_and_preprocess() in revision/core/data.py"
provides:
  - "Pipeline A pure functions: forward_minmax_od / inverse_minmax_od (global min-max → [0,1])"
  - "Pipeline B pure functions: forward_logreturns / inverse_logreturns (standardized log-returns + cumsum-prepend-zero inverse anchored at caller-supplied od_start)"
  - "ABL-01 acceptance gate GREEN: revision/results/transform_ablation/roundtrip_check.json with all_passed=true"
  - "revision/03_ablation_preprocess_check.ipynb — round-trip verification notebook mirroring the Phase 9 EVAL-06 pattern"
affects: [09.1-02-cli-driver-and-smoke-test, 09.1-03-multi-seed-sweep-resumable, 09.1-04-analysis-figures-tstr-and-summary]

# Tech tracking
tech-stack:
  added: []  # No new dependencies — torch.cumsum / torch.log only
  patterns:
    - "Forward/inverse pure-function pair returning (transformed, *aux) with caller-managed aux state (matches v1.1 normalize/denormalize convention at revision/core/data.py:29-42)"
    - "Round-trip verification notebook: imports → repo-root finder → real-data load (.double()) → per-pipeline assert → JSON artifact (matches Phase 9 02_eval06_roundtrip.ipynb cell structure verbatim)"

key-files:
  created:
    - "revision/03_ablation_preprocess_check.ipynb"
    - "revision/results/transform_ablation/roundtrip_check.json"
  modified:
    - "revision/core/preprocessing.py"

key-decisions:
  - "Pipeline B inverse cumsum-prepend-zero pattern: pad zeros to first index along last dim then cumsum so OD_0 is preserved exactly (matches RESEARCH.md Q3 / D-09.1-01 CD-5)"
  - "Pipeline A global min-max over full 778-pt OD (per D-09.1-01 + RESEARCH.md Q4) — single campaign means no inter-trajectory variation to control for"
  - "torch.std default ddof=1 in forward_logreturns to match v1.1 normalize() at data.py:36"
  - "Pipeline C re-export round-trip exercised through revision.core.preprocessing (NOT directly through data.py) to mitigate T-09.1.01-01 (silent re-export breakage)"
  - "Inverse_logreturns od_start broadcasts via unsqueeze(-1) only if log_od_start.dim() < cum_full.dim() — supports both scalar od_start (whole-series Task 2 case) and batched od_start (per-window run_ablation.py case)"

patterns-established:
  - "Pattern A: Pure forward/inverse pairs in revision/core/preprocessing.py — no class state, no global mutation, all auxiliary state returned as extra tuple elements (D-09.1-17)"
  - "Pattern B: ABL-01-class acceptance gates emit machine-readable JSON at revision/results/<bucket>/<gate>.json with pass + per-component error + git_sha + seed (matches Phase 9 EVAL-06 pattern)"

requirements-completed: [ABL-01]

# Metrics
duration: 3m32s
completed: 2026-05-15
---

# Phase 09.1 Plan 01: Preprocessing Pipelines + ABL-01 Round-Trip Summary

**Filled the four Phase 09.1 NotImplementedError stubs in revision/core/preprocessing.py with pure-function Pipeline A (min-max) and Pipeline B (standardized log-returns + cumsum inverse), then proved A/B/C all round-trip on real 778-pt OD via revision/03_ablation_preprocess_check.ipynb. ABL-01 gate GREEN — Wave 2 unblocked.**

## Performance

- **Duration:** 3m32s (212 s)
- **Started:** 2026-05-15T18:56:36Z
- **Completed:** 2026-05-15T19:00:08Z
- **Tasks:** 2 of 2
- **Files modified/created:** 3 (1 modified, 2 created)

## Accomplishments

- Pipeline A and B implemented as pure functions in revision/core/preprocessing.py (no class state, full type hints, docstrings cite CONTEXT.md / RESEARCH.md decisions).
- ABL-01 round-trip gate executed end-to-end via revision/03_ablation_preprocess_check.ipynb (cell outputs committed in-place).
- All three pipelines round-trip on real 778-pt OD well below tolerance:
  - **A** max_abs_err = **0.0** (tol 1e-8) — exact affine inverse on float64-promoted OD.
  - **B** max_abs_err = **4.441e-15** (tol 1e-8) — anchored at `od[0]`; cumsum-prepend-zero pattern is exact up to float-eps.
  - **C** max_abs_err = **4.441e-16** (tol 1e-6) — re-exported Lambert W pair via preprocessing.py (T-09.1.01-01 mitigation).
- revision/results/transform_ablation/roundtrip_check.json written with all_passed=true, seed=42, git_sha=bde33ef9ad0b899303624ed59a96a7c5dd1a2a97.

## Task Commits

Each task was committed atomically (--no-verify, worktree-parallel-safe):

1. **Task 1: Implement Pipeline A and Pipeline B forward/inverse pairs in preprocessing.py** — `bde33ef` (feat)
2. **Task 2: Round-trip verification notebook + roundtrip_check.json (ABL-01 acceptance gate)** — `aaa0fe1` (feat)

_Note: STATE.md / ROADMAP.md updates intentionally skipped — orchestrator owns those writes after Wave 1 completes (worktree-parallel execution mode)._

## Files Created/Modified

- `revision/core/preprocessing.py` *(modified)* — Replaced 4 raise NotImplementedError stubs with the Pipeline A/B implementations from RESEARCH.md Code Examples; Pipeline C re-export block and `__all__` preserved verbatim.
- `revision/03_ablation_preprocess_check.ipynb` *(created)* — 7-cell notebook mirroring revision/02_eval06_roundtrip.ipynb: imports + repo-root finder + SEED=42, load_and_preprocess('./data.csv'), Pipeline A round-trip on od.double(), Pipeline B round-trip anchored at od[0], Pipeline C round-trip via preprocessing.py re-export with _BIG=1e20 clip-disable, emit JSON artifact, final assert.
- `revision/results/transform_ablation/roundtrip_check.json` *(created)* — Machine-readable artifact: per-pipeline max_abs_err + tolerance + pass flags, all_passed=true, seed, git_sha, notes.

## Decisions Made

- **Forward signature for Pipeline B returns Tuple[Tensor, Tensor, Tensor]** (not Tuple[Tensor, Tuple[Tensor, Tensor]]) — flat 3-tuple matches Pipeline A's (scaled, od_min, od_max) signature and the RESEARCH.md "Code Examples" recommendation. Plan stub had Tuple[Tensor, Tensor] return type (likely a docstring transcription drift); flat 3-tuple is the documented contract from RESEARCH lines 350-366 and matches every consumer in Task 2.
- **inverse_logreturns broadcast handling:** unsqueeze(-1) only when log(od_start).dim() < cum_full.dim(). Supports the whole-series ABL-01 round-trip (scalar od_start) AND the future per-window run_ablation.py path (batched od_start with shape `(...,)`).
- **No new dependencies introduced** — all four new functions use torch.log, torch.min/max, torch.mean/std, torch.cumsum, torch.cat, torch.exp from existing torch 2.10.0.

## Deviations from Plan

None — plan executed exactly as written. The Pipeline B forward-function return-type annotation was widened to Tuple[Tensor, Tensor, Tensor] (matching RESEARCH.md Code Examples and the existing stub-body docstring "return (r_norm, mu, sigma)"); this is a faithful read of the planner's specification, not a deviation.

## Issues Encountered

- **First Write went to non-worktree path:** Initial Write to `/Users/shawngibford/dev/phd/qGAN/revision/core/preprocessing.py` (the parent repo) succeeded but the Python import resolved against the worktree copy at `.claude/worktrees/agent-a83da323461406c79/revision/core/preprocessing.py`, so the self-check still raised NotImplementedError. Resolution: re-wrote to the worktree-absolute path. Both files now match because the parent-repo write is irrelevant — only the worktree commits are merged back. No corrective action needed for downstream phases.
- **Pipeline A error is exactly 0.0:** The affine map `scaled * (od_max - od_min) + od_min` is computed in float64 and the inputs are float64-promoted OD; the round-trip is bit-identical (not float-eps-bounded). This is correct and stricter than the documented 1e-8 tolerance.

## User Setup Required

None — no external service configuration, no secrets, no auth. ABL-01 is a pure-numerical gate.

## Next Phase Readiness

- **ABL-01 GREEN — Wave 2 unblocked.** Plan 09.1-02 (cli-driver-and-smoke-test) can now import the four pure functions through `from revision.core.preprocessing import forward_minmax_od, inverse_minmax_od, forward_logreturns, inverse_logreturns, forward_lambert, inverse_lambert`.
- **Re-export path verified** — Phase 9's `lambert_w_transform` / `inverse_lambert_w_transform` are reachable via `revision.core.preprocessing` (T-09.1.01-01 mitigation active on every CI run that executes 03_ablation_preprocess_check.ipynb).
- **No threat flags raised.** Threat surface unchanged from plan's `<threat_model>` — pure-numerical functions on internal tensors, scipy.special.lambertw boundary already mitigated in Phase 9.
- **Known stubs:** None. All four NotImplementedError stubs in revision/core/preprocessing.py are now filled.

## Self-Check: PASSED

Verified at SUMMARY-write time:

- **File checks:**
  - FOUND: revision/core/preprocessing.py
  - FOUND: revision/03_ablation_preprocess_check.ipynb
  - FOUND: revision/results/transform_ablation/roundtrip_check.json
- **Commit checks:**
  - FOUND: bde33ef (Task 1: implement Pipeline A and B forward/inverse pairs)
  - FOUND: aaa0fe1 (Task 2: ABL-01 round-trip notebook + JSON artifact)
- **Gate check:**
  - roundtrip_check.json `all_passed: true`
  - A.pass=true (0.0 ≤ 1e-8), B.pass=true (4.44e-15 ≤ 1e-8), C.pass=true (4.44e-16 ≤ 1e-6)
- **No raise NotImplementedError remaining in preprocessing.py:** `grep -c "raise NotImplementedError" revision/core/preprocessing.py` returns 0.

## TDD Gate Compliance

Plan type is `execute` (not `tdd`); the round-trip notebook is the canonical acceptance gate per D-09.1-03. No TDD gate-sequence enforcement applies. Phase 9 EVAL-06's verified-passing pattern was re-used verbatim, which provides equivalent regression coverage.

---
*Phase: 09.1-r1-m3-preprocessing-ablation*
*Plan: 01-preprocessing-ablation-fns-and-roundtrip*
*Completed: 2026-05-15*
