---
phase: 01-foundation-and-correctness-infrastructure
plan: 03
type: execute
wave: 3
depends_on: [01-02]
files_modified: [qgan_pennylane.ipynb]
autonomous: true
requirements: [QUAL-01, QUAL-02, QUAL-07, QUAL-10]

must_haves:
  truths:
    - "Data loaded from relative path ./data.csv, not an absolute path"
    - "No duplicate imports exist (numpy imported once, random imported once)"
    - "All hyperparameters use UPPER_CASE naming in a single config cell near top of notebook"
    - "Data pipeline variables use stage-based names: raw_data, log_delta, scaled_data, windowed_data"
    - "Notebook is organized with markdown section header cells"
  artifacts:
    - path: "qgan_pennylane.ipynb"
      provides: "Standardized naming conventions, clean imports, organized notebook structure"
  key_links:
    - from: "Config cell (UPPER_CASE hyperparameters)"
      to: "qGAN constructor call"
      via: "NUM_EPOCHS, BATCH_SIZE, N_CRITIC, LAMBDA passed as constructor args"
      pattern: "qGAN\\(NUM_EPOCHS"
    - from: "Config cell (UPPER_CASE hyperparameters)"
      to: "qGAN class internals (self.*)"
      via: "constructor stores as self.num_epochs, self.batch_size, self.n_critic, self.lambda_gp"
      pattern: "self\\.lambda_gp"
    - from: "Data loading cell (raw_data)"
      to: "Preprocessing cells (log_delta, scaled_data, windowed_data)"
      via: "sequential variable naming through data pipeline stages"
      pattern: "raw_data|log_delta|scaled_data|windowed_data"
---

<objective>
Standardize all naming conventions (hyperparameters to UPPER_CASE, data variables to stage-based names), clean up duplicate imports, fix the data file path, and add markdown section headers to organize the notebook.

Purpose: Make the notebook self-documenting and consistent. UPPER_CASE hyperparameters in a single config cell eliminate scavenger hunts for values. Stage-based variable names make the data pipeline traceable. These renames are done last to avoid merge conflicts with prior plans.

Output: Modified qgan_pennylane.ipynb with consistent naming throughout and organized notebook structure.
</objective>

<execution_context>
@/Users/shawngibford/.claude/get-shit-done/workflows/execute-plan.md
@/Users/shawngibford/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-correctness-infrastructure/01-RESEARCH.md
@.planning/phases/01-foundation-and-correctness-infrastructure/01-02-SUMMARY.md

<interfaces>
<!-- Key rename mappings the executor must apply globally: -->

Hyperparameter renames (QUAL-07):
  EPOCHS → NUM_EPOCHS
  n_critic → N_CRITIC (was lowercase)
  gp (constructor param) → lambda_gp
  self.gp → self.lambda_gp
  LAMBDA stays LAMBDA (already correct)
  LR_CRITIC stays LR_CRITIC (already correct)
  LR_GENERATOR stays LR_GENERATOR (already correct)

Data variable renames (QUAL-10):
  data (CSV load, cell 4) → raw_data
  data (dropna, cell 4) → raw_data (in-place op, same var)
  OD_log_delta → log_delta (stage-based naming per user decision)
  The user specified: raw_data, log_delta, scaled_data, windowed_data

Constructor parameter rename:
  Old: qGAN(num_epochs, batch_size, window_length, n_critic, gp, num_layers, num_qubits)
  New: qGAN(num_epochs, batch_size, window_length, n_critic, lambda_gp, num_layers, num_qubits)

Import locations (QUAL-02):
  Cell 2: duplicate `import numpy as np` (lines 104, 115)
  Cell 2: duplicate `import random` (lines 103, 118)
  Cell 26: another `import numpy as np`
  Keep first occurrence, remove duplicates

Data path (QUAL-01):
  Cell 4: hardcoded absolute path to data.csv
  data.csv exists at project root alongside notebook
  Change to: ./data.csv
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Standardize hyperparameter naming and create config cell</name>
  <files>qgan_pennylane.ipynb</files>
  <action>
Three changes: fix imports, fix data path, and rename all hyperparameters to UPPER_CASE.

**QUAL-02 — Remove duplicate imports (Cell 2 and Cell 26):**
In cell 2, find and remove the second `import numpy as np` and the second `import random`. Keep the first occurrence of each. In cell 26, remove the `import numpy as np` (it's redundant since cell 2 already imports it). Also add `from datetime import datetime` and `from pathlib import Path` if Plan 01 didn't already add them to cell 2.

**QUAL-01 — Fix data path (Cell 4):**
Find the hardcoded absolute path (something like `/Users/shawngibford/dev/qml/qGAN/data.csv`) and change to `./data.csv`. The file exists at the project root alongside the notebook.

**QUAL-07 — Hyperparameter naming convention:**

Step 1: Create/update the config cell (cell 24 or whichever cell defines hyperparameters). Ensure ALL hyperparameters are defined in this single cell using UPPER_CASE:
```python
# Hyperparameter Configuration
NUM_EPOCHS = 2000
BATCH_SIZE = 12          # or whatever the current value is
WINDOW_LENGTH = 10       # or current value
NUM_QUBITS = 5           # or current value
NUM_LAYERS = 2           # or current value
N_CRITIC = 1             # renamed from lowercase n_critic
LAMBDA = 0.8             # gradient penalty coefficient (already UPPER_CASE)
LR_CRITIC = 3e-5
LR_GENERATOR = 8e-5
```

Step 2: Rename the qGAN constructor parameter `gp` to `lambda_gp`:
- In `__init__` signature: `gp` → `lambda_gp`
- Inside class: all `self.gp` → `self.lambda_gp`
- In the gradient penalty computation where `self.gp` is used as the lambda coefficient

Step 3: Rename `EPOCHS` to `NUM_EPOCHS` everywhere:
- Config cell definition
- qGAN instantiation call
- Any print statements referencing it

Step 4: Rename `n_critic` (lowercase) to `N_CRITIC` everywhere:
- Config cell definition
- qGAN instantiation call
- Inside the class, `self.n_critic` remains lowercase (it's an instance attribute, not a constant) but the config cell variable and constructor argument naming should be consistent

Step 5: Update the qGAN instantiation call in cell 24 to use the new names:
```python
qgan = qGAN(NUM_EPOCHS, BATCH_SIZE, WINDOW_LENGTH, N_CRITIC, LAMBDA, NUM_LAYERS, NUM_QUBITS, delta=1)
```

**CRITICAL:** After all renames, do a comprehensive search for every old name (`EPOCHS` without `NUM_`, `n_critic` lowercase, `self.gp`, `gp` as parameter) and verify zero remaining references. Miss one and the notebook breaks on run-all.

Remove stale comments that reference removed or renamed variables (per user decision: clean slate).

**Do NOT delete cells** — replace code in place per user decision.
  </action>
  <verify>
    <automated>cd /Users/shawngibford/dev/phd/qGAN && python3 -c "
import json, re
with open('qgan_pennylane.ipynb') as f:
    nb = json.load(f)
src = ''.join([''.join(c['source']) for c in nb['cells']])

# QUAL-01: No absolute path
assert '/Users/' not in src or '/Users/' not in src.split('data.csv')[0][-50:], 'Absolute data path still present'
assert './data.csv' in src, 'Relative data path not found'

# QUAL-02: Count numpy imports (should be exactly 1)
numpy_imports = re.findall(r'import numpy as np', src)
assert len(numpy_imports) == 1, f'Found {len(numpy_imports)} numpy imports, expected 1'

# QUAL-07: Check renames
# Old names should be gone
assert 'self.gp' not in src.replace('self.gp_', '').replace('self.gpu', ''), 'self.gp still present (should be self.lambda_gp)'
assert 'self.lambda_gp' in src, 'self.lambda_gp not found'
assert 'NUM_EPOCHS' in src, 'NUM_EPOCHS not found'

print('PASS: Naming conventions standardized')
"</automated>
  </verify>
  <done>All hyperparameters use UPPER_CASE in single config cell; duplicate imports removed; data path is ./data.csv; gp renamed to lambda_gp throughout; EPOCHS renamed to NUM_EPOCHS; n_critic config variable is N_CRITIC</done>
</task>

<task type="auto">
  <name>Task 2: Rename data pipeline variables and add section headers</name>
  <files>qgan_pennylane.ipynb</files>
  <action>
Two changes: stage-based variable naming for the data pipeline, and markdown section headers.

**QUAL-10 — Stage-based data variable naming:**

Apply the following rename map across all cells that reference these variables:

1. `data` (the variable assigned from CSV load in cell 4) → `raw_data`
   - Cell 4: `data = pd.read_csv(...)` → `raw_data = pd.read_csv(...)`
   - Cell 4: `data = data.dropna()` → `raw_data = raw_data.dropna()`
   - Trace ALL downstream references to `data` that refer to this CSV-loaded DataFrame and rename them. Be careful: `data` is used for MULTIPLE things in the notebook (CSV load, numpy conversion, etc.). Only rename references that trace back to the CSV load.

2. `OD_log_delta` → `log_delta` (per user decision on stage-based naming)
   - This is the log-transformed delta series. Rename everywhere it appears.
   - Also rename related variables like `OD_log_delta_tensor` if they exist, following the pattern: `log_delta_tensor`

3. For the normalized data variable (whatever it's currently called after normalization), use `scaled_data`

4. For the windowed data variable (after rolling window preprocessing), use `windowed_data`

**IMPORTANT:** The variable `data` is overwritten multiple times in the original notebook (cell 4 CSV load, cell 8 numpy conversion, etc.). Each reassignment should get the appropriate stage-based name. Do NOT rename `data` references that are local variables inside functions/methods (e.g., method parameters named `data`).

After renaming, verify that the variable name `data` is no longer used as a module-level assignment for multiple different things. It's OK if `data` appears as a local variable inside functions.

**Markdown section headers:**
Add markdown cells to organize the notebook into logical sections. Insert markdown cells (as new cells) before the relevant code sections with headers:
- `# Imports` (before cell 2)
- `# Configuration` (before the hyperparameter config cell)
- `# Data Loading` (before cell 4)
- `# Data Preprocessing` (before the preprocessing utility cells)
- `# Model Definition` (before cell 23 / the qGAN class)
- `# Training` (before the training cell)
- `# Evaluation and Visualization` (before the post-training analysis cells)

Use your judgment on exact wording and placement. These are NEW markdown cells inserted at logical boundaries — existing cells are not deleted.

**CRITICAL:** After all renames, trace every renamed variable through all downstream cells to ensure no NameError. The notebook must run top-to-bottom without errors. Pay special attention to:
- Cell references to `OD_log_delta` that become `log_delta`
- Cell references to `data` that become `raw_data`
- Print statements, comments, and string literals that reference old names (update or remove stale comments per user decision)
  </action>
  <verify>
    <automated>cd /Users/shawngibford/dev/phd/qGAN && python3 -c "
import json, re
with open('qgan_pennylane.ipynb') as f:
    nb = json.load(f)

# Check stage-based names exist
src = ''.join([''.join(c['source']) for c in nb['cells']])
assert 'raw_data' in src, 'raw_data not found'
assert 'log_delta' in src, 'log_delta not found'

# Check old OD_log_delta is gone (except possibly in comments explaining the rename)
code_cells = [c for c in nb['cells'] if c['cell_type'] == 'code']
code_src = ''.join([''.join(c['source']) for c in code_cells])
# Allow OD_log_delta in string literals but not as bare variable references
od_refs = re.findall(r'(?<![\"\\'])\\bOD_log_delta\\b(?![\"\\'])', code_src)
# This is a heuristic; manual verification recommended

# Check markdown section headers exist
md_cells = [c for c in nb['cells'] if c['cell_type'] == 'markdown']
md_src = ' '.join([''.join(c['source']) for c in md_cells])
has_sections = any(keyword in md_src for keyword in ['# Import', '# Config', '# Data', '# Model', '# Train', '# Eval'])
assert has_sections, 'No section header markdown cells found'

print('PASS: Variable renames and section headers applied')
"</automated>
  </verify>
  <done>Data pipeline uses stage-based variable names (raw_data, log_delta, scaled_data, windowed_data); OD_log_delta renamed to log_delta throughout; no silent data variable overwrites; notebook organized with markdown section headers</done>
</task>

</tasks>

<verification>
After both tasks complete, verify the combined state:
1. `./data.csv` used for data loading (no absolute path)
2. Exactly one `import numpy as np` in the notebook
3. Exactly one `import random` in the notebook
4. All hyperparameters UPPER_CASE in a single config cell
5. `self.lambda_gp` used instead of `self.gp` in qGAN class
6. `NUM_EPOCHS` used instead of `EPOCHS`
7. `raw_data`, `log_delta` stage-based names present
8. `OD_log_delta` no longer used as a variable name in code cells
9. Markdown section headers present
10. Run a mental trace: cell-by-cell, every variable referenced is defined in a prior cell
</verification>

<success_criteria>
- Data loaded from ./data.csv (portable, no user-specific path)
- No duplicate imports
- Single hyperparameter config cell with all UPPER_CASE constants
- gp → lambda_gp throughout the class
- EPOCHS → NUM_EPOCHS throughout
- Data pipeline variables follow stage-based naming convention
- Notebook organized with markdown section headers
- No NameError when running cells in sequence (all renames consistently applied)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-correctness-infrastructure/01-03-SUMMARY.md`
</output>
