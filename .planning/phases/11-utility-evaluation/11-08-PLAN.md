---
phase: 11-utility-evaluation
plan: 08
type: execute
wave: 2
depends_on: ["11-07"]
files_modified:
  - revision/tests/test_timegan_scores.py
autonomous: true
gap_closure: true
requirements: [EVAL-03]
must_haves:
  truths:
    - "WR-06 closed: revision/tests/test_timegan_scores.py contains a pytest test asserting discriminative_score is deterministic (two identical calls with the same seed return the exact same value)"
    - "The test runs under plain `pytest` collection (a real `def test_...` function), not only inside an `if __name__ == '__main__'` block"
    - "The test proves the WR-05 single-Generator fix: it would fail against the old mixed-global-RNG implementation"
    - "EVAL-03 not regressed: full pytest suite still passes and data_hash invariant holds"
  artifacts:
    - path: "revision/tests/test_timegan_scores.py"
      provides: "test_discriminative_score_deterministic + range coverage collected by pytest"
      contains: "def test_discriminative_score_deterministic"
  key_links:
    - from: "revision/tests/test_timegan_scores.py::test_discriminative_score_deterministic"
      to: "revision/run_timegan_scores.py::discriminative_score"
      via: "two identical seeded calls asserted equal"
      pattern: "discriminative_score\\("
---

<objective>
Close WR-06: add a `test_discriminative_score_deterministic` pytest test (mirroring the existing `test_scores_deterministic` for `predictive_score`) to `revision/tests/test_timegan_scores.py`, so the discriminative path's determinism is locked by the suite that pytest actually collects — not only by the `__main__`-block smoke assertions that pytest never runs.

Purpose: WR-06 — `test_scores_deterministic` covers only `predictive_score`. The discriminative path has the riskier RNG coupling (the WR-05 fix in 11-07) and a shape contract; without a collected determinism test a regression in RNG plumbing or GRU output shape would not be caught by the suite that "locks the invariants". This plan depends on 11-07 so the test asserts the corrected single-Generator behavior.

Output: A determinism test that passes against the 11-07 fix and would have failed against the old mixed-RNG implementation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-utility-evaluation/11-REVIEW.md
@.planning/phases/11-utility-evaluation/11-07-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add test_discriminative_score_deterministic to the collected pytest suite</name>
  <files>revision/tests/test_timegan_scores.py</files>
  <read_first>
    - revision/tests/test_timegan_scores.py (entire file — existing `test_scores_deterministic`, `test_discriminative_score_in_range`, and the `if __name__ == "__main__"` block to extend)
    - revision/run_timegan_scores.py lines 262-330 (the `discriminative_score` signature and the 11-07-corrected single-Generator implementation this test must pin)
    - .planning/phases/11-utility-evaluation/11-REVIEW.md section WR-06 (lines 210-227)
    - .planning/phases/11-utility-evaluation/11-07-PLAN.md (the single-Generator contract being asserted)
  </read_first>
  <action>
    Add a new module-level function `def test_discriminative_score_deterministic():` to `revision/tests/test_timegan_scores.py`, structurally mirroring the existing `test_scores_deterministic` (lines 52-58). Build small fixed-size float64 arrays via a seeded `np.random.default_rng` (e.g. `real = rng.random((160, 10))`, `synth = rng.random((160, 10))`), call `m.discriminative_score(real, synth, 42, 10, iters=40, bs=32)` twice with identical arguments, and assert `a == b, (a, b)` (exact equality, not approximate — the WR-05 fix makes it bit-deterministic). Also assert the returned value is a finite float in `[0.0, 0.5 + 1e-6]` (reuse the range bound from `test_discriminative_score_in_range`). Keep array sizes and `iters` small so the test runs in well under 60 seconds. Append `test_discriminative_score_deterministic()` to the `if __name__ == "__main__":` block so the script-mode smoke path also exercises it. Do not modify `revision/run_timegan_scores.py` or any other test.
  </action>
  <verify>
    <automated>cd /Users/shawngibford/dev/phd/qGAN && grep -nE 'def test_discriminative_score_deterministic' revision/tests/test_timegan_scores.py && /opt/homebrew/bin/pytest "revision/tests/test_timegan_scores.py::test_discriminative_score_deterministic" -q && /opt/homebrew/bin/pytest revision/tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - `revision/tests/test_timegan_scores.py` contains a module-level `def test_discriminative_score_deterministic():` (collected by pytest, not only in `__main__`)
    - The test calls `discriminative_score` twice with identical seeded inputs and asserts exact equality (`a == b`)
    - The test also asserts the result is finite and in `[0.0, 0.5 + 1e-6]`
    - `pytest "revision/tests/test_timegan_scores.py::test_discriminative_score_deterministic" -q` exits 0 (passes against the 11-07 single-Generator fix)
    - The `if __name__ == "__main__"` block calls `test_discriminative_score_deterministic()`
    - `git diff --stat -- revision/core/` is empty
    - All 4 result JSON `data_hash` remain `91e447d4624e25b3` (this plan adds a test only — no driver/data change)
    - `pytest revision/tests/ -q` exits 0 with a test count strictly greater than the prior 22 (new test added)
  </acceptance_criteria>
  <done>WR-06 closed: discriminative_score determinism is locked by a pytest-collected test that proves the 11-07 single-Generator fix and would fail against the old mixed-RNG implementation.</done>
</task>

</tasks>

<threat_model>
No new external attack surface — test-only addition; no network, auth, or untrusted input, no driver/data change. Primary risk is a flaky or non-deterministic test masking a real regression — mitigated by exact-equality assertion (not tolerance) and small fixed seeded inputs; the full pytest gate confirms no collateral suite breakage.
</threat_model>

<verification>
`git diff --stat -- revision/core/` empty; all 4 result JSON data_hash == 91e447d4624e25b3 (no driver change); `pytest revision/tests/ -q` exits 0 with count > 22.
</verification>

<success_criteria>
WR-06 closed; the discriminative determinism contract is now enforced by the collected suite; EVAL-03 not regressed; data_hash invariant preserved.
</success_criteria>

<output>
Create `.planning/phases/11-utility-evaluation/11-08-SUMMARY.md` when done.
</output>
