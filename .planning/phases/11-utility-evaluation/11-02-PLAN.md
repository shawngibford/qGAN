---
phase: 11-utility-evaluation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - revision/run_timegan_scores.py
  - revision/results/predictive_discriminative.json
autonomous: true
requirements: [EVAL-02, EVAL-03]
must_haves:
  truths:
    - "A faithful single-layer-GRU TimeGAN predictive score (next-step MAE, train-synth/test-real) is computed per (model_kind,pipeline) and reported mean +/- std over seeds {42..46}"
    - "A faithful single-layer-GRU TimeGAN discriminative score (|0.5 - test_accuracy|, 80/20 split, real=1/synth=0) is computed per (model_kind,pipeline) and reported mean +/- std over seeds {42..46}"
    - "revision/results/predictive_discriminative.json records the locked H, the univariate-adaptation rationale, and the jsyoon0823/TimeGAN repo URL + master commit in metadata"
    - "Recomputed data_hash equals 91e447d4624e25b3 and equals every one of the 50 baseline config.yaml data_hash fields"
    - "revision/core/ is untouched (git diff --stat revision/core/ empty)"
  artifacts:
    - path: "revision/run_timegan_scores.py"
      provides: "EVAL-02/03 faithful TimeGAN post-hoc GRU predictive + discriminative driver"
      min_lines: 200
    - path: "revision/results/predictive_discriminative.json"
      provides: "predictive_score + discriminative_score long-form rows, mean+/-std, TimeGAN citation metadata"
      contains: "jsyoon0823/TimeGAN"
  key_links:
    - from: "revision/run_timegan_scores.py"
      to: "revision/core/preprocessing.py::inverse_logreturns"
      via: "verbatim reconstruct_od Pipeline-B branch"
      pattern: "inverse_logreturns"
    - from: "revision/run_timegan_scores.py"
      to: "revision/core/data.py::load_and_preprocess"
      via: "real windowed OD + data-hash recompute"
      pattern: "load_and_preprocess"
---

<objective>
Build `revision/run_timegan_scores.py`, the Phase 11 driver delivering EVAL-02
(TimeGAN predictive score) and EVAL-03 (TimeGAN discriminative score) using
faithful single-layer-GRU post-hoc nets ported from the canonical
`jsyoon0823/TimeGAN` NeurIPS 2019 algorithm (D-11-03/D-11-04). It consumes the
same 60 frozen `samples.npy` artifacts via the verbatim `reconstruct_od`
recipe (no regeneration — D-11-08) and writes one long-form JSON with the
TimeGAN reference pinned in metadata.

Purpose: Reviewers explicitly asked for *standard* utility tests; faithful
TimeGAN scores (not the Phase 10 lite scaffolding) are the strongest rebuttal
anchor for R1-M2.
Output: `revision/run_timegan_scores.py`,
`revision/results/predictive_discriminative.json`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-utility-evaluation/11-CONTEXT.md
@.planning/phases/11-utility-evaluation/11-RESEARCH.md
@.planning/phases/11-utility-evaluation/11-PATTERNS.md

<interfaces>
<!-- Verified contracts. Executor uses these directly. -->

reconstruct_od recipe — COPY VERBATIM from revision/_build_baseline_notebook.py
lines ~167-208 (identical to Plan 01 Task 1; same load-bearing
np.random.default_rng(seed*7919+1) Pipeline-B od_start draw).
Returns {"od_samples": ndarray(3840,10), "transformed": ndarray|None, ...}.

revision.core.preprocessing.inverse_logreturns(...) -> torch tensor
revision.core.data.load_and_preprocess(str) -> dict (key "OD")
revision.core.data.rolling_window(OD, 10, 2) -> torch (384,10)
revision.core.WINDOW_LENGTH == 10

Constants: MODEL_KINDS=["quantum","wgan_mlp","wgan_cnn","wgan_lstm","vae","ar"];
PIPELINES=["A","B"]; SEEDS=[42,43,44,45,46].

TimeGAN PINNED DEFINITIONS (jsyoon0823/TimeGAN, NeurIPS 2019, master branch):
  Predictive  (metrics/predictive_metrics.py):
    single-layer GRU + Linear head; Adam lr=1e-3; 5000 iters; batch 128;
    train=synthetic, test=real; X=window[:, :-1], y=window[:, -1] (one-step);
    score = mean(|y_real - y_pred|). Lower better.
  Discriminative (metrics/discriminative_metrics.py):
    single-layer GRU + Linear(H,1)->sigmoid logit; Adam lr=1e-3; 2000 iters;
    batch 128; real label=1, synth label=0; 80/20 split of each pool
    independently (np.random.permutation with recorded seed);
    score = abs(0.5 - test_accuracy). Lower better.
  Canonical hidden_dim = int(dim/2); dim=1 here -> DEGENERATE (Pitfall 1 / A1).
  LOCK H = WINDOW_LENGTH = 10 (D-11-04 "hidden dim ~ input_dim", reading the
  length-10 window as the input). Use the SAME H for both nets. RECORD H + the
  adaptation rationale + repo URL https://github.com/jsyoon0823/TimeGAN +
  branch "master" in predictive_discriminative.json["metadata"].

Long-form JSON row: {"model_kind","pipeline","seed","metric_name","scale","value"}
data_hash == "91e447d4624e25b3"
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Driver scaffold + faithful TimeGAN GRU nets (predictive + discriminative)</name>
  <files>revision/run_timegan_scores.py</files>
  <behavior>
    - PredictiveGRU(H=10): GRU(input_size=1,hidden_size=10,num_layers=1,batch_first=True) + Linear(10,1); forward((B,9,1)) -> (B,9,1) or (B,1) next-step prediction; trainable params > 0 (NOT degenerate — Pitfall 1).
    - DiscriminativeGRU(H=10): same GRU shape + Linear(10,1); forward returns a logit per sequence.
    - predictive_score(synth_windows, real_windows, seed) -> float == mean(|y_real - y_pred|) after Adam(lr=1e-3) x 5000 iters batch 128; deterministic given torch.manual_seed.
    - discriminative_score(real_windows, synth_windows, seed) -> float == abs(0.5 - test_acc); 80/20 split each pool; real=1/synth=0; Adam(lr=1e-3) x 2000 iters batch 128.
    - With H=10 and dim=1 the GRU constructs successfully (regression guard for the degenerate int(dim/2)=0 trap).
  </behavior>
  <read_first>
    - revision/run_baselines.py (driver/argparse shape ~430-529; --csv-path default ~451-453; data-hash compute ~226-234)
    - revision/_build_baseline_notebook.py lines ~67-99 (imports), ~104-149 (data-hash assert loop), ~167-208 (_run_base + reconstruct_od — COPY VERBATIM), ~395-440 (TSTRLiteLSTM/Adam/minibatch idiom to model the GRU loop on)
    - .planning/phases/11-utility-evaluation/11-RESEARCH.md (TimeGAN Score Definitions; Pitfall 1 degenerate hidden_dim; Pitfall 4 quantum no data_hash; Code Examples PredictiveGRU)
    - .planning/phases/11-utility-evaluation/11-PATTERNS.md (run_timegan_scores.py section; univariate adaptation note)
  </read_first>
  <action>
    Create `revision/run_timegan_scores.py` patterned on `run_baselines.py`.
    argparse: `--out` (default `Path("revision/results")`), `--csv-path`
    (default `Path("./data.csv")`, resolved to absolute repo-root path —
    Pitfall 6), `--hidden` (default 10), `--pred-iters` (default 5000),
    `--disc-iters` (default 2000), `--batch` (default 128). Copy the import
    block + `_compute_data_hash` + the 50-baseline-config data-hash assert loop
    + `_run_base` + `reconstruct_od` VERBATIM from
    `_build_baseline_notebook.py` (same as Plan 01 Task 1; do NOT assert
    data_hash on quantum configs — Pitfall 4). Define `PredictiveGRU(H)` =
    `torch.nn.GRU(input_size=1, hidden_size=H, num_layers=1, batch_first=True)`
    + `torch.nn.Linear(H, 1)`; `forward(x)` returns `self.fc(self.gru(x)[0])`.
    Define `DiscriminativeGRU(H)` with the same single-layer-GRU shape +
    `Linear(H,1)`; classification uses the last hidden state -> logit.
    Implement `predictive_score(synth_windows, real_windows, seed, H,
    iters=5000, bs=128)`: X=`w[:, :-1]` reshaped `(N,9,1)`, y=`w[:, -1]`;
    train on SYNTHETIC, `torch.optim.Adam(lr=1e-3)`, MAE-target regression,
    permuted-minibatch loop for `iters`; evaluate on REAL; return
    `mean(|y_real - y_pred|)`. Implement `discriminative_score(real_windows,
    synth_windows, seed, H, iters=2000, bs=128)`: label real=1 / synth=0;
    `np.random.permutation` (seed recorded) for an independent 80/20 split of
    EACH pool; `BCEWithLogitsLoss`, Adam(lr=1e-3), `iters` minibatches;
    test accuracy at 0.5 threshold; return `abs(0.5 - test_accuracy)`. LOCK
    `H = WINDOW_LENGTH = 10` (Pitfall 1 / A1 — the canonical `int(dim/2)` is
    degenerate at dim=1; same H for both nets). Add unit-style asserts at the
    bottom under `if __name__ == "__main__":` smoke path: GRU constructs at
    H=10 dim=1 (param count > 0); both scores return a finite float on a tiny
    synthetic/real fixture.
  </action>
  <verify>
    <automated>cd /Users/shawngibford/dev/phd/qGAN && ./qgan_env/bin/python -c "import ast; ast.parse(open('revision/run_timegan_scores.py').read()); print('parse ok')" && ./qgan_env/bin/python -c "import sys; sys.path.insert(0,'.'); import numpy as np, revision.run_timegan_scores as m; g=m.PredictiveGRU(10); n=sum(p.numel() for p in g.parameters()); assert n>0, n; import torch; o=g(torch.zeros(4,9,1)); print('GRU ok params=',n,'out',tuple(o.shape)); s=m.predictive_score(np.random.rand(200,10).astype('float64'), np.random.rand(120,10).astype('float64'), 42, 10, iters=50, bs=32); assert np.isfinite(s); d=m.discriminative_score(np.random.rand(200,10).astype('float64'), np.random.rand(200,10).astype('float64'), 42, 10, iters=50, bs=32); assert 0.0<=d<=0.5+1e-6, d; print('scores OK', round(float(s),4), round(float(d),4))"</automated>
  </verify>
  <done>
    Driver parses; `PredictiveGRU(10)` constructs with >0 params (degenerate
    trap avoided); `predictive_score` and `discriminative_score` return finite
    floats on a fixture; `discriminative_score in [0, 0.5]`.
  </done>
  <acceptance_criteria>
    - `ast.parse` succeeds.
    - `PredictiveGRU(10)` and `DiscriminativeGRU(10)` construct with parameter count > 0 at `input_size=1` (regression guard for the `int(dim/2)=0` degenerate trap — Pitfall 1).
    - `predictive_score(...)` returns a finite non-negative float (MAE).
    - `discriminative_score(...)` returns a float in `[0, 0.5]` (== `abs(0.5 - acc)`).
    - `_compute_data_hash(Path('data.csv')) == "91e447d4624e25b3"`; assert loop covers the 50 baseline configs only (no KeyError on quantum configs).
    - `git diff --stat revision/core/` is empty.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Per-seed roll-up + write predictive_discriminative.json with TimeGAN citation</name>
  <files>revision/run_timegan_scores.py, revision/results/predictive_discriminative.json</files>
  <read_first>
    - revision/_build_baseline_notebook.py lines ~352-378 (long-form JSON writer), ~464-501 (per-seed pooling idiom)
    - revision/results/baseline_comparison.json (top-level key set + row shape)
    - .planning/phases/11-utility-evaluation/11-RESEARCH.md (Per-seed roll-up D-11-05; Sources -> TimeGAN repo URLs; A1 record-H mandate)
    - .planning/phases/11-utility-evaluation/11-PATTERNS.md (Shared Patterns -> Long-form JSON; predictive_discriminative.json bullet)
    - .planning/phases/11-utility-evaluation/11-CONTEXT.md (D-11-05; Claude's Discretion -> pin reference in metadata)
  </read_first>
  <action>
    For each `(model_kind, pipeline)` in MODEL_KINDS x PIPELINES, compute
    `predictive_score` and `discriminative_score` PER training seed in
    {42,43,44,45,46} using that seed's `reconstruct_od(...)["od_samples"]` as
    the synthetic pool and `real_windowed_OD` as the real pool (D-11-05). Emit
    one long-form row per (model_kind,pipeline,seed,metric) with
    `metric_name in {predictive_score, discriminative_score}`, `scale="OD"`.
    Compute `mean +/- std` across the 5 seeds per (model_kind,pipeline) and
    place those aggregates in a `scores` block keyed by `{mk}|{p}` with
    `predictive_mean/predictive_std/discriminative_mean/discriminative_std`.
    Write `revision/results/predictive_discriminative.json` via
    `Path(...).write_text(json.dumps(obj, indent=2))` with top-level keys
    mirroring `baseline_comparison.json`: `schema` (= "long-form rows[] +
    scores block (EVAL-02/03, D-11-05)"), `model_kinds`, `pipelines`, `seeds`,
    `data_hash` (="91e447d4624e25b3"), `data_hash_verification`, `rows[]`,
    `scores`, and a `metadata` block recording: `timegan_reference_url` =
    "https://github.com/jsyoon0823/TimeGAN", `timegan_reference_branch` =
    "master", `predictive_metrics_file` =
    "metrics/predictive_metrics.py", `discriminative_metrics_file` =
    "metrics/discriminative_metrics.py", `hidden_dim` = 10,
    `univariate_adaptation` = a one-paragraph rationale (canonical
    `int(dim/2)` degenerate at dim=1; H locked to WINDOW_LENGTH=10 per D-11-04,
    same H for both nets), `pred_iters`=5000, `disc_iters`=2000, `batch`=128,
    `optimizer`="Adam(lr=1e-3)". Run the driver end-to-end.
  </action>
  <verify>
    <automated>cd /Users/shawngibford/dev/phd/qGAN && ./qgan_env/bin/python revision/run_timegan_scores.py 2>&1 | tail -5 && ./qgan_env/bin/python -c "import json; d=json.load(open('revision/results/predictive_discriminative.json')); assert d['data_hash']=='91e447d4624e25b3'; mn={r['metric_name'] for r in d['rows']}; assert {'predictive_score','discriminative_score'}<=mn, mn; sd={(r['model_kind'],r['pipeline']) for r in d['rows']}; assert len(sd)==12, len(sd); m=d['metadata']; assert 'jsyoon0823/TimeGAN' in m['timegan_reference_url'] and m['hidden_dim']==10 and len(str(m['univariate_adaptation']))>40; sk=next(iter(d['scores'])); assert 'predictive_std' in d['scores'][sk]; print('predictive_discriminative.json OK', len(d['rows']),'rows')"</automated>
  </verify>
  <done>
    `revision/results/predictive_discriminative.json` written with per-seed
    rows for both scores across all 6x2 model/pipeline combos, mean+/-std in a
    `scores` block, and the TimeGAN reference + locked H + adaptation rationale
    in `metadata`.
  </done>
  <acceptance_criteria>
    - `predictive_discriminative.json` exists; `json.load` succeeds; `data_hash == "91e447d4624e25b3"`.
    - `rows[]` `metric_name` covers `{predictive_score, discriminative_score}`; every row has the exact long-form keys; `scale=="OD"`.
    - All 6 model_kinds x 2 pipelines x 5 seeds represented (12 distinct (mk,pipeline) groups).
    - `scores` block has `*_mean` and `*_std` for both metrics per (mk,pipeline) (D-11-05 mean +/- std).
    - `metadata.timegan_reference_url` contains `jsyoon0823/TimeGAN`; `metadata.hidden_dim == 10`; `metadata.univariate_adaptation` is a non-trivial rationale string (A1 record mandate).
    - `git diff --stat revision/core/` is empty.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| frozen artifact -> driver | `samples.npy` read-only; mis-pairing/corruption must be detected via shape + data-hash |
| driver -> results JSON | Phase 14 cites these scores; a non-faithful score implementation would weaken/invalidate the rebuttal |

Local, offline, no network/auth/PII/untrusted input. Dominant risks are
scientific-integrity (faithfulness drift, degenerate net, data mis-hash).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-06 | Tampering | TimeGAN faithfulness drift | mitigate | pin exact algorithm to jsyoon0823/TimeGAN; record repo URL + branch + iters/batch/optimizer in JSON metadata (Task 2) |
| T-11-07 | Denial of service | degenerate hidden_dim (int(dim/2)=0) | mitigate | lock H=WINDOW_LENGTH=10; param-count>0 assert (Task 1, Pitfall 1) |
| T-11-01 | Tampering | data.csv / sample provenance | mitigate | recomputed data_hash == 91e447d4624e25b3 == 50 baseline config hashes (Task 1) |
| T-11-04 | Tampering | core-module invariant (D-11-10) | mitigate | `git diff --stat revision/core/` empty in every task acceptance |
| T-11-SC | Tampering | npm/pip installs | accept | zero package installs; sklearn deliberately NOT added (RESEARCH Package Legitimacy Audit) |
</threat_model>

<verification>
- `./qgan_env/bin/python revision/run_timegan_scores.py` runs end-to-end (<15 min, RESEARCH A5).
- `predictive_discriminative.json` validates against the long-form schema; `data_hash==91e447d4624e25b3`.
- `metadata` records the TimeGAN repo URL + branch + locked H + univariate-adaptation rationale (A1).
- `git diff --stat revision/core/` empty (D-11-10).
</verification>

<success_criteria>
- EVAL-02: faithful TimeGAN predictive score for quantum + classical WGAN-GP + non-adversarial, mean +/- std across seeds (ROADMAP SC-2).
- EVAL-03: faithful TimeGAN discriminative score for the same three model families, mean +/- std (ROADMAP SC-2).
- Reference implementation pinned in JSON metadata (D-11-04 discretion).
- `revision/core/` untouched; no generator retrained.
</success_criteria>

<output>
Create `.planning/phases/11-utility-evaluation/11-02-SUMMARY.md` when done.
</output>
