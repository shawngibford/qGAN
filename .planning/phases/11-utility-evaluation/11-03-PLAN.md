---
phase: 11-utility-evaluation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - run_dualscale_fidelity.py
  - results/fidelity_dualscale.json
autonomous: true
requirements: [EVAL-05]
must_haves:
  truths:
    - "Every revision.core.eval fidelity metric (EMD, ACF, moments, DTW) is re-emitted on the OD scale for all 6 model kinds x 2 pipelines x 5 seeds"
    - "For Pipeline B every fidelity metric is also emitted on the log_return scale; for Pipeline A the log_return scale is explicitly null with a documented reason"
    - "results/fidelity_dualscale.json carries an explicit scale field ('OD' | 'log_return') on every row"
    - "Recomputed data_hash equals 91e447d4624e25b3; quantum equivalence is by-construction (no data_hash assert on quantum configs)"
    - "core/ is untouched (git diff --stat core/ empty) — eval.py reused unchanged"
  artifacts:
    - path: "run_dualscale_fidelity.py"
      provides: "EVAL-05 scale-tagged fidelity re-emit driver (reuses revision.core.eval unchanged)"
      min_lines: 150
    - path: "results/fidelity_dualscale.json"
      provides: "Dual-scale long-form fidelity rows with explicit scale field"
      contains: "log_return"
  key_links:
    - from: "run_dualscale_fidelity.py"
      to: "core/eval.py::compute_emd"
      via: "unchanged metric helpers, driver-level scale tag"
      pattern: "compute_emd"
    - from: "run_dualscale_fidelity.py"
      to: "core/preprocessing.py::inverse_logreturns"
      via: "verbatim reconstruct_od (od_samples + transformed)"
      pattern: "inverse_logreturns"
---

<objective>
Build `scripts/run_dualscale_fidelity.py`, the Phase 11 driver delivering
EVAL-05: every `revision.core.eval` fidelity metric (EMD, moments, ACF, DTW)
re-emitted with an explicit `scale: "OD" | "log_return"` field, for all 60
frozen sample artifacts (no regeneration — D-11-08; both Pipeline A and B per
D-11-09). The fidelity math is reused UNCHANGED from `core/eval.py`
(D-11-10); only a driver-level scale-tagging loop is new.

Purpose: Directly answers reviewer R1-m3 / R1-M3 ("which scale are metrics
reported on?") by making the scale explicit on every metric.
Output: `scripts/run_dualscale_fidelity.py`,
`results/fidelity_dualscale.json`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-utility-evaluation/11-CONTEXT.md
@.planning/phases/11-utility-evaluation/11-RESEARCH.md
@.planning/phases/11-utility-evaluation/11-PATTERNS.md

<interfaces>
<!-- Verified contracts. Executor uses these directly. -->

reconstruct_od — COPY VERBATIM from _build_baseline_notebook.py
~167-208 (identical to Plan 01/02; load-bearing Pipeline-B
np.random.default_rng(seed*7919+1) od_start draw). Returns
{"od_samples": ndarray(3840,10), "transformed": ndarray|None (log-return,
Pipeline B only), "n_synth", "pipeline", "seed"}.

revision.core.eval helpers (REUSE UNCHANGED — D-11-10):
  compute_emd(real_flat, synth_flat) -> float        (eval.py ~:25, raw-sample EMD)
  compute_moments(arr) -> dict (mean,std,kurtosis...) (eval.py ~:42, Fisher kurtosis, ddof=0)
  compute_acf(arr, ...) -> per-lag values             (eval.py ~:64, FFT ACF, lags 0..9)
  compute_dtw(a, b) -> float                          (eval.py ~:78, fastdtw+euclidean)
revision.core.data.load_and_preprocess / rolling_window — real OD + log-return source.

Long-form row: {"model_kind","pipeline","seed","metric_name","scale","value"}
scale in {"OD","log_return"}. Pipeline A has no log-return space ->
emit explicit "value": null rows with scale="log_return" and a
"scale_na_reason" field so the gap is visible, not silently omitted.

DTW sub-sampling (copy from _build_baseline_notebook.py ~266-284):
DTW_N_PAIRS=100 nearest-neighbour pairs, np.random.default_rng(seed*31) —
do NOT change the seed or pair count (numbers drift otherwise).

data_hash == "91e447d4624e25b3"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Driver scaffold + verbatim reconstruct_od + real OD/log-return references</name>
  <files>run_dualscale_fidelity.py</files>
  <read_first>
    - run_baselines.py (driver/argparse shape; --csv-path default ~451-453)
    - _build_baseline_notebook.py lines ~67-99 (imports), ~104-149 (data-hash assert loop), ~167-208 (_run_base + reconstruct_od — COPY VERBATIM), ~234-291 (Cell-6 long-form metric loop — copy structure), ~266-284 (DTW DTW_N_PAIRS sub-sample recipe)
    - core/eval.py (compute_emd/compute_moments/compute_acf/compute_dtw signatures — DO NOT MODIFY)
    - .planning/phases/11-utility-evaluation/11-PATTERNS.md (run_dualscale_fidelity.py section; Fidelity metric helpers reuse note)
    - .planning/phases/11-utility-evaluation/11-RESEARCH.md (Pitfall 3 seeded od_start; Pitfall 4 quantum no data_hash; Open Question 3 file boundary)
  </read_first>
  <action>
    Create `scripts/run_dualscale_fidelity.py` patterned on
    `scripts/run_baselines.py`. argparse: `--out` (default `Path("revision/results")`),
    `--csv-path` (default `Path("./data.csv")`, resolved to absolute repo-root
    path — Pitfall 6). Copy the import block + `_compute_data_hash` + the
    50-baseline-config data-hash assert loop + `_run_base` + `reconstruct_od`
    VERBATIM from `_build_baseline_notebook.py` (same as Plan 01/02; NO
    data_hash assert on quantum configs — Pitfall 4). Add
    `from revision.core.eval import compute_emd, compute_moments, compute_acf,
    compute_dtw` (import only — never edit eval.py). Build the real references:
    `d_real = load_and_preprocess(abs_csv)`; `real_OD_windowed =
    rolling_window(d_real["OD"], WINDOW_LENGTH, 2).cpu().numpy()` ->
    `real_OD_flat = real_OD_windowed.reshape(-1)`; and the real log-return
    reference array for the Pipeline-B comparison, sourced via the SAME
    construction `_build_baseline_notebook.py` Cell-6 uses for its
    `scale="transformed"` rows (reuse that exact reference so numbers
    reconcile with `baseline_comparison.json`). Smoke-print recomputed
    data_hash and assert == `"91e447d4624e25b3"`.
  </action>
  <verify>
    <automated>cd /Users/shawngibford/dev/phd/qGAN && ./qgan_env/bin/python -c "import ast; ast.parse(open('run_dualscale_fidelity.py').read()); print('parse ok')" && ./qgan_env/bin/python -c "import sys; sys.path.insert(0,'.'); import revision.run_dualscale_fidelity as m; from pathlib import Path; assert m._compute_data_hash(Path('data.csv'))=='91e447d4624e25b3'; r=m.reconstruct_od('quantum','B',42); assert r['od_samples'].shape==(3840,10) and r['transformed'] is not None; a=m.reconstruct_od('vae','A',42); assert a['transformed'] is None; print('scaffold OK')"</automated>
  </verify>
  <done>
    Driver parses; `_compute_data_hash(data.csv)==91e447d4624e25b3`;
    `reconstruct_od('quantum','B',42)` has `od_samples (3840,10)` and a
    non-None `transformed`; Pipeline-A `transformed` is None; `eval.py` not
    edited.
  </done>
  <acceptance_criteria>
    - `ast.parse` of `scripts/run_dualscale_fidelity.py` succeeds.
    - `_compute_data_hash(Path('data.csv')) == "91e447d4624e25b3"`; assert loop covers the 50 baseline configs only (no KeyError on quantum configs — Pitfall 4).
    - `reconstruct_od('quantum','B',42)["od_samples"].shape == (3840,10)` and `["transformed"] is not None`.
    - `reconstruct_od('vae','A',42)["transformed"] is None` (Pipeline A has no log-return space).
    - `compute_emd/compute_moments/compute_acf/compute_dtw` imported from `revision.core.eval` (not redefined).
    - `git diff --stat core/` is empty.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Scale-tagged metric loop + write fidelity_dualscale.json</name>
  <files>run_dualscale_fidelity.py, results/fidelity_dualscale.json</files>
  <read_first>
    - _build_baseline_notebook.py lines ~234-291 (Cell-6 long-form metric loop — copy structure incl. ACF lags 0..9 mean+std), ~266-284 (DTW_N_PAIRS=100, rng=np.random.default_rng(seed*31)), ~352-378 (JSON writer)
    - results/baseline_comparison.json (existing scale="OD" / scale="transformed" rows + top-level key set)
    - .planning/phases/11-utility-evaluation/11-RESEARCH.md (EVAL-05 row; Pattern 4 long-form schema; Open Question 3)
    - .planning/phases/11-utility-evaluation/11-PATTERNS.md (Shared Patterns -> Long-form JSON; run_dualscale_fidelity.py scale-tagging loop)
    - .planning/phases/11-utility-evaluation/11-CONTEXT.md (D-11-09 both pipelines, EVAL-05 dual-scale on every metric)
  </read_first>
  <action>
    Implement the scale-tagging loop (copy the structure from
    `_build_baseline_notebook.py:234-291`; do NOT add new metric math —
    D-11-10). For each `(model_kind, pipeline, seed)` in MODEL_KINDS x
    PIPELINES x SEEDS: `r = reconstruct_od(mk, p, s)`;
    `od = r["od_samples"]`. Emit OD-scale rows for every metric:
    `compute_emd(real_OD_flat, od.reshape(-1))` -> metric_name="emd"; the
    `compute_moments` keys (mean/std/kurtosis...) each as their own
    metric_name (e.g. "moment_mean","moment_std","moment_kurtosis");
    `compute_acf` per-lag (lags 0..9) mean+std as metric_name like
    "acf_lag{k}"; `compute_dtw` with the verbatim DTW sub-sample recipe
    (`DTW_N_PAIRS=100`, `np.random.default_rng(s*31)`) -> metric_name="dtw".
    Every such row carries `scale="OD"`. For Pipeline B (`r["transformed"]
    is not None`) emit the SAME metric set a second time on the log-return
    arrays (real log-return reference vs `r["transformed"].reshape(-1)`) with
    `scale="log_return"`. For Pipeline A emit, per metric, an explicit row
    `{... ,"scale":"log_return","value":null,"scale_na_reason":"Pipeline A is
    raw-OD; no log-return space (D-11-09)"}` so the schema stays rectangular
    and the gap is visible (RESEARCH Open Question 3 — emit explicit nulls,
    do not silently omit). Write `results/fidelity_dualscale.json`
    via `Path(...).write_text(json.dumps(obj, indent=2))` with top-level keys
    mirroring `baseline_comparison.json`: `schema` (= "long-form dual-scale
    rows[] (EVAL-05, D-11-09)"), `model_kinds`, `pipelines`, `seeds`,
    `data_hash` (="91e447d4624e25b3"), `data_hash_verification`,
    `metric_helpers` (list the reused `revision.core.eval` fn names + the note
    "reused unchanged, D-11-10"), and `rows[]`. Run the driver end-to-end.
  </action>
  <verify>
    <automated>cd /Users/shawngibford/dev/phd/qGAN && ./qgan_env/bin/python scripts/run_dualscale_fidelity.py 2>&1 | tail -5 && ./qgan_env/bin/python -c "import json; d=json.load(open('results/fidelity_dualscale.json')); assert d['data_hash']=='91e447d4624e25b3'; sc={r['scale'] for r in d['rows']}; assert sc=={'OD','log_return'}, sc; mn={r['metric_name'] for r in d['rows']}; assert 'emd' in mn and 'dtw' in mn and any(x.startswith('acf_lag') for x in mn) and any(x.startswith('moment_') for x in mn), mn; b_lr=[r for r in d['rows'] if r['pipeline']=='B' and r['scale']=='log_return' and r['metric_name']=='emd']; assert b_lr and b_lr[0]['value'] is not None; a_lr=[r for r in d['rows'] if r['pipeline']=='A' and r['scale']=='log_return']; assert a_lr and a_lr[0]['value'] is None and 'scale_na_reason' in a_lr[0], 'Pipeline A log_return rows must be explicit null'; print('fidelity_dualscale.json OK', len(d['rows']),'rows')"</automated>
  </verify>
  <done>
    `results/fidelity_dualscale.json` written; every row has an
    explicit `scale` in `{OD,log_return}`; Pipeline-B log-return values are
    non-null; Pipeline-A log-return rows are explicit nulls with a reason;
    `eval.py` unchanged.
  </done>
  <acceptance_criteria>
    - `fidelity_dualscale.json` exists; `json.load` succeeds; `data_hash == "91e447d4624e25b3"`.
    - Every row has the exact long-form keys plus an explicit `scale in {"OD","log_return"}`.
    - Metric coverage includes `emd`, `dtw`, `acf_lag*`, `moment_*` (EMD/ACF/moments/DTW — ROADMAP SC-4).
    - Pipeline B has non-null `log_return`-scale values; Pipeline A `log_return` rows are explicit `value: null` with a `scale_na_reason` (no silent omission).
    - All 6 model_kinds x 2 pipelines x 5 seeds covered on the OD scale.
    - `git diff --stat core/` is empty (eval.py reused unchanged — D-11-10).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| frozen artifact -> driver | `samples.npy` read-only; corruption/mis-pairing detected via shape + data-hash |
| driver -> results JSON | Phase 14 cites scale-labeled fidelity numbers; an ambiguous/mis-scaled metric would mislead reviewers |

Local, offline, no network/auth/PII/untrusted input. Dominant risks are
scientific-integrity (scale mislabeling, silent metric omission, eval.py drift).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-08 | Spoofing | metric scale mislabel | mitigate | explicit `scale` field on every row; Pipeline-A log_return = explicit null + reason (Task 2) |
| T-11-09 | Information disclosure | silent metric omission | mitigate | rectangular schema — emit explicit null rows, never drop a metric (Task 2, Open Question 3) |
| T-11-04 | Tampering | core-module invariant (D-11-10) | mitigate | eval.py imported not edited; `git diff --stat core/` empty in every task |
| T-11-01 | Tampering | data.csv / sample provenance | mitigate | recomputed data_hash == 91e447d4624e25b3 == 50 baseline config hashes (Task 1) |
| T-11-03 | Tampering | DTW sub-sample RNG drift | mitigate | copy DTW_N_PAIRS=100 + np.random.default_rng(seed*31) verbatim (Task 2) |
| T-11-SC | Tampering | npm/pip installs | accept | zero package installs (RESEARCH Package Legitimacy Audit) |
</threat_model>

<verification>
- `./qgan_env/bin/python scripts/run_dualscale_fidelity.py` runs end-to-end (<10 min, RESEARCH A5).
- `fidelity_dualscale.json` validates against the long-form schema; every row has an explicit `scale`.
- Pipeline-B log-return EMD reconciles with `baseline_comparison.json`'s existing transformed-scale rows (no numeric drift).
- `git diff --stat core/` empty (D-11-10).
</verification>

<success_criteria>
- EVAL-05: EMD, ACF, moments, DTW reported on BOTH log-return and OD scales with explicit `scale` fields in JSON (ROADMAP SC-4).
- `revision.core.eval` reused unchanged; both Pipeline A and B covered (D-11-09).
</success_criteria>

<output>
Create `.planning/phases/11-utility-evaluation/11-03-SUMMARY.md` when done.
</output>
