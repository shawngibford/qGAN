---
phase: 11-utility-evaluation
plan: 04
type: execute
wave: 2
depends_on: ["11-01", "11-02", "11-03"]
files_modified:
  - revision/tests/test_utility.py
autonomous: true
requirements: [EVAL-01, EVAL-02, EVAL-03, EVAL-04, EVAL-05]
must_haves:
  truths:
    - "A pytest suite asserts all three Phase 11 driver outputs exist, validate against the long-form schema, and share data_hash 91e447d4624e25b3"
    - "Scientific-integrity sentinels are codified as tests: negative real-only-baseline R2 (no leakage), (3840,10) sample shape, augmentation partition disjointness, revision/core/ untouched"
    - "Phase 10 reproduction sanity check: quantum|B fidelity reconciles with the Phase 10 anchor (no numeric drift from verbatim reuse)"
    - "The full pytest suite passes via ./qgan_env/bin/python -m pytest revision/tests/ -q"
  artifacts:
    - path: "revision/tests/test_utility.py"
      provides: "Cross-artifact scientific-integrity test suite for all Phase 11 outputs"
      min_lines: 80
  key_links:
    - from: "revision/tests/test_utility.py"
      to: "revision/results/tstr.json"
      via: "schema + leakage-sentinel assertions"
      pattern: "tstr.json"
    - from: "revision/tests/test_utility.py"
      to: "revision/results/predictive_discriminative.json"
      via: "schema + TimeGAN-metadata assertions"
      pattern: "predictive_discriminative.json"
    - from: "revision/tests/test_utility.py"
      to: "revision/results/fidelity_dualscale.json"
      via: "dual-scale schema assertions"
      pattern: "fidelity_dualscale.json"
---

<objective>
Build `revision/tests/test_utility.py`, the Phase 11 cross-artifact
verification suite. It locks the scientific-integrity invariants for all three
Wave-1 driver outputs (`tstr.json`, `augmentation.json`,
`predictive_discriminative.json`, `fidelity_dualscale.json`) as executable
pytest checks so any future regression (data-hash drift, eval/train leakage,
core-module edit, numeric drift vs the Phase 10 anchor) is caught.

Purpose: The natural correctness oracles RESEARCH flagged (Nyquist disabled —
asserted as plain pytest) become a permanent guard, not a one-time manual
inspection. Closes Phase 11 with goal-backward verification.
Output: `revision/tests/test_utility.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-utility-evaluation/11-CONTEXT.md
@.planning/phases/11-utility-evaluation/11-RESEARCH.md
@.planning/phases/11-utility-evaluation/11-PATTERNS.md

<interfaces>
<!-- Verified contracts + Wave-1 outputs this suite validates. -->

pytest convention: existing revision/tests/test_classical.py,
revision/tests/test_nonadversarial.py. Run via
`./qgan_env/bin/python -m pytest revision/tests/ -x -q`. No config file.

Wave-1 outputs under revision/results/ (produced by Plans 01-03):
  tstr.json                    : long-form rows {r2,mae,rmse,mse}@scale=OD +
                                 tstr block w/ real_only_baseline
                                 (n_train_real=65, n_eval_real=320, R2~-13.354)
  augmentation.json            : rows {r2_delta,mae_delta,rmse_delta}@scale=OD,
                                 injection_ratio field, metadata caveat
  predictive_discriminative.json: rows {predictive_score,discriminative_score}
                                 @scale=OD, scores mean/std block,
                                 metadata.timegan_reference_url + hidden_dim=10
  fidelity_dualscale.json      : rows w/ scale in {OD,log_return}; Pipeline-A
                                 log_return = explicit null + scale_na_reason

Anchors (RESEARCH Validation Architecture): data_hash="91e447d4624e25b3";
samples.npy = (3840,10) float64; real_only R2 NEGATIVE (~-13.354 +/- 0.583,
positive => leakage); Phase 10 quantum|B OD-EMD ~ 0.0276 +/- 0.0046.
revision.run_utility.reconstruct_od is the verbatim recon under test.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Schema + provenance + sentinel test suite</name>
  <files>revision/tests/test_utility.py</files>
  <read_first>
    - revision/tests/test_classical.py and revision/tests/test_nonadversarial.py (pytest style/imports/path-resolution convention)
    - .planning/phases/11-utility-evaluation/11-RESEARCH.md (Validation Architecture -> Natural correctness oracles table; Test Framework section)
    - .planning/phases/11-utility-evaluation/11-PATTERNS.md (test_utility.py section: assert shape/hash/round-trip/disjointness/core-diff)
    - revision/results/tstr.json, revision/results/augmentation.json, revision/results/predictive_discriminative.json, revision/results/fidelity_dualscale.json (the Wave-1 outputs being validated)
  </read_first>
  <action>
    Create `revision/tests/test_utility.py` following the existing
    `test_classical.py` pytest convention (same import/path-resolution style;
    resolve repo root the same way). Implement these test functions:
    (1) `test_all_outputs_exist` — all four JSONs exist and `json.load`
    succeeds.
    (2) `test_data_hash_consistency` — every JSON top-level
    `data_hash == "91e447d4624e25b3"`; and the recomputed hash from
    `revision.run_utility.reconstruct_od`'s `_compute_data_hash` /
    `load_and_preprocess('data.csv')` equals it (cross-phase invariant).
    (3) `test_long_form_schema` — every row in every JSON has exactly the keys
    `{model_kind,pipeline,seed,metric_name,scale,value}` (plus the documented
    extras: `injection_ratio` for augmentation, `scale_na_reason` allowed on
    Pipeline-A null fidelity rows).
    (4) `test_no_leakage_sentinel` — `tstr.json` real_only_baseline R2 is
    NEGATIVE and `augmentation.json` real_only-condition R2 is NEGATIVE
    (a positive value => eval/train leakage, Pitfall 5); assert
    `n_train_real==65`, `n_eval_real==320`.
    (5) `test_sample_shape_invariant` — `reconstruct_od('wgan_mlp','B',42)
    ["od_samples"].shape == (3840,10)` and dtype float64 (parametrize over a
    few model_kinds/pipelines).
    (6) `test_timegan_metadata` — `predictive_discriminative.json` metadata
    has `timegan_reference_url` containing `jsyoon0823/TimeGAN`,
    `hidden_dim==10`, and a non-trivial `univariate_adaptation` string (A1).
    (7) `test_dualscale_coverage` — `fidelity_dualscale.json` has both
    `scale` values; Pipeline-B `log_return` EMD rows are non-null; Pipeline-A
    `log_return` rows are explicit `value is None` with `scale_na_reason`.
    (8) `test_core_untouched` — `subprocess` `git diff --stat revision/core/`
    output is empty (D-11-10 hard invariant).
    (9) `test_phase10_reproduction` — recompute quantum|B OD-EMD via the
    verbatim `reconstruct_od` + `revision.core.eval.compute_emd` and assert it
    is within tolerance of the Phase 10 anchor `0.0276 +/- 0.0046`
    (proves verbatim reuse did not drift; mark `xfail`-soft only if the
    Phase 10 anchor JSON is unavailable, otherwise hard-assert).
    Keep tests fast (reuse already-written JSONs; only #5/#9 recompute, on a
    small subset).
  </action>
  <verify>
    <automated>cd /Users/shawngibford/dev/phd/qGAN && ./qgan_env/bin/python -m pytest revision/tests/test_utility.py -q 2>&1 | tail -15</automated>
  </verify>
  <done>
    `revision/tests/test_utility.py` exists; all test functions pass;
    leakage/shape/hash/core-untouched/dual-scale/TimeGAN-metadata/Phase-10-
    reproduction sentinels are codified and green.
  </done>
  <acceptance_criteria>
    - `./qgan_env/bin/python -m pytest revision/tests/test_utility.py -q` exits 0 (all tests pass).
    - `test_data_hash_consistency` asserts all four JSONs carry `data_hash == "91e447d4624e25b3"`.
    - `test_no_leakage_sentinel` fails if real-only R2 is non-negative (leakage guard — Pitfall 5).
    - `test_sample_shape_invariant` asserts `(3840,10)` float64 from the verbatim `reconstruct_od`.
    - `test_timegan_metadata` asserts `jsyoon0823/TimeGAN` URL + `hidden_dim==10` + non-empty adaptation rationale.
    - `test_dualscale_coverage` asserts explicit `scale` on every fidelity row and Pipeline-A `log_return` explicit-null.
    - `test_core_untouched` asserts `git diff --stat revision/core/` is empty.
    - `test_phase10_reproduction` asserts quantum|B OD-EMD within `0.0276 +/- 0.0046` of the Phase 10 anchor.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Full-suite regression run + Phase 11 closeout assertion</name>
  <files>revision/tests/test_utility.py</files>
  <read_first>
    - revision/tests/ (full existing suite — test_classical.py, test_nonadversarial.py, test_utility.py)
    - .planning/phases/11-utility-evaluation/11-RESEARCH.md (Test Framework -> Full suite command)
    - .planning/ROADMAP.md (Phase 11 Success Criteria 1-4 — the closeout contract)
  </read_first>
  <action>
    Run the FULL existing test suite to prove Phase 11 introduced no
    regression in the prior Phase 8-10 modules:
    `./qgan_env/bin/python -m pytest revision/tests/ -q`. Confirm
    `test_classical.py` / `test_nonadversarial.py` still pass alongside the
    new `test_utility.py`. Then assert the four ROADMAP Phase 11 Success
    Criteria are each backed by a concrete artifact: SC-1 -> `tstr.json` rows
    with r2/mae/rmse; SC-2 -> `predictive_discriminative.json` scores block
    with mean/std over 5 seeds; SC-3 -> `augmentation.json` delta rows with
    injection_ratio; SC-4 -> `fidelity_dualscale.json` explicit dual `scale`.
    Add a small `test_phase11_success_criteria` aggregator test (or extend the
    suite) that programmatically checks all four artifacts satisfy their SC.
    Do NOT modify the Wave-1 drivers or `revision/core/`; this task only adds
    the aggregator test and runs the full suite.
  </action>
  <verify>
    <automated>cd /Users/shawngibford/dev/phd/qGAN && ./qgan_env/bin/python -m pytest revision/tests/ -q 2>&1 | tail -8 && git diff --stat revision/core/ | wc -l | tr -d ' '</automated>
  </verify>
  <done>
    Full `revision/tests/` suite green (no Phase 8-10 regression);
    `test_phase11_success_criteria` confirms all four ROADMAP SC are
    artifact-backed; `git diff revision/core/` byte-count is 0.
  </done>
  <acceptance_criteria>
    - `./qgan_env/bin/python -m pytest revision/tests/ -q` exits 0 (entire suite, no regressions).
    - A `test_phase11_success_criteria` test asserts each of ROADMAP SC 1-4 maps to a populated artifact (tstr/predictive_discriminative/augmentation/fidelity_dualscale).
    - `git diff --stat revision/core/` produces zero lines (D-11-10 final invariant).
    - No Wave-1 driver file or `revision/core/` file modified by this plan (only `revision/tests/test_utility.py`).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Wave-1 JSON outputs -> test suite | tests treat the JSONs as the artifact-of-record Phase 14 will cite; integrity faults must fail loudly |
| prior-phase modules -> full suite | Phase 11 must not silently regress Phase 8-10 behavior |

Local, offline, no network/auth/PII. Risks are scientific-integrity
(undetected drift / leakage / silent core edit) — this plan is the guard.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-10 | Repudiation | undetected metric/leakage drift in published artifacts | mitigate | sentinels codified as pytest (leakage/shape/hash/Phase-10 reproduction) — Task 1 |
| T-11-04 | Tampering | core-module invariant (D-11-10) | mitigate | `test_core_untouched` pytest + Task 2 byte-count gate |
| T-11-11 | Tampering | silent regression of Phase 8-10 modules | mitigate | full `revision/tests/` suite run (Task 2) |
| T-11-SC | Tampering | npm/pip installs | accept | zero installs; pytest already in qgan_env (RESEARCH Environment Availability) |
</threat_model>

<verification>
- `./qgan_env/bin/python -m pytest revision/tests/ -q` passes (full suite, including the three pre-existing test files + new `test_utility.py`).
- All scientific-integrity sentinels (data-hash, leakage, sample-shape, dual-scale, TimeGAN-metadata, core-untouched, Phase-10 reproduction) are green.
- `git diff --stat revision/core/` empty (D-11-10).
</verification>

<success_criteria>
- All four Phase 11 ROADMAP Success Criteria are artifact-backed and verified by an executable suite.
- No Phase 8-10 regression; `revision/core/` untouched across the whole phase.
</success_criteria>

<output>
Create `.planning/phases/11-utility-evaluation/11-04-SUMMARY.md` when done.
</output>
