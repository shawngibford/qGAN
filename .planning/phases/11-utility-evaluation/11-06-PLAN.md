---
phase: 11-utility-evaluation
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - revision/run_dualscale_fidelity.py
autonomous: true
gap_closure: true
requirements: [EVAL-05]
must_haves:
  truths:
    - "CR-01 closed: run_dualscale_fidelity.py contains no hardcoded machine-specific absolute path; the canonical-checkout fallback is opt-in via the QGAN_CANONICAL_REPO env var only"
    - "Driver fails loudly with an actionable message naming QGAN_CANONICAL_REPO when a frozen run dir is absent in-tree and the env var is unset"
    - "Cross-checkout artifact mixing cannot pass silently: a single fidelity emission asserts all resolved run dirs share one root, failing loudly if a worktree run and a fallback run are mixed"
    - "EVAL-05 not regressed: fidelity_dualscale.json reproduces with data_hash 91e447d4624e25b3 and 3360 rows; existing pytest suite still passes"
  artifacts:
    - path: "revision/run_dualscale_fidelity.py"
      provides: "Env-var-based canonical-repo resolver, fail-loud _resolve_run_dir, single-root provenance assertion"
      contains: "QGAN_CANONICAL_REPO"
  key_links:
    - from: "revision/run_dualscale_fidelity.py::_resolve_run_dir"
      to: "os.environ['QGAN_CANONICAL_REPO']"
      via: "opt-in fallback resolution"
      pattern: "QGAN_CANONICAL_REPO"
---

<objective>
Close CR-01: remove the hardcoded `_CANONICAL_REPO_FALLBACK = Path("/Users/shawngibford/dev/phd/qGAN")` from `revision/run_dualscale_fidelity.py`, replace it with an opt-in `QGAN_CANONICAL_REPO` env-var resolver that fails loudly with guidance, and add a single-root provenance assertion so a fidelity emission can never silently mix frozen artifacts from two different checkouts. This is also the open HUMAN-UAT reproducibility item (`11-HUMAN-UAT.md` test 1).

Purpose: The hardcoded home-directory path structurally breaks the Phase-11 cross-machine reproducibility guarantee — on any other machine/CI runner the fallback silently fails or could mix a stale checkout's artifacts into `fidelity_dualscale.json` with no loud failure. This is the single critical (CR-01) finding and the one pending human-UAT blocker.

Output: A portable `revision/run_dualscale_fidelity.py` whose artifact provenance is verifiable off the author's machine.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-utility-evaluation/11-REVIEW.md
@.planning/phases/11-utility-evaluation/11-HUMAN-UAT.md
@.planning/phases/11-utility-evaluation/11-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace hardcoded fallback with QGAN_CANONICAL_REPO env resolver + single-root provenance assertion</name>
  <files>revision/run_dualscale_fidelity.py</files>
  <read_first>
    - revision/run_dualscale_fidelity.py lines 60-170 (imports, `_CANONICAL_REPO_FALLBACK` at :112, `_find_repo_root`, `_resolve_run_dir`, `_run_base`)
    - .planning/phases/11-utility-evaluation/11-REVIEW.md section CR-01 (lines 50-111 — includes the exact suggested env-var resolver and single-root assertion requirement)
    - .planning/phases/11-utility-evaluation/11-HUMAN-UAT.md (the pending reproducibility test this closes)
    - .planning/phases/11-utility-evaluation/11-03-SUMMARY.md (D-11-08 no-regeneration constraint and how the 60 frozen run dirs are consumed in emit_rows)
  </read_first>
  <action>
    Add `import os` at the module top (alongside the existing stdlib imports near line 60-64) if not already present. Replace the constant assignment at `revision/run_dualscale_fidelity.py:112` — currently `_CANONICAL_REPO_FALLBACK = Path("/Users/shawngibford/dev/phd/qGAN")` — with an opt-in resolver: `_CANONICAL_REPO_FALLBACK = (Path(os.environ["QGAN_CANONICAL_REPO"]).resolve() if os.environ.get("QGAN_CANONICAL_REPO") else None)`. The literal string `/Users/shawngibford/dev/phd/qGAN` must not remain anywhere in the file.

    Rewrite `_resolve_run_dir` (currently `:134-151`) so that: (1) it returns `repo / rel` if that exists; (2) else, only if `_CANONICAL_REPO_FALLBACK is not None`, it tries `_CANONICAL_REPO_FALLBACK / rel` and returns it if it exists; (3) else it raises `FileNotFoundError` whose message names the missing in-tree path AND explicitly instructs the operator to set `QGAN_CANONICAL_REPO` to a checkout containing the frozen bundles, and notes D-11-08 forbids regeneration. Do not silently swallow the missing-env case.

    Add a single-root provenance guard: in the function that loops over the 60 (model_kind, pipeline, seed) run dirs to emit fidelity rows (the `emit_rows` loop, follow `_run_base` / `_resolve_run_dir` call sites), collect the resolved root for each run (the parent above the `revision/results/...` rel prefix — i.e. `repo` vs `_CANONICAL_REPO_FALLBACK`). After resolving all run dirs for a single emission, assert that the set of distinct roots has size <= 1: `assert len(resolved_roots) <= 1, f"fidelity emission mixed frozen artifacts from multiple checkouts: {sorted(resolved_roots)} — refusing to assemble fidelity_dualscale.json from inconsistent provenance"`. Implement this by recording each resolved base's root (you may compare `str(resolved).startswith(str(repo))` vs the fallback) into a set as runs are resolved; raise before any JSON is written if more than one distinct root contributed. Do not modify `reconstruct_od`, the `np.random.default_rng(seed*7919+1)` od_start draw, the metric helper imports from `revision/core/eval`, or any computed metric.
  </action>
  <verify>
    <automated>cd /Users/shawngibford/dev/phd/qGAN && ! grep -nF '/Users/shawngibford/dev/phd/qGAN' revision/run_dualscale_fidelity.py && grep -nF 'QGAN_CANONICAL_REPO' revision/run_dualscale_fidelity.py && grep -nE 'os\.environ' revision/run_dualscale_fidelity.py && QGAN_CANONICAL_REPO= python3 -c "import ast,sys; ast.parse(open('revision/run_dualscale_fidelity.py').read())" && /opt/homebrew/bin/pytest revision/tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nF '/Users/shawngibford/dev/phd/qGAN' revision/run_dualscale_fidelity.py` returns no match (exit 1)
    - `run_dualscale_fidelity.py` references `os.environ` and the exact env var name `QGAN_CANONICAL_REPO`
    - `_CANONICAL_REPO_FALLBACK` evaluates to `None` when `QGAN_CANONICAL_REPO` is unset (no hardcoded path)
    - `_resolve_run_dir` raises `FileNotFoundError` with a message mentioning `QGAN_CANONICAL_REPO` when the in-tree path is absent and the env var is unset
    - A single-root assertion exists that fails loudly with a message mentioning mixed checkouts / inconsistent provenance when a fidelity emission resolves run dirs from more than one root
    - `git diff --stat -- revision/core/` is empty
    - `revision/results/fidelity_dualscale.json` `data_hash` is still `91e447d4624e25b3` and the file still has 3360 rows
    - `pytest revision/tests/ -q` exits 0
  </acceptance_criteria>
  <done>CR-01 closed and the open HUMAN-UAT reproducibility item resolvable: the driver is portable (env-var opt-in), fails loudly with guidance off-box, and cannot silently mix cross-checkout artifacts.</done>
</task>

</tasks>

<threat_model>
No new external attack surface — internal evaluation driver fix; no network, auth, or untrusted input. The env var `QGAN_CANONICAL_REPO` is operator-supplied local config, not untrusted input. Primary risk is scientific-integrity regression (data_hash change or silent cross-checkout mixing) — directly mitigated by this plan's single-root provenance assertion plus the data_hash invariant acceptance criterion and the full pytest gate.
</threat_model>

<verification>
`git diff --stat -- revision/core/` empty; `fidelity_dualscale.json` data_hash == 91e447d4624e25b3 with 3360 rows; `pytest revision/tests/ -q` exits 0; no `/Users/shawngibford` literal remains in the file.
</verification>

<success_criteria>
CR-01 closed; HUMAN-UAT reproducibility test 1 now resolvable (fail-loud + env-var opt-in); EVAL-05 not regressed; data_hash invariant preserved; existing pytest suite green.
</success_criteria>

<output>
Create `.planning/phases/11-utility-evaluation/11-06-SUMMARY.md` when done.
</output>
