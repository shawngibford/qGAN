---
phase: 11-utility-evaluation
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - revision/run_utility.py
autonomous: true
gap_closure: true
requirements: [EVAL-01, EVAL-04]
must_haves:
  truths:
    - "WR-01 closed: the shape comment in _real_windowed_od is derived/correct and consistent with the n_train_real==65 (385-window) invariant — no literal `(384,10)` remains"
    - "WR-02 closed: r2_score_inline returns NaN (not 0.0) on a zero-variance eval target, and train_eval_tstr asserts eval-set non-degeneracy before training so a degenerate slice fails loudly instead of confusing the <0 leakage sentinel"
    - "WR-03 closed: the augmentation subsample seed is collision-free and fully-qualified by (model_kind, pipeline, label), recorded verbatim in subsample_rng_seed, and no longer derived from the lossy int(ratio*1000)"
    - "WR-04 closed: the injection grid asserts non-degeneracy against the actual synth pool size so +100% and synthetic_only can never silently collapse to identical training sets"
    - "EVAL-01 and EVAL-04 not regressed: tstr.json + augmentation.json data_hash stays 91e447d4624e25b3 and the existing pytest suite still passes"
  artifacts:
    - path: "revision/run_utility.py"
      provides: "Corrected shape comment, NaN-on-degenerate r2, collision-free subsample seed, non-degenerate-grid assertion"
      contains: "default_rng"
  key_links:
    - from: "revision/run_utility.py::train_eval_tstr"
      to: "revision/run_utility.py::r2_score_inline"
      via: "non-degeneracy guard before training"
      pattern: "std\\(\\) > 0|ss_tot == 0"
---

<objective>
Close the four localized correctness/reproducibility warnings in `revision/run_utility.py` (WR-01 stale shape comment, WR-02 degenerate-R2 masking, WR-03 lossy subsample seed, WR-04 unguarded injection-grid collapse) without changing any produced number, the `data_hash`, or `revision/core/`.

Purpose: WR-01 misleads readers reconciling the load-bearing 385-window partition; WR-02 lets a degenerate eval slice surface as a confusing leakage-sentinel failure instead of its true cause; WR-03/WR-04 make the augmentation grid silently collapse or mis-seed on any future pool/grid change. These are scientific-integrity hardening fixes for a reproducibility artifact.

Output: A hardened `revision/run_utility.py` with identical produced metrics and `data_hash`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-utility-evaluation/11-REVIEW.md
@.planning/phases/11-utility-evaluation/11-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix WR-01 (shape comment) and WR-02 (degenerate-R2 masking)</name>
  <files>revision/run_utility.py</files>
  <read_first>
    - revision/run_utility.py lines 180-256 (current `_real_windowed_od`, `r2_score_inline`, `train_eval_tstr`)
    - .planning/phases/11-utility-evaluation/11-REVIEW.md sections WR-01 (lines 115-130) and WR-02 (lines 132-153)
    - .planning/phases/11-utility-evaluation/11-VERIFICATION.md (lines 56, 137 — the n_train_real==65 / HELD_OUT_N=320 invariant and the INFO note on the 0.0 fallback)
  </read_first>
  <action>
    WR-01: In `_real_windowed_od` (currently `revision/run_utility.py:187`), replace the trailing `# (384,10)` comment with a derived, correct annotation that states the true window count. Use the formula form: shape == ((len(OD)-WINDOW_LENGTH)//2 + 1, WINDOW_LENGTH) == (385,10); downstream `[HELD_OUT_N:]` with HELD_OUT_N=320 leaves 65 training windows. Do not change the `rolling_window(d_real["OD"], WINDOW_LENGTH, 2)` call itself — only the comment. The literal substring `(384,10)` must not remain anywhere in the file.

    WR-02: In `r2_score_inline` (currently `:216-219`), change the `else 0.0` branch to `else float("nan")` so a zero-variance eval target yields NaN (matching sklearn `r2_score` behavior) instead of a plausible-looking 0.0 that defeats the strict `<0` leakage sentinel. Additionally, in `train_eval_tstr` (currently `:222-255`), before the training loop, add an explicit non-degeneracy guard on the held-out target column: `assert float(eval_windows[:, 9:10].std()) > 0.0, "degenerate (zero-variance) eval target — TSTR R2 undefined"`. This keeps every computed number byte-identical (current data is non-degenerate) while making a future degenerate slice fail loudly at its true cause rather than as a confusing leakage-test failure. Do NOT modify the `mse`/`mae`/`rmse` computations.
  </action>
  <verify>
    <automated>cd /Users/shawngibford/dev/phd/qGAN && ! grep -nF '(384,10)' revision/run_utility.py && grep -nF 'float("nan")' revision/run_utility.py && grep -nE 'eval_windows\[:, 9:10\]\.std\(\) > 0' revision/run_utility.py && /opt/homebrew/bin/pytest revision/tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nF '(384,10)' revision/run_utility.py` returns no match (exit 1)
    - The `_real_windowed_od` comment references the derived formula `((len(OD)-WINDOW_LENGTH)//2 + 1, WINDOW_LENGTH)` and the value `385` and `65`
    - `r2_score_inline` returns `float("nan")` (not `0.0`) when `ss_tot == 0`
    - `train_eval_tstr` contains an `assert` on `eval_windows[:, 9:10].std() > 0` placed before the training loop
    - `git diff --stat -- revision/core/` is empty
    - `revision/results/tstr.json` and `revision/results/augmentation.json` `data_hash` is still `91e447d4624e25b3`
    - `pytest revision/tests/ -q` exits 0
  </acceptance_criteria>
  <done>WR-01 and WR-02 are closed; no number changes; the existing 22-test suite still passes.</done>
</task>

<task type="auto">
  <name>Task 2: Fix WR-03 (lossy subsample seed) and WR-04 (unguarded grid collapse)</name>
  <files>revision/run_utility.py</files>
  <read_first>
    - revision/run_utility.py lines 378 (_INJECTION_GRID) and 405-490 (augmentation loop: synth_pool, the +25/+50/+100 loop, synthetic_only, delta table)
    - .planning/phases/11-utility-evaluation/11-REVIEW.md sections WR-03 (lines 155-172) and WR-04 (lines 174-188)
    - .planning/phases/11-utility-evaluation/11-01-SUMMARY.md (augmentation driver design + the recorded subsample_rng_seed field contract)
  </read_first>
  <action>
    WR-03: Replace the lossy `np.random.default_rng(int(ratio * 1000) + 1)` (currently `:446`) with a stable, collision-free, fully-qualified seed derived from `(mk, p, label)`. Compute `seed_int = (zlib.crc32(f"augsub|{mk}|{p}|{label}".encode()) & 0xFFFFFFFF)` (add `import zlib` at module top alongside the other stdlib imports) and use `sub_rng = np.random.default_rng(seed_int)`. Record the exact integer in the emitted condition dict: replace the `"subsample_rng_seed": int(int(ratio * 1000) + 1)` field (currently `:462`) with `"subsample_rng_seed": int(seed_int)` and add a sibling field `"subsample_rng_seed_derivation": "crc32('augsub|{mk}|{p}|{label}') & 0xFFFFFFFF"`. The seed governs ONLY which synthetic windows are subsampled for the augmentation mix — it does NOT touch `load_and_preprocess`, the canonical OD tensor, or any `data_hash` input, so the invariant is preserved (the produced augmentation numbers MAY change because the subsample changes; that is acceptable and correct — the prior seed was the defect. The leakage sentinel — all real_only R2 negative — must still hold).

    WR-04: Immediately after `n_synth = int(round(ratio * n_real_train))` (currently `:444`) inside the `_INJECTION_GRID` loop, add `assert n_synth < synth_pool.shape[0], (label, n_synth, synth_pool.shape, "injection grid collapsed — +100% would equal synthetic_only")`. This makes a shrunken pool fail loudly instead of silently emitting duplicated `+100%`/`synthetic_only` conditions. For the documented pool size (~3840) this assertion always passes, so no current number changes from WR-04 alone.
  </action>
  <verify>
    <automated>cd /Users/shawngibford/dev/phd/qGAN && grep -nE "crc32\(.*augsub" revision/run_utility.py && grep -nF "subsample_rng_seed_derivation" revision/run_utility.py && grep -nE "assert n_synth < synth_pool\.shape\[0\]" revision/run_utility.py && ! grep -nF "int(ratio * 1000) + 1" revision/run_utility.py && /opt/homebrew/bin/pytest revision/tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - `revision/run_utility.py` no longer contains the literal `int(ratio * 1000) + 1`
    - The subsample RNG is `np.random.default_rng(seed_int)` where `seed_int = zlib.crc32(f"augsub|{mk}|{p}|{label}".encode()) & 0xFFFFFFFF`
    - The emitted condition dict carries `subsample_rng_seed` (the int) and `subsample_rng_seed_derivation` (the formula string)
    - An `assert n_synth < synth_pool.shape[0]` exists inside the `_INJECTION_GRID` loop with a message mentioning grid collapse
    - All `real_only` R2 values in regenerated `augmentation.json` remain strictly negative (leakage sentinel intact) — verified by `pytest revision/tests/test_utility.py::test_no_leakage_sentinel`
    - `git diff --stat -- revision/core/` is empty
    - `revision/results/augmentation.json` `data_hash` is still `91e447d4624e25b3`
    - `pytest revision/tests/ -q` exits 0
  </acceptance_criteria>
  <done>WR-03 and WR-04 are closed; subsample seed is collision-free and qualified by (mk,p,label); grid-collapse is guarded; data_hash and leakage sentinel intact.</done>
</task>

</tasks>

<threat_model>
No new external attack surface — internal evaluation driver fixes; no network, auth, or untrusted input. Primary risk is scientific-integrity regression (data_hash change or leakage-sentinel violation), mitigated by the data_hash invariant acceptance criteria, the `test_no_leakage_sentinel` check, and the full `pytest revision/tests/ -q` gate on every task.
</threat_model>

<verification>
After both tasks: `git diff --stat -- revision/core/` empty; all 4 result JSON `data_hash == 91e447d4624e25b3`; `pytest revision/tests/ -q` exits 0; leakage sentinel (all real_only R2 < 0) holds.
</verification>

<success_criteria>
WR-01, WR-02, WR-03, WR-04 all closed in `revision/run_utility.py`; EVAL-01/EVAL-04 not regressed; data_hash invariant preserved; existing pytest suite green.
</success_criteria>

<output>
Create `.planning/phases/11-utility-evaluation/11-05-SUMMARY.md` when done.
</output>
