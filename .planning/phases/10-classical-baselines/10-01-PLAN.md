---
phase: 10-classical-baselines
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - core/models/classical.py
  - core/models/nonadversarial.py
  - core/models/__init__.py
autonomous: true
requirements: [BASE-01, BASE-02]
must_haves:
  truths:
    - "All 3 classical WGAN-GP generators expose count_params() empirically within 71..79 (D-10-02)"
    - "Each classical generator holds a single live nn.Parameter named params_pqc that the optimizer steps (D-10-11, Pitfall 1)"
    - "Each classical generator forward accepts (5,B) noise and returns (B,10) (train_wgan_gp contract)"
    - "VAE.count_params() reports its natural size and AR exposes phi/sigma2/p helpers (D-10-03, D-10-12)"
    - "The 10 Phase 09.1 quantum run dirs (A,B x seeds 42-46) exist on disk (D-10-04 precondition)"
  artifacts:
    - path: "core/models/classical.py"
      provides: "WGANMLPGenerator, WGANCNNGenerator, WGANLSTMGenerator with params_pqc + count_params"
      contains: "class WGANMLPGenerator"
    - path: "core/models/nonadversarial.py"
      provides: "VAEBaseline (nn.Module, ELBO-ready) + ARBaseline (fit/sample helpers)"
      contains: "class VAEBaseline"
    - path: "core/models/__init__.py"
      provides: "barrel import exposing classical, nonadversarial"
      contains: "classical"
  key_links:
    - from: "core/models/classical.py"
      to: "core/training.py::train_wgan_gp"
      via: "params_pqc single nn.Parameter + forward((5,B))->(B,10)"
      pattern: "params_pqc"
---

<objective>
Implement the 5 new model definitions Phase 10 needs, satisfying the exact `train_wgan_gp` interface contract, and verify the Phase 09.1 quantum run directories (the comparison-table reference column) exist on disk.

Purpose: BASE-01 requires matched-parameter classical WGAN-GP generators; BASE-02 requires a non-adversarial baseline. Model definitions are the foundation every later wave builds on. The `params_pqc` contract (RESEARCH Pitfall 1) is the single highest-risk interface decision in the phase — get it right here so Wave 2/3 plug in unchanged.
Output: `core/models/classical.py`, `core/models/nonadversarial.py`, updated `core/models/__init__.py`, and a verified precondition that the 10 quantum run dirs exist.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-classical-baselines/10-CONTEXT.md
@.planning/phases/10-classical-baselines/10-RESEARCH.md
@.planning/phases/10-classical-baselines/10-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement 3 classical WGAN-GP generators in classical.py with params_pqc single-nn.Parameter functional design</name>
  <files>core/models/classical.py, core/models/__init__.py</files>
  <read_first>
    - core/models/quantum.py (the analog; copy the interface surface — lines 53-70 num_qubits/window_length/params_pqc declaration, lines 80-85 count_params(), lines 173-202 forward returning (batch, window_length))
    - core/training.py lines 220-360 (the loop's hard contract: line 234 `Adam([generator.params_pqc])`, lines 282-283 `generator(noise_batch)` then `.to(float64) * 0.1`, lines 228-229 getattr num_qubits/window_length, line 255 _ESAdapter reads params_pqc)
    - core/models/__init__.py (current 3-line barrel — only quantum, critic)
    - .planning/phases/10-classical-baselines/10-RESEARCH.md §"Standard Stack — Parameter Arithmetic Recipes" (lines 156-215) and §"Pitfall 1" (lines 456-464) and §"Pitfall 2" (lines 466-471)
  </read_first>
  <behavior>
    - WGANMLPGenerator().count_params() == 74 and == sum(p.numel() for p in module.parameters())
    - WGANCNNGenerator().count_params() == 73 and == sum(p.numel() for p in module.parameters())
    - WGANLSTMGenerator().count_params() == 78 and == sum(p.numel() for p in module.parameters())
    - For all 3: params_pqc is a single nn.Parameter; module.parameters() yields exactly that one tensor; gen(torch.randn(5,12)).shape == (12,10)
    - After one optimizer step on params_pqc, params_pqc values change (autograd flows — not detached)
  </behavior>
  <action>
    Create `core/models/classical.py` with three nn.Module subclasses: `WGANMLPGenerator`, `WGANCNNGenerator`, `WGANLSTMGenerator`. Each sets class attributes `num_qubits = 5` and `window_length = 10`. Each holds ALL trainable weights as a SINGLE flat `nn.Parameter` named `self.params_pqc` (this is the train_wgan_gp:234 contract — `torch.optim.Adam([generator.params_pqc])` steps exactly one tensor; a @property returning a flattened copy detaches autograd per Pitfall 1, do NOT use that). Implement `forward(noise)` functionally: accept `(5,B)` noise, transpose to `(B,5)` latent, slice `self.params_pqc` into per-layer weight/bias views and apply via `torch.nn.functional` ops (`F.linear`, `F.conv1d`, `F.conv_transpose1d`, manual LSTM cell, etc.), returning `(B,10)`. Add `count_params(self) -> int` returning `self.params_pqc.numel()` (mirrors quantum.py:80-85 signature; quantum target is 75, range 71-79 per D-10-02).
    Exact layer arithmetic from RESEARCH (latent dim 5, output length 10):
    - MLP: Linear(5,4)+bias -> Tanh -> Linear(4,10)+bias = (5*4+4)+(4*10+10) = 74 params. params_pqc length 74.
    - CNN: view (B,1,5) -> ConvTranspose1d(1,9,kernel=6,stride=1)+bias -> param-free LeakyReLU/Tanh -> Conv1d(9,1,kernel=1)+bias -> view (B,10) = (1*9*6+9)+(9*1*1+1) = 73 params. params_pqc length 73. Use a parameter-free deterministic reshape from (B,5) to (B,1,5) (NOT a learnable projection — that blows the budget, anti-pattern RESEARCH line 423).
    - LSTM: parameter-free tile/pad (B,5) -> (B,seq=3,2); LSTM(input_size=2,hidden_size=2,num_layers=1,bias=True) = 4*(2*2+2*2+2+2)=48 params; last hidden (B,2) -> Linear(2,10)+bias = 30 params; total 78. params_pqc length 78. Implement the LSTM cell functionally from sliced weights (do not use nn.LSTM holding its own parameters — all weights must live in the single params_pqc tensor). The PyTorch two-bias-vector trap (Pitfall 2) does not apply here since you carve biases yourself, but the empirical assertion in Task 2 is still mandatory.
    Initialize `params_pqc` with `torch.randn(N) * _INIT_SCALE` mirroring quantum.py's init style (small scale); document the exact slice layout in each class docstring. Then modify `core/models/__init__.py`: change the import line to `from revision.core.models import quantum, critic, classical, nonadversarial` and `__all__ = ["quantum", "critic", "classical", "nonadversarial"]` (per PATTERNS.md exact-pattern; nonadversarial is created in Task 3 of this same plan so the import resolves).
  </action>
  <verify>
    <automated>./qgan_env/bin/python -c "import torch; from revision.core.models.classical import WGANMLPGenerator as M, WGANCNNGenerator as C, WGANLSTMGenerator as L;
mm,cc,ll=M(),C(),L();
assert mm.count_params()==74==sum(p.numel() for p in mm.parameters()), mm.count_params();
assert cc.count_params()==73==sum(p.numel() for p in cc.parameters()), cc.count_params();
assert ll.count_params()==78==sum(p.numel() for p in ll.parameters()), ll.count_params();
import inspect
for g in (mm,cc,ll):
  assert sum(1 for _ in g.parameters())==1, 'must be exactly one nn.Parameter (params_pqc)'
  assert g.params_pqc.requires_grad
  o=g(torch.randn(5,12)); assert tuple(o.shape)==(12,10), o.shape
  before=g.params_pqc.detach().clone(); opt=torch.optim.Adam([g.params_pqc],lr=0.1); loss=g(torch.randn(5,4)).pow(2).mean(); loss.backward(); opt.step()
  assert not torch.allclose(before,g.params_pqc.detach()), 'params_pqc not updated -> autograd detached (Pitfall 1)'
print('OK classical 74/73/78, single-param, autograd-live')"</automated>
  </verify>
  <acceptance_criteria>
    - `./qgan_env/bin/python -c "from revision.core.models.classical import WGANMLPGenerator; print(WGANMLPGenerator().count_params())"` outputs `74`
    - `WGANCNNGenerator().count_params()` outputs `73`; `WGANLSTMGenerator().count_params()` outputs `78`; each in [71,79] and within ±5% of 75
    - For each generator: `sum(1 for _ in g.parameters()) == 1` and the single parameter is named via `g.params_pqc`
    - `g(torch.randn(5,12)).shape == (12,10)` for all three
    - One Adam step on `[g.params_pqc]` changes `params_pqc` (autograd is live; Pitfall 1 negative test passes)
    - `./qgan_env/bin/python -c "from revision.core.models import classical, nonadversarial"` succeeds (barrel updated)
  </acceptance_criteria>
  <done>classical.py defines WGANMLPGenerator(74), WGANCNNGenerator(73), WGANLSTMGenerator(78), each a single-params_pqc functional nn.Module satisfying the train_wgan_gp contract; __init__.py barrel updated.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement VAEBaseline + ARBaseline in nonadversarial.py</name>
  <files>core/models/nonadversarial.py</files>
  <read_first>
    - core/models/quantum.py lines 80-85 (count_params() signature to mirror for VAEBaseline)
    - .planning/phases/10-classical-baselines/10-RESEARCH.md §"VAE Sizing" (lines 217-239) and §"AR Baseline" (lines 241-267) and §"Pitfall 6" (lines 494-499)
    - .planning/phases/10-classical-baselines/10-PATTERNS.md "nonadversarial.py" section (D-10-13 — only model definitions here; ELBO loop + AR fit/sample orchestration live in run_baselines.py, NOT here)
  </read_first>
  <behavior>
    - VAEBaseline().count_params() ≈ 562 and == sum(p.numel() for p in module.parameters())
    - VAEBaseline.encode(x[B,10]) returns (mu[B,4], logvar[B,4]); VAEBaseline.decode(z[B,4]) returns (B,10); reparameterize(mu,logvar) returns (B,4)
    - ARBaseline.fit(x_1d) sets phi (shape (p,)), sigma2 (float), p (int=2); ARBaseline.sample(n, rng) returns (n,10)
    - AR parameter count == p+1 == 3
  </behavior>
  <action>
    Create `core/models/nonadversarial.py` with `VAEBaseline(nn.Module)` and `ARBaseline` (a plain class, NOT nn.Module — it is fit/sample helpers).
    VAEBaseline architecture (RESEARCH §VAE Sizing lines 217-229, D-10-03 "smallest deep VAE"): encoder `Linear(10,16) -> ReLU -> [Linear(16,4) mu, Linear(16,4) logvar]`; latent dim Lz=4; decoder `Linear(4,16) -> ReLU -> Linear(16,10)` (no final activation; data in [-1,1]). Expose `encode(x)->(mu,logvar)`, `reparameterize(mu,logvar)->z` (`mu + eps*exp(0.5*logvar)`, eps~N(0,I)), `decode(z)->x_hat`, `forward(x)->(x_hat,mu,logvar)`, and `count_params()` mirroring quantum.py:80-85 returning `sum(p.numel() for p in self.parameters())` (≈562, reported transparently per D-10-03 — NOT param-matched). Do NOT put the ELBO loss here (D-10-13 — it lives in run_baselines.py in Wave 2). Add a `sample(n, rng)` helper that draws `z~N(0,I)` shape `(n,4)` and returns `decode(z)` shape `(n,10)` in [-1,1]-ish window space (NO `*0.1` — that is a quantum-output artifact per Pitfall 3; document this in the class docstring).
    ARBaseline (RESEARCH §AR Baseline lines 241-267, D-10-03 parameter-minimal): default order `p=2`. Methods: `fit(x_1d)` builds the lag design matrix and solves `np.linalg.lstsq` (X = stack of p lagged slices, y = x[p:]), stores `self.phi` (shape (p,)), `self.sigma2 = float(resid.var(ddof=0))`, `self.p`. `sample(n, rng)` recursively simulates n windows of length 10 (seed first p values from a real-window context or zeros+burn-in; for t in p..9: `x[t]=phi@x[t-1..t-p] + rng.normal(0,sqrt(sigma2))`), returns `(n,10)` in the same transformed/windowed space the other models emit (NO `*0.1`). `count_params()` returns `self.p + 1` as a plain int (== 3). Checkpoint serialization (`{phi,sigma2,p}` -> .npz) is done by run_baselines.py in Wave 2, not here.
  </action>
  <verify>
    <automated>./qgan_env/bin/python -c "import numpy as np, torch; from revision.core.models.nonadversarial import VAEBaseline, ARBaseline;
v=VAEBaseline(); n=sum(p.numel() for p in v.parameters());
assert v.count_params()==n, (v.count_params(),n); assert 540<=n<=580, n;
x=torch.randn(7,10); xh,mu,lv=v(x); assert tuple(xh.shape)==(7,10) and tuple(mu.shape)==(7,4) and tuple(lv.shape)==(7,4);
s=v.sample(5, torch.Generator()); assert tuple(s.shape)==(5,10);
rng=np.random.default_rng(0); a=ARBaseline(); a.fit(rng.standard_normal(400).astype('float64'));
assert a.phi.shape==(2,) and a.p==2 and a.count_params()==3 and a.sigma2>0;
ss=a.sample(6, np.random.default_rng(1)); assert ss.shape==(6,10);
print('OK nonadversarial VAE~'+str(n)+' AR p+1=3')"</automated>
  </verify>
  <acceptance_criteria>
    - `VAEBaseline().count_params()` outputs an int in [540,580] and equals `sum(p.numel() for p in module.parameters())`
    - `VAEBaseline().forward(torch.randn(7,10))` returns `(x_hat[7,10], mu[7,4], logvar[7,4])`
    - `VAEBaseline().sample(5, gen)` returns shape `(5,10)` with no `*0.1` applied (verified by docstring + Pitfall-3 smoke in Wave 2)
    - `ARBaseline().fit(x_1d)` sets `phi.shape==(2,)`, `sigma2>0`, `p==2`; `count_params()==3`
    - `ARBaseline().sample(6, rng)` returns shape `(6,10)`
    - No ELBO loop or AR-fit orchestration code in this file (D-10-13: grep file shows no training loop / no Adam / no lstsq-driven fit *loop* beyond the closed-form fit method)
  </acceptance_criteria>
  <done>nonadversarial.py defines VAEBaseline (~562 params, ELBO-ready interface) and ARBaseline (p=2, 3 params, lstsq fit + recursive sample), model-definitions only per D-10-13.</done>
</task>

<task type="auto">
  <name>Task 3: Verify Phase 09.1 quantum run-dir precondition (10 dirs) for the comparison-table reference column</name>
  <files>core/models/classical.py</files>
  <read_first>
    - .planning/phases/10-classical-baselines/10-CONTEXT.md D-10-04, D-10-18 (quantum runs reused as-is; the table reports the quantum reference)
    - .planning/phases/10-classical-baselines/10-RESEARCH.md §"Data-Hash" lines 283-290 (09.1 quantum runs at results/transform_ablation/runs/{A,B}/{seed}/)
  </read_first>
  <action>
    This is a precondition gate, not a code change — the `<files>` entry is nominal (no edit). Run the verification command below. It asserts all 10 Phase 09.1 quantum run directories exist with their core artifacts (config.yaml, samples.npy, inverse_kwargs.npz) for pipelines A and B × seeds {42,43,44,45,46}. These are the reference column of the Wave-4 comparison table (D-10-04/D-10-18) and are reused read-only (no quantum retraining). If ANY directory or core artifact is absent, STOP and report the phase BLOCKED in the SUMMARY (the comparison table cannot be assembled without them) — do not attempt to regenerate quantum runs (out of scope, D-10-04). Pipeline C dirs are intentionally ignored (D-10-05 dropped C).
  </action>
  <verify>
    <automated>./qgan_env/bin/python -c "
import pathlib, sys
base=pathlib.Path('results/transform_ablation/runs')
missing=[]
for p in ('A','B'):
    for s in (42,43,44,45,46):
        d=base/p/str(s)
        for f in ('config.yaml','samples.npy','inverse_kwargs.npz'):
            if not (d/f).exists(): missing.append(str(d/f))
if missing:
    print('BLOCKED missing quantum reference artifacts:'); [print(' ',m) for m in missing]; sys.exit(1)
print('OK all 10 Phase 09.1 quantum run dirs present (A,B x 42-46)')"</automated>
  </verify>
  <acceptance_criteria>
    - Command exits 0 and prints `OK all 10 Phase 09.1 quantum run dirs present`
    - Each of `results/transform_ablation/runs/{A,B}/{42,43,44,45,46}/` contains `config.yaml`, `samples.npy`, `inverse_kwargs.npz`
    - If blocked: SUMMARY explicitly records the phase as BLOCKED with the missing path list (no quantum regeneration attempted)
  </acceptance_criteria>
  <done>All 10 Phase 09.1 quantum run dirs verified present; the Wave-4 comparison-table reference column is available, or the phase is explicitly flagged BLOCKED.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem -> python | Reads existing Phase 09.1 run artifacts (read-only, project-authored, not external) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering | params_pqc property/copy detaching autograd (silent non-learning) | mitigate | Empirical Pitfall-1 negative test in Task 1 verify: one Adam step must change params_pqc |
| T-10-02 | Information disclosure | none — no PII, no secrets, no network in this phase | accept | Local research code; reads only project-authored artifacts |
| T-10-SC | Tampering | npm/pip/cargo installs | mitigate | RESEARCH §Package Legitimacy Audit: zero new packages installed; all deps pre-verified in ./qgan_env. No install tasks in this plan. |

No high-severity threats: pure model-definition code, no untrusted deserialization, no CLI arg parsing in this plan.
</threat_model>

<verification>
- `./qgan_env/bin/python -c "from revision.core.models import classical, nonadversarial"` imports cleanly
- count_params: WGANMLP=74, WGANCNN=73, WGANLSTM=78 (all in [71,79], ±5% of 75); VAE in [540,580]; AR=3 — all empirical via `sum(p.numel())`, never formula-only (Pitfall 2)
- All 10 Phase 09.1 quantum run dirs present
</verification>

<success_criteria>
- classical.py: 3 generators, single-params_pqc functional design, autograd-live, count in 71/73/74/78 empirically asserted
- nonadversarial.py: VAEBaseline (~562, ELBO-ready interface) + ARBaseline (p=2, 3 params), model-defs only (D-10-13)
- __init__.py barrel exposes classical, nonadversarial
- Phase 09.1 quantum reference column available (or phase explicitly BLOCKED)
</success_criteria>

<output>
Create `.planning/phases/10-classical-baselines/10-01-SUMMARY.md` when done
</output>
