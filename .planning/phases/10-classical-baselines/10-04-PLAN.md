---
phase: 10-classical-baselines
plan: 04
type: execute
wave: 4
depends_on: ["10-03"]
files_modified:
  - 06_baseline_comparison.ipynb
  - results/baseline_classical_wgan.json
  - results/baseline_nonadversarial.json
  - results/baseline_comparison.json
  - results/baseline_comparison.md
files_read:
  - _build_analysis_notebook.py
  - core/eval.py
autonomous: true
requirements: [BASE-01, BASE-02, BASE-03]
must_haves:
  truths:
    - "baseline_comparison.json has a long-form rows[] {model_kind,pipeline,seed,metric_name,scale,value} + a models[] array with parameter_count/family per model (D-10-16)"
    - "The table includes the quantum reference (5 seeds x 2 pipelines from Phase 09.1) AND all 5 new models on the same pipeline rows (D-10-18)"
    - "Every model's fidelity metrics are computed via revision.core.eval — no new metric helpers (D-10-20)"
    - "TSTR-lite (LSTM-32, 3 init seeds {40,41,42}, 320 held-out windows) is reported per model x pipeline (D-10-21)"
    - "data_hash is recomputed once from load_and_preprocess and verified equal across all 50 new configs; quantum equivalence stated by-construction (D-10-15, Pitfall 4)"
    - "baseline_comparison.md renders one row per model with params, OD-EMD mean±std, OD-ACF lag-1, OD-DTW mean, transformed-EMD (B), TSTR-lite R² (D-10-17)"
    - "NO new recommendation is emitted — only the apples-to-apples table (D-10-19)"
  artifacts:
    - path: "results/baseline_comparison.json"
      provides: "long-form rows[] + models[] aggregate across quantum + 5 new models x 2 pipelines x 5 seeds"
      contains: "models"
    - path: "results/baseline_comparison.md"
      provides: "markdown comparison table, one row per model per pipeline"
      contains: "param"
    - path: "results/baseline_classical_wgan.json"
      provides: "WGAN-GP variant metrics (BASE-01 artifact)"
    - path: "results/baseline_nonadversarial.json"
      provides: "VAE + AR metrics (BASE-02 artifact)"
    - path: "06_baseline_comparison.ipynb"
      provides: "aggregation notebook (reconstruct_od + eval + TSTR-lite, copied verbatim from 09.1)"
  key_links:
    - from: "06_baseline_comparison.ipynb"
      to: "results/baselines/runs + transform_ablation/runs"
      via: "load all 50 new runs + reuse 09.1 quantum runs; reconstruct_od + eval.py"
      pattern: "reconstruct_od"
---

<objective>
Build the `06_baseline_comparison.ipynb` aggregation notebook that loads all 50 new runs plus the reused Phase 09.1 quantum runs, reconstructs OD via the copied-verbatim `reconstruct_od`, computes the locked fidelity metric suite via `revision.core.eval`, runs the copied-verbatim TSTR-lite, verifies the data-hash invariant, and emits the BASE-01/02/03 artifacts.

Purpose: BASE-03 is the headline deliverable — the apples-to-apples comparison table (quantum / 3 classical WGAN-GP / VAE / AR) for both pipelines. This wave delivers it with NO new recommendation (D-10-19 — Phase 14 owns that, driven by Phase 11 utility numbers).
Output: `06_baseline_comparison.ipynb` + `baseline_classical_wgan.json`, `baseline_nonadversarial.json`, `baseline_comparison.{json,md}`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-classical-baselines/10-CONTEXT.md
@.planning/phases/10-classical-baselines/10-RESEARCH.md
@.planning/phases/10-classical-baselines/10-PATTERNS.md
@.planning/phases/10-classical-baselines/10-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build 06_baseline_comparison.ipynb — reconstruct_od + eval.py metrics over all 50 new runs + reused 09.1 quantum runs + data-hash verification</name>
  <files>06_baseline_comparison.ipynb</files>
  <read_first>
    - _build_analysis_notebook.py lines 95-149 (reconstruct_od — copy VERBATIM, keep A branch 105-114 + B branch 115-127, DROP the C branch 129-147)
    - core/eval.py (compute_emd / compute_acf / compute_dtw / compute_moments — the ONLY metric helpers permitted, D-10-20; no new helpers)
    - core/data.py (load_and_preprocess — for recomputing data_hash and real OD windows)
    - .planning/phases/10-classical-baselines/10-RESEARCH.md §"Comparison Table Schema" (lines 292-315) + §"Data-Hash" (lines 283-290, Pitfall 4 lines 480-485) + §"Pitfall 3" (sample-space already gated in Wave 2)
    - .planning/phases/10-classical-baselines/10-PATTERNS.md "06_baseline_comparison.ipynb" section (copy-anchor ranges; quantum-row reuse from runs/{A,B}/{seed}/, new-row from runs/<model>/{A,B}/{seed}/)
  </read_first>
  <action>
    Create `06_baseline_comparison.ipynb` (a notebook; an optional deterministic generator `_build_baseline_notebook.py` mirroring `_build_analysis_notebook.py` is acceptable but optional per RESEARCH line 379). Cells:
    1. Copy `reconstruct_od` VERBATIM from `_build_analysis_notebook.py:95-149` — keep the A branch (lines 105-114) and B branch (lines 115-127) exactly (identical inverse_kwargs contract); DELETE the C branch (lines 129-147, D-10-05). Parametrize the run-dir base so it accepts BOTH `results/baselines/runs/<model>/<pipeline>/<seed>/` (new models) AND `results/transform_ablation/runs/<pipeline>/<seed>/` (reused quantum, model_kind='quantum', D-10-04/18).
    2. For each (model_kind ∈ {quantum, wgan_mlp, wgan_cnn, wgan_lstm, vae, ar}) × pipeline ∈ {A,B} × seed ∈ {42..46}: load `samples.npy` + `inverse_kwargs.npz`, reconstruct OD via the copied `reconstruct_od`, compute the locked metric suite via revision.core.eval ONLY (D-10-20): OD-scale EMD, moments (mean/std/skew/kurtosis), per-lag ACF mean+std lags 0..9, DTW mean/median/std on the nearest-neighbor sub-sample, plus transformed-space EMD where applicable. Emit long-form rows `{model_kind, pipeline, seed, metric_name, scale, value}` (mirrors 09.1 metrics.csv + model_kind, D-10-16).
    3. data_hash verification (D-10-15, Pitfall 4): recompute `hashlib.sha256(load_and_preprocess('data.csv')["OD"].cpu().numpy().tobytes()).hexdigest()[:16]` ONCE; assert all 50 new `config.yaml` data_hash fields equal it. Quantum equivalence is established BY CONSTRUCTION (the 09.1 quantum runs used the same `load_and_preprocess` on the same `data.csv` — 09.1 wrote no data_hash field, so do NOT grep 09.1 configs; document the by-construction argument in a notebook markdown cell and in baseline_comparison.json).
    4. Build the top-level `models[]` array: `{kind, parameter_count, family, train_protocol_notes}` — parameter_count from each model's count_params() (quantum=75, wgan_mlp=74, wgan_cnn=73, wgan_lstm=78, vae≈562, ar=3), family per D-10-16; train_protocol_notes carries the `*0.1` asymmetry note for VAE/AR and any KL warmup applied (from Wave-2 config.yaml).
    Do NOT add new metric helpers to eval.py (D-10-20). Do NOT emit any recommendation on which baseline is best (D-10-19).
  </action>
  <verify>
    <automated>cd revision && ../qgan_env/bin/jupyter nbconvert --to notebook --execute --inplace 06_baseline_comparison.ipynb --ExecutePreprocessor.timeout=2400 && cd .. && ./qgan_env/bin/python -c "
import json
j=json.load(open('results/baseline_comparison.json'))
kinds={m['kind'] for m in j['models']}
assert kinds=={'quantum','wgan_mlp','wgan_cnn','wgan_lstm','vae','ar'}, kinds
pc={m['kind']:m['parameter_count'] for m in j['models']}
assert pc['quantum']==75 and pc['wgan_mlp']==74 and pc['wgan_cnn']==73 and pc['wgan_lstm']==78 and pc['ar']==3 and 540<=pc['vae']<=580, pc
rows=j['rows']; assert all({'model_kind','pipeline','seed','metric_name','scale','value'} <= set(r) for r in rows)
got={(r['model_kind'],r['pipeline']) for r in rows}
for k in kinds:
  for p in ('A','B'): assert (k,p) in got, ('missing rows for',k,p)
assert any(r['metric_name']=='emd' and r['scale']=='OD' for r in rows)
print('OK comparison.json: 6 models x 2 pipelines, long-form rows, param counts')"</automated>
  </verify>
  <acceptance_criteria>
    - `06_baseline_comparison.ipynb` executes top-to-bottom with no errors via nbconvert
    - `baseline_comparison.json` `models[]` has exactly {quantum,wgan_mlp,wgan_cnn,wgan_lstm,vae,ar} with parameter_count {75,74,73,78,≈562,3}
    - `rows[]` is long-form `{model_kind,pipeline,seed,metric_name,scale,value}` covering all 6 models × pipelines A and B (quantum reused from 09.1, D-10-18)
    - data_hash recomputed once and asserted equal across all 50 new configs; by-construction quantum-equivalence argument documented (no 09.1 config grep — Pitfall 4)
    - Metrics computed only via revision.core.eval (no new helper functions added to eval.py — D-10-20)
    - No recommendation text emitted (D-10-19)
  </acceptance_criteria>
  <done>06_baseline_comparison.ipynb produces a long-form rows[] + models[] aggregate over all 6 models × 2 pipelines × 5 seeds (quantum reused), with the data-hash invariant verified by construction.</done>
</task>

<task type="auto">
  <name>Task 2: TSTR-lite (copied verbatim) + emit baseline_classical_wgan.json, baseline_nonadversarial.json, baseline_comparison.md</name>
  <files>06_baseline_comparison.ipynb, results/baseline_classical_wgan.json, results/baseline_nonadversarial.json, results/baseline_comparison.md</files>
  <read_first>
    - _build_analysis_notebook.py lines 432-477 (TSTRLiteLSTM 432-440, r2_score_inline 442-445 sklearn-free, train_eval_tstr 447-477) and line 483 (HELD_OUT_N=320) — copy VERBATIM
    - .planning/phases/09.1-r1-m3-preprocessing-ablation/09.1-04-SUMMARY.md (tstr_lite.json schema precedent: mse_mean/std, r2_mean/std, per_init_seed)
    - .planning/phases/10-classical-baselines/10-RESEARCH.md §"TSTR-lite Scaffolding" (lines 317-328) + §"Comparison Table Schema" md columns (line 315) + D-10-17/D-10-21
  </read_first>
  <action>
    Extend `06_baseline_comparison.ipynb`:
    1. Copy `TSTRLiteLSTM`, `r2_score_inline`, `train_eval_tstr` VERBATIM from `_build_analysis_notebook.py:432-477`. Do NOT promote to core/ (D-10-13). Config: TSTRLiteLSTM hidden=32, 1-layer LSTM(input_size=1,hidden_size=32,batch_first=True)+Linear(32,1); input window `[:, :9]` → predict `[:, 9:10]`; 3 init seeds {40,41,42} (NOT training seeds); eval on first 320 real windows, train baseline on `real_windowed_OD[320:]` (HELD_OUT_N=320); Adam lr=1e-3, MSE, 50 epochs, batch 64; r2_score_inline = `1 - ss_res/ss_tot` (sklearn NOT installed — do not add it). Per model×pipeline: pool synthetic OD windows across the 5 seeds (`np.concatenate([recon[(m,p,s)] for s in SEEDS])`), train 3 LSTMs, report mse_mean/std + r2_mean/std + per_init_seed (matches 09.1 tstr_lite.json schema). Add these as TSTR-lite rows / a tstr block keyed by (model_kind,pipeline) into baseline_comparison.json.
    2. Emit `results/baseline_classical_wgan.json` — the BASE-01 artifact: the subset of rows[] + models[] for {wgan_mlp,wgan_cnn,wgan_lstm} (long-form schema, same as comparison.json filtered).
    3. Emit `results/baseline_nonadversarial.json` — the BASE-02 artifact: the subset for {vae,ar} including each model's train_protocol_notes (ELBO/beta + KL-warmup if applied; AR order p=2 rationale; the `*0.1` asymmetry note).
    4. Emit `results/baseline_comparison.md` — the D-10-17 markdown render: one row per model (aggregated over 5 seeds, per pipeline as blocks or a pipeline column) with columns: model | parameter_count | OD-EMD (mean±std) | OD-ACF lag-1 | OD-DTW mean | transformed-EMD (Pipeline B) | TSTR-lite R². Include the quantum reference row (D-10-18). No recommendation prose (D-10-19) — the markdown is a table + a one-line caption stating Phase 14 owns the highlight decision.
  </action>
  <verify>
    <automated>./qgan_env/bin/python -c "
import json, pathlib
cw=json.load(open('results/baseline_classical_wgan.json'))
na=json.load(open('results/baseline_nonadversarial.json'))
cmp=json.load(open('results/baseline_comparison.json'))
cwk={m['kind'] for m in cw['models']}; nak={m['kind'] for m in na['models']}
assert cwk=={'wgan_mlp','wgan_cnn','wgan_lstm'}, cwk
assert nak=={'vae','ar'}, nak
# TSTR-lite present per model x pipeline
tstr=cmp.get('tstr', cmp.get('tstr_lite'))
assert tstr is not None, 'TSTR-lite block missing from comparison.json'
md=pathlib.Path('results/baseline_comparison.md').read_text().lower()
for col in ('param','emd','acf','dtw','tstr'): assert col in md, col
for k in ('quantum','wgan_mlp','wgan_cnn','wgan_lstm','vae','ar'): assert k in md, k
assert 'recommend' not in md or 'phase 14' in md, 'no Phase-10 recommendation allowed (D-10-19)'
print('OK BASE-01/02/03 artifacts emitted; TSTR-lite present; md table complete; no Phase-10 recommendation')"</automated>
  </verify>
  <acceptance_criteria>
    - `baseline_classical_wgan.json` `models[]` == {wgan_mlp,wgan_cnn,wgan_lstm} (BASE-01)
    - `baseline_nonadversarial.json` `models[]` == {vae,ar} with train_protocol_notes documenting ELBO/beta/KL-warmup + AR p=2 + `*0.1` asymmetry (BASE-02)
    - `baseline_comparison.json` contains a TSTR-lite block keyed per (model_kind,pipeline) with mse_mean/std, r2_mean/std, per_init_seed (3 init seeds {40,41,42}, HELD_OUT_N=320) (D-10-21)
    - `baseline_comparison.md` is a table with columns params/OD-EMD(mean±std)/OD-ACF lag-1/OD-DTW mean/transformed-EMD(B)/TSTR-lite R², one row per model incl. quantum reference (D-10-17/18)
    - No recommendation prose unless explicitly deferring to Phase 14 (D-10-19)
    - TSTRLiteLSTM/r2_score_inline/train_eval_tstr are verbatim copies (not promoted to core/, D-10-13); sklearn not added
  </acceptance_criteria>
  <done>BASE-01 (baseline_classical_wgan.json), BASE-02 (baseline_nonadversarial.json), and BASE-03 (baseline_comparison.{json,md} with TSTR-lite) artifacts are emitted; the apples-to-apples table is complete with no Phase-10 recommendation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem -> np.load / torch.load | notebook loads samples.npy + checkpoint.pt/.npz from the sweep (project-authored, not external) |
| notebook -> result JSON | aggregate JSON/MD written to results/ |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-09 | Tampering | np.load(allow_pickle) / torch.load of sweep artifacts | accept | Artifacts are produced by this same Phase 10 sweep in the same repo; no external/untrusted inputs; load samples.npy with default allow_pickle=False where possible |
| T-10-10 | Tampering | silent-corruption: comparison aggregates incomparable cross-family numbers | mitigate | Cross-family sample-space gate already passed in Wave 2 (Pitfall 3); data_hash invariant re-verified here across all 50 configs (Pitfall 4 by-construction for quantum) |
| T-10-11 | Information disclosure | none — outputs are fidelity metrics, no PII/secrets | accept | Local research artifacts only |
| T-10-SC | Tampering | npm/pip/cargo installs | mitigate | Zero new packages (RESEARCH Package Legitimacy Audit); sklearn explicitly NOT added (inline r2_score_inline used) |

No high-severity threats: aggregation notebook over self-produced artifacts, no network/auth, no untrusted external deserialization.
</threat_model>

<verification>
- Notebook executes clean via nbconvert; baseline_comparison.json has 6 models × 2 pipelines long-form rows + models[] with correct param counts
- data_hash invariant verified across 50 configs; quantum by-construction documented
- BASE-01/02/03 artifacts emitted; TSTR-lite present; md table complete; no Phase-10 recommendation (D-10-19)
</verification>

<success_criteria>
- baseline_comparison.{json,md}: apples-to-apples quantum/wgan×3/vae/ar across A+B, params + full fidelity suite + TSTR-lite
- baseline_classical_wgan.json (BASE-01) + baseline_nonadversarial.json (BASE-02) emitted
- data-hash invariant proven; metrics via eval.py only; no new recommendation
</success_criteria>

<output>
Create `.planning/phases/10-classical-baselines/10-04-SUMMARY.md` when done
</output>
