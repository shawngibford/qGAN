---
phase: 10-classical-baselines
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - revision/run_baselines.py
files_read:
  - revision/run_ablation.py
  - revision/core/training.py
  - revision/core/models/classical.py
  - revision/core/models/nonadversarial.py
autonomous: true
requirements: [BASE-01, BASE-02]
must_haves:
  truths:
    - "python -m revision.run_baselines --model X --pipeline {A,B} --seed N --epochs M produces the 5-file bundle for every model kind (D-10-22)"
    - "WGAN models train via train_wgan_gp UNCHANGED with the same Critic + HPO constants (D-10-08)"
    - "VAE trains via a local ELBO loop, AR via lstsq fit — both outside the WGAN loop (D-10-09, D-10-13)"
    - "Every config.yaml carries data_hash = sha256(real_OD.tobytes())[:16] recomputed from load_and_preprocess (D-10-15)"
    - "A cross-family sample-space smoke gate proves wgan/vae/ar samples reconstruct into real OD range (Pitfall 3)"
  artifacts:
    - path: "revision/run_baselines.py"
      provides: "per-(model,pipeline,seed) CLI driver with WGAN/VAE/AR branches + 5-file bundle + data_hash"
      contains: "def main"
  key_links:
    - from: "revision/run_baselines.py"
      to: "revision/core/training.py::train_wgan_gp"
      via: "WGAN branch calls train_wgan_gp(gen, Critic(), loader, ...HPO consts...)"
      pattern: "train_wgan_gp"
    - from: "revision/run_baselines.py"
      to: "revision/core/preprocessing.py"
      via: "build_dataset_for_pipeline A/B forward/inverse + inverse_kwargs.npz"
      pattern: "inverse_kwargs"
---

<objective>
Build `revision/run_baselines.py`, the idempotent per-(model,pipeline,seed) CLI driver, mirroring `run_ablation.py` with three model branches (WGAN-GP via the unchanged shared loop, VAE via a local ELBO loop, AR via lstsq fit), and gate cross-family sample-space comparability before any sweep runs.

Purpose: BASE-01/BASE-02 require every model trained under identical data/seed/epoch conditions and emitting the same 5-file artifact bundle as Phase 09.1. The cross-family reconstruction smoke gate (RESEARCH Pitfall 3) is the top BASE-03 risk control — it must pass before the 50-run sweep aggregates incomparable numbers.
Output: `revision/run_baselines.py` with a verified smoke run for one model per family.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-classical-baselines/10-CONTEXT.md
@.planning/phases/10-classical-baselines/10-RESEARCH.md
@.planning/phases/10-classical-baselines/10-PATTERNS.md
@.planning/phases/10-classical-baselines/10-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create run_baselines.py CLI driver — argparse, build_dataset_for_pipeline (A/B only), data_hash, WGAN/VAE/AR branches, 5-file bundle</name>
  <files>revision/run_baselines.py</files>
  <read_first>
    - revision/run_ablation.py (the verbatim copy template — imports+HPO block lines 32-65; build_dataset_for_pipeline lines 94-177; generate_samples lines 180-209; _save_inverse_kwargs lines 212-225; main()+bundle lines 228-336)
    - revision/core/training.py lines 220-360 (train_wgan_gp signature + contract; WGAN branch must call it verbatim)
    - revision/core/models/classical.py (WGANMLPGenerator/WGANCNNGenerator/WGANLSTMGenerator from plan 01 — names + params_pqc + count_params)
    - revision/core/models/nonadversarial.py (VAEBaseline encode/decode/reparameterize/sample, ARBaseline fit/sample from plan 01)
    - revision/core/models/critic.py (Critic — same critic for every WGAN variant, D-10-08)
    - revision/core/__init__.py (HPO constants: BATCH_SIZE, EVAL_EVERY, LAMBDA, LR_CRITIC, LR_GENERATOR, N_CRITIC, NOISE_HIGH, NOISE_LOW, NUM_LAYERS, NUM_QUBITS, WINDOW_LENGTH)
    - .planning/phases/10-classical-baselines/10-PATTERNS.md "revision/run_baselines.py" section (copy-anchor line ranges) + .planning/phases/10-classical-baselines/10-RESEARCH.md §"Run-Directory + Artifact Contract" (lines 269-290) + §"VAE Sizing" ELBO block (lines 231-237) + §"AR Baseline" (lines 247-267) + §"Pitfall 5" (lines 487-492)
  </read_first>
  <action>
    Create `revision/run_baselines.py` by copying `revision/run_ablation.py` structure verbatim and adapting:
    - Imports + HPO-constant block: copy run_ablation.py lines 32-65 verbatim — `from revision.core import BATCH_SIZE, EVAL_EVERY, LAMBDA, LR_CRITIC, LR_GENERATOR, N_CRITIC, NOISE_HIGH, NOISE_LOW, NUM_LAYERS, NUM_QUBITS, WINDOW_LENGTH` and `from revision.core.models.critic import Critic`. Never hardcode literals (D-10-08, RESEARCH "Don't Hand-Roll" line 436). Add imports for WGANMLPGenerator/WGANCNNGenerator/WGANLSTMGenerator from revision.core.models.classical and VAEBaseline/ARBaseline from revision.core.models.nonadversarial.
    - `build_dataset_for_pipeline`: copy run_ablation.py lines 94-177 A and B branches VERBATIM (D-10-07 same windowed data + inverse_kwargs contract); DELETE the C branch (D-10-05). Keep the identical `DataLoader(ds, batch_size=BATCH_SIZE, shuffle=False, drop_last=True)` call (lines 169-171).
    - `_save_inverse_kwargs`: copy run_ablation.py lines 212-225 verbatim.
    - data_hash (NEW, no 09.1 precedent — RESEARCH lines 283-290, Pitfall 4 line 480): `hashlib.sha256(load_and_preprocess(str(csv_path))["OD"].cpu().numpy().tobytes()).hexdigest()[:16]`. Use the SAME csv_path the 09.1 quantum runs used (data.csv per RESEARCH line 290). Do NOT grep 09.1 configs for data_hash (it does not exist there).
    - argparse: `--model {wgan_mlp,wgan_cnn,wgan_lstm,vae,ar}`, `--pipeline {A,B}` (NO C), `--seed int`, `--epochs int`, `--out-root` default `revision/results/baselines` (D-10-22).
    - run-dir: `out_root / "runs" / args.model / args.pipeline / str(args.seed)` (D-10-14). Idempotent: clean/overwrite the dir on rerun.
    - `torch.manual_seed(args.seed)` before model construction (run_ablation.py line 291 verbatim); each invocation is a fresh interpreter so RNG is clean (Pitfall 5 — never multiprocessing.Pool anywhere).
    - WGAN branch (wgan_mlp/wgan_cnn/wgan_lstm): construct the matching generator, `critic = Critic(window_length=WINDOW_LENGTH)`, call `train_wgan_gp(generator, critic, bundle.dataloader, num_epochs=args.epochs, n_critic=N_CRITIC, lambda_gp=LAMBDA, lr_critic=LR_CRITIC, lr_generator=LR_GENERATOR, seed=args.seed, eval_every=EVAL_EVERY)` — copied verbatim from run_ablation.py lines 302-313 (D-10-08, loop UNCHANGED). Generate samples mirroring run_ablation.py generate_samples lines 195-209 with `noise=(NUM_QUBITS,bs)` then `generator(noise).to(float64) * 0.1` (the `*0.1` is mandatory for WGAN — mirrors training.py:283). checkpoint.pt = `{gen_state_dict, critic_state_dict}`. metrics.json = per-epoch dict returned by train_wgan_gp.
    - VAE branch: local ELBO loop (RESEARCH lines 231-237) — single `torch.optim.Adam(vae.parameters(), lr=1e-3)`, NO critic, NO n_critic, NO gradient penalty; per-epoch `recon=MSE(x_hat,x); kld=-0.5*mean(1+logvar-mu^2-exp(logvar)); loss=recon+beta*kld` with `beta=1.0` (if Wave-2 smoke shows posterior collapse — sample std ≪ real std — apply linear KL warmup beta:0→1 over first ~20% epochs and document in train_protocol_notes; Pitfall 6). Same dataloader/windowed input/seed/epoch budget as WGAN (D-10-09). Sampling via `vae.sample(N_synth, ...)` → `(N_synth,10)` in [-1,1] window space, NO `*0.1` (Pitfall 3). checkpoint.pt = `{vae_state_dict}`. metrics.json = per-epoch elbo/recon/kld.
    - AR branch: `ARBaseline()` with p=2; fit on the flattened standardized training series via its lstsq method; recursively simulate `N_synth` windows of length 10 in the SAME window space (NO `*0.1`, Pitfall 3). checkpoint is `checkpoint.npz` (D-10-14) storing `{phi, sigma2, p}`. metrics.json = fit diagnostics (sigma2, residual mean/std).
    - 5-file bundle for every branch: `config.yaml`, `checkpoint.pt`|`.npz`, `samples.npy` (shape `(N_synth,10)`, `N_synth = 10 * n_real_windows` per run_ablation.py:316-318), `metrics.json`, `inverse_kwargs.npz`. config.yaml (copy run_ablation.py lines 266-284 then extend): add `model_kind`, `data_hash`, `parameter_count` (from the model's count_params()), `family` (`adversarial-classical` for wgan_*, `non-adversarial` for vae/ar), `train_protocol_notes` (string; for VAE/AR explicitly note the `*0.1` asymmetry and any KL warmup). For VAE/AR, set WGAN-only fields (n_critic, lambda_gp, lr_critic, lr_generator) to null/omitted with the notes string explaining why.
    Do NOT add any new metric helpers (D-10-20); per-run fidelity metrics are computed in the Wave-4 notebook via revision.core.eval, not here.
  </action>
  <verify>
    <automated>./qgan_env/bin/python -m revision.run_baselines --model wgan_mlp --pipeline B --seed 42 --epochs 3 --out-root revision/results/_smoke_baselines && ./qgan_env/bin/python -c "
import pathlib, numpy as np, yaml
d=pathlib.Path('revision/results/_smoke_baselines/runs/wgan_mlp/B/42')
for f in ('config.yaml','checkpoint.pt','samples.npy','metrics.json','inverse_kwargs.npz'):
    assert (d/f).exists() and (d/f).stat().st_size>0, f
c=yaml.safe_load((d/'config.yaml').read_text())
assert len(c['data_hash'])==16, c['data_hash']
assert c['model_kind']=='wgan_mlp' and c['parameter_count']==74 and c['family']=='adversarial-classical'
s=np.load(d/'samples.npy'); assert s.ndim==2 and s.shape[1]==10, s.shape
print('OK wgan_mlp 5-file bundle + data_hash + config fields')"</automated>
  </verify>
  <acceptance_criteria>
    - `./qgan_env/bin/python -m revision.run_baselines --model wgan_mlp --pipeline B --seed 42 --epochs 3 --out-root revision/results/_smoke_baselines` exits 0
    - `revision/results/_smoke_baselines/runs/wgan_mlp/B/42/` contains all 5 non-empty bundle files
    - `config.yaml` has `data_hash` (16 hex chars), `model_kind=='wgan_mlp'`, `parameter_count==74`, `family=='adversarial-classical'`
    - `samples.npy` is shape `(N_synth,10)` with `N_synth == 10 * n_real_windows`
    - argparse rejects `--pipeline C` (choices are A,B only) and accepts all 5 `--model` kinds
    - Source contains no `multiprocessing.Pool` and no new functions added to revision/core/eval.py (D-10-20/24)
  </acceptance_criteria>
  <done>run_baselines.py is an idempotent per-(model,pipeline,seed) driver with WGAN(verbatim train_wgan_gp)/VAE(ELBO)/AR(lstsq) branches, the 5-file bundle, and the data_hash field.</done>
</task>

<task type="auto">
  <name>Task 2: Cross-family sample-space reconstruction smoke gate (Pitfall 3 — top BASE-03 risk)</name>
  <files>revision/run_baselines.py</files>
  <read_first>
    - revision/_build_analysis_notebook.py lines 95-149 (reconstruct_od — A branch lines 105-114, B branch lines 115-127; the identical inverse_kwargs contract every model must reconstruct through)
    - .planning/phases/10-classical-baselines/10-RESEARCH.md §"Pitfall 3" (lines 473-478) — the canonical saved-sample space + the Wave-2 smoke gate spec
    - revision/core/data.py (load_and_preprocess — for real_OD min/max bounds)
  </read_first>
  <action>
    Produce one short smoke run for each family (a wgan, the vae, the ar) at small epochs on Pipeline B seed 42 into a throwaway out-root, then reconstruct one sample from each through the IDENTICAL inverse path used by the Wave-4 notebook (the `reconstruct_od` pattern from _build_analysis_notebook.py:95-149, B branch, consuming each run's own inverse_kwargs.npz). Assert all three reconstructed OD windows land within `[real_OD.min()*0.5, real_OD.max()*1.5]` (RESEARCH Pitfall 3 acceptance). This proves VAE/AR (which do NOT inherit the WGAN loop's `*0.1`) emit samples in the same canonical [-1,1] window space the shared reconstruct consumes — i.e. the comparison table will aggregate comparable numbers. This is a verification gate, not new production code; the `<files>` entry is the same driver (no edit required unless the gate fails, in which case fix the offending branch's sample-space in run_baselines.py and re-run).
    If the gate fails for VAE/AR: the fix is to ensure their `sample()` output range matches what `reconstruct_od` expects (do NOT add `*0.1`; `*0.1` is a quantum-output artifact). Document the resolved sample-space convention in each model's config.yaml `train_protocol_notes`.
  </action>
  <verify>
    <automated>set -e; for M in wgan_cnn vae ar; do ./qgan_env/bin/python -m revision.run_baselines --model $M --pipeline B --seed 42 --epochs 3 --out-root revision/results/_smoke_baselines; done; ./qgan_env/bin/python -c "
import pathlib, numpy as np
from revision.core.data import load_and_preprocess
od=load_and_preprocess('data.csv')['OD'].cpu().numpy()
lo,hi=float(od.min())*0.5, float(od.max())*1.5
import importlib.util
# Reconstruct via the B-branch inverse using each run's inverse_kwargs.npz (mirror _build_analysis_notebook reconstruct_od B branch)
from revision.core.preprocessing import inverse_logreturns
bad=[]
for m in ('wgan_cnn','vae','ar'):
    d=pathlib.Path('revision/results/_smoke_baselines/runs')/m/'B'/'42'
    s=np.load(d/'samples.npy'); ik=dict(np.load(d/'inverse_kwargs.npz'))
    rec=inverse_logreturns(s[:1], **{k:ik[k] for k in ik.files})
    rec=np.asarray(rec).ravel()
    if not (rec.min()>=lo and rec.max()<=hi): bad.append((m,float(rec.min()),float(rec.max())))
assert not bad, ('sample-space mismatch (Pitfall 3): '+str(bad)+' allowed=['+str(lo)+','+str(hi)+']')
print('OK cross-family sample-space gate: wgan_cnn/vae/ar reconstruct into real OD range')"</automated>
  </verify>
  <acceptance_criteria>
    - Smoke runs for `wgan_cnn`, `vae`, `ar` (Pipeline B, seed 42, 3 epochs) all complete exit 0
    - One reconstructed OD window from each of the three lands within `[real_OD.min()*0.5, real_OD.max()*1.5]`
    - If the gate failed and was fixed: the resolved sample-space convention is recorded in the offending model's `train_protocol_notes` (no `*0.1` added to VAE/AR)
  </acceptance_criteria>
  <done>The cross-family sample-space comparability gate passes — WGAN, VAE, and AR samples all reconstruct into the real OD range through the identical inverse path, so the Wave-4 comparison table will aggregate comparable numbers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI args -> run-dir path | `--model`/`--pipeline`/`--seed`/`--out-root` flow into a filesystem path |
| filesystem -> torch.load | checkpoint.pt is written and later loaded by the Wave-4 notebook (project-authored, not external) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-03 | Tampering | path traversal via `--model`/`--pipeline`/`--out-root` into run-dir construction | mitigate | argparse `choices=` for `--model` (5 fixed values) and `--pipeline` (A,B); `--seed` is `type=int`; run-dir built from these constrained tokens only — mirrors run_ablation.py's already-hardened pattern |
| T-10-04 | Tampering | non-atomic / partial write of 5-file bundle leaving a "complete-looking" dir | mitigate | Idempotent overwrite: clean run-dir before writing; sweep (plan 03) gates on all-5-files `-s` non-empty check |
| T-10-05 | Tampering | torch.load of checkpoint.pt with untrusted pickle | accept | Checkpoints are produced by this same codebase in the same sweep; no external/untrusted artifacts deserialized |
| T-10-SC | Tampering | npm/pip/cargo installs | mitigate | Zero new packages (RESEARCH Package Legitimacy Audit); no install tasks |

No high-severity threats: local research driver, constrained CLI args, no network/auth, no external deserialization.
</threat_model>

<verification>
- run_baselines.py smoke: wgan_mlp/wgan_cnn/vae/ar each emit the 5-file bundle with data_hash + correct config fields
- Cross-family sample-space gate (Pitfall 3) passes for wgan/vae/ar
- No multiprocessing.Pool; no new eval.py helpers; HPO constants imported not hardcoded
</verification>

<success_criteria>
- run_baselines.py: idempotent CLI driver, 3 model branches, 5-file bundle + data_hash, A/B pipelines only
- WGAN branch calls train_wgan_gp UNCHANGED with the shared Critic + HPO constants (D-10-08)
- Cross-family reconstruction smoke gate green before any sweep
</success_criteria>

<output>
Create `.planning/phases/10-classical-baselines/10-02-SUMMARY.md` when done
</output>
