---
phase: 10-classical-baselines
plan: 03
type: execute
wave: 3
depends_on: ["10-02"]
files_modified:
  - run_baselines_sweep.sh
files_read:
  - run_ablation_sweep.sh
  - run_baselines.py
autonomous: true
requirements: [BASE-01, BASE-02]
must_haves:
  truths:
    - "run_baselines_sweep.sh fans out 5 models x 2 pipelines x 5 seeds = 50 runs (D-10-23)"
    - "is_complete() is .npz-aware (checkpoint.npz for ar, checkpoint.pt otherwise) and skips done runs (D-10-14, D-10-23)"
    - "Parallelism is xargs -P {1,2} OS-process only — never multiprocessing.Pool (D-10-24, Pitfall 5)"
    - "sweep_status.json is written atomically (flock + tempfile + os.rename) keyed on (model,pipeline,seed)"
    - "The full 50-run sweep completes and 50/50 runs report complete"
  artifacts:
    - path: "run_baselines_sweep.sh"
      provides: "resumable 50-run sweep driver mirroring run_ablation_sweep.sh"
      contains: "xargs -P"
  key_links:
    - from: "run_baselines_sweep.sh"
      to: "run_baselines.py"
      via: "run_one() invokes python -m revision.run_baselines --model --pipeline --seed --epochs"
      pattern: "run_baselines"
---

<objective>
Build `run_baselines_sweep.sh`, the resumable 50-run sweep driver (5 model kinds × 2 pipelines × 5 seeds × 1000 epochs), mirroring `run_ablation_sweep.sh` verbatim with a `.npz`-aware `is_complete()` and the atomic status writer, then execute the full sweep.

Purpose: BASE-01/BASE-02 require all 5 new model types trained under the identical-conditions invariant (D-10-08) across the same seed set {42-46} and 2 pipelines, producing the 50 run dirs the Wave-4 comparison table aggregates.
Output: `run_baselines_sweep.sh` and 50 completed run directories with `sweep_status.json` reporting 50/50.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-classical-baselines/10-CONTEXT.md
@.planning/phases/10-classical-baselines/10-RESEARCH.md
@.planning/phases/10-classical-baselines/10-PATTERNS.md
@.planning/phases/10-classical-baselines/10-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create run_baselines_sweep.sh — verbatim copy of run_ablation_sweep.sh with 50-pair worklist and .npz-aware is_complete()</name>
  <files>run_baselines_sweep.sh</files>
  <read_first>
    - run_ablation_sweep.sh (the verbatim copy template — set -euo pipefail + python detection lines 77-102; constants block lines 82-87; --parallel 1|2 guardrail lines 140-156; is_complete() lines 164-172; update_status() atomic writer lines 187-266; run_one() lines 274-318; main dispatch + xargs worklist lines 369-395)
    - run_baselines.py (the per-run driver this sweep invokes — its argparse: --model, --pipeline, --seed, --epochs, --out-root)
    - .planning/phases/10-classical-baselines/10-PATTERNS.md "run_baselines_sweep.sh" section (exact copy-anchor line ranges + adaptations) + .planning/phases/10-classical-baselines/10-RESEARCH.md §"Pitfall 5" (lines 487-492) + D-10-23/D-10-24
  </read_first>
  <action>
    Create `run_baselines_sweep.sh` by copying `run_ablation_sweep.sh` VERBATIM, changing only the dimensions and `is_complete()`:
    - `set -euo pipefail` + python-interpreter detection (lines 77-102): copy verbatim (prefer `./qgan_env/bin/python`).
    - Constants block (lines 82-87): set `MODELS="wgan_mlp wgan_cnn wgan_lstm vae ar"`, `PIPELINES="A B"` (NO C, D-10-05), `SEEDS="42 43 44 45 46"`, `EPOCHS=1000` (D-10-08), `OUT_ROOT="results/baselines"` (D-10-14).
    - `--parallel {1,2}` guardrail (lines 140-156): copy verbatim — reject anything other than 1 or 2 (D-10-24 / Pitfall 5: xargs -P 2 OS-process parallelism only; NEVER multiprocessing.Pool — there must be zero Python multiprocessing anywhere in this sweep or the driver).
    - `is_complete()` (lines 164-172): copy and make `.npz`-aware — `local ckpt="checkpoint.pt"; [[ "$m" == "ar" ]] && ckpt="checkpoint.npz"`; require `config.yaml`, `$ckpt`, `samples.npy`, `metrics.json`, `inverse_kwargs.npz` all present and non-empty (`-s`) under `${OUT_ROOT}/runs/${m}/${p}/${s}`. (09.1 keyed on `(p,s)` with fixed checkpoint.pt; Phase 10 keys on `(model,p,s)` with the conditional `.npz` — D-10-14.)
    - `update_status()` atomic writer (lines 187-266): copy verbatim including `flock -x 9` advisory lock + `tempfile.mkstemp` + `os.rename`. Change `total_count` 15 → 50; key per-run records on the 3-tuple `(model,pipeline,seed)` (replace the `r["pipeline"]==p and r["seed"]==s` match with a model/pipeline/seed match). Status file: `${OUT_ROOT}/sweep_status.json`.
    - `run_one()` (lines 274-318): copy verbatim; change the inner call to `"$PYTHON" -m revision.run_baselines --model "$m" --pipeline "$p" --seed "$s" --epochs "$EPOCHS" --out-root "$OUT_ROOT"`. It must take a 3rd positional (model) in addition to pipeline+seed.
    - Main dispatch + xargs worklist (lines 369-395): copy verbatim; expand the nested loop to emit 50 lines (`for m in $MODELS; for p in $PIPELINES; for s in $SEEDS`); the `xargs -P {1,2} -L 1 bash -c 'run_one "$0" "$1" "$2"'` invocation gets a third positional. Skip already-complete `(model,pipeline,seed)` triples via `is_complete()` so the sweep is resumable.
    Make the script executable (`chmod +x`).
  </action>
  <verify>
    <automated>bash -n run_baselines_sweep.sh && grep -q 'xargs -P' run_baselines_sweep.sh && ! grep -qi 'multiprocessing' run_baselines_sweep.sh && grep -q 'checkpoint.npz' run_baselines_sweep.sh && grep -q 'flock' run_baselines_sweep.sh && grep -Eq 'MODELS=.*wgan_mlp.*wgan_cnn.*wgan_lstm.*vae.*ar' run_baselines_sweep.sh && grep -Eq 'SEEDS=.*42.*43.*44.*45.*46' run_baselines_sweep.sh && grep -q 'EPOCHS=1000' run_baselines_sweep.sh && grep -vq 'PIPELINES=.*C' run_baselines_sweep.sh && test -x run_baselines_sweep.sh && echo "OK sweep script: 50-pair worklist, .npz-aware, flock, xargs -P, no multiprocessing"</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n run_baselines_sweep.sh` passes (no syntax errors) and the file is executable
    - `grep 'xargs -P'` matches; `grep -i multiprocessing` does NOT match (D-10-24/Pitfall 5)
    - Script contains `checkpoint.npz` conditional in is_complete() and `flock` in the status writer
    - Constants: MODELS = the 5 kinds, SEEDS = 42-46, EPOCHS=1000, PIPELINES has no C
    - The xargs worklist enumerates exactly 50 (model,pipeline,seed) triples
  </acceptance_criteria>
  <done>run_baselines_sweep.sh is a verbatim mirror of run_ablation_sweep.sh with a 50-pair worklist, .npz-aware is_complete(), atomic flock status writer, and xargs -P {1,2} parallelism (no multiprocessing).</done>
</task>

<task type="auto">
  <name>Task 2: Execute the full 50-run sweep at --parallel 2 and verify 50/50 complete with consistent data_hash</name>
  <files>run_baselines_sweep.sh</files>
  <read_first>
    - run_baselines_sweep.sh (the script from Task 1)
    - .planning/phases/10-classical-baselines/10-CONTEXT.md D-10-10 (≈110 min at --parallel 2; ≤3 h budget; relaxable if classical >5x faster than quantum)
    - .planning/phases/09.1-r1-m3-preprocessing-ablation/09.1-03-SUMMARY.md (09.1 sweep wall-time precedent: 15 runs in 29.1 min @ parallel=2)
  </read_first>
  <action>
    Run the full sweep: `./run_baselines_sweep.sh --parallel 2`. This is the long-running step (D-10-10 estimates ≈110 min; classical paths are likely faster than the quantum path so the real wall time may be well under the ≤3 h budget). Run it in the background and monitor `results/baselines/sweep_status.json` for progress; the script is resumable (is_complete() skips done triples) so a re-invocation after any interruption continues cleanly. On completion, assert 50/50 triples are complete and that the `data_hash` field is identical across all 50 `config.yaml` files (D-10-15 — recomputed from load_and_preprocess, NOT grepped from 09.1; quantum equivalence is established by-construction in Wave 4). Do NOT use multiprocessing to speed this up (D-10-24).
  </action>
  <verify>
    <automated>./qgan_env/bin/python -c "
import pathlib, yaml, sys
base=pathlib.Path('results/baselines/runs')
MODELS=['wgan_mlp','wgan_cnn','wgan_lstm','vae','ar']; PIPES=['A','B']; SEEDS=[42,43,44,45,46]
hashes=set(); missing=[]; n=0
for m in MODELS:
  for p in PIPES:
    for s in SEEDS:
      d=base/m/p/str(s)
      ck='checkpoint.npz' if m=='ar' else 'checkpoint.pt'
      ok=all((d/f).exists() and (d/f).stat().st_size>0 for f in ('config.yaml',ck,'samples.npy','metrics.json','inverse_kwargs.npz'))
      if not ok: missing.append(str(d)); continue
      n+=1; hashes.add(yaml.safe_load((d/'config.yaml').read_text())['data_hash'])
if missing: print('INCOMPLETE',len(missing),'missing:',missing[:5]); sys.exit(1)
assert n==50, n
assert len(hashes)==1, ('data_hash not uniform across 50 runs: '+str(hashes))
print('OK 50/50 complete; uniform data_hash='+next(iter(hashes)))"</automated>
  </verify>
  <acceptance_criteria>
    - All 50 `results/baselines/runs/<model>/<pipeline>/<seed>/` dirs contain the complete 5-file bundle (ar uses checkpoint.npz, others checkpoint.pt)
    - `sweep_status.json` reports 50/50 complete
    - The `data_hash` value is identical across all 50 `config.yaml` files (single unique value)
    - Sweep completed within the ≤3 h budget (or documented relaxation per D-10-10 in the SUMMARY)
  </acceptance_criteria>
  <done>The full 5×2×5=50-run sweep is complete (50/50) with a uniform data_hash across all runs; artifacts are ready for Wave-4 aggregation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI `--parallel` arg -> xargs -P | numeric arg controls OS-process fan-out |
| concurrent writers -> sweep_status.json | parallel xargs workers append run records |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-06 | Tampering | shell injection via `--parallel` / loop vars in run_baselines_sweep.sh | mitigate | `--parallel` validated to exactly {1,2} (copied guardrail); MODELS/PIPELINES/SEEDS are script-internal constants, not external input; quoted expansions throughout (verbatim from hardened 09.1 script) |
| T-10-07 | Tampering | non-atomic concurrent write corrupting sweep_status.json | mitigate | `flock -x 9` advisory lock + `tempfile.mkstemp` + `os.rename` atomic-write pattern copied verbatim from run_ablation_sweep.sh:187-266 |
| T-10-08 | Repudiation | silent mixed-budget contamination (is_complete checks presence not epoch count) | mitigate | Fresh OUT_ROOT for the 1000-epoch sweep; idempotent overwrite in run_baselines.py; SUMMARY documents epoch budget (mirrors 09.1 archival lesson in STATE.md) |
| T-10-SC | Tampering | npm/pip/cargo installs | mitigate | Zero new packages (RESEARCH Package Legitimacy Audit); no install tasks |

No high-severity threats: local bash sweep, validated numeric parallelism arg, atomic status writes, no network/auth.
</threat_model>

<verification>
- `bash -n` clean; xargs -P present; no multiprocessing anywhere; .npz-aware is_complete; flock atomic writer; 50-pair worklist
- Full sweep: 50/50 complete, uniform data_hash, within ≤3 h budget
</verification>

<success_criteria>
- run_baselines_sweep.sh: resumable 50-run mirror of run_ablation_sweep.sh, .npz-aware, atomic status, xargs -P {1,2} only
- 50/50 runs complete with identical data_hash
</success_criteria>

<output>
Create `.planning/phases/10-classical-baselines/10-03-SUMMARY.md` when done
</output>
