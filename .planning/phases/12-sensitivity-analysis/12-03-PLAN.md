---
phase: 12-sensitivity-analysis
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - revision/run_multiseed_rollup.py
  - revision/results/multiseed_summary.json
autonomous: true
requirements: [SENS-03]
must_haves:
  truths:
    - "Every headline comparison table from Phases 10-11 is re-emitted with ≥5 seeds, mean ± std (n) in every cell"
    - "The data_hash invariant (D-10-15) is asserted equal across all 5 consumed headline JSONs before any roll-up math"
    - "multiseed_summary.json consolidates the roll-up with a provenance header listing every consumed file and its data_hash"
    - "No new training, no new seeds, no device/model/torch — pure aggregation of frozen on-disk artifacts (D-12-03)"
    - "revision/core/ is byte-untouched"
  artifacts:
    - path: "revision/run_multiseed_rollup.py"
      provides: "SENS-03 pure aggregator (single invocation, stdlib only)"
      min_lines: 60
      contains: "data_hash"
    - path: "revision/results/multiseed_summary.json"
      provides: "SENS-03 deliverable — consolidated mean ± std roll-up + provenance"
      contains: "rollup"
  key_links:
    - from: "revision/run_multiseed_rollup.py"
      to: "revision/results/{baseline_comparison,tstr,predictive_discriminative,augmentation,fidelity_dualscale}.json"
      via: "read + cross-artifact data_hash equality assertion (D-10-15)"
      pattern: "data_hash"
    - from: "revision/run_multiseed_rollup.py"
      to: "revision/results/multiseed_summary.json"
      via: "long-form groupby -> mean ± std (n) per cell"
      pattern: "statistics\\.(fmean|stdev)"
---

<objective>
Build `revision/run_multiseed_rollup.py`: the SENS-03 pure aggregator (single invocation,
stdlib only — NO device, NO model, NO torch, NO pennylane). It reads the five frozen
Phase 10/11 headline JSONs, asserts their `data_hash` fields are mutually equal (D-10-15
hard gate, expected `91e447d4624e25b3`), groups the long-form `rows[]` by
`(source, model_kind, pipeline, metric_name, scale, injection_ratio)`, and emits
`revision/results/multiseed_summary.json` with `{mean, std, n, seeds}` per cell plus a
provenance header. Pure aggregation of existing 5-seed data — NO new training, NO new
seeds (D-12-03).

Purpose: SENS-03 is the headline mean±std roll-up the manuscript prints for R1-M4
calibrated-uncertainty bars. This plan is parallel-safe with Plans 01/02 (zero file
overlap, consumes only frozen artifacts).
Output: `revision/run_multiseed_rollup.py`, `revision/results/multiseed_summary.json`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-sensitivity-analysis/12-CONTEXT.md
@.planning/phases/12-sensitivity-analysis/12-RESEARCH.md
@.planning/phases/12-sensitivity-analysis/12-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build run_multiseed_rollup.py — repo resolver, cross-artifact data_hash gate, long-form groupby roll-up</name>
  <files>revision/run_multiseed_rollup.py</files>
  <read_first>
    - revision/run_utility.py (lines 38-58 — _find_repo_root resolver + sys.path.insert; pure-consumer shape — copy the resolver verbatim, this driver imports NO torch/pennylane/core)
    - .planning/phases/12-sensitivity-analysis/12-RESEARCH.md (Code Example 4 — the full groupby + cross-artifact data_hash assertion; Open Q3 — prefer stdlib statistics.fmean/stdev, zero new dependency; verified row counts: baseline_comparison 1710, fidelity_dualscale 3360, tstr 144, predictive_discriminative 120, augmentation 180; data_hash 91e447d4624e25b3 in all five)
    - .planning/phases/12-sensitivity-analysis/12-PATTERNS.md (run_multiseed_rollup.py section — copy-from excerpts; Output JSON provenance header schema; Anti-Pattern: do NOT re-derive data_hash from transform_ablation)
    - revision/results/baseline_comparison.json (confirm top-level keys schema/model_kinds/pipelines/seeds/data_hash/data_hash_verification and rows[] element shape; read header only, not all 1710 rows)
  </read_first>
  <action>
    Create `revision/run_multiseed_rollup.py`, single-invocation, stdlib only:
    - Repo-root resolver: copy `_find_repo_root()` + `sys.path.insert(0, str(REPO))`
      verbatim from run_utility.py:38-58; `RESULTS = REPO / "revision/results"`.
    - HEADLINE = ["baseline_comparison.json", "tstr.json",
      "predictive_discriminative.json", "augmentation.json", "fidelity_dualscale.json"].
      Load all five with `json.load`.
    - Cross-artifact data_hash gate (D-10-15 — HARD gate, before any roll-up math):
      `hashes = {f: d["data_hash"] for f,d in docs.items()}`;
      `assert len(set(hashes.values())) == 1, f"data_hash mismatch: {hashes}"`;
      `canonical_hash = next(iter(hashes.values()))` (expect "91e447d4624e25b3").
      Do NOT re-derive the hash from transform_ablation configs (Anti-Pattern — the
      existing data_hash_verification.quantum_equivalence blocks already encode by-construction
      equivalence; SENS-03 only asserts mutual equality of the five `data_hash` fields).
    - Groupby: bucket every `rows[]` element by key
      `(source_filename, model_kind, pipeline, metric_name, scale, r.get("injection_ratio"))`
      (augmentation.json adds `injection_ratio`; others -> None). Accumulate `(seed, value)`.
    - Per bucket emit `{source, model_kind, pipeline, metric_name, scale, injection_ratio,
      mean: statistics.fmean(vals), std: statistics.stdev(vals) if n>1 else 0.0,
      n: len(vals), seeds: sorted(unique seeds)}`. Use stdlib `statistics` only — NO pandas,
      NO numpy required (RESEARCH Open Q3).
    - Emit `revision/results/multiseed_summary.json` with provenance header:
      `{schema:"SENS-03 multi-seed roll-up: mean ± std per headline cell",
      data_hash: canonical_hash, consumed_artifacts:{<file>:<its data_hash>},
      seed_set:[42,43,44,45,46], rollup:[...]}`. Write via `Path.write_text(json.dumps(...,
      indent=2))` anchored at REPO.
    This driver imports NO torch, NO pennylane, NO revision.core model code (Pattern 2).
    No fenced code in this action — implementation detail is in the read_first excerpts.
  </action>
  <verify>
    <automated>cd /Users/shawngibford/dev/phd/qGAN && python -c "import ast; ast.parse(open('revision/run_multiseed_rollup.py').read())" && grep -q '_find_repo_root' revision/run_multiseed_rollup.py && grep -q 'data_hash' revision/run_multiseed_rollup.py && grep -Eq 'statistics\.(fmean|stdev)' revision/run_multiseed_rollup.py && ! grep -Eq '^import (torch|pennylane)|import pennylane|from revision.core.models' revision/run_multiseed_rollup.py && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `revision/run_multiseed_rollup.py` parses as valid Python (ast.parse exits 0).
    - Contains `_find_repo_root` and anchors paths at `REPO`.
    - Contains an `assert` on equality of all five `data_hash` fields (D-10-15) executed before any roll-up accumulation.
    - Uses `statistics.fmean` and `statistics.stdev` (stdlib); imports NO `torch`, NO `pennylane`, NO `revision.core.models` (grep confirms absence).
    - HEADLINE list is exactly the five files: baseline_comparison, tstr, predictive_discriminative, augmentation, fidelity_dualscale.
    - Bucket key includes `injection_ratio` (for augmentation.json) and `source` filename.
  </acceptance_criteria>
  <done>run_multiseed_rollup.py exists, stdlib-only, with the verbatim repo resolver, the D-10-15 cross-artifact data_hash hard gate, and the long-form groupby mean±std logic. No device/model/torch imports.</done>
</task>

<task type="auto">
  <name>Task 2: Run the aggregator and verify multiseed_summary.json correctness</name>
  <files>revision/results/multiseed_summary.json</files>
  <read_first>
    - revision/run_multiseed_rollup.py (Task 1 output)
    - .planning/phases/12-sensitivity-analysis/12-RESEARCH.md (verified data_hash 91e447d4624e25b3 across all five; row counts per file; Validation Note — cross-artifact hash hard gate)
    - .planning/phases/12-sensitivity-analysis/12-PATTERNS.md (Output JSON artifacts — provenance header + rollup[] element schema)
  </read_first>
  <action>
    Run `python revision/run_multiseed_rollup.py` (no args — single invocation). Then verify
    the emitted `revision/results/multiseed_summary.json`:
    - Provenance header present: `schema`, `data_hash == "91e447d4624e25b3"`,
      `consumed_artifacts` maps all five filenames each to `"91e447d4624e25b3"`,
      `seed_set == [42,43,44,45,46]`.
    - Every `rollup[]` cell has keys `{source, model_kind, pipeline, metric_name, scale,
      injection_ratio, mean, std, n, seeds}`; every cell has a numeric `mean` and a numeric
      `std`; cells with n>1 have std computed (not the n==1 zero fallback) — i.e. the
      headline 5-seed cells report `n == 5` and `seeds == [42,43,44,45,46]` where the
      underlying data has all five seeds.
    - The roll-up covers every (model_kind, pipeline, metric_name, scale) combination present
      in the five source JSONs (no headline cell silently dropped).
    If the data_hash gate fails (mismatch across the five JSONs), STOP — this is a hard
    cross-phase consistency failure (D-10-15), not something to work around.
  </action>
  <verify>
    <automated>cd /Users/shawngibford/dev/phd/qGAN && python -c "import json; s=json.load(open('revision/results/multiseed_summary.json')); assert s['data_hash']=='91e447d4624e25b3', s['data_hash']; assert set(s['consumed_artifacts'])=={'baseline_comparison.json','tstr.json','predictive_discriminative.json','augmentation.json','fidelity_dualscale.json'}, s['consumed_artifacts']; assert all(v=='91e447d4624e25b3' for v in s['consumed_artifacts'].values()), s['consumed_artifacts']; assert s['seed_set']==[42,43,44,45,46], s['seed_set']; rk={'source','model_kind','pipeline','metric_name','scale','injection_ratio','mean','std','n','seeds'}; assert all(rk<=set(c) for c in s['rollup']), 'rollup schema'; assert all(isinstance(c['mean'],(int,float)) and isinstance(c['std'],(int,float)) for c in s['rollup']); assert any(c['n']==5 and c['seeds']==[42,43,44,45,46] for c in s['rollup']), 'no 5-seed headline cell'; print('ROLLUP_OK', len(s['rollup']))"</automated>
  </verify>
  <acceptance_criteria>
    - `revision/results/multiseed_summary.json` exists; `data_hash == "91e447d4624e25b3"`.
    - `consumed_artifacts` lists all five headline files, each mapped to `"91e447d4624e25b3"`.
    - `seed_set == [42, 43, 44, 45, 46]`.
    - Every `rollup[]` cell has the full key set `{source,model_kind,pipeline,metric_name,scale,injection_ratio,mean,std,n,seeds}` with numeric `mean` and `std`.
    - At least one 5-seed headline cell has `n == 5` and `seeds == [42,43,44,45,46]` (proves ≥5-seed mean±std re-emission, Success Criterion 3).
    - `git diff --stat revision/core/` is empty (D-10-13).
  </acceptance_criteria>
  <done>multiseed_summary.json emitted with correct provenance, the asserted D-10-15 canonical data_hash, and every headline cell re-emitted as mean ± std (n) over the 5-seed set — SENS-03 deliverable complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Aggregator → frozen headline JSONs | Trust boundary between Phase 10/11 frozen output and the SENS-03 deliverable |
| Cross-phase consistency | data_hash equality is the integrity control linking Phase 10/11 artifacts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-10 | Tampering | silent inconsistency across consumed headline JSONs | mitigate | Hard `assert` that all five `data_hash` fields are mutually equal (D-10-15) before any roll-up math; canonical hash recorded in provenance |
| T-12-11 | Tampering | re-deriving / weakening the data_hash gate | mitigate | Plan forbids re-deriving hash from transform_ablation (Anti-Pattern); only mutual-equality assertion of the five frozen fields permitted |
| T-12-12 | Tampering | revision/core/ integrity (D-10-13) | mitigate | Aggregator imports no core model code; `git diff --stat revision/core/` empty asserted |
| T-12-13 | Information Disclosure | offline aggregation of local result JSONs, no PII/network/secrets | accept | No network/auth/credentials — low-value local target |
</threat_model>

<verification>
- `run_multiseed_rollup.py` parses; stdlib-only; contains the D-10-15 cross-artifact hash assertion; no torch/pennylane/core imports.
- `multiseed_summary.json` present with canonical data_hash 91e447d4624e25b3, all five consumed artifacts, seed_set [42..46], full rollup schema, ≥1 five-seed headline cell.
- `git diff --stat revision/core/` empty.
</verification>

<success_criteria>
SENS-03 deliverable complete: `multiseed_summary.json` consolidates every Phase 10/11
headline comparison table re-emitted with ≥5 seeds as mean ± std (n), the D-10-15
cross-artifact data_hash invariant is asserted, and the roll-up is pure aggregation of
frozen artifacts with no new training/seeds and `revision/core/` untouched.
</success_criteria>

<output>
Create `.planning/phases/12-sensitivity-analysis/12-03-SUMMARY.md` when done. Record: the
asserted canonical data_hash, the rollup cell count, and confirmation that the 5-seed
headline cells report n==5 over seeds {42,43,44,45,46}.
</output>
