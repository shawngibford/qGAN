---
phase: 12-sensitivity-analysis
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - run_sensitivity.py
autonomous: true
requirements: [SENS-01, SENS-02]
must_haves:
  truths:
    - "A single CLI invocation regenerates samples for one (pipeline, seed, condition) cell from frozen trained params on a noisy/finite-shot device and writes a per-cell artifact bundle"
    - "The condition=analytic cell reuses the frozen samples.npy and reproduces the corresponding fidelity_dualscale.json quantum row within fp tolerance (harness-faithfulness proof)"
    - "core/ is byte-untouched (git diff --stat core/ empty)"
    - "Driver fails loud if PennyLane is not exactly 0.44.0"
  artifacts:
    - path: "run_sensitivity.py"
      provides: "Per-cell SENS-01/02 inference driver (CLI, one cell/invocation)"
      min_lines: 200
      contains: "set_shots"
    - path: "results/sensitivity/runs/analytic/B/42/metrics.json"
      provides: "Smoke-cell artifact bundle proving harness faithfulness"
  key_links:
    - from: "run_sensitivity.py"
      to: "results/transform_ablation/runs/<pipeline>/<seed>/checkpoint.pt"
      via: "torch.load params_pqc reload (no retraining, D-12-01)"
      pattern: "params_pqc"
    - from: "run_sensitivity.py"
      to: "revision.core.eval.full_metric_suite"
      via: "unchanged fidelity recompute (D-12-03)"
      pattern: "full_metric_suite"
---

<objective>
Build `run_sensitivity.py`: the per-cell inference driver for SENS-01 (shot
noise) and SENS-02 (noise channels). One invocation = one `(pipeline, seed, condition)`
cell: reload the frozen 75-element `params_pqc` tensor (NO retraining — D-12-01), build
the appropriate noisy/finite-shot QNode in the driver (never in `core/` — D-10-13),
regenerate samples honoring the `*0.1` and RNG contracts, reconstruct OD scale, recompute
the existing fidelity suite unchanged (D-12-03), and emit long-form rows. Includes the
smoke gate that proves the device-swap harness is faithful to the frozen Phase 09.1/10
numbers before any full sweep runs.

Purpose: SENS-01 and SENS-02 are the R1-M4 / R2-1 robustness rebuttal. The harness must
produce numbers byte-comparable to the frozen analytic reference, or the degradation
curves are meaningless.
Output: `run_sensitivity.py` + a verified smoke-cell artifact bundle.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-sensitivity-analysis/12-CONTEXT.md
@.planning/phases/12-sensitivity-analysis/12-RESEARCH.md
@.planning/phases/12-sensitivity-analysis/12-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build run_sensitivity.py harness skeleton (resolver, version gate, params load, generation+reconstruction contracts)</name>
  <files>run_sensitivity.py</files>
  <read_first>
    - run_baselines.py (CLI/argparse/idempotency skeleton — lines 1-91 docstring+imports, 430-522 main+run-dir bundle)
    - run_ablation.py (generate_samples body, lines 179-209 — the *0.1 and np.random.default_rng(seed) contract, copy verbatim swapping call site)
    - run_utility.py (lines 38-58 _find_repo_root resolver; lines 144-185 reconstruct_od Pipeline-A/B branches incl. seed*7919+1 and od[:,:10] truncation)
    - core/models/quantum.py (lines 103-202 — generator_circuit body + the (window_length,batch).T transpose in forward, lines 194-199)
    - .planning/phases/12-sensitivity-analysis/12-RESEARCH.md (Code Examples 1 & 2; Pitfalls 3, 4, 5, 6)
    - .planning/phases/12-sensitivity-analysis/12-PATTERNS.md (run_sensitivity.py section, all copy-from excerpts)
  </read_first>
  <action>
    Create `run_sensitivity.py` as a CLI driver, one `(pipeline, seed, condition)`
    cell per invocation. Concrete required elements:
    - Repo-root resolver: copy `_find_repo_root()` + `sys.path.insert(0, str(REPO))` verbatim
      from run_utility.py:38-58; anchor EVERY artifact path at `REPO` (Pitfall 6).
    - PennyLane version gate at startup: `assert qml.__version__ == "0.44.0"` with a message
      stating "Do NOT run via ./qgan_env (0.43.0)" (RESEARCH Open Q1 recommendation (a),
      Pitfall 5). This is a deliberate, documented deviation from the analog sweep's
      venv-preference; note it in the module docstring.
    - HPO/circuit constants imported from `revision.core` (BATCH_SIZE, NOISE_LOW, NOISE_HIGH,
      NUM_QUBITS, NUM_LAYERS, WINDOW_LENGTH) — never literals.
    - Trained-params load: `QuantumGenerator(num_qubits=NUM_QUBITS, num_layers=NUM_LAYERS,
      window_length=WINDOW_LENGTH)`, then `torch.load(REPO/"results/transform_ablation/runs"/pipeline/str(seed)/"checkpoint.pt",
      map_location="cpu", weights_only=False)`, assign `g.params_pqc.data = ck["params_pqc"]`,
      `g.eval()`. NO retraining anywhere (D-12-01). Do NOT use `g.qnode` (Pitfall 2).
    - Generation contract: copy run_ablation.py:195-208 generate_samples body verbatim —
      `np.random.default_rng(seed)`, `rng.uniform(NOISE_LOW, NOISE_HIGH, size=(NUM_QUBITS, bs))`,
      `BATCH_SIZE` loop, `torch.no_grad()`, the `* 0.1` float64 scaling (Pitfall 3 — load-bearing),
      and the `(window_length, batch) -> .T` transpose for the stacked tuple of 10 expvals
      (replicate quantum.py:194-199). The ONLY change vs analog: the per-batch call site uses
      an injected `qnode` argument instead of `generator(noise)`.
    - OD reconstruction: copy reconstruct_od Pipeline-A and Pipeline-B branches verbatim from
      run_utility.py:144-185, including the Pipeline-B `np.random.default_rng(seed * 7919 + 1)`
      od_start draw (Pitfall 4 — load-bearing, do NOT refactor) and the `od[:, :10]` truncation
      when inverse_logreturns returns length-11.
    Do NOT inline full file contents — point implementation detail at the read_first excerpts.
  </action>
  <verify>
    <automated>cd /Users/shawngibford/dev/phd/qGAN && python -c "import ast; ast.parse(open('run_sensitivity.py').read())" && grep -q 'set_shots\|_find_repo_root\|0.44.0\|params_pqc' run_sensitivity.py && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `run_sensitivity.py` parses as valid Python (ast.parse exits 0).
    - File contains the literal string `0.44.0` inside an assertion on `qml.__version__`.
    - File contains `_find_repo_root` and anchors paths at `REPO`.
    - File reloads `ck["params_pqc"]` from `transform_ablation/runs/<pipeline>/<seed>/checkpoint.pt` and contains NO training loop / optimizer / `.backward()` call.
    - Generation code path multiplies generator output by `0.1` and uses `np.random.default_rng(seed)` (grep both, filtering comments: `grep -v '^[[:space:]]*#' | grep`).
    - Pipeline-B reconstruction uses the exact expression `np.random.default_rng(seed * 7919 + 1)`.
    - `git diff --stat core/` is empty.
  </acceptance_criteria>
  <done>run_sensitivity.py skeleton exists with repo resolver, 0.44.0 gate, params reload, and the verbatim *0.1 generation + Pipeline-A/B reconstruction contracts; core/ untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Add finite-shot + noise-channel QNode construction, fidelity recompute, and idempotent per-cell bundle + CLI</name>
  <files>run_sensitivity.py</files>
  <read_first>
    - run_sensitivity.py (current state from Task 1)
    - .planning/phases/12-sensitivity-analysis/12-RESEARCH.md (Code Example 2 make_shot_qnode; Code Example 3 + implementation note for per-layer channel insertion; Pitfalls 1 & 2; Validation Note)
    - .planning/phases/12-sensitivity-analysis/12-PATTERNS.md (noisy/finite-shot QNode excerpt; idempotent run-dir bundle excerpt; CLI surface excerpt; dual-scale EVAL-05 note)
    - core/models/quantum.py (lines 122-171 — generator_circuit gate body to copy into the driver as noisy_generator_circuit)
    - core/eval.py (lines 143-163 — full_metric_suite signature/return dict; reuse UNCHANGED)
    - run_baselines.py (lines 430-454 argparse surface; lines 456-522 idempotent run-dir + config.yaml/samples.npy/metrics.json bundle)
  </read_first>
  <action>
    Extend run_sensitivity.py with the device construction, fidelity recompute, and CLI:
    - `make_shot_qnode(g, shots)` (SENS-01): `qml.device("default.qubit", wires=NUM_QUBITS)`
      with NO `shots=` kwarg (Pitfall 1 — deprecated in 0.44), `qml.QNode(g.generator_circuit,
      dev, interface="torch", diff_method=None)` (NOT backprop — Pitfall 2), then
      `qn = qml.set_shots(qn, shots=shots)` when shots is not None (0.44 transform API).
    - `make_noisy_qnode(g, channel, level)` (SENS-02): `qml.device("default.mixed",
      wires=NUM_QUBITS)`. Copy the generator_circuit gate body (quantum.py:122-171) into the
      driver as `noisy_generator_circuit`, inserting `qml.DepolarizingChannel(level, wires=q)`
      (channel="depolarizing") or `qml.AmplitudeDamping(level, wires=q)`
      (channel="amplitude_damping") after each entangling block — PER-LAYER insertion is the
      documented default (RESEARCH Assumption A1 / Open Q2). `diff_method=None`. Document this
      circuit-body copy in the function docstring as a deliberate noise-study duplication that
      does NOT violate D-10-13 (it lives in run_sensitivity.py, not core/).
    - Conditions (exact tokens, 11 total): `analytic`, `shots_8192`, `shots_1024`,
      `depol_0.0`, `depol_0.001`, `depol_0.01`, `depol_0.05`, `ampdamp_0.0`,
      `ampdamp_0.001`, `ampdamp_0.01`, `ampdamp_0.05` (amplitude-damping γ ∈
      {0,0.001,0.01,0.05} per D-12-02 / ROADMAP SC-2). For `condition=analytic`: do NOT
      regenerate — read the frozen
      `results/transform_ablation/runs/<pipeline>/<seed>/samples.npy`
      (no-regeneration invariant, mirrors D-11-08). All other conditions do a fresh forward pass.
    - Fidelity recompute: call `revision.core.eval.full_metric_suite` UNCHANGED on OD-scale
      `(real_od, fake_od)` AND once on the transformed/log-return scale; tag each emitted row
      with `scale` (dual-scale per EVAL-05). Emit long-form rows `{model_kind:"quantum",
      pipeline, seed, metric_name, scale, value, condition}` plus `shots` (SENS-01) or
      `noise_model`+`noise_level` (SENS-02) dimensions — extend the long-form schema, do not
      replace it.
    - Idempotent per-cell bundle: `run_dir = out_root/"runs"/condition/pipeline/str(seed)`;
      if it exists `shutil.rmtree` then recreate (no stale partial bundle); write
      `config.yaml` (yaml.safe_dump), `samples.npy` (np.save), `metrics.json`
      (json.dumps default=float). Copy this bundle shape from run_baselines.py:456-522.
    - CLI: `--pipeline {A|B} --seed N --condition <one of the 11 tokens above>
      [--out-root results/sensitivity] [--csv-path ./data.csv]`, one cell per
      invocation, patterned after run_baselines.py:430-454.
    No fenced code in this action — implementation detail is in the read_first excerpts.
  </action>
  <verify>
    <automated>cd /Users/shawngibford/dev/phd/qGAN && python -c "import ast; ast.parse(open('run_sensitivity.py').read())" && grep -q 'default.mixed' run_sensitivity.py && grep -q 'DepolarizingChannel' run_sensitivity.py && grep -q 'AmplitudeDamping' run_sensitivity.py && grep -q 'ampdamp_0.01' run_sensitivity.py && grep -q 'diff_method=None' run_sensitivity.py && ! grep -q 'multiprocessing' run_sensitivity.py && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `python run_sensitivity.py --help` exits 0 and lists `--pipeline`, `--seed`, `--condition`.
    - File contains `qml.set_shots(` and does NOT pass a `shots=` kwarg to `qml.device(` (grep: `grep 'qml.device(' run_sensitivity.py | grep -c 'shots=' == 0`).
    - File constructs `qml.device("default.mixed"` and inserts both `qml.DepolarizingChannel` and `qml.AmplitudeDamping`.
    - The 11 condition tokens are all recognized, including `ampdamp_0.01` (grep `ampdamp_0.01`); the four `ampdamp_*` tokens cover γ ∈ {0,0.001,0.01,0.05}.
    - Every QNode constructed uses `diff_method=None` (no `diff_method="backprop"` / `"adjoint"` in the file).
    - No `multiprocessing` import or `Pool` reference anywhere (09.1 Pitfall 4).
    - `full_metric_suite` is imported from `revision.core.eval` and called without modification; metrics emitted at two `scale` values per cell.
    - `condition=analytic` branch reads frozen `transform_ablation/.../samples.npy` and does not call the generation function.
    - `git diff --stat core/` is empty (D-10-13).
  </acceptance_criteria>
  <done>run_sensitivity.py is a complete CLI driver supporting all 11 condition tokens (incl. ampdamp_0.01), builds set_shots and default.mixed QNodes with diff_method=None, recomputes the unchanged dual-scale fidelity suite, and writes an idempotent per-cell bundle. core/ untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Smoke gate — prove the device-swap harness is faithful to the frozen analytic reference</name>
  <files>results/sensitivity/runs/analytic/B/42/metrics.json</files>
  <read_first>
    - run_sensitivity.py (final state from Task 2)
    - .planning/phases/12-sensitivity-analysis/12-RESEARCH.md (Validation Note — smoke gate + baseline-cell sanity; Pitfalls 3 & 4 detectors)
    - results/fidelity_dualscale.json (the quantum Pipeline-B seed-42 OD-scale rows the smoke cell must match within fp tolerance)
  </read_first>
  <action>
    Run the harness-faithfulness smoke gate (no new file beyond the produced bundle):
    1. Run `python run_sensitivity.py --pipeline B --seed 42 --condition analytic`
       and confirm it reuses the frozen samples.npy (does not regenerate).
    2. Additionally regenerate the analytic cell through the generation path (a one-off check,
       e.g. `--condition shots_None`-equivalent path or a temporary harness call) and assert
       the regenerated samples reproduce the frozen
       `transform_ablation/runs/B/42/samples.npy` to fp tolerance (Pitfall 3 detector — same
       params + same default_rng(seed) + same analytic device must byte-match within fp).
    3. Assert the analytic cell's OD-scale fidelity metrics reproduce the corresponding
       quantum Pipeline-B seed-42 rows in `results/fidelity_dualscale.json` within fp
       tolerance (Pitfall 4 detector — proves the *0.1 + seed*7919+1 reconstruction contracts
       are wired correctly).
    If either assertion fails, the harness is not faithful — STOP and fix run_sensitivity.py
    before any sweep. Record the smoke result (max abs error vs frozen samples, and the
    metric deltas vs fidelity_dualscale.json) in the plan SUMMARY.
  </action>
  <verify>
    <automated>cd /Users/shawngibford/dev/phd/qGAN && test -s results/sensitivity/runs/analytic/B/42/metrics.json && python -c "import json; m=json.load(open('results/sensitivity/runs/analytic/B/42/metrics.json')); print('emd_keys', any('emd' in str(k) or 'emd' in str(m) for k in [m]))" && if git diff --quiet core/; then echo OK; else echo CORE_DIRTY; exit 1; fi</automated>
  </verify>
  <acceptance_criteria>
    - `results/sensitivity/runs/analytic/B/42/metrics.json` exists and is non-empty.
    - Regenerated analytic samples match frozen `transform_ablation/runs/B/42/samples.npy` with max abs error < 1e-6 (recorded in SUMMARY).
    - Analytic-cell OD-scale EMD matches the quantum Pipeline-B seed-42 OD-scale value in `fidelity_dualscale.json` within < 1e-6 absolute (recorded in SUMMARY).
    - `git diff --stat core/` is empty (the automated verify exits non-zero — CORE_DIRTY — when core/ is modified).
    - SUMMARY documents the deliberate PennyLane-0.44.0-pin deviation (driver asserts version; sweep must not use ./qgan_env).
  </acceptance_criteria>
  <done>The analytic smoke cell is produced and proven faithful: regenerated samples byte-match the frozen reference and OD-scale metrics match fidelity_dualscale.json within fp tolerance. The full sweep (Plan 02) is now safe to run.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI args → filesystem paths | `--pipeline`, `--seed`, `--condition`, `--out-root`, `--csv-path` interpolated into artifact paths |
| Python interpreter → PennyLane version | Wrong interpreter (qgan_env 0.43.0) silently changes shot/noise semantics |
| Driver → frozen reference artifacts | transform_ablation checkpoints + samples.npy are the reproduction baseline |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Tampering | env / PennyLane version skew | mitigate | Startup `assert qml.__version__ == "0.44.0"` fails loud (Pitfall 5); sweep wrapper pins 0.44 interpreter |
| T-12-02 | Tampering | frozen reference artifacts (samples.npy / checkpoint.pt) | mitigate | Driver opens checkpoints/samples read-only; writes only under `results/sensitivity/runs/`; smoke gate detects any drift from frozen reference |
| T-12-03 | Tampering | `core/` integrity (D-10-13) | mitigate | `git diff --quiet core/` asserted (exits non-zero on dirty core/) in every task's acceptance criteria; noisy circuit body copied into driver, never edited in core/ |
| T-12-04 | Information Disclosure | local scientific compute, no PII/network/secrets | accept | Offline simulator; no untrusted input, no network surface, no credentials — low-value target |
</threat_model>

<verification>
- `python run_sensitivity.py --help` exits 0; CLI exposes pipeline/seed/condition.
- Smoke cell `analytic/B/42` produced; regenerated samples match frozen reference < 1e-6; OD-scale metrics match fidelity_dualscale.json < 1e-6.
- `git diff --quiet core/` (verify exits non-zero on dirty core/).
- No `multiprocessing` / `Pool`; no `shots=` device kwarg; no `backprop`/`adjoint` diff_method.
- All 11 condition tokens recognized incl. `ampdamp_0.01`.
- Driver asserts PennyLane 0.44.0 at startup.
</verification>

<success_criteria>
SENS-01/SENS-02 harness is built and proven faithful: a single CLI invocation produces a
faithful per-cell bundle, the analytic smoke cell reproduces the frozen Phase 09.1/10
numbers within fp tolerance, and `core/` is byte-untouched. Full grid execution
is delegated to Plan 02.
</success_criteria>

<output>
Create `.planning/phases/12-sensitivity-analysis/12-01-SUMMARY.md` when done. Record:
the smoke max-abs-error vs frozen samples, the OD-scale metric delta vs
fidelity_dualscale.json, the chosen per-layer channel-insertion strategy, and the
documented PennyLane-0.44.0-pin deviation.
</output>
</content>
