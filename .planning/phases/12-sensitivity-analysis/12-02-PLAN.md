---
phase: 12-sensitivity-analysis
plan: 02
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - revision/run_sensitivity_sweep.sh
  - revision/results/shot_noise_sensitivity.json
  - revision/results/noise_model_sensitivity.json
autonomous: true
requirements: [SENS-01, SENS-02]
must_haves:
  truths:
    - "A sweep wrapper runs every (condition, pipeline, seed) cell idempotently with at most 2 concurrent OS processes (xargs -P, never multiprocessing.Pool)"
    - "Shot-noise degradation curve at shots {analytic, 8192, 1024} written to revision/results/shot_noise_sensitivity.json"
    - "Noise-model degradation for depolarizing p in {0,0.001,0.01,0.05} and amplitude-damping gamma in {0,0.001,0.01,0.05} written to revision/results/noise_model_sensitivity.json"
    - "Full sweep completes on local Mac within the phase session (wall time documented)"
    - "revision/core/ is byte-untouched"
  artifacts:
    - path: "revision/results/shot_noise_sensitivity.json"
      provides: "SENS-01 deliverable — shot-noise degradation, long-form rows"
      contains: "shots"
    - path: "revision/results/noise_model_sensitivity.json"
      provides: "SENS-02 deliverable — noise-channel degradation, long-form rows"
      contains: "noise_model"
    - path: "revision/run_sensitivity_sweep.sh"
      provides: "Idempotent xargs -P 2 sweep orchestrator with atomic sweep_status.json"
      min_lines: 150
  key_links:
    - from: "revision/run_sensitivity_sweep.sh"
      to: "revision/run_sensitivity.py"
      via: "one (pipeline,seed,condition) cell per python -m invocation"
      pattern: "run_sensitivity"
    - from: "revision/run_sensitivity_sweep.sh"
      to: "xargs -P 2"
      via: "OS-process parallelism, never multiprocessing.Pool (09.1 Pitfall 4)"
      pattern: "xargs -P"
---

<objective>
Build `revision/run_sensitivity_sweep.sh` (idempotent, atomic-status, `xargs -P 2`
orchestrator copied near-verbatim from `run_baselines_sweep.sh`), execute the full
SENS-01 + SENS-02 grid via the Plan 01 driver, and aggregate the per-cell bundles into
the two headline deliverables `shot_noise_sensitivity.json` and
`noise_model_sensitivity.json`. Pipeline B is headline, Pipeline A supplementary control.
3-seed degradation grids {42, 43, 44} (D-12-02). Document the wall time (Success
Criterion 4) and the deliberate PennyLane-0.44.0 interpreter pin (Pitfall 5).

Purpose: Produces the two SENS-01/SENS-02 degradation artifacts the manuscript reads for
the R1-M4 / R2-1 robustness rebuttal.
Output: `run_sensitivity_sweep.sh`, `shot_noise_sensitivity.json`,
`noise_model_sensitivity.json`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-sensitivity-analysis/12-CONTEXT.md
@.planning/phases/12-sensitivity-analysis/12-RESEARCH.md
@.planning/phases/12-sensitivity-analysis/12-PATTERNS.md
@.planning/phases/12-sensitivity-analysis/12-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build run_sensitivity_sweep.sh (idempotent skip, atomic status, xargs -P 2 guardrail, 0.44 interpreter pin)</name>
  <files>revision/run_sensitivity_sweep.sh</files>
  <read_first>
    - revision/run_baselines_sweep.sh (the analog — copy near-verbatim; lines 20-24 retry note, 34-43 no-Pool rationale, 45-60 status schema, 97-107 interpreter selection TO DEVIATE FROM, 146-156 --parallel guardrail, 174-184 is_complete, 199-282 update_status, 290-335 run_one, 400-419 xargs dispatch)
    - revision/run_sensitivity.py (Plan 01 driver — confirm CLI tokens: --pipeline {A|B} --seed N --condition <token>, --out-root)
    - .planning/phases/12-sensitivity-analysis/12-PATTERNS.md (run_sensitivity_sweep.sh section — every copy-from line reference; the interpreter-selection DEVIATION instruction)
    - .planning/phases/12-sensitivity-analysis/12-RESEARCH.md (Pitfall 5 venv skew; Open Q1 recommendation (a))
    - .planning/phases/12-sensitivity-analysis/12-01-SUMMARY.md (smoke-gate result; confirmed driver behavior)
  </read_first>
  <action>
    Create `revision/run_sensitivity_sweep.sh` by copying `run_baselines_sweep.sh`
    near-verbatim. Concrete required changes vs the analog:
    - Worklist: cartesian product of CONDITIONS × PIPELINES × SEEDS. CONDITIONS =
      {analytic, shots_8192, shots_1024, depol_0.0, depol_0.001, depol_0.01, depol_0.05,
      ampdamp_0.0, ampdamp_0.001, ampdamp_0.01, ampdamp_0.05} — exactly 11 tokens
      (3 shot: analytic/shots_8192/shots_1024; 4 depol: 0.0/0.001/0.01/0.05;
      4 ampdamp: 0.0/0.001/0.01/0.05), matching the Plan 01 driver token set and
      ROADMAP Success Criterion 2 / CONTEXT D-12-02 (amplitude-damping γ ∈
      {0,0.001,0.01,0.05}). Note: depol_0.0 and ampdamp_0.0 are the
      same physical p=0/γ=0 baseline — emit BOTH so each curve has its own zero anchor, OR
      reuse one and tag both noise_model rows; document the choice). PIPELINES = {B, A}
      (B headline, A supplementary). SEEDS = {42, 43, 44} (D-12-02 — 3-seed degradation
      grids; the 5-seed roll-up is Plan 03 only, do NOT add seeds here).
    - `is_complete c p s`: bundle present iff
      `${OUT_ROOT}/runs/${c}/${p}/${s}/{config.yaml,samples.npy,metrics.json}` all non-empty
      (copy run_baselines_sweep.sh:174-184, retarget tokens).
    - `update_status`: copy verbatim from run_baselines_sweep.sh:199-282 (tmp-file +
      `os.fsync` + `os.rename` POSIX-atomic + `flock -x 9`); status schema
      `{started_at, parallel, runs:[...], all_complete, completed_count, total_count}`.
    - `--parallel` guardrail: copy run_baselines_sweep.sh:146-156 — accept only 1 or 2,
      exit 3 otherwise.
    - xargs dispatch: `< "$WORKLIST" xargs -P "$PARALLEL" -L 1 bash -c 'run_one "$0" "$1" "$2"'`
      (copy run_baselines_sweep.sh:400-419). NEVER multiprocessing.Pool (09.1 Pitfall 4) —
      keep the no-Pool rationale comment from :34-43.
    - `run_one`: copy run_baselines_sweep.sh:290-335 — `set +e` around the python call,
      capture rc, mark failed and continue (sweep does not abort on one cell), per-cell
      `_stdout.log`/`_stderr.log`. Target = `python -m` / `python revision/run_sensitivity.py
      --pipeline <p> --seed <s> --condition <c> --out-root revision/results/sensitivity`.
    - Interpreter selection: DEVIATE from analog (run_baselines_sweep.sh:97-107 hard-prefers
      ./qgan_env which is PennyLane 0.43.0). Do NOT prefer the venv. Select an explicit
      0.44.0 interpreter (system python3) and rely on the driver's
      `assert qml.__version__ == "0.44.0"` to fail loud (Pitfall 5 / RESEARCH Open Q1 (a)).
      Document this deviation prominently in the sweep header comment.
    No fenced code blocks in this action — implementation detail is in the read_first excerpts.
  </action>
  <verify>
    <automated>cd /Users/shawngibford/dev/phd/qGAN && bash -n revision/run_sensitivity_sweep.sh && grep -q 'xargs -P' revision/run_sensitivity_sweep.sh && grep -q 'flock' revision/run_sensitivity_sweep.sh && grep -q 'os.rename' revision/run_sensitivity_sweep.sh && ! grep -q 'qgan_env' revision/run_sensitivity_sweep.sh && ! grep -qi 'multiprocessing' revision/run_sensitivity_sweep.sh && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n revision/run_sensitivity_sweep.sh` exits 0 (syntax valid).
    - Script uses `xargs -P` with `-L 1`; the `--parallel` guardrail rejects values other than 1 or 2 (exit 3).
    - `update_status` writes via tempfile + `os.fsync` + `os.rename` under `flock -x 9` (atomic).
    - Script does NOT reference `qgan_env` (deliberate interpreter deviation, Pitfall 5) and contains NO `multiprocessing`/`Pool`.
    - CONDITIONS list enumerates exactly the 11 tokens above — includes `ampdamp_0.01` (grep `ampdamp_0.01` in the CONDITIONS list); the 4 ampdamp tokens cover γ ∈ {0,0.001,0.01,0.05} (D-12-02 / ROADMAP SC-2).
    - Worklist enumerates seeds {42,43,44} only (no 45/46 — D-12-02); grep shows no `45`/`46` seed tokens in the SEEDS list.
    - `is_complete` checks all three of config.yaml, samples.npy, metrics.json non-empty.
  </acceptance_criteria>
  <done>run_sensitivity_sweep.sh exists, syntax-valid, idempotent with atomic status, xargs -P 1|2 guarded, no venv preference, no multiprocessing, 11-condition (incl. ampdamp_0.01) × 3-seed grid.</done>
</task>

<task type="auto">
  <name>Task 2: Execute the full SENS-01 + SENS-02 sweep and verify per-cell bundles</name>
  <files>revision/results/sensitivity/runs</files>
  <read_first>
    - revision/run_sensitivity_sweep.sh (Task 1 output)
    - .planning/phases/12-sensitivity-analysis/12-RESEARCH.md (compute budget: ≤~12 s/run, full grid well under 10 min — Success Criterion 4; Validation Note monotonicity expectation; baseline-cell sanity)
    - .planning/phases/12-sensitivity-analysis/12-01-SUMMARY.md (smoke-gate faithfulness result)
  </read_first>
  <action>
    Run `bash revision/run_sensitivity_sweep.sh --parallel 2` (system python3 / PennyLane
    0.44.0). Grid: 11 conditions × 2 pipelines × 3 seeds = 66 cells (the analytic cells
    reuse frozen samples per Plan 01, so only the 10 fresh-pass conditions × 2 × 3 = 60 cells
    do forward passes). Capture total wall time (Success Criterion 4 — research budget probe
    says well under 10 min). After the sweep:
    - Confirm `sweep_status.json` reports `all_complete: true` and `completed_count ==
      total_count`; no cell marked `failed`. Re-run the sweep once if any cell failed
      (idempotent — completed cells skip).
    - Baseline-cell sanity (Pitfall 4 detector): the `depol_0.0` / `ampdamp_0.0` /
      `shots`=analytic-equivalent cells for Pipeline B seed 42 must reproduce the
      corresponding `fidelity_dualscale.json` quantum OD-scale rows within fp tolerance.
    - Record the observed degradation trend (EMD generally increases as shots decrease and
      as p/γ increase) — note any non-monotonicity as a flag, NOT a hard failure (3-seed
      noise can produce local non-monotonicity; D-12-02 acknowledges this).
    Do NOT escalate to 5 seeds unless the 3-seed spread visibly obscures the trend — that is
    a deferred planning-time decision (CONTEXT Deferred Ideas), not a default.
  </action>
  <verify>
    <automated>cd /Users/shawngibford/dev/phd/qGAN && python -c "import json; s=json.load(open('revision/results/sensitivity/sweep_status.json')); assert s['all_complete'] is True, s; assert s['completed_count']==s['total_count'], s; assert not any(r.get('status')=='failed' for r in s['runs']), [r for r in s['runs'] if r.get('status')=='failed']; print('SWEEP_OK', s['completed_count'], s['total_count'])"</automated>
  </verify>
  <acceptance_criteria>
    - `revision/results/sensitivity/sweep_status.json` has `all_complete: true` and `completed_count == total_count` (total_count == 66 = 11×2×3).
    - No run in `sweep_status.json` has `status == "failed"`.
    - Every `(condition, pipeline, seed)` cell directory contains non-empty config.yaml, samples.npy, metrics.json — including all four `ampdamp_*` conditions (0.0/0.001/0.01/0.05).
    - Baseline cell (Pipeline B, seed 42, p=0/γ=0) OD-scale EMD matches `fidelity_dualscale.json` quantum row within < 1e-6 absolute (recorded in SUMMARY).
    - Total sweep wall time recorded in SUMMARY (Success Criterion 4).
    - `git diff --stat revision/core/` is empty.
  </acceptance_criteria>
  <done>All 66 sweep cells complete (sweep_status all_complete true, zero failed), baseline cell matches the frozen reference, wall time documented within session budget.</done>
</task>

<task type="auto">
  <name>Task 3: Aggregate per-cell bundles into shot_noise_sensitivity.json and noise_model_sensitivity.json</name>
  <files>revision/results/shot_noise_sensitivity.json, revision/results/noise_model_sensitivity.json, revision/run_sensitivity.py</files>
  <read_first>
    - revision/run_sensitivity.py (current state — add an aggregation entrypoint, e.g. `--emit-rollup`, OR a small `aggregate()` invoked by the sweep; do NOT create a third driver file)
    - revision/results/baseline_comparison.json (the established long-form rows[] contract to extend — top-level keys schema/model_kinds/pipelines/seeds/data_hash; row schema {model_kind,pipeline,seed,metric_name,scale,value})
    - .planning/phases/12-sensitivity-analysis/12-PATTERNS.md (Output JSON artifacts section — extend-not-replace; add condition+shots | noise_model+noise_level dims)
    - .planning/phases/12-sensitivity-analysis/12-RESEARCH.md (long-form schema; degradation-curve representation is Claude's discretion within the long-form contract)
  </read_first>
  <action>
    Add an aggregation step (a function/CLI flag inside run_sensitivity.py — NOT a new
    driver file; files_modified declares run_sensitivity.py for this) that walks
    `revision/results/sensitivity/runs/*/{B,A}/{42,43,44}/metrics.json` and emits two
    headline JSONs anchored at REPO:
    - `revision/results/shot_noise_sensitivity.json` — SENS-01: rows for conditions
      {analytic, shots_8192, shots_1024}. Each row extends the long-form schema:
      `{model_kind:"quantum", pipeline, seed, metric_name, scale, value, condition, shots}`
      (shots = None for analytic, 8192, 1024). Top-level provenance block:
      `{schema, model_kinds, pipelines, seeds:[42,43,44], conditions, generated_at}`.
    - `revision/results/noise_model_sensitivity.json` — SENS-02: rows for conditions
      {depol_*, ampdamp_*} — all 8 noise cells incl. `ampdamp_0.01`. Row schema:
      `{model_kind:"quantum", pipeline, seed, metric_name, scale, value, condition,
      noise_model, noise_level}` (noise_model ∈ {depolarizing, amplitude_damping},
      noise_level = p or γ; per noise_model the noise_level set is {0,0.001,0.01,0.05}).
      Top-level provenance block records the per-layer channel-insertion
      strategy used (RESEARCH Open Q2 default) and seeds [42,43,44].
    Keep `{model_kind, pipeline, seed, metric_name, scale, value}` intact in every row
    (extend, do not replace — code_context line 89). Dual-scale rows (OD + transformed)
    preserved via the `scale` field. Do NOT compute mean±std here — the degradation grids
    are raw per-seed rows (D-12-02 says degradation is a trend; the 5-seed mean±std roll-up
    is SENS-03/Plan 03 territory only).
  </action>
  <verify>
    <automated>cd /Users/shawngibford/dev/phd/qGAN && python -c "import json; a=json.load(open('revision/results/shot_noise_sensitivity.json')); b=json.load(open('revision/results/noise_model_sensitivity.json')); ra=a['rows']; rb=b['rows']; assert all({'model_kind','pipeline','seed','metric_name','scale','value','condition','shots'} <= set(r) for r in ra), 'shot schema'; assert all({'model_kind','pipeline','seed','metric_name','scale','value','condition','noise_model','noise_level'} <= set(r) for r in rb), 'noise schema'; assert {r['shots'] for r in ra} == {None,8192,1024}, sorted({str(r['shots']) for r in ra}); assert {r['noise_model'] for r in rb} == {'depolarizing','amplitude_damping'}; assert {r['noise_level'] for r in rb if r['noise_model']=='amplitude_damping'} == {0,0.001,0.01,0.05}, sorted({r['noise_level'] for r in rb if r['noise_model']=='amplitude_damping'}); assert {r['seed'] for r in ra}=={42,43,44} and {r['seed'] for r in rb}=={42,43,44}; print('AGG_OK', len(ra), len(rb))"</automated>
  </verify>
  <acceptance_criteria>
    - `revision/results/shot_noise_sensitivity.json` exists; every row has keys `{model_kind,pipeline,seed,metric_name,scale,value,condition,shots}`; the set of `shots` values is exactly `{None, 8192, 1024}`.
    - `revision/results/noise_model_sensitivity.json` exists; every row has keys `{model_kind,pipeline,seed,metric_name,scale,value,condition,noise_model,noise_level}`; `noise_model` values are exactly `{depolarizing, amplitude_damping}`; `noise_level` covers exactly `{0,0.001,0.01,0.05}` for EACH of depolarizing and amplitude_damping (incl. γ=0.01).
    - Both files cover seeds exactly `{42,43,44}` (D-12-02) and both pipelines `{A,B}`.
    - Both files carry two `scale` values per cell (dual-scale EVAL-05).
    - `noise_model_sensitivity.json` provenance records the per-layer channel-insertion strategy.
    - No new third driver file created; aggregation lives in run_sensitivity.py. `git diff --stat revision/core/` empty.
  </acceptance_criteria>
  <done>shot_noise_sensitivity.json and noise_model_sensitivity.json exist with correct extended long-form schema, full 3-seed × 2-pipeline × dual-scale coverage (incl. ampdamp_0.01), and provenance — SENS-01 and SENS-02 deliverables complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Sweep wrapper → interpreter | Wrong interpreter (qgan_env 0.43.0) silently corrupts shot/noise semantics |
| CLI worklist → filesystem | condition/pipeline/seed tokens interpolated into run-dir paths |
| Aggregator → per-cell metrics.json | Trust boundary between sweep output and headline deliverable |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-05 | Tampering | sweep interpreter selection (Pitfall 5) | mitigate | Wrapper does NOT prefer ./qgan_env; pins 0.44 interpreter; driver's `assert qml.__version__=="0.44.0"` fails loud per cell |
| T-12-06 | Denial of Service | unbounded parallelism on local Mac | mitigate | `--parallel` guardrail rejects >2; xargs -P 2 OS-process cap; no multiprocessing.Pool (09.1 Pitfall 4) |
| T-12-07 | Tampering | partial/stale per-cell bundle on interrupted sweep | mitigate | Driver rmtree+recreate run dir (idempotent); atomic sweep_status.json (tmpfile+fsync+rename under flock); is_complete gates re-run |
| T-12-08 | Tampering | accidental overwrite of frozen reference artifacts | mitigate | Sweep writes only under revision/results/sensitivity/runs/; baseline-cell sanity check detects drift; core/ diff asserted empty |
| T-12-09 | Information Disclosure | offline compute, no PII/network/secrets | accept | No network/auth/credentials — low-value local target |
</threat_model>

<verification>
- `bash -n run_sensitivity_sweep.sh` exits 0; xargs -P 1|2 guarded; no qgan_env; no multiprocessing.
- CONDITIONS list has all 11 tokens incl. `ampdamp_0.01`; total_count == 66.
- `sweep_status.json`: all_complete true, completed==total, zero failed.
- shot_noise_sensitivity.json / noise_model_sensitivity.json present with correct extended long-form schema, seeds {42,43,44}, dual-scale, full noise/shot coverage (ampdamp γ set {0,0.001,0.01,0.05}).
- Baseline cell matches fidelity_dualscale.json within fp tolerance.
- Sweep wall time documented (Success Criterion 4). `git diff --stat revision/core/` empty.
</verification>

<success_criteria>
SENS-01 and SENS-02 deliverables complete: idempotent xargs -P 2 sweep executed across the
full 3-seed × 2-pipeline × 11-condition shot/noise grid (amplitude-damping γ ∈
{0,0.001,0.01,0.05}) within the local-Mac session budget,
`shot_noise_sensitivity.json` and `noise_model_sensitivity.json` emitted on the extended
long-form schema, baseline cells faithful to the frozen reference, `revision/core/`
untouched.
</success_criteria>

<output>
Create `.planning/phases/12-sensitivity-analysis/12-02-SUMMARY.md` when done. Record:
total sweep wall time (Success Criterion 4), the depol_0.0/ampdamp_0.0 baseline-cell
delta vs fidelity_dualscale.json, observed monotonicity of the degradation curves (and any
flagged non-monotonicity), and whether the 3→5 seed escalation was triggered (default: no).
</output>
</content>
