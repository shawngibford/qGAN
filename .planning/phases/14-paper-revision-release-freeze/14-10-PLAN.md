---
phase: 14-paper-revision-release-freeze
plan: 10
type: execute
wave: 8
depends_on: ["14-03", "14-04", "14-08", "14-09"]
files_modified:
  - run_figure_suite.py
  - figures/
autonomous: true
requirements: [PAPER-01, PAPER-09]
must_haves:
  truths:
    - "Render-only: NO retraining, NO resampling, NO checkpoint reload — every figure is a pure read of existing audited JSON/npz under results/ (no `.fit(`, no `def train_`, no `model.sample(`, no `best_checkpoint.pt` in the 7 new render functions)"
    - "revision.core surface used ONLY for trivial mean/std aggregations over already-evaluated values (numpy/statistics); no metric recomputation, no new compute_emd/compute_moments/compute_acf/compute_dtw calls inside the 7 new render functions beyond what 14-04 already established for OD-reconstruction (the new figures consume the audited JSONs as-is)"
    - "All 7 new figures land as triple PNG + PDF + companion-JSON under figures/, each driven by `_save()` (dpi=150, bbox_inches='tight', plt.close), every figure traceable to its source JSON via `companion.source_artifact`"
    - "Every new companion JSON sets `render_only: true` and lists its source artifact path(s); the auto-rglob in verify_number_provenance.py covers them with NO verifier edit"
    - "tstr.json, noise_model_sensitivity.json, and shot_noise_sensitivity.json — three currently-unconsumed audited paper-facing JSONs — are each consumed by exactly one new figure (tstr_crossmodel, noise_robustness_quantum, shot_noise_robustness)"
    - "Frozen-checkpoint headline (source=frozen_checkpoint_epoch_1969, D-14-10) is rendered as a visually distinct marker (HEADLINE_COLOR + HEADLINE_LABEL, a separate dashed line / diamond / annotated series — NEVER a same-style bar/point as iqp_sel_55_repro) on every new figure where it could appear (training_convergence_all_models, failure_modes_summary, param_efficiency_pareto)"
    - "Negative R² values from tstr.json are plotted HONESTLY — no clamping, no abs(), no log-rescaling; the companion JSON's `caption_note` field carries the explicit reviewer-facing note ('R² < 0 means worse-than-mean — informative, not a render bug')"
    - "core/ remains byte-frozen (D-14-22): `git diff --stat core/` returns empty after this plan completes"
    - "verify_number_provenance.py is NOT modified (D-14-16): every companion JSON lands under figures/ which is auto-rglob'd by the existing gate"
    - "Plan extends run_figure_suite.py with 7 new render-* functions wired into main() AFTER the existing 14-08 dual-scale block and BEFORE render_existing_introspection — the established render-only contract (headless matplotlib.use('Agg'), _require/_load_json loud-fail, _save dual PNG+PDF+JSON, _find_repo_root) is reused unchanged; no new entry-point script is added"
  artifacts:
    - path: "figures/training_convergence_all_models.png"
      provides: "9-model EMD-vs-epoch trajectories with mean±std seed band and frozen-headline marker"
    - path: "figures/training_convergence_all_models.pdf"
      provides: "PDF twin of training_convergence_all_models.png"
    - path: "figures/training_convergence_all_models.json"
      provides: "Companion: source = 45 matched2000/runs/<model>/<seed>/metrics.json + headline_canonical.json; per-model mean/std emd_avg trajectories; frozen headline epoch 1969 marker"
    - path: "figures/tstr_crossmodel.png"
      provides: "Cross-model TSTR grouped bars (R², MAE, RMSE) for Pipeline A and Pipeline B; negative R² plotted honestly"
    - path: "figures/tstr_crossmodel.pdf"
      provides: "PDF twin of tstr_crossmodel.png"
    - path: "figures/tstr_crossmodel.json"
      provides: "Companion: source = results/tstr.json; per-(model,pipeline) r2_mean/r2_std/mae_mean/mae_std/rmse_mean/rmse_std + caption_note about R²<0"
    - path: "figures/failure_modes_summary.png"
      provides: "Diagnostic grid: distribution overlay row, ACF-lag-1 row, log_return EMD row × 9 models (ordered by ascending OD EMD) with red failure-mode annotations"
    - path: "figures/failure_modes_summary.pdf"
      provides: "PDF twin of failure_modes_summary.png"
    - path: "figures/failure_modes_summary.json"
      provides: "Companion: source = matched2000_dualscale.json + per-model dist_*.json + acf_*.json; per-cell metric values + failure-mode annotations"
    - path: "figures/param_efficiency_pareto.png"
      provides: "log10(params) × EMD scatter, OD vs log_return facets, family-colored markers, frozen-headline diamond"
    - path: "figures/param_efficiency_pareto.pdf"
      provides: "PDF twin of param_efficiency_pareto.png"
    - path: "figures/param_efficiency_pareto.json"
      provides: "Companion: source = model_info.json + matched2000_dualscale.json; per-model n_params + EMD mean±std for both scales + headline marker"
    - path: "figures/seed_variance_per_model.png"
      provides: "3×3 facet grid of per-seed EMD trajectories (light) + mean (bold) per model"
    - path: "figures/seed_variance_per_model.pdf"
      provides: "PDF twin of seed_variance_per_model.png"
    - path: "figures/seed_variance_per_model.json"
      provides: "Companion: source = 45 matched2000/runs/<model>/<seed>/metrics.json; per-(model,seed) emd_avg trajectories + tight-vs-noisy caption_note"
    - path: "figures/noise_robustness_quantum.png"
      provides: "EMD vs noise level for depolarizing AND amplitude-damping channels"
    - path: "figures/noise_robustness_quantum.pdf"
      provides: "PDF twin of noise_robustness_quantum.png"
    - path: "figures/noise_robustness_quantum.json"
      provides: "Companion: source = results/noise_model_sensitivity.json; per-(noise_model,noise_level,pipeline) aggregated EMD + monotonicity note"
    - path: "figures/shot_noise_robustness.png"
      provides: "EMD vs shot count (log-x) with analytic-statevector reference line"
    - path: "figures/shot_noise_robustness.pdf"
      provides: "PDF twin of shot_noise_robustness.png"
    - path: "figures/shot_noise_robustness.json"
      provides: "Companion: source = results/shot_noise_sensitivity.json; per-(condition,pipeline) aggregated EMD + analytic baseline marker"
  key_links:
    - from: "figures/training_convergence_all_models.{png,pdf,json}"
      to: "results/matched2000/runs/<model>/<seed>/metrics.json (×45) + results/headline_canonical.json"
      via: "loud-fail _require/_load_json on every per-run metrics.json + headline read; mean/std aggregated via numpy over emd_avg arrays (no recompute)"
      pattern: "FileNotFoundError|_load_json|emd_avg"
    - from: "figures/tstr_crossmodel.{png,pdf,json}"
      to: "results/tstr.json"
      via: "render-only consumption of tstr[<model>|<pipeline>] block r2_mean/r2_std/mae_mean/mae_std/rmse_mean/rmse_std; negative R² plotted honestly"
      pattern: "tstr.json|r2_mean|caption_note"
    - from: "figures/failure_modes_summary.{png,pdf,json}"
      to: "results/matched2000_dualscale.json + figures/dist_*.json + figures/acf_*.json"
      via: "render-only lookup of aggregates[]+companion JSONs; cell values + failure-mode annotations; models ordered by ascending OD EMD"
      pattern: "matched2000_dualscale.json|_agg_lookup|dist_|acf_"
    - from: "figures/param_efficiency_pareto.{png,pdf,json}"
      to: "results/model_info.json + results/matched2000_dualscale.json"
      via: "render-only lookup of models[].parameter_count + aggregates[] EMD mean±std; family colors; frozen-headline diamond"
      pattern: "model_info.json|parameter_count|HEADLINE_COLOR"
    - from: "figures/seed_variance_per_model.{png,pdf,json}"
      to: "results/matched2000/runs/<model>/<seed>/metrics.json (×45)"
      via: "loud-fail _require on every per-run metrics.json; overlay 5 emd_avg lines + bold mean; no recompute"
      pattern: "FileNotFoundError|emd_avg"
    - from: "figures/noise_robustness_quantum.{png,pdf,json}"
      to: "results/noise_model_sensitivity.json"
      via: "render-only iteration over rows[] filtered on metric_name=='emd', grouped by (noise_model,noise_level,pipeline); aggregated by mean over 3 seeds"
      pattern: "noise_model_sensitivity.json|noise_model|noise_level"
    - from: "figures/shot_noise_robustness.{png,pdf,json}"
      to: "results/shot_noise_sensitivity.json"
      via: "render-only iteration over rows[] filtered on metric_name=='emd', grouped by (condition,shots,pipeline); analytic baseline as horizontal reference"
      pattern: "shot_noise_sensitivity.json|shots|condition"
    - from: "run_figure_suite.py main()"
      to: "render_training_convergence_all_models / render_tstr_crossmodel / render_failure_modes_summary / render_param_efficiency_pareto / render_seed_variance_per_model / render_noise_robustness_quantum / render_shot_noise_robustness"
      via: "wired AFTER render_matched2000_dualscale_comparison_table and BEFORE render_existing_introspection; each function loud-fails on missing input JSON, never silently produces partial output"
      pattern: "render_training_convergence_all_models|render_tstr_crossmodel|render_failure_modes_summary|render_param_efficiency_pareto|render_seed_variance_per_model|render_noise_robustness_quantum|render_shot_noise_robustness"
---

<objective>
Extend `run_figure_suite.py` with 7 NEW render-only figures that complete
the manuscript's "full story" by consuming previously-unconsumed audited paper-facing
JSONs (`tstr.json`, `noise_model_sensitivity.json`, `shot_noise_sensitivity.json`)
and previously-absent head-to-head views over already-computed artifacts (training
convergence across all models, failure-mode diagnostic grid, parameter-efficiency
Pareto, per-model seed-variance facets). NO retraining, NO resampling, NO new
evaluation — render-only over existing audited JSON / npz.

Purpose: Phase 14 already emits 76 figures, but the paper's full narrative arc is
incomplete because (a) three audited JSONs that exist on disk are not consumed by
any figure (tstr/noise/shot-noise), and (b) several head-to-head views the paper
needs (cross-model training convergence, failure-mode triage table, parameter-vs-
EMD Pareto, per-model seed variance) are missing. Closing the gap requires zero
new compute — only new render functions over JSON that's already on disk and
already audited (data_hash 91e447d4624e25b3 lineage).

Output: 7 new figure triples (PNG + PDF + companion JSON) under
`figures/`, all gated by the existing `verify_number_provenance.py`
auto-rglob (no verifier edit), all driven by the established 14-04 render-only
contract (headless matplotlib.use("Agg"), `_require`/`_load_json` loud-fail, `_save`
dual PNG+PDF+JSON), and all preserving the D-14-10 conflation guard (frozen-
checkpoint headline visually distinct from iqp_sel_55_repro on every figure where
both could appear) and the D-14-22 byte-freeze (`core/` untouched).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-paper-revision-release-freeze/14-CONTEXT.md
@.planning/phases/14-paper-revision-release-freeze/14-RESEARCH.md
@.planning/phases/14-paper-revision-release-freeze/14-PATTERNS.md
@.planning/phases/14-paper-revision-release-freeze/14-04-PLAN.md
@.planning/phases/14-paper-revision-release-freeze/14-08-PLAN.md
@.planning/phases/14-paper-revision-release-freeze/14-09-PLAN.md

<interfaces>
<!-- All 7 tasks extend run_figure_suite.py — the established render-only
     module from 14-04 + 14-08. The interfaces below are the EXACT building
     blocks already in that file; reuse them verbatim, do NOT re-implement. -->

run_figure_suite.py building blocks (lines noted are post-14-08):
- Headless: `import matplotlib; matplotlib.use("Agg")` BEFORE pyplot (lines 45-47).
- `_bootstrap_repo_on_path()` (lines 54-69) + `_find_repo_root()` (lines 144-150).
- `_require(path, what)` — raises FileNotFoundError with the render-only message
  on any missing artifact (lines 153-168). USE THIS for every input JSON/npz.
- `_load_json(path, what)` = `_require` + `json.loads(path.read_text())` (171-174).
- `_save(fig, figures_dir, stem, companion)` — writes `<stem>.png` + `<stem>.pdf`
  + `<stem>.json` at dpi=150 bbox_inches="tight" + plt.close + json default=float
  (177-193). EVERY new figure goes through this — do NOT hand-roll savefig.
- Constants already in scope: `MATCHED2000_REL`, `HEADLINE_REL`, `SEEDS`,
  `MODEL_ORDER` (the 9 matched2000 models in stable order), `MODEL_LABELS`,
  `MODEL_COLORS`, `HEADLINE_COLOR` ("#000000"), `HEADLINE_LABEL` ("IQP:SEL 55p
  FROZEN headline (ckpt epoch 1969)"), `MATCHED2000_DUALSCALE_REL`,
  `DUALSCALE_MODEL_ORDER`, `HEADLINE_KIND` ("frozen_checkpoint_headline"),
  `HEADLINE_SOURCE` ("frozen_checkpoint_epoch_1969"), `_agg_lookup`, `_fmt`.
- main() wiring point: AFTER `render_matched2000_dualscale_comparison_table(...)`
  and BEFORE `render_existing_introspection(figures_dir)` (lines 1117-1128).
  Each new render_* function appends its written paths to the `written` list.

results/tstr.json schema (Task 2 source):
- Top-level keys: schema, model_kinds (6), pipelines ["A","B"], seeds (3 init
  seeds 40-42), data_hash, rows[] (long-form), tstr (dict keyed "<model>|<pipeline>").
- model_kinds: ["quantum","wgan_mlp","wgan_cnn","wgan_lstm","vae","ar"] (NO V1/V2/V3
  — tstr.json is the EVAL-01 paper artifact and uses model_kind="quantum" for the
  55-param IQP:SEL only; do NOT fabricate V1/V2/V3 entries in this figure).
- tstr["<model>|<pipeline>"] contains: r2_mean, r2_std, mae_mean, mae_std,
  rmse_mean, rmse_std, mse_mean, mse_std, per_init_seed{...}.
- NEGATIVE R² IS PRESENT: tstr["quantum|A"]["r2_mean"] = -4.5723… (Pipeline A
  for several models has R²<0; honest plotting required, no clamp).
- Also contains `tstr["real_only_baseline"]` — render as a separate "real-only"
  bar group (no pipeline) for context if natural, else skip; Claude's discretion.

results/noise_model_sensitivity.json schema (Task 6 source):
- Top-level: schema, model_kinds ["quantum"], pipelines ["A","B"], seeds (3
  values [42,43,44]), conditions (8: depol_0.0..depol_0.X + ampdamp_0.0..),
  noise_models (dict: depolarizing → 4 levels [0.0, p1, p2, p3]; amplitude_damping
  → 4 levels), channel_insertion ("per-layer (after each entangling block)"),
  zero_anchor_note (depol_0.0 == ampdamp_0.0 same baseline, kept as two rows
  for per-curve zero anchor), generated_at, rows[].
- rows[]: 720 entries with keys {model_kind, pipeline, seed, metric_name, scale,
  value, condition, noise_model, noise_level}. For the figure, filter on
  `metric_name == "emd"` and `scale == "OD"`, then groupby (noise_model,
  noise_level, pipeline) and aggregate mean over seeds.

results/shot_noise_sensitivity.json schema (Task 7 source):
- Top-level: schema, model_kinds ["quantum"], pipelines ["A","B"], seeds (3
  values), conditions ["analytic","shots_8192","shots_1024",...], shot_levels
  (dict: analytic → None, shots_<N> → <N>), generated_at, rows[].
- rows[]: 270 entries with keys {..., condition, shots}. analytic row has
  shots=None — render this as the horizontal reference line (the analytic-
  statevector baseline shots=∞ asymptote).

results/matched2000_dualscale.json schema (Tasks 3, 4 source — already
the load-bearing artifact for 14-08):
- aggregates[]: {model_kind, scale, metric_name, mean, std, n_seeds, source}.
- The 9 matched2000 models carry n_seeds=5; the frozen headline carries
  model_kind="frozen_checkpoint_headline", n_seeds=1, source="frozen_checkpoint
  _epoch_1969" (D-14-10 distinct row-set).
- Use `_agg_lookup(aggs, model_kind, scale, metric_name)` (already defined,
  lines 764-773) — do NOT re-implement.

results/model_info.json schema (Task 4 source):
- models[]: per-model record with `parameter_count`, `family`
  ("adversarial-quantum"|"adversarial-classical"|"non-adversarial"),
  `model`, `kind`, `source`, etc. The 9 matched2000 entrants + 2 quantum
  variants (iqp_sel_55_headline + iqp_sel_55_repro) are all present.
- For the Pareto facet, use models[*].parameter_count keyed by the lookup
  `models[*].model`. iqp_sel_55_headline carries parameter_count=55 + source
  =frozen_checkpoint_epoch_1969 (the frozen-headline diamond marker).

results/matched2000/runs/<model>/<seed>/metrics.json schema
(Tasks 1, 5 source — the per-run training trajectories):
- Adversarial models (iqp_sel_55_repro, V1, V2, V3, wgan_mlp, wgan_cnn,
  wgan_lstm): keys critic_loss_avg (length 2000), generator_loss_avg (2000),
  emd_avg (201 — eval every 10 epochs over 2000), acf_avg (201), vol_avg,
  lev_avg, kurt_avg (201 each).
- VAE: keys elbo, recon, kld, sample_std, real_window_std (no emd_avg) —
  handle as "no convergence trajectory" in Task 1; skip the model on the
  training-convergence figure with a small caption note.
- AR(p): keys sigma2, phi, p, fit_series_len, sample_mean, sample_std — no
  epoch loop; same treatment as VAE (skip, note in caption).
- IMPORTANT: training_convergence figure (Task 1) operates ONLY over the
  adversarial set (7 models with emd_avg); seed_variance facets (Task 5)
  likewise — the 3×3 grid covers the 7 adversarial models + 2 empty/note
  panels (or a 3×3 of the 7 + 2 short caption panels noting "VAE — no
  emd_avg trajectory" / "AR — closed-form, no training curve"). Claude's
  discretion on the exact panel composition.

results/headline_canonical.json (Tasks 1, 3, 4 source):
- Keys include source ("frozen_checkpoint_epoch_1969"), checkpoint_epoch
  (1969), checkpoint_emd (load-bearing scalar), rows[] (with model_kind=
  quantum, pipeline=B, metric_name in {emd, moment_*, acf_lag*_*, dtw_*}).
- For the training-convergence figure (Task 1), plot the headline as a
  SINGLE point at epoch 1969 with checkpoint_emd as the y-value — distinct
  HEADLINE_COLOR diamond + dashed reference line.

D-14-10 conflation guard (applies to Tasks 1, 3, 4):
- Whenever a figure could show both the frozen headline and iqp_sel_55_repro,
  the headline MUST be a visually distinct series (separate color, separate
  marker shape, separate label). HEADLINE_COLOR ("#000000") + HEADLINE_LABEL
  ("IQP:SEL 55p FROZEN headline (ckpt epoch 1969)") are already in scope —
  reuse them. Diamond marker shape (`marker="D"`) is the established
  convention from `render_matched2000_dualscale_sidebyside` (line 850).

verify_number_provenance.py auto-coverage:
- The gate's `_json_blobs()` rglobs `results/*.json` (line 99). The
  7 new companion JSONs land under `figures/` and are
  therefore AUTOMATICALLY in the gate's resolution corpus the moment they
  are written. NO verifier edit; the figures' caption strings and companion
  JSON's caption_note are gated by this corpus.

Render-only enforcement greps (every task's verify block):
- `grep -q 'matplotlib.use("Agg")' run_figure_suite.py` (still present)
- `! grep -qE '\.fit\(|def train_|model\.sample\(|best_checkpoint\.pt' run_figure_suite.py`
- `[ -z "$(git diff --stat core/)" ]` (D-14-22 byte-freeze)
- `[ -z "$(git diff --stat verify_number_provenance.py)" ]` (D-14-16)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: render_training_convergence_all_models — 9-model EMD-vs-epoch + headline marker</name>
  <files>run_figure_suite.py, figures/training_convergence_all_models.png, figures/training_convergence_all_models.pdf, figures/training_convergence_all_models.json</files>
  <read_first>
    - run_figure_suite.py (the host module — lines 45-49 headless agg, 153-193 _require/_load_json/_save, 100-141 MODEL_ORDER/LABELS/COLORS/HEADLINE_*; 1044-1135 main() to know where to wire the call)
    - results/matched2000/runs/V1/42/metrics.json (representative — confirm emd_avg is length 201, eval-every-10-epochs over 2000)
    - results/matched2000/runs/vae/42/metrics.json (VAE — confirm no emd_avg; skip in this figure with caption note)
    - results/matched2000/runs/ar/42/metrics.json (AR — confirm no emd_avg; skip with caption note)
    - results/headline_canonical.json (checkpoint_epoch=1969, checkpoint_emd scalar — single-point marker)
  </read_first>
  <action>
    Add `def render_training_convergence_all_models(repo: Path, figures_dir:
    Path) -> list[Path]` to `run_figure_suite.py`. Iterate over the
    7 adversarial models in `MODEL_ORDER` that carry `emd_avg` (iqp_sel_55_repro,
    V1, V2, V3, wgan_mlp, wgan_cnn, wgan_lstm). For each (model, seed) in
    MODEL_ORDER × SEEDS, `_require` + `_load_json` the
    `matched2000/runs/<model>/<seed>/metrics.json` and pull `emd_avg` (length
    201). Stack per-seed arrays into a (5, 201) ndarray per model, compute
    per-epoch mean and std via numpy (NOT a metric recomputation — these are
    aggregations of already-evaluated EMD values; this is the only kind of
    numeric aggregation the plan allows). Plot all 7 model mean curves on one
    log-y axis vs the epoch index (multiplied by 10 to recover absolute epoch
    number — `epoch = 10 * eval_step`, since emd_avg is "eval every 10 epochs
    over 2000"; assert length 201 → 0..2000 inclusive). Shade ±1 std band per
    model in matching MODEL_COLORS at alpha=0.18. Use MODEL_LABELS for the
    legend. Overlay the FROZEN-CHECKPOINT HEADLINE as a single distinct point:
    load `headline_canonical.json`, plot a diamond (`marker="D"`, color
    HEADLINE_COLOR, edgecolor="white", s=100, zorder=10) at x=1969, y=
    headline["checkpoint_emd"], with label HEADLINE_LABEL. Also draw a thin
    horizontal dashed line at the headline EMD (color HEADLINE_COLOR,
    linestyle="--", linewidth=1.2, alpha=0.5) so the convergence-vs-headline
    delta is visually obvious. VAE and AR have no emd_avg — do NOT fabricate
    a trajectory for them; add a small text annotation in the lower-right of
    the axes ("VAE / AR: no emd_avg eval trajectory (ELBO / closed-form)"
    — D-14 discretion on exact wording). If the JSON for any of the 35
    required (7 models × 5 seeds) per-run files is missing, loud-fail via
    `_require` (the render-only message). Save via `_save` with stem
    "training_convergence_all_models" and a companion dict:
    {
      "figure": "training_convergence_all_models",
      "render_only": True,
      "source_artifacts": ["results/matched2000/runs/<7 models>/<5 seeds>/metrics.json (×35)",
                           "results/headline_canonical.json"],
      "models_plotted": [<7 adversarial>],
      "models_skipped_no_emd_avg": ["vae", "ar"],
      "epochs_per_eval_step": 10,
      "n_eval_steps": 201,
      "epoch_axis_range": [0, 2000],
      "frozen_headline": {"checkpoint_epoch": 1969,
                          "checkpoint_emd": <from headline_canonical>,
                          "source": "frozen_checkpoint_epoch_1969",
                          "marker_style": "diamond, HEADLINE_COLOR",
                          "conflation_guard": "D-14-10: frozen headline is a "
                                              "DISTINCT marker, never merged "
                                              "into iqp_sel_55_repro mean curve"},
      "per_model_mean_emd_avg": {model: [...201 floats...]},
      "per_model_std_emd_avg":  {model: [...201 floats...]},
      "caption_note": "EMD-vs-epoch on log-y, per matched-2000ep budget; the "
                      "frozen checkpoint epoch 1969 is shown as a distinct "
                      "diamond + horizontal reference (D-14-10)."
    }
    Wire it into `main()` AFTER `render_matched2000_dualscale_comparison_table`
    and BEFORE `render_existing_introspection`. Commit message:
    `feat(14-10): training_convergence_all_models`.

    No `revision.core.eval` calls inside this function — only numpy mean/std
    over already-evaluated emd_avg arrays. No model `.fit(`, no sampling, no
    checkpoint reload. Faceting OD vs log_return is OPTIONAL (Claude's
    discretion): if both `emd_avg` (OD) AND a log_return equivalent exist in
    the metrics.json, render side-by-side facets and note it in caption_note;
    if only OD exists (the current state per the V1/42 schema confirmation —
    only `emd_avg` is present, no log_return trajectory), single-panel OD with
    the caption_note stating "log_return per-epoch trajectories were not
    captured during training; only OD eval trajectories exist". The schema
    confirmation shows only `emd_avg` — proceed single-panel OD.
  </action>
  <verify>
    <automated>./qgan_env/bin/python run_figure_suite.py &amp;&amp; ./qgan_env/bin/python -c "import pathlib,json,sys; d=pathlib.Path('figures'); s='training_convergence_all_models'; ok=(d/f'{s}.png').exists() and (d/f'{s}.pdf').exists() and (d/f'{s}.json').exists(); j=json.load(open(d/f'{s}.json')); ok = ok and j.get('render_only') is True and 'frozen_headline' in j and j['frozen_headline'].get('checkpoint_epoch')==1969 and 'iqp_sel_55_repro' in j['models_plotted'] and 'V1' in j['models_plotted']; sys.exit(0 if ok else 1)" &amp;&amp; grep -q 'render_training_convergence_all_models' run_figure_suite.py &amp;&amp; grep -q 'matplotlib.use("Agg")' run_figure_suite.py &amp;&amp; ! grep -qE '\.fit\(|def train_|model\.sample\(|best_checkpoint\.pt' run_figure_suite.py &amp;&amp; [ -z "$(git diff --stat core/)" ] &amp;&amp; [ -z "$(git diff --stat verify_number_provenance.py)" ]</automated>
  </verify>
  <acceptance_criteria>
    - `training_convergence_all_models.{png,pdf,json}` triple exists under `figures/`
    - Companion JSON has `render_only: true`, `frozen_headline.checkpoint_epoch == 1969`, `models_plotted` includes the 7 adversarial models, `models_skipped_no_emd_avg == ["vae","ar"]`
    - The 35 per-run `metrics.json` files are loaded via `_require`/`_load_json` (loud-fail on any missing)
    - The frozen headline appears as a distinct diamond marker (companion JSON `marker_style` records this) + a horizontal dashed reference line — never as a same-style series as iqp_sel_55_repro mean curve (D-14-10)
    - No metric recomputation: function body uses only `numpy.mean` / `numpy.std` over pre-evaluated emd_avg arrays; no `compute_emd`/`compute_moments`/`compute_acf`/`compute_dtw` calls in this function
    - `grep 'matplotlib.use("Agg")'` still present (host module headless agg preserved); no `.fit(`/`def train_`/`model.sample(`/`best_checkpoint.pt` added
    - `git diff --stat core/` empty (D-14-22)
    - `git diff --stat verify_number_provenance.py` empty (D-14-16)
    - Atomic commit `feat(14-10): training_convergence_all_models` lands
  </acceptance_criteria>
  <done>9-model (7 trajectories + 2 caption-skipped) EMD-vs-epoch convergence figure rendered with mean±std seed band and a visually distinct frozen-headline diamond/reference at epoch 1969, render-only, no recompute, atomic commit landed.</done>
</task>

<task type="auto">
  <name>Task 2: render_tstr_crossmodel — TSTR R²/MAE/RMSE grouped bars, negative R² honest</name>
  <files>run_figure_suite.py, figures/tstr_crossmodel.png, figures/tstr_crossmodel.pdf, figures/tstr_crossmodel.json</files>
  <read_first>
    - run_figure_suite.py (host module — `_require`/`_load_json`/`_save` building blocks; MODEL_COLORS for the 6 model_kinds present in tstr.json)
    - results/tstr.json (the SOLE source — confirm tstr["<model>|<pipeline>"] schema: r2_mean/r2_std/mae_mean/mae_std/rmse_mean/rmse_std; note model_kinds list = ["quantum","wgan_mlp","wgan_cnn","wgan_lstm","vae","ar"] — NO V1/V2/V3 here; honor that, do NOT fabricate)
    - verify_number_provenance.py (the gate — confirm tstr.json is auto-rglob'd; no verifier edit needed)
  </read_first>
  <action>
    Add `def render_tstr_crossmodel(repo: Path, figures_dir: Path) -> list[Path]`
    to `run_figure_suite.py`. `_load_json` `results/tstr.json`
    (loud-fail if absent). Build a 1×3 figure: panel A = R², panel B = MAE,
    panel C = RMSE. For each panel, render two side-by-side grouped bar
    clusters (Pipeline A on the left, Pipeline B on the right; mirrors the
    Pipeline-A/B duality the TSTR audit framed in 14-04's cross-model coverage).
    The 6 models from tstr["model_kinds"] (in tstr.json's stated order:
    ["quantum","wgan_mlp","wgan_cnn","wgan_lstm","vae","ar"]) are the bar
    positions inside each cluster; use MODEL_COLORS keyed by model_kind (note
    "quantum" key in tstr.json maps to the IQP:SEL 55-param case — color it
    MODEL_COLORS["iqp_sel_55_repro"], i.e. "#0072B2"). Error bars = the
    matching `*_std` from the tstr.json aggregate block (mean over 3 init
    seeds — tstr.json's `init_seeds` are [40,41,42], distinct from the
    matched2000 sample seeds 42-46 which is fine; surface that in caption_note).

    NEGATIVE R² IS PLOTTED HONESTLY: do NOT clamp at 0, do NOT abs(), do NOT
    log-rescale, do NOT split the y-axis. Let the R² panel's y-axis extend
    negative as far as the data goes (the tstr["quantum|A"]["r2_mean"] is
    -4.5723…; the wgan_lstm|A mean is -0.75). Set yticks generously so negative
    values are clearly visible. Annotate the negative-R² panel with a small
    inline note "(R² < 0 ⇒ worse than predicting the mean)" or place the
    equivalent in the companion JSON's `caption_note` field, MUST INCLUDE the
    explicit reviewer-facing note: "R² < 0 means the model is worse than
    predicting the mean — this is informative, not a render bug. The exact
    set of models with Pipeline A negative R² MUST be listed dynamically by
    reading tstr.json's per_model_pipeline block and selecting all
    (model, A) entries with r2_mean < 0 — DO NOT hard-code model names in the
    caption text, since the data ground truth (currently: quantum, wgan_lstm,
    wgan_mlp) is the source of record and a hand-typed list would drift.
    Then continue: '... because the soft-sensor trained on synthetic windows
    fails to generalize back to real windows when the synthetic distribution
    is far from real; Pipeline B (synth→real, the bioprocess-relevant
    direction) is positive for all models.'" Companion JSON includes the full per-(model, pipeline) record:
    {
      "figure": "tstr_crossmodel",
      "render_only": True,
      "source_artifact": "results/tstr.json",
      "models": [6 model_kinds from tstr["model_kinds"]],
      "pipelines": ["A", "B"],
      "init_seeds": [40, 41, 42],
      "per_model_pipeline": { "<model>|<pipeline>": {
          "r2_mean": …, "r2_std": …,
          "mae_mean": …, "mae_std": …,
          "rmse_mean": …, "rmse_std": …,
          "n_train_synth": …, "n_eval_real": … } for every key in tstr },
      "negative_r2_observed": [<list of "<model>|<pipeline>" with r2_mean<0>],
      "caption_note": "<the explicit reviewer-facing R²<0 explanation above>",
      "soft_sensor": "TSTRLiteLSTM (1-layer LSTM, hidden=32) — verbatim from "
                     "tstr.json metadata"
    }
    Optionally render the `real_only_baseline` block as a separate context bar
    (no pipeline) if natural; otherwise skip and note in caption_note. Save
    via `_save` with stem "tstr_crossmodel". Wire into `main()` AFTER the
    Task-1 call. Commit message: `feat(14-10): tstr_crossmodel`.

    No metric recomputation: every plotted value is `tstr[k][<field>]` read
    verbatim from the audited JSON.
  </action>
  <verify>
    <automated>./qgan_env/bin/python run_figure_suite.py &amp;&amp; ./qgan_env/bin/python -c "import pathlib,json,sys; d=pathlib.Path('figures'); s='tstr_crossmodel'; ok=(d/f'{s}.png').exists() and (d/f'{s}.pdf').exists() and (d/f'{s}.json').exists(); j=json.load(open(d/f'{s}.json')); src=j.get('source_artifact',''); ok = ok and j.get('render_only') is True and 'tstr.json' in src and 'caption_note' in j and 'R²' in j['caption_note'] and 'negative_r2_observed' in j and len(j['negative_r2_observed'])>0 and 'quantum|A' in j['negative_r2_observed']; sys.exit(0 if ok else 1)" &amp;&amp; grep -q 'render_tstr_crossmodel' run_figure_suite.py &amp;&amp; ! grep -qE '\.fit\(|def train_|model\.sample\(|best_checkpoint\.pt' run_figure_suite.py &amp;&amp; [ -z "$(git diff --stat core/)" ] &amp;&amp; [ -z "$(git diff --stat verify_number_provenance.py)" ]</automated>
  </verify>
  <acceptance_criteria>
    - `tstr_crossmodel.{png,pdf,json}` triple exists under `figures/`
    - Companion JSON has `render_only: true`, `source_artifact == "results/tstr.json"`, `caption_note` contains the explicit "R² < 0 means worse than predicting the mean" reviewer-facing wording, `negative_r2_observed` contains at minimum "quantum|A"
    - No clamping/abs/rescale of R² (the figure's y-axis extends negative; companion JSON's per_model_pipeline carries the raw negative means verbatim — assert any r2_mean < 0 appears verbatim in the JSON)
    - Pipeline A AND Pipeline B both plotted, 6 model_kinds present (no fabricated V1/V2/V3 entries)
    - tstr.json is now consumed (no longer an orphan paper-facing JSON)
    - `git diff --stat core/` empty; `git diff --stat verify_number_provenance.py` empty
    - Atomic commit `feat(14-10): tstr_crossmodel` lands
  </acceptance_criteria>
  <done>TSTR cross-model figure renders R²/MAE/RMSE × Pipeline A/B for 6 model_kinds with negative R² plotted honestly and the reviewer-facing R²<0 note in companion JSON; tstr.json is now consumed.</done>
</task>

<task type="auto">
  <name>Task 3: render_failure_modes_summary — 3-row × 9-column diagnostic grid</name>
  <files>run_figure_suite.py, figures/failure_modes_summary.png, figures/failure_modes_summary.pdf, figures/failure_modes_summary.json</files>
  <read_first>
    - run_figure_suite.py (host module — `_agg_lookup` lines 764-773, DUALSCALE_MODEL_ORDER, HEADLINE_KIND, MODEL_COLORS, MODEL_LABELS)
    - results/matched2000_dualscale.json (the per-(model,scale,metric) aggregates — confirm both OD and log_return EMD aggregates exist for all 9 models)
    - figures/dist_*.json (per-model distribution companions from 14-04 — confirm 9 exist: dist_iqp_sel_55_repro, dist_V1..V3, dist_wgan_*, dist_vae, dist_ar; carry `real_mean`/`fake_mean` and emd at minimum)
    - figures/acf_*.json (per-model ACF companions from 14-04 — confirm 9 exist; carry `acf_real_OD`/`acf_fake_OD` arrays; lag-1 is index 1)
  </read_first>
  <action>
    Add `def render_failure_modes_summary(repo: Path, figures_dir: Path) ->
    list[Path]` to `run_figure_suite.py`. Build a 3-row × 9-column
    diagnostic grid (matplotlib gridspec) where columns are the 9 matched2000
    models ordered by ASCENDING OD EMD (rank the models via
    `_agg_lookup(aggs, m, "OD", "emd")["mean"]` for each m in
    DUALSCALE_MODEL_ORDER; sort ascending — best-performing on the left). Rows:

    Row A (distribution overlay): for each model column, draw a thin
    distribution overlay sourced from the corresponding
    `figures/dist_<model>.json` (companion JSON written by
    14-04's `render_distribution_comparison` — schema confirmed: `n_real`,
    `n_fake`, `real_mean`, `fake_mean`, `emd`). The figure cell shows two
    overlaid histograms (real grey, generated MODEL_COLORS[model]); if the
    companion JSON does not carry the underlying histogram bins/counts (it
    carries summary stats), Claude's discretion: either (a) re-derive the
    histogram from `reconstruct_od(repo, model, PRIMARY_SEED)` + the existing
    `_real_references` helpers (this is rendering the same data the parent
    PNG already plotted — not a metric recomputation, just numpy histogram
    over the already-reconstructed OD windows), or (b) embed the parent
    `dist_<model>.png` thumbnail via `ax.imshow(plt.imread(...))` as a
    composite. Pick whichever is cleaner; option (a) keeps everything as
    vectors in the PDF and is preferred. NO new metric computation; numpy
    histogram is not a metric.

    Row B (ACF lag-1 bar): for each model column, draw a 2-bar mini panel
    (real vs generated) showing the lag-1 autocorrelation value pulled
    DIRECTLY from `figures/acf_<model>.json` —
    `acf_real_OD[1]` and `acf_fake_OD[1]`. No recompute; the companion JSON
    already carries the full ACF array.

    Row C (log_return EMD bar): for each model column, draw a 1-bar panel
    of `_agg_lookup(aggs, model, "log_return", "emd")["mean"]` (with the
    matching std as an error bar). Highlight outliers (models whose
    log_return EMD is > some natural threshold like
    median+1.5×IQR of the 9 values) with a red bar edge.

    For each cell where the model fails (defined as: log_return EMD outlier
    in row C, OR |fake_mean - real_mean| > some natural threshold like
    0.1 × |real_mean| in row A, OR |acf_fake_OD[1] - acf_real_OD[1]| > 0.5 in
    row B), add a small red text annotation naming the failure mode ("mode
    collapse" / "wrong mean" / "ACF lag-1 mismatch" / "log_return blowup" —
    Claude's discretion on the short labels). Annotations stay short (< 18
    chars) so the grid remains legible.

    The frozen headline: row C log_return EMD has a row-level annotation
    indicating where the frozen headline value sits (via a horizontal dashed
    reference line at `_agg_lookup(aggs, HEADLINE_KIND, "log_return", "emd")
    ["mean"]`); ditto for row A's mean-shift via the headline's OD
    `moment_mean` aggregate. The headline is NEVER merged into the
    iqp_sel_55_repro column — it sits as a reference line spanning the row
    (D-14-10).

    Save via `_save` with stem "failure_modes_summary" and a companion:
    {
      "figure": "failure_modes_summary",
      "render_only": True,
      "source_artifacts": [
        "results/matched2000_dualscale.json",
        "figures/dist_<model>.json (×9)",
        "figures/acf_<model>.json (×9)"
      ],
      "models_ordered_by_OD_emd_ascending": [<the sorted column order>],
      "rows": ["distribution_overlay", "acf_lag1", "log_return_emd"],
      "per_cell": { "<row>|<model>": { "value": …,
                                       "is_failure": bool,
                                       "failure_label": "…" or null,
                                       "source": "<artifact path>" } },
      "frozen_headline_references": {
        "OD_moment_mean": …,
        "log_return_emd": …,
        "conflation_guard": "D-14-10: headline shown as a row-spanning "
                            "dashed reference line, never merged into the "
                            "iqp_sel_55_repro column"
      },
      "caption_note": "Failure-mode triage at a glance. Columns are the 9 "
                      "matched-2000ep models in ascending OD EMD; rows are "
                      "(a) OD distribution overlay, (b) ACF lag-1 vs real, "
                      "(c) log_return EMD with red-edged outliers."
    }
    Wire into `main()` AFTER the Task-2 call. Commit message:
    `feat(14-10): failure_modes_summary`.

    Render-only: no model `.fit(`, no `model.sample(`, no metric recompute
    (numpy histogram for row A is rendering, not metric math; the lag-1 ACF
    value is read directly from the companion JSON).
  </action>
  <verify>
    <automated>./qgan_env/bin/python run_figure_suite.py &amp;&amp; ./qgan_env/bin/python -c "import pathlib,json,sys; d=pathlib.Path('figures'); s='failure_modes_summary'; ok=(d/f'{s}.png').exists() and (d/f'{s}.pdf').exists() and (d/f'{s}.json').exists(); j=json.load(open(d/f'{s}.json')); ok = ok and j.get('render_only') is True and len(j.get('models_ordered_by_OD_emd_ascending',[]))==9 and 'frozen_headline_references' in j and 'D-14-10' in j['frozen_headline_references'].get('conflation_guard',''); sys.exit(0 if ok else 1)" &amp;&amp; grep -q 'render_failure_modes_summary' run_figure_suite.py &amp;&amp; ! grep -qE '\.fit\(|def train_|model\.sample\(|best_checkpoint\.pt' run_figure_suite.py &amp;&amp; [ -z "$(git diff --stat core/)" ] &amp;&amp; [ -z "$(git diff --stat verify_number_provenance.py)" ]</automated>
  </verify>
  <acceptance_criteria>
    - `failure_modes_summary.{png,pdf,json}` triple exists under `figures/`
    - Companion JSON `models_ordered_by_OD_emd_ascending` has 9 entries, sorted by ascending OD EMD pulled from `_agg_lookup`
    - 3 rows (distribution_overlay, acf_lag1, log_return_emd) covered; `per_cell` carries (value, is_failure, failure_label) for every (row, model)
    - Frozen headline rendered as a row-spanning dashed reference line, NOT a same-style column (D-14-10) — `frozen_headline_references.conflation_guard` records this
    - dist_*.json and acf_*.json are read via `_load_json` (loud-fail on missing); matched2000_dualscale.json is read via `_load_json` (already in scope)
    - `git diff --stat core/` empty; `git diff --stat verify_number_provenance.py` empty
    - Atomic commit `feat(14-10): failure_modes_summary` lands
  </acceptance_criteria>
  <done>3-row × 9-column failure-mode triage grid renders the audited dist_*/acf_*/matched2000_dualscale aggregates with ascending-OD-EMD ordering, red failure-mode annotations, and the frozen headline as a row-spanning reference line distinct from iqp_sel_55_repro.</done>
</task>

<task type="auto">
  <name>Task 4: render_param_efficiency_pareto — log10(params) × EMD scatter, family-colored, OD vs log_return facets, headline diamond</name>
  <files>run_figure_suite.py, figures/param_efficiency_pareto.png, figures/param_efficiency_pareto.pdf, figures/param_efficiency_pareto.json</files>
  <read_first>
    - run_figure_suite.py (host module — `_agg_lookup`, MODEL_COLORS, HEADLINE_COLOR, HEADLINE_LABEL, HEADLINE_KIND)
    - results/model_info.json (per-model parameter_count + family; iqp_sel_55_headline parameter_count=55 source=frozen_checkpoint_epoch_1969; the 9 matched2000 entrants present)
    - results/matched2000_dualscale.json (aggregates[] — EMD mean+std × OD and log_return × 9 matched models + the frozen headline)
  </read_first>
  <action>
    Add `def render_param_efficiency_pareto(repo: Path, figures_dir: Path) ->
    list[Path]` to `run_figure_suite.py`. `_load_json` model_info.json
    and matched2000_dualscale.json (the latter is already loaded by 14-08's
    sidebyside renderer — re-`_load_json` is fine; the file is small).
    Index model_info.json's `models[]` list by `model` field to get
    `parameter_count` per model. For the 9 matched2000 models, build the
    scatter data: for each model m in DUALSCALE_MODEL_ORDER:
    - x = log10(parameter_count) — pulled from model_info[m].parameter_count
      (iqp_sel_55_repro=55, V1=75, V2=135, V3=75, wgan_mlp=74, wgan_cnn=73,
      wgan_lstm=78, vae=562, ar=3; these are the audited values)
    - y_OD     = `_agg_lookup(aggs, m, "OD",         "emd")["mean"]`
    - y_logret = `_agg_lookup(aggs, m, "log_return", "emd")["mean"]`
    - yerr_OD     = `_agg_lookup(...)["std"]` (n_seeds=5)
    - yerr_logret = `_agg_lookup(...)["std"]` (n_seeds=5)

    Build a 1×2 figure (left panel: OD scale, right panel: log_return scale).
    Both panels share the x-axis (log10 params). Markers colored by family:
    - adversarial-quantum (iqp_sel_55_repro, V1, V2, V3): circle, color from
      MODEL_COLORS keyed by model name
    - adversarial-classical (wgan_mlp, wgan_cnn, wgan_lstm): square,
      MODEL_COLORS keyed by model name
    - non-adversarial (vae, ar): triangle, MODEL_COLORS keyed by model name
    (Family lookups via model_info[m].family.) Use error bars from `_agg_lookup
    (...).std`. Annotate each point with its MODEL_LABELS label (small text,
    slight offset).

    The FROZEN-CHECKPOINT HEADLINE is a DISTINCT diamond marker: x = log10(55)
    (from model_info["iqp_sel_55_headline"].parameter_count = 55), y =
    `_agg_lookup(aggs, HEADLINE_KIND, "OD", "emd")["mean"]` for the OD panel
    and similarly for log_return. Marker `marker="D"`, color HEADLINE_COLOR,
    s=120, edgecolor="white", linewidths=1.2, zorder=10, label HEADLINE_LABEL.
    This satisfies D-14-10: the headline is plotted with a different marker
    shape and color from iqp_sel_55_repro (which is a circle at the same x,
    different y).

    Optionally draw a thin Pareto frontier guideline (left-down stairstep
    connecting the lowest-EMD point at each x range) — Claude's discretion;
    if added, label it "Pareto front (lower-left = better param-efficiency)".

    Save via `_save` with stem "param_efficiency_pareto" and a companion:
    {
      "figure": "param_efficiency_pareto",
      "render_only": True,
      "source_artifacts": [
        "results/model_info.json",
        "results/matched2000_dualscale.json"
      ],
      "panels": ["OD", "log_return"],
      "x_axis": "log10(parameter_count)",
      "per_model": { m: {
          "parameter_count": int,
          "family": str,
          "log10_params": float,
          "OD_emd_mean": float, "OD_emd_std": float,
          "log_return_emd_mean": float, "log_return_emd_std": float,
          "marker": "<circle|square|triangle>",
          "color": MODEL_COLORS[m]
      } for m in DUALSCALE_MODEL_ORDER },
      "frozen_headline": {
          "parameter_count": 55,
          "log10_params": log10(55),
          "OD_emd_mean": …,
          "log_return_emd_mean": …,
          "marker_style": "diamond, HEADLINE_COLOR",
          "source": "frozen_checkpoint_epoch_1969",
          "conflation_guard": "D-14-10: headline is a diamond at the same x "
                              "as iqp_sel_55_repro (both 55p) but a distinct "
                              "color/marker/y — never merged into the repro "
                              "circle"
      },
      "caption_note": "Lower-left = better parameter efficiency. The frozen "
                      "headline (epoch 1969, 55p) is the diamond — distinct "
                      "from the 5-seed iqp_sel_55_repro circle at the same x."
    }
    Wire into `main()` AFTER the Task-3 call. Commit message:
    `feat(14-10): param_efficiency_pareto`.

    No metric recomputation — every numeric value is read from model_info.json
    or matched2000_dualscale.json directly.
  </action>
  <verify>
    <automated>./qgan_env/bin/python run_figure_suite.py &amp;&amp; ./qgan_env/bin/python -c "import pathlib,json,sys; d=pathlib.Path('figures'); s='param_efficiency_pareto'; ok=(d/f'{s}.png').exists() and (d/f'{s}.pdf').exists() and (d/f'{s}.json').exists(); j=json.load(open(d/f'{s}.json')); ok = ok and j.get('render_only') is True and 'frozen_headline' in j and j['frozen_headline'].get('parameter_count')==55 and 'D-14-10' in j['frozen_headline'].get('conflation_guard','') and set(j.get('panels',[]))=={'OD','log_return'} and len(j.get('per_model',{}))==9; sys.exit(0 if ok else 1)" &amp;&amp; grep -q 'render_param_efficiency_pareto' run_figure_suite.py &amp;&amp; ! grep -qE '\.fit\(|def train_|model\.sample\(|best_checkpoint\.pt' run_figure_suite.py &amp;&amp; [ -z "$(git diff --stat core/)" ] &amp;&amp; [ -z "$(git diff --stat verify_number_provenance.py)" ]</automated>
  </verify>
  <acceptance_criteria>
    - `param_efficiency_pareto.{png,pdf,json}` triple exists
    - Companion JSON's `panels == ["OD","log_return"]`, `per_model` has 9 entries (matched2000 models), `frozen_headline.parameter_count == 55`, `frozen_headline.conflation_guard` references D-14-10
    - Markers colored by family per model_info[*].family; iqp_sel_55_repro is a circle, the frozen headline at the same x is a diamond with a different y (D-14-10 visually distinct)
    - All 9 model parameter_counts read from model_info.json (55, 75, 135, 75, 74, 73, 78, 562, 3); no hand-typed
    - `git diff --stat core/` empty; `git diff --stat verify_number_provenance.py` empty
    - Atomic commit `feat(14-10): param_efficiency_pareto` lands
  </acceptance_criteria>
  <done>Parameter-efficiency Pareto scatter (OD + log_return facets, family-colored markers, frozen-headline diamond at log10(55)) rendered render-only from model_info.json + matched2000_dualscale.json with D-14-10 conflation guard preserved.</done>
</task>

<task type="auto">
  <name>Task 5: render_seed_variance_per_model — 3×3 grid of per-seed EMD trajectories per model</name>
  <files>run_figure_suite.py, figures/seed_variance_per_model.png, figures/seed_variance_per_model.pdf, figures/seed_variance_per_model.json</files>
  <read_first>
    - run_figure_suite.py (host module — MODEL_ORDER, MODEL_LABELS, MODEL_COLORS, SEEDS, MATCHED2000_REL)
    - results/matched2000/runs/V1/42/metrics.json (representative — emd_avg length 201; same for the 7 adversarial models)
    - results/matched2000/runs/vae/42/metrics.json (no emd_avg — handle as caption panel)
    - results/matched2000/runs/ar/42/metrics.json (no emd_avg — handle as caption panel)
  </read_first>
  <action>
    Add `def render_seed_variance_per_model(repo: Path, figures_dir: Path) ->
    list[Path]` to `run_figure_suite.py`. Build a 3×3 facet grid
    (sharex, sharey for fair visual comparison). Panel order MODEL_ORDER:
    [iqp_sel_55_repro, V1, V2, V3, wgan_mlp, wgan_cnn, wgan_lstm, vae, ar]
    laid out row-major (row 0: iqp_sel_55_repro, V1, V2; row 1: V3, wgan_mlp,
    wgan_cnn; row 2: wgan_lstm, vae, ar).

    For each adversarial panel (7 models that carry emd_avg): for each seed
    in SEEDS, `_load_json(_run_dir(repo, m, s)/"metrics.json", ...)` and pull
    `emd_avg` (length 201). Plot the 5 per-seed lines in light color
    (MODEL_COLORS[m] at alpha=0.35, linewidth=1.0). Overlay the per-epoch
    mean across the 5 seeds as a bold line (MODEL_COLORS[m] at alpha=1.0,
    linewidth=2.2). x-axis = epoch (eval_step × 10, 0..2000). y-axis log scale
    for visibility of tight regions. Panel title = MODEL_LABELS[m].

    For VAE and AR panels: render a short caption inside the panel ("VAE —
    no emd_avg eval trajectory (ELBO loop)" / "AR — closed-form, no
    training curve") on an otherwise blank axes (still set the axis bounds
    to match shared y for grid consistency). Do NOT fabricate a trajectory.

    Add a per-panel sub-caption (small text bottom-right): "5-seed spread:
    {tight|moderate|noisy}" derived from the across-seed std of the final
    eval step's emd value (tight: std < 5% of final mean; moderate: 5-15%;
    noisy: > 15%). The aggregate caption note explicitly contrasts the
    quantum cluster (typically tight) vs wgan_cnn (typically noisy) — this
    is the figure's reviewer-facing payoff.

    Save via `_save` with stem "seed_variance_per_model" and a companion:
    {
      "figure": "seed_variance_per_model",
      "render_only": True,
      "source_artifacts": ["results/matched2000/runs/<7 adversarial>/<5 seeds>/metrics.json (×35)"],
      "panel_layout_row_major": [<MODEL_ORDER, the 9 panels>],
      "adversarial_panels": [<the 7 with emd_avg>],
      "no_trajectory_panels": ["vae", "ar"],
      "per_model_final_emd_mean":   { m: float for m in adversarial },
      "per_model_final_emd_std":    { m: float for m in adversarial },
      "per_model_spread_label":     { m: "tight"|"moderate"|"noisy" },
      "caption_note": "Per-model 5-seed EMD-vs-epoch trajectories (light) "
                      "with mean (bold). Quantum cluster (IQP:SEL 55p + "
                      "V1/V2/V3) shows tight seed agreement; wgan_cnn is "
                      "noisier across seeds; VAE/AR have no eval-epoch "
                      "trajectory (ELBO loop / closed-form fit)."
    }
    Wire into `main()` AFTER the Task-4 call. Commit message:
    `feat(14-10): seed_variance_per_model`.

    No metric recomputation; only numpy mean/std over already-evaluated
    emd_avg arrays.
  </action>
  <verify>
    <automated>./qgan_env/bin/python run_figure_suite.py &amp;&amp; ./qgan_env/bin/python -c "import pathlib,json,sys; d=pathlib.Path('figures'); s='seed_variance_per_model'; ok=(d/f'{s}.png').exists() and (d/f'{s}.pdf').exists() and (d/f'{s}.json').exists(); j=json.load(open(d/f'{s}.json')); ok = ok and j.get('render_only') is True and len(j.get('panel_layout_row_major',[]))==9 and set(j.get('no_trajectory_panels',[]))=={'vae','ar'} and len(j.get('adversarial_panels',[]))==7 and 'per_model_spread_label' in j; sys.exit(0 if ok else 1)" &amp;&amp; grep -q 'render_seed_variance_per_model' run_figure_suite.py &amp;&amp; ! grep -qE '\.fit\(|def train_|model\.sample\(|best_checkpoint\.pt' run_figure_suite.py &amp;&amp; [ -z "$(git diff --stat core/)" ] &amp;&amp; [ -z "$(git diff --stat verify_number_provenance.py)" ]</automated>
  </verify>
  <acceptance_criteria>
    - `seed_variance_per_model.{png,pdf,json}` triple exists
    - Companion JSON's `panel_layout_row_major` has 9 entries; `adversarial_panels` has 7; `no_trajectory_panels == ["vae","ar"]`
    - Per-model spread labels classified from per-seed final-step std (tight/moderate/noisy); caption_note contrasts quantum cluster vs wgan_cnn
    - 35 per-run metrics.json files loaded via `_require`/`_load_json` (loud-fail on any missing); no fabrication for VAE/AR
    - `git diff --stat core/` empty; `git diff --stat verify_number_provenance.py` empty
    - Atomic commit `feat(14-10): seed_variance_per_model` lands
  </acceptance_criteria>
  <done>3×3 grid of per-model 5-seed EMD trajectories with shared axes, mean overlay, spread labels, and explicit no-trajectory caption panels for VAE/AR rendered render-only.</done>
</task>

<task type="auto">
  <name>Task 6: render_noise_robustness_quantum — EMD vs noise level for depolarizing and amplitude-damping</name>
  <files>run_figure_suite.py, figures/noise_robustness_quantum.png, figures/noise_robustness_quantum.pdf, figures/noise_robustness_quantum.json</files>
  <read_first>
    - run_figure_suite.py (host module — `_load_json`, `_save`)
    - results/noise_model_sensitivity.json (the SOLE source — confirm schema: rows[] with keys condition/noise_model/noise_level + the long-form metric_name/scale/value + pipelines ["A","B"] + 3 seeds [42,43,44]; noise_models dict carries the levels for each channel; zero_anchor_note states depol_0.0 and ampdamp_0.0 are the same physical baseline)
  </read_first>
  <action>
    Add `def render_noise_robustness_quantum(repo: Path, figures_dir: Path) ->
    list[Path]` to `run_figure_suite.py`. `_load_json`
    `results/noise_model_sensitivity.json` (loud-fail if absent).

    Inspect the JSON's actual shape and render what's there. From the schema:
    - rows[] entries carry (model_kind, pipeline, seed, metric_name, scale,
      value, condition, noise_model, noise_level)
    - noise_models = {"depolarizing": [4 levels including 0.0], "amplitude_damping": [4 levels including 0.0]}
    - pipelines = ["A","B"], seeds = [42,43,44]
    - model_kinds = ["quantum"] (single model — this is a quantum-only SENS-02 figure)

    Filter rows to metric_name == "emd" AND scale == "OD". Group by
    (noise_model, noise_level, pipeline) and aggregate mean over the 3 seeds;
    record the per-seed std for error bars.

    Build a 1×2 figure (Panel A: depolarizing, Panel B: amplitude_damping)
    OR a single panel with two curves at distinct linestyles — Claude's
    discretion based on what reads cleanest. Each panel:
    - x-axis: noise_level (linear; the levels are small probabilities like
      0.0, 0.001, 0.01, 0.1 — log-x is reasonable but obscures the zero
      anchor; use linear x with zero on the left)
    - y-axis: EMD (linear)
    - Two curves per panel: Pipeline A (dashed) and Pipeline B (solid),
      both with point markers + error bars over the 3 seeds
    - The zero anchor (depol_0.0 / ampdamp_0.0) gets a distinct point per
      panel — the JSON's `zero_anchor_note` states they are the same
      physical baseline kept as two distinct rows; preserve that.
    - Caption inside each panel: noting whether degradation is monotonic
      (increasing noise → increasing EMD across the level set). Compute the
      monotonicity check post-aggregation per (channel, pipeline): for each
      curve, check all consecutive deltas have the same sign (positive).

    Save via `_save` with stem "noise_robustness_quantum" and a companion:
    {
      "figure": "noise_robustness_quantum",
      "render_only": True,
      "source_artifact": "results/noise_model_sensitivity.json",
      "noise_models": ["depolarizing", "amplitude_damping"],
      "noise_levels": {
        "depolarizing": [<4 floats from the JSON's noise_models.depolarizing>],
        "amplitude_damping": [<4 floats>]
      },
      "pipelines": ["A", "B"],
      "seeds": [42, 43, 44],
      "channel_insertion": "per-layer (after each entangling block)",
      "zero_anchor_note": "depol_0.0 == ampdamp_0.0 same physical baseline; "
                          "kept as two distinct rows for per-curve anchor",
      "per_curve_aggregates": { "<channel>|<pipeline>": [
          { "noise_level": float, "emd_mean": float, "emd_std": float, "n_seeds": 3 }
          for each level in that channel ] },
      "monotonicity": { "<channel>|<pipeline>": "monotonic_increase" | "non_monotonic" },
      "caption_note": "EMD vs noise level for depolarizing and amplitude-"
                      "damping channels inserted per-layer (after each "
                      "entangling block). Mean over 3 seeds (42-44); error "
                      "bars are seed std. Monotonicity per curve recorded "
                      "in companion JSON."
    }
    Wire into `main()` AFTER the Task-5 call. Commit message:
    `feat(14-10): noise_robustness_quantum`.

    No metric recomputation — every plotted value is the audited EMD already
    in rows[] of noise_model_sensitivity.json.
  </action>
  <verify>
    <automated>./qgan_env/bin/python run_figure_suite.py &amp;&amp; ./qgan_env/bin/python -c "import pathlib,json,sys; d=pathlib.Path('figures'); s='noise_robustness_quantum'; ok=(d/f'{s}.png').exists() and (d/f'{s}.pdf').exists() and (d/f'{s}.json').exists(); j=json.load(open(d/f'{s}.json')); ok = ok and j.get('render_only') is True and j.get('source_artifact')=='results/noise_model_sensitivity.json' and set(j.get('noise_models',[]))=={'depolarizing','amplitude_damping'} and 'per_curve_aggregates' in j and 'monotonicity' in j and 'zero_anchor_note' in j; sys.exit(0 if ok else 1)" &amp;&amp; grep -q 'render_noise_robustness_quantum' run_figure_suite.py &amp;&amp; ! grep -qE '\.fit\(|def train_|model\.sample\(|best_checkpoint\.pt' run_figure_suite.py &amp;&amp; [ -z "$(git diff --stat core/)" ] &amp;&amp; [ -z "$(git diff --stat verify_number_provenance.py)" ]</automated>
  </verify>
  <acceptance_criteria>
    - `noise_robustness_quantum.{png,pdf,json}` triple exists
    - Companion JSON's `source_artifact == "results/noise_model_sensitivity.json"`, `noise_models == ["depolarizing","amplitude_damping"]`, `per_curve_aggregates` covers both channels × both pipelines × all levels with n_seeds=3
    - `monotonicity` recorded per (channel, pipeline); `zero_anchor_note` carried verbatim from the source JSON's note
    - noise_model_sensitivity.json is now consumed (no longer an orphan)
    - `git diff --stat core/` empty; `git diff --stat verify_number_provenance.py` empty
    - Atomic commit `feat(14-10): noise_robustness_quantum` lands
  </acceptance_criteria>
  <done>Noise-robustness figure rendered for depolarizing and amplitude-damping channels (both pipelines, mean ± std over 3 seeds, monotonicity recorded); noise_model_sensitivity.json is now consumed.</done>
</task>

<task type="auto">
  <name>Task 7: render_shot_noise_robustness — EMD vs shot count with analytic-statevector reference</name>
  <files>run_figure_suite.py, figures/shot_noise_robustness.png, figures/shot_noise_robustness.pdf, figures/shot_noise_robustness.json</files>
  <read_first>
    - run_figure_suite.py (host module — `_load_json`, `_save`)
    - results/shot_noise_sensitivity.json (the SOLE source — confirm schema: rows[] with keys condition/shots + the long-form metric_name/scale/value; conditions like "analytic", "shots_8192", "shots_1024"; shot_levels dict maps each condition to its shot count, analytic→None)
  </read_first>
  <action>
    Add `def render_shot_noise_robustness(repo: Path, figures_dir: Path) ->
    list[Path]` to `run_figure_suite.py`. `_load_json`
    `results/shot_noise_sensitivity.json` (loud-fail if absent).

    Inspect the JSON's actual shape and render what's there. From the schema:
    - rows[] entries carry (model_kind, pipeline, seed, metric_name, scale,
      value, condition, shots)
    - shot_levels = {"analytic": None, "shots_8192": 8192, "shots_1024": 1024, ...}
    - pipelines = ["A","B"], seeds = [42,43,44]
    - model_kinds = ["quantum"] (single model — this is a quantum-only SENS-01 figure)

    Filter rows to metric_name == "emd" AND scale == "OD". Separate the
    "analytic" condition (shots=None) from the finite-shot conditions. Group
    finite-shot rows by (shots, pipeline) and aggregate mean+std over 3 seeds.
    The analytic baseline is plotted as a HORIZONTAL REFERENCE LINE (one per
    pipeline) — it is the shots=∞ asymptote.

    Build a single panel (or two panels by pipeline — Claude's discretion):
    - x-axis: shots (log scale; the finite-shot conditions are typically
      powers of 2 like 1024, 8192, ...)
    - y-axis: EMD (linear or log — Claude's discretion based on dynamic range)
    - Two curves: Pipeline A and Pipeline B, point markers + error bars
      over the 3 seeds, lines connecting consecutive shot levels in
      ascending order
    - Horizontal reference lines for the analytic baseline of each pipeline
      (distinct linestyle, labeled "Pipeline A analytic (shots=∞)" /
      "Pipeline B analytic (shots=∞)" — use HEADLINE_COLOR for the analytic
      lines to make them visually canonical as the reference, or distinct
      grey — Claude's discretion; the established render-only contract
      mandates the analytic baseline as horizontal reference)

    Save via `_save` with stem "shot_noise_robustness" and a companion:
    {
      "figure": "shot_noise_robustness",
      "render_only": True,
      "source_artifact": "results/shot_noise_sensitivity.json",
      "conditions": [<list of conditions from the JSON>],
      "shot_levels": { condition: shots_or_None from the JSON },
      "pipelines": ["A","B"],
      "seeds": [42, 43, 44],
      "per_curve_aggregates": { "<pipeline>": [
          { "shots": int, "emd_mean": float, "emd_std": float, "n_seeds": 3 }
          for each finite-shot condition ] },
      "analytic_baseline": { "<pipeline>": { "emd_mean": float,
                                             "emd_std": float,
                                             "n_seeds": 3 } },
      "x_scale": "log",
      "caption_note": "EMD vs shot count (log-x) for Pipeline A and B; the "
                      "analytic-statevector baseline (shots=∞) is shown as "
                      "a horizontal reference per pipeline. Mean over 3 "
                      "seeds (42-44); error bars are seed std."
    }
    Wire into `main()` AFTER the Task-6 call. Commit message:
    `feat(14-10): shot_noise_robustness`.

    No metric recomputation — every plotted value is the audited EMD already
    in rows[] of shot_noise_sensitivity.json.
  </action>
  <verify>
    <automated>./qgan_env/bin/python run_figure_suite.py &amp;&amp; ./qgan_env/bin/python -c "import pathlib,json,sys; d=pathlib.Path('figures'); s='shot_noise_robustness'; ok=(d/f'{s}.png').exists() and (d/f'{s}.pdf').exists() and (d/f'{s}.json').exists(); j=json.load(open(d/f'{s}.json')); ok = ok and j.get('render_only') is True and j.get('source_artifact')=='results/shot_noise_sensitivity.json' and 'analytic_baseline' in j and 'per_curve_aggregates' in j and j.get('x_scale')=='log'; sys.exit(0 if ok else 1)" &amp;&amp; grep -q 'render_shot_noise_robustness' run_figure_suite.py &amp;&amp; ! grep -qE '\.fit\(|def train_|model\.sample\(|best_checkpoint\.pt' run_figure_suite.py &amp;&amp; [ -z "$(git diff --stat core/)" ] &amp;&amp; [ -z "$(git diff --stat verify_number_provenance.py)" ] &amp;&amp; ./qgan_env/bin/python -c "import pathlib,sys; d=pathlib.Path('figures'); stems=['training_convergence_all_models','tstr_crossmodel','failure_modes_summary','param_efficiency_pareto','seed_variance_per_model','noise_robustness_quantum','shot_noise_robustness']; ok=all((d/f'{s}.png').exists() and (d/f'{s}.pdf').exists() and (d/f'{s}.json').exists() for s in stems); print('all_7_complete',ok); sys.exit(0 if ok else 1)"</automated>
  </verify>
  <acceptance_criteria>
    - `shot_noise_robustness.{png,pdf,json}` triple exists
    - Companion JSON's `source_artifact == "results/shot_noise_sensitivity.json"`, `analytic_baseline` records per-pipeline baseline mean+std, `per_curve_aggregates` covers both pipelines × all finite-shot conditions with n_seeds=3, `x_scale == "log"`
    - shot_noise_sensitivity.json is now consumed (no longer an orphan)
    - ALL 7 new triples are present (the final assertion in the verify block confirms this) — `training_convergence_all_models`, `tstr_crossmodel`, `failure_modes_summary`, `param_efficiency_pareto`, `seed_variance_per_model`, `noise_robustness_quantum`, `shot_noise_robustness`
    - `git diff --stat core/` empty; `git diff --stat verify_number_provenance.py` empty
    - Atomic commit `feat(14-10): shot_noise_robustness` lands
  </acceptance_criteria>
  <done>Shot-noise robustness figure rendered with EMD vs log(shots), analytic-statevector baseline as horizontal reference per pipeline; shot_noise_sensitivity.json is now consumed; all 7 plan-14-10 figure triples present.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| audited JSON → rendered figure | a render that silently drops a row / mis-aggregates an array would put an unverifiable number into a paper-facing figure |
| frozen-checkpoint headline ↔ matched-2000ep reproduction | conflating the two on any figure would misrepresent the load-bearing 55-param headline as a multi-seed reproduction (D-14-10) |
| tstr.json negative R² → figure | clamping or rescaling negative R² would mask a real signal (Pipeline A fails to generalize) — reviewers would not see it |
| revision.core byte-freeze (D-14-22) | any inadvertent edit to core/ during plan execution would invalidate Phases 8-13's frozen-default reproducibility |
| verify_number_provenance.py (D-14-16) | any edit to the gate would weaken provenance enforcement; the new figures must pass via auto-rglob without verifier changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-25 | Tampering | silent partial render on missing input JSON | mitigate | Every new render_* function uses `_require`/`_load_json` (loud-fail FileNotFoundError); verify greps ensure these patterns are present |
| T-14-26 | Tampering | frozen headline merged into iqp_sel_55_repro on a new figure | mitigate | Tasks 1, 3, 4 each render the headline as a DISTINCT marker (diamond + HEADLINE_COLOR + HEADLINE_LABEL + horizontal reference line where applicable); each companion JSON records `conflation_guard: "D-14-10..."`; verify asserts this field is present on the three at-risk figures |
| T-14-27 | Spoofing | hand-typed numbers in figure captions / companion JSON | mitigate | Every plotted value is read from an audited JSON via `_load_json` + dict access; no hand-typed numeric literals in the 7 new render functions; the new companion JSONs themselves are auto-rglob'd by `verify_number_provenance.py` (D-14-16, no verifier edit) |
| T-14-28 | Tampering | negative R² clamped or rescaled in tstr_crossmodel | mitigate | Task 2 verify asserts no clamping (raw negative r2_mean values appear verbatim in the companion JSON's `per_model_pipeline` block) AND the `caption_note` field contains the explicit reviewer-facing R²<0 explanation |
| T-14-29 | Tampering | `core/` edit slips in | mitigate | Every task verify ends with `[ -z "$(git diff --stat core/)" ]` (D-14-22 byte-freeze) |
| T-14-30 | Tampering | metric recomputation inside the 7 new render functions | mitigate | Every task verify grep asserts no `.fit(`/`def train_`/`model.sample(`/`best_checkpoint.pt` is added; the only numeric ops allowed are numpy mean/std over already-evaluated emd_avg arrays + numpy histogram for distribution rendering (rendering, not metric math) |
| T-14-31 | Tampering | `verify_number_provenance.py` modified to make a new figure pass | mitigate | Every task verify ends with `[ -z "$(git diff --stat verify_number_provenance.py)" ]`; new companion JSONs land under `figures/` and are AUTO-rglob'd by the existing gate |
| T-14-32 | Repudiation | figure ↔ data provenance for the 7 new figures | mitigate | Every companion JSON records `source_artifact(s)` and `render_only: true`; the new files are auto-discoverable by the existing number-provenance gate via its `results/*.json` rglob |
</threat_model>

<verification>
- 7 new figure triples present under `figures/`: training_convergence_all_models, tstr_crossmodel, failure_modes_summary, param_efficiency_pareto, seed_variance_per_model, noise_robustness_quantum, shot_noise_robustness — each as PNG + PDF + companion JSON
- Every companion JSON sets `render_only: true` and lists its `source_artifact`(s); the three at-risk figures (training_convergence, failure_modes_summary, param_efficiency_pareto) carry an explicit `conflation_guard` field referencing D-14-10
- tstr.json, noise_model_sensitivity.json, shot_noise_sensitivity.json are each consumed by exactly one new figure (those three previously-unconsumed paper-facing JSONs are no longer orphans)
- `run_figure_suite.py` adds 7 new `render_*` functions wired into `main()` after `render_matched2000_dualscale_comparison_table` and before `render_existing_introspection`
- `core/` byte-frozen (D-14-22): `git diff --stat core/` empty
- `verify_number_provenance.py` byte-frozen (D-14-16): `git diff --stat verify_number_provenance.py` empty
- 7 atomic commits land in order, one per figure, named `feat(14-10): <figure>`; the SUMMARY commit follows after all 7
</verification>

<success_criteria>
The manuscript's full story is rendered end-to-end: training convergence across all models, TSTR cross-model R²/MAE/RMSE (honest negative R²), failure-mode triage at a glance, parameter-efficiency Pareto, per-model seed variance, quantum noise robustness, and shot-noise robustness. Every figure is render-only over audited JSON, every companion JSON is auto-gated by `verify_number_provenance.py` without a verifier edit, the frozen-checkpoint headline is visually distinct from iqp_sel_55_repro on every figure where both could appear (D-14-10), negative R² is plotted honestly with a reviewer-facing caption note, and `core/` stays byte-frozen (D-14-22). Three previously-unconsumed audited JSONs (tstr.json, noise_model_sensitivity.json, shot_noise_sensitivity.json) are now consumed.
</success_criteria>

<output>
Create `.planning/phases/14-paper-revision-release-freeze/14-10-SUMMARY.md` when done
</output>
