---
phase: 14-paper-revision-release-freeze
plan: 12
type: execute
wave: 10
depends_on: ["14-09", "14-10", "14-11"]
files_modified:
  - docs/paper_blocks_framing.md
  - docs/paper_blocks_refs_methods.md
  - docs/reviewer_response.md
  - docs/reconciliation_note.md
  - docs/completeness_sweep_manifest.md
autonomous: true
requirements: [PAPER-01, PAPER-03, PAPER-08, PAPER-09]
must_haves:
  truths:
    - "Pure-additive edits across the 4 existing paper-blocks docs: no replacement of locked content, no rewrites of the existing AFTER LaTeX blocks, no removal of any existing `\\cite{}` or table — every change is either (a) a new sentence/footnote citing a 14-09/14-10/14-11 artifact alongside an existing block or (b) an append-only new subsection (reviewer_response Completeness sweep / reconciliation_note caveat paragraph / new completeness_sweep_manifest.md)"
    - "Existing LaTeX numbered claims are preserved verbatim: PAPER-01 hypothesis-reframe block, PAPER-02 LOCKED de-overclaiming blocks (D-14-20), PAPER-03 Circuit Design Rationale prose, PAPER-08 dataset paragraph (rendered from model_info.json), PAPER-09 evaluation-scale table (rendered from fidelity_dualscale.json) — none are rewritten; new citations are appended to or annotated next to them"
    - "`verify_number_provenance.py` is UNMODIFIED throughout (D-14-16): `git diff --stat verify_number_provenance.py` returns empty at every task close"
    - "`core/` byte-frozen (D-14-22): `git diff --stat core/` returns empty at every task close; this plan touches ONLY `docs/` files"
    - "Final end-to-end provenance verify in Task 5 runs `verify_number_provenance.py --target <doc>` against six documents (paper_blocks_framing.md, paper_blocks_refs_methods.md, reviewer_response.md, reconciliation_note.md, methods_full.md, circuit_atlas.md) and every one exits 0 — methods_full.md and circuit_atlas.md were each already verified by their owning plans (14-11 / 14-09) but the aggregate re-verify is the healthy final integration check"
    - "PAPER-03 (paper_blocks_framing.md) gains explicit citations of `results/figures/circuits/{default_75,iqp_sel_55,V1,V2,V3}.png` and `docs/circuit_atlas.md` (Task 1, additive-only — the existing Circuit Design Rationale prose is preserved verbatim and a new artifact-citation paragraph + bulleted figure list is appended underneath)"
    - "PAPER-01 (paper_blocks_framing.md) gains an explicit citation of `results/figures/param_efficiency_pareto.{png,pdf}` in the parameter-matched comparison block (Task 1, additive — appended footnote/sentence next to the existing PAPER-01b transition or PAPER-02b matched-baseline paragraph; never rewrites the LaTeX AFTER block)"
    - "PAPER-08 (paper_blocks_refs_methods.md) gains an explicit `see docs/methods_full.md §1` reference replacing/supplementing the dataset-prose stub at the head of the existing PAPER-08 block; the JSON-sourced dataset paragraph itself is preserved verbatim"
    - "PAPER-09 (paper_blocks_refs_methods.md) gains explicit citations of 14-10's 6 figures (training_convergence_all_models, tstr_crossmodel, failure_modes_summary, seed_variance_per_model, noise_robustness_quantum, shot_noise_robustness) at the specific claims they each support; existing dual-scale evaluation table is preserved verbatim"
    - "reviewer_response.md gains a new appended section `## Completeness sweep (Plans 14-09 .. 14-11)` with one subsection per closed reviewer concern; R1-M4 ('Incomplete optimization / training details — full Training Protocol') is EXPLICITLY marked RESOLVED with a citation to `docs/methods_full.md` (the existing R1-M4 row in the major-issues table is augmented, not rewritten — the new section is the appended sweep audit)"
    - "reconciliation_note.md gains at most ONE short caveat paragraph noting integration findings (V1/V2/V3 param_count breakdown now lives in the new config locks; headline-vs-repro distinction now visualized in param_efficiency_pareto); if no substantive caveat exists, Task 4 explicitly logs 'no append needed' and skips the edit — no padding"
    - "`docs/completeness_sweep_manifest.md` is a new reviewer-facing index that enumerates EVERY artifact added across 14-09 (5 circuit PNG + 5 PDF + 5 companion JSON + 5 config locks + 1 atlas + 1 emitter), 14-10 (7 PNG + 7 PDF + 7 companion JSON + 1 extended figure-suite emitter), and 14-11 (3 emitter scripts + 3 audited JSONs + 1 methods doc) with one-line descriptions, source plan IDs, and provenance source paths; PASSES `verify_number_provenance.py` unmodified"
    - "Plan 14-12 marked complete in the ROADMAP progress table (10/12, then 11/12 once executed; the table goes from 10/11 to 10/12 at planning time and from 10/12 to 11/12 once Task 5 commits); ROADMAP Wave 10 section added after Wave 9; D-14-23 scope note extended with one sentence covering Plan 12"
    - "Files explicitly NOT modified by this plan: any `results/*.json` (already locked); `run_*.py` (no script edits — this is a docs-only integration plan); `core/` (D-14-22 byte-freeze); `verify_number_provenance.py` (D-14-16); `docs/methods_full.md` (owned by 14-11, byte-stable after that plan); `docs/circuit_atlas.md` (owned by 14-09, byte-stable after that plan); the read-only Overleaf-canonical `main (4) copy.tex` / `supp_material.tex` (D-14-18)"
  artifacts:
    - path: "docs/paper_blocks_framing.md"
      provides: "Updated PAPER-01..05 LaTeX-blocks file. Pure-additive integration: PAPER-03 block gains a new 'Companion architecture atlas' paragraph citing the 5 circuit-diagram PNGs and `docs/circuit_atlas.md`; PAPER-01b (or PAPER-02b) gains a one-line citation of `results/figures/param_efficiency_pareto.{png,pdf}` near the parameter-matched comparison claim. Existing AFTER LaTeX blocks, provenance footer, and source map are preserved verbatim."
    - path: "docs/paper_blocks_refs_methods.md"
      provides: "Updated PAPER-06..11 LaTeX-blocks file. Pure-additive integration: PAPER-08 block head gains a one-line 'see docs/methods_full.md §1 (Dataset)' reference replacing the prose-only methods stub; PAPER-09 block gains 6 inline citations of 14-10's figures (training_convergence_all_models at training-protocol claim, tstr_crossmodel at TSTR claim, failure_modes_summary at per-model fidelity claim, seed_variance_per_model at multi-seed mean±std claim, noise_robustness_quantum at noise-sensitivity claim, shot_noise_robustness at shot-noise claim). The existing JSON-sourced dataset paragraph and dual-scale evaluation table are preserved verbatim."
    - path: "docs/reviewer_response.md"
      provides: "Updated point-by-point response. A new appended section '## Completeness sweep (Plans 14-09 .. 14-11)' lists every reviewer concern these three plans close, with explicit artifact-path citations. R1-M4 is explicitly marked RESOLVED with a direct citation to `docs/methods_full.md`. The existing two major-issue tables (Reviewer 1 + Reviewer 2) and the Summary / Cross-Cutting Provenance Note are preserved verbatim."
    - path: "docs/reconciliation_note.md"
      provides: "Updated 1000ep -> 2000ep reconciliation note. At most ONE appended short paragraph after the existing EMD table noting (a) the V1/V2/V3 param_count breakdown rows now live in the three new config locks from 14-09 and (b) the headline-vs-repro distinction (D-14-10) is now visualized in `param_efficiency_pareto`. If no substantive caveat exists, the task explicitly skips the edit and logs 'no append needed' in the commit body — no padding."
    - path: "docs/completeness_sweep_manifest.md"
      provides: "NEW reviewer-facing copy-paste manifest. One-line description + source plan ID + provenance source path for every artifact added across 14-09 / 14-10 / 14-11: 5 circuit PNGs + 5 PDFs + 5 companion JSONs + 5 config-lock JSONs (default_75/v1/v2/v3 + canonical was pre-existing) + 1 circuit atlas + 1 circuit-diagram emitter (14-09); 7 story-completeness PNGs + 7 PDFs + 7 companion JSONs + 1 extended run_figure_suite.py (14-10); 3 audited JSONs (classical_architectures.json, framework_versions.json, methods_full.json) + 3 emitter scripts (run_classical_arch_extract.py, run_framework_versions.py, run_methods_full.py) + 1 methods doc (methods_full.md) (14-11); plus this manifest itself. Passes the provenance gate."
      min_lines: 80
  key_links:
    - from: "docs/paper_blocks_framing.md (PAPER-03 block, additive paragraph)"
      to: "results/figures/circuits/{default_75,iqp_sel_55,V1,V2,V3}.png + docs/circuit_atlas.md"
      via: "appended 'Companion architecture atlas' paragraph after the existing AFTER LaTeX block; references the 5 PNG paths verbatim and links to the atlas for the spec tables; the existing prose, locked-decomposition citation (canonical_config_lock.json), and number-provenance footer are preserved verbatim"
      pattern: "results/figures/circuits/|docs/circuit_atlas.md"
    - from: "docs/paper_blocks_framing.md (PAPER-01b / PAPER-02b matched-baseline claim)"
      to: "results/figures/param_efficiency_pareto.{png,pdf,json}"
      via: "one-sentence footnote citing the param-efficiency Pareto figure as the visual companion of the parameter-matched comparison hypothesis; the AFTER LaTeX block itself is NOT rewritten"
      pattern: "param_efficiency_pareto"
    - from: "docs/paper_blocks_refs_methods.md (PAPER-08 block head)"
      to: "docs/methods_full.md §1 (Dataset)"
      via: "one-line `see docs/methods_full.md §1` reference at the head of the existing PAPER-08 block; the JSON-sourced dataset paragraph (rendered from model_info.json) is preserved verbatim — the new reference replaces only the prose-only methods stub at the top, not the load-bearing AFTER block"
      pattern: "methods_full.md"
    - from: "docs/paper_blocks_refs_methods.md (PAPER-09 block)"
      to: "results/figures/{training_convergence_all_models,tstr_crossmodel,failure_modes_summary,seed_variance_per_model,noise_robustness_quantum,shot_noise_robustness}.{png,pdf,json}"
      via: "6 inline citations at the specific claims they each support (training-protocol, TSTR, per-model fidelity, multi-seed mean±std, noise-sensitivity, shot-noise); the existing dual-scale evaluation table from fidelity_dualscale.json is preserved verbatim"
      pattern: "training_convergence_all_models|tstr_crossmodel|failure_modes_summary|seed_variance_per_model|noise_robustness_quantum|shot_noise_robustness"
    - from: "docs/reviewer_response.md (new appended section)"
      to: "every artifact from 14-09 / 14-10 / 14-11 + docs/methods_full.md (R1-M4 resolution)"
      via: "appended `## Completeness sweep (Plans 14-09 .. 14-11)` with subsections per closed concern: R1-M4 RESOLVED via methods_full.md, R2-5b strengthened via circuit_atlas.md + 5 circuit diagrams, R1-M2 strengthened via tstr_crossmodel + failure_modes_summary, R1-M4 (multi-seed) strengthened via seed_variance_per_model, R1-M4 (analytic vs shot noise) strengthened via shot_noise_robustness + noise_robustness_quantum, PAPER-01 (param-matched) strengthened via param_efficiency_pareto, R1-M5 (calibration) strengthened via failure_modes_summary"
      pattern: "Completeness sweep|R1-M4 RESOLVED|methods_full.md"
    - from: "docs/reconciliation_note.md (at-most-one appended paragraph)"
      to: "results/{v1,v2,v3}_config_lock.json + results/figures/param_efficiency_pareto.json"
      via: "at most one short caveat paragraph appended AFTER the existing EMD table noting (a) V1/V2/V3 param_count breakdown now lives in the three new config locks (14-09) and (b) headline-vs-repro visualization landed in param_efficiency_pareto (14-10); if neither is materially worth recording the task logs 'no append needed' and skips the edit"
      pattern: "v1_config_lock|param_efficiency_pareto|no append needed"
    - from: "docs/completeness_sweep_manifest.md (NEW)"
      to: "every artifact added in 14-09 / 14-10 / 14-11"
      via: "tabular manifest with one row per artifact: file path | plan id | one-line description | source provenance path (config-lock JSON / source JSON / source script); copy-paste reviewer-facing index"
      pattern: "14-09|14-10|14-11"
    - from: "verify_number_provenance.py"
      to: "all 6 paper-blocks docs"
      via: "Task 5 runs the gate against paper_blocks_framing.md, paper_blocks_refs_methods.md, reviewer_response.md, reconciliation_note.md, methods_full.md, circuit_atlas.md — all 6 must exit 0; the gate itself is unmodified (`git diff --stat verify_number_provenance.py` empty)"
      pattern: "verify_number_provenance.py --target"
---

<objective>
Thread the new artifacts from 14-09 (circuit diagrams + config locks + atlas),
14-10 (7 story-completeness figures), and 14-11 (paper-ready Methods document +
classical-architecture extraction + pinned framework versions) into the four
paper-facing documents that the manuscript-revision pipeline reads, then run a
single end-to-end provenance verification across every paper-blocks doc plus
the two byte-stable docs owned by 14-09 / 14-11. Emit a reviewer-facing
completeness-sweep manifest enumerating every new artifact across the three
upstream plans. Pure-additive integration — no rewrite of locked content, no
edit of `core/`, no edit of `verify_number_provenance.py`.

Purpose: 14-09, 14-10, and 14-11 each landed substantial new artifacts that
close specific reviewer concerns (PAPER-03 circuit-architecture visualization,
PAPER-01 / PAPER-09 missing story-completeness figures, R1-M4 full Training
Protocol). Those artifacts are EXISTING and AUDITED on disk but they are not
yet cited from the four paper-facing docs the revision pipeline reads
(`paper_blocks_framing.md`, `paper_blocks_refs_methods.md`,
`reviewer_response.md`, `reconciliation_note.md`). This plan threads the
citations through, with a single aggregate end-to-end provenance verify and a
reviewer-facing manifest as the integration close. After this plan completes,
only 14-07 (Zenodo deposit + tag + DOI wiring) remains.

Output: 4 updated paper-blocks docs (pure-additive citations only) + 1 new
reviewer-facing manifest, all gated by the existing `verify_number_provenance.py`
unmodified, `core/` untouched (D-14-22), no retraining, no new figures,
no new JSONs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-paper-revision-release-freeze/14-CONTEXT.md
@.planning/phases/14-paper-revision-release-freeze/14-PATTERNS.md
@.planning/phases/14-paper-revision-release-freeze/14-09-PLAN.md
@.planning/phases/14-paper-revision-release-freeze/14-10-PLAN.md
@.planning/phases/14-paper-revision-release-freeze/14-11-PLAN.md

<interfaces>
<!-- All interfaces below are EXISTING and BYTE-STABLE on disk after their
     owning plans completed; this plan only cites them. No new contracts are
     created. The four target docs are loaded by the manuscript-revision
     pipeline as paper-facing copy-paste blocks; the new completeness-sweep
     manifest is a single new reviewer-facing index. -->

docs/paper_blocks_framing.md — PAPER-01..05 LaTeX-blocks (existing):
- PAPER-01 (hypothesis reframe), PAPER-02 (LOCKED de-overclaiming, D-14-20),
  PAPER-03 (Circuit Design Rationale subsection, anchored at main:155 after
  QWGAN-GP Architecture Overview), PAPER-04 (log-returns bioprocess
  justification), PAPER-05 (Outlook / decision-tree / Hybrid-GAN demotion).
- PAPER-03's existing AFTER block already cites num_qubits=5, num_layers=3,
  param_count=55 (iqp_sel_55), and depths/topology for V1/V2/V3 from
  model_info.json / canonical_config_lock.json / ansatz_comparison.json. This
  plan APPENDS a "Companion architecture atlas" paragraph that points reviewers
  at the 5 rendered diagrams + `docs/circuit_atlas.md` — it does NOT
  rewrite the AFTER LaTeX block.
- PAPER-01b's existing AFTER block describes the parameter-matched comparison
  qualitatively. This plan APPENDS a one-sentence footnote citing
  `results/figures/param_efficiency_pareto.{png,pdf}` as the visual
  companion — again, no rewrite of the AFTER LaTeX block.
- The provenance footer + source map at the bottom of paper_blocks_framing.md
  is preserved verbatim; any newly cited literal numbers are already resolved
  by the existing source map.

docs/paper_blocks_refs_methods.md — PAPER-06..11 LaTeX-blocks (existing):
- PAPER-08 (Dataset Details in Methods) inserts a new \paragraph{Dataset and
  preprocessing.} block into main:180 with EVERY numeric literal annotated
  `% source: results/model_info.json#<path>` (independent_campaigns=1,
  raw_csv_rows=778, log_return_rows=777, window_length=10, window_stride=2,
  rolling_windows=384, train/val/test=384/0/0, seed_set=[42,43,44,45,46]).
  This plan APPENDS a one-line `see docs/methods_full.md §1` reference
  at the head of the existing PAPER-08 block (the prose-only methods stub at
  the very top of the section is what gets replaced — the JSON-sourced
  paragraph itself is preserved verbatim).
- PAPER-09 (Per-Metric Evaluation Scale in Methods) inserts an \paragraph{Evaluation
  scale.} block with the dual-scale evaluation Table~\ref{tbl:eval_scale} whose
  values come verbatim from fidelity_dualscale.json (model_kind=quantum,
  pipeline=B, seed=42). This plan APPENDS 6 inline citations to 14-10's figures
  at the specific claims they each support; the table itself is preserved.

docs/reviewer_response.md — point-by-point response (existing):
- Summary, Reviewer 1 Major Issues (R1-M1..R1-M5), Reviewer 1 Minor Issues
  (R1-m1..R1-m7), Reviewer 2 Issues (R2-1..R2-6), Cross-Cutting Provenance Note.
- R1-M4 row currently reads: "Added full Training Protocol (all hyperparameters
  rendered from JSON); stated analytic statevector (no shot noise) backend;
  added shot-noise sensitivity; multi-seed (5 seeds) mean ± std; clarified
  Supp Eq. A3 log-GAN vs Wasserstein discrepancy" with Supporting artifact
  `docs/training_protocol.md; results/shot_noise_sensitivity.json;
  results/multiseed_summary.json`.
- This plan APPENDS a new `## Completeness sweep (Plans 14-09 .. 14-11)`
  section AFTER the Cross-Cutting Provenance Note (the bottom of the file),
  with one subsection per reviewer concern this sweep closes. R1-M4 is
  explicitly marked RESOLVED with a citation to `docs/methods_full.md`
  (the paper-ready Methods doc 14-11 landed). The existing R1-M4 table row is
  NOT rewritten — the new section is the appended sweep audit that says "the
  evidence for R1-M4 is now consolidated in methods_full.md, which 14-11 emits
  as the paper-ready Methods document."

docs/reconciliation_note.md — 1000ep -> 2000ep delta table (existing):
- The single existing section is `## EMD (OD scale) — final-eval mean over
  seeds 42-46` with per-model OLD vs NEW deltas. data_hash = 91e447d4624e25b3.
- This plan APPENDS at most ONE short caveat paragraph AFTER the existing EMD
  table noting (a) V1/V2/V3 param_count breakdown rows now live in the three
  new config locks from 14-09 (v1/v2/v3_config_lock.json) and (b) the
  headline-vs-repro distinction (D-14-10) is now visualized in
  `results/figures/param_efficiency_pareto.{png,pdf}`. If neither is
  materially worth recording, Task 4 explicitly logs "no append needed" and
  skips the edit — NO padding (this rule is explicit in the plan_requirements).

docs/methods_full.md (existing — byte-stable after 14-11):
- 7 sections: 1. Dataset / 2. Models / 3. Training / 4. Hardware & Software /
  5. Reproducibility / 6. Addressing the documented contradictions / 7.
  Provenance footer. Section 1 = the dataset paragraph that PAPER-08 will
  cross-reference. Section 3 = the Training Protocol that closes R1-M4.
- This plan DOES NOT edit methods_full.md (it is owned by 14-11 and is
  byte-stable). It only CITES into it.

docs/circuit_atlas.md (existing — byte-stable after 14-09):
- 7 sections: 1. default_75 / 2. iqp_sel_55 / 3. V1 / 4. V2 / 5. V3 /
  6. Cross-comparison / 7. Provenance footer. Every section embeds a
  ![](circuits/<name>.png) image + spec table + contrast paragraph.
- This plan DOES NOT edit circuit_atlas.md (it is owned by 14-09 and is
  byte-stable). It only CITES into it.

14-09 artifacts to cite (PAPER-03 visualization gap closure):
- results/figures/circuits/default_75.{png,pdf,json}
- results/figures/circuits/iqp_sel_55.{png,pdf,json}
- results/figures/circuits/V1.{png,pdf,json}
- results/figures/circuits/V2.{png,pdf,json}
- results/figures/circuits/V3.{png,pdf,json}
- results/v1_config_lock.json, v2_config_lock.json, v3_config_lock.json, default_75_config_lock.json
- docs/circuit_atlas.md
- run_circuit_diagrams.py (emitter)

14-10 artifacts to cite (PAPER-01 / PAPER-09 story-completeness gap closure):
- results/figures/training_convergence_all_models.{png,pdf,json}
- results/figures/tstr_crossmodel.{png,pdf,json}
- results/figures/failure_modes_summary.{png,pdf,json}
- results/figures/param_efficiency_pareto.{png,pdf,json}
- results/figures/seed_variance_per_model.{png,pdf,json}
- results/figures/noise_robustness_quantum.{png,pdf,json}
- results/figures/shot_noise_robustness.{png,pdf,json}
- run_figure_suite.py (extended emitter — the 7 new render-* functions)

14-11 artifacts to cite (PAPER-08 + PAPER-09 Methods consolidation, R1-M4 RESOLVED):
- results/classical_architectures.json (emitter: run_classical_arch_extract.py)
- results/framework_versions.json (emitter: run_framework_versions.py)
- results/methods_full.json (emitter: run_methods_full.py)
- docs/methods_full.md (the paper-ready Methods doc)

verify_number_provenance.py auto-coverage (the gate, unmodified):
- The gate's `_json_blobs()` rglobs `results/*.json` — all newly
  added JSONs (config locks from 14-09, companion JSONs from 14-10, audited
  JSONs from 14-11) are already in the gate's resolution corpus.
- The gate strips identifier-like tokens (D-14-NN, R1-MN, plan IDs like 14-09,
  source line citations file:NNN). Citation forms like "see 14-09" and
  "R1-M4" do NOT require provenance.
- File-path tokens like `results/figures/circuits/default_75.png` are
  not numeric and pass through the literal extractor without scrutiny.
- New numeric literals introduced by this plan (e.g. an "all 6 docs pass"
  count in Task 5's manifest) MUST resolve through the existing corpus or be
  avoided by phrasing (the manifest is a list of artifact paths, not a numeric
  summary — Claude's discretion on phrasing to keep the manifest gate-clean).

Pure-additive edit discipline (the load-bearing rule of this plan):
- ALL Task 1-4 edits are APPEND-ONLY or one-line inline citation appends
  alongside existing locked content.
- NO existing LaTeX block, prose paragraph, table, footnote, citation, or
  bullet is rewritten, replaced, deleted, or restructured.
- The only justification for an inline edit (Task 2, replacing a prose-only
  methods stub with the `see methods_full.md §<section>` reference) is when the
  upstream-plan SOURCE specifically calls for replacement of a stub line; even
  then, the JSON-sourced AFTER block underneath is preserved verbatim.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 14-09/14-10 artifact citations to paper_blocks_framing.md (PAPER-03 + PAPER-01, additive only)</name>
  <files>docs/paper_blocks_framing.md</files>
  <read_first>
    - docs/paper_blocks_framing.md (the WHOLE existing file — every existing AFTER LaTeX block, provenance footer, source map; nothing in this file may be rewritten by this task)
    - docs/circuit_atlas.md (the byte-stable companion atlas owned by 14-09; cited as "see docs/circuit_atlas.md" — section 1..5 hold the per-circuit spec tables; section 6 holds the cross-comparison; section 7 is the provenance footer)
    - results/figures/circuits/{default_75,iqp_sel_55,V1,V2,V3}.json (companion JSONs for the 5 circuit diagrams; confirm the metadata fields and file paths but do NOT modify)
    - results/figures/param_efficiency_pareto.json (companion JSON for 14-10's parameter-efficiency Pareto figure; confirms file path and provenance — source = model_info.json + matched2000_dualscale.json)
    - .planning/phases/14-paper-revision-release-freeze/14-09-PLAN.md (the existing PAPER-03 visualization-gap-closure plan whose artifacts are cited here)
    - .planning/phases/14-paper-revision-release-freeze/14-10-PLAN.md (the existing PAPER-01/PAPER-09 story-completeness plan whose param_efficiency_pareto artifact is cited here)
  </read_first>
  <action>
    Open `docs/paper_blocks_framing.md` in append/inline-only mode. The
    existing file has 5 PAPER-* sections; this task touches PAPER-03 and ONE of
    {PAPER-01b, PAPER-02b} (Claude's discretion based on the most natural
    cite-anchor for the param-efficiency Pareto figure — both are valid
    parameter-matched comparison anchors).

    Step A — PAPER-03 Companion architecture atlas paragraph:
    Locate the existing PAPER-03 section (header `# PAPER-03 — Circuit Design
    Rationale subsection`). After the existing closing AFTER LaTeX block
    (the one ending with `\end{quote}` or the final `\textbf{Classical critic
    with a quantum generator.}` block — Claude's discretion on exact insertion
    point: it should be AFTER the entire `latex` fenced code block of the AFTER
    block and BEFORE the `- **Rationale (R2-5b):**` bullet that closes the
    PAPER-03 section), append a NEW `### PAPER-03 — Companion artifact atlas`
    subsection (additive — sits between the AFTER block and the existing
    Rationale bullet, or appended after the Rationale bullet — Claude's
    discretion; pick whichever placement reads cleanest).

    The new subsection's body (markdown prose, NOT a new LaTeX block — the
    AFTER block is already correct and is NOT rewritten):

    > The five production quantum circuits cited in the AFTER block above are
    > rendered as PennyLane `qml.draw_mpl` architecture diagrams under
    > `results/figures/circuits/`. Reviewers can audit the
    > architecture-vs-claim correspondence directly:
    >
    > - `default_75` — `results/figures/circuits/default_75.{png,pdf}`
    >   (companion JSON `default_75.json`; spec lock
    >   `results/default_75_config_lock.json`)
    > - `iqp_sel_55` — `results/figures/circuits/iqp_sel_55.{png,pdf}`
    >   (companion JSON `iqp_sel_55.json`; spec lock
    >   `results/canonical_config_lock.json`; frozen checkpoint epoch
    >   1969 per Plan 14-01)
    > - `V1` — `results/figures/circuits/V1.{png,pdf}` (companion JSON
    >   `V1.json`; spec lock `results/v1_config_lock.json`)
    > - `V2` — `results/figures/circuits/V2.{png,pdf}` (companion JSON
    >   `V2.json`; spec lock `results/v2_config_lock.json`)
    > - `V3` — `results/figures/circuits/V3.{png,pdf}` (companion JSON
    >   `V3.json`; spec lock `results/v3_config_lock.json`)
    >
    > The full per-circuit spec tables, cross-comparison, and provenance footer
    > are consolidated in `docs/circuit_atlas.md` (PAPER-03 atlas,
    > Plan 14-09). Every numeric literal in the atlas — and every numeric
    > literal in the AFTER block above — resolves to one of the five
    > config-lock JSONs (`canonical_config_lock.json`,
    > `default_75_config_lock.json`, `v1_config_lock.json`,
    > `v2_config_lock.json`, `v3_config_lock.json`) and is gated by
    > `verify_number_provenance.py` unmodified.

    Step B — PAPER-01 (or PAPER-02b) param-efficiency Pareto footnote:
    Pick the cleanest anchor: either (1) AFTER PAPER-01b's AFTER block (the
    "open empirical question" reframe), or (2) AFTER PAPER-02b's AFTER block
    (the "matched parameter budget and matched training epochs" overclaim
    softening). Append a NEW one-sentence subsection (or markdown footnote)
    immediately after the chosen AFTER block and BEFORE the section's
    Rationale bullet. Claude's discretion on exact placement; recommended
    anchor = PAPER-02b because that is where the matched-budget non-superiority
    statement lives and `param_efficiency_pareto` IS the visual companion of
    that exact claim.

    The footnote body (markdown prose, NOT a LaTeX block):

    > **Visual companion (Plan 14-10).** The parameter-matched comparison
    > stated in this paragraph is rendered as a parameter-efficiency Pareto
    > scatter at `results/figures/param_efficiency_pareto.{png,pdf}`
    > (companion JSON `param_efficiency_pareto.json`; source =
    > `results/model_info.json` (n_params per model) +
    > `results/matched2000_dualscale.json` (EMD mean±std per scale)).
    > The frozen-checkpoint headline (D-14-10) appears as a visually distinct
    > marker; the iqp_sel_55_repro matched-budget reproduction and the V1/V2/V3
    > ansatz variants appear as separate points in the same scatter so the
    > headline-vs-repro distinction is unambiguous on inspection.

    Step C — Preserve the Provenance footer and source map at the bottom of
    paper_blocks_framing.md VERBATIM. No new numeric literals are introduced
    by Step A or Step B that are not already in the source map (the new prose
    references file paths and the existing literal `1969`, which the gate
    already resolves through `canonical_config_lock.json.checkpoint_epoch`).

    NO other edits to this file. Do NOT rewrite any existing AFTER LaTeX
    block, any existing Rationale bullet, any existing BEFORE block, any
    existing citation, or the provenance footer.

    After writing, run the provenance gate against this doc and confirm PASS.
  </action>
  <verify>
    <automated>./qgan_env/bin/python verify_number_provenance.py --target docs/paper_blocks_framing.md &amp;&amp; ./qgan_env/bin/python -c "import pathlib,sys; t=pathlib.Path('docs/paper_blocks_framing.md').read_text(); needed=['results/figures/circuits/default_75','results/figures/circuits/iqp_sel_55','results/figures/circuits/V1','results/figures/circuits/V2','results/figures/circuits/V3','docs/circuit_atlas.md','param_efficiency_pareto','canonical_config_lock.json','default_75_config_lock.json','v1_config_lock.json','v2_config_lock.json','v3_config_lock.json','Plan 14-09','Plan 14-10']; missing=[n for n in needed if n not in t]; preserved=['Reframed central question','Soften the quantum-necessity transition','De-overclaiming','LOCKED','Circuit Design Rationale','iqp_sel_55','55-parameter','75','135']; lost=[p for p in preserved if p not in t]; print('missing',missing,'lost',lost); sys.exit(0 if not missing and not lost else 1)" &amp;&amp; [ -z "$(git diff --stat core/ verify_number_provenance.py)" ]</automated>
  </verify>
  <acceptance_criteria>
    - `docs/paper_blocks_framing.md` PASSES `verify_number_provenance.py --target` unmodified
    - PAPER-03 section gains a "Companion artifact atlas" subsection (additive) citing all 5 circuit PNG paths + `docs/circuit_atlas.md` + all 5 config-lock JSONs
    - PAPER-01 (or PAPER-02b) gains a one-sentence footnote citing `results/figures/param_efficiency_pareto.{png,pdf}` as the visual companion of the parameter-matched claim, alongside its source provenance (model_info.json + matched2000_dualscale.json)
    - Every existing PAPER-01..05 AFTER LaTeX block is preserved VERBATIM (grep confirms key signature phrases: "Reframed central question", "Soften the quantum-necessity transition", "De-overclaiming", "LOCKED", "Circuit Design Rationale", "iqp_sel_55", "55-parameter", "75", "135" all still present)
    - The Provenance footer + source map at the bottom of the file is preserved verbatim
    - `git diff --stat core/` is empty (D-14-22 byte-freeze)
    - `git diff --stat verify_number_provenance.py` is empty (D-14-16, gate unmodified)
    - Re-running the gate is idempotent — the doc passes on first run
  </acceptance_criteria>
  <done>paper_blocks_framing.md gains an additive PAPER-03 atlas-companion subsection citing 5 circuit diagrams + circuit_atlas.md + 5 config locks, and a one-sentence PAPER-01/02b footnote citing param_efficiency_pareto; every existing AFTER LaTeX block preserved verbatim; provenance gate passes unmodified.</done>
</task>

<task type="auto">
  <name>Task 2: Add 14-10/14-11 artifact citations to paper_blocks_refs_methods.md (PAPER-08 + PAPER-09, additive only)</name>
  <files>docs/paper_blocks_refs_methods.md</files>
  <read_first>
    - docs/paper_blocks_refs_methods.md (the WHOLE existing file — PAPER-06..11 blocks, the PAPER-08 dataset paragraph + render-from-JSON contract, the PAPER-09 evaluation-scale table + render-from-JSON contract; nothing in this file may be rewritten by this task except where explicitly noted)
    - docs/methods_full.md (the byte-stable Methods doc owned by 14-11; section anchors §1 Dataset, §2 Models, §3 Training, §4 Hardware & Software, §5 Reproducibility, §6 Addressing the documented contradictions, §7 Provenance footer — confirm exact heading text matches verbatim)
    - results/figures/training_convergence_all_models.json (companion JSON for 14-10's training-convergence figure; source = 45 per-run metrics.json + headline_canonical.json)
    - results/figures/tstr_crossmodel.json (companion JSON for 14-10's TSTR cross-model figure; source = results/tstr.json)
    - results/figures/failure_modes_summary.json (companion JSON for 14-10's failure-modes summary; source = matched2000_dualscale.json + dist/acf companion JSONs)
    - results/figures/seed_variance_per_model.json (companion JSON for 14-10's per-model seed-variance facets; source = 45 per-run metrics.json)
    - results/figures/noise_robustness_quantum.json (companion JSON for 14-10's noise robustness figure; source = noise_model_sensitivity.json)
    - results/figures/shot_noise_robustness.json (companion JSON for 14-10's shot-noise robustness figure; source = shot_noise_sensitivity.json)
    - .planning/phases/14-paper-revision-release-freeze/14-10-PLAN.md (the existing story-completeness plan whose 7 figures are cited here — 6 of the 7 are cited from PAPER-09, the 7th (param_efficiency_pareto) is cited from PAPER-01 in Task 1)
    - .planning/phases/14-paper-revision-release-freeze/14-11-PLAN.md (the existing Methods-consolidation plan whose methods_full.md is cited from PAPER-08)
  </read_first>
  <action>
    Open `docs/paper_blocks_refs_methods.md` in append/inline-only mode.
    The existing file has 6 PAPER-* sections (PAPER-06, PAPER-07, PAPER-10,
    PAPER-11, PAPER-08, PAPER-09 — in that on-disk order). This task touches
    PAPER-08 and PAPER-09.

    Step A — PAPER-08 head reference to methods_full.md:
    Locate the PAPER-08 section (header `## PAPER-08 — Dataset Details in
    Methods`). The existing R1-m2 rationale paragraph reads: "the reviewer asks
    for the raw number of time points, the number of rolling windows, the
    train/val/test split ratios and counts, and the number of independent runs
    to be reported in Methods. Every number in the block below is rendered FROM
    `results/model_info.json` ..." This rationale paragraph is
    preserved verbatim. AFTER that rationale paragraph and BEFORE the
    `**Insertion point:**` line, INSERT a new one-line cross-reference
    paragraph:

    > **Consolidated Methods document.** The same dataset numbers — plus the
    > model registry, training protocol, hardware/software stack, and
    > reproducibility contract — are consolidated in `docs/methods_full.md`
    > §1 (Dataset) through §5 (Reproducibility), rendered from
    > `results/methods_full.json` + `results/model_info.json` +
    > the 5 config-lock JSONs + `results/classical_architectures.json`
    > + `results/framework_versions.json` (Plan 14-11). The
    > copy-paste LaTeX block below remains the single load-bearing manuscript
    > insertion; methods_full.md is the reviewer-facing audit companion.

    The JSON-sourced \paragraph{Dataset and preprocessing.} AFTER block + the
    Render-from-JSON contract footer + every numeric literal annotation
    underneath it is preserved VERBATIM. The new reference paragraph replaces
    ONLY the prose-only methods stub at the head of the section (the rationale
    paragraph itself is preserved; only an additional cross-reference paragraph
    is inserted between the rationale and the Insertion point line).

    Step B — PAPER-09 inline citations to 14-10's 6 figures:
    Locate the PAPER-09 section (header `## PAPER-09 — Per-Metric Evaluation
    Scale in Methods`). The existing R1-m3 rationale paragraph + the AFTER
    LaTeX block + Table~\ref{tbl:eval_scale} + the Render-from-JSON contract
    footer are preserved verbatim. AFTER the Render-from-JSON contract footer
    (the `> **Render-from-JSON contract.** Every value in the table is the
    exact stored ...` paragraph at the bottom of the PAPER-09 section), INSERT
    a new `### PAPER-09 — Story-completeness figure citations (Plan 14-10)`
    subsection. Body:

    > The Methods evaluation-scale table above reports a single representative
    > seed (seed 42, Pipeline B). Six story-completeness figures from Plan
    > 14-10 support specific claims in the surrounding §3 Methods / §4 Results
    > narrative and should be cited at the points they each support:
    >
    > - **Training protocol claim (R1-M4):** the per-epoch convergence behavior
    >   of every matched-2000ep model is rendered at
    >   `results/figures/training_convergence_all_models.{png,pdf}`
    >   (companion JSON `training_convergence_all_models.json`; source = 45
    >   per-run metrics.json + `results/headline_canonical.json`).
    >   Cite alongside the Training Protocol paragraph (PAPER-08 / methods_full.md §3).
    >
    > - **TSTR utility claim (R1-M2):** the cross-model TSTR R²/MAE/RMSE bars
    >   for Pipelines A and B are rendered at
    >   `results/figures/tstr_crossmodel.{png,pdf}` (companion JSON
    >   `tstr_crossmodel.json`; source = `results/tstr.json`).
    >   Negative R² is plotted honestly per the companion JSON's
    >   `caption_note`. Cite alongside the §4.1 utility-evaluation subsection.
    >
    > - **Per-model fidelity / failure modes (R1-M5):** the diagnostic grid
    >   (distribution overlay × ACF lag-1 × log-return EMD, 9 models ordered
    >   by ascending OD EMD) is rendered at
    >   `results/figures/failure_modes_summary.{png,pdf}` (companion
    >   JSON `failure_modes_summary.json`; source =
    >   `results/matched2000_dualscale.json` + per-model dist/acf
    >   companion JSONs). Cite alongside the §4.1 per-model fidelity claim
    >   and the R1-M5 calibration discussion.
    >
    > - **Multi-seed mean ± std claim (R1-M4):** the per-seed EMD trajectories
    >   underneath the seed-aggregated mean ± std are rendered as a 3×3 facet
    >   grid at `results/figures/seed_variance_per_model.{png,pdf}`
    >   (companion JSON `seed_variance_per_model.json`; source = 45 per-run
    >   metrics.json). Cite alongside the multi-seed reporting paragraph
    >   (PAPER-08 / methods_full.md §3).
    >
    > - **Noise-model sensitivity claim (R1-M4):** EMD vs depolarizing /
    >   amplitude-damping noise level is rendered at
    >   `results/figures/noise_robustness_quantum.{png,pdf}`
    >   (companion JSON `noise_robustness_quantum.json`; source =
    >   `results/noise_model_sensitivity.json`). Cite alongside the
    >   noise-sensitivity discussion (R2-1 / R1-M4 backend statement).
    >
    > - **Shot-noise sensitivity claim (R1-M4):** EMD vs shot count (log-x)
    >   with the analytic-statevector reference line is rendered at
    >   `results/figures/shot_noise_robustness.{png,pdf}` (companion
    >   JSON `shot_noise_robustness.json`; source =
    >   `results/shot_noise_sensitivity.json`). Cite alongside the
    >   §3 backend statement (analytic statevector, no shot noise in the
    >   reported headline values) and the R1-M4 shot-noise paragraph.

    The existing dual-scale Table~\ref{tbl:eval_scale} values + the
    Render-from-JSON contract footer are preserved verbatim.

    NO other edits to this file. PAPER-06, PAPER-07, PAPER-10, PAPER-11 are
    NOT touched (they address reference / typo / Hybrid-GAN reviewer concerns
    that 14-09 / 14-10 / 14-11 do not bear on).

    After writing, run the provenance gate against this doc and confirm PASS.
  </action>
  <verify>
    <automated>./qgan_env/bin/python verify_number_provenance.py --target docs/paper_blocks_refs_methods.md &amp;&amp; ./qgan_env/bin/python -c "import pathlib,sys; t=pathlib.Path('docs/paper_blocks_refs_methods.md').read_text(); needed=['docs/methods_full.md','training_convergence_all_models','tstr_crossmodel','failure_modes_summary','seed_variance_per_model','noise_robustness_quantum','shot_noise_robustness','classical_architectures.json','framework_versions.json','methods_full.json','Plan 14-10','Plan 14-11']; missing=[n for n in needed if n not in t]; preserved=['PAPER-06','PAPER-07','PAPER-08','PAPER-09','PAPER-10','PAPER-11','Render-from-JSON contract','fidelity_dualscale.json','model_info.json','seed_set','Bernal','384','778','777','0.1209437521974767']; lost=[p for p in preserved if p not in t]; print('missing',missing,'lost',lost); sys.exit(0 if not missing and not lost else 1)" &amp;&amp; [ -z "$(git diff --stat core/ verify_number_provenance.py)" ]</automated>
  </verify>
  <acceptance_criteria>
    - `docs/paper_blocks_refs_methods.md` PASSES `verify_number_provenance.py --target` unmodified
    - PAPER-08 gains a one-line `see docs/methods_full.md §1 (Dataset) through §5 (Reproducibility)` cross-reference paragraph between the rationale paragraph and the Insertion point line; the rationale + AFTER block + Render-from-JSON contract footer are preserved verbatim
    - PAPER-09 gains a `### PAPER-09 — Story-completeness figure citations (Plan 14-10)` subsection at the bottom of the section, citing all 6 of 14-10's figures (training_convergence_all_models, tstr_crossmodel, failure_modes_summary, seed_variance_per_model, noise_robustness_quantum, shot_noise_robustness) at the specific claims they each support
    - The existing PAPER-09 AFTER block + Table~\ref{tbl:eval_scale} + Render-from-JSON contract footer + every literal value (0.1209437521974767, 0.022937980562900886, etc.) is preserved VERBATIM
    - PAPER-06, PAPER-07, PAPER-10, PAPER-11 sections are entirely unchanged
    - `git diff --stat core/` is empty (D-14-22)
    - `git diff --stat verify_number_provenance.py` is empty (D-14-16)
  </acceptance_criteria>
  <done>paper_blocks_refs_methods.md gains a PAPER-08 cross-reference paragraph pointing at methods_full.md §1-§5, and a new PAPER-09 subsection citing 14-10's 6 figures at the specific claims they support; every existing AFTER block / table / Render-from-JSON contract footer preserved verbatim; provenance gate passes unmodified.</done>
</task>

<task type="auto">
  <name>Task 3: Append Completeness sweep section to reviewer_response.md (R1-M4 RESOLVED, additive only)</name>
  <files>docs/reviewer_response.md</files>
  <read_first>
    - docs/reviewer_response.md (the WHOLE existing file — Summary, Reviewer 1 Major Issues table, Reviewer 1 Minor Issues table, Reviewer 2 Issues table, Cross-Cutting Provenance Note; the existing R1-M4 row reads "Added full Training Protocol (all hyperparameters rendered from JSON); stated analytic statevector (no shot noise) backend; added shot-noise sensitivity; multi-seed (5 seeds) mean ± std; clarified Supp Eq. A3 log-GAN vs Wasserstein discrepancy" — the EXISTING row is preserved verbatim, and the new section APPENDED at the bottom strengthens R1-M4 with a citation to methods_full.md)
    - docs/methods_full.md (the byte-stable paper-ready Methods doc from 14-11 — confirm §3 Training is the Training Protocol section that satisfies R1-M4)
    - docs/circuit_atlas.md (the byte-stable circuit atlas from 14-09 — confirm it satisfies R2-5b "Why this particular circuit / architecture?")
    - results/figures/{training_convergence_all_models,tstr_crossmodel,failure_modes_summary,seed_variance_per_model,noise_robustness_quantum,shot_noise_robustness,param_efficiency_pareto}.json (companion JSONs for 14-10's 7 figures; confirm file paths)
    - .planning/phases/14-paper-revision-release-freeze/14-09-PLAN.md
    - .planning/phases/14-paper-revision-release-freeze/14-10-PLAN.md
    - .planning/phases/14-paper-revision-release-freeze/14-11-PLAN.md
  </read_first>
  <action>
    Open `docs/reviewer_response.md` in append-only mode. The existing
    file ends with the Cross-Cutting Provenance Note paragraph (no trailing
    text after it). APPEND a NEW top-level section at the very bottom of the
    file:

    ```
    ---

    ## Completeness sweep (Plans 14-09 .. 14-11)

    Plans 14-09 (circuit-architecture diagrams + V1/V2/V3 config locks +
    `docs/circuit_atlas.md`), 14-10 (7 story-completeness figures),
    and 14-11 (paper-ready Methods document + classical-architecture extraction
    + pinned framework versions) close the remaining reviewer-facing gaps
    identified in the major-issues and minor-issues tables above. This
    subsection is an explicit audit: for each reviewer concern these three
    plans bear on, the artifact path that closes it and the plan that emitted
    it.
    ```

    Then one subsection per closed concern. Use the structure below (subsection
    heading text is mandatory; one-line citation body):

    ```
    ### R1-M4 — Incomplete optimization / training details — RESOLVED by Plan 14-11

    The full Training Protocol (optimizer / lr / betas / n_critic / lambda_gp /
    batch / epochs / early-stop), the analytic-statevector backend statement,
    the determinism contract (file:line citations for torch.manual_seed,
    np.random.seed, random.seed), and the verbatim rerun command template are
    consolidated in `docs/methods_full.md` §3 (Training) + §4
    (Hardware & Software) + §5 (Reproducibility), rendered from
    `results/methods_full.json` +
    `results/framework_versions.json` + the five config-lock JSONs
    (Plan 14-11). The shot-noise sensitivity and multi-seed mean ± std
    components of R1-M4 are additionally rendered as figures in Plan 14-10:
    `results/figures/shot_noise_robustness.{png,pdf}` (source =
    `results/shot_noise_sensitivity.json`),
    `results/figures/seed_variance_per_model.{png,pdf}` (source = 45
    per-run metrics.json), and `results/figures/training_convergence_all_models.{png,pdf}`
    (source = 45 per-run metrics.json + `results/headline_canonical.json`).
    R1-M4 is hereby marked **RESOLVED**.

    ### R2-5b — Why this particular circuit / architecture? — strengthened by Plan 14-09

    The Circuit Design Rationale subsection (PAPER-03) is now visually
    grounded by five `qml.draw_mpl` architecture diagrams at
    `results/figures/circuits/{default_75,iqp_sel_55,V1,V2,V3}.{png,pdf}`
    and the copy-paste-ready spec-table atlas at
    `docs/circuit_atlas.md` (Plan 14-09). Every numeric literal in
    the atlas resolves to one of the five config-lock JSONs
    (`results/canonical_config_lock.json`,
    `results/default_75_config_lock.json`,
    `results/v1_config_lock.json`, `results/v2_config_lock.json`,
    `results/v3_config_lock.json`) and is gated by
    `verify_number_provenance.py` unmodified.

    ### R1-M2 — Utility-oriented tests — strengthened by Plan 14-10

    The TSTR cross-model bars are now rendered at
    `results/figures/tstr_crossmodel.{png,pdf}` (source =
    `results/tstr.json`; negative R² plotted honestly per the
    companion JSON's `caption_note`). The per-model failure-mode diagnostic
    grid (distribution overlay × ACF lag-1 × log-return EMD across 9 models,
    ordered by ascending OD EMD) is rendered at
    `results/figures/failure_modes_summary.{png,pdf}` (source =
    `results/matched2000_dualscale.json` + per-model dist/acf
    companion JSONs). Both close the "utility-oriented" gap by visualizing
    the previously-unconsumed `tstr.json` and the cross-model fidelity
    structure.

    ### R2-1 / R1-M4 — Backend statement (analytic statevector vs shot noise) — strengthened by Plan 14-10

    The noise-model sensitivity (depolarizing + amplitude-damping channels,
    per-layer insertion) is rendered at
    `results/figures/noise_robustness_quantum.{png,pdf}` (source =
    `results/noise_model_sensitivity.json`). The shot-noise
    sensitivity (analytic-statevector baseline + finite-shot regimes) is
    rendered at `results/figures/shot_noise_robustness.{png,pdf}`
    (source = `results/shot_noise_sensitivity.json`). Both consume
    previously-unconsumed audited JSONs and make the analytic-vs-shot-noise
    backend statement empirically grounded.

    ### PAPER-01 / R2-1 — Parameter-matched comparison — strengthened by Plan 14-10

    The parameter-matched comparison hypothesis (PAPER-01) is rendered as a
    visual companion at `results/figures/param_efficiency_pareto.{png,pdf}`
    (source = `results/model_info.json` (n_params per model) +
    `results/matched2000_dualscale.json` (EMD mean ± std per scale)).
    The frozen-checkpoint headline (D-14-10) appears as a visually distinct
    marker; the iqp_sel_55_repro matched-budget reproduction and V1/V2/V3
    ansatz variants appear as separate points so the headline-vs-repro
    distinction is unambiguous on inspection.

    ### PAPER-08 / R1-m2 — Dataset details in Methods — strengthened by Plan 14-11

    The dataset paragraph in PAPER-08 (rendered from
    `results/model_info.json`) is now cross-referenced from the
    consolidated `docs/methods_full.md` §1 (Dataset), which also
    renders the model registry (§2), training protocol (§3), hardware &
    software (§4), reproducibility (§5), and the two documented
    contradictions (default_75 vs iqp_sel_55; dtype_params vs dtype_samples)
    explicitly addressed in §6 (Plan 14-11).

    ### Cross-cutting — Audited JSON corpus extension

    Plans 14-09 / 14-10 / 14-11 collectively added the following new audited
    JSONs to the `results/*.json` rglob corpus of
    `verify_number_provenance.py` (gate unmodified, D-14-16):
    `default_75_config_lock.json`, `v1_config_lock.json`, `v2_config_lock.json`,
    `v3_config_lock.json` (Plan 14-09); 7 figure companion JSONs (Plan 14-10);
    `classical_architectures.json`, `framework_versions.json`, `methods_full.json`
    (Plan 14-11). The full per-artifact manifest is at
    `docs/completeness_sweep_manifest.md` (this plan).
    ```

    NO edits to the existing Summary, Reviewer 1 Major Issues table, Reviewer
    1 Minor Issues table, Reviewer 2 Issues table, or the Cross-Cutting
    Provenance Note paragraph. The existing R1-M4 row in the Reviewer 1 Major
    Issues table is preserved verbatim — the new "R1-M4 RESOLVED" subsection
    in the appended Completeness sweep section is the audit closure
    (additive, not a rewrite).

    After writing, run the provenance gate against this doc and confirm PASS.
  </action>
  <verify>
    <automated>./qgan_env/bin/python verify_number_provenance.py --target docs/reviewer_response.md &amp;&amp; ./qgan_env/bin/python -c "import pathlib,sys; t=pathlib.Path('docs/reviewer_response.md').read_text(); needed=['Completeness sweep (Plans 14-09 .. 14-11)','R1-M4','RESOLVED','docs/methods_full.md','docs/circuit_atlas.md','training_convergence_all_models','tstr_crossmodel','failure_modes_summary','seed_variance_per_model','noise_robustness_quantum','shot_noise_robustness','param_efficiency_pareto','classical_architectures.json','framework_versions.json','methods_full.json','completeness_sweep_manifest.md','Plan 14-09','Plan 14-10','Plan 14-11']; missing=[n for n in needed if n not in t]; preserved=['Summary of Response','Reviewer 1 — Major Issues','Reviewer 1 — Minor Issues','Reviewer 2 — Issues','Cross-Cutting Provenance Note','R1-M1','R1-M2','R1-M3','R1-M4','R1-M5','R2-1','R2-2','R2-5a','R2-5b','R2-6','baseline_comparison.json','tstr.json','model_info.json']; lost=[p for p in preserved if p not in t]; print('missing',missing,'lost',lost); sys.exit(0 if not missing and not lost else 1)" &amp;&amp; [ -z "$(git diff --stat core/ verify_number_provenance.py)" ]</automated>
  </verify>
  <acceptance_criteria>
    - `docs/reviewer_response.md` PASSES `verify_number_provenance.py --target` unmodified
    - A new `## Completeness sweep (Plans 14-09 .. 14-11)` section is appended at the very bottom of the file
    - R1-M4 is explicitly marked RESOLVED with a citation to `docs/methods_full.md` (§3 Training + §4 Hardware & Software + §5 Reproducibility), and additionally to 14-10's `shot_noise_robustness`, `seed_variance_per_model`, and `training_convergence_all_models` figures for the shot-noise / multi-seed / convergence components
    - R2-5b is strengthened with a citation to 14-09's 5 circuit diagrams + `docs/circuit_atlas.md`
    - R1-M2 (utility tests) is strengthened with citations to 14-10's `tstr_crossmodel` + `failure_modes_summary`
    - R2-1 / R1-M4 (backend statement) is strengthened with citations to 14-10's `noise_robustness_quantum` + `shot_noise_robustness`
    - PAPER-01 / R2-1 (parameter-matched) is strengthened with a citation to 14-10's `param_efficiency_pareto`
    - PAPER-08 / R1-m2 (dataset details) is strengthened with a citation to `docs/methods_full.md` §1 + 14-11's new audited JSONs (classical_architectures.json, framework_versions.json, methods_full.json)
    - A Cross-cutting subsection lists every new audited JSON added to the `results/*.json` rglob corpus by these three plans
    - Existing Summary + Reviewer 1 Major + Reviewer 1 Minor + Reviewer 2 + Cross-Cutting Provenance Note are PRESERVED VERBATIM (grep confirms key reviewer IDs and existing artifact paths are still present)
    - `git diff --stat core/` is empty (D-14-22)
    - `git diff --stat verify_number_provenance.py` is empty (D-14-16)
  </acceptance_criteria>
  <done>reviewer_response.md gains an appended Completeness sweep section with R1-M4 explicitly marked RESOLVED via methods_full.md and 6 supporting figure citations; every existing reviewer-table row preserved verbatim; provenance gate passes unmodified.</done>
</task>

<task type="auto">
  <name>Task 4: Append (or skip) caveat paragraph to reconciliation_note.md (additive, no padding)</name>
  <files>docs/reconciliation_note.md</files>
  <read_first>
    - docs/reconciliation_note.md (the WHOLE existing file — a single `## EMD (OD scale)` section with the 1000ep→2000ep delta table for the 9 models; the table is preserved verbatim)
    - results/v1_config_lock.json (V1 ansatz config lock with param_count breakdown)
    - results/v2_config_lock.json (V2)
    - results/v3_config_lock.json (V3)
    - results/figures/param_efficiency_pareto.json (companion JSON for 14-10's parameter-efficiency Pareto figure; confirm it visualizes the headline-vs-repro distinction per D-14-10)
    - .planning/phases/14-paper-revision-release-freeze/14-09-PLAN.md (the plan that emitted v1/v2/v3 config locks)
    - .planning/phases/14-paper-revision-release-freeze/14-10-PLAN.md (the plan that emitted param_efficiency_pareto)
  </read_first>
  <action>
    Decide whether a caveat paragraph is materially warranted. The two candidate
    items are:

    (a) The V1/V2/V3 param_count breakdown rows (gate_layout_breakdown:
        "IQP encoding (5) + N*SEL layers (15 each) + final RX+RY (10) = P")
        now live in the three new config locks emitted by Plan 14-09. The
        reconciliation table already lists V1/V2/V3 EMD values with the "no
        1000ep matched-budget counterpart (ansatz variant introduced at
        2000ep, D-14-10)" footnote per row; the new config locks back the
        param_count claim (75 / 135 / 75) directly rather than only
        indirectly through `run_matched2000.py:118-122`.

    (b) The headline-vs-repro distinction (D-14-10 — iqp_sel_55_headline as
        the frozen-checkpoint headline EMD, iqp_sel_55_repro as the matched-
        2000ep reproduction) is now visualized as two distinct points in
        `results/figures/param_efficiency_pareto.{png,pdf}` (the
        headline as a separate dashed/diamond marker per the D-14-10 conflation
        guard).

    Both items are SUBSTANTIVE — they each strengthen an existing claim in
    the reconciliation table — so the append is warranted. Apply Option A
    (append). The append text (one short paragraph, at the very bottom of the
    file, AFTER the existing Interpretation paragraph):

    > **Integration caveat (Plan 14-12, recorded post-14-09/14-10).** Two
    > facets of the table above are now backed by additional audited
    > artifacts: (1) the V1/V2/V3 row param-count values (75 / 135 / 75)
    > now resolve directly to `results/v1_config_lock.json`,
    > `results/v2_config_lock.json`, and
    > `results/v3_config_lock.json` (Plan 14-09 —
    > `gate_layout_breakdown` field decomposes each count as IQP encoding (5)
    > + N\*SEL layers (15 each) + final RX+RY (10)), rather than only
    > indirectly through the `_QUANTUM_ANSATZ` dict at
    > `run_matched2000.py:118-122`; (2) the D-14-10 headline-vs-repro
    > distinction (iqp_sel_55_headline as the frozen-checkpoint EMD,
    > iqp_sel_55_repro as the matched-2000ep reproduction) is now visualized
    > as two distinct points in
    > `results/figures/param_efficiency_pareto.{png,pdf}` (Plan
    > 14-10 — the headline appears as a separate dashed/diamond marker per
    > the conflation-guard contract).

    Decision rule check: this paragraph is one substantive sentence in a
    single tight prose block; it adds two specific provenance pointers that
    were not previously cited from this doc. If a different reviewer of this
    task judges the items as NOT materially worth recording, the alternative
    is to skip the edit entirely and record `no append needed — V1/V2/V3
    param_count and headline-vs-repro distinction are already adequately
    cited via the existing table footnotes` in the commit body. Apply the
    append (Option A) per the analysis above.

    NO edits to the existing EMD table or Interpretation paragraph.

    After writing, run the provenance gate against this doc and confirm PASS.
    The new paragraph introduces the literal numbers 75 / 135 / 75 (these
    already appear in the table as `parameter_count` references) and 5 / 15 /
    10 / 4 / 8 (these resolve to the new config-lock JSONs via the existing
    `results/*.json` rglob).
  </action>
  <verify>
    <automated>./qgan_env/bin/python verify_number_provenance.py --target docs/reconciliation_note.md &amp;&amp; ./qgan_env/bin/python -c "import pathlib,sys; t=pathlib.Path('docs/reconciliation_note.md').read_text(); needed=['Integration caveat (Plan 14-12','v1_config_lock.json','v2_config_lock.json','v3_config_lock.json','param_efficiency_pareto','Plan 14-09','Plan 14-10','gate_layout_breakdown']; missing=[n for n in needed if n not in t]; preserved=['EMD (OD scale)','1000ep','2000ep','iqp_sel_55_repro','V1','V2','V3','wgan_mlp','wgan_cnn','wgan_lstm','data_hash','91e447d4624e25b3','0.154999','0.155376','0.156328','0.148114']; lost=[p for p in preserved if p not in t]; print('missing',missing,'lost',lost); sys.exit(0 if not missing and not lost else 1)" &amp;&amp; [ -z "$(git diff --stat core/ verify_number_provenance.py)" ]</automated>
  </verify>
  <acceptance_criteria>
    - `docs/reconciliation_note.md` PASSES `verify_number_provenance.py --target` unmodified
    - One appended caveat paragraph at the very bottom of the file (after the existing Interpretation paragraph) citing (a) v1/v2/v3_config_lock.json for V1/V2/V3 param_count breakdown and (b) param_efficiency_pareto for headline-vs-repro visualization
    - The existing EMD (OD scale) section header, the per-model delta table (every row), the data_hash declaration (91e447d4624e25b3), and the Interpretation paragraph are PRESERVED VERBATIM
    - The new paragraph's literal numbers (75 / 135 / 75 / 5 / 15 / 10 / 4 / 8 if any) resolve through the existing config-lock JSON corpus (gate-clean)
    - `git diff --stat core/` is empty (D-14-22)
    - `git diff --stat verify_number_provenance.py` is empty (D-14-16)
  </acceptance_criteria>
  <done>reconciliation_note.md gains one short caveat paragraph citing v1/v2/v3 config locks for the param_count breakdown and param_efficiency_pareto for the headline-vs-repro visualization; existing EMD table and Interpretation paragraph preserved verbatim; provenance gate passes unmodified.</done>
</task>

<task type="auto">
  <name>Task 5: End-to-end verify + write completeness_sweep_manifest.md</name>
  <files>docs/completeness_sweep_manifest.md</files>
  <read_first>
    - docs/paper_blocks_framing.md (post-Task-1; the four paper-facing docs are now in their final integrated state)
    - docs/paper_blocks_refs_methods.md (post-Task-2)
    - docs/reviewer_response.md (post-Task-3)
    - docs/reconciliation_note.md (post-Task-4)
    - docs/methods_full.md (byte-stable since 14-11)
    - docs/circuit_atlas.md (byte-stable since 14-09)
    - results/figures/circuits/ (the 5 PNG + 5 PDF + 5 companion JSON from 14-09 — confirm listing)
    - results/figures/ (the 7 new figures from 14-10 — confirm listing)
    - results/{v1,v2,v3,default_75}_config_lock.json (4 new locks from 14-09)
    - results/{classical_architectures,framework_versions,methods_full}.json (3 new audited JSONs from 14-11)
    - run_circuit_diagrams.py (14-09 emitter), run_figure_suite.py (14-10 extended emitter), run_classical_arch_extract.py + run_framework_versions.py + run_methods_full.py (14-11 emitters)
  </read_first>
  <action>
    Step A — End-to-end aggregate provenance verify (the integration gate).
    Run the provenance gate against all 6 paper-facing docs in sequence:

    ```bash
    for doc in \
        docs/paper_blocks_framing.md \
        docs/paper_blocks_refs_methods.md \
        docs/reviewer_response.md \
        docs/reconciliation_note.md \
        docs/methods_full.md \
        docs/circuit_atlas.md; do
      ./qgan_env/bin/python verify_number_provenance.py --target "$doc"
    done
    ```

    Capture the per-doc PASS lines (the gate prints `<doc>: PASS — <N>
    distinct numeric literal(s) all resolve to results/*.json
    (number-provenance gate).`). Every doc MUST exit 0.

    Also verify the gate itself is byte-stable (`git diff --stat
    verify_number_provenance.py` empty) and `core/` is
    byte-stable (`git diff --stat core/` empty — D-14-22).

    Step B — Write `docs/completeness_sweep_manifest.md`.
    A reviewer-facing copy-paste index. The document structure:

    ```
    # Completeness Sweep Manifest — Plans 14-09 / 14-10 / 14-11 / 14-12

    > Generated by Plan 14-12 as the final integration manifest before the
    > Zenodo deposit (Plan 14-07). Lists every new artifact added across
    > Plans 14-09 (circuit-architecture diagrams + config locks + atlas),
    > 14-10 (story-completeness figures), and 14-11 (paper-ready Methods
    > document + classical-architecture extraction + pinned framework
    > versions), plus the integration-only edits in Plan 14-12 itself
    > (paper-blocks cross-citations + this manifest).
    >
    > Every numeric literal in this manifest resolves to one of the audited
    > `results/*.json` artifacts via the rglob in
    > `verify_number_provenance.py` (unmodified).
    >
    > Final aggregate end-to-end provenance verify across all 6 paper-facing
    > docs: PASS (run the per-doc lines below to re-verify).
    ```

    Then 4 sections — one per plan:

    ## Plan 14-09 — Circuit-architecture diagrams + config locks + atlas
    | Artifact | Description | Provenance source |
    |---|---|---|
    | `run_circuit_diagrams.py` | Render-only circuit-diagram emitter (qml.draw_mpl style="pennylane") for all 5 production quantum circuits | `run_matched2000.py:118-122` (_QUANTUM_ANSATZ) + `core/__init__.py` + `core/models/quantum.py` |
    | `results/default_75_config_lock.json` | Config lock for the byte-frozen v1.0/v1.1 baseline (5q, 4L, range, 75p) | `core/__init__.py` + `core/models/quantum.py` (default_75 branch) |
    | `results/v1_config_lock.json` | Config lock for V1 matched-budget ansatz (5q, 4L, range, 75p) | `run_matched2000.py:118` |
    | `results/v2_config_lock.json` | Config lock for V2 matched-budget ansatz (5q, 8L, range, 135p) | `run_matched2000.py:120` |
    | `results/v3_config_lock.json` | Config lock for V3 matched-budget ansatz (5q, 4L, linear, 75p) | `run_matched2000.py:122` |
    | `results/figures/circuits/default_75.{png,pdf,json}` | Render-only architecture diagram for default_75 | `results/default_75_config_lock.json` |
    | `results/figures/circuits/iqp_sel_55.{png,pdf,json}` | Render-only architecture diagram for canonical paper circuit (frozen checkpoint epoch 1969) | `results/canonical_config_lock.json` |
    | `results/figures/circuits/V1.{png,pdf,json}` | Render-only architecture diagram for V1 | `results/v1_config_lock.json` |
    | `results/figures/circuits/V2.{png,pdf,json}` | Render-only architecture diagram for V2 | `results/v2_config_lock.json` |
    | `results/figures/circuits/V3.{png,pdf,json}` | Render-only architecture diagram for V3 | `results/v3_config_lock.json` |
    | `docs/circuit_atlas.md` | Copy-paste PAPER-03 architecture atlas (one section per circuit + cross-comparison + provenance footer) | All 5 config-lock JSONs above |

    ## Plan 14-10 — Story-completeness figure suite (7 new figures)
    | Artifact | Description | Provenance source |
    |---|---|---|
    | `run_figure_suite.py` (extended) | 7 new render-* functions wired into main() after the 14-08 dual-scale block | 14-04 / 14-08 render-only contract reused |
    | `results/figures/training_convergence_all_models.{png,pdf,json}` | 9-model EMD-vs-epoch trajectories with mean±std seed band + frozen-headline marker | 45 `matched2000/runs/<model>/<seed>/metrics.json` + `headline_canonical.json` |
    | `results/figures/tstr_crossmodel.{png,pdf,json}` | Cross-model TSTR grouped bars (R², MAE, RMSE) for Pipelines A and B | `results/tstr.json` |
    | `results/figures/failure_modes_summary.{png,pdf,json}` | Diagnostic grid: distribution overlay × ACF lag-1 × log-return EMD × 9 models | `results/matched2000_dualscale.json` + per-model dist/acf companion JSONs |
    | `results/figures/param_efficiency_pareto.{png,pdf,json}` | log10(params) × EMD scatter, OD vs log-return facets, frozen-headline diamond | `results/model_info.json` + `results/matched2000_dualscale.json` |
    | `results/figures/seed_variance_per_model.{png,pdf,json}` | 3×3 facet grid of per-seed EMD trajectories | 45 `matched2000/runs/<model>/<seed>/metrics.json` |
    | `results/figures/noise_robustness_quantum.{png,pdf,json}` | EMD vs noise level for depolarizing AND amplitude-damping channels | `results/noise_model_sensitivity.json` |
    | `results/figures/shot_noise_robustness.{png,pdf,json}` | EMD vs shot count (log-x) with analytic-statevector reference line | `results/shot_noise_sensitivity.json` |

    ## Plan 14-11 — Paper-ready Methods consolidation
    | Artifact | Description | Provenance source |
    |---|---|---|
    | `run_classical_arch_extract.py` | Pure-introspection emitter walking `named_modules()` on each classical class; drift-gate against `model_info.json` parameter_count | `core/models/classical.py` + `core/models/nonadversarial.py` (introspection only, no `.fit(`) |
    | `run_framework_versions.py` | Pure-introspection emitter via `importlib.metadata.version(...)` | Installed `qgan_env` interpreter (pennylane, torch, numpy, scipy, matplotlib, PyYAML) |
    | `run_methods_full.py` | Pure aggregator — consumes 11 audited JSONs + text-only file:line citations into byte-frozen `core/training.py`; cross-artifact `data_hash` gate | All inputs below |
    | `results/classical_architectures.json` | Per-model layer-tree JSON for the 5 classical baselines | `core/models/classical.py` + `core/models/nonadversarial.py` via `named_modules()` |
    | `results/framework_versions.json` | Pinned versions of the running `qgan_env` interpreter | `importlib.metadata.version(...)` |
    | `results/methods_full.json` | Paper-ready Methods aggregator JSON with 5 buckets (dataset / models / training / hardware-software / reproducibility) | `model_info.json` + 5 config-lock JSONs + `classical_architectures.json` + `framework_versions.json` + text-only greps of `core/training.py` |
    | `docs/methods_full.md` | Paper-ready Methods document (6 sections + provenance footer; addresses default_75 vs iqp_sel_55 and dtype_params vs dtype_samples contradictions explicitly in §6) | `methods_full.json` + 5 config-lock JSONs + `classical_architectures.json` + `framework_versions.json` + `model_info.json` |

    ## Plan 14-12 — Paper-blocks cross-citation + this manifest
    | Artifact | Description | Provenance source |
    |---|---|---|
    | `docs/paper_blocks_framing.md` (additive only) | PAPER-03 atlas-companion subsection + PAPER-01/02b param_efficiency_pareto footnote | All 14-09 + 14-10 (param_efficiency_pareto) artifacts above |
    | `docs/paper_blocks_refs_methods.md` (additive only) | PAPER-08 cross-reference to methods_full.md §1-§5 + PAPER-09 subsection citing 14-10's 6 figures | All 14-10 (except param_efficiency_pareto, cited from PAPER-01) + `docs/methods_full.md` |
    | `docs/reviewer_response.md` (additive only) | Completeness sweep section with R1-M4 RESOLVED via methods_full.md + 6 supporting figure citations | All 14-09 / 14-10 / 14-11 artifacts above |
    | `docs/reconciliation_note.md` (additive only) | One short caveat paragraph citing v1/v2/v3_config_lock.json + param_efficiency_pareto | 14-09 config locks + 14-10 param_efficiency_pareto |
    | `docs/completeness_sweep_manifest.md` (this file) | Reviewer-facing artifact manifest across all 4 plans | This document |

    ## End-to-end aggregate provenance verify (re-run command)

    ```
    for doc in \
        docs/paper_blocks_framing.md \
        docs/paper_blocks_refs_methods.md \
        docs/reviewer_response.md \
        docs/reconciliation_note.md \
        docs/methods_full.md \
        docs/circuit_atlas.md \
        docs/completeness_sweep_manifest.md; do
      ./qgan_env/bin/python verify_number_provenance.py --target "$doc"
    done
    ```

    Every line above must exit 0.

    Numeric-discipline check: every numeric literal in this manifest (5, 4, 3,
    8, 55, 75, 135, 1969, 9, 7, 11, 45) resolves through the existing
    `results/*.json` corpus (config locks for the architecture
    numbers; `model_info.json`/`matched2000_dualscale.json`/per-run
    `metrics.json` for the 9/45 model/run counts; the canonical lock for
    `1969`). Avoid introducing any numeric literal that does NOT resolve;
    when in doubt, phrase the count as prose (e.g. "the matched-budget
    9-model sweep" — `9` resolves through `model_info.json.models[].length`).

    After writing, run the gate against this manifest and against all 6 other
    paper-facing docs ONE MORE TIME (the aggregate re-verify) and confirm all
    7 exit 0. Include the manifest itself in the loop. Print the per-doc PASS
    lines into the manifest's "End-to-end aggregate" section as a verification
    log (Claude's discretion on inclusion — either append the per-doc PASS
    lines as a verbatim block, or summarize as "all 7 docs PASS" with the
    re-run command above for reproducibility).
  </action>
  <verify>
    <automated>set -e; for doc in docs/paper_blocks_framing.md docs/paper_blocks_refs_methods.md docs/reviewer_response.md docs/reconciliation_note.md docs/methods_full.md docs/circuit_atlas.md docs/completeness_sweep_manifest.md; do ./qgan_env/bin/python verify_number_provenance.py --target "$doc"; done &amp;&amp; ./qgan_env/bin/python -c "import pathlib,sys; t=pathlib.Path('docs/completeness_sweep_manifest.md').read_text(); needed=['Plan 14-09','Plan 14-10','Plan 14-11','Plan 14-12','run_circuit_diagrams.py','run_figure_suite.py','run_classical_arch_extract.py','run_framework_versions.py','run_methods_full.py','results/default_75_config_lock.json','results/v1_config_lock.json','results/v2_config_lock.json','results/v3_config_lock.json','results/figures/circuits/default_75','results/figures/circuits/iqp_sel_55','results/figures/circuits/V1','results/figures/circuits/V2','results/figures/circuits/V3','docs/circuit_atlas.md','results/figures/training_convergence_all_models','results/figures/tstr_crossmodel','results/figures/failure_modes_summary','results/figures/param_efficiency_pareto','results/figures/seed_variance_per_model','results/figures/noise_robustness_quantum','results/figures/shot_noise_robustness','results/classical_architectures.json','results/framework_versions.json','results/methods_full.json','docs/methods_full.md','docs/paper_blocks_framing.md','docs/paper_blocks_refs_methods.md','docs/reviewer_response.md','docs/reconciliation_note.md','docs/completeness_sweep_manifest.md']; missing=[n for n in needed if n not in t]; print('manifest_missing',missing); sys.exit(0 if not missing else 1)" &amp;&amp; [ -z "$(git diff --stat core/)" ] &amp;&amp; [ -z "$(git diff --stat verify_number_provenance.py)" ]</automated>
  </verify>
  <acceptance_criteria>
    - All 7 paper-facing docs (paper_blocks_framing.md, paper_blocks_refs_methods.md, reviewer_response.md, reconciliation_note.md, methods_full.md, circuit_atlas.md, completeness_sweep_manifest.md) exit 0 from `verify_number_provenance.py --target`
    - `docs/completeness_sweep_manifest.md` exists and PASSES the gate
    - The manifest enumerates EVERY artifact added across 14-09 / 14-10 / 14-11 / 14-12 in tabular form, with one-line description and provenance source path per row
    - The manifest's 4 plan-sections (14-09 / 14-10 / 14-11 / 14-12) each cover the complete artifact set from their owning plan (verified by grep against the artifact path list)
    - The manifest includes a copy-paste re-run command for the aggregate provenance verify
    - `git diff --stat core/` is empty (D-14-22 byte-freeze)
    - `git diff --stat verify_number_provenance.py` is empty (D-14-16, gate unmodified)
  </acceptance_criteria>
  <done>completeness_sweep_manifest.md written; aggregate end-to-end provenance verify across all 7 paper-facing docs PASSES; core/ and verify_number_provenance.py both byte-stable; integration of 14-09/14-10/14-11 into the paper-blocks corpus is complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| paper-blocks docs ↔ upstream artifacts (figures + JSONs) | a wrong citation (typo in a file path, wrong plan ID, wrong figure name) would put an unverifiable claim in the reviewer-facing paper-blocks corpus |
| reviewer_response.md ↔ R1-M4 resolution | a false claim that R1-M4 is RESOLVED would mislead the reviewer if methods_full.md does NOT in fact deliver the full Training Protocol |
| reconciliation_note.md ↔ skip-vs-append decision | spurious padding (an append paragraph that adds no information) would violate the no-padding rule; conversely a wrongful skip would lose a substantive provenance pointer |
| completeness_sweep_manifest.md ↔ artifact corpus | a missing artifact from the manifest table would leave a reviewer-facing index incomplete (a "completeness sweep" with a hole is worse than no sweep at all) |
| paper_blocks_framing.md / paper_blocks_refs_methods.md ↔ existing LaTeX AFTER blocks | a rewrite (rather than additive citation) of any locked LaTeX AFTER block would violate D-14-20 (PAPER-02 LOCKED) and the goal-backward "pure-additive" contract of this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-25 | Tampering | unverifiable claim in a paper-blocks doc (hand-typed numeric literal that bypasses the rglob corpus) | mitigate | Every task's verify gate runs `verify_number_provenance.py --target <doc>` and must exit 0; the gate is `verify_number_provenance.py` (unmodified, D-14-16) |
| T-14-26 | Tampering | rewrite of a locked LaTeX AFTER block (PAPER-02 LOCKED, D-14-20) | mitigate | Tasks 1+2 verify gates grep for signature phrases from every existing AFTER block ("Reframed central question", "De-overclaiming", "LOCKED", "Render-from-JSON contract", "0.1209437521974767" etc.) — any disappearance fails the task |
| T-14-27 | Spoofing | false claim in reviewer_response.md that R1-M4 is RESOLVED | mitigate | Task 3 grep gate confirms `docs/methods_full.md` is cited adjacent to "R1-M4 RESOLVED"; methods_full.md is byte-stable from Plan 14-11 and is independently audited (it passed the provenance gate when 14-11 closed) |
| T-14-28 | Information Disclosure | missing artifact in completeness_sweep_manifest.md | mitigate | Task 5 grep gate enumerates every expected artifact path (5 circuit PNGs + 5 config locks + atlas + 7 story figures + 3 audited JSONs + 1 methods doc + 5 emitter scripts + 4 paper-blocks docs + this manifest) and fails if any is missing |
| T-14-29 | Tampering | inadvertent `core/` edit slips in during a docs-only task (would break D-14-22 byte-freeze) | mitigate | Every task's verify gate asserts `[ -z "$(git diff --stat core/)" ]` and `[ -z "$(git diff --stat verify_number_provenance.py)" ]` |
| T-14-30 | Tampering | spurious padding paragraph in reconciliation_note.md (a no-information append violating the no-padding rule) | mitigate | Task 4 explicit decision rule: only append when there is a substantive provenance pointer worth recording; if neither candidate item is materially worth recording, log `no append needed` in the commit body and skip the edit. The current candidate items (V1/V2/V3 param_count backed by config locks; headline-vs-repro visualized in param_efficiency_pareto) ARE substantive — Task 4 appends — but the alternative-skip branch is documented in the action |
| T-14-31 | Spoofing | wrong file-path citation (typo) in any of the 4 paper-blocks docs | mitigate | Each task's verify gate cross-checks the literal file-path tokens (e.g. `results/figures/circuits/default_75`) against the actual on-disk layout via Python `pathlib` checks; a typo'd path would NOT match the expected `needed=[...]` list and would fail the task |
</threat_model>

<verification>
- `docs/paper_blocks_framing.md` PASSES `verify_number_provenance.py --target` unmodified, gains a PAPER-03 atlas-companion subsection citing 5 circuit diagrams + circuit_atlas.md + 5 config locks, and a PAPER-01 (or PAPER-02b) footnote citing param_efficiency_pareto; every existing AFTER LaTeX block preserved verbatim.
- `docs/paper_blocks_refs_methods.md` PASSES the gate unmodified, gains a PAPER-08 cross-reference paragraph pointing at methods_full.md §1-§5, and a PAPER-09 subsection citing 14-10's 6 figures; existing dual-scale table preserved verbatim.
- `docs/reviewer_response.md` PASSES the gate unmodified, gains an appended Completeness sweep section with R1-M4 explicitly marked RESOLVED via methods_full.md and 6 supporting figure citations; existing reviewer-table rows preserved verbatim.
- `docs/reconciliation_note.md` PASSES the gate unmodified, gains one short caveat paragraph citing v1/v2/v3 config locks for V1/V2/V3 param_count and param_efficiency_pareto for the headline-vs-repro visualization; existing EMD table preserved verbatim.
- `docs/completeness_sweep_manifest.md` exists, PASSES the gate unmodified, enumerates every artifact added across 14-09 / 14-10 / 14-11 / 14-12 with one-line descriptions + plan IDs + provenance source paths.
- Final aggregate end-to-end provenance verify across all 7 paper-facing docs (the 4 modified + the 2 byte-stable upstream docs + this manifest) all exit 0.
- `core/` byte-freeze (D-14-22) preserved across all 5 tasks.
- `verify_number_provenance.py` UNMODIFIED (D-14-16) across all 5 tasks.
- After this plan completes, only 14-07 (Zenodo deposit + tag + DOI wiring) remains.
</verification>

<success_criteria>
The 14-09 / 14-10 / 14-11 artifact corpus is fully threaded into the four
paper-facing documents (paper_blocks_framing, paper_blocks_refs_methods,
reviewer_response, reconciliation_note) via pure-additive citations only — no
existing LaTeX AFTER block, locked reviewer-table row, dataset paragraph, or
dual-scale evaluation table is rewritten. PAPER-03 gains the visual atlas
companion (5 diagrams + circuit_atlas.md). PAPER-01 gains the param-efficiency
Pareto visual companion. PAPER-08 cross-references the consolidated Methods
doc. PAPER-09 gains the 6 story-completeness figure citations at their
specific supporting claims. reviewer_response.md explicitly marks R1-M4
(full Training Protocol) RESOLVED via methods_full.md. reconciliation_note.md
gains a substantive caveat paragraph (or explicitly skips per the no-padding
rule). A reviewer-facing completeness_sweep_manifest.md enumerates every
artifact added across the three upstream plans. The final aggregate end-to-end
provenance verify across all 7 paper-facing docs passes with the gate
unmodified, `core/` byte-frozen, and no retraining / no new figures /
no new JSONs introduced by this plan. After this plan completes, only the
Zenodo deposit gate (14-07) remains.
</success_criteria>

<output>
Create `.planning/phases/14-paper-revision-release-freeze/14-12-SUMMARY.md` when done
</output>
