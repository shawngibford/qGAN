---
phase: 14-paper-revision-release-freeze
plan: 03
type: execute
wave: 3
depends_on: ["14-02"]
files_modified:
  - run_model_info.py
  - results/model_info.json
  - docs/training_protocol.md
  - docs/dataset_stats.md
  - docs/reconciliation_note.md
  - verify_number_provenance.py
autonomous: true
requirements: [PAPER-08]
must_haves:
  truths:
    - "One unified model_info.json contains every model as a row (55-param IQP:SEL, V1/V2/V3, wgan_mlp/cnn/lstm, VAE, AR) with config + device/dtype from the 2000ep artifacts"
    - "training_protocol.md and dataset_stats.md are regenerated FROM model_info.json (no hand-typed numbers)"
    - "A reconciliation note records the 1000ep→2000ep metric deltas"
    - "A number-provenance gate script asserts a numeric literal resolves to a results/*.json value at stated precision"
  artifacts:
    - path: "run_model_info.py"
      provides: "pure-aggregator JSON emitter (no torch/pennylane/core import)"
      min_lines: 60
    - path: "results/model_info.json"
      provides: "unified per-model long-form table source + data_hash"
    - path: "docs/reconciliation_note.md"
      provides: "1000ep→2000ep metric delta record"
    - path: "verify_number_provenance.py"
      provides: "grep-able number-provenance gate (explicit-raise)"
      min_lines: 40
  key_links:
    - from: "run_model_info.py"
      to: "results/*.json (2000ep artifacts)"
      via: "cross-artifact data_hash equality gate (explicit-raise)"
      pattern: "data_hash"
    - from: "docs/training_protocol.md"
      to: "results/model_info.json"
      via: "JSON->markdown render (no hand-typed numbers)"
      pattern: "model_info"
    - from: "verify_number_provenance.py"
      to: "results/*.json"
      via: "numeric-literal resolution at stated precision"
      pattern: "AssertionError"
---

<objective>
Build `run_model_info.py` — the pure-aggregator emitter that produces
the single unified `model_info.json` (every model a row, D-14-15) from the
accepted 2000ep artifacts — then regenerate `training_protocol.md` and
`dataset_stats.md` FROM that JSON (D-14-16, no hand-typed numbers), write the
1000ep→2000ep `reconciliation_note.md` (D-14-13), and deliver the reusable
number-provenance gate script (RESEARCH Pattern 3) that downstream LaTeX-block
plans are verified against. Provides the PAPER-08 dataset details source.

Purpose: Success criterion 5 ("all numbers trace to a JSON artifact") must be
an executable gate, and the model-info table + docs must render from JSON so
they stop drifting from artifacts.

Output: `model_info.json`, regenerated provenance docs, reconciliation note,
and a reusable number-provenance verifier.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-paper-revision-release-freeze/14-CONTEXT.md
@.planning/phases/14-paper-revision-release-freeze/14-RESEARCH.md
@.planning/phases/14-paper-revision-release-freeze/14-PATTERNS.md
@.planning/phases/14-paper-revision-release-freeze/14-02-SUMMARY.md

<interfaces>
<!-- Pure-aggregator + JSON->markdown render idioms — copy directly -->

run_multiseed_rollup.py — the canonical pure-aggregator template:
- Repo-root resolver + RESULTS anchor (lines 42-59): walk up to `core/preprocessing.py`; `RESULTS = REPO / "revision/results"`.
- Cross-artifact data_hash gate (lines 85-92): assert ONLY mutual equality of frozen `data_hash` fields (do NOT re-derive); explicit-raise (`raise AssertionError`), not bare assert.
- Output write idiom (lines 176-187): `out = {"schema": ..., "data_hash": canonical_hash, "consumed_artifacts": {...}, ...}`; `write_text(json.dumps(out, indent=2))`; print row count + data_hash.
- NO torch/pennylane/core import — pure JSON consumer.

Long-form schema (results/baseline_comparison.json): top-level keys `schema`, `model_kinds`, `pipelines`, `seeds`, `data_hash`, `data_hash_verification`, `metric_helpers`, `models`, `rows`. Per-models[] record: `{ "kind", "parameter_count", "family", "train_protocol_notes" }`. Header literals: `"schema": "long-form rows[] + models[] aggregate (D-10-16)"`, `"metric_helpers": "revision.core.eval ONLY (D-10-20)"`. For Phase-14 the 55-param IQP:SEL row replaces the 75-param quantum row (D-14-04); add `device`/`dtype` fields from the manifest (D-14-12).

JSON→markdown render (_build_baseline_notebook.py:550-593): `_agg()` computes mean/std straight off `comparison["rows"]` — no number ever typed; emits pipe-table lines with a `data_hash` provenance callout. Current `training_protocol.md:1-26` layout: "Source of truth" callout + `| Constant | Value | Source |` tables — preserve layout, drive Value/Source from `model_info.json`.

Number-provenance gate (RESEARCH Pattern 3): extract numeric tokens from a target text; for each, assert it appears at stated precision in some `results/*.json`; explicit-raise (run_multiseed_rollup.py:86-92) so `python -O` can't disable it.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: run_model_info.py emitter + reconciliation note</name>
  <read_first>
    - run_multiseed_rollup.py (pure-aggregator template: resolver 42-59, data_hash gate 85-92, output idiom 176-187 — copy these)
    - results/baseline_comparison.json (long-form schema + models[] record shape + header literals to conform to)
    - results/canonical_config_lock.json (Plan 01 locked 55-param decomposition + checkpoint optimizer breadcrumbs for the quantum row)
    - results/headline_canonical.json + results/matched2000/ accepted artifacts (Plan 02 — the 2000ep source rows + device/dtype manifests)
    - results/baselines/sweep_status.json (prior 1000ep artifacts for the reconciliation delta basis)
    - .planning/phases/14-paper-revision-release-freeze/14-CONTEXT.md (D-14-15/16 unified table; D-14-13 reconciliation requirement)
  </read_first>
  <action>
    Create `run_model_info.py` using the `run_multiseed_rollup.py`
    pure-aggregator template (repo-root resolver lines 42-59, cross-artifact
    `data_hash` explicit-raise gate lines 85-92, output write idiom 176-187).
    NO torch/pennylane/core import — read only the accepted 2000ep result
    JSONs (`headline_canonical.json` + the `results/matched2000/`
    bundle). For each model emit one `models[]` record conforming to the
    `baseline_comparison.json` shape plus a `device`/`dtype` field from the
    artifact's manifest and the columns from D-14-15 (params, epochs=2000,
    early-stop state, optimizer/LR/betas — quantum LR/betas from the
    checkpoint optimizer breadcrumbs in `canonical_config_lock.json` — batch,
    N_CRITIC, λ, seeds {42..46}, window config, data_hash, wall-time). The
    55-param IQP:SEL row replaces the 75-param quantum row (D-14-04); the
    frozen-checkpoint headline and the 2000ep reproduction are SEPARATE rows
    (D-14-10). Assert mutual `data_hash` equality across every consumed
    artifact with the explicit-raise idiom. Write
    `results/model_info.json` (long-form schema header literals,
    `consumed_artifacts` map). Then write
    `docs/reconciliation_note.md` recording, per model/metric, the
    1000ep→2000ep delta (old value from the prior `baselines`/`ansatz` 1000ep
    artifacts, new value from the 2000ep artifact) — this is the authoritative
    source for any manuscript number that changed (D-14-13, Pitfall 5).
  </action>
  <verify>
    <automated>./qgan_env/bin/python run_model_info.py && ./qgan_env/bin/python -c "import json,sys; d=json.load(open('results/model_info.json')); ks={m['kind'] for m in d['models']}; req={'wgan_mlp','wgan_cnn','wgan_lstm','vae','ar'}; sys.exit(0 if 'data_hash' in d and req<=ks and all('device' in m for m in d['models']) else 1)" && test -f docs/reconciliation_note.md && ! grep -rn 'import torch\|import pennylane\|revision.core' run_model_info.py</automated>
  </verify>
  <acceptance_criteria>
    - `results/model_info.json` exists with header literal `"schema": "long-form rows[] + models[] aggregate (D-10-16)"`, a `data_hash`, and one `models[]` record per model including `wgan_mlp/cnn/lstm`, `vae`, `ar`, and the 55-param IQP:SEL
    - Every `models[]` record has a `device` and `dtype` field; `epochs` == 2000
    - The cross-artifact `data_hash` check uses `raise AssertionError(` (grep) — not a bare assert
    - `run_model_info.py` contains no `import torch`, `import pennylane`, or `revision.core` import (pure aggregator)
    - The frozen-checkpoint headline and the 2000ep reproduction appear as two distinct rows/records (grep for distinct `source` markers)
    - `docs/reconciliation_note.md` exists and contains a per-metric old(1000ep)→new(2000ep) delta table
  </acceptance_criteria>
  <done>Unified model_info.json emitted from 2000ep artifacts behind the data_hash gate; reconciliation note records all 1000ep→2000ep deltas.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate provenance docs from JSON + number-provenance gate</name>
  <read_first>
    - _build_baseline_notebook.py (lines 550-593 — the JSON→markdown render idiom with _agg() and data_hash callout to copy)
    - docs/training_protocol.md (current hand-maintained layout lines 1-26: "Source of truth" callout + `| Constant | Value | Source |` tables — layout to preserve, values to drive from JSON)
    - docs/dataset_stats.md (current hand-maintained dataset doc — same regeneration treatment)
    - results/model_info.json (Task 1 output — the sole numeric source for the regenerated docs)
    - run_multiseed_rollup.py (lines 86-92 explicit-raise gate idiom for the provenance verifier)
    - .planning/phases/14-paper-revision-release-freeze/14-RESEARCH.md (Pattern 3 number-provenance grep-gate; Pitfall 5 hand-typed-number leak)
  </read_first>
  <action>
    Add a markdown-render path (either to `run_model_info.py` or a
    small `revision/render_docs.py` invoked by it) that regenerates
    `docs/training_protocol.md` and `docs/dataset_stats.md`
    ENTIRELY from `results/model_info.json` using the
    `_build_baseline_notebook.py:550-593` `_agg()`-style render (every cell
    value pulled from the JSON, never typed; preserve the existing "Source of
    truth" callout + `| Constant | Value | Source |` table layout but the
    Value/Source columns now cite `model_info.json` provenance instead of
    hand-typed `core/__init__.py:NN` lines). Then create
    `verify_number_provenance.py` (RESEARCH Pattern 3): given a target
    text file (a regenerated doc or, later, a PAPER LaTeX-blocks file passed as
    `--target`), extract every numeric literal and, for each, assert it
    resolves to a value present at stated precision in some
    `results/*.json`; on any unresolved literal use the explicit-raise
    idiom (`raise AssertionError`, run_multiseed_rollup.py:86-92) and exit
    non-zero. Self-check: run the verifier against the two regenerated docs —
    they must pass (proving the no-hand-typed-numbers contract). This script is
    the reusable gate Plans 05 and 06 are verified against.
  </action>
  <verify>
    <automated>./qgan_env/bin/python verify_number_provenance.py --target docs/training_protocol.md && ./qgan_env/bin/python verify_number_provenance.py --target docs/dataset_stats.md && grep -q 'model_info.json' docs/training_protocol.md && grep -q 'AssertionError' verify_number_provenance.py</automated>
  </verify>
  <acceptance_criteria>
    - `verify_number_provenance.py --target docs/training_protocol.md` exits 0; same for `dataset_stats.md`
    - Both regenerated docs contain a `model_info.json` provenance reference (grep) and preserve the `| Constant | Value | Source |` table layout
    - `verify_number_provenance.py` uses `raise AssertionError(` for unresolved numeric literals (grep) and exits non-zero on failure (verifiable by pointing `--target` at a doc with an injected fake number in a scratch copy — must exit non-zero)
    - The regenerated docs are produced by a render path that reads `model_info.json`; no numeric literal in them is hand-typed (verifier proves this)
  </acceptance_criteria>
  <done>Provenance docs regenerated from model_info.json; reusable number-provenance gate built and self-verified against the docs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 2000ep artifacts → model_info.json | a mismatched-hash artifact would put incoherent numbers into the paper table |
| model_info.json → regenerated docs | a hand-typed number bypassing the renderer breaks success-criterion 5 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-07 | Tampering | model_info.json provenance | mitigate | Cross-artifact `data_hash` explicit-raise equality gate over every consumed 2000ep artifact (run_multiseed_rollup.py:86-92 idiom) |
| T-14-08 | Tampering | hand-typed number leak into docs/paper | mitigate | Docs render FROM model_info.json only; `verify_number_provenance.py` explicit-raise gate is the executable success-criterion-5 enforcement |
| T-14-09 | Information disclosure | optimizer LR/betas breadcrumbs | accept | training hyperparameters, not secrets; intended for the paper Methods section (PAPER-08) |
</threat_model>

<verification>
- `model_info.json` present, schema-conformant, one record per model, device/dtype + epochs=2000, data_hash gate explicit-raise
- `reconciliation_note.md` records 1000ep→2000ep deltas
- Regenerated `training_protocol.md` / `dataset_stats.md` pass the number-provenance gate
- `verify_number_provenance.py` explicit-raise + non-zero exit on unresolved literals
</verification>

<success_criteria>
The unified model-info table source exists, the provenance docs render from it
with zero hand-typed numbers, the 1000ep→2000ep reconciliation is recorded, and
a reusable executable number-provenance gate enforces success-criterion 5 for
all downstream LaTeX-block plans.
</success_criteria>

<output>
Create `.planning/phases/14-paper-revision-release-freeze/14-03-SUMMARY.md` when done
</output>
