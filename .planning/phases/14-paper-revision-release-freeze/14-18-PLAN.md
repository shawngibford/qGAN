---
phase: 14-paper-revision-release-freeze
plan: 18
type: execute
wave: 15
depends_on: []
files_modified:
  - docs/reviewer_response.md
  - docs/methods_full.md
  - results/welch_pairwise.json
gap_closure: true
autonomous: true
requirements: [PAPER-01, PAPER-02]
must_haves:
  truths:
    - "The word 'equivalent'/'equivalence' is no longer used for the OD-EMD claim; it is reframed as 'no statistically detectable difference at n=5 (underpowered)'"
    - "The OD-DTW Orlandi-improvement claim is framed as matched-budget-wide, not quantum-specific; only LR-DTW is stated as quantum-distinguishing"
    - "The multiple-comparisons posture for the LR-DTW pairwise family is stated explicitly and reconciled across the EMD/DTW pairwise families (SYNTHESIS H1)"
    - "The wgan_cnn seed-42 outlier dependence and the n=5 power limitation are disclosed at the claim site"
    - "verify_number_provenance.py still PASSES on every edited paper-facing doc"
  artifacts:
    - path: "docs/reviewer_response.md"
      provides: "Recalibrated OD-EMD and OD-DTW claim wording with n=5 power + outlier disclosure + LR-DTW multiple-comparisons posture"
      contains: "underpowered"
    - path: "results/welch_pairwise.json"
      provides: "A notes field clarifying the non-equivalence interpretation of the OD-EMD result"
      contains: "not an equivalence claim"
  key_links:
    - from: "docs/reviewer_response.md"
      to: "docs/methods_full.md"
      via: "OD-DTW claim aligned to the honest matched-budget-wide framing already in methods_full.md:436-442"
      pattern: "matched-budget|non-significant"
    - from: "results/welch_pairwise.json notes"
      to: "the OD-EMD claim interpretation"
      via: "machine-readable note: non-significant difference test, not an equivalence claim"
      pattern: "underpowered|not an equivalence"
---

<objective>
Recalibrate the surviving OD-EMD and OD-DTW claim wording so it is statistically
defensible before the irreversible Zenodo freeze. The peer-review r4 math
reviewer (Agent 1, CRITICAL B1) and claims reviewer (Agent 4, MEDIUM F1) both
found that the OD-EMD "parametric-efficiency equivalence" claim equates a
non-significant difference test with positive evidence of equivalence — the
"absence of evidence is not evidence of absence" fallacy — and that a proper
TOST equivalence test fails 0/20 pairs. The math reviewer (Agent 1, B2) also
found the multiple-comparisons posture inconsistent across pairwise families
and unstated for the surviving LR-DTW claim. This plan closes SYNTHESIS findings
C1, H1, M1, M2, M3, M5.

Purpose: `welch_pairwise.json` is the cited anchor for a "strong claim" about to
be frozen under a permanent DOI. The claim must be reworded so it states what
the data support — no statistically detectable difference at an underpowered
n=5 — not equivalence, and the multiple-comparisons posture for every surviving
pairwise-family claim must be stated explicitly. This is a wording-only fix.

Output: Recalibrated claim text in `docs/reviewer_response.md` and
`docs/methods_full.md`, an explicit multiple-comparisons posture for
the LR-DTW pairwise family, and a clarifying `notes` field in
`results/welch_pairwise.json`. The downstream consumer is plan 14-07,
whose freeze-ready gate runs the number-provenance gate over these docs — they
must still pass it. 14-07 MUST NOT run until this plan completes.

CONSTRAINT (D-11-10): no recomputation, no retraining, no metric re-evaluation —
every number is already certified. This plan changes only the *interpretation
wording* around the existing numbers. The numeric literals themselves
(p > 0.36, |d| ≤ 0.65, n=5, the per-pair table values) are unchanged so the
number-provenance gate continues to resolve them.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-paper-revision-release-freeze/peer-review-r4/SYNTHESIS.md
@.planning/phases/14-paper-revision-release-freeze/peer-review-r4/math-stats-review.md
@.planning/phases/14-paper-revision-release-freeze/peer-review-r4/claims-integrity-review.md
@docs/reviewer_response.md
@docs/methods_full.md

<interfaces>
<!-- Claim sites and the honest framing already present, verified this session -->

The OD-EMD "equivalence" claim lives at `docs/reviewer_response.md`
under "## Parametric-efficiency equivalence (post-r3 corrected metrics)"
(section header ~line 260; claim sentence ~269-272; supporting paragraphs
~283-323; per-pair table ~298-312) and at `docs/methods_full.md` ~398.

The OD-DTW "6.5x vs Orlandi ... temporal-structure capture quantum is
specifically designed for" claim is at `reviewer_response.md` ~278-281 and the
DTW summary ~354-364.

The honest framing to MIRROR already exists in `methods_full.md`:
- ~441-442: DTW OD-scale ordering "statistically non-significant under the
  strict-accept gate; no equivalence test is computed" — copy this register.
- ~436-442: OD-DTW is "mixed with the classical generator cluster" — the
  matched-budget-wide framing the reviewer_response.md OD-DTW claim must align to.

Key facts from the r4 math review (Agent 1) — use these verbatim in the disclosure:
- n=5/group two-sample Welch t-test power: ~15% against d=0.65; the 80%-power
  floor is d ≈ 2.0 (the minimum effect detectable at 80% power, n=5/group).
- Proper TOST equivalence test fails 0/20 pairs at any defensible margin.
- The wgan_cnn OD-EMD: seed 42 = 0.1587 vs the other four seeds 0.020-0.034
  (~5x outlier); this single seed sets BOTH `strong_claim_thresholds` extrema —
  the p-floor (0.3652) and the |d|-ceiling (0.6442).
- OD-DTW: wgan_lstm (0.301) and wgan_mlp (0.302) achieve the same OD-DTW as the
  quantum cluster (0.298-0.302) — the ~6.5x-vs-Orlandi improvement is shared by
  two classical baselines, NOT quantum-specific. Only LR-DTW distinguishes
  quantum (every quantum variant 0.94-1.12 < every WGAN 1.58-6.86).

Multiple-comparisons posture (Agent 1, B2 — SYNTHESIS H1): the OD-EMD claim is
evaluated over a 20-pair family (5 classical generators x 4 quantum variants, or
the equivalent pairing) and the surviving LR-DTW dominance claim is evaluated
over its own pairwise family (every quantum variant vs every WGAN+AR baseline).
The reviewer found the multiple-comparisons treatment is stated for one family
but not the other, and not reconciled. The LR-DTW claim is a *uniform-dominance*
claim (every quantum beats every classical) — uniform dominance over a finite
family does not require a multiplicity correction in the same way a "≥1
significant pair" claim does, because the claim is the conjunction over all
pairs, not the disjunction; the relevant posture is to state that the LR-DTW
claim holds for *every* pair in the family with no correction needed for a
conjunctive claim, while the OD-EMD non-significance result is reported without
a positive-equivalence inference (so multiplicity does not inflate a false
claim). This plan must state that posture explicitly and consistently in
`reviewer_response.md` — do not leave H1 silently uncovered.

`welch_pairwise.json` is a structured JSON; its `notes` field (~line 683 per
Agent 4) already carries a Path A retraction record — add the OD-EMD
clarification as an additional `notes` entry without disturbing existing keys
or any numeric value.

NO `.tex` file is touched by this plan (that is plan 14-17's job).
`run_welch_aggregator.py` is NOT edited — only the emitted JSON's `notes` field
is hand-edited (a documentation field, not a computed value).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reframe the OD-EMD equivalence claim in reviewer_response.md and methods_full.md</name>
  <read_first>
    - docs/reviewer_response.md (the "Parametric-efficiency equivalence" section ~260-323 — every use of "equivalent"/"equivalence" for the OD-EMD claim)
    - docs/methods_full.md (~398-399 the OD-EMD equivalence sentence; ~441-442 the honest DTW non-significance framing to mirror)
    - .planning/phases/14-paper-revision-release-freeze/peer-review-r4/math-stats-review.md (Agent 1 B1 — the exact recommended reframe wording and the power-analysis numbers)
    - .planning/phases/14-paper-revision-release-freeze/peer-review-r4/claims-integrity-review.md (Agent 4 F1 — "statistically indistinguishable (n=5)" as the calibrated phrasing)
  </read_first>
  <action>
    In `docs/reviewer_response.md` and `docs/methods_full.md`,
    replace every use of "equivalent" / "equivalence" / "statistically
    equivalent" as applied to the OD-EMD parametric-efficiency claim with the
    calibrated reframe: "no statistically detectable OD-EMD difference at n=5
    (underpowered — the two-sample Welch test has ~15% power against d=0.65 and
    an 80%-power detection floor of d ≈ 2.0; this is not an equivalence claim
    and a TOST equivalence test is not satisfied)." Mirror the register
    methods_full.md:441-442 already uses for DTW ("statistically non-significant
    ... no equivalence test is computed"). Also rename the
    `reviewer_response.md` section header "## Parametric-efficiency equivalence
    (post-r3 corrected metrics)" so it no longer asserts equivalence (e.g.
    "## Parametric efficiency: no detectable OD-EMD difference at matched budget
    (n=5, underpowered)"). Do NOT change any numeric literal — p > 0.36,
    |d| ≤ 0.65, n=5, the per-pair table values, the parameter counts (55,
    73-562) all stay byte-identical so the number-provenance gate keeps
    resolving them. The claim's *direction* (the 55-param quantum generator's
    OD-EMD is not statistically distinguishable from the size-matched classical
    generators) is retained — only the word "equivalent" and the unsupported
    positive-equivalence framing are removed. Disclose the n=5 power limitation
    (SYNTHESIS M3) in the same paragraph as the claim, not in a distant
    footnote.
  </action>
  <verify>
    <automated>! grep -niE 'statistically equivalent|parametric-efficiency equivalence' docs/reviewer_response.md docs/methods_full.md && grep -qi 'underpowered' docs/reviewer_response.md && grep -qi 'not an equivalence claim' docs/reviewer_response.md && grep -qi 'underpowered' docs/methods_full.md && echo EMD_REFRAME_OK</automated>
  </verify>
  <acceptance_criteria>
    - No occurrence of "statistically equivalent" or "parametric-efficiency equivalence" applied to the OD-EMD claim survives in either doc
    - The claim is reworded to "no statistically detectable OD-EMD difference at n=5", with the explicit power disclosure (~15% power vs d=0.65, 80%-power floor d ≈ 2.0) in the same paragraph
    - The `reviewer_response.md` section header no longer asserts "equivalence"
    - Every numeric literal (p > 0.36, |d| ≤ 0.65, n=5, per-pair table values, parameter counts) is byte-unchanged
    - The wording mirrors the honest register already used at `methods_full.md:441-442` for DTW
  </acceptance_criteria>
  <done>The OD-EMD claim is reframed as a non-significant difference at an underpowered n=5 in both docs, with the n=5 power limitation disclosed at the claim site; no number changed.</done>
</task>

<task type="auto">
  <name>Task 2: Align the OD-DTW Orlandi claim, state the LR-DTW multiple-comparisons posture, disclose the wgan_cnn outlier dependence</name>
  <read_first>
    - docs/reviewer_response.md (~278-281 the OD-DTW "6.5x ... specifically designed for" claim; ~354-364 the DTW summary; ~298-312 the per-pair OD-EMD table where the wgan_cnn row appears)
    - docs/methods_full.md (~436-442 the honest OD-DTW "mixed with the classical generator cluster" framing; ~451-457 the Orlandi reconciliation)
    - .planning/phases/14-paper-revision-release-freeze/peer-review-r4/math-stats-review.md (Agent 1 B2 multiple-comparisons posture, B3 OD-DTW overclaim, B4 wgan_cnn seed-42 outlier)
  </read_first>
  <action>
    Three edits to `docs/reviewer_response.md`.
    (1) SYNTHESIS M1 — align the OD-DTW claim at ~278-281 to the honest
    `methods_full.md:436-442` framing. The current text says the ~6.5x-vs-Orlandi
    OD-DTW improvement is "the temporal-structure capture quantum is
    specifically designed for". Reword: state that the ~6.5x OD-DTW improvement
    over the Orlandi reference (1.954) is achieved by the matched-budget cluster
    *as a whole* — wgan_lstm (0.301) and wgan_mlp (0.302) sit inside the same
    0.298-0.302 cluster as the quantum variants, so the OD-DTW Orlandi
    improvement is matched-budget-wide and is NOT quantum-specific. State
    explicitly that the quantum-distinguishing DTW result is the LR-DTW one
    (every quantum variant beats every WGAN+AR on log-return DTW) — that is the
    only DTW claim scoped to quantum. Drop "specifically designed for" framing.
    (2) SYNTHESIS H1 — state the multiple-comparisons posture for the surviving
    LR-DTW pairwise family and reconcile it across the EMD/DTW families. Add a
    sentence (near the LR-DTW claim and/or in the methods/statistics-posture
    paragraph) stating: the LR-DTW dominance claim is a *uniform-dominance*
    (conjunctive) claim — every quantum variant beats every WGAN+AR baseline on
    log-return DTW — and a conjunctive "holds for every pair" claim over a
    finite pairwise family does not require a multiplicity correction (unlike a
    disjunctive "≥1 significant pair" claim, where multiplicity inflates the
    false-positive rate); the claim is reported as the worst-case margin over
    the family (the smallest quantum-minus-classical gap). State that the OD-EMD
    non-significance result is correspondingly reported WITHOUT a
    positive-equivalence inference, so no multiplicity correction is needed there
    either (a high p-value is not asserted as a finding). This makes the
    multiple-comparisons posture explicit and consistent across both pairwise
    families — closing SYNTHESIS H1. Do not leave H1 uncovered.
    (3) SYNTHESIS M2 — disclose the wgan_cnn outlier-seed dependence at the
    OD-EMD claim site. Add a sentence near the per-pair table (~298-312) stating
    that the wgan_cnn OD-EMD column is dominated by a single anomalous seed
    (seed 42 = 0.1587 vs the other four seeds at 0.020-0.034, ~5x), and that
    this single seed sets BOTH `strong_claim_thresholds` extrema — the p-floor
    (0.3652) and the |d|-ceiling (0.6442) — so the |d| = 0.65 ceiling is an
    outlier-driven extremum, not a typical pair. Keep all numeric literals
    unchanged. No `methods_full.md` edit is required for this task unless its
    OD-DTW wording also overclaims — if `methods_full.md` is already honest
    (~436-442 indicates it is), leave it.
  </action>
  <verify>
    <automated>grep -qi 'matched-budget' docs/reviewer_response.md && grep -qiE 'seed 42|outlier' docs/reviewer_response.md && grep -qi 'LR-DTW' docs/reviewer_response.md && grep -qiE 'multiple comparison|multiplicity|uniform[- ]dominance|conjunctive' docs/reviewer_response.md && ! grep -qi 'temporal-structure capture quantum is\s*specifically designed for' docs/reviewer_response.md && echo DTW_ALIGN_OK</automated>
  </verify>
  <acceptance_criteria>
    - The OD-DTW Orlandi-improvement claim is reframed as matched-budget-wide (shared by wgan_lstm/wgan_mlp), not quantum-specific; "specifically designed for" framing removed
    - The text explicitly states that LR-DTW is the only quantum-distinguishing DTW result
    - The multiple-comparisons posture for the LR-DTW pairwise family is stated explicitly: it is a uniform-dominance (conjunctive) claim, reported as the worst-case margin over the family, and a conjunctive claim requires no multiplicity correction; the OD-EMD non-significance result is reconciled as reported-without-positive-equivalence-inference so multiplicity does not inflate it either — SYNTHESIS H1 is concretely addressed in `reviewer_response.md`, not left to the header
    - A disclosure sentence near the per-pair table states the wgan_cnn seed-42 outlier (0.1587 vs 0.020-0.034) sets both `strong_claim_thresholds` extrema (p-floor 0.3652, |d|-ceiling 0.6442)
    - All numeric literals are byte-unchanged
  </acceptance_criteria>
  <done>The OD-DTW claim is aligned to the honest matched-budget-wide framing, the LR-DTW multiple-comparisons posture is stated and reconciled across families, and the wgan_cnn outlier-seed dependence is disclosed at the claim site; no number changed.</done>
</task>

<task type="auto">
  <name>Task 3: Add the welch_pairwise.json notes field + verify the number-provenance gate still passes</name>
  <read_first>
    - results/welch_pairwise.json (the existing `notes` field and `strong_claim_thresholds` block — note structure; do not disturb computed values)
    - verify_number_provenance.py (the gate that plan 14-07 runs over the paper-facing docs — confirm it is invoked, not edited)
    - .planning/phases/14-paper-revision-release-freeze/peer-review-r4/math-stats-review.md (Agent 1 B1 — the recommended `notes` clarification text)
  </read_first>
  <action>
    Add a clarification to the `notes` field of
    `results/welch_pairwise.json` (a documentation field — NOT a
    computed value; `run_welch_aggregator.py` is not run and not edited). The
    note must state, machine-readably: "no statistically detectable OD-EMD
    difference at n=5 (underpowered — two-sample Welch power ~15% vs d=0.65,
    80%-power detection floor d ≈ 2.0; a TOST equivalence test is not satisfied;
    this is not an equivalence claim)." If `notes` is a string, append; if it is
    a list/object, add a new entry — preserve every existing note (including the
    Path A retraction record) and every other key and every numeric value
    byte-identical. Keep the JSON valid (parseable). Then run the
    number-provenance gate over the two docs edited in Tasks 1-2 to confirm the
    wording changes did not break number resolution:
    `./qgan_env/bin/python verify_number_provenance.py --target
    docs/reviewer_response.md` and the same for
    `docs/methods_full.md` — both MUST exit 0 / PASS. If either fails,
    the wording edit accidentally removed or altered a literal the gate needs —
    fix the wording (not the number) until the gate passes. This is the
    acceptance gate the SYNTHESIS constraint mandates.
  </action>
  <verify>
    <automated>python -c "import json; n=json.load(open('results/welch_pairwise.json'))['notes']; s=n if isinstance(n,str) else json.dumps(n); assert 'not an equivalence claim' in s and 'underpowered' in s, 'note missing'; print('NOTES_OK')" && ./qgan_env/bin/python verify_number_provenance.py --target docs/reviewer_response.md && ./qgan_env/bin/python verify_number_provenance.py --target docs/methods_full.md && echo PROVENANCE_OK</automated>
  </verify>
  <acceptance_criteria>
    - `results/welch_pairwise.json` parses as valid JSON and its `notes` field contains the non-equivalence clarification ("underpowered", "not an equivalence claim", the d ≈ 2.0 80%-power floor)
    - Every pre-existing `notes` entry (including the Path A retraction record), every other key, and every numeric value in `welch_pairwise.json` is byte-unchanged
    - `verify_number_provenance.py --target docs/reviewer_response.md` exits 0 (PASS)
    - `verify_number_provenance.py --target docs/methods_full.md` exits 0 (PASS)
    - `run_welch_aggregator.py` was not run and not edited
  </acceptance_criteria>
  <done>The welch_pairwise.json notes field carries the machine-readable non-equivalence clarification, and the number-provenance gate still passes on both edited paper-facing docs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| non-significant difference test → "equivalence" claim | a high p-value asserted as evidence of equivalence inverts hypothesis-testing logic |
| unstated multiple-comparisons posture → pairwise-family claim | a pairwise-family claim with no stated multiplicity posture is not auditable as defensible |
| wording edit → number-provenance gate | a careless wording edit could delete a literal the gate resolves, breaking 14-07's freeze gate |
| welch_pairwise.json notes field → computed values | hand-editing the JSON risks disturbing a computed value or the Path A retraction record |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-25 | Tampering | claim integrity (statistically indefensible equivalence wording frozen into a DOI) | mitigate | Tasks 1-2 reframe to the honest "no detectable difference at n=5" register; grep gate confirms "statistically equivalent" absent |
| T-14-26 | Tampering | number-provenance gate regression | mitigate | Task 3 re-runs `verify_number_provenance.py` over both edited docs; no numeric literal is altered, only interpretation wording |
| T-14-27 | Tampering | welch_pairwise.json computed-value integrity | mitigate | Only the `notes` documentation field is edited; JSON-parse + byte-unchanged-value acceptance criteria; aggregator not run/edited |
| T-14-33 | Repudiation | unstated multiple-comparisons posture (H1) | mitigate | Task 2 states the LR-DTW uniform-dominance posture explicitly and reconciles it across the EMD/DTW pairwise families; grep gate confirms a multiplicity/conjunctive posture statement is present |
| T-14-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs; documentation-wording edits only |
</threat_model>

<verification>
- No "statistically equivalent" / "parametric-efficiency equivalence" wording survives for the OD-EMD claim
- OD-DTW claim aligned to matched-budget-wide framing; LR-DTW stated as the quantum-distinguishing result
- The LR-DTW multiple-comparisons posture is stated explicitly and reconciled across the EMD/DTW pairwise families (SYNTHESIS H1)
- wgan_cnn seed-42 outlier dependence and n=5 power limitation disclosed at the claim site
- `welch_pairwise.json` notes field carries the non-equivalence clarification; JSON valid; computed values unchanged
- `verify_number_provenance.py` PASSES on both `reviewer_response.md` and `methods_full.md`
</verification>

<success_criteria>
The surviving OD-EMD and OD-DTW claims are statistically defensible as worded:
the OD-EMD claim states "no statistically detectable difference at n=5
(underpowered)" rather than equivalence, the OD-DTW Orlandi improvement is
framed as matched-budget-wide, the LR-DTW multiple-comparisons posture is stated
and reconciled across pairwise families, the wgan_cnn outlier and n=5 power
limitation are disclosed, and `welch_pairwise.json` carries a machine-readable
clarification — all with zero recomputation and with the number-provenance gate
still passing, closing SYNTHESIS C1, H1, M1, M2, M3, M5.
</success_criteria>

<output>
Create `.planning/phases/14-paper-revision-release-freeze/14-18-SUMMARY.md` when done
</output>
</content>
</invoke>
