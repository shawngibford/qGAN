---
phase: 14-paper-revision-release-freeze
plan: 07
type: execute
wave: 5
depends_on: ["14-05", "14-06"]
files_modified:
  - docs/release.md
  - .gitignore
  - verify_freeze_ready.py
autonomous: false
requirements: [INFRA-03]
user_setup:
  - service: zenodo
    why: "Mint a citable version DOI + concept DOI for the frozen tag (reviewer ask R1-m4); GitHub-integration cannot pre-reserve a DOI so a manual deposit is required"
    env_vars:
      - name: ZENODO_TOKEN
        source: "Zenodo -> Account -> Applications -> Personal access tokens (supplied interactively at deposit time; NEVER committed or written into release.md / LaTeX blocks)"
    dashboard_config:
      - task: "New upload -> Reserve DOI -> copy reserved version DOI; upload the git archive of tag v2.0-revision; Publish"
        location: "Zenodo web UI (manual deposit, prereserve_doi)"
must_haves:
  truths:
    - "git check-ignore confirms results/*.json is NOT excluded from the tag (the provenance backbone ships in the frozen archive)"
    - "Tag v2.0-revision exists and its archive contains results/*.json + data.csv but excludes qgan_env/ and large checkpoints"
    - "A reserved Zenodo version DOI is written into the PAPER LaTeX blocks (Data Availability) and docs/release.md"
    - "release.md records the tag SHA, the reserved version + concept DOI, the git check-ignore result, and copy-paste reproduce steps"
    - "Release is the LAST step, hard-blocked until every cited number passed the number-provenance gate (D-14-22)"
  artifacts:
    - path: "verify_freeze_ready.py"
      provides: "pre-tag gate: gitignore check + provenance-gate pass over all paper-blocks files (explicit-raise)"
      min_lines: 40
    - path: "docs/release.md"
      provides: "tag SHA + reserved DOIs + check-ignore result + reproduce steps"
      min_lines: 30
  key_links:
    - from: "verify_freeze_ready.py"
      to: "git check-ignore results/*.json"
      via: "explicit-raise if any provenance JSON is gitignored out of the tag"
      pattern: "check-ignore|AssertionError"
    - from: "docs/release.md"
      to: "tag v2.0-revision + reserved Zenodo DOI"
      via: "records git rev-parse SHA + version/concept DOI + reproduce steps"
      pattern: "v2.0-revision|DOI"
---

<objective>
Freeze the repository at tag `v2.0-revision` and mint the Zenodo DOI via the
MANUAL deposit path (`prereserve_doi` — GitHub integration cannot pre-reserve,
RESEARCH Pitfall 1 / Pattern 2), write the reserved DOI into the PAPER Data
Availability LaTeX block + `docs/release.md`, and record the freeze
provenance. This is the LAST wave, hard-blocked until every cited number passed
the number-provenance gate (D-14-22 step 7). Closes INFRA-03.

Purpose: Reviewers must cite the exact code state (R1-m4). The DOI must appear
inside the manuscript minted from the tagged tree — the only path that resolves
the chicken-and-egg is reserve-DOI-first via a manual Zenodo deposit. A real
risk (`.gitignore` line 62 = `results/`, RESEARCH Pitfall 4) is that
`results/*.json` is excluded from the tag, which would ship the DOI'd
archive without its provenance backbone — a mandatory pre-tag check.

Output: A pre-tag freeze-ready gate, the tag `v2.0-revision`, the reserved
Zenodo DOI wired into the manuscript blocks + `release.md`, and a complete
release record.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-paper-revision-release-freeze/14-CONTEXT.md
@.planning/phases/14-paper-revision-release-freeze/14-RESEARCH.md
@.planning/phases/14-paper-revision-release-freeze/14-PATTERNS.md
@.planning/phases/14-paper-revision-release-freeze/14-05-SUMMARY.md
@.planning/phases/14-paper-revision-release-freeze/14-06-SUMMARY.md

<interfaces>
<!-- Release sequence + gitignore reality (verified this session) -->

`.gitignore` line 62 is exactly `results/` (verified). `results/` is the artifact home — the broad pattern may exclude the nested provenance JSON from the tag. Pre-tag MUST run `git check-ignore results/<file>.json` (empty = safe); if it matches, add a `!results/` negation or `git add -f` and re-verify (RESEARCH Pitfall 4 / Open Question 1). D-14-21: tag includes `revision/` (core, run scripts, results JSON, docs, figures) + `.tex` reference files + `.planning` + `data.csv`; excludes `qgan_env/` and large checkpoints (referenced by hash, NOT committed).

Reserve-DOI-first sequence (RESEARCH Pattern 2 — manual Zenodo, NEVER the GitHub webhook):
1. After ALL numbers pass the gate: `git tag -a v2.0-revision -m "AIChE aic-4719598 major revision freeze"`; `git rev-parse v2.0-revision` → SHA for release.md.
2. `git check-ignore results/baseline_comparison.json` MUST be empty.
3. Manual Zenodo: New upload → Reserve DOI → copy version DOI. Write the reserved DOI into the PAPER Data Availability block (`main (4) copy.tex` line 292 currently a bare GitHub URL, no DOI) + `docs/release.md`.
4. `git archive --format=tar.gz -o v2.0-revision.tar.gz v2.0-revision` → upload to the reserved deposit → Publish.
5. Concept DOI = stable "all versions" citation; version DOI = this exact freeze.

Explicit-raise gate idiom (run_multiseed_rollup.py:86-92): `if <violation>: raise AssertionError(...)` — survives `python -O`.

ZENODO_TOKEN is an operator credential supplied interactively at deposit time — it MUST NOT be committed and MUST NOT be written into `release.md` or any LaTeX block (security_context).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-tag freeze-ready gate + cut tag v2.0-revision</name>
  <read_first>
    - .gitignore (line 62 `results/` — the exclusion risk for results/*.json, RESEARCH Pitfall 4)
    - run_multiseed_rollup.py (lines 86-92 explicit-raise gate idiom to reuse)
    - verify_number_provenance.py (Plan 03 — the per-file gate this plan runs over every paper-blocks file before tagging)
    - docs/paper_blocks_framing.md (Plan 05 — must pass the provenance gate before freeze)
    - docs/paper_blocks_refs_methods.md (Plan 06 — must pass the provenance gate before freeze)
    - docs/reviewer_response.md (Plan 06 — must pass the provenance gate before freeze)
    - .planning/phases/14-paper-revision-release-freeze/14-RESEARCH.md (Pitfall 4 gitignore; Pattern 2 reserve-DOI-first; D-14-21/22 tag scope + release-LAST ordering)
  </read_first>
  <action>
    Create `scripts/verify_freeze_ready.py` (the D-14-22 step-7 hard block).
    It MUST, using the explicit-raise idiom (run_multiseed_rollup.py:86-92,
    `raise AssertionError`): (a) run `git check-ignore` on each
    `results/*.json` and raise if ANY is gitignored out of the tag
    (RESEARCH Pitfall 4) — if matched, the script fixes it by adding a
    `!results/` negation to `.gitignore` (or staging with `git add
    -f`) and re-verifies until `git check-ignore` is empty AND `git ls-files
    revision/results | wc -l` is non-zero; (b) invoke
    `verify_number_provenance.py --target` on each of
    `docs/paper_blocks_framing.md`,
    `docs/paper_blocks_refs_methods.md`,
    `docs/reviewer_response.md` and raise if any fails (no cited
    number may be unprovenanced — D-14-22: DOI mints only over final numbers);
    (c) assert `qgan_env/` and large checkpoints (`*.pt`/`*.pth`) are NOT in
    `git ls-files` but `data.csv` IS (D-14-21 tag scope). Only after the gate
    passes, cut the annotated tag: `git tag -a v2.0-revision -m "AIChE
    aic-4719598 major revision freeze"`, then `git rev-parse v2.0-revision`,
    and verify `git archive v2.0-revision | tar -t` contains at least one
    `results/*.json` and `data.csv`. Record nothing secret. (Branch
    first if on the default branch before any commit; do not push.)
  </action>
  <verify>
    <automated>./qgan_env/bin/python scripts/verify_freeze_ready.py && git tag --list | grep -qx 'v2.0-revision' && [ -z "$(git check-ignore results/*.json 2>/dev/null)" ] && git archive v2.0-revision 2>/dev/null | tar -t | grep -q 'results/.*\.json' && git archive v2.0-revision 2>/dev/null | tar -t | grep -qx 'data.csv' && ! git ls-files | grep -q 'qgan_env/' && echo FREEZE_OK</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/verify_freeze_ready.py` uses `raise AssertionError(` (grep) for the gitignore, provenance, and tag-scope checks — not bare assert
    - `git check-ignore results/*.json` returns empty (no provenance JSON excluded from the tag)
    - `git tag --list` contains exactly `v2.0-revision`
    - `git archive v2.0-revision | tar -t` contains at least one `results/*.json` AND `data.csv`, and contains NO `qgan_env/` path and no `*.pt`/`*.pth`
    - The freeze-ready gate fails (non-zero exit) if any of `paper_blocks_framing.md` / `paper_blocks_refs_methods.md` / `reviewer_response.md` fails the number-provenance gate (verifiable by temporarily pointing at a scratch copy with an injected fake number)
  </acceptance_criteria>
  <done>Provenance + gitignore + tag-scope gate passes; tag v2.0-revision cut with the provenance JSON and data.csv inside the archive and qgan_env/checkpoints excluded.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2: Operator-interactive Zenodo manual deposit + reserve DOI</name>
  <action>
    Operator-interactive only — there is no autonomous implementation for this
    task. Pause execution and hand off to the human for the Zenodo manual
    deposit + reserve-DOI flow detailed in the checkpoint fields below; resume
    with the reserved version + concept DOI. RESEARCH Pitfall 1: the GitHub↔
    Zenodo integration cannot pre-reserve a DOI, so the reserve-DOI-first flow
    has no non-interactive CLI/API substitute. The executor performs NO file
    edits in this task.
  </action>
  <what-built>
    Tag `v2.0-revision` is cut and the freeze-ready gate (gitignore + number
    provenance + tag scope) has passed. A `git archive` of the tag is ready to
    upload. The only remaining work is the Zenodo manual deposit, which
    requires an operator-supplied `ZENODO_TOKEN` and interactive web UI actions
    that have no non-interactive CLI/API substitute for the reserve-DOI-first
    flow (RESEARCH Pitfall 1: GitHub integration cannot pre-reserve).
  </what-built>
  <how-to-verify>
    1. Create the archive: `git archive --format=tar.gz -o v2.0-revision.tar.gz v2.0-revision`
    2. In the Zenodo web UI: New upload → click "Reserve DOI" → copy the reserved VERSION DOI and the CONCEPT DOI.
    3. Confirm `ZENODO_TOKEN` was supplied interactively and is NOT present in any committed file (run `git grep -i zenodo_token` → must be empty) and NOT written into `release.md` or any LaTeX block.
    4. Upload `v2.0-revision.tar.gz` to the reserved deposit, fill metadata (title = aic-4719598 major revision freeze, authors), then Publish.
    5. Confirm the published version DOI resolves and equals the reserved DOI.
  </how-to-verify>
  <verify><human-check>Operator pastes the reserved VERSION + CONCEPT DOI and confirms the deposit published over the v2.0-revision archive with no ZENODO_TOKEN committed</human-check></verify>
  <resume-signal>Paste the reserved VERSION DOI and CONCEPT DOI (e.g. "10.5281/zenodo.XXXXXXX"), confirm publish succeeded, or describe issues</resume-signal>
  <done>Reserved version + concept DOI obtained via manual Zenodo deposit; deposit published over the v2.0-revision archive; no operator token committed</done>
</task>

<task type="auto">
  <name>Task 3: Wire reserved DOI into the manuscript block + write release.md</name>
  <read_first>
    - main (4) copy.tex (READ-ONLY — Data Availability statement line 292, currently a bare GitHub URL with no DOI; the anchor for the INFRA-03 block)
    - docs/paper_blocks_refs_methods.md (Plan 06 — where the INFRA-03 Data Availability block is appended, same keyed-block convention)
    - docs/reconciliation_note.md (Plan 03 — for any reproduce-step number)
    - .planning/phases/14-paper-revision-release-freeze/14-PATTERNS.md (release.md content spec — RESEARCH "Tag + reserved-DOI" sequence; NO code analog)
    - .planning/phases/14-paper-revision-release-freeze/14-RESEARCH.md (Pattern 2 reserve-DOI-first; Pitfall 1; D-14-21 release.md contents)
  </read_first>
  <action>
    Append an INFRA-03 keyed LaTeX block to
    `docs/paper_blocks_refs_methods.md` updating the Data
    Availability statement at `main (4) copy.tex` line 292 to add the reserved
    Zenodo VERSION DOI (and mention the CONCEPT DOI for "all versions"),
    before→after form, R1-m4 rationale. Then create
    `docs/release.md` recording: the tag name `v2.0-revision`, the tag
    SHA (`git rev-parse v2.0-revision`), the reserved VERSION DOI + CONCEPT DOI
    (from the checkpoint resume signal), the `git check-ignore
    results/*.json` result (must be "empty / not ignored"), the tag
    scope (includes revision/ + .tex + .planning + data.csv; excludes
    qgan_env/ + large checkpoints referenced by hash), and copy-paste reproduce
    steps (clone, checkout tag, env setup, run sweep + model_info + figures).
    The `ZENODO_TOKEN` MUST NOT appear anywhere in `release.md` or the LaTeX
    block (security_context). Do NOT edit any `.tex` file (D-14-18). Update
    `.planning/ROADMAP.md` / `.planning/REQUIREMENTS.md` per the D-14-23
    scope-reconciliation flag (record the intentional Phase-14 expansion as a
    recorded deviation).
  </action>
  <verify>
    <automated>test -f docs/release.md && grep -q 'v2.0-revision' docs/release.md && grep -Eq '10\.[0-9]+/zenodo\.[0-9]+' docs/release.md && grep -q 'INFRA-03' docs/paper_blocks_refs_methods.md && ! git grep -iq 'ZENODO_TOKEN' -- docs/release.md docs/paper_blocks_refs_methods.md && git diff --stat -- "main (4) copy.tex" | grep -cv . | grep -q '^0$' && echo RELEASE_OK</automated>
  </verify>
  <acceptance_criteria>
    - `docs/release.md` exists and contains `v2.0-revision`, the tag SHA, a resolvable DOI string matching `10.\d+/zenodo.\d+` (version + concept), and the `git check-ignore` "not ignored" result + reproduce steps
    - `docs/paper_blocks_refs_methods.md` contains an `INFRA-03` keyed block updating the Data Availability statement (line 292) with the reserved DOI, before→after form
    - No `ZENODO_TOKEN` (or any token value) appears in `release.md` or the LaTeX-blocks file (`git grep -i zenodo_token` over those files is empty)
    - `git diff --stat -- "main (4) copy.tex"` shows zero changes (D-14-18)
    - `.planning/ROADMAP.md` Phase 14 entry and `.planning/REQUIREMENTS.md` reflect the D-14-23 recorded scope expansion
  </acceptance_criteria>
  <done>Reserved DOI wired into the manuscript block + release.md, release record complete with tag SHA / DOIs / check-ignore result / reproduce steps, no secrets committed, roadmap/requirements scope-reconciled.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo working tree → frozen tag archive | gitignored provenance JSON would ship a DOI'd archive without its number backbone (success-criterion 5 broken) |
| operator credential → committed files | a leaked `ZENODO_TOKEN` in release.md / LaTeX / git history is a credential disclosure |
| reserved DOI → manuscript of record | a DOI minted before numbers were final would cite a non-frozen state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-18 | Tampering | frozen-artifact integrity (gitignored provenance) | mitigate | `scripts/verify_freeze_ready.py` explicit-raise `git check-ignore` gate + archive-content assertion; results/*.json + data.csv proven inside `git archive v2.0-revision` |
| T-14-19 | Information disclosure | ZENODO_TOKEN secret handling | mitigate | Token supplied interactively at deposit time only; `git grep -i zenodo_token` over release.md + LaTeX blocks must be empty; never written to any committed file (security_context) |
| T-14-20 | Tampering | premature DOI over non-final numbers | mitigate | D-14-22 release-LAST ordering: freeze-ready gate requires all paper-blocks files to pass the number-provenance gate before the tag is cut and before the deposit |
| T-14-21 | Tampering | tag immutability / wrong scope | mitigate | Annotated tag + archive-content assertion (data.csv in, qgan_env/ + *.pt out); tag is the immutable frozen reference the DOI points at |
| T-14-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan; RESEARCH Package Legitimacy Audit is a documented no-op for Phase 14 |
</threat_model>

<verification>
- `scripts/verify_freeze_ready.py` explicit-raise gate passes (gitignore + provenance + tag scope)
- Tag `v2.0-revision` cut; archive contains results/*.json + data.csv, excludes qgan_env/ + checkpoints
- Reserved version + concept DOI written into the INFRA-03 LaTeX block + release.md
- No ZENODO_TOKEN in any committed file
- `.tex` byte-untouched; ROADMAP/REQUIREMENTS scope-reconciled (D-14-23)
</verification>

<success_criteria>
The repository is frozen at an immutable `v2.0-revision` tag whose archive
carries the full provenance JSON + data.csv, a Zenodo version + concept DOI is
reserved via the manual deposit path and cited in the manuscript Data
Availability block + recorded in `release.md` with reproduce steps, no operator
secret is committed, and the release was gated LAST behind every number passing
the provenance gate — INFRA-03 closed and the phase frozen.
</success_criteria>

<output>
Create `.planning/phases/14-paper-revision-release-freeze/14-07-SUMMARY.md` when done
</output>
