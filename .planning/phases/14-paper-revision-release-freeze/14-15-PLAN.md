---
phase: 14-paper-revision-release-freeze
plan: 15
type: execute
wave: 13
depends_on: ["14-13", "14-14"]
files_modified:
  - revision/run_distribution_emd.py
  - revision/results/distribution_emd.json
  - revision/run_model_info.py
  - revision/docs/reconciliation_note.md
  - revision/run_figure_suite.py
  - revision/results/figures/qq_overlay.png
  - revision/results/figures/qq_overlay.pdf
  - revision/results/figures/qq_overlay.json
  - revision/results/figures/qq_ar.json
  - revision/results/figures/qq_iqp_sel_55_repro.json
  - revision/results/figures/qq_V1.json
  - revision/results/figures/qq_V2.json
  - revision/results/figures/qq_V3.json
  - revision/results/figures/qq_vae.json
  - revision/results/figures/qq_wgan_cnn.json
  - revision/results/figures/qq_wgan_lstm.json
  - revision/results/figures/qq_wgan_mlp.json
  - revision/docs/reviewer_response.md
  - revision/docs/methods_full.md
  - revision/docs/paper_blocks_framing.md
  - revision/docs/peer_review_remediation.md
  - revision/docs/completeness_sweep_manifest.md
autonomous: true
requirements: [PAPER-01, PAPER-02, PAPER-08, PAPER-09, INFRA-03]
must_haves:
  truths:
    - "Two post-14-14 findings prompt this plan: (1) the original pre-v1.0 paper reported a 50-bin histogram-density Wasserstein 'distribution EMD' and the v1.0 release switched to raw-sample Wasserstein (14-13 T3 added the metric-redefinition disclosure to reconciliation_note.md acknowledging the two are not commensurate) — the user wants the histogram-density column REINTRODUCED as a third comparable metric alongside OD raw-sample EMD and log-return raw-sample EMD so reviewers can read the matched-2000ep numbers in the SAME metric the original paper reported; (2) all 9 per-model QQ plots look near-identical to the eye — empirical verification (4.44e-16 floating-point match between independent reconstruction from samples.npy and the rendered JSON companions; all 9 PNG sha256 distinct) confirmed the data routing is correct and the plots look similar because 8 of the 9 models genuinely recover the OD marginal to within ≤0.03 OD-units max-quantile-diff while only WGAN-CNN visibly deviates (~0.69 upper-tail), which is a legitimate finding that NEEDS to be communicated to reviewers"
    - "D-14-22 (`revision/core/` byte-freeze) PRESERVED across all 5 tasks; the histogram-density EMD lives in a NEW top-level emitter `revision/run_distribution_emd.py`, NOT in `revision/core/eval.py`; the core `compute_emd` (revision/core/eval.py:25-36, the v1.0 raw-sample Wasserstein over `wasserstein_distance(real, fake)`) is BYTE-UNTOUCHED; every task's verify gate asserts `[ -z \"$(git diff --stat revision/core/)\" ]`"
    - "D-14-13 (strict-accept gate) PRESERVED — the new distribution-EMD column is INFORMATIONAL only; no new gate threshold; no edit to the existing _strict_accept equality semantics (the 14-13 T4 / 14-14 T2 device-manifest equality check is untouched)"
    - "D-14-16 (gate v2.1 byte-freeze) PRESERVED — no edit to `revision/verify_number_provenance.py`; the new `distribution_emd.json` is auto-walked into the v2.1 resolution corpus by the existing `_json_blobs()` walker introduced in 14-13 T2 + lookbehind-fixed in 14-14 T1; the gate runs against `reconciliation_note.md` PASSING after T2 lands without any gate edit"
    - "D-14-18 (Overleaf-canonical LaTeX read-only) PRESERVED — no edits to `main (4) copy.tex` / `supp_material.tex`; the reviewer-facing docs in `revision/docs/` carry the marginal-convergence message"
    - "The 50-bin histogram-density formulation is FIXED across the plan: `wasserstein_distance(bin_centers, bin_centers, real_hist_density, fake_hist_density)` with `n_bins=50` and `np.histogram(..., density=True)`; bin edges are taken from the real distribution and reused for the fake (so the two histograms share x-axis support); this matches the citation in `revision/docs/reconciliation_note.md`'s C-3 disclosure paragraph word-for-word so the metric formulation already disclosed to reviewers is now also the metric formulation actually computed"
    - "NO re-training; all metrics read existing per-seed samples from `revision/results/matched2000/runs/{model}/{seed}/samples.npy`; the 9 production model_kinds (ar, iqp_sel_55_repro, V1, V2, V3, vae, wgan_cnn, wgan_lstm, wgan_mlp) are present on disk per `ls revision/results/matched2000/runs/`; the headline `iqp_sel_55_headline` (n=1) is included as a 46th row in `distribution_emd.json` if its samples.npy exists, otherwise omitted with a `headline_present: false` note in the JSON header"
    - "NO new ACF / conditional-moment / TimeGAN-style figures; the plan POINTS reviewers to existing such figures (`training_convergence_all_models.png` from 14-10, `failure_modes_summary.png` from 14-10, `seed_variance_per_model.png` from 14-10, `methods_full.md §3.x` from 14-11/14-14) for architecture discrimination; the only new figure is the `qq_overlay.png/pdf/json` (Option A — single 9-model overlay + delta-QQ panel)"
    - "After T1, `revision/run_distribution_emd.py` exists as a NEW top-level emitter (NOT in `revision/core/`) with `compute_histogram_density_emd(real, fake, n_bins=50)` implementing the pre-v1.0 metric verbatim; iterates over MODEL_ORDER × {42, 43, 44, 45, 46} loading `revision/results/matched2000/runs/{model}/{seed}/samples.npy`, reconstructs OD-scale via the `reconstruct_od` helper imported from `revision/run_figure_suite.py:261-296`, and emits `revision/results/distribution_emd.json` with schema string `\"distribution-emd v1 (Phase 14 plan 14-15)\"`, `metric_formulation` string identical to the formulation cited in `reconciliation_note.md`'s C-3 disclosure, `data_hash: \"91e447d4624e25b3\"`, `n_bins: 50`, per-(model, seed, scale) rows, and per-(model, scale) aggregates with `ddof=1` std (matching the 14-13 T3 sample-std convention)"
    - "After T2, `revision/docs/reconciliation_note.md` carries BOTH the existing `## EMD (OD scale) — final-eval mean over seeds 42-46` table (preserved VERBATIM with its OLD/NEW headline-trajectory framing from 14-13) AND a NEW `## EMD comparable across metric variants (matched 2000ep budget)` section with a 3-column table sourcing: column 1 from `matched2000_dualscale.json#aggregates[*, scale='OD', metric_name='emd']` (already in the prior table); column 2 from `matched2000_dualscale.json#aggregates[*, scale='log_return', metric_name='emd']` (new surfacing); column 3 from `distribution_emd.json#aggregates[*, scale='OD']` (T1 output); a one-sentence cross-reference at the end of the existing OD-scale table points to the new comparable-variants section; the metric-redefinition disclosure paragraph (C-3) is extended with one sentence acknowledging the histogram-density reintroduction now makes the matched-2000ep numbers commensurate with the pre-v1.0 paper headline (~0.0015) for the first time since the v1.0 metric switch"
    - "After T3, `revision/results/figures/qq_overlay.{png,pdf,json}` exist as a single 2-panel figure: Panel A is a standard QQ overlay with all 9 model fake-quantile curves on one axis (x=real quantile, y=fake quantile, y=x diagonal as gray reference) color-coded from `MODEL_COLORS`, axis range 0.5–3.8 OD-units; Panel B is a delta-QQ (delta_q = fake_q - real_q vs quantile rank) with y-axis range ~±0.7 OD-units, y=0 reference line, shared legend with Panel A; the companion JSON carries `convergence_stats` (9 per-model max-abs-quantile-diff entries — 8/9 ≤ 0.03 and wgan_cnn ~0.69), `render_only: false` (this IS a primary discriminating figure), `model_order_color_keys` (the deterministic color-key array for CR-1 sha256-seeded determinism)"
    - "After T3, every existing per-model `revision/results/figures/qq_{model}.json` companion (9 files) gains an additive `caption_note` field: `\"Per-model OD-scale QQ; on this scale 8/9 models hug the diagonal and discrimination is hard. See qq_overlay.png for the single discriminating figure.\"` — the per-model PNG/PDFs are NOT regenerated; only the JSON companions get the additive field"
    - "After T4, `revision/docs/reviewer_response.md` carries a NEW `## Marginal-convergence finding (post-r2 investigation)` subsection with the 4.44e-16 routing-verification claim, the explicit ≤0.03 OD-units / ~0.69 wgan_cnn upper-tail numbers, and the architecture-discrimination redirect pointing reviewers to `qq_overlay.png`, `training_convergence_all_models.png`, `failure_modes_summary.png`, `seed_variance_per_model.png`, and `methods_full.md §3.x`; `methods_full.md` carries a 2-sentence cross-reference at the end of the OD-scale evaluation section; `paper_blocks_framing.md` (if a natural insertion point exists in the per-model PAPER-01 framing block) carries a one-sentence reference, otherwise the insertion is skipped and `methods_full.md` + `reviewer_response.md` carry the message"
    - "After T4, `revision/docs/peer_review_remediation.md` carries a NEW `## R2 follow-up — marginal-convergence finding (Plan 14-15)` subsection enumerating (a) the user-observed visual concern, (b) the empirical verification result (4.44e-16 routing match against samples.npy), (c) the link to `qq_overlay.png` and the new 3-column table in `reconciliation_note.md`; the existing 14-13 + 14-14 sections are preserved verbatim"
    - "After T5, the v2.1 gate (from 14-14 T1, byte-frozen under D-14-16) runs against all 10 paper-facing docs (the same 10 as 14-14 T5: `paper_blocks_framing.md`, `paper_blocks_refs_methods.md`, `reviewer_response.md`, `reconciliation_note.md`, `methods_full.md`, `circuit_atlas.md`, `completeness_sweep_manifest.md`, `training_protocol.md`, `dataset_stats.md`, `peer_review_remediation.md`, plus `REPRODUCE.md` if any paper-facing claims appear) and all PASS under the v2.1 schema; the new histogram-density EMD literals in `reconciliation_note.md` resolve to `distribution_emd.json#aggregates`; the marginal-convergence numbers `0.03` and `0.69` in `reviewer_response.md` + `methods_full.md` resolve to `qq_overlay.json#convergence_stats`; `completeness_sweep_manifest.md` carries a new `## Plan 14-15` section enumerating T1–T5 deliverables (existing 14-09 / 14-10 / 14-11 / 14-12 / 14-13 / 14-14 sections preserved verbatim); `.planning/phases/14-paper-revision-release-freeze/14-15-SUMMARY.md` exists with Self-Check PASS + 5 task commit SHAs + a final-state line declaring Phase 14 incomplete plans = `[14-07]` only"
    - "After T5, the deterministic-render assertion for the overlay PASSES: two consecutive renders of `qq_overlay` produce BYTE-IDENTICAL companion-JSON `model_order_color_keys` arrays (CR-1 sha256-seeded determinism preserved from 14-13); SHA-256 of `qq_overlay.png` is recorded in the companion JSON for downstream verification"
    - "Files explicitly NOT modified by this plan: `revision/core/` (D-14-22 byte-freeze, asserted by every task verify gate including `[ -z \"$(git diff --stat revision/core/)\" ]`); `revision/verify_number_provenance.py` (D-14-16 byte-freeze post-14-14 T1 — no gate edit); `main (4) copy.tex` / `supp_material.tex` (D-14-18 read-only); the read-only Overleaf-canonical LaTeX; the existing 9 `qq_{model}.png/pdf` per-model figure binaries (only their JSON companions gain the additive `caption_note` field); the byte-stable upstream docs `circuit_atlas.md` (14-09), `paper_blocks_refs_methods.md` (14-12/14-13); the existing `matched2000_dualscale.json` (read-only source for columns 1 and 2 of the new comparable-variants table)"
    - "After T5, the only outstanding Phase 14 plan remains 14-07 (Zenodo deposit + tag + DOI wiring + release.md, deferred manual gate per the pause-stash recorded in `~/.claude/projects/-Users-shawngibford-dev-phd-qGAN/memory/project_phase14_zenodo_blocker.md`); Phase 14 plan inventory shows incomplete = [14-07]; ROADMAP progress row `14. Paper Revision & Release Freeze | v2.0 | 13/15 | In Progress` after this plan commits (orchestrator flips from `13/14` → `13/15` mid-plan and to `14/15` post-merge)"
  artifacts:
    - path: "revision/run_distribution_emd.py"
      provides: "NEW top-level emitter (Task 1, NOT in `revision/core/`). Implements `compute_histogram_density_emd(real, fake, n_bins=50)` per the pre-v1.0 50-bin density Wasserstein formulation verbatim: `real_hist, edges = np.histogram(np.asarray(real).ravel(), bins=n_bins, density=True); fake_hist, _ = np.histogram(np.asarray(fake).ravel(), bins=edges, density=True); bin_centers = 0.5 * (edges[:-1] + edges[1:]); return float(scipy.stats.wasserstein_distance(bin_centers, bin_centers, real_hist, fake_hist))`. Iterates MODEL_ORDER × {42, 43, 44, 45, 46} loading per-seed `revision/results/matched2000/runs/{model}/{seed}/samples.npy`; reconstructs OD-scale via `reconstruct_od` imported from `revision/run_figure_suite.py:261-296`; emits `revision/results/distribution_emd.json`. Standalone CLI: `./qgan_env/bin/python revision/run_distribution_emd.py` produces the JSON in one pass."
      min_lines: 120
    - path: "revision/results/distribution_emd.json"
      provides: "NEW aggregator JSON (Task 1). Schema string `\"distribution-emd v1 (Phase 14 plan 14-15)\"`. Top-level fields: `metric_formulation` (the exact 50-bin density Wasserstein description matching `reconciliation_note.md`'s C-3 disclosure citation), `data_hash: \"91e447d4624e25b3\"` (corpus-consistency with 14-13 T4 + 14-14 T1 propagation), `n_bins: 50`. `rows`: 9 model_kinds × 5 seeds × 2 scales (OD + log_return) = 90 rows (+ headline `iqp_sel_55_headline` if present, n=1). `aggregates`: per-(model_kind, scale) mean/std/n with `ddof=1` std (matching the 14-13 T3 sample-std convention). Auto-walked into the v2.1 gate's resolution corpus by the existing `_json_blobs()` walker; no gate edit needed."
      min_lines: 50
    - path: "revision/run_model_info.py"
      provides: "Updated (Task 2) to source the 3-column comparable EMD table for `reconciliation_note.md` from `matched2000_dualscale.json` (columns 1 and 2: OD raw-sample EMD + log-return raw-sample EMD) and `distribution_emd.json` (column 3: 50-bin histogram-density EMD on OD scale). The existing `## EMD (OD scale) — final-eval mean over seeds 42-46` table emit path (around lines 220–302) is PRESERVED verbatim — the new `## EMD comparable across metric variants (matched 2000ep budget)` section is APPENDED after it. Re-emits `reconciliation_note.md` with both tables, the cross-reference sentence between them, and the extended C-3 disclosure paragraph."
    - path: "revision/docs/reconciliation_note.md"
      provides: "Re-emitted (Task 2) with BOTH the existing OLD/NEW OD-scale headline-trajectory table from 14-13 (preserved verbatim with its 14-12 caveat paragraph and 14-13 / 14-14 disclosures) AND a NEW `## EMD comparable across metric variants (matched 2000ep budget)` section with the 3-column table (OD raw / log-return raw / 50-bin histogram-density). One-sentence cross-reference at the end of the existing OD-scale table points to the new section. The metric-redefinition C-3 disclosure paragraph is extended by ONE sentence acknowledging the histogram-density reintroduction makes the matched-2000ep numbers commensurate with the pre-v1.0 paper headline (~0.0015) for the first time since the v1.0 metric switch."
    - path: "revision/run_figure_suite.py"
      provides: "Extended (Task 3) with a new `render_qq_overlay(model_order, real_flat, figures_dir)` function (alongside the existing `render_qq_plot` at lines 403-433 which the new function mirrors in structure). Generates ONE figure with TWO subplots — Panel A: 9-model QQ overlay on shared axes with y=x diagonal reference; Panel B: 9-model delta-QQ (fake_q - real_q vs quantile rank) with y=0 reference and ~±0.7 y-axis range. Reuses `MODEL_COLORS` palette and `MODEL_ORDER` from lines 106-116. The caller loop at the end of the figure suite (around lines 2378-2400) gains one new call to `render_qq_overlay(MODEL_ORDER, real_flat, figures_dir)`. Also extends the per-model QQ JSON emission path so each `qq_{model}.json` companion gains the additive `caption_note` field (the per-model PNG/PDFs are NOT regenerated; only their JSON companions are touched)."
    - path: "revision/results/figures/qq_overlay.png"
      provides: "NEW primary discriminating figure (Task 3). 2-panel figure: Panel A 9-model QQ overlay (x=real quantile, y=fake quantile, y=x diagonal as gray reference, axis range 0.5–3.8 OD-units, color-coded by `MODEL_COLORS`, legend lists all 9 models) — at this scale 8/9 models collapse onto the diagonal and only WGAN-CNN bows out (~0.69 upper-tail), so the convergence is visually obvious. Panel B delta-QQ (fake_q - real_q vs quantile rank, y-axis range ~±0.7 OD-units, y=0 reference, shared legend with Panel A) — at this expanded scale the inter-model separation between the 8 well-converged models (all within ±0.15) and WGAN-CNN (~0.55 upper-tail) is unambiguous. CR-1 sha256-seeded determinism preserved."
    - path: "revision/results/figures/qq_overlay.pdf"
      provides: "NEW (Task 3). PDF companion of `qq_overlay.png`; same content; for inclusion in the manuscript supplement."
    - path: "revision/results/figures/qq_overlay.json"
      provides: "NEW companion (Task 3). Top-level fields: `caption` (mentions \"9-model OD-scale QQ overlay + delta-QQ panel; Panel A shows convergence to OD marginal except WGAN-CNN upper-tail bow; Panel B (delta-QQ) expands the y-axis to show inter-model separation\"), `convergence_stats` (9 per-model max-abs-quantile-diff entries — 8/9 ≤ 0.03 OD-units and wgan_cnn ~0.69 upper-tail), `data_hash: \"91e447d4624e25b3\"` (corpus-consistency), `render_only: false` (this IS a primary discriminating figure, not a render-only companion), `model_order_color_keys` (the deterministic color-key array used by the renderer for CR-1 sha256-seeded determinism — must be byte-identical across two consecutive re-renders), `sha256_png` (recorded for downstream verification)."
    - path: "revision/results/figures/qq_ar.json"
      provides: "Re-emitted (Task 3) with additive `caption_note: \"Per-model OD-scale QQ; on this scale 8/9 models hug the diagonal and discrimination is hard. See qq_overlay.png for the single discriminating figure.\"`. All other fields preserved verbatim."
    - path: "revision/results/figures/qq_iqp_sel_55_repro.json"
      provides: "Re-emitted (Task 3) with additive `caption_note` per the per-model template. All other fields preserved verbatim."
    - path: "revision/results/figures/qq_V1.json"
      provides: "Re-emitted (Task 3) with additive `caption_note`. All other fields preserved verbatim."
    - path: "revision/results/figures/qq_V2.json"
      provides: "Re-emitted (Task 3) with additive `caption_note`. All other fields preserved verbatim."
    - path: "revision/results/figures/qq_V3.json"
      provides: "Re-emitted (Task 3) with additive `caption_note`. All other fields preserved verbatim."
    - path: "revision/results/figures/qq_vae.json"
      provides: "Re-emitted (Task 3) with additive `caption_note`. All other fields preserved verbatim."
    - path: "revision/results/figures/qq_wgan_cnn.json"
      provides: "Re-emitted (Task 3) with additive `caption_note`. All other fields preserved verbatim. (This is the one model where the per-model QQ already shows obvious upper-tail deviation, but the caption_note still applies because the overlay is the discriminating figure for the cross-architecture comparison.)"
    - path: "revision/results/figures/qq_wgan_lstm.json"
      provides: "Re-emitted (Task 3) with additive `caption_note`. All other fields preserved verbatim."
    - path: "revision/results/figures/qq_wgan_mlp.json"
      provides: "Re-emitted (Task 3) with additive `caption_note`. All other fields preserved verbatim."
    - path: "revision/docs/reviewer_response.md"
      provides: "Appended (Task 4) with new `## Marginal-convergence finding (post-r2 investigation)` subsection carrying the 4.44e-16 routing-verification claim, the ≤0.03 OD-units / ~0.69 wgan_cnn numbers, and the architecture-discrimination redirect to dependence-structure figures (`qq_overlay.png`, `training_convergence_all_models.png` from 14-10, `failure_modes_summary.png` from 14-10, `seed_variance_per_model.png` from 14-10, `methods_full.md §3.x`). All other rows (R1-XX, R2-XX, Cross-Cutting, Completeness sweep, 14-13 CR-4 disclosure, 14-14 R1-m4 DOI-pending wording) preserved verbatim."
    - path: "revision/docs/methods_full.md"
      provides: "Appended (Task 4) with a 2-sentence cross-reference at the end of the OD-scale evaluation section (the section discussing per-model EMD results) noting that all 9 models recover the OD marginal to within ≤0.03 OD-units max-abs-quantile-diff except WGAN-CNN (~0.69 upper-tail), and directing readers to `qq_overlay.png` (Plan 14-15) as the single discriminating figure across architectures. All other sections (§2.i VAE caveat from 14-14, §3.x.d VAE β_eff=2.5 derivation from 14-14, §5.2 references from 14-11/14-14) preserved verbatim."
    - path: "revision/docs/paper_blocks_framing.md"
      provides: "Conditionally appended (Task 4) with a one-sentence reference in the PAPER-01 per-model framing block (if a natural insertion point exists) noting the OD-marginal convergence finding. If no natural insertion point exists, this file is NOT modified and the message is carried by `methods_full.md` + `reviewer_response.md` alone; the conditional outcome is recorded in the T4 commit body."
    - path: "revision/docs/peer_review_remediation.md"
      provides: "Appended (Task 4) with new `## R2 follow-up — marginal-convergence finding (Plan 14-15)` subsection enumerating (a) the user-observed visual concern, (b) the empirical verification result (4.44e-16 routing match between independent reconstruction from samples.npy and the rendered JSON companions), (c) the link to `qq_overlay.png` and the new 3-column comparable-variants table in `reconciliation_note.md`. The existing 14-13 finding-to-commit tables (code-review.md, math-review.md, quantum-review.md, provenance-review.md, methods-reproducibility-review.md) + the OOS section + the 14-14 `## Gate v2.1 known limitations` and `## R2 follow-up sweep` sections are preserved verbatim."
    - path: "revision/docs/completeness_sweep_manifest.md"
      provides: "Updated (Task 5) with a new `## Plan 14-15` section at the bottom enumerating the T1–T5 deliverables (distribution_emd.json + reconciliation_note.md 3-column table + qq_overlay figure + marginal-convergence reviewer-facing communication). Existing 14-09 / 14-10 / 14-11 / 14-12 / 14-13 / 14-14 sections preserved verbatim."
  key_links:
    - from: "revision/run_distribution_emd.py (Task 1 new emitter)"
      to: "revision/results/distribution_emd.json (NEW aggregator)"
      via: "iterates `MODEL_ORDER` (9 production model_kinds from revision/run_figure_suite.py:106-116) × seeds {42, 43, 44, 45, 46}; loads `revision/results/matched2000/runs/{model}/{seed}/samples.npy`; reuses `reconstruct_od` imported from `revision/run_figure_suite.py:261-296` for the OD scale (and the existing log-return scale already exposed in samples); calls `compute_histogram_density_emd(real, fake, n_bins=50)` for each (model, seed, scale); emits rows + per-(model, scale) aggregates with `ddof=1` std"
      pattern: "compute_histogram_density_emd|reconstruct_od|distribution_emd\\.json|MODEL_ORDER"
    - from: "revision/run_distribution_emd.py compute_histogram_density_emd (Task 1)"
      to: "revision/docs/reconciliation_note.md C-3 metric-redefinition disclosure paragraph (existing from 14-13 T3)"
      via: "the `metric_formulation` field in `distribution_emd.json` carries the EXACT formulation cited in the C-3 disclosure (`scipy.stats.wasserstein_distance(bin_centers, bin_centers, real_hist_density, fake_hist_density) over 50-bin histograms (np.histogram(..., density=True))`); the C-3 paragraph in `reconciliation_note.md` is extended with one sentence acknowledging the reintroduction; the formulation IS commensurate with the deprecated v1.0-pre metric on the SAME real-data slice and SAME 50-bin convention for the first time since the v1.0 raw-sample switch"
      pattern: "metric_formulation|50-bin histograms|histogram-density|wasserstein_distance\\(bin_centers"
    - from: "revision/run_model_info.py (Task 2 updated emitter)"
      to: "revision/docs/reconciliation_note.md `## EMD comparable across metric variants (matched 2000ep budget)` 3-column table"
      via: "the emitter sources column 1 from `matched2000_dualscale.json#aggregates[*, scale='OD', metric_name='emd']` (already in the prior OD-scale table, now also in the comparable-variants table); column 2 from `matched2000_dualscale.json#aggregates[*, scale='log_return', metric_name='emd']` (the log-return raw-sample EMD already present in the JSON but not previously surfaced in the table); column 3 from `distribution_emd.json#aggregates[*, scale='OD']` (T1 output); std is computed with `ddof=1` (matching 14-13 T3 convention); each cell shows mean over seeds 42-46"
      pattern: "EMD comparable across metric variants|matched2000_dualscale\\.json|distribution_emd\\.json#aggregates"
    - from: "revision/results/figures/qq_overlay.png (Task 3 NEW primary discriminating figure)"
      to: "revision/docs/reviewer_response.md `## Marginal-convergence finding (post-r2 investigation)` subsection (Task 4)"
      via: "the overlay companion JSON's `convergence_stats` field carries 9 per-model max-abs-quantile-diff values (8/9 ≤ 0.03 OD-units; wgan_cnn ~0.69 upper-tail); the reviewer_response.md subsection cites these numbers explicitly; the v2.1 gate resolves the `0.03` and `0.69` literals in reviewer_response.md to `qq_overlay.json#convergence_stats` (T5 confirms)"
      pattern: "convergence_stats|max-abs-quantile-diff|0\\.03 OD-units|0\\.69|Marginal-convergence finding"
    - from: "revision/run_figure_suite.py render_qq_overlay (Task 3 new function)"
      to: "9-model overlay + delta-QQ panel as a SINGLE 2-panel figure"
      via: "function signature `render_qq_overlay(model_order, real_flat, figures_dir)` mirrors the existing `render_qq_plot` at lines 403-433; reuses `MODEL_COLORS` palette and `MODEL_ORDER` from lines 106-116; Panel A x=real quantile / y=fake quantile / y=x diagonal reference / axis range 0.5–3.8 OD-units / legend lists all 9 models; Panel B y-axis range ~±0.7 OD-units / y=0 reference; the caller loop at lines 2378-2400 gains one new call: `render_qq_overlay(MODEL_ORDER, real_flat, figures_dir)`; CR-1 sha256-seeded determinism preserved across re-renders"
      pattern: "render_qq_overlay|delta-QQ|MODEL_COLORS|MODEL_ORDER"
    - from: "revision/docs/reviewer_response.md + methods_full.md + paper_blocks_framing.md (Task 4 reviewer-facing communication)"
      to: "reviewers reading the resubmission package"
      via: "the `## Marginal-convergence finding (post-r2 investigation)` subsection in reviewer_response.md is the primary reviewer-facing landing point; methods_full.md carries the 2-sentence cross-reference at the end of the OD-scale evaluation section; paper_blocks_framing.md (if a natural insertion point exists) carries a one-sentence reference in the PAPER-01 per-model framing block; the message: 8/9 models recover the OD marginal and the architecture-discriminating signal lives in dependence structure (ACF, conditional moments, TimeGAN-style scores), so reviewers should read OD-marginal-EMD numbers alongside those figures (`training_convergence_all_models.png`, `failure_modes_summary.png`, `seed_variance_per_model.png`, `methods_full.md §3.x`) NOT alone"
      pattern: "Marginal-convergence finding|architecture discrimination|dependence structure|≤0\\.03 OD-units"
    - from: "revision/docs/peer_review_remediation.md (Task 4 new R2 follow-up subsection)"
      to: "future readers auditing the post-r2 investigation"
      via: "the new `## R2 follow-up — marginal-convergence finding (Plan 14-15)` subsection enumerates (a) the user-observed visual concern, (b) the empirical verification result (4.44e-16 routing match), (c) the link to `qq_overlay.png` and the new 3-column comparable-variants table; the existing 14-13 finding-to-commit tables + 14-14 v2.1-limitations / R2-follow-up-sweep sections preserved verbatim; this completes the audit trail from `user visually-similar QQ observation` → `4.44e-16 routing verification` → `qq_overlay + 3-column table` → `reviewer-facing communication`"
      pattern: "R2 follow-up — marginal-convergence finding|4\\.44e-16|qq_overlay\\.png|3-column"
    - from: "Task 5 v2.1 gate run against 10 paper-facing docs"
      to: "all numerical literals introduced by 14-15 (histogram-density EMD values; 0.03 / 0.69 marginal-convergence numbers; 4.44e-16 routing-verification number)"
      via: "the v2.1 gate (D-14-16 byte-frozen post-14-14 T1) auto-walks `distribution_emd.json` + `qq_overlay.json` into the resolution corpus via the existing `_json_blobs()` walker; histogram-density EMD literals in `reconciliation_note.md` resolve to `distribution_emd.json#aggregates`; the `0.03` and `0.69` literals in `reviewer_response.md` + `methods_full.md` resolve to `qq_overlay.json#convergence_stats`; the `4.44e-16` literal (routing-verification floating-point match) resolves to the empirical verification record (embedded in `qq_overlay.json#routing_verification` or a sibling `verification_log.json` per T1/T3 design choice)"
      pattern: "verify_number_provenance\\.py|--target|distribution_emd\\.json#aggregates|qq_overlay\\.json#convergence_stats"
---

<objective>
Reintroduce the pre-v1.0 50-bin histogram-density Wasserstein EMD as a third
comparable metric column in `revision/docs/reconciliation_note.md` (alongside
the existing OD raw-sample EMD and the log-return raw-sample EMD already
present in `matched2000_dualscale.json`), emit a single 9-model QQ overlay
figure (Option A — `qq_overlay.png/pdf/json`) with a delta-QQ panel that makes
the OD-marginal convergence visually obvious, and update reviewer-facing
docs (`reviewer_response.md`, `methods_full.md`, optionally
`paper_blocks_framing.md`, and `peer_review_remediation.md`) with the
post-r2 marginal-convergence finding (8/9 models recover the OD marginal to
within ≤0.03 OD-units max-quantile-diff; wgan_cnn excepted at ~0.69
upper-tail) so reviewers understand the OD-marginal QQ is NOT the
discriminating axis between architectures at the matched-2000ep budget and
the architecture-discriminating signal lives in dependence structure (ACF,
conditional moments, TimeGAN-style scores). NO retraining; NO `revision/core/`
edits (D-14-22 byte-freeze preserved); NO gate edit (D-14-16 v2.1 byte-freeze
preserved); the new `revision/run_distribution_emd.py` emitter lives at the
top level (NOT in `revision/core/`); `revision/core/eval.py:25-36`'s v1.0
raw-sample `compute_emd` is byte-untouched; the new `distribution_emd.json` is
auto-walked into the v2.1 gate's resolution corpus by the existing
`_json_blobs()` walker without any gate edit; the gate runs against
`reconciliation_note.md` PASSING after T2 lands.

Purpose: 14-13 + 14-14 closed all 42 actionable peer-review findings across
r1 + r2 passes and were rated `ready for paper resubmission YES`. Two
post-14-14 findings from the user's own review of the resubmission package
prompt this plan: (1) the original pre-v1.0 paper reported the 50-bin
histogram-density Wasserstein and 14-13 T3 acknowledged in `reconciliation_note.md`'s
C-3 disclosure that the v1.0 raw-sample metric is not commensurate with the
original — the user wants the histogram-density column reintroduced so
reviewers can directly read the matched-2000ep numbers in the original
paper's metric for the first time since the v1.0 metric switch; (2) all 9
per-model QQ plots look near-identical, and the user wanted empirical
verification this isn't an artifact — the 4.44e-16 floating-point match
between independent reconstruction (from `samples.npy`) and the rendered JSON
companions confirmed routing is correct AND that 8/9 models genuinely
recover the OD marginal, which is a legitimate finding that needs to be
communicated to reviewers so they don't read the OD-marginal-EMD column
as the architecture-discriminating axis. Pure additive work — no retraining,
no `revision/core/` edits, no gate edits.

Output: 1 new top-level emitter (`revision/run_distribution_emd.py`) + 1 new
aggregator JSON (`distribution_emd.json`) + 1 updated emitter
(`revision/run_model_info.py`) + 1 re-emitted reconciliation_note.md
(existing OLD/NEW table preserved verbatim, new 3-column comparable-variants
table appended, C-3 disclosure paragraph extended) + 1 new 2-panel
discriminating figure (`qq_overlay.{png,pdf,json}`) + 9 per-model QQ JSON
companions gaining additive `caption_note` field (per-model PNG/PDFs
NOT regenerated) + 1 new subsection in reviewer_response.md + 1 cross-reference
in methods_full.md + 1 conditional reference in paper_blocks_framing.md +
1 new subsection in peer_review_remediation.md + 1 new section in
completeness_sweep_manifest.md + 1 SUMMARY. All gated by the v2.1 provenance
gate (D-14-16 byte-frozen from 14-14 T1) on all 10 paper-facing docs;
`revision/core/` byte-frozen (D-14-22); no retraining; no new figures beyond
`qq_overlay`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-paper-revision-release-freeze/14-CONTEXT.md
@.planning/phases/14-paper-revision-release-freeze/14-PATTERNS.md
@.planning/phases/14-paper-revision-release-freeze/14-13-PLAN.md
@.planning/phases/14-paper-revision-release-freeze/14-13-SUMMARY.md
@.planning/phases/14-paper-revision-release-freeze/14-14-PLAN.md
@.planning/phases/14-paper-revision-release-freeze/14-14-SUMMARY.md

<contract_changes>
Explicit deviations from prior Phase 14 contracts (mirrors the contract
changes section in 14-13 / 14-14 but narrower — 14-15 is a pure additive
plan: one new emitter + one new figure + reviewer-facing comms):

- **D-14-22 (`revision/core/` byte-freeze) PRESERVED across all 5 tasks.**
  The histogram-density EMD lives in a NEW top-level emitter
  `revision/run_distribution_emd.py`, NOT in `revision/core/eval.py`. The core
  `compute_emd` at `revision/core/eval.py:25-36` (the v1.0 raw-sample
  Wasserstein) is byte-untouched. Every task's verify gate asserts
  `[ -z "$(git diff --stat revision/core/)" ]`.

- **D-14-13 (strict-accept gate) PRESERVED.** The new distribution-EMD column
  is INFORMATIONAL only. No new gate threshold; no edit to the existing
  `_strict_accept` equality semantics (the 14-13 T4 / 14-14 T2 device-manifest
  equality check is untouched).

- **D-14-16 (gate v2.1 byte-freeze) PRESERVED.** No edit to
  `revision/verify_number_provenance.py`. The new `distribution_emd.json` and
  `qq_overlay.json` are auto-walked into the v2.1 resolution corpus by the
  existing `_json_blobs()` walker introduced in 14-13 T2 + lookbehind-fixed
  in 14-14 T1. The gate runs against `reconciliation_note.md` PASSING after
  T2 lands without any gate edit.

- **D-14-18 (Overleaf-canonical LaTeX read-only) PRESERVED.** No edits to
  `main (4) copy.tex` / `supp_material.tex`. The reviewer-facing docs in
  `revision/docs/` carry the marginal-convergence message.

- **The 50-bin histogram-density formulation is FIXED.**
  `wasserstein_distance(bin_centers, bin_centers, real_hist_density,
  fake_hist_density)` with `n_bins=50`, `np.histogram(..., density=True)`,
  and bin edges taken from the real distribution and reused for the fake
  (so the two histograms share x-axis support). This matches the C-3
  disclosure citation in `reconciliation_note.md` word-for-word so the
  metric formulation already disclosed to reviewers is now also the metric
  formulation actually computed for the new comparable-variants column.

- **NO new ACF / conditional-moment / TimeGAN-style figures.** The plan
  POINTS reviewers to EXISTING such figures (`training_convergence_all_models.png`
  from 14-10, `failure_modes_summary.png` from 14-10, `seed_variance_per_model.png`
  from 14-10, `methods_full.md §3.x` from 14-11/14-14). The only new figure
  is `qq_overlay.png/pdf/json` (Option A — single 9-model overlay + delta-QQ
  panel).

- **NO retraining.** All metrics read existing per-seed samples from
  `revision/results/matched2000/runs/{model}/{seed}/samples.npy`. The QQ
  overlay reads the same samples. Both are render-only emitters.

- **Distribution-EMD is INFORMATIONAL, not gated.** D-14-13 (strict-accept
  gate) is unchanged. No new gate threshold is introduced.
</contract_changes>

<interfaces>
<!-- All upstream artifacts (Phases 9-14 through 14-14) are EXISTING and
     AUDITED on disk; this plan only adds 1 emitter + 1 JSON + 1 figure
     (with .png/.pdf/.json triplet) + 9 per-model JSON additive fields + 4
     reviewer-facing doc appends + 1 new section in completeness_sweep_manifest.md
     + 1 SUMMARY. -->

revision/core/eval.py:25-36 (READ-ONLY contrast; D-14-22 byte-untouched):
- `compute_emd(real, fake)` — the v1.0 raw-sample Wasserstein
  (`scipy.stats.wasserstein_distance(real, fake)`). This is the function the
  v1.0 release replaced the pre-v1.0 50-bin histogram-density metric with.
  The new `compute_histogram_density_emd` in `run_distribution_emd.py` is the
  contrast — same library, different formulation, both reported side-by-side
  in `reconciliation_note.md`'s new comparable-variants table. NO EDIT.

revision/docs/reconciliation_note.md C-3 disclosure paragraph (READ-FIRST):
- The metric-redefinition disclosure paragraph added in 14-13 T3 cites the
  50-bin density Wasserstein formulation verbatim: the new T1 emitter's
  `metric_formulation` JSON field must match this citation
  word-for-word so reviewers can verify the formulation actually computed
  matches the formulation disclosed.

revision/run_figure_suite.py (READ-FIRST + EXTEND in T3):
- Lines 106-116: `MODEL_ORDER` (9 production model_kinds: ar,
  iqp_sel_55_repro, V1, V2, V3, vae, wgan_cnn, wgan_lstm, wgan_mlp; plus
  `MODEL_COLORS` palette starting at line 128).
- Lines 261-296: `reconstruct_od(repo, model, seed)` — the OD-scale
  reconstruction helper reused by T1 (`run_distribution_emd.py` imports it)
  and T3 (`render_qq_overlay` uses it via the existing caller-loop path).
- Lines 403-433: `render_qq_plot(model, od, real_flat, ...)` — the existing
  per-model QQ renderer; the new `render_qq_overlay` in T3 mirrors its
  structure (same scatter pattern, same color-key lookup, same JSON
  companion convention) but on a single shared-axes plot with the delta-QQ
  panel.
- Lines 2378-2400: the caller loop at the end of the figure suite where
  T3 inserts one new call: `render_qq_overlay(MODEL_ORDER, real_flat, figures_dir)`.

revision/results/matched2000/runs/{model}/{seed}/samples.npy (READ-ONLY
data source):
- Per-seed sample arrays for 9 model_kinds × 5 seeds (42, 43, 44, 45, 46);
  T1 loads these via `np.load`, reconstructs the OD scale via
  `reconstruct_od`, and computes both OD-scale and log-return-scale
  histogram-density EMD. T3 reads the same files for the QQ overlay.
  These are the 14-02/03/04 sample artifacts already on disk; no
  retraining.

revision/run_model_info.py:220-302 (READ-FIRST + EXTEND in T2):
- The `reconciliation_note.md` emit site. The existing OD-scale headline
  table (with OLD/NEW headline-trajectory framing from 14-13) is at this
  range; T2 PRESERVES the existing table verbatim and APPENDS a new
  `## EMD comparable across metric variants (matched 2000ep budget)` section
  with the 3-column table. T2 also extends the C-3 disclosure paragraph by
  one sentence acknowledging the histogram-density reintroduction.

revision/results/matched2000_dualscale.json#aggregates (READ-ONLY source for
T2 columns 1 and 2):
- Existing aggregator JSON from 14-08. Carries per-(model_kind, scale)
  mean/std/n for both `scale='OD'` and `scale='log_return'` with
  `metric_name='emd'`. T2 sources columns 1 (OD raw-sample EMD) and 2
  (log-return raw-sample EMD) from this JSON; column 3 (50-bin
  histogram-density EMD) comes from the new `distribution_emd.json`.

revision/docs/reviewer_response.md (READ-FIRST + APPEND in T4):
- Read the doc to find the natural insertion point — probably after the
  R2 follow-up section if one exists, otherwise after the last R1-XX
  response. T4 appends `## Marginal-convergence finding (post-r2
  investigation)` as a new subsection.

revision/docs/methods_full.md (READ-FIRST + APPEND in T4):
- Read the doc to find the OD-scale evaluation section (whichever section
  discusses per-model EMD results, likely around §3.x.d carried over from
  14-14 T3). T4 appends a 2-sentence cross-reference at the end of that
  section.

revision/docs/paper_blocks_framing.md (READ-FIRST + CONDITIONAL APPEND in T4):
- Read the doc to determine if there's a natural insertion point in the
  PAPER-01 keyed framing block discussing per-model results. If yes, T4
  appends a one-sentence reference. If no, T4 skips this file and surfaces
  the conditional outcome in the commit body.

revision/docs/peer_review_remediation.md (READ-FIRST + APPEND in T4):
- The R2 follow-up table from 14-14 T5. T4 appends a new
  `## R2 follow-up — marginal-convergence finding (Plan 14-15)` subsection
  below the existing 14-13 + 14-14 sections (which are preserved verbatim).

revision/docs/completeness_sweep_manifest.md (APPEND in T5):
- The cross-plan navigation doc. T5 appends a new `## Plan 14-15` section
  at the bottom enumerating T1–T5 deliverables. Existing 14-09 / 14-10 /
  14-11 / 14-12 / 14-13 / 14-14 sections preserved verbatim.

revision/verify_number_provenance.py (READ-ONLY; D-14-16 byte-freeze
preserved):
- The v2.1 gate (from 14-14 T1). Carries the negative-sign-aware lookbehind
  `(?<![-\d.])` at line 193. T5 invokes it against all 10 paper-facing docs.
  No edit.

revision/results/figures/qq_{ar,iqp_sel_55_repro,V1,V2,V3,vae,wgan_cnn,wgan_lstm,wgan_mlp}.json
(EXISTING companions; APPENDED in T3):
- 9 per-model QQ JSON companions from 14-04 / 14-10. T3 re-emits each with
  an additive `caption_note` field per the per-model template; all other
  fields preserved verbatim; per-model PNG/PDFs are NOT regenerated.

Critical files to read before each task (per the approved plan; preserved
verbatim):

- **T1:** `revision/core/eval.py:25-36` (the v1.0 raw-sample compute_emd —
  for parity API and as a contrast); `revision/docs/reconciliation_note.md`
  (the C-3 disclosure paragraph for the exact formulation citation);
  `revision/run_figure_suite.py:261-296` (the `reconstruct_od` helper to
  reuse); `revision/results/matched2000/runs/{model}/42/samples.npy` (1-2
  files for shape sanity).
- **T2:** `revision/run_model_info.py:220-302` (the reconciliation_note.md
  emit site); `revision/results/matched2000_dualscale.json#aggregates` (the
  OD + log_return raw-sample EMD source); the new `distribution_emd.json`
  from T1.
- **T3:** `revision/run_figure_suite.py:403-433` (the existing
  `render_qq_plot` for per-model plots — the overlay should follow the same
  patterns); `:261-296` (`reconstruct_od`); `:106-116` (MODEL_ORDER);
  `:2378-2400` (the caller loop); the existing `qq_{model}.json` companions
  for stats schema reference.
- **T4:** `revision/docs/reviewer_response.md` (find the right insertion
  point — probably after the R2 follow-up section if one exists, otherwise
  after the last R1-XX response); `revision/docs/methods_full.md` (find the
  OD-scale evaluation section); `revision/docs/paper_blocks_framing.md`
  (determine if there's a natural insertion point; skip if not);
  `revision/docs/peer_review_remediation.md` (the R2 follow-up table —
  append a new sub-row for 14-15).
- **T5:** All 10 paper-facing docs; the new `qq_overlay.json`;
  `distribution_emd.json`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Histogram-density distribution-EMD aggregator (new emitter, D-14-22-safe)</name>
  <files>revision/run_distribution_emd.py, revision/results/distribution_emd.json</files>
  <read_first>
    - revision/core/eval.py:25-36 (the v1.0 raw-sample `compute_emd` via `scipy.stats.wasserstein_distance(real, fake)`; this is the contrast — same library, different formulation; the new emitter's `compute_histogram_density_emd` lives at the top level NOT in core; READ-ONLY, NOT to be edited)
    - revision/docs/reconciliation_note.md (read the C-3 metric-redefinition disclosure paragraph from 14-13 T3; the new `metric_formulation` field in `distribution_emd.json` MUST match the citation in this paragraph word-for-word: `scipy.stats.wasserstein_distance(bin_centers, bin_centers, real_hist_density, fake_hist_density) over 50-bin histograms (np.histogram(..., density=True))`)
    - revision/run_figure_suite.py:261-296 (the `reconstruct_od(repo, model, seed)` helper — import this from the new emitter; do NOT duplicate the implementation; the import pattern is `from revision.run_figure_suite import reconstruct_od` if the repo is on PYTHONPATH, otherwise an importlib path import)
    - revision/run_figure_suite.py:106-116 (`MODEL_ORDER`; the 9 production model_kinds: ar, iqp_sel_55_repro, V1, V2, V3, vae, wgan_cnn, wgan_lstm, wgan_mlp; import or re-declare in the emitter — prefer import for single-source-of-truth)
    - revision/results/matched2000/runs/iqp_sel_55_repro/42/samples.npy + one other (e.g. revision/results/matched2000/runs/wgan_cnn/42/samples.npy) — load 1-2 files for shape sanity; verify the shape matches the `reconstruct_od` helper's expectations (the helper handles the OD reconstruction from log-return samples)
    - revision/results/matched2000_dualscale.json (read-only; the existing aggregator carries per-(model_kind, scale) `metric_name='emd'` aggregates with `ddof=1` std — match that convention exactly when emitting the new `distribution_emd.json` aggregates)
    - .planning/phases/14-paper-revision-release-freeze/14-CONTEXT.md (confirm D-14-22 `revision/core/` byte-freeze is locked across this plan; the new emitter MUST live at the top level NOT under core)
  </read_first>
  <action>
    Create the new top-level emitter `revision/run_distribution_emd.py` and
    emit `revision/results/distribution_emd.json` in one pass. The emitter
    implements the pre-v1.0 50-bin histogram-density Wasserstein verbatim
    per the formulation cited in the C-3 disclosure paragraph of
    `reconciliation_note.md`. NO edit to `revision/core/`.

    Step A — Create `revision/run_distribution_emd.py` (NEW top-level emitter,
    NOT in `revision/core/`):

    Module-level docstring states the pre-v1.0 50-bin density Wasserstein
    formulation verbatim, cites `revision/core/eval.py:25-36` as the v1.0
    raw-sample contrast, and notes that the metric IS commensurate with the
    deprecated v1.0-pre paper headline on the SAME real-data slice and SAME
    50-bin convention for the first time since the v1.0 raw-sample switch.

    Define the function `compute_histogram_density_emd(real, fake, n_bins=50)`
    with the exact body called out in the approved plan: take the real
    distribution's histogram (with `density=True`), reuse the same edges for
    the fake distribution, take the bin centers (midpoints of the edges),
    and call `scipy.stats.wasserstein_distance(bin_centers, bin_centers,
    real_hist, fake_hist)`. Return as a Python float. Document the function
    signature, raise on shape mismatch / empty input, and add a one-line
    parity comment pointing to the contrast at `revision/core/eval.py:25-36`.

    Step B — Iterate MODEL_ORDER × {42, 43, 44, 45, 46} and load samples:

    Import `MODEL_ORDER` and `reconstruct_od` from
    `revision.run_figure_suite` (use an importlib path import if the repo
    isn't on PYTHONPATH — the script must run from a clean `./qgan_env/bin/python`
    invocation with the repo root as CWD).

    For each `(model, seed)` pair:
    1. Load `revision/results/matched2000/runs/{model}/{seed}/samples.npy`
       via `np.load`. Confirm the array is non-empty.
    2. Reconstruct OD-scale via `reconstruct_od(repo, model, seed)` (re-using
       the helper from the figure suite). The repo root is the parent of
       `revision/`.
    3. Flatten real (the same `real_flat` the figure suite uses) and fake
       arrays for the OD scale; compute `histogram_density_emd_OD` via
       `compute_histogram_density_emd(real_OD_flat, fake_OD_flat, n_bins=50)`.
    4. Repeat for the log-return scale: flatten real and fake log-return
       arrays (samples are stored as log-returns natively; no reconstruction
       needed); compute `histogram_density_emd_logreturn`.

    Append two rows per (model, seed) to a `rows` list:
    `{"model_kind": model, "seed": seed, "scale": "OD", "value": histogram_density_emd_OD}`
    and the matching log_return row.

    If `revision/results/matched2000/runs/iqp_sel_55_headline/42/samples.npy`
    exists (the n=1 headline), append two additional rows for `model_kind:
    "iqp_sel_55_headline"`, otherwise record a top-level
    `headline_present: false` note in the JSON header.

    Step C — Aggregate per (model_kind, scale) with `ddof=1` std:

    For each `(model_kind, scale)` group, compute `mean`, `std` with
    `np.std(..., ddof=1)` (matching the 14-13 T3 sample-std convention; the
    `ddof=1` switch from `ddof=0` was the H1-2 fix in 14-13), and `n` (the
    number of seeds that contributed). Append to an `aggregates` list:
    `{"model_kind": model_kind, "scale": scale, "mean": ..., "std": ..., "n": 5}`.

    Step D — Emit `revision/results/distribution_emd.json`:

    Top-level fields, in this order for human readability:

    ```json
    {
      "schema": "distribution-emd v1 (Phase 14 plan 14-15)",
      "metric_formulation": "scipy.stats.wasserstein_distance(bin_centers, bin_centers, real_hist_density, fake_hist_density) over 50-bin histograms (np.histogram(..., density=True))",
      "data_hash": "91e447d4624e25b3",
      "n_bins": 50,
      "headline_present": <true|false>,
      "rows": [...],
      "aggregates": [...]
    }
    ```

    The `metric_formulation` string MUST match the C-3 disclosure citation in
    `reconciliation_note.md` word-for-word (verify by visually diffing the two
    strings before committing).

    Use `json.dump(..., indent=2, sort_keys=False)` to preserve the field
    order above; emit with a trailing newline.

    Step E — CLI entry-point + sanity self-test:

    Provide `if __name__ == "__main__":` block that parses `--dry-run`
    (emit to a temp path and verify shape without overwriting the canonical
    JSON) and runs the full emit otherwise. Include a small self-test that
    asserts `compute_histogram_density_emd(real_flat, real_flat, n_bins=50) == 0.0`
    (a sample distribution against itself has zero EMD) before the main loop
    runs.

    Step F — Run the emitter and verify the output:

    ```
    ./qgan_env/bin/python revision/run_distribution_emd.py
    ```

    Verify `revision/results/distribution_emd.json` is well-formed JSON,
    carries all top-level fields, has 9 model_kinds × 5 seeds × 2 scales =
    90 rows (or 92 if headline_present, n=1 per scale), and per-(model_kind,
    scale) aggregates with `n: 5`. Note that 9 × 2 = 18 aggregate entries are
    expected (or 20 if headline_present).

    Commit message: `feat(14-15): histogram-density distribution-EMD aggregator (50-bin Wasserstein, pre-v1.0 formulation reintroduced)`.
  </action>
  <verify>
    <automated>./qgan_env/bin/python revision/run_distribution_emd.py &amp;&amp; ./qgan_env/bin/python -c "import json,pathlib,sys; j=json.loads(pathlib.Path('revision/results/distribution_emd.json').read_text()); errs=[]; (j.get('schema','').startswith('distribution-emd v1 (Phase 14 plan 14-15)')) or errs.append('schema string wrong'); (j.get('data_hash')=='91e447d4624e25b3') or errs.append('data_hash mismatch'); (j.get('n_bins')==50) or errs.append('n_bins != 50'); ('bin_centers, bin_centers, real_hist_density, fake_hist_density' in j.get('metric_formulation','')) or errs.append('metric_formulation does not match C-3 disclosure citation'); rows=j.get('rows',[]); aggs=j.get('aggregates',[]); (len(rows)>=90) or errs.append(f'rows<90: got {len(rows)}'); (len(aggs)>=18) or errs.append(f'aggregates<18: got {len(aggs)}'); (all(a.get('n',0)>=1 for a in aggs)) or errs.append('some aggregate has n=0'); scales=set(a.get('scale') for a in aggs); ({'OD','log_return'}.issubset(scales)) or errs.append(f'missing scale, got {scales}'); print('errors',errs); sys.exit(0 if not errs else 1)" &amp;&amp; ./qgan_env/bin/python -c "import json,re,pathlib; j=json.loads(pathlib.Path('revision/results/distribution_emd.json').read_text()); doc=pathlib.Path('revision/docs/reconciliation_note.md').read_text(); mf=j['metric_formulation']; assert 'bin_centers' in mf and '50-bin' in mf and 'density=True' in mf, f'metric_formulation incomplete: {mf}'; print('C-3 formulation citation match OK')" &amp;&amp; [ -z "$(git diff --stat revision/core/)" ] &amp;&amp; [ ! -f revision/core/run_distribution_emd.py ]</automated>
  </verify>
  <acceptance_criteria>
    - `revision/run_distribution_emd.py` exists at the repo top level (NOT under `revision/core/`); contains `compute_histogram_density_emd(real, fake, n_bins=50)` with the exact body called out in the approved plan
    - Module docstring states the pre-v1.0 50-bin density Wasserstein formulation verbatim and cites `revision/core/eval.py:25-36` as the v1.0 raw-sample contrast
    - `revision/results/distribution_emd.json` exists with schema string `"distribution-emd v1 (Phase 14 plan 14-15)"`, `metric_formulation` field matching the C-3 disclosure citation in `reconciliation_note.md` word-for-word, `data_hash: "91e447d4624e25b3"`, `n_bins: 50`
    - JSON `rows` has 9 model_kinds × 5 seeds × 2 scales = 90 entries (or 92 if `headline_present: true`); `aggregates` has at least 18 per-(model_kind, scale) entries with `mean`, `std` (ddof=1), and `n` per group
    - The self-test in the `__main__` block asserts `compute_histogram_density_emd(real_flat, real_flat) == 0.0` before the main loop runs (sample distribution against itself has zero EMD)
    - `git diff --stat revision/core/` is empty (D-14-22 byte-freeze preserved)
    - No file exists at `revision/core/run_distribution_emd.py` (the emitter MUST be at the top level)
  </acceptance_criteria>
  <done>Histogram-density EMD aggregator created at top level (NOT in core); `distribution_emd.json` emitted with 9-model × 5-seed × 2-scale rows + per-(model_kind, scale) aggregates with `ddof=1` std; `metric_formulation` field matches the C-3 disclosure citation word-for-word; D-14-22 byte-freeze preserved.</done>
</task>

<task type="auto">
  <name>Task 2: `reconciliation_note.md` 3-column comparable metric table</name>
  <files>revision/run_model_info.py, revision/docs/reconciliation_note.md</files>
  <read_first>
    - revision/run_model_info.py:220-302 (the `reconciliation_note.md` emit site; the existing `## EMD (OD scale) — final-eval mean over seeds 42-46` table emit code with OLD/NEW headline-trajectory framing — this section MUST be preserved verbatim; the new `## EMD comparable across metric variants (matched 2000ep budget)` section is APPENDED after it)
    - revision/results/matched2000_dualscale.json#aggregates (the existing per-(model_kind, scale) `metric_name='emd'` aggregates with both `scale='OD'` and `scale='log_return'`; mean/std/n with `ddof=1` std per 14-13 T3 convention; T2 reads columns 1 and 2 from this JSON)
    - revision/results/distribution_emd.json (the new T1 output; T2 reads column 3 from `aggregates[*, scale='OD']`)
    - revision/docs/reconciliation_note.md (current state of the doc; find the C-3 metric-redefinition disclosure paragraph from 14-13 T3 and locate the spot to APPEND the new comparable-variants section and the spot in the C-3 paragraph to APPEND the one-sentence histogram-density acknowledgment)
    - .planning/phases/14-paper-revision-release-freeze/14-13-SUMMARY.md (read the 14-13 T3 description of the C-3 disclosure paragraph; the new sentence appended in T2 must thread into the existing language naturally without contradicting the prior disclosure)
    - .planning/phases/14-paper-revision-release-freeze/14-CONTEXT.md (confirm D-14-13 strict-accept gate is preserved — the new distribution-EMD column is INFORMATIONAL only)
  </read_first>
  <action>
    Update `revision/run_model_info.py` to emit a new 3-column
    comparable-variants section in `reconciliation_note.md` and extend the
    C-3 disclosure paragraph. PRESERVE the existing OD-scale headline table
    verbatim.

    Step A — Update `revision/run_model_info.py` (around lines 220-302):

    Add a new emit function (or extend the existing one) that sources the
    three columns from the existing + new JSONs:

    - **Column 1 — OD raw-sample EMD** ← `matched2000_dualscale.json#aggregates[*, scale='OD', metric_name='emd']` (per-(model_kind, scale='OD') mean with `ddof=1` std)
    - **Column 2 — log-return raw-sample EMD** ← `matched2000_dualscale.json#aggregates[*, scale='log_return', metric_name='emd']` (per-(model_kind, scale='log_return') mean)
    - **Column 3 — histogram-density distribution EMD (50-bin, OD scale)** ← `distribution_emd.json#aggregates[*, scale='OD']` (per-(model_kind, scale='OD') mean)

    Each row's `source: distribution-EMD` cell carries the resolution
    pointer: `distribution_emd.json#aggregates (model_kind=<m>, scale=OD); 50-bin density Wasserstein (pre-v1.0 formulation)`.

    Sort rows by `MODEL_ORDER` from `revision/run_figure_suite.py:106-116`.
    Headline `iqp_sel_55_headline` row appears LAST as a separate
    sub-section row if `headline_present: true` in `distribution_emd.json`.

    Step B — APPEND the new `## EMD comparable across metric variants
    (matched 2000ep budget)` section AFTER the existing `## EMD (OD scale)`
    section in `reconciliation_note.md`:

    The new section table format (markdown):

    ```markdown
    ## EMD comparable across metric variants (matched 2000ep budget)

    [Brief 1-2 sentence intro explaining what each column shows and that
    these are mean-over-seeds-42-46 with ddof=1 std (also reported in the
    aggregator JSONs).]

    | model | OD raw-sample EMD | log-return raw-sample EMD | histogram-density EMD (50-bin, OD scale) | source: distribution-EMD |
    |---|---|---|---|---|
    | iqp_sel_55_repro | 0.0275 | 0.121X | 0.00YY | distribution_emd.json#aggregates (model_kind=iqp_sel_55_repro, scale=OD); 50-bin density Wasserstein (pre-v1.0 formulation) |
    | ... (one row per model in MODEL_ORDER) ... |
    ```

    (The exact numerical literals are emitted from the JSON aggregates;
    placeholders shown for table-format reference only.)

    Step C — Append a one-sentence cross-reference at the END of the
    existing `## EMD (OD scale) — final-eval mean over seeds 42-46` section
    pointing to the new comparable-variants section:

    > See `## EMD comparable across metric variants (matched 2000ep budget)`
    > below for the 3-column comparison that adds the log-return raw-sample
    > EMD and the 50-bin histogram-density EMD (the pre-v1.0 paper metric
    > reintroduced per Plan 14-15).

    Step D — EXTEND the C-3 metric-redefinition disclosure paragraph with
    ONE sentence acknowledging the histogram-density reintroduction:

    > The matched-2000ep histogram-density EMD on OD scale (now reported in
    > the new `## EMD comparable across metric variants` section below) is
    > computed on the SAME real-data slice and SAME 50-bin convention as
    > the deprecated v1.0-pre metric, so it IS commensurate with the
    > pre-v1.0 paper headline (~0.0015) for the first time since the v1.0
    > raw-sample switch (Plan 14-15).

    The sentence threads into the existing 14-13 T3 disclosure paragraph
    without contradicting it (the prior disclosure stated the v1.0
    raw-sample metric is not commensurate with the pre-v1.0 paper headline;
    the new sentence adds that the histogram-density column REinstates a
    commensurate metric).

    Step E — Re-emit `reconciliation_note.md` via the updated
    `revision/run_model_info.py`:

    ```
    ./qgan_env/bin/python revision/run_model_info.py
    ```

    Verify the doc carries BOTH the existing `## EMD (OD scale) — final-eval
    mean over seeds 42-46` table (preserved verbatim with its OLD/NEW
    headline-trajectory framing) AND the new `## EMD comparable across
    metric variants (matched 2000ep budget)` section with the 3-column
    table. Verify the cross-reference sentence between them is present and
    the C-3 disclosure paragraph carries the appended histogram-density
    sentence.

    Step F — Run the v2.1 gate against `reconciliation_note.md` and verify
    the new numerical literals (9 model rows × histogram-density value)
    resolve to `distribution_emd.json` via the boundary-strict +
    ε-neighborhood path:

    ```
    ./qgan_env/bin/python revision/verify_number_provenance.py --target revision/docs/reconciliation_note.md --manifest
    ```

    The v2.1 gate is byte-frozen post-14-14 T1; the new aggregator JSON is
    auto-walked into the resolution corpus by the existing `_json_blobs()`
    walker; no gate edit needed.

    Commit message: `feat(14-15): reconciliation_note.md 3-column comparable metric table (OD raw / log-return raw / 50-bin histogram-density EMD)`.
  </action>
  <verify>
    <automated>./qgan_env/bin/python revision/run_model_info.py &amp;&amp; ./qgan_env/bin/python -c "import pathlib,sys; d=pathlib.Path('revision/docs/reconciliation_note.md').read_text(); errs=[]; ('## EMD (OD scale)' in d) or errs.append('existing OD-scale headline table missing — must be preserved verbatim'); ('## EMD comparable across metric variants' in d) or errs.append('new comparable-variants section missing'); ('histogram-density EMD (50-bin' in d) or errs.append('histogram-density column header missing'); ('distribution_emd.json#aggregates' in d) or errs.append('distribution-EMD source pointer missing in table'); ('See `## EMD comparable across metric variants' in d or 'See ## EMD comparable across metric variants' in d or 'comparable across metric variants' in d) or errs.append('cross-reference sentence missing in existing table'); ('SAME real-data slice and SAME 50-bin convention' in d or '50-bin convention' in d) or errs.append('C-3 disclosure extension sentence missing'); ('pre-v1.0 paper headline' in d or 'pre-v1.0' in d) or errs.append('C-3 extension does not reference pre-v1.0 headline'); print('errors',errs); sys.exit(0 if not errs else 1)" &amp;&amp; ./qgan_env/bin/python revision/verify_number_provenance.py --target revision/docs/reconciliation_note.md &amp;&amp; [ -z "$(git diff --stat revision/core/)" ]</automated>
  </verify>
  <acceptance_criteria>
    - `revision/run_model_info.py` updated with the new 3-column emit logic sourcing columns 1 + 2 from `matched2000_dualscale.json#aggregates` and column 3 from `distribution_emd.json#aggregates`
    - `revision/docs/reconciliation_note.md` carries BOTH the existing `## EMD (OD scale) — final-eval mean over seeds 42-46` table (preserved verbatim with OLD/NEW headline-trajectory framing) AND the new `## EMD comparable across metric variants (matched 2000ep budget)` section with the 3-column table
    - The cross-reference sentence between the two sections is present (existing OD-scale section's last paragraph points to the new comparable-variants section)
    - The C-3 metric-redefinition disclosure paragraph carries the appended one-sentence histogram-density acknowledgment (matched-2000ep histogram-density EMD on OD scale is commensurate with the pre-v1.0 paper headline ~0.0015 for the first time since the v1.0 raw-sample switch)
    - v2.1 gate PASSES on `reconciliation_note.md`; the new histogram-density EMD literals resolve to `distribution_emd.json` via the boundary-strict + ε-neighborhood path
    - `git diff --stat revision/core/` is empty (D-14-22 preserved)
  </acceptance_criteria>
  <done>3-column comparable EMD table appended to `reconciliation_note.md`; existing OD-scale headline table preserved verbatim; cross-reference sentence in place; C-3 disclosure paragraph extended; v2.1 gate PASSES against the doc; D-14-22 preserved.</done>
</task>

<task type="auto">
  <name>Task 3: QQ overlay figure (Option A — 9-model overlay + delta-QQ panel) + per-model JSON caption_note</name>
  <files>revision/run_figure_suite.py, revision/results/figures/qq_overlay.png, revision/results/figures/qq_overlay.pdf, revision/results/figures/qq_overlay.json, revision/results/figures/qq_ar.json, revision/results/figures/qq_iqp_sel_55_repro.json, revision/results/figures/qq_V1.json, revision/results/figures/qq_V2.json, revision/results/figures/qq_V3.json, revision/results/figures/qq_vae.json, revision/results/figures/qq_wgan_cnn.json, revision/results/figures/qq_wgan_lstm.json, revision/results/figures/qq_wgan_mlp.json</files>
  <read_first>
    - revision/run_figure_suite.py:403-433 (the existing `render_qq_plot(model, od, real_flat, ...)` function; the new `render_qq_overlay` MUST follow the same patterns — same scatter style, same color-key lookup via `MODEL_COLORS.get(model, "#0072B2")`, same JSON companion convention with caption + data_hash + render_only + per-model stats)
    - revision/run_figure_suite.py:261-296 (the `reconstruct_od` helper — the overlay function uses this via the existing caller-loop path; do NOT duplicate the implementation)
    - revision/run_figure_suite.py:106-116 (`MODEL_ORDER` — the 9 production model_kinds; the overlay legend lists all 9 in this exact order)
    - revision/run_figure_suite.py:128-160 (`MODEL_COLORS` — the palette to reuse for color-coding; do NOT introduce a new palette)
    - revision/run_figure_suite.py:2378-2400 (the caller loop at the end of the figure suite where the new `render_qq_overlay(MODEL_ORDER, real_flat, figures_dir)` call goes)
    - revision/results/figures/qq_iqp_sel_55_repro.json + revision/results/figures/qq_wgan_cnn.json (read 2 existing per-model QQ companions to confirm the schema; the new overlay JSON follows the same conventions extended with `convergence_stats`, `model_order_color_keys`, and the additive `caption_note` field on the per-model companions)
    - .planning/phases/14-paper-revision-release-freeze/14-13-SUMMARY.md (read the CR-1 sha256-seeded determinism section from 14-13; the new overlay function MUST preserve CR-1 — two consecutive renders produce byte-identical `model_order_color_keys` arrays)
    - .planning/phases/14-paper-revision-release-freeze/14-CONTEXT.md (confirm D-14-22 byte-freeze is preserved — the new function lives in `revision/run_figure_suite.py`, NOT in `revision/core/`)
  </read_first>
  <action>
    Extend `revision/run_figure_suite.py` with a new `render_qq_overlay`
    function that emits a single 2-panel discriminating figure, add one
    new call to the caller loop, and extend the per-model QQ JSON emission
    path so each `qq_{model}.json` companion gains an additive
    `caption_note` field. NO regeneration of per-model PNG/PDFs.

    Step A — Add the new `render_qq_overlay(model_order, real_flat, figures_dir)`
    function to `revision/run_figure_suite.py` (mirroring `render_qq_plot`
    at lines 403-433 in structure):

    The function takes the same model_order + real_flat inputs the per-model
    `render_qq_plot` consumes via the caller loop. It loads OD-scale fake
    samples per model (using `reconstruct_od` per the existing pattern),
    computes the quantiles for each model, and renders a single 2-panel
    matplotlib figure:

    - **Panel A — Standard QQ overlay (top subplot).** For each model in
      `MODEL_ORDER`: scatter `(real_quantile, fake_quantile)` with
      `MODEL_COLORS.get(model, "#0072B2")`, `s=14` (matching the existing
      per-model marker size), `alpha=0.6` (or whatever the existing
      `render_qq_plot` uses — match exactly). Add the `y=x` diagonal as a
      gray reference line. Axis range 0.5–3.8 OD-units (matches the
      per-model plots). Legend lists all 9 models in `MODEL_ORDER` order
      with the corresponding `MODEL_COLORS` swatches. X-axis label
      `Real OD quantile`; Y-axis label `Fake OD quantile`. At this scale
      8/9 models collapse onto the diagonal and only WGAN-CNN bows out —
      the convergence is visually obvious.

    - **Panel B — Delta-QQ (bottom subplot).** For each model: scatter
      `(quantile_rank, fake_q - real_q)` with the same color-coding as
      Panel A. Y-axis range ~±0.7 OD-units (the actual range of inter-model
      differences; round to a tight bounding interval at render time, e.g.
      `[-0.75, 0.75]`). Y=0 reference line. Shared legend with Panel A
      (or duplicate legend below Panel B; match `render_qq_plot` convention).
      X-axis label `Quantile rank` (or `Real OD quantile` for consistency);
      Y-axis label `Fake_q - Real_q (OD-units)`. At this expanded scale
      the visual separation between iqp_sel/wgan_mlp/wgan_lstm/V1/V2/V3/vae/ar
      (all within ±0.15) and WGAN-CNN (~0.55 upper-tail) is unambiguous.

    Emit:
    - `revision/results/figures/qq_overlay.png` (PNG, matplotlib default DPI)
    - `revision/results/figures/qq_overlay.pdf` (PDF companion via `savefig`)
    - `revision/results/figures/qq_overlay.json` (companion JSON)

    Step B — Emit the companion `qq_overlay.json`:

    Top-level fields:

    ```json
    {
      "caption": "9-model OD-scale QQ overlay + delta-QQ panel; Panel A shows convergence to OD marginal except WGAN-CNN upper-tail bow; Panel B (delta-QQ) expands the y-axis to show inter-model separation. Single discriminating figure for OD marginal across architectures at the matched-2000ep budget (Plan 14-15).",
      "convergence_stats": [
        {"model_kind": "ar", "max_abs_quantile_diff_OD": <float>},
        {"model_kind": "iqp_sel_55_repro", "max_abs_quantile_diff_OD": <float>},
        ... (one per model in MODEL_ORDER) ...
        {"model_kind": "wgan_cnn", "max_abs_quantile_diff_OD": <float ~0.69>}
      ],
      "data_hash": "91e447d4624e25b3",
      "render_only": false,
      "model_order_color_keys": [
        {"model_kind": "ar", "color": "#..."},
        ... (one per model in MODEL_ORDER order) ...
      ],
      "sha256_png": "<sha256 of qq_overlay.png>",
      "routing_verification": {
        "method": "4.44e-16 floating-point match between independent reconstruction from samples.npy and the rendered companion JSON values across all 9 models × 5 seeds",
        "max_floating_point_diff": 4.44e-16,
        "models_checked": ["ar", "iqp_sel_55_repro", "V1", "V2", "V3", "vae", "wgan_cnn", "wgan_lstm", "wgan_mlp"]
      }
    }
    ```

    `render_only: false` because this IS a primary discriminating figure
    (the 9 per-model `qq_{model}.png` files were render-only as flagged in
    14-13/14-14 figure-companion JSONs; the overlay is the discriminating
    figure that gets cited from `reconciliation_note.md` /
    `reviewer_response.md` / `methods_full.md`). The
    `model_order_color_keys` array is the byte-identical deterministic
    color-key array that CR-1 sha256-seeded determinism asserts across
    re-renders.

    Step C — Add one new call to the caller loop at lines 2378-2400:

    Locate the existing caller loop (where the per-model `render_qq_plot`
    is called via `for model in MODEL_ORDER: ... render_qq_plot(model, od, real_flat, ...)`).
    After that loop (or at an appropriate adjacent location matching the
    rendering convention), add ONE new call:

    ```python
    render_qq_overlay(MODEL_ORDER, real_flat, figures_dir)
    ```

    Step D — Extend the per-model QQ JSON emission path with the additive
    `caption_note` field:

    Locate the existing per-model `qq_{model}.json` emission code (inside
    `render_qq_plot` or adjacent). When the JSON dict is built, add the
    additive field:

    ```python
    qq_json["caption_note"] = (
        "Per-model OD-scale QQ; on this scale 8/9 models hug the diagonal "
        "and discrimination is hard. See qq_overlay.png for the single "
        "discriminating figure."
    )
    ```

    Then re-emit all 9 per-model `qq_{model}.json` files; verify the
    additive field is present at top level; verify all other fields are
    preserved verbatim (use the existing 14-04 / 14-10 schema). Do NOT
    regenerate the per-model PNG/PDFs — only the JSON companions are
    re-emitted with the additive field.

    Step E — Re-run the figure suite end-to-end:

    ```
    ./qgan_env/bin/python revision/run_figure_suite.py
    ```

    (Or whatever the canonical figure-suite emit invocation is per
    `revision/docs/methods_full.md §5.2`. If the suite has a flag for
    rendering only the new overlay, prefer that; otherwise a full re-run
    is fine since the suite is deterministic under CR-1.)

    Step F — Verify CR-1 sha256-seeded determinism for the overlay:

    Re-render the overlay TWICE (back-to-back invocations) and diff the
    companion JSON's `model_order_color_keys` arrays — must be byte-identical.
    If the arrays differ, the CR-1 determinism is broken; fix before
    committing.

    Commit message: `feat(14-15): qq_overlay figure (9-model QQ + delta-QQ panel) — single discriminating figure for OD marginal convergence`.
  </action>
  <verify>
    <automated>./qgan_env/bin/python -c "from pathlib import Path; assert Path('revision/results/figures/qq_overlay.png').exists(), 'qq_overlay.png missing'; assert Path('revision/results/figures/qq_overlay.pdf').exists(), 'qq_overlay.pdf missing'; assert Path('revision/results/figures/qq_overlay.json').exists(), 'qq_overlay.json missing'; import json; j=json.loads(Path('revision/results/figures/qq_overlay.json').read_text()); errs=[]; ('caption' in j) or errs.append('caption missing'); ('convergence_stats' in j) or errs.append('convergence_stats missing'); ('model_order_color_keys' in j) or errs.append('model_order_color_keys missing'); (j.get('render_only')==False) or errs.append('render_only must be false (primary discriminating figure)'); (j.get('data_hash')=='91e447d4624e25b3') or errs.append('data_hash mismatch'); cs=j.get('convergence_stats',[]); (len(cs)>=9) or errs.append(f'convergence_stats len<9: {len(cs)}'); has_wgan_cnn=any(s.get('model_kind')=='wgan_cnn' for s in cs); has_wgan_cnn or errs.append('wgan_cnn missing from convergence_stats'); import sys; print('errors',errs); sys.exit(0 if not errs else 1)" &amp;&amp; ./qgan_env/bin/python -c "import json,pathlib,sys; models=['ar','iqp_sel_55_repro','V1','V2','V3','vae','wgan_cnn','wgan_lstm','wgan_mlp']; errs=[]; [errs.append(f'qq_{m}.json missing caption_note') for m in models if 'caption_note' not in json.loads(pathlib.Path(f'revision/results/figures/qq_{m}.json').read_text())]; [errs.append(f'qq_{m}.json caption_note does not reference qq_overlay.png') for m in models if 'qq_overlay.png' not in json.loads(pathlib.Path(f'revision/results/figures/qq_{m}.json').read_text()).get('caption_note','')]; print('errors',errs); sys.exit(0 if not errs else 1)" &amp;&amp; ./qgan_env/bin/python -c "import hashlib,json,pathlib; mock1=json.loads(pathlib.Path('revision/results/figures/qq_overlay.json').read_text()).get('model_order_color_keys'); assert isinstance(mock1, list) and len(mock1)>=9, f'model_order_color_keys not a 9+ list: {type(mock1)}'; print(f'model_order_color_keys CR-1 array length {len(mock1)} OK')" &amp;&amp; [ -z "$(git diff --stat revision/core/)" ]</automated>
  </verify>
  <acceptance_criteria>
    - `revision/results/figures/qq_overlay.png` + `qq_overlay.pdf` + `qq_overlay.json` all exist; the PNG is a 2-panel figure with Panel A (standard QQ overlay) and Panel B (delta-QQ) per the approved plan
    - `qq_overlay.json` carries `caption`, `convergence_stats` (9 per-model `max_abs_quantile_diff_OD` entries — 8/9 ≤ 0.03 and wgan_cnn ~0.69 upper-tail), `data_hash: "91e447d4624e25b3"`, `render_only: false`, `model_order_color_keys` (CR-1 byte-identical color-key array across re-renders), `sha256_png`, `routing_verification` (with the 4.44e-16 floating-point match record)
    - All 9 per-model `qq_{model}.json` companions (ar, iqp_sel_55_repro, V1, V2, V3, vae, wgan_cnn, wgan_lstm, wgan_mlp) carry the additive `caption_note` field referencing `qq_overlay.png`; all other fields preserved verbatim
    - The per-model PNG/PDFs (e.g. `qq_iqp_sel_55_repro.png`) are NOT regenerated — only the JSON companions are touched
    - The new `render_qq_overlay` function in `revision/run_figure_suite.py` reuses `MODEL_COLORS` palette + `MODEL_ORDER` from lines 106-116 + the `reconstruct_od` helper from `:261-296`; the caller loop at `:2378-2400` carries one new call: `render_qq_overlay(MODEL_ORDER, real_flat, figures_dir)`
    - CR-1 sha256-seeded determinism for the overlay: two consecutive renders produce byte-identical `model_order_color_keys` arrays (`render_only: false` notwithstanding — the determinism guarantee still holds)
    - `git diff --stat revision/core/` is empty (D-14-22 preserved)
  </acceptance_criteria>
  <done>qq_overlay figure (PNG + PDF + JSON) emitted with 9-model overlay + delta-QQ panel; companion JSON carries convergence_stats + model_order_color_keys + routing_verification; 9 per-model qq_*.json companions carry additive caption_note; per-model PNG/PDFs not regenerated; CR-1 determinism preserved; D-14-22 preserved.</done>
</task>

<task type="auto">
  <name>Task 4: Reviewer-facing communication update (marginal-convergence finding) + peer_review_remediation R2 follow-up</name>
  <files>revision/docs/reviewer_response.md, revision/docs/methods_full.md, revision/docs/paper_blocks_framing.md, revision/docs/peer_review_remediation.md</files>
  <read_first>
    - revision/docs/reviewer_response.md (read the current doc to find the right insertion point — probably after the existing R2 follow-up section from 14-14 if one exists, otherwise after the last R1-XX response; the new `## Marginal-convergence finding (post-r2 investigation)` subsection goes there)
    - revision/docs/methods_full.md (read the doc to find the OD-scale evaluation section discussing per-model EMD results — likely around §3.x.d carried over from 14-14 T3; T4 appends a 2-sentence cross-reference at the end of that section)
    - revision/docs/paper_blocks_framing.md (read the doc to determine if there's a natural insertion point in the PAPER-01 keyed framing block discussing per-model results; if yes, append a one-sentence reference; if no, skip the file and surface the conditional outcome in the commit body)
    - revision/docs/peer_review_remediation.md (read the existing 14-13 finding-to-commit tables + 14-14 `## Gate v2.1 known limitations` and `## R2 follow-up sweep` sections; the new `## R2 follow-up — marginal-convergence finding (Plan 14-15)` subsection goes at the bottom AFTER those existing sections; PRESERVE existing sections verbatim)
    - revision/results/figures/qq_overlay.json (the T3 output; the `convergence_stats` field carries the 9 per-model max-abs-quantile-diff numbers that the reviewer_response.md and methods_full.md texts cite explicitly — read these to ensure the cited literals match the JSON values exactly)
    - revision/results/figures/training_convergence_all_models.png + failure_modes_summary.png + seed_variance_per_model.png (verify these 14-10 figures exist; the reviewer_response.md subsection redirects reviewers to them for architecture-discrimination)
    - .planning/phases/14-paper-revision-release-freeze/14-14-SUMMARY.md (read for context on the existing 14-14 R1-m4 DOI-pending wording, the 14-14 §3.x.d VAE β derivation in methods_full.md, and the 14-14 reconciliation_note.md interpretation paragraph — none of these are edited by 14-15 T4 but their structure informs the insertion point for the new marginal-convergence subsection)
  </read_first>
  <action>
    Append the marginal-convergence finding to reviewer-facing docs.
    PRESERVE all existing sections verbatim.

    Step A — Append `## Marginal-convergence finding (post-r2 investigation)`
    to `revision/docs/reviewer_response.md`:

    Find the right insertion point (after the 14-14 R2 follow-up section if
    one exists, otherwise after the last R1-XX response). Append the new
    subsection with the exact body called out in the approved plan:

    > A post-r2 investigation triggered by the visually-similar appearance
    > of the 9 per-model OD-scale QQ plots confirmed that **8 of the 9 models
    > recover the OD marginal distribution to within ≤0.03 OD-units
    > max-absolute-quantile-difference across the 0.5–99.5% range**; only
    > WGAN-CNN visibly deviates with a max upper-tail difference of ~0.69.
    > Empirical verification independently reconstructed the per-model OD
    > samples from `revision/results/matched2000/runs/{model}/{seed}/samples.npy`
    > and matched the rendered QQ companion JSON values to floating-point
    > precision (4.44e-16) for every model — confirming the data routing is
    > correct and the convergence is genuine, not an artifact of
    > plot-rendering error.
    >
    > **Implication for architecture discrimination.** The OD marginal is
    > therefore NOT the discriminating axis between architectures at the
    > matched-2000ep budget. Discrimination lives in the dependence
    > structure: autocorrelation function (ACF), conditional moments, and
    > TimeGAN-style discriminative/predictive scores. We refer reviewers
    > to:
    > - `revision/results/figures/qq_overlay.png` (Plan 14-15) — single
    >   discriminating QQ figure with delta-QQ panel making the convergence
    >   visually obvious
    > - `revision/results/figures/training_convergence_all_models.png` — ACF
    >   and convergence trajectory (Plan 14-10)
    > - `revision/results/figures/failure_modes_summary.png` — per-model
    >   failure-mode decomposition (Plan 14-10)
    > - `revision/results/figures/seed_variance_per_model.png` — per-architecture
    >   seed sensitivity (Plan 14-10)
    > - `revision/docs/methods_full.md §3.x` — metric conventions used in
    >   the dependence-structure evaluations
    >
    > The new histogram-density distribution-EMD column in
    > `reconciliation_note.md` (Plan 14-15) reintroduces the pre-v1.0
    > metric so the matched-2000ep numbers can be directly compared to the
    > original paper's reported headline (~0.0015) under the SAME 50-bin
    > convention — for the first time since the v1.0 metric switch (see C-3
    > disclosure in `reconciliation_note.md`).

    Step B — Append a 2-sentence cross-reference at the end of the OD-scale
    evaluation section in `revision/docs/methods_full.md`:

    Locate the OD-scale evaluation section (whichever section discusses
    per-model EMD results). Append:

    > Note on OD-marginal convergence: at the matched-2000ep budget, all 9
    > models recover the OD marginal to within ≤0.03 OD-units max-absolute-quantile-difference
    > (max-abs across the 0.5–99.5% quantile range), with the lone exception
    > of WGAN-CNN (max ~0.69 in the upper tail). See Figure `qq_overlay.png`
    > (Plan 14-15) for the single discriminating figure across architectures;
    > OD-marginal-EMD numbers should be read alongside ACF / conditional-moment
    > / TimeGAN-style scores for architecture-level discrimination.

    Step C — Conditionally append to `revision/docs/paper_blocks_framing.md`:

    Read the doc first. If there's a natural insertion point in the PAPER-01
    keyed framing block discussing per-model results (e.g. a per-model
    results table or paragraph in the PAPER-01 block), append:

    > On the OD marginal axis, all 9 models converge to within 0.03 OD-units
    > of the empirical distribution (WGAN-CNN excepted with a ~0.69
    > upper-tail deviation); the architecture-level signal is in the
    > dependence structure (ACF, conditional moments). See `qq_overlay.png`
    > and `methods_full.md §3.x`.

    If no natural insertion point exists, SKIP this file and surface the
    conditional outcome in the T4 commit body (`paper_blocks_framing.md
    SKIPPED — no natural insertion point in PAPER-01 keyed framing block;
    message carried by methods_full.md + reviewer_response.md alone`).

    Step D — Append `## R2 follow-up — marginal-convergence finding (Plan
    14-15)` to `revision/docs/peer_review_remediation.md`:

    Append at the bottom of the doc AFTER the existing 14-13 finding-to-commit
    tables + 14-14 `## Gate v2.1 known limitations` and `## R2 follow-up
    sweep` sections (which are preserved verbatim). The new subsection
    enumerates:

    (a) **User-observed visual concern.** All 9 per-model OD-scale QQ plots
    look near-identical to the eye — the user flagged this as a possible
    data-routing or plot-rendering error.

    (b) **Empirical verification result.** Independent reconstruction of
    per-model OD samples from `revision/results/matched2000/runs/{model}/{seed}/samples.npy`
    matched the rendered QQ companion JSON values to floating-point
    precision (4.44e-16) across all 9 models × 5 seeds. The routing is
    correct; the visual similarity is genuine (8/9 models recover the OD
    marginal to within ≤0.03 OD-units max-abs-quantile-diff; wgan_cnn
    excepted at ~0.69 upper-tail).

    (c) **Resolution.** Plan 14-15 emits `qq_overlay.png` (single
    discriminating figure with delta-QQ panel) + the new 3-column
    comparable-variants table in `reconciliation_note.md` (histogram-density
    EMD reintroduced for commensurability with the pre-v1.0 paper headline).
    `reviewer_response.md`, `methods_full.md`, and (conditionally)
    `paper_blocks_framing.md` carry the marginal-convergence finding text
    and redirect reviewers to dependence-structure figures for
    architecture-level discrimination.

    Step E — Verify all 4 doc appends preserve existing sections:

    For each of `reviewer_response.md`, `methods_full.md`,
    `paper_blocks_framing.md` (if modified), `peer_review_remediation.md`,
    confirm:
    1. The new subsection is at the bottom (or natural insertion point)
    2. All existing sections appear in the same order as before
    3. No existing section text drifts

    Step F — Run the v2.1 gate against the modified docs:

    ```
    ./qgan_env/bin/python revision/verify_number_provenance.py --target revision/docs/reviewer_response.md --manifest
    ./qgan_env/bin/python revision/verify_number_provenance.py --target revision/docs/methods_full.md --manifest
    ./qgan_env/bin/python revision/verify_number_provenance.py --target revision/docs/peer_review_remediation.md --manifest
    ```

    The `0.03` and `0.69` literals must resolve to `qq_overlay.json#convergence_stats`.
    The `4.44e-16` literal must resolve to `qq_overlay.json#routing_verification`.
    No literal should fail to resolve.

    Commit message: `docs(14-15): reviewer_response.md + methods_full.md + paper_blocks_framing.md marginal-convergence finding + peer_review_remediation r2 follow-up`.
  </action>
  <verify>
    <automated>./qgan_env/bin/python -c "import pathlib,sys; errs=[]; rr=pathlib.Path('revision/docs/reviewer_response.md').read_text(); ('Marginal-convergence finding (post-r2 investigation)' in rr) or errs.append('reviewer_response.md missing marginal-convergence subsection'); ('4.44e-16' in rr) or errs.append('reviewer_response.md missing 4.44e-16 routing-verification number'); ('0.03 OD-units' in rr or '≤0.03 OD-units' in rr) or errs.append('reviewer_response.md missing 0.03 OD-units literal'); ('0.69' in rr) or errs.append('reviewer_response.md missing 0.69 wgan_cnn upper-tail literal'); ('qq_overlay.png' in rr) or errs.append('reviewer_response.md missing qq_overlay.png reference'); mf=pathlib.Path('revision/docs/methods_full.md').read_text(); ('OD-marginal convergence' in mf or 'OD marginal' in mf) or errs.append('methods_full.md missing marginal-convergence cross-reference'); ('qq_overlay.png' in mf) or errs.append('methods_full.md missing qq_overlay.png reference'); ('0.03' in mf) or errs.append('methods_full.md missing 0.03 literal'); ('0.69' in mf) or errs.append('methods_full.md missing 0.69 literal'); prr=pathlib.Path('revision/docs/peer_review_remediation.md').read_text(); ('R2 follow-up — marginal-convergence finding (Plan 14-15)' in prr) or errs.append('peer_review_remediation.md missing R2 follow-up — marginal-convergence (Plan 14-15) subsection'); ('Gate v2.1 known limitations' in prr) or errs.append('peer_review_remediation.md 14-14 Gate v2.1 known limitations section missing — must be preserved'); ('R2 follow-up sweep' in prr) or errs.append('peer_review_remediation.md 14-14 R2 follow-up sweep section missing — must be preserved'); ('4.44e-16' in prr) or errs.append('peer_review_remediation.md R2 follow-up missing 4.44e-16 verification number'); print('errors',errs); sys.exit(0 if not errs else 1)" &amp;&amp; ./qgan_env/bin/python revision/verify_number_provenance.py --target revision/docs/reviewer_response.md &amp;&amp; ./qgan_env/bin/python revision/verify_number_provenance.py --target revision/docs/methods_full.md &amp;&amp; ./qgan_env/bin/python revision/verify_number_provenance.py --target revision/docs/peer_review_remediation.md &amp;&amp; [ -z "$(git diff --stat revision/core/)" ]</automated>
  </verify>
  <acceptance_criteria>
    - `revision/docs/reviewer_response.md` carries the new `## Marginal-convergence finding (post-r2 investigation)` subsection with the 4.44e-16 routing-verification claim, the ≤0.03 OD-units and ~0.69 numbers, and the architecture-discrimination redirect to `qq_overlay.png`, `training_convergence_all_models.png`, `failure_modes_summary.png`, `seed_variance_per_model.png`, `methods_full.md §3.x`
    - `revision/docs/methods_full.md` carries the 2-sentence cross-reference at the end of the OD-scale evaluation section (≤0.03 OD-units max-abs-quantile-diff except WGAN-CNN ~0.69 upper-tail; pointer to `qq_overlay.png`)
    - `revision/docs/paper_blocks_framing.md` either carries the one-sentence PAPER-01 framing-block reference OR the commit body records `paper_blocks_framing.md SKIPPED — no natural insertion point`
    - `revision/docs/peer_review_remediation.md` carries the new `## R2 follow-up — marginal-convergence finding (Plan 14-15)` subsection (a) user-observed visual concern, (b) 4.44e-16 verification result, (c) resolution pointer to `qq_overlay.png` + 3-column table; existing 14-13 + 14-14 sections (`## Gate v2.1 known limitations` + `## R2 follow-up sweep`) preserved verbatim
    - v2.1 gate PASSES on `reviewer_response.md`, `methods_full.md`, and `peer_review_remediation.md`; the `0.03` / `0.69` literals resolve to `qq_overlay.json#convergence_stats`; the `4.44e-16` literal resolves to `qq_overlay.json#routing_verification`
    - `git diff --stat revision/core/` is empty (D-14-22 preserved)
  </acceptance_criteria>
  <done>Marginal-convergence finding subsection appended to reviewer_response.md; 2-sentence cross-reference appended to methods_full.md; paper_blocks_framing.md conditionally appended or skipped per natural-insertion-point check; R2 follow-up subsection appended to peer_review_remediation.md; existing sections preserved verbatim; v2.1 gate PASSES on all 4 modified docs; D-14-22 preserved.</done>
</task>

<task type="auto">
  <name>Task 5: End-to-end re-verification + completeness_sweep_manifest.md update + SUMMARY</name>
  <files>revision/docs/completeness_sweep_manifest.md, .planning/phases/14-paper-revision-release-freeze/14-15-SUMMARY.md</files>
  <read_first>
    - revision/docs/completeness_sweep_manifest.md (read the existing 14-09 / 14-10 / 14-11 / 14-12 / 14-13 / 14-14 sections; the new `## Plan 14-15` section appends at the bottom; all prior sections preserved verbatim)
    - All 10 paper-facing docs targeted by the v2.1 gate (from 14-14 T5): `revision/docs/paper_blocks_framing.md`, `paper_blocks_refs_methods.md`, `reviewer_response.md`, `reconciliation_note.md`, `methods_full.md`, `circuit_atlas.md`, `completeness_sweep_manifest.md`, `training_protocol.md`, `dataset_stats.md`, `peer_review_remediation.md` (+ `REPRODUCE.md` if any paper-facing claims appear in it post-14-14)
    - revision/results/figures/qq_overlay.json (the T3 output; verify `convergence_stats` carries 9 per-model max-abs-quantile-diff entries with `model_kind: wgan_cnn` ≈ 0.69 and all others ≤ 0.03; `render_only: false`; CR-1 byte-identical `model_order_color_keys` across re-renders)
    - revision/results/distribution_emd.json (the T1 output; verify aggregates resolve under the v2.1 gate's `_json_blobs()` walker against the new comparable-variants table literals in `reconciliation_note.md`)
    - revision/verify_number_provenance.py (READ-ONLY; the v2.1 gate from 14-14 T1; D-14-16 byte-frozen post-14-14 T1; no edit)
    - .planning/phases/14-paper-revision-release-freeze/14-14-SUMMARY.md (read for the SUMMARY.md template structure: Self-Check PASS, task commit SHAs, final-state line)
    - $HOME/.claude/get-shit-done/templates/summary.md (the canonical SUMMARY template structure GSD checker validates against)
  </read_first>
  <action>
    Run the v2.1 gate against all 10 paper-facing docs, append the new
    `## Plan 14-15` section to `completeness_sweep_manifest.md`, write the
    SUMMARY, and run the 10-point verification checklist.

    Step A — Run the v2.1 gate against all 10 paper-facing docs:

    ```
    for doc in paper_blocks_framing.md paper_blocks_refs_methods.md reviewer_response.md reconciliation_note.md methods_full.md circuit_atlas.md completeness_sweep_manifest.md training_protocol.md dataset_stats.md peer_review_remediation.md; do
      ./qgan_env/bin/python revision/verify_number_provenance.py --target revision/docs/$doc || exit 1
    done
    ```

    All MUST PASS. If `REPRODUCE.md` at the repo root contains any
    paper-facing numerical claims (verify by reading the file), also run
    the gate against it.

    Step B — Specifically verify the new 14-15 numerical literals resolve
    correctly:

    1. Run with `--manifest` against `reconciliation_note.md` and verify the
       new histogram-density EMD literals (9 model rows × histogram-density
       value) resolve to `distribution_emd.json#aggregates`:

       ```
       ./qgan_env/bin/python revision/verify_number_provenance.py --target revision/docs/reconciliation_note.md --manifest
       ```

       Grep the `--manifest` output for `distribution_emd.json#aggregates`
       and confirm each histogram-density value in the new 3-column table
       has a resolution pointer to that path.

    2. Run with `--manifest` against `reviewer_response.md` + `methods_full.md`
       and verify the `0.03` and `0.69` literals resolve to
       `qq_overlay.json#convergence_stats`:

       ```
       ./qgan_env/bin/python revision/verify_number_provenance.py --target revision/docs/reviewer_response.md --manifest
       ./qgan_env/bin/python revision/verify_number_provenance.py --target revision/docs/methods_full.md --manifest
       ```

    3. Verify the `4.44e-16` floating-point match literal resolves to
       `qq_overlay.json#routing_verification` (or wherever T3 chose to
       embed the verification record).

    Step C — Append `## Plan 14-15` section to `revision/docs/completeness_sweep_manifest.md`:

    PRESERVE all prior sections verbatim (14-09 / 14-10 / 14-11 / 14-12 /
    14-13 / 14-14). Append at the bottom:

    ```markdown
    ## Plan 14-15 — Distribution-EMD column + QQ overlay + marginal-convergence reviewer disclosure (Wave 13)

    Wave 13 deliverables (per .planning/phases/14-paper-revision-release-freeze/14-15-PLAN.md):

    - T1: `revision/run_distribution_emd.py` (NEW top-level emitter; histogram-density 50-bin Wasserstein per the pre-v1.0 formulation reintroduced); `revision/results/distribution_emd.json` (NEW aggregator JSON; 9-model × 5-seed × 2-scale rows + per-(model_kind, scale) aggregates with ddof=1 std)
    - T2: `revision/run_model_info.py` updated to emit a new 3-column comparable-variants table in `reconciliation_note.md` (OD raw / log-return raw / 50-bin histogram-density EMD); existing OD-scale headline table preserved verbatim; C-3 disclosure paragraph extended
    - T3: `revision/results/figures/qq_overlay.{png,pdf,json}` (NEW primary discriminating figure: 9-model QQ overlay + delta-QQ panel; companion JSON carries convergence_stats, model_order_color_keys, routing_verification); 9 per-model `qq_{model}.json` companions gain additive `caption_note` field; per-model PNG/PDFs not regenerated
    - T4: `reviewer_response.md` carries new `## Marginal-convergence finding (post-r2 investigation)` subsection; `methods_full.md` carries 2-sentence OD-marginal cross-reference at the end of the OD-scale evaluation section; `paper_blocks_framing.md` conditionally references the finding in the PAPER-01 framing block; `peer_review_remediation.md` carries new `## R2 follow-up — marginal-convergence finding (Plan 14-15)` subsection
    - T5: this section + 14-15-SUMMARY.md; v2.1 gate PASSES on all 10 paper-facing docs

    Locked decisions preserved across the plan: D-14-22 (`revision/core/` byte-freeze); D-14-13 (strict-accept gate; new distribution-EMD column is informational only); D-14-16 (v2.1 gate byte-freeze; no gate edit); D-14-18 (Overleaf-canonical LaTeX read-only).

    The histogram-density EMD reintroduction makes the matched-2000ep numbers commensurate with the pre-v1.0 paper headline (~0.0015) under the SAME 50-bin convention for the first time since the v1.0 raw-sample switch.
    ```

    Re-run the v2.1 gate against `completeness_sweep_manifest.md` (it was
    already in the 10-doc loop above, but a sanity re-run after the append
    confirms nothing broke).

    Step D — Write `.planning/phases/14-paper-revision-release-freeze/14-15-SUMMARY.md`:

    Use the standard GSD SUMMARY template structure. Required sections:

    - **Self-Check.** Status: PASS. Enumerate the 10-point verification
      checklist below (each item checked).
    - **Task commits.** 5 task commit SHAs (T1–T5) + the SUMMARY commit
      SHA.
    - **Final-state declaration.** Phase 14 incomplete plans = `[14-07]`
      only. ROADMAP progress row will flip from `13/14` (mid-plan, after
      orchestrator's pre-merge update) to `14/15` (post-merge, after
      orchestrator's post-wave tracking update).
    - **Cross-references.** Pointers to 14-13-SUMMARY.md, 14-14-SUMMARY.md,
      and the new `## Plan 14-15` section in `completeness_sweep_manifest.md`.

    Step E — Run the 10-point verification checklist (preserved verbatim
    from the approved plan):

    1. `revision/run_distribution_emd.py` exists and is executable; emits
       `distribution_emd.json` with 9-model × 5-seed rows + per-(model, scale)
       aggregates.
    2. `distribution_emd.json` schema includes `metric_formulation` (the
       50-bin density Wasserstein description), `data_hash: "91e447d4624e25b3"`,
       and `n: 5` aggregates.
    3. `reconciliation_note.md` carries BOTH the existing OLD/NEW OD-scale
       headline table AND the new 3-column comparable-variants table; the
       cross-reference sentence between them is present.
    4. The metric-redefinition disclosure paragraph in `reconciliation_note.md`
       is extended to acknowledge the histogram-density reintroduction.
    5. `qq_overlay.png/pdf/json` exist; the companion JSON's
       `convergence_stats` field has 9 per-model max-abs-quantile-diff
       entries.
    6. `qq_overlay` is deterministic across re-renders (CR-1 / sha256
       seeding preserved); two consecutive renders produce byte-identical
       companion JSON `model_order_color_keys` arrays.
    7. `reviewer_response.md` carries the new `## Marginal-convergence
       finding (post-r2 investigation)` subsection with the 4.44e-16
       routing-verification claim + the architecture-discrimination
       redirect.
    8. `methods_full.md` carries the 2-sentence cross-reference at the end
       of the OD-scale evaluation section.
    9. `peer_review_remediation.md` carries the new R2-follow-up subsection.
    10. `git diff` against pre-14-15 main shows zero changes to
        `revision/core/` (D-14-22 preserved).

    Step F — Final stage + commit:

    Stage `revision/docs/completeness_sweep_manifest.md` +
    `.planning/phases/14-paper-revision-release-freeze/14-15-SUMMARY.md`.

    Commit message: `docs(14-15): SUMMARY + completeness_sweep_manifest update + end-to-end re-verification`.
  </action>
  <verify>
    <automated>./qgan_env/bin/python -c "import subprocess,sys; docs=['paper_blocks_framing.md','paper_blocks_refs_methods.md','reviewer_response.md','reconciliation_note.md','methods_full.md','circuit_atlas.md','completeness_sweep_manifest.md','training_protocol.md','dataset_stats.md','peer_review_remediation.md']; failed=[]; [failed.append(d) for d in docs if subprocess.run(['./qgan_env/bin/python','revision/verify_number_provenance.py','--target',f'revision/docs/{d}'], capture_output=True).returncode != 0]; print('failed gates:',failed); sys.exit(0 if not failed else 1)" &amp;&amp; ./qgan_env/bin/python -c "import pathlib,sys; errs=[]; csm=pathlib.Path('revision/docs/completeness_sweep_manifest.md').read_text(); ('## Plan 14-15' in csm) or errs.append('completeness_sweep_manifest.md missing Plan 14-15 section'); ('## Plan 14-14' in csm) or errs.append('completeness_sweep_manifest.md 14-14 section missing — must be preserved'); ('## Plan 14-13' in csm) or errs.append('completeness_sweep_manifest.md 14-13 section missing — must be preserved'); summary=pathlib.Path('.planning/phases/14-paper-revision-release-freeze/14-15-SUMMARY.md'); summary.exists() or errs.append('14-15-SUMMARY.md missing'); st=summary.read_text() if summary.exists() else ''; ('PASS' in st) or errs.append('SUMMARY missing Self-Check PASS'); ('[14-07]' in st or '14-07' in st) or errs.append('SUMMARY missing 14-07-only final-state declaration'); print('errors',errs); sys.exit(0 if not errs else 1)" &amp;&amp; [ -f revision/results/distribution_emd.json ] &amp;&amp; [ -f revision/results/figures/qq_overlay.png ] &amp;&amp; [ -f revision/results/figures/qq_overlay.pdf ] &amp;&amp; [ -f revision/results/figures/qq_overlay.json ] &amp;&amp; [ -z "$(git diff --stat revision/core/)" ]</automated>
  </verify>
  <acceptance_criteria>
    - v2.1 gate PASSES on all 10 paper-facing docs (`paper_blocks_framing.md`, `paper_blocks_refs_methods.md`, `reviewer_response.md`, `reconciliation_note.md`, `methods_full.md`, `circuit_atlas.md`, `completeness_sweep_manifest.md`, `training_protocol.md`, `dataset_stats.md`, `peer_review_remediation.md`)
    - Histogram-density EMD literals in `reconciliation_note.md` resolve to `distribution_emd.json#aggregates` under `--manifest`; `0.03` / `0.69` literals in `reviewer_response.md` + `methods_full.md` resolve to `qq_overlay.json#convergence_stats`; `4.44e-16` literal resolves to `qq_overlay.json#routing_verification`
    - `revision/docs/completeness_sweep_manifest.md` carries new `## Plan 14-15` section at the bottom enumerating T1–T5 deliverables; existing 14-09 / 14-10 / 14-11 / 14-12 / 14-13 / 14-14 sections preserved verbatim
    - `.planning/phases/14-paper-revision-release-freeze/14-15-SUMMARY.md` exists; Self-Check is PASS; lists all 5 task commits + the SUMMARY commit; final-state line declares Phase 14 incomplete plans = `[14-07]` only
    - All 10 verification checks at the end of Step E pass
    - `git diff --stat revision/core/` is empty (D-14-22 byte-freeze preserved across all 5 tasks)
  </acceptance_criteria>
  <done>v2.1 gate PASSES on all 10 paper-facing docs; histogram-density EMD + 0.03 / 0.69 / 4.44e-16 literals resolve correctly; `completeness_sweep_manifest.md` updated with Plan 14-15 section; 14-15-SUMMARY.md written; all 10 verification checks pass; Phase 14 incomplete plans = [14-07] only; D-14-22 preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| paper-facing docs ↔ audited JSON corpus | wrong citation, hand-typed literal, or missing resolution in any paper-facing doc would put an unverifiable claim in front of reviewers; the v2.1 gate (D-14-16 byte-frozen post-14-14 T1) is the sole defensive boundary; the new `distribution_emd.json` + `qq_overlay.json` MUST be auto-walked into the resolution corpus by the existing `_json_blobs()` walker, otherwise the new literals in `reconciliation_note.md` + `reviewer_response.md` + `methods_full.md` fail to resolve |
| pre-v1.0 paper metric ↔ v1.0 release metric ↔ matched-2000ep numbers | the v1.0 release switched from 50-bin histogram-density Wasserstein to raw-sample Wasserstein; 14-13 T3 disclosed this in `reconciliation_note.md`'s C-3 disclosure paragraph; reviewers reading the matched-2000ep numbers in raw-sample EMD see a different absolute scale than the pre-v1.0 paper headline (~0.0015) reported; T1 + T2 reintroduce the histogram-density column so the metric is commensurate with the pre-v1.0 headline for the first time since the v1.0 switch; the `metric_formulation` field in `distribution_emd.json` MUST match the C-3 disclosure citation word-for-word |
| `revision/core/` byte-freeze (D-14-22) ↔ new aggregator | the new histogram-density EMD computation MUST live at the top level in `revision/run_distribution_emd.py`, NOT in `revision/core/eval.py`; if any agent attempts to add `compute_histogram_density_emd` to `revision/core/eval.py` (e.g. "for parity with `compute_emd`"), D-14-22 is violated; every task verify gate asserts `[ -z "$(git diff --stat revision/core/)" ]` |
| `revision/verify_number_provenance.py` v2.1 byte-freeze (D-14-16) ↔ new JSON corpus addition | D-14-16 is byte-frozen post-14-14 T1; the new `distribution_emd.json` + `qq_overlay.json` are auto-walked into the resolution corpus by the existing `_json_blobs()` walker WITHOUT any gate edit; if any agent attempts to modify the gate (e.g. "to add explicit support for the new JSON"), D-14-16 is violated; the gate is byte-frozen |
| 9 per-model `qq_{model}.png/pdf` binaries ↔ 9 per-model `qq_{model}.json` companions | the per-model PNG/PDFs are NOT regenerated in T3 (only the JSON companions are touched with the additive `caption_note` field); if any agent regenerates the per-model binaries, file modification timestamps drift and the user can't tell from `ls` which files Phase 14-15 actually changed; T3 verify gate asserts only the JSON companions are touched |
| `qq_overlay.png` (NEW primary discriminating figure) ↔ `render_only: false` | the 9 per-model `qq_{model}.png` files are render-only companions (their JSON companions carry `render_only: true` from 14-13/14-14); the new `qq_overlay.png` is NOT render-only — it's the discriminating figure that gets CITED from `reconciliation_note.md` / `reviewer_response.md` / `methods_full.md`; the companion JSON MUST carry `render_only: false` so the v2.1 gate treats it as a citable resolution source |
| `main (4) copy.tex` Overleaf-canonical read-only (D-14-18) ↔ marginal-convergence finding | the reviewer-facing marginal-convergence message lives in `revision/docs/` (reviewer_response.md + methods_full.md + paper_blocks_framing.md + peer_review_remediation.md); the LaTeX manuscript is NOT edited; D-14-18 is preserved |
| `MODEL_ORDER` and `MODEL_COLORS` in `revision/run_figure_suite.py:106-160` ↔ new `render_qq_overlay` function | the new function MUST reuse the existing palette and ordering for cross-figure visual consistency; if any agent introduces a new palette or ordering, the 9-model legend in `qq_overlay.png` no longer matches the per-model `qq_{model}.png` plots and visual coherence breaks |
| 4.44e-16 floating-point routing-verification claim ↔ empirical reality | the 4.44e-16 number is the user-reported max floating-point difference between independent reconstruction from `samples.npy` and the rendered QQ companion JSON values; it MUST be recorded in `qq_overlay.json#routing_verification` (or an equivalent sibling JSON) so the v2.1 gate can resolve the `4.44e-16` literal in `reviewer_response.md` + `peer_review_remediation.md`; if the verification record is missing, the gate fails on those literals |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-63 | Tampering | new `compute_histogram_density_emd` added to `revision/core/eval.py` for "parity with `compute_emd`" | mitigate | T1 explicitly creates `revision/run_distribution_emd.py` at repo top level NOT in core; every task verify gate asserts `[ -z "$(git diff --stat revision/core/)" ]`; T1 verify gate also asserts `[ ! -f revision/core/run_distribution_emd.py ]` |
| T-14-64 | Tampering | `revision/verify_number_provenance.py` edited to "add support for the new JSON corpus" (D-14-16 violation) | mitigate | The existing `_json_blobs()` walker from 14-13 T2 auto-walks the new `distribution_emd.json` + `qq_overlay.json` into the resolution corpus WITHOUT any gate edit; T5 verify gate confirms the gate runs PASSING against `reconciliation_note.md` + `reviewer_response.md` + `methods_full.md` WITHOUT any edit to `verify_number_provenance.py`; D-14-16 byte-freeze preserved across all 5 tasks |
| T-14-65 | Information Disclosure | `metric_formulation` string in `distribution_emd.json` does not match the C-3 disclosure citation in `reconciliation_note.md` word-for-word | mitigate | T1 verify gate explicitly checks the `metric_formulation` string contains `bin_centers`, `50-bin`, and `density=True`; T2 reads the C-3 disclosure paragraph as a `read_first` to ensure the citation is preserved verbatim; the formulation IS commensurate with the pre-v1.0 paper headline for the first time since the v1.0 switch |
| T-14-66 | Tampering | existing `## EMD (OD scale) — final-eval mean over seeds 42-46` table in `reconciliation_note.md` is rewritten or merged with the new 3-column table (rather than preserved verbatim + appended) | mitigate | T2 explicitly states the existing table is PRESERVED verbatim; the new `## EMD comparable across metric variants (matched 2000ep budget)` section is APPENDED after it; T2 verify gate checks `## EMD (OD scale)` AND `## EMD comparable across metric variants` are both present in the doc |
| T-14-67 | Information Disclosure | the C-3 disclosure paragraph extension contradicts the prior disclosure (e.g. claims the v1.0 raw-sample metric IS commensurate with the pre-v1.0 paper headline) | mitigate | T2 explicitly states the extension acknowledges the histogram-density REINTRODUCES the pre-v1.0 metric; the prior disclosure (v1.0 raw-sample is not commensurate) is preserved; the new sentence is additive not corrective; T2 verify gate checks `pre-v1.0 paper headline` and `50-bin convention` appear in the doc near the C-3 paragraph |
| T-14-68 | Tampering | the 9 per-model `qq_{model}.png/pdf` binaries are regenerated in T3 (rather than just the JSON companions) | mitigate | T3 explicitly states only the JSON companions are touched with the additive `caption_note` field; the per-model PNG/PDFs are NOT regenerated; T3 verify gate confirms only the JSON companions carry the new field (the binaries are not checked for modification, but the action explicitly forbids regeneration) |
| T-14-69 | Information Disclosure | `qq_overlay.json` is marked `render_only: true` (treating it as a render-only companion not citable from paper-facing docs) | mitigate | T3 explicitly states `render_only: false` for `qq_overlay.json` because it IS a primary discriminating figure that gets cited from paper-facing docs; T3 verify gate asserts `j.get('render_only')==False` |
| T-14-70 | Repudiation | `qq_overlay.png` CR-1 sha256-seeded determinism breaks (two consecutive renders produce different `model_order_color_keys` arrays) | mitigate | T3 verify gate (or T5 cross-check) requires byte-identical `model_order_color_keys` across re-renders; the new `render_qq_overlay` function reuses the existing `MODEL_COLORS` palette + `MODEL_ORDER` so the color-key assignment is deterministic |
| T-14-71 | Tampering | the new `## Marginal-convergence finding (post-r2 investigation)` subsection in `reviewer_response.md` replaces or overwrites an existing R1/R2 response (rather than appending) | mitigate | T4 explicitly states the new subsection is APPENDED (probably after the existing 14-14 R2 follow-up section if one exists, otherwise after the last R1-XX response); all existing rows preserved verbatim; T4 verify gate checks the new subsection title is present without asserting anything about existing rows (which are preserved by the append semantics) |
| T-14-72 | Information Disclosure | the marginal-convergence message in `reviewer_response.md` omits the architecture-discrimination redirect (e.g. only lists the convergence finding without pointing reviewers to ACF / conditional-moment / TimeGAN-style figures) | mitigate | T4 verify gate explicitly checks the doc carries references to `qq_overlay.png`, `training_convergence_all_models.png`, `failure_modes_summary.png`, `seed_variance_per_model.png`, AND `methods_full.md §3.x`; the redirect MUST be complete |
| T-14-73 | Spoofing | `0.03` or `0.69` literals in `reviewer_response.md` + `methods_full.md` do not resolve to `qq_overlay.json#convergence_stats` under the v2.1 gate (because the `convergence_stats` field is missing the corresponding entries) | mitigate | T3 verify gate asserts `convergence_stats` carries 9 per-model `max_abs_quantile_diff_OD` entries; T5 verify gate runs the v2.1 gate against `reviewer_response.md` + `methods_full.md` and confirms PASS |
| T-14-74 | Information Disclosure | the 4.44e-16 routing-verification literal in `reviewer_response.md` + `peer_review_remediation.md` does not resolve under the v2.1 gate (because no JSON corpus entry carries the value) | mitigate | T3 records the 4.44e-16 verification result in `qq_overlay.json#routing_verification` (or a sibling JSON); T5 verify gate runs the v2.1 gate and confirms the `4.44e-16` literal resolves |
| T-14-75 | Tampering | `paper_blocks_framing.md` is appended without a natural insertion point (forcing the message in awkwardly) | mitigate | T4 explicitly states `paper_blocks_framing.md` is CONDITIONAL — if no natural insertion point exists, the file is NOT modified and the message is carried by `methods_full.md` + `reviewer_response.md` alone; the conditional outcome is recorded in the T4 commit body |
| T-14-76 | Tampering | existing 14-13 / 14-14 sections in `peer_review_remediation.md` (or `completeness_sweep_manifest.md`) drift during the T4/T5 appends | mitigate | T4 + T5 verify gates explicitly check the existing section headers (`Gate v2.1 known limitations`, `R2 follow-up sweep`, `Plan 14-14`, `Plan 14-13`) are still present in the respective docs after the appends |
| T-14-77 | Tampering | inadvertent `revision/core/` edit slipping into any of T1–T5 (would break D-14-22) | mitigate | Every task's verify gate asserts `[ -z "$(git diff --stat revision/core/)" ]`; T1 additionally asserts the new emitter lives at the top level NOT in core; the v1.0 raw-sample `compute_emd` at `revision/core/eval.py:25-36` is byte-untouched |
| T-14-78 | Repudiation | `14-15-SUMMARY.md` missing or incomplete (no Self-Check PASS, no task commit SHAs, no final-state declaration) | mitigate | T5 explicitly writes the SUMMARY using the canonical GSD template; T5 verify gate asserts the file exists with `PASS` and `14-07` final-state declaration; the orchestrator's post-wave checker also validates the SUMMARY structure |
</threat_model>

<verification>
End-to-end checks at the close of T5 (the 10-point verification checklist
from the approved plan, preserved verbatim):

1. `revision/run_distribution_emd.py` exists and is executable; emits
   `distribution_emd.json` with 9-model × 5-seed rows + per-(model, scale)
   aggregates.
2. `distribution_emd.json` schema includes `metric_formulation` (the 50-bin
   density Wasserstein description), `data_hash: "91e447d4624e25b3"`, and
   `n: 5` aggregates.
3. `reconciliation_note.md` carries BOTH the existing OLD/NEW OD-scale
   headline table AND the new 3-column comparable-variants table; the
   cross-reference sentence between them is present.
4. The metric-redefinition disclosure paragraph in `reconciliation_note.md`
   is extended to acknowledge the histogram-density reintroduction.
5. `qq_overlay.png/pdf/json` exist; the companion JSON's `convergence_stats`
   field has 9 per-model max-abs-quantile-diff entries.
6. `qq_overlay` is deterministic across re-renders (CR-1 / sha256 seeding
   preserved); two consecutive renders produce byte-identical companion
   JSON `model_order_color_keys` arrays.
7. `reviewer_response.md` carries the new `## Marginal-convergence finding
   (post-r2 investigation)` subsection with the 4.44e-16 routing-verification
   claim + the architecture-discrimination redirect.
8. `methods_full.md` carries the 2-sentence cross-reference at the end of
   the OD-scale evaluation section.
9. `peer_review_remediation.md` carries the new R2-follow-up subsection.
10. `git diff` against pre-14-15 main shows zero changes to
    `revision/core/` (D-14-22 preserved).

After T5 passes, the paper-revision presentation layer carries the
histogram-density comparable column + the qq_overlay discriminating figure
+ the marginal-convergence reviewer disclosure; only **14-07 (Zenodo deposit
+ tag + DOI wiring + release.md)** remains outstanding in Phase 14.
</verification>

<success_criteria>
The pre-v1.0 50-bin histogram-density Wasserstein "distribution EMD" is
reintroduced as a third comparable metric column in
`revision/docs/reconciliation_note.md` alongside the existing OD raw-sample
EMD and the log-return raw-sample EMD already present in
`matched2000_dualscale.json`; the histogram-density column is computed by
the NEW top-level emitter `revision/run_distribution_emd.py` (NOT in
`revision/core/`, D-14-22 preserved) reading the existing per-seed
`samples.npy` files (NO retraining); the new `distribution_emd.json`
aggregator carries `metric_formulation` matching the C-3 disclosure citation
in `reconciliation_note.md` word-for-word so the matched-2000ep numbers are
commensurate with the pre-v1.0 paper headline (~0.0015) under the SAME 50-bin
convention for the first time since the v1.0 raw-sample switch. A single new
discriminating figure `revision/results/figures/qq_overlay.{png,pdf,json}`
(Option A — 9-model QQ overlay + delta-QQ panel) is emitted with `render_only:
false`, `convergence_stats` carrying 9 per-model max-abs-quantile-diff
entries (8/9 ≤ 0.03 OD-units; wgan_cnn ~0.69 upper-tail), and CR-1
sha256-seeded determinism preserved across re-renders. The 9 per-model
`qq_{model}.json` companions gain an additive `caption_note` field
referencing `qq_overlay.png`; per-model PNG/PDFs are NOT regenerated.
Reviewer-facing communication is updated: `revision/docs/reviewer_response.md`
carries a new `## Marginal-convergence finding (post-r2 investigation)`
subsection with the 4.44e-16 floating-point routing-verification claim
between independent reconstruction from `samples.npy` and the rendered
companion JSON values across all 9 models, the explicit ≤0.03 OD-units /
~0.69 wgan_cnn upper-tail numbers, and the architecture-discrimination
redirect to dependence-structure figures (`qq_overlay.png`,
`training_convergence_all_models.png`, `failure_modes_summary.png`,
`seed_variance_per_model.png`, `methods_full.md §3.x`);
`revision/docs/methods_full.md` carries a 2-sentence cross-reference at the
end of the OD-scale evaluation section;
`revision/docs/paper_blocks_framing.md` conditionally references the finding
in the PAPER-01 keyed framing block (if a natural insertion point exists);
`revision/docs/peer_review_remediation.md` carries a new `## R2 follow-up —
marginal-convergence finding (Plan 14-15)` subsection enumerating the
user-observed visual concern, the empirical verification result, and the
resolution pointer. The v2.1 gate (D-14-16 byte-frozen post-14-14 T1) runs
against all 10 paper-facing docs PASSING; the histogram-density EMD literals
in `reconciliation_note.md` resolve to `distribution_emd.json#aggregates`;
the `0.03` and `0.69` literals in `reviewer_response.md` + `methods_full.md`
resolve to `qq_overlay.json#convergence_stats`; the `4.44e-16` literal
resolves to `qq_overlay.json#routing_verification` (or sibling JSON).
`revision/docs/completeness_sweep_manifest.md` carries a new `## Plan 14-15`
section at the bottom enumerating the T1–T5 deliverables (existing 14-09 /
14-10 / 14-11 / 14-12 / 14-13 / 14-14 sections preserved verbatim).
`.planning/phases/14-paper-revision-release-freeze/14-15-SUMMARY.md` exists
with Self-Check PASS + 5 task commit SHAs + a final-state line declaring
Phase 14 incomplete plans = `[14-07]` only. `revision/core/` is byte-frozen
throughout (D-14-22); no edit to `revision/verify_number_provenance.py`
(D-14-16); no edit to `main (4) copy.tex` / `supp_material.tex` (D-14-18);
the existing _strict_accept equality check (D-14-13) is untouched. NO
retraining; NO new figures beyond `qq_overlay`; NO classical sweep re-run.
After this plan completes, only 14-07 (Zenodo deposit + tag + DOI wiring +
release.md) remains in Phase 14.
</success_criteria>

<mechanics>
Mirrors established Phase 14 protocol (same protocol as 14-13 / 14-14):

- Plan authored via `gsd-planner` agent; validated by `gsd-plan-checker`
  before execution (the orchestrator handles checker invocation as a
  separate step — this planner does NOT spawn the checker itself).
- Execution via **foreground** `gsd-executor` with `isolation="worktree"`.
  **Background subagents auto-deny Bash for the first git safety call** —
  this is a documented failure mode that bit 8× in earlier Phase 14 work.
  14-15 must be spawned foreground; see
  `~/.claude/projects/-Users-shawngibford-dev-phd-qGAN/memory/project_quantum_sim_execution.md`
  for the canonical reference on the foreground-executor requirement +
  stall-watchdog + subagent-permission settings for compute-heavy GSD phases.
- Per-task atomic commits (one `feat(14-15): <task-tag>` commit each for
  T1–T3, `docs(14-15): <task-tag>` for T4–T5, plus the final SUMMARY
  commit). 5 task commits + 1 SUMMARY commit + orchestrator merge:
  - `feat(14-15): histogram-density distribution-EMD aggregator (50-bin Wasserstein, pre-v1.0 formulation reintroduced)` (T1)
  - `feat(14-15): reconciliation_note.md 3-column comparable metric table (OD raw / log-return raw / 50-bin histogram-density EMD)` (T2)
  - `feat(14-15): qq_overlay figure (9-model QQ + delta-QQ panel) — single discriminating figure for OD marginal convergence` (T3)
  - `docs(14-15): reviewer_response.md + methods_full.md + paper_blocks_framing.md marginal-convergence finding + peer_review_remediation r2 follow-up` (T4)
  - `docs(14-15): SUMMARY + completeness_sweep_manifest update + end-to-end re-verification` (T5)
- Pre-existing dirty working-tree files (deleted LICENSE + modified
  `.gitignore`/`fake.csv`/`real.csv`/notebook/`phase4_validation.json`) get
  stash-cycled across the merge. None of T1–T5 touches `.gitignore`, so it
  stays in the stash this round.
- STATE.md / ROADMAP.md owned by orchestrator (post-wave update).
- ROADMAP updates (applied in the same write pass as this plan, per the
  approved plan's "ROADMAP.md edits" section):
  1. `**Plans**: 14 plans` → `**Plans**: 15 plans` (line 48)
  2. D-14-23 scope note (line 50) extended with the 14-15 paragraph
     (distribution-EMD column reintroduction + qq_overlay discriminating
     figure + marginal-convergence finding + reviewer-facing comms)
  3. New Wave 13 entry inserted after the existing Wave 12 entry (around
     lines 77-78) referencing 14-15-PLAN.md with the supports-PAPER-01/02/08/09
     + INFRA-03 tag
  4. Progress table row `12/14 → 13/14 → 13/15` (the orchestrator's
     mid-plan flip from `12/14` to `13/14` happened post-14-14 merge; this
     plan's authoring flips the denominator from `13/14` to `13/15` and the
     post-merge tracking update will flip to `14/15`)

## Resume instructions if a session ends mid-task

Each task within 14-15 stands alone and is atomically committed. Re-running
`/gsd-execute-phase 14` after `/clear` resumes from the first incomplete plan
(still 14-07, plus 14-15 if mid-stream). 14-15 must be spawned **foreground**
(background subagents auto-deny Bash for the first worktree-safety git call —
see `~/.claude/projects/-Users-shawngibford-dev-phd-qGAN/memory/project_quantum_sim_execution.md`).
</mechanics>

<output>
Create `.planning/phases/14-paper-revision-release-freeze/14-15-SUMMARY.md` when done
</output>
