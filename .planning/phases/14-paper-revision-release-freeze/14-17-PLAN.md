---
phase: 14-paper-revision-release-freeze
plan: 17
type: execute
wave: 15
depends_on: []
files_modified:
  - "main (4) copy.tex"
  - supp_material.tex
gap_closure: true
autonomous: true
requirements: [PAPER-01, PAPER-02, PAPER-03, PAPER-04, PAPER-05, PAPER-06, PAPER-07, PAPER-08, PAPER-09, PAPER-10, PAPER-11]
must_haves:
  truths:
    - "The manuscript .tex contains the revised PAPER-01..11 text — a grep for the un-revised overclaim phrases returns zero matches"
    - "The abstract of `main (4) copy.tex` no longer claims 'high fidelity' or 'strong performance' (SYNTHESIS H3)"
    - "The stale headline DTW 0.6843 is no longer presented as a bare live result anywhere in the manuscript"
    - "The Data Availability section carries a Zenodo DOI placeholder for plan 14-07 to mint into"
    - "Notation is unified on r_t; the 20L/300L LUCY mismatch and the malformed mid-sentence \\label are fixed"
  artifacts:
    - path: "main (4) copy.tex"
      provides: "Revised main manuscript with all PAPER-01..11 blocks integrated, de-overclaimed abstract, DOI placeholder, DTW headline resolved"
      contains: "matched parameter budget"
    - path: "supp_material.tex"
      provides: "Revised supplementary with PAPER-04/05b/10 blocks, r_t notation, 20L LUCY caption"
      contains: "r_t"
  key_links:
    - from: "main (4) copy.tex"
      to: "results/matched2000_dualscale.json"
      via: "matched-budget DTW ~0.30 replaces / reconciles the stale 0.6843 headline"
      pattern: "0\\.30|frozen pre-v1.0"
    - from: "main (4) copy.tex Data Availability"
      to: "plan 14-07 Zenodo deposit"
      via: "DOI placeholder string the 14-07 INFRA-03 block mints into"
      pattern: "zenodo|DOI"
---

<objective>
Integrate the complete PAPER-01..11 revised LaTeX blocks from
`docs/paper_blocks_framing.md` and
`docs/paper_blocks_refs_methods.md` into the actual manuscript files
`main (4) copy.tex` and `paper/supp_material.tex`. The r1/r2/r3 revision currently
exists only in markdown — the `.tex` files on disk are the un-revised
pre-revision version carrying overclaiming language and a stale headline
number. This plan closes peer-review r4 SYNTHESIS findings C2, C3, H2, H3, H4,
M6, M7, M8, M9.

Purpose: A DOI minted by plan 14-07 over the current `.tex` would freeze either
no manuscript or the un-revised overclaiming manuscript with a number the
project's own audit docs internally retract (DTW 0.6843). This plan makes the
manuscript consistent with the certified evidence base before the freeze.

Output: Revised `main (4) copy.tex` and `paper/supp_material.tex` with every PAPER-*
block applied, the abstract de-overclaimed, the stale DTW headline resolved, a
Zenodo DOI placeholder added, and notation/label defects fixed. The downstream
consumer is plan 14-07, which freezes the tree and mints the DOI — 14-07 MUST
NOT run until this plan completes.

Recorded deviation: the existing paper-block docs mark the `.tex` as READ-ONLY
(D-14-18) on the assumption the manuscript of record was Overleaf-external. The
r4 freeze review (Agent 5 BLOCK, C-3) establishes that the `.tex` must instead
be revised and committed into the tagged tree. This plan therefore edits the
`.tex` directly; D-14-18 is superseded for Phase 14 by the r4 BLOCK verdict.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-paper-revision-release-freeze/peer-review-r4/SYNTHESIS.md
@.planning/phases/14-paper-revision-release-freeze/peer-review-r4/manuscript-consistency-review.md
@docs/paper_blocks_framing.md
@docs/paper_blocks_refs_methods.md
@docs/reconciliation_note.md
@docs/methods_full.md

<interfaces>
<!-- Manuscript files and their state, verified this session -->

Both manuscript files exist in the working tree as UNTRACKED files (note the
literal spaces in `main (4) copy.tex` — always quote the path):
- `main (4) copy.tex` (30 KB)
- `paper/supp_material.tex` (24 KB)

The PAPER-* blocks are fully authored in the two `paper_blocks_*.md` docs as
keyed BEFORE/AFTER pairs. Each block states: target file, `\label`/anchor,
`:NNN` line citation, verbatim BEFORE text, copy-paste AFTER LaTeX, rationale.
Apply the AFTER text by matching the BEFORE text in the actual `.tex` file.

CRITICAL — the manuscript on disk is OLDER than the framing doc's stated
baseline (Agent 5: the PAPER-02b BEFORE block quotes `main:266` as already
de-numeralized, but the actual `.tex` line 266 still reads "...DTW score of
0.6843"). Therefore: do NOT blind-match the BEFORE text. For each block, locate
the anchor by `\label`/section name, read the surrounding lines in the actual
`.tex`, and apply the AFTER block's intent — replacing whatever pre-revision
text is actually present.

ABSTRACT (SYNTHESIS H3) — the abstract at `main (4) copy.tex` ~line 49 still
carries the pre-revision overclaim. Two phrases must be removed/softened: "Our
results demonstrate strong performance in capturing temporal dynamics..." and
"The synthetic data showed high fidelity to actual historical experimental
data." There is no dedicated PAPER-* block for the abstract (the framing doc
predates the abstract finding); apply the de-overclaiming intent of the LOCKED
PAPER-02 set to the abstract by hand — reframe to a scoped, non-superiority
proof-of-concept statement consistent with the matched-budget evidence (e.g.
the synthetic OD series reproduces the marginal and temporal structure of the
historical data within the evaluated metrics, as a proof-of-concept; no "high
fidelity" / "strong performance" superlative survives).

Stale headline DTW: the literal `0.6843` appears 3 times — `main (4) copy.tex`
line ~190, line ~266, and `paper/supp_material.tex` line ~290. The matched-budget
quantum OD-DTW cluster is ~0.298-0.302 (iqp_sel_55_repro 0.3019) and the
frozen-checkpoint headline is 0.2608, sourced from
`results/matched2000_dualscale.json#aggregates` and
`docs/reconciliation_note.md`. `methods_full.md:427-434` already
documents 0.6843 as a pre-v1.0 best-case number not re-emitted under the
matched-budget contract.

LaTeX-only edits — no LaTeX compilation is required or expected here (no
toolchain assumed). Verification is structural (grep) over the `.tex` text.

NUMBER-PROVENANCE GATE OVER `.tex` (WARNING 5 resolution): the freeze gate
`scripts/verify_number_provenance.py` is normally run over the paper-facing `.md` docs,
not `.tex`. This plan introduces numeric literals into `main (4) copy.tex`
(PAPER-03 structural numbers, PAPER-08 dataset numbers, PAPER-09 evaluation-scale
table values, the matched-budget DTW ~0.30). Task 3 therefore runs
`verify_number_provenance.py --target "main (4) copy.tex"` (and on
`paper/supp_material.tex`) as an explicit provenance check so the `.tex` numbers are
not gate-exempt by silence. If `scripts/verify_number_provenance.py` does not accept a
`.tex` target (it may be `.md`-scoped by design), Task 3 instead records an
explicit, concrete gate-exemption rationale in the SUMMARY: every numeric
literal introduced resolves to a named `results/*.json` value or a
named config-lock JSON (enumerated per-literal), and the `.md` provenance gate
already certifies those same source values — the `.tex` is a downstream render
of already-gated numbers. Do not leave the `.tex` numbers silently unchecked.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integrate PAPER-01/02/03/04/05 framing + de-overclaiming blocks, de-overclaim the abstract</name>
  <read_first>
    - docs/paper_blocks_framing.md (PAPER-01..05 — every BEFORE/AFTER block, especially the LOCKED D-14-20 PAPER-02a/b/c/d set)
    - "main (4) copy.tex" (the actual pre-revision manuscript — read the abstract at ~line 49, §1.4, §2.4, §3.1-3.2, §4, Key Contributions, Theoretical/Practical Implications, Concluding Remarks, Future Work)
    - supp_material.tex (Data Transformation Details §A.7, the "reduced mode collapse" bullets at ~135/~334, Table A2 at ~225-248)
    - .planning/phases/14-paper-revision-release-freeze/peer-review-r4/manuscript-consistency-review.md (Agent 5 — exact line citations for each un-applied block, including the abstract H-2/H3 finding)
  </read_first>
  <action>
    Apply every PAPER-01..05 block from `docs/paper_blocks_framing.md`
    into `main (4) copy.tex` and `paper/supp_material.tex` by locating each block's
    anchor (`\label`/section heading/verbatim sentence fragment) in the actual
    `.tex` and replacing the pre-revision text with the block's AFTER LaTeX.
    Apply, at minimum: PAPER-01a (reframed central question, §1.4), PAPER-01b
    (softened quantum-necessity transition, §2.4). PAPER-02 is LOCKED (D-14-20)
    — all four sub-blocks are mandatory: PAPER-02a (satisfied by 02b's anchor at
    §2.4), PAPER-02b (Key Contributions sentence — remove "high fidelity ...
    industrial bioprocesses" and "successfully generates ... with high
    fidelity", closes H2), PAPER-02c (Concluding Remarks §296 scoped reframe),
    PAPER-02d (Theoretical/Practical Implications §276 — remove "computational
    advantages" + the two `paper/supp_material.tex` "reduced mode collapse" bullets at
    ~135/~334 + the `main:174` mode-collapse caveat). PAPER-03 (new "Circuit
    Design Rationale" subsection inserted after `\subsection{QWGAN-GP
    Architecture Overview}`). PAPER-04 (`paper/supp_material.tex` Data Transformation
    Details — bioprocess growth-rate justification for log-returns). PAPER-05a
    (new `\subsection*{Outlook}` demoting decision-tree + Hybrid-GAN out of
    contributions), PAPER-05b (`paper/supp_material.tex` Table A2 aspirational
    caveat). PAPER-05c is handled in Task 3. For PAPER-02b's anchor: the actual
    line reads "...DTW score of 0.6843..." not the doc's de-numeralized BEFORE
    — apply the AFTER block, which drops that sentence's DTW number entirely
    (the DTW result moves to Section 4). Do NOT alter any numeric literal that
    the AFTER block does not change; the AFTER blocks contain no hand-typed
    result numbers (verified — PAPER-01/02/04/05 are qualitative; PAPER-03's
    structural numbers 5/3/55/75/135/4/8/10/2/2000 resolve to model_info.json /
    canonical_config_lock.json / ansatz_comparison.json).
    DE-OVERCLAIM THE ABSTRACT (SYNTHESIS H3, Agent 5 H-2): the abstract at
    `main (4) copy.tex` ~line 49 carries two un-corrected overclaims. Remove the
    superlative framing — "Our results demonstrate strong performance in
    capturing temporal dynamics of bioprocess data" must be reworded to a
    scoped, non-superiority statement (e.g. the model reproduces the temporal
    dynamics of the OD series within the evaluated metrics), and "The synthetic
    data showed high fidelity to actual historical experimental data" must be
    removed or reworded so the words "high fidelity" do not appear. Reframe the
    abstract consistently with the LOCKED PAPER-02 de-overclaiming register and
    the proof-of-concept scoping already used in PAPER-02c — the abstract must
    NOT claim "high fidelity" or "strong performance" after this task. Keep the
    abstract under its 150-word cap and keep the proof-of-concept framing intact.
  </action>
  <verify>
    <automated>grep -q "matched parameter budget" "main (4) copy.tex" && grep -q "Circuit Design Rationale" "main (4) copy.tex" && grep -q "Outlook" "main (4) copy.tex" && ! grep -qi "high fidelity, effectively capturing the underlying dynamics of industrial bioprocesses" "main (4) copy.tex" && ! grep -qi "reduced susceptibility to mode collapse" supp_material.tex && grep -qi "aspirational" supp_material.tex && grep -qi "specific growth rate" supp_material.tex && ! grep -qi "high fidelity" "main (4) copy.tex" && ! grep -qi "demonstrate strong performance" "main (4) copy.tex" && echo PAPER_01_05_OK</automated>
  </verify>
  <acceptance_criteria>
    - `main (4) copy.tex` contains the PAPER-01a falsifiable-question reframe and the PAPER-01b "open empirical question" transition
    - All four PAPER-02 (LOCKED) sub-blocks applied: the "high fidelity ... industrial bioprocesses" / "successfully generates ... with high fidelity" Key Contributions overclaim is gone; "computational advantages" removed from §276; both `paper/supp_material.tex` "reduced susceptibility to mode collapse" clauses softened
    - The abstract at `main (4) copy.tex` ~line 49 no longer contains the phrase "high fidelity" and no longer contains the phrase "strong performance" — verified by `! grep -qi "high fidelity" "main (4) copy.tex"` and `! grep -qi "demonstrate strong performance" "main (4) copy.tex"` (SYNTHESIS H3 closed)
    - The reworded abstract retains the proof-of-concept framing and stays within the 150-word cap
    - A new `\subsection{Circuit Design Rationale}` exists in `main (4) copy.tex` immediately after `\subsection{QWGAN-GP Architecture Overview}`, containing the qubit-count, expressibility-vs-trainability, and classical-critic sub-points
    - A new `\subsection*{Outlook}` exists; the decision-tree and Hybrid-GAN material is moved out of `\subsection{Key Contributions and Findings}`
    - `paper/supp_material.tex` Data Transformation section uses the bioprocess growth-rate ($\mathrm{OD}\propto e^{\mu t}$) justification; Table A2 caption + closing sentence marked aspirational/not-validated
    - No numeric result literal was introduced that does not resolve to a `results/*.json` value (PAPER-03 structural numbers are the only numbers added and they resolve per the framing doc footer)
  </acceptance_criteria>
  <done>All PAPER-01..05 framing and LOCKED de-overclaiming blocks are integrated into the two `.tex` files; the abstract no longer claims "high fidelity" or "strong performance"; the overclaim phrases are absent; the Circuit Design Rationale and Outlook subsections exist.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate PAPER-06/07/08/09/10 references + methods blocks</name>
  <read_first>
    - docs/paper_blocks_refs_methods.md (PAPER-06..11 — every BEFORE/AFTER block)
    - "main (4) copy.tex" (§1.3, §1.4, §2.4, §3.1, §3.2, §4.1 — the citation anchors and the Methods/Results insertion points)
    - supp_material.tex (§A.2.3, §A.3 Hybrid-GAN, Table A2)
    - results/model_info.json (PAPER-08 dataset numbers — independent_campaigns, raw_csv_rows, log_return_rows, window_length/stride, rolling_windows, train/val/test_windows, seed_set)
    - results/fidelity_dualscale.json (PAPER-09 evaluation-scale table — the quantum Pipeline B seed-42 values per metric_name/scale)
  </read_first>
  <action>
    Apply PAPER-06..10 blocks from `docs/paper_blocks_refs_methods.md`.
    PAPER-06 (reference surgery, R1-m1): apply the sentence-rewrites for
    refs [27]/[28]/[39]/[18]/[19]/[41]/[55]-[57]/[59] at their `main (4)
    copy.tex` / `paper/supp_material.tex` anchors — these are `\cite{}`-keyed
    in-text edits. `.bib` entries are NOT added here (the `paper/bib.bib` file is not
    in the repo working tree; the bib-entry additions are out of scope for the
    `.tex` integration and are recorded in the SUMMARY as an Overleaf-side
    action). PAPER-07 (add Bernal et al. `\cite{bernal2022perspectives}` at the
    §1.3→§1.4 transition — apply the softened in-text sentence; same `.bib`
    note). PAPER-08 (new "Dataset and preprocessing" paragraph in §3.2 after the
    data-logging sentence — paste the copy-paste LaTeX block verbatim including
    the `% source:` annotations; the literals 778/777/10/2/384/0 and the
    seed_set resolve to `model_info.json`). PAPER-09 (new "Evaluation scale"
    paragraph + `tbl:eval_scale` table in §4.1 before the DTW paragraph — paste
    the block verbatim; every table value is the exact stored value from
    `fidelity_dualscale.json`, full precision, do not round). PAPER-10
    (`paper/supp_material.tex` §A.3 — relabel the Hybrid-GAN section as "Proposed
    Extension (Outlook)", add the "not implemented in this study" banner, insert
    the log-GAN-vs-Wasserstein clarification paragraph, apply the Table A2
    caveat — note PAPER-10.c overlaps PAPER-05b; apply once). PAPER-11 is
    handled in Task 3. Preserve every `% source:` provenance comment from the
    pasted blocks verbatim.
  </action>
  <verify>
    <automated>grep -q "Dataset and preprocessing" "main (4) copy.tex" && grep -q "Evaluation scale" "main (4) copy.tex" && grep -q "tbl:eval_scale" "main (4) copy.tex" && grep -q "bernal2022perspectives" "main (4) copy.tex" && grep -q "0.1209437521974767" "main (4) copy.tex" && grep -qi "Proposed Extension" supp_material.tex && grep -qi "not implemented" supp_material.tex && echo PAPER_06_10_OK</automated>
  </verify>
  <acceptance_criteria>
    - PAPER-06 sentence-rewrites applied: the healthcare/RCGAN, optimization-only, quantum-kernel, GPR, time-series-GAN, rolling-window, and VQE/QAOA citation fixes are present in the in-text sentences
    - `\cite{bernal2022perspectives}` appears at the §1.3→§1.4 transition with the softened "may offer advantages" wording
    - A "Dataset and preprocessing" paragraph exists in §3.2 with the PAPER-08 block verbatim including `% source:` annotations; dataset numbers (778, 777, 384, 10, 2) match `model_info.json`
    - An "Evaluation scale" paragraph + `\label{tbl:eval_scale}` table exists in §4.1; every table value is byte-identical to the stored `fidelity_dualscale.json` value (full precision, not rounded)
    - `paper/supp_material.tex` §A.3 Hybrid-GAN is relabelled "Proposed Extension (Outlook)" with the "not implemented" banner and the log-GAN-vs-Wasserstein clarification paragraph
    - Bib-entry additions are recorded in the SUMMARY as an Overleaf-side follow-up (the `.tex` integration does not depend on them)
  </acceptance_criteria>
  <done>PAPER-06..10 reference and methods blocks are integrated; the Dataset/Evaluation-scale Methods material and the Hybrid-GAN relabel are in the `.tex`; provenance annotations preserved.</done>
</task>

<task type="auto">
  <name>Task 3: Resolve the stale DTW 0.6843 headline, add the Zenodo DOI placeholder, fix notation + LUCY defects (PAPER-11), provenance-check the .tex numbers</name>
  <read_first>
    - "main (4) copy.tex" (the 3 occurrences of 0.6843 at ~190/~266; the Data Availability section ~291-292; the malformed `\label{fig:lucy}` at ~178; the ACF caption "log $\delta$" at ~250; abstract ~49)
    - supp_material.tex (the 0.6843 at ~290; the LUCY caption ~346; the r_t definition ~356; Data Availability ~295-298)
    - docs/reconciliation_note.md (the matched-budget DTW comparable table — quantum OD-DTW 0.298-0.302)
    - docs/methods_full.md (lines 427-457 — the existing honest framing for 0.6843 as a pre-v1.0 checkpoint, and the Orlandi ~6.5x reconciliation; mirror this wording into the manuscript)
    - docs/paper_blocks_refs_methods.md (PAPER-11 — typos + notation unification blocks)
    - verify_number_provenance.py (the provenance gate — confirm whether it accepts a `.tex` `--target`; do not edit it)
  </read_first>
  <action>
    Resolve SYNTHESIS C2/H4 — the stale headline DTW. At each of the 3
    occurrences of `0.6843` (`main (4) copy.tex` ~190 and ~266,
    `paper/supp_material.tex` ~290), apply the methods_full.md:427-457 framing: either
    (a) replace `0.6843` with the matched-budget quantum OD-DTW (the
    iqp_sel_55_repro cluster value ~0.30 from `matched2000_dualscale.json`, or
    the frozen-checkpoint headline 0.2608 if the sentence is the
    frozen-checkpoint headline), OR (b) keep `0.6843` only if it is explicitly
    labelled "frozen pre-v1.0 best-case checkpoint" with the matched-budget
    number stated alongside in the same sentence and a pointer to the
    reconciliation. A bare unlabelled `0.6843` must NOT survive. Recommended:
    use option (a) for the Key Contributions / Results sentences and ensure the
    Orlandi comparison (currently anchored to 0.6843) is re-anchored to the
    matched-budget ~0.30 with the ~6.5x improvement per methods_full.md:451-457
    — the comparison becomes stronger, not weaker, and reproducible. The Orlandi
    reference DTW 1.954 is retained as a labelled comparator.
    Resolve SYNTHESIS M6 — add a Zenodo DOI placeholder to the Data
    Availability section (`main (4) copy.tex` ~291-292 and
    `paper/supp_material.tex` ~295-298): alongside the existing GitHub URL, add an
    "archived snapshot, DOI: \texttt{10.5281/zenodo.XXXXXX} (to be assigned)"
    placeholder string so plan 14-07's INFRA-03 block has a destination to mint
    into. Do NOT invent a real DOI — use an explicit placeholder.
    Apply PAPER-11 (SYNTHESIS M7/M8/M9): unify the return-variable notation on
    `r_t` everywhere (replace every "log $\delta$" / stray return symbol in the
    ACF caption ~250 and elsewhere with `r_t`); fix the 20L/300L LUCY mismatch
    and the malformed mid-sentence `\label{fig:lucy}` at `main (4) copy.tex`
    ~178 (apply PAPER-11 item 4 AFTER block — remove the orphan `\label`, use
    `\ref`, make the volume description a complete correct sentence); fix the
    `paper/supp_material.tex` LUCY caption ~346 (PAPER-11 item 3 — `\textregistered`,
    correct volume); apply the remaining PAPER-11 typo items (missing space
    "A5).This", "Dry Biomass"→"dry biomass", "bio-manufacturing"→
    "biomanufacturing", "QWGAN-GPs"→"QWGAN-GP").
    PROVENANCE-CHECK THE `.tex` NUMBERS (WARNING 5 — `.tex` numbers must not be
    gate-exempt by silence). After all `.tex` edits across Tasks 1-3 are
    applied, run `./qgan_env/bin/python scripts/verify_number_provenance.py
    --target "main (4) copy.tex"` and the same for `paper/supp_material.tex`. If the
    gate accepts a `.tex` target, it MUST exit 0 / PASS — if it fails, a numeric
    literal introduced by Tasks 1-3 (PAPER-03 structural numbers, PAPER-08
    dataset numbers, PAPER-09 evaluation-scale table values, the matched-budget
    DTW ~0.30) does not resolve; fix the `.tex` literal to the named
    `results/*.json` value until it passes. If the gate does NOT accept
    a `.tex` target (it is `.md`-scoped by design), record an explicit
    gate-exemption rationale in the 14-17 SUMMARY: enumerate every numeric
    literal introduced into the `.tex` and name the `results/*.json` or
    config-lock JSON each resolves to, and state that the `.md` provenance gate
    already certifies those same source values so the `.tex` is a downstream
    render of already-gated numbers. Do not leave the `.tex` numbers silently
    unchecked.
  </action>
  <verify>
    <automated>test "$(grep -c '0\.6843' "main (4) copy.tex" supp_material.tex 2>/dev/null | awk -F: '{s+=$2} END{print s}')" = "0" || grep -l 'frozen pre-v1.0' "main (4) copy.tex" supp_material.tex >/dev/null; grep -qi 'zenodo' "main (4) copy.tex" && ! grep -qi 'log \$\\delta\$' "main (4) copy.tex" && ! grep -q 'Figure \\label{fig:lucy}' "main (4) copy.tex" && ! grep -qi 'Dry Biomass' "main (4) copy.tex" && { ./qgan_env/bin/python scripts/verify_number_provenance.py --target "main (4) copy.tex" || echo "TEX_GATE_EXEMPT — rationale recorded in SUMMARY"; } && echo PAPER_11_OK</automated>
  </verify>
  <acceptance_criteria>
    - No bare unlabelled `0.6843` survives in either `.tex` file; every surviving occurrence (if any) is explicitly labelled "frozen pre-v1.0 ... checkpoint" with the matched-budget number stated in the same sentence
    - The Key Contributions / Results DTW statements reference the matched-budget OD-DTW (~0.30) sourced from `matched2000_dualscale.json`; the Orlandi 1.954 comparison is re-anchored to the matched-budget number
    - The Data Availability section in `main (4) copy.tex` (and `paper/supp_material.tex`) contains an explicit Zenodo DOI placeholder string (e.g. `10.5281/zenodo.XXXXXX`, "to be assigned") — not a fabricated real DOI
    - The return symbol is unified on `r_t`: no `log $\delta$` survives; the ACF caption uses `r_t`
    - The malformed mid-sentence `\label{fig:lucy}` at `main:178` is removed and replaced with a `\ref`; the 20L/300L description is a single coherent sentence; the `paper/supp_material.tex` LUCY caption uses `\textregistered` and a consistent volume
    - The PAPER-11 typo items (space after "A5).", "dry biomass", "biomanufacturing", "QWGAN-GP" singular) are applied
    - `scripts/verify_number_provenance.py` was run against `"main (4) copy.tex"` and `paper/supp_material.tex`: either it exits 0 (PASS), OR the SUMMARY records a concrete per-literal gate-exemption rationale naming the source JSON for every `.tex` numeric literal introduced — the `.tex` numbers are NOT left silently unchecked
  </acceptance_criteria>
  <done>The stale 0.6843 headline is reconciled to the matched-budget evidence, the Data Availability section has a DOI placeholder for 14-07, the PAPER-11 notation/typo/LUCY defects are fixed, and the `.tex` numbers are provenance-checked or explicitly gate-exempted with a recorded rationale.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| markdown revision layer → manuscript .tex | the revision exists only in `paper_blocks_*.md`; un-integrated, the frozen DOI cites the un-revised paper |
| stale pre-v1.0 number → manuscript of record | a bare 0.6843 frozen into a cited paper is a permanent citation to a retracted number |
| placeholder DOI → 14-07 mint site | a malformed/missing placeholder leaves 14-07 with nowhere to write the reserved DOI |
| .tex numeric literals → freeze gate | the freeze gate runs over `.md` docs; `.tex` numbers escape provenance verification unless explicitly checked |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-22 | Tampering | manuscript integrity (un-revised .tex) | mitigate | Task 1+2 integrate every PAPER-01..11 block + de-overclaim the abstract; grep gate confirms overclaim phrases ("high fidelity", "strong performance") absent and revised phrases present |
| T-14-23 | Tampering | stale headline number frozen into a DOI'd paper | mitigate | Task 3 removes/labels every `0.6843`; grep gate asserts no bare occurrence survives; matched-budget value sourced from JSON |
| T-14-24 | Information disclosure | fabricated DOI | mitigate | Task 3 uses an explicit `zenodo.XXXXXX` placeholder; the real DOI is minted only by 14-07 |
| T-14-32 | Tampering | un-provenanced .tex numeric literals | mitigate | Task 3 runs `scripts/verify_number_provenance.py` over both `.tex` files, or records a concrete per-literal gate-exemption rationale in the SUMMARY |
| T-14-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan; documentation/LaTeX edits only |
</threat_model>

<verification>
- `grep` confirms PAPER-01..11 revised phrases present and un-revised overclaim phrases absent in both `.tex` files
- The abstract no longer contains "high fidelity" or "strong performance" (SYNTHESIS H3)
- No bare `0.6843` survives; matched-budget DTW sourced from `matched2000_dualscale.json`
- Data Availability section carries a Zenodo DOI placeholder for plan 14-07
- Notation unified on `r_t`; malformed `\label` and 20L/300L mismatch fixed
- No result number introduced that does not resolve to a `results/*.json` value
- The `.tex` numbers are provenance-checked or explicitly gate-exempted with a recorded rationale
</verification>

<success_criteria>
The manuscript `.tex` files carry the complete r1/r2/r3 revision: every
PAPER-01..11 block integrated, the LOCKED de-overclaiming set applied, the
abstract de-overclaimed (no "high fidelity" / "strong performance"), the stale
DTW 0.6843 headline reconciled to the matched-budget evidence, a Zenodo DOI
placeholder added for plan 14-07, the notation/label defects fixed, and the
`.tex` numbers provenance-checked or explicitly gate-exempted — closing
SYNTHESIS C2, C3, H2, H3, H4, M6, M7, M8, M9. The manuscript is now consistent
with the certified JSON evidence base and ready for plan 14-07 to commit and
freeze.
</success_criteria>

<output>
Create `.planning/phases/14-paper-revision-release-freeze/14-17-SUMMARY.md` when done
</output>
</content>
</invoke>
