---
phase: 14-paper-revision-release-freeze
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - revision/core/models/quantum.py
  - revision/run_recover_canonical.py
  - revision/results/canonical_recovery.json
  - revision/results/canonical_config_lock.json
autonomous: true
requirements: [PAPER-03]
must_haves:
  truths:
    - "best_checkpoint.pt loads into a reconstructed 55-param IQP:SEL config with params_pqc shape == (55,)"
    - "The 55-param IQP:SEL circuit is selectable in quantum.py without changing the byte-frozen 75-param default path"
    - "A config-equivalence gate raises AssertionError (not bare assert) if the checkpoint does not load into the reconstructed config"
    - "The canonical Phase-09.1 preprocessing pipeline (A or B) the checkpoint was trained on is identified and recorded"
  artifacts:
    - path: "revision/core/models/quantum.py"
      provides: "config-selectable 55-param IQP:SEL circuit (non-default)"
      contains: "ansatz"
    - path: "revision/run_recover_canonical.py"
      provides: "checkpoint-driven 55-param reconstruction + equivalence hard-assert"
      min_lines: 60
    - path: "revision/results/canonical_recovery.json"
      provides: "reconstructed decomposition + checkpoint provenance (mu/sigma/epoch/emd)"
    - path: "revision/results/canonical_config_lock.json"
      provides: "locked canonical config + pinned native pipeline + checkpoint sha256"
  key_links:
    - from: "revision/run_recover_canonical.py"
      to: "best_checkpoint.pt"
      via: "torch.load(weights_only=False) + params_pqc shape gate"
      pattern: "params_pqc"
    - from: "revision/core/models/quantum.py"
      to: "55-param selectable circuit"
      via: "config-selectable switch like existing topology arg"
      pattern: "ansatz|circuit_id"
---

<objective>
Recover the lost canonical 55-parameter IQP:SEL quantum circuit (the proven-good
2000-epoch result preserved only as `best_checkpoint.pt` epoch 1969 + the 16
canonical figures), add it to `revision/core/models/quantum.py` as a
NON-default, config-selectable circuit, and hard-assert config-equivalence so
the checkpoint loads into the reconstructed config with `params_pqc` shape
`(55,)`. Identify and pin the native Phase-09.1 preprocessing pipeline for the
headline. This is Tier-1 of the D-14-22 strict gated pipeline and BLOCKS every
downstream plan (D-14-07).

Purpose: The current `revision/core` default is a 75-param circuit (qubits=5,
layers=4) — NOT the paper's quantum circuit (D-14-01/04). Every cross-model
comparison and the headline must use the reconstructed 55-param IQP:SEL. Without
a deterministic, checkpoint-driven reconstruction and a hard equivalence gate
the whole phase is unsound.

Output: A config-selectable 55-param IQP:SEL circuit in `quantum.py`, a
recovery+equivalence-assert driver, and two provenance JSON artifacts locking
the canonical config and its native pipeline.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-paper-revision-release-freeze/14-CONTEXT.md
@.planning/phases/14-paper-revision-release-freeze/14-RESEARCH.md
@.planning/phases/14-paper-revision-release-freeze/14-PATTERNS.md

<interfaces>
<!-- Extracted from revision/core/models/quantum.py and run_multiseed_rollup.py — use directly, no exploration needed -->

quantum.py existing config-selectable precedent (the EXACT shape to copy for the new circuit selector):
- `_TOPOLOGIES = ("range", "linear")` module constant; eager validation in `__init__` raising ValueError on unknown value; `assert window_length == 2 * num_qubits` invariant.
- Param formula (quantum.py:77-80): `self.num_params = num_qubits + num_layers * (num_qubits * 3) + num_qubits * 2` → (5,4)=75. 55 is NON-unique by this formula (RESEARCH Pitfall 2) — decomposition MUST be driven by the checkpoint, not a hard-coded layer count.
- `generator_circuit` indexes `params_pqc[idx]` sequentially — structural introspection target for the decomposition.

Explicit-raise integrity-gate idiom (run_multiseed_rollup.py:86-92) — use this, NOT bare `assert` (python -O strips assert):
`if <integrity violated>: raise AssertionError(f"...{detail}...")`

best_checkpoint.pt verified tensor layout (this session): keys include `epoch`(=1969), `emd`, `params_pqc`(shape (55,)), `critic_state`, `c_optimizer`, `g_optimizer`, `mu`(scalar), `sigma`(scalar). Load with `torch.load(map_location="cpu", weights_only=False)`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reverse-engineer the 55-param decomposition from the checkpoint</name>
  <read_first>
    - best_checkpoint.pt (ground truth — D-14-02; the params_pqc shape and gate-layout drive the decomposition)
    - revision/core/models/quantum.py (current 75-param impl: param formula lines 77-80, generator_circuit sequential params_pqc indexing, existing _TOPOLOGIES config-selectable precedent lines 38-63)
    - revision/core/__init__.py (NUM_QUBITS=5, NUM_LAYERS=4, WINDOW_LENGTH=10 defaults)
    - results/run_unconditioned_wgan/stats.json (canonical run statistics + config breadcrumbs for pipeline identification, D-14-06)
    - .planning/phases/14-paper-revision-release-freeze/14-RESEARCH.md (Pitfall 2: non-unique decomposition; Code Examples: checkpoint oracle assertions)
    - .planning/phases/14-paper-revision-release-freeze/14-PATTERNS.md (quantum.py:38-80 config-selectable + param-formula analog)
  </read_first>
  <action>
    Create `revision/run_recover_canonical.py`. Use the repo-root resolver +
    RESULTS anchor idiom from `run_multiseed_rollup.py:42-59` (walk up to
    `revision/core/preprocessing.py`; `RESULTS = REPO / "revision/results"`).
    Load `best_checkpoint.pt` with `torch.load(map_location="cpu",
    weights_only=False)`. Treat the checkpoint as ground truth (D-14-02):
    enumerate every candidate `(num_qubits, num_layers, gate-layout)`
    decomposition consistent with the existing `quantum.py` param formula
    family (`q + L*3q + 2q` and the encoding/final-rotation variants noted in
    RESEARCH Pitfall 2 — IQP encoding params present/absent, final RX/RY per
    qubit ×1 or ×2) and select the one that yields exactly `params_pqc.shape ==
    (55,)` AND a structurally consistent sequential `params_pqc[idx]` walk
    through a forward pass of the reconstructed circuit. Notebook git history
    (`git log -S NUM_LAYERS -- qgan_pennylane.ipynb`) is CORROBORATING ONLY
    (D-14-02) — do not let it override the checkpoint. Identify the canonical
    headline pipeline (A or B) per D-14-06 from
    `results/run_unconditioned_wgan/stats.json` + git archaeology and record
    the rationale. Write `revision/results/canonical_recovery.json` containing:
    the chosen decomposition (qubits, layers, gate layout, derived param count
    = 55), the checkpoint provenance (`epoch`=1969, stored scalar `mu`/`sigma`,
    stored `emd`, optimizer `param_groups[0].lr` for g and c), the identified
    native pipeline + identification evidence, and a sha256 of
    `best_checkpoint.pt`. Conform to the long-form schema header literals
    (`"schema"`, `"data_hash"` if a dataset hash is resolvable, otherwise omit
    with an explicit note). Do NOT recompute mu/sigma — record the
    checkpoint's stored scalars verbatim (D-14-05). Do NOT mutate the core
    default path.
  </action>
  <verify>
    <automated>./qgan_env/bin/python revision/run_recover_canonical.py --recover-only && ./qgan_env/bin/python -c "import json,sys; d=json.load(open('revision/results/canonical_recovery.json')); sys.exit(0 if d['param_count']==55 and d['checkpoint_epoch']==1969 and d['native_pipeline'] in ('A','B') else 1)"</automated>
  </verify>
  <acceptance_criteria>
    - `revision/results/canonical_recovery.json` exists and `param_count == 55`, `checkpoint_epoch == 1969`, `native_pipeline` is `"A"` or `"B"`
    - JSON records stored scalar `mu` and `sigma` copied verbatim from the checkpoint (no recompute) — grep `canonical_recovery.json` for `"mu"` and `"sigma"` keys with the checkpoint values
    - JSON records a `checkpoint_sha256` field and the chosen `(num_qubits, num_layers, gate_layout)` decomposition
    - `git diff --stat revision/core/__init__.py` is empty (default path untouched)
  </acceptance_criteria>
  <done>Decomposition selected deterministically from the checkpoint, native pipeline identified, provenance JSON written, core default unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Add the 55-param IQP:SEL as a config-selectable non-default circuit</name>
  <read_first>
    - revision/core/models/quantum.py (existing _TOPOLOGIES config-selectable switch lines 38-63, generator_circuit sequential params_pqc indexing, param formula 77-80 — replicate this exact selector shape)
    - revision/results/canonical_recovery.json (the decomposition produced by Task 1 — the exact qubits/layers/gate-layout to wire in)
    - revision/results/parity_check.json (Phase-8 zero-drift parity baseline — model for the equivalence-assert harness)
    - .planning/phases/14-paper-revision-release-freeze/14-PATTERNS.md (config-selectable switch pattern + explicit-raise gate idiom)
  </read_first>
  <action>
    In `revision/core/models/quantum.py`, add the reconstructed 55-param
    IQP:SEL as a NON-default, config-selectable circuit using the IDENTICAL
    shape as the existing `_TOPOLOGIES` switch (module-level allowed-values
    constant, eager validation in `__init__` raising `ValueError` on unknown
    value, default value = the existing 75-param behavior so the byte-frozen
    default path is unchanged — verify against
    `revision/results/parity_check.json`). The new selector keys the
    qubits/layers/gate-layout from `canonical_recovery.json`'s decomposition.
    Do NOT change the default constructor arguments or
    `revision/core/__init__.py`. Then extend `revision/run_recover_canonical.py`
    with an equivalence-assert mode (`--assert-equivalence`) that constructs
    the new 55-param circuit, loads `best_checkpoint.pt`'s `params_pqc` into
    it, and uses the explicit-raise idiom
    (`run_multiseed_rollup.py:86-92`) — `if tuple(ck["params_pqc"].shape) !=
    (55,): raise AssertionError(...)` plus a structural forward-pass check
    that the reconstructed circuit consumes exactly 55 params. Write
    `revision/results/canonical_config_lock.json` recording the locked
    selector value, the decomposition, the pinned native pipeline (D-14-06),
    the stored mu/sigma (D-14-05), and the `checkpoint_sha256`. Failure of the
    equivalence assert must exit non-zero (this is the D-14-07 phase-blocking
    gate).
  </action>
  <verify>
    <automated>./qgan_env/bin/python revision/run_recover_canonical.py --assert-equivalence && ./qgan_env/bin/python -c "from revision.core.models.quantum import QuantumGenerator; g=QuantumGenerator(); import inspect; print('default-constructible OK')" && git diff --stat revision/core/__init__.py | grep -qv . && echo DEFAULT_FROZEN</automated>
  </verify>
  <acceptance_criteria>
    - `revision/run_recover_canonical.py --assert-equivalence` exits 0 and loads `best_checkpoint.pt` into the 55-param circuit with shape `(55,)`
    - The equivalence gate uses `raise AssertionError(` (grep the script) — NOT a bare `assert` statement on the shape check
    - `QuantumGenerator()` with no args still constructs the 75-param default (default path byte-frozen); `git diff --stat revision/core/__init__.py` is empty
    - `revision/results/canonical_config_lock.json` exists with the locked selector value, pinned native pipeline, stored mu/sigma, and `checkpoint_sha256`
    - `quantum.py` contains a module-level allowed-values constant for the new circuit selector mirroring `_TOPOLOGIES` (grep for a `_*` tuple constant + `raise ValueError` eager-validation line)
  </acceptance_criteria>
  <done>55-param IQP:SEL selectable in quantum.py, equivalence hard-assert passes with explicit-raise, default path frozen, canonical config locked to JSON.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| checkpoint → reconstructed config | `best_checkpoint.pt` is ground truth (D-14-02); a wrong decomposition silently produces fabricated headline numbers |
| recovery script → core default path | accidental mutation of the byte-frozen default would re-baseline Phases 8–13 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Tampering | 55-param decomposition (non-unique by formula) | mitigate | Checkpoint-driven deterministic selection + D-14-07 explicit-raise equivalence gate (`raise AssertionError`, not bare assert — survives `python -O`); structural forward-pass check |
| T-14-02 | Tampering | core default path (frozen artifact integrity) | mitigate | Non-default config-selectable add only; `git diff --stat revision/core/__init__.py` empty assertion in verify |
| T-14-03 | Information disclosure | checkpoint provenance / mu-sigma | accept | mu/sigma are model normalization scalars, not secrets; recorded verbatim per D-14-05 — no PII, local artifact |
| T-14-14 | Spoofing | checkpoint identity | mitigate | `checkpoint_sha256` recorded in both recovery + lock JSON so downstream plans verify the same ground-truth tensor |
</threat_model>

<verification>
- `./qgan_env/bin/python revision/run_recover_canonical.py --assert-equivalence` exits 0
- `canonical_recovery.json` and `canonical_config_lock.json` both present with param_count 55, epoch 1969, identified native pipeline, checkpoint sha256
- Core default path byte-unchanged (`git diff --stat revision/core/__init__.py` empty; `QuantumGenerator()` still 75-param)
</verification>

<success_criteria>
The reconstructed 55-param IQP:SEL is config-selectable and non-default, the
checkpoint loads into it under an explicit-raise equivalence gate (D-14-07
phase-blocking), the native headline pipeline is pinned, and all provenance is
in JSON — Tier-1 of D-14-22 complete, unblocking the 2000ep re-execution.
</success_criteria>

<output>
Create `.planning/phases/14-paper-revision-release-freeze/14-01-SUMMARY.md` when done
</output>
