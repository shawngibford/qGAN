---
phase: 14-paper-revision-release-freeze
plan: 06
type: execute
wave: 4
depends_on: ["14-03", "14-04"]
files_modified:
  - docs/paper_blocks_refs_methods.md
  - docs/reviewer_response.md
autonomous: true
requirements: [PAPER-06, PAPER-07, PAPER-08, PAPER-09, PAPER-10, PAPER-11]
must_haves:
  truths:
    - "Misplaced references corrected and Bernal et al. added, delivered as .bib-entry + sentence-rewrite blocks keyed to \\cite{} keys (PAPER-06, PAPER-07)"
    - "Methods reports dataset details and per-metric evaluation scale, rendered from JSON (PAPER-08, PAPER-09)"
    - "Appendix A3 log-GAN vs Wasserstein discrepancy clarified (PAPER-10)"
    - "All typos and notation unified per the R1-m7 checklist (PAPER-11)"
    - "reviewer_response.md is a per-reviewer point-by-point rebuttal: comment ID -> concern -> change -> location -> supporting artifact"
    - "Every numeric literal in the blocks resolves to a results/*.json value (number-provenance gate passes)"
  artifacts:
    - path: "docs/paper_blocks_refs_methods.md"
      provides: "copy-paste LaTeX blocks for PAPER-06..11 keyed to cite-keys/label/anchor"
      min_lines: 60
    - path: "docs/reviewer_response.md"
      provides: "AIChE per-reviewer point-by-point rebuttal mapping every comment ID to a change + artifact"
      min_lines: 40
  key_links:
    - from: "docs/paper_blocks_refs_methods.md"
      to: "main (4) copy.tex / supp_material.tex cite-keys + anchors"
      via: ".bib-entry + sentence-rewrite blocks keyed to \\cite{} keys (location-independent)"
      pattern: "cite|bibitem"
    - from: "docs/reviewer_response.md"
      to: "results/*.json + figures"
      via: "every supporting-artifact cell points at a real JSON or figure path"
      pattern: "revision/results"
---

<objective>
Produce `docs/paper_blocks_refs_methods.md` — copy-paste LaTeX blocks
for PAPER-06 (reference surgery), PAPER-07 (add Bernal et al.), PAPER-08
(dataset details in Methods, from JSON), PAPER-09 (per-metric eval scale, from
`fidelity_dualscale`), PAPER-10 (Appendix A3 discrepancy), PAPER-11 (typos +
notation unify per the R1-m7 checklist) — plus `docs/reviewer_response.md`,
the AIChE per-reviewer point-by-point rebuttal (D-14-19) mapping every comment
ID to its change + supporting artifact. `.bib` is Overleaf-external (RESEARCH
A1) so PAPER-06/07 are delivered as `.bib`-entry + sentence-rewrite blocks keyed
to `\cite{}` keys (location-independent). The in-repo `.tex` is READ-ONLY
(D-14-18).

Purpose: Closes the references/methods/typos reviewer items (R1-m1, R1-m2,
R1-m3, R1-m6, R1-m7, R2-5a) and produces the point-by-point rebuttal document
that maps success-criterion 1.

Output: A keyed JSON-traceable refs/methods/typos LaTeX-blocks file and the
per-reviewer response document, both passing the number-provenance gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-paper-revision-release-freeze/14-CONTEXT.md
@.planning/phases/14-paper-revision-release-freeze/14-RESEARCH.md
@.planning/phases/14-paper-revision-release-freeze/14-PATTERNS.md
@.planning/phases/14-paper-revision-release-freeze/14-03-SUMMARY.md
@.planning/phases/14-paper-revision-release-freeze/14-04-SUMMARY.md

<interfaces>
<!-- Exact reference/methods/typo targets from RESEARCH "Phase Requirements" -->

PAPER-06 (R1-m1): reviewer plan table gives exact fixes — rewrite sentence for [27]; remove/reassign [28]; replace [39] with Havlicek 2019 + Schuld & Killoran 2019; replace [18] with GPR reference; replace [19] with time-series GAN; replace [41] with rolling-window subsequence reference; remove/replace [55]-[57],[59]; keep anchors [21]-[23],[34]-[36],[61]. `.bib` is Overleaf-external (A1) — deliver `.bib`-entry + sentence-rewrite blocks keyed to the `\cite{...}` keys in `main (4) copy.tex` (e.g. `\cite{orlandi2024enhancing}`, `\cite{Dallaire_Demers_2018}`) so they apply regardless of .bib location.

PAPER-07 (R1-m6/R2-2): add Bernal et al. "Perspectives of quantum computing for chemical engineering" cited in section 1.3/section 2 transition (line 90/146) — new `\bibitem`/`.bib` block + insertion sentence.

PAPER-08 (R1-m2): dataset details into section 3 Methods after Photobioreactor setup (line 176). Source = `results/model_info.json` + `docs/dataset_stats.md` (778 raw → 777 log-returns → 384 windows; single-campaign LUCY). MUST render from JSON (D-14-16, success-criterion 5).

PAPER-09 (R1-m3): every evaluation metric labeled transformed (log-return) or original OD. Source = `results/` `fidelity_dualscale` artifact (2000ep, dual-scale). Methods table block.

PAPER-10 (R2-5a): Appendix A3 / Hybrid-GAN math `supp_material.tex` lines 156-252 (eq:balance, eq:constraint1/2, eq:constitutive). Relabel as "proposed extension"; clarify the log-GAN vs Wasserstein equation discrepancy.

PAPER-11 (R1-m7): full checklist — Fig.6 "Laas"→"Lags"; "Figure A5).This"→"Figure A5). This"; "LUCY ©photobioreactor"→"LUCY® photobioreactor" (supp line 346 \textcopyright); 300L/20L sentence (main line 178, malformed mid-sentence label); "Dry Biomass"→"dry biomass"; "bio-manufacturing"/"biomanufacturing" standardize; Ref[39] "Approac"→"Approach"; Ref[51] title caps; "QWGAN-GPs"→"QWGAN-GP" (Concluding Remarks); single return symbol (log δ vs ς); enlarge Figs 2-6. Each = one keyed block.

reviewer_response.md (D-14-19): per-reviewer sections; each row = comment ID (from `QGAN_Review_Response_Plan.md.pdf` R1-M1..M5, R1-m1..m7, R2-1..6) → verbatim concern → change made → manuscript location (section/table/fig) → supporting artifact (`results/*.json` or figure path). Every artifact cell points at a real path (success-criterion 5).

Number-provenance gate: `verify_number_provenance.py --target <file>` (Plan 03). `docs/reconciliation_note.md` authoritative for 1000ep→2000ep changed numbers.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: PAPER-06/07/10/11 keyed reference + typo LaTeX blocks</name>
  <read_first>
    - main (4) copy.tex (READ-ONLY — `\cite{...}` keys for [18][19][27][28][39][41][55-57][59] and anchors [21-23][34-36][61]; section 1.3/2 transition line 90/146 for Bernal; line 178 malformed label fig:lucy; Fig.6/Concluding-Remarks typo locations)
    - supp_material.tex (READ-ONLY — A3 math lines 156-252 eq:balance/eq:constraint1/eq:constraint2/eq:constitutive; line 346 \textcopyright; "Figure A5).This")
    - QGAN_Review_Response_Plan.md.pdf (R1-m1 exact reference-fix table; R1-m6 Bernal; R2-5a A3 discrepancy; R1-m7 full typo checklist — verbatim concerns for the blocks)
    - docs/reconciliation_note.md (Plan 03 — any number used in a ref/typo block that changed at 2000ep)
    - .planning/phases/14-paper-revision-release-freeze/14-RESEARCH.md (Phase Requirements PAPER-06/07/10/11; Assumption A1 .bib Overleaf-external; D-14-18 read-only .tex)
  </read_first>
  <action>
    Create `docs/paper_blocks_refs_methods.md`. For PAPER-06: one
    keyed block per reference fix from the R1-m1 table — rewrite sentence for
    [27]; remove/reassign [28]; replace [39] with Havlicek 2019 + Schuld &
    Killoran 2019; replace [18] with a GPR reference; replace [19] with a
    time-series GAN reference; replace [41] with a rolling-window subsequence
    reference; remove/replace [55]-[57],[59]; explicitly note anchors
    [21]-[23],[34]-[36],[61] are RETAINED. Because `.bib` is Overleaf-external
    (A1), deliver each as a `.bib`-entry block PLUS a sentence-rewrite keyed to
    the `\cite{...}` key in `main (4) copy.tex` (location-independent), each
    with an R1-m1 rationale. For PAPER-07: a new `\bibitem`/`.bib` entry for
    Bernal et al. "Perspectives of quantum computing for chemical engineering"
    + an insertion sentence keyed to the section 1.3/section 2 transition
    (line 90/146), R1-m6/R2-2 rationale. For PAPER-10: a block relabeling the
    Appendix A3 / Hybrid-GAN math (`supp_material.tex` lines 156-252,
    eq:balance/eq:constraint1/2/eq:constitutive) as a "proposed extension" and
    clarifying the log-GAN vs Wasserstein equation discrepancy, R2-5a
    rationale. For PAPER-11: one keyed block per R1-m7 checklist item (Fig.6
    "Laas"→"Lags"; "Figure A5).This"→". This"; "LUCY ©photobioreactor"→"LUCY®
    photobioreactor" via `\textcopyright`; 300L/20L sentence at main line 178
    incl. the malformed mid-sentence label; "Dry Biomass"→"dry biomass";
    bio-manufacturing/biomanufacturing standardized; Ref[39]
    "Approac"→"Approach"; Ref[51] title caps; "QWGAN-GPs"→"QWGAN-GP"; single
    return symbol decision (log δ vs ς); enlarge Figs 2-6) with a before→after
    snippet + R1-m7 rationale. Do NOT edit any `.tex` file (D-14-18).
  </action>
  <verify>
    <automated>test -f docs/paper_blocks_refs_methods.md && for t in PAPER-06 PAPER-07 PAPER-10 PAPER-11; do grep -q "$t" docs/paper_blocks_refs_methods.md || exit 1; done && grep -q 'Bernal' docs/paper_blocks_refs_methods.md && grep -qi 'Lags' docs/paper_blocks_refs_methods.md && git diff --stat -- "main (4) copy.tex" supp_material.tex | grep -cv . | grep -q '^0$' && echo REFS_OK</automated>
  </verify>
  <acceptance_criteria>
    - `docs/paper_blocks_refs_methods.md` exists with keyed blocks tagged `PAPER-06`, `PAPER-07`, `PAPER-10`, `PAPER-11`
    - PAPER-06 has a discrete keyed block for each of [27],[28],[39],[18],[19],[41],[55-57],[59] and an explicit "retained" note for [21-23],[34-36],[61]
    - PAPER-06/07 blocks are keyed to `\cite{}` keys and include `.bib`-entry blocks (grep for `cite` and a bib entry like `@article`/`\bibitem`) — location-independent
    - PAPER-07 block names "Bernal" and "Perspectives of quantum computing for chemical engineering"
    - PAPER-11 block contains the named before→after fixes (grep "Lags", "QWGAN-GP", "biomanufacturing")
    - `git diff --stat -- "main (4) copy.tex" supp_material.tex` shows zero changes (D-14-18)
  </acceptance_criteria>
  <done>PAPER-06/07/10/11 delivered as cite-key-keyed, location-independent before→after blocks with reviewer rationale; .tex untouched.</done>
</task>

<task type="auto">
  <name>Task 2: PAPER-08/09 Methods blocks (from JSON) + reviewer_response.md + provenance gate</name>
  <read_first>
    - main (4) copy.tex (READ-ONLY — section 3 Methods after Photobioreactor setup line 176, the PAPER-08/09 insertion anchors)
    - results/model_info.json (Plan 03 — dataset + per-model numbers; PAPER-08 source, render-from-JSON)
    - docs/dataset_stats.md (Plan 03 — regenerated dataset doc: 778 raw → 777 log-returns → 384 windows, single-campaign LUCY)
    - results/ fidelity_dualscale artifact (Plan 02 — dual-scale OD + log_return metric labeling for PAPER-09)
    - docs/reconciliation_note.md (Plan 03 — any 1000ep→2000ep changed number)
    - verify_number_provenance.py (Plan 03 — the gate both new files must pass)
    - docs/training_protocol.md (Plan 03 — the sourced-table doc-structure convention to mirror in reviewer_response.md)
    - QGAN_Review_Response_Plan.md.pdf (the complete R1-M*/R1-m*/R2-* comment-ID list + verbatim concerns for reviewer_response.md)
    - .planning/phases/14-paper-revision-release-freeze/14-PATTERNS.md (reviewer_response.md structure analog: training_protocol.md sourced-row table discipline)
  </read_first>
  <action>
    Append to `docs/paper_blocks_refs_methods.md`: a PAPER-08 block —
    a Methods dataset-details LaTeX block (raw time points, rolling windows,
    train/val/test splits + counts, independent-run count) inserted after
    `main (4) copy.tex` line 176 — with every number annotated `% source:
    results/model_info.json#<path>` (render-from-JSON, D-14-16); and a
    PAPER-09 block — a Methods table labeling every evaluation metric as
    transformed (log-return) or original OD, every value annotated to the
    `fidelity_dualscale` artifact. Then create
    `docs/reviewer_response.md` (D-14-19) using the
    `training_protocol.md` sourced-row table discipline: per-reviewer sections
    (Reviewer 1, Reviewer 2), one row per comment ID from
    `QGAN_Review_Response_Plan.md.pdf` (R1-M1..M5, R1-m1..m7, R2-1..6) with
    columns: comment ID | verbatim concern | change made | manuscript location
    (section/table/fig) | supporting artifact (`results/*.json` or
    `results/figures/*` path). Every supporting-artifact cell MUST
    point at a real existing path. Finally run
    `verify_number_provenance.py --target
    docs/paper_blocks_refs_methods.md` AND `--target
    docs/reviewer_response.md` and resolve every failure until both
    pass (success-criterion 5). Do NOT edit any `.tex` file.
  </action>
  <verify>
    <automated>grep -q 'PAPER-08' docs/paper_blocks_refs_methods.md && grep -q 'PAPER-09' docs/paper_blocks_refs_methods.md && test -f docs/reviewer_response.md && grep -q 'R1-M1' docs/reviewer_response.md && grep -q 'R2-6' docs/reviewer_response.md && ./qgan_env/bin/python verify_number_provenance.py --target docs/paper_blocks_refs_methods.md && ./qgan_env/bin/python verify_number_provenance.py --target docs/reviewer_response.md && git diff --stat -- "main (4) copy.tex" supp_material.tex | grep -cv . | grep -q '^0$' && echo METHODS_OK</automated>
  </verify>
  <acceptance_criteria>
    - `paper_blocks_refs_methods.md` contains `PAPER-08` and `PAPER-09` keyed blocks; every numeric literal has a `% source: results/...json` annotation
    - `docs/reviewer_response.md` exists with per-reviewer sections and a row for every comment ID R1-M1..M5, R1-m1..m7, R2-1..6 (grep R1-M1, R1-m7, R2-1, R2-6)
    - Every supporting-artifact cell in `reviewer_response.md` points at a path that exists under `results/` (spot-checkable; no placeholder/TODO cells — grep returns no `TODO`/`TBD`)
    - `verify_number_provenance.py --target docs/paper_blocks_refs_methods.md` exits 0
    - `verify_number_provenance.py --target docs/reviewer_response.md` exits 0
    - `git diff --stat -- "main (4) copy.tex" supp_material.tex` shows zero changes
  </acceptance_criteria>
  <done>PAPER-08/09 Methods blocks render from JSON, reviewer_response.md is a complete per-comment-ID rebuttal with real artifact paths, both files pass the number-provenance gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JSON → Methods LaTeX block | a hand-typed dataset/metric number fabricates a published methods detail |
| reviewer_response.md → artifact paths | a dangling artifact path makes the rebuttal unverifiable |
| revision package → in-repo .tex | accidental edit desyncs the manuscript of record |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-13 | Tampering | hand-typed number in published Methods | mitigate | PAPER-08/09 render from JSON with `% source:` annotations; `verify_number_provenance.py` explicit-raise gate is a hard pass (success-criterion 5) |
| T-14-17 | Repudiation | unverifiable rebuttal claim | mitigate | Every `reviewer_response.md` supporting-artifact cell points at a real `results/*` path; no TODO/placeholder cells (grep) |
| T-14-16 | Tampering | read-only .tex edited | mitigate | `git diff --stat` on `main (4) copy.tex` / `supp_material.tex` must be empty (D-14-18) |
| T-14-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs; RESEARCH Package Legitimacy Audit is a documented no-op for Phase 14 |
</threat_model>

<verification>
- `paper_blocks_refs_methods.md` contains PAPER-06..11 keyed blocks (cite-key/label/anchor + rationale)
- PAPER-08/09 numbers annotated to JSON; render-from-JSON
- `reviewer_response.md` per-reviewer, every comment ID present, every artifact cell a real path
- `verify_number_provenance.py` exits 0 on both files
- `.tex` files byte-untouched (git diff empty)
</verification>

<success_criteria>
PAPER-06..11 are delivered as keyed, location-independent, reviewer-rationaled
LaTeX blocks and the AIChE per-reviewer point-by-point rebuttal maps every
comment ID to a change + a real supporting artifact, all passing the executable
number-provenance gate with the read-only manuscript untouched — the
references/methods/typos + traceability core of the reviewer response is
complete.
</success_criteria>

<output>
Create `.planning/phases/14-paper-revision-release-freeze/14-06-SUMMARY.md` when done
</output>
