---
phase: 14-paper-revision-release-freeze
plan: 19
type: execute
wave: 16
depends_on: ["14-17"]
files_modified:
  - .gitignore
  - LICENSE
  - requirements-pinned.txt
  - verify_freeze_ready.py
  - real.csv
  - fake.csv
  - results/phase4_validation.json
gap_closure: true
autonomous: false
requirements: [INFRA-03]
must_haves:
  truths:
    - "LICENSE is restored in the working tree (the unintended deletion is reverted)"
    - "The .gitignore results/ exclusion and the !results/ negations are committed together in one atomic commit; the ' *.csv' leading-space typo is fixed"
    - "results/baselines/runs/ metrics.json + config.yaml artifacts are tracked and committed"
    - "The 14-17-revised .tex manuscripts are git-tracked and committed into the candidate tree"
    - "requirements-pinned.txt includes fastdtw and pins pandas<3.0"
    - "verify_freeze_ready.py validates the committed tree, asserts a clean working tree, and asserts docs/release.md exists"
    - "The freeze candidate is the committed HEAD after 14-17, 14-18 and 14-19 have all landed; verify_freeze_ready.py certifies that exact commit (SYNTHESIS H5)"
  artifacts:
    - path: ".gitignore"
      provides: "Committed provenance-backbone protection — results/ exclusion + !results/ negations atomic"
      contains: "!results/"
    - path: "verify_freeze_ready.py"
      provides: "Hardened freeze gate validating the committed tree, not the dirty working tree"
      contains: "status --porcelain"
  key_links:
    - from: ".gitignore"
      to: "git check-ignore results/*.json"
      via: "results/ exclusion + !results/ negations committed in the same commit"
      pattern: "!results/"
    - from: "verify_freeze_ready.py"
      to: "the committed tag-candidate tree"
      via: "asserts git status --porcelain is empty and release.md exists before the gate passes"
      pattern: "porcelain|release.md"
---

<objective>
Establish the working-tree-hygiene and freeze-gate pre-conditions that must be
true before plan 14-07 cuts the `v2.0-revision` tag and mints the irreversible
Zenodo DOI. The peer-review r4 freeze-readiness reviewer (Agent 6, BLOCK)
returned 3 CRITICAL + 3 HIGH findings: the working tree is dirty, the
`.gitignore` provenance protections exist only uncommitted, the manuscript
`.tex` files are untracked, the freeze gate validates the dirty working tree
rather than the tag candidate, and the pinned requirements are incomplete. This
plan closes SYNTHESIS findings C4, C5, C6, H5, H6, H7, M4, M10, M11, M12.

Purpose: A green `scripts/verify_freeze_ready.py` today does not certify the tag — it
ran against the working tree's uncommitted `.gitignore`. If the `.gitignore`
change is committed without the negations in the same commit, every
provenance JSON becomes ignored and the DOI'd archive ships without its number
backbone. The data CSVs have drifted on every row. The hygiene must be resolved
deliberately, not frozen by accident.

WAVE / ORDERING (this plan is wave 16, `depends_on: ["14-17"]`): Task 3 of this
plan tracks and commits the manuscript `.tex` files into the tag-candidate tree.
Those `.tex` files are only correct AFTER plan 14-17 (wave 15) has integrated the
PAPER-01..11 revisions and de-overclaimed the abstract. If this plan ran in the
same wave as 14-17, execute-phase could commit the un-revised `.tex` into the
freeze. This plan therefore runs in wave 16, strictly after 14-17. Plan 14-18
(wave 15) touches no `.tex` and is independent of this plan, so 14-17 and 14-18
run in parallel in wave 15; this plan runs once both manuscript revision and
claims recalibration are wave-15-complete.

Output: a clean, committed tag-candidate tree — LICENSE restored, `.gitignore`
atomically committed, baseline run artifacts tracked, the revised `.tex`
committed, `requirements-pinned.txt` fixed — and a hardened
`scripts/verify_freeze_ready.py` that validates the committed tree. The downstream
consumer is plan 14-07, which cuts the tag and runs the (now hardened) gate.

BOUNDARY: this plan does NOT cut the `v2.0-revision` tag, does NOT author
`release.md`'s final SHA/DOI content, and does NOT perform the Zenodo deposit —
all of that remains plan 14-07's job. This plan only establishes the
pre-conditions and hardens the gate. It also does NOT modify `core/`
(byte-frozen, D-11-10) and does NOT recompute or retrain any numbers.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-paper-revision-release-freeze/peer-review-r4/SYNTHESIS.md
@.planning/phases/14-paper-revision-release-freeze/peer-review-r4/freeze-readiness-review.md
@.planning/phases/14-paper-revision-release-freeze/14-07-PLAN.md
@verify_freeze_ready.py
@requirements-pinned.txt

<interfaces>
<!-- Working-tree state verified this session -->

`git status` working tree (HEAD = `8180a5e`, branch `main`): `LICENSE` deleted;
`.gitignore`, `real.csv`, `fake.csv`, `results/phase4_validation.json` modified;
`results/baselines/runs/` untracked (47 MB, 250 files).

LICENSE: present at HEAD (full MIT, committed in `fdbbdfe`). The deletion is
working-tree-only and uncommitted — `git checkout -- LICENSE` restores it.

`.gitignore` working-tree diff (verified):
- line 40: `# *.csv` → ` *.csv` (a leading-space typo — ` *.csv` is a different,
  inert pattern; the intent is the CSVs stay tracked, so fix the line back to
  `# *.csv` so it reads correctly).
- adds `results/` (line ~66) AND two negations at the end:
  `!results/` and `!results/**/*.json`.
The committed HEAD `.gitignore` has NONE of these. The `results/` line and BOTH
negations MUST land in ONE atomic commit — committing `results/` alone ignores
the entire `results/` provenance backbone.

`real.csv` / `fake.csv`: 777/770 rows changed vs HEAD — every data row differs
(unintended drift). `results/phase4_validation.json`: 83-line diff. These
require an OWNER DECISION — which version is canonical and consistent with the
certified `results/*.json` — before commit or revert.

`.tex` manuscripts: `main (4) copy.tex` + `supp_material.tex` are untracked.
D-14-21 says the tag includes `.tex` reference files. Plan 14-17 (wave 15)
revises them; this plan (wave 16, `depends_on: ["14-17"]`) tracks + commits the
REVISED versions — it runs strictly after 14-17 has landed.

`bib.bib`: not present in the repo root working tree — Agent 5 L-3 flags it as
needed for the manuscript to compile from the DOI snapshot. If it exists
elsewhere or is added, commit it; if it genuinely does not exist, record that in
the SUMMARY (the manuscript bib is then Overleaf-side, a documented decision).

`requirements-pinned.txt` (verified contents): matplotlib, numpy, pennylane,
PyYAML, scipy, statsmodels, torch — `fastdtw` is MISSING (used by
`core/eval.py` DTW) and `pandas` is absent/unpinned (resolves to
pandas 3.0 which breaks statsmodels). Add `fastdtw` and pin `pandas<3.0`.

`scripts/verify_freeze_ready.py` exists (9.7 KB, `raise AssertionError` idiom). Agent 6
HIGH-3: it validates the live working tree. Harden it to (a) assert
`git status --porcelain` is empty, (b) re-run the gitignore/provenance/tag-scope
checks against the committed tree, (c) assert `docs/release.md` exists.

SYNTHESIS H5 (Agent 3 HIGH-1): the review worktrees were provisioned at stale
commits; the DOI must be minted from the CURRENT verified freeze-candidate HEAD
— and after this remediation that HEAD is the commit that lands once 14-17,
14-18 and 14-19 are all complete. The freeze candidate is NOT the pre-14-17
commit `8180a5e` and NOT any review worktree commit; it is the post-14-19
committed HEAD. `scripts/verify_freeze_ready.py` must certify that exact commit, and
14-07 must cut the tag from it.

Junk to NOT commit (Agent 6 L-2): `qgan_pennylane copy.ipynb`, `datasets.zip`,
`amp`, `circuit_diagram.png`, `QGAN_Review_Response_Plan.md.pdf`, `.claude/`,
`main (4) copy.tex`'s "copy" naming notwithstanding the `.tex` IS the manuscript
and IS committed. Stage files explicitly by name — never `git add -A`.

OWNER-DECISION items in Task 1 are checkpoint tasks (this plan is
`autonomous: false`).
</interfaces>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: OWNER DECISION — canonical real.csv / fake.csv, phase4_validation.json drift, .planning/ in the deposit</name>
  <read_first>
    - .planning/phases/14-paper-revision-release-freeze/peer-review-r4/freeze-readiness-review.md (CRITICAL-2 CSV drift, MEDIUM-2 phase4_validation.json, MEDIUM-3 .planning/ in the archive)
  </read_first>
  <decision>
    Three owner decisions are required before any pre-freeze commit. The
    executor must present the `git diff` evidence for each and pause for the
    owner; the executor does NOT decide these.
    (1) `real.csv` / `fake.csv` have drifted on every row vs HEAD. Which version
    is canonical and consistent with the certified `results/*.json`
    that the number-provenance gate already passed — the HEAD version or the
    working-tree version? The chosen version is then committed or reverted
    deliberately.
    (2) `results/phase4_validation.json` is modified in the working tree
    (83-line diff). Is the change intended? Commit or revert accordingly.
    (3) D-14-21 says the tag includes `.planning/`, making the public DOI
    archive ~112 MB. Confirm `.planning/` (full GSD planning history) belongs in
    the public Zenodo deposit, or decide to exclude it.
  </decision>
  <context>
    These are irreversible-once-frozen choices. Committing drifted CSVs that do
    not match the certified results JSON would ship a DOI'd archive that is
    internally inconsistent (Agent 6 CRITICAL-2). Freezing accidental drift is
    the failure mode the freeze gate exists to prevent.
  </context>
  <options>
    <option id="csv-head">
      <name>Revert real.csv/fake.csv to HEAD</name>
      <pros>HEAD CSVs are the version the certified results/*.json were computed against (most likely consistent)</pros>
      <cons>Discards the working-tree changes — only correct if the drift was unintended</cons>
    </option>
    <option id="csv-working">
      <name>Commit the working-tree real.csv/fake.csv</name>
      <pros>Keeps the new data if the drift was a deliberate update</pros>
      <cons>Risks inconsistency with the certified results JSON unless re-verified</cons>
    </option>
  </options>
  <verify><human-check>Owner states: (1) canonical CSV version + consistency confirmation, (2) phase4_validation.json intent, (3) .planning/ inclusion decision</human-check></verify>
  <resume-signal>State the canonical CSV decision (head / working + consistency confirmed), the phase4_validation.json decision (commit / revert), and the .planning/ decision (include / exclude)</resume-signal>
  <done>Owner has decided all three: canonical CSV version, phase4_validation.json disposition, and whether .planning/ ships in the deposit.</done>
</task>

<task type="auto">
  <name>Task 2: Restore LICENSE, fix requirements, atomically commit .gitignore + baseline artifacts + drift resolution</name>
  <read_first>
    - .planning/phases/14-paper-revision-release-freeze/peer-review-r4/freeze-readiness-review.md (CRITICAL-1 .gitignore atomic commit, CRITICAL-3, HIGH-1 baseline runs, M4 requirements, the RECOMMENDED PRE-FREEZE SEQUENCE)
    - .gitignore (the working-tree diff — the ` *.csv` typo, the results/ line, the two !results/ negations)
    - requirements-pinned.txt (the current pin set — fastdtw missing, pandas unpinned)
  </read_first>
  <action>
    Execute the working-tree hygiene fixes (no tag is cut here).
    (a) Restore LICENSE: `git checkout -- LICENSE` (SYNTHESIS step 8 / Agent 6
    CRITICAL re LICENSE).
    (b) Fix `requirements-pinned.txt` (SYNTHESIS M4): add a `fastdtw`
    line (used by `core/eval.py` DTW) and add `pandas<3.0` (pandas 3.0
    breaks statsmodels). Place them consistently with the existing pin block;
    keep the existing header comments.
    (c) Fix the `.gitignore` ` *.csv` leading-space typo back to `# *.csv` (the
    CSVs must stay tracked — the comment form is correct).
    (d) Apply the owner's Task-1 decisions: revert or stage `real.csv`/`fake.csv`
    and `results/phase4_validation.json` per the resume signal.
    (e) Track the baseline run artifacts (SYNTHESIS step 11 / Agent 6 HIGH-1):
    `git add -f` the `metrics.json` and `config.yaml` files under
    `results/baselines/runs/` (the per-seed metrics + config that the
    classical-baseline comparison numbers derive from). Commit the per-seed
    `metrics.json`/`config.yaml` at minimum; do NOT commit the `samples.npy` /
    `checkpoint.pt` / `inverse_kwargs.npz` files in those run dirs unless the
    owner directed otherwise — they inflate the archive (Agent 6 HIGH-1 / M10).
    (f) Make the atomic `.gitignore` commit (SYNTHESIS step 10 / Agent 6
    CRITICAL-1): the SAME commit MUST contain the `results/` exclusion line AND
    both `!results/` negations AND the ` *.csv`→`# *.csv` fix. After
    staging, before committing, verify `git check-ignore results/*.json`
    against the staged `.gitignore` returns empty (no provenance JSON ignored).
    Stage files explicitly by name (never `git add -A`) — the junk listed in the
    interfaces must not be committed. Branch first if on the default branch
    before any commit; do not push. Commit message:
    `chore(14-19): pre-freeze hygiene — LICENSE, atomic .gitignore, baseline artifacts, requirements`.
  </action>
  <verify>
    <automated>git show HEAD:LICENSE >/dev/null 2>&1 && [ -f LICENSE ] && ! git status --porcelain | grep -q '^.D LICENSE' && grep -q '^fastdtw' requirements-pinned.txt && grep -qE '^pandas<3' requirements-pinned.txt && ! grep -qx ' \*.csv' .gitignore && git show HEAD:.gitignore | grep -q '!results/' && [ -z "$(git check-ignore results/*.json 2>/dev/null)" ] && git ls-files results/baselines/runs | grep -q 'metrics.json' && echo HYGIENE_OK</automated>
  </verify>
  <acceptance_criteria>
    - `LICENSE` is present in the working tree and not staged for deletion
    - `requirements-pinned.txt` contains a `fastdtw` line and a `pandas<3.0` pin
    - `.gitignore` no longer has the ` *.csv` leading-space line; the committed `.gitignore` contains the `results/` exclusion AND both `!results/` negations (all in one commit)
    - `git check-ignore results/*.json` against the committed `.gitignore` returns empty (no provenance JSON ignored)
    - `results/baselines/runs/**/metrics.json` and `config.yaml` are git-tracked; `samples.npy`/`checkpoint.pt` in those dirs are NOT committed (unless owner directed)
    - `real.csv`/`fake.csv`/`results/phase4_validation.json` are committed or reverted per the Task-1 owner decision; no unintended drift remains
    - Files were staged explicitly by name; none of the Agent 6 L-2 junk list is committed
  </acceptance_criteria>
  <done>LICENSE restored; requirements fixed; the .gitignore committed atomically with the negations; baseline metrics/config tracked; CSV/json drift resolved per the owner — the working tree is clean of unintended drift.</done>
</task>

<task type="auto">
  <name>Task 3: Track + commit the revised .tex manuscripts; harden verify_freeze_ready.py; certify the post-14-19 freeze candidate</name>
  <read_first>
    - .planning/phases/14-paper-revision-release-freeze/peer-review-r4/freeze-readiness-review.md (HIGH-2 .tex tracking, HIGH-3 gate validates the working tree)
    - .planning/phases/14-paper-revision-release-freeze/peer-review-r4/results-provenance-review.md (Agent 3 HIGH-1 / SYNTHESIS H5 — the DOI must be minted from the current verified freeze-candidate HEAD, not a stale commit)
    - verify_freeze_ready.py (the current gate — the gitignore/provenance/tag-scope checks and the `raise AssertionError` idiom)
    - .planning/phases/14-paper-revision-release-freeze/14-07-PLAN.md (the must_haves the gate must enforce; 14-07 is the downstream consumer that cuts the tag)
  </read_first>
  <action>
    Two edits.
    (a) Track + commit the REVISED manuscripts (SYNTHESIS step 12 / Agent 6
    HIGH-2). This task runs AFTER plan 14-17 has integrated the PAPER-* blocks
    into the `.tex` — this plan is wave 16 with `depends_on: ["14-17"]`, so 14-17
    is already complete when this runs. Before staging, confirm the `.tex`
    carries the 14-17 revisions (e.g. `grep -q "Circuit Design Rationale"
    "main (4) copy.tex"` and `! grep -qi "high fidelity" "main (4) copy.tex"`);
    if the `.tex` is still the un-revised version, STOP — 14-17 has not landed
    and committing now would freeze the un-revised manuscript. Then `git add`
    `"main (4) copy.tex"` and `supp_material.tex` (quote the literal-space
    filename) and commit them so they are in the tag-candidate tree per D-14-21.
    If `bib.bib` is present in the working tree, `git add` + commit it too; if it
    is genuinely absent, record in the SUMMARY that the manuscript bibliography
    is Overleaf-side (a documented decision per Agent 5 L-3) so 14-07's
    `release.md` can note it. Commit message:
    `docs(14-19): track revised manuscript .tex into the freeze candidate`.
    (b) Harden `scripts/verify_freeze_ready.py` (SYNTHESIS step 15 / Agent 6
    HIGH-3) using the existing `raise AssertionError` explicit-raise idiom:
    add an assertion that `git status --porcelain` is EMPTY (the gate must fail
    over a dirty working tree, so a green run certifies the committed tree);
    add an assertion that `docs/release.md` EXISTS (it is a 14-07
    must_have the current gate does not check — note: release.md is authored by
    14-07, so this assertion will correctly fail until 14-07 runs, which is the
    intended ordering guard); ensure the existing gitignore / number-provenance
    / tag-scope checks are evaluated against the committed tree (use
    `git show HEAD:.gitignore` / `git ls-files` rather than reading the live
    working-tree `.gitignore`). Do NOT change the gate's existing
    explicit-raise checks' semantics — only add the clean-tree and release.md
    assertions and switch the gitignore read to the committed tree. Commit the
    hardened gate. Do NOT cut the tag and do NOT run the Zenodo deposit — those
    are 14-07.
    (c) SYNTHESIS H5 — certify the post-14-19 freeze candidate. After the
    `.tex` commit and the hardened-gate commit have both landed, the current
    committed `HEAD` IS the freeze candidate — the commit that contains all of
    14-17 (manuscript revision), 14-18 (claims recalibration) and 14-19
    (hygiene + hardened gate). Record this HEAD SHA in the 14-19 SUMMARY and
    state explicitly that 14-07 must cut `v2.0-revision` from THIS commit, not
    from `8180a5e` and not from any review worktree commit. Run
    `./qgan_env/bin/python scripts/verify_freeze_ready.py` against this
    committed HEAD and confirm it evaluates the committed tree (it is expected
    to still fail ONLY on the release.md assertion, since release.md is 14-07's
    deliverable — every other check must pass against this exact commit). The
    SUMMARY must name the SHA and confirm the gate ran against it.
  </action>
  <verify>
    <automated>git ls-files | grep -q 'main (4) copy.tex' && git ls-files | grep -q 'supp_material.tex' && git show HEAD:"main (4) copy.tex" | grep -q 'Circuit Design Rationale' && grep -q 'status --porcelain' verify_freeze_ready.py && grep -q "release.md" verify_freeze_ready.py && grep -qE 'show HEAD:.gitignore|ls-files' verify_freeze_ready.py && grep -c 'raise AssertionError' verify_freeze_ready.py | grep -qvE '^0$' && echo GATE_HARDENED_OK</automated>
  </verify>
  <acceptance_criteria>
    - `main (4) copy.tex` and `supp_material.tex` (the 14-17-revised versions) are git-tracked and committed — verified by `git show HEAD:"main (4) copy.tex"` containing the 14-17 marker `Circuit Design Rationale`
    - `bib.bib` is committed if present; if absent, the SUMMARY records the Overleaf-side bibliography decision
    - `scripts/verify_freeze_ready.py` asserts `git status --porcelain` is empty using the `raise AssertionError` idiom
    - `scripts/verify_freeze_ready.py` asserts `docs/release.md` exists (correctly fails until 14-07 authors it — the intended ordering guard)
    - The gitignore / provenance / tag-scope checks read the COMMITTED tree (`git show HEAD:.gitignore` / `git ls-files`), not the live working-tree `.gitignore`
    - SYNTHESIS H5: the 14-19 SUMMARY records the post-14-19 committed HEAD SHA as the freeze candidate, states that 14-07 must cut `v2.0-revision` from THAT exact commit (not `8180a5e`, not a review worktree commit), and confirms `scripts/verify_freeze_ready.py` was run against that committed HEAD (passing every check except the release.md assertion, which is 14-07's deliverable)
    - The gate's pre-existing explicit-raise checks are semantically unchanged; no tag is cut and no Zenodo deposit is performed
  </acceptance_criteria>
  <done>The revised .tex manuscripts are tracked and committed into the freeze candidate, verify_freeze_ready.py is hardened to validate the committed tree and require a clean working tree + release.md, and the post-14-19 committed HEAD is recorded and certified as the exact freeze candidate for 14-07.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dirty working tree → frozen tag archive | committing the `.gitignore` without negations, or freezing drifted CSVs, ships an inconsistent or backbone-less DOI archive |
| working-tree gate → committed tag candidate | a gate that validates the live working tree gives a false-green certification of the tag |
| un-revised .tex → freeze candidate | if this plan ran before 14-17, the un-revised manuscript would be committed into the tag |
| stale commit → DOI mint site | a DOI minted from `8180a5e` or a review worktree commit would freeze a tree missing the 14-17/18/19 remediation |
| owner-judgment decisions → irreversible freeze | CSV canonicality and `.planning/` inclusion are owner calls, not executor calls |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-28 | Tampering | provenance-backbone exclusion (non-atomic .gitignore commit) | mitigate | Task 2 commits `results/` + both `!results/` negations in one commit; `git check-ignore` verified against the committed `.gitignore` |
| T-14-29 | Tampering | frozen data inconsistency (drifted real.csv/fake.csv) | mitigate | Task 1 owner-decision checkpoint resolves canonicality before commit; no accidental freeze of drift |
| T-14-30 | Spoofing | false-green freeze certification (gate validates working tree) | mitigate | Task 3 hardens `scripts/verify_freeze_ready.py` to assert a clean tree and read the committed `.gitignore` |
| T-14-31 | Repudiation | license-less deposit (LICENSE deleted in working tree) | mitigate | Task 2 `git checkout -- LICENSE` restores it before any pre-freeze commit |
| T-14-34 | Tampering | un-revised .tex frozen into the tag | mitigate | This plan is wave 16 / `depends_on: ["14-17"]`; Task 3 asserts the 14-17 marker is present before committing the `.tex` |
| T-14-35 | Spoofing | tag cut from a stale commit (SYNTHESIS H5) | mitigate | Task 3 records the post-14-19 committed HEAD as the freeze candidate and certifies the gate ran against that exact commit |
| T-14-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs; this plan only edits a pinned-requirements text file and does not run pip |
</threat_model>

<verification>
- `LICENSE` restored; `requirements-pinned.txt` has `fastdtw` + `pandas<3.0`
- `.gitignore` committed atomically with the `results/` line + both `!results/` negations; ` *.csv` typo fixed; `git check-ignore` empty for provenance JSON
- `results/baselines/runs/` metrics.json + config.yaml tracked
- 14-17-revised `.tex` manuscripts tracked + committed (the `Circuit Design Rationale` marker is present in the committed `.tex`); `bib.bib` committed or its absence documented
- `scripts/verify_freeze_ready.py` asserts a clean working tree + release.md existence, validates the committed tree
- The post-14-19 committed HEAD is recorded as the freeze candidate and `scripts/verify_freeze_ready.py` certified against that exact commit (SYNTHESIS H5)
- No tag cut, no Zenodo deposit, no `core/` edit, no recomputation
</verification>

<success_criteria>
The repository is in a clean, deliberately-committed state ready for plan 14-07
to cut the tag: LICENSE restored, the `.gitignore` provenance protections
committed atomically, baseline artifacts tracked, the 14-17-revised manuscripts
committed, requirements complete, the CSV/json drift resolved by owner decision,
`scripts/verify_freeze_ready.py` hardened to certify the committed tag candidate rather
than the dirty working tree, and the post-14-19 committed HEAD recorded and
certified as the exact freeze candidate — closing SYNTHESIS C4, C5, C6, H5, H6,
H7, M4, M10, M11, M12. Plan 14-07 can now run its freeze sequence against a
trustworthy tree at a known commit.
</success_criteria>

<output>
Create `.planning/phases/14-paper-revision-release-freeze/14-19-SUMMARY.md` when done
</output>
</content>
</invoke>
