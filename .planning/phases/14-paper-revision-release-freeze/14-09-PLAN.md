---
phase: 14-paper-revision-release-freeze
plan: 09
type: execute
wave: 7
depends_on: ["14-01", "14-02"]
files_modified:
  - run_circuit_diagrams.py
  - results/v1_config_lock.json
  - results/v2_config_lock.json
  - results/v3_config_lock.json
  - results/figures/circuits/
  - docs/circuit_atlas.md
autonomous: true
requirements: [PAPER-03]
must_haves:
  truths:
    - "Render-only: NO training, NO sampling, NO checkpoint reload — `revision.core.models.quantum` and `run_matched2000.py` are imported ONLY to reconstruct the QNode tape for drawing (no `.fit(`, no `train_`, no `model.sample(`, no `best_checkpoint.pt` reload anywhere in run_circuit_diagrams.py)"
    - "All 5 production quantum circuits are diagrammed end-to-end: default_75 (5q×4L=75p, IQP+SEL+RX+RY, range), iqp_sel_55 (5q×3L=55p, IQP+3*SEL+RX-only, range, source=canonical_config_lock.json), V1 (5q×4L=75p, range), V2 (5q×8L=135p, range), V3 (5q×4L=75p, LINEAR) — 15 emitted artifacts total (5 PNG + 5 PDF + 5 companion JSON) under `results/figures/circuits/`"
    - "Three NEW config-lock JSONs (`v1_config_lock.json`, `v2_config_lock.json`, `v3_config_lock.json`) are added next to `canonical_config_lock.json`, mirroring its schema (circuit_id, num_qubits, num_layers, topology, param_count, gate_layout breakdown, native_pipeline) so every numeric literal in the circuit atlas (depth, qubit count, param_count, layer breakdown) is gated by the existing `verify_number_provenance.py` rglob over `results/*.json`"
    - "Param-count consistency is enforced by an explicit `raise AssertionError` (python -O safe) inside the config-lock emitter: each computed param_count from the source dict MUST equal the param_count it writes (V1=5+4*15+10=75, V2=5+8*15+10=135, V3=5+4*15+10=75); loud-fail listing the offending variant on any mismatch"
    - "`qml.draw_mpl(qnode, style=\"pennylane\")` is the renderer — NEVER hand-rolled matplotlib gate art; the placeholder param tensor is a deterministic zeros tensor of the exact shape the locked config requires (param values do not affect circuit topology rendering)"
    - "`core/` byte-freeze (D-14-22) is preserved: `git diff --stat core/` stays empty; this plan adds artifacts under `results/` and `docs/` only — `revision.core.models.quantum.QuantumGenerator(circuit_id=...)` is the public constructor path; no monkey-patch, no subclass override"
    - "`docs/circuit_atlas.md` PASSES `./qgan_env/bin/python verify_number_provenance.py --target docs/circuit_atlas.md` unmodified — every numeric literal (param counts, depths, qubit counts) resolves to one of {canonical_config_lock.json, v1_config_lock.json, v2_config_lock.json, v3_config_lock.json, the new default_75_config_lock.json}; no hand-typed numbers"
    - "Render-only contract (mirroring `run_figure_suite.py`): headless `matplotlib.use(\"Agg\")` BEFORE pyplot import; loud-fail `FileNotFoundError` on any missing config-lock JSON; dual PNG+PDF save at `dpi=150, bbox_inches=\"tight\"` + `plt.close(fig)`; `_find_repo_root()` resolver; CLI + print-every-written-path"
    - "iqp_sel_55 is labelled in both the figure caption and the atlas as 'canonical paper circuit (frozen checkpoint epoch 1969)' to preserve continuity with 14-01's framing; D-14-10 (headline vs reproduction conflation) does NOT apply here — this plan diagrams circuit ARCHITECTURE, not generation results — but the canonical-circuit label is retained as a consistency convention"
    - "Plan 14-08 marked complete in the ROADMAP progress table (7/9, not 6/8); ROADMAP D-14-23 scope note appended to record that Plan 09 closes the PAPER-03 visualization gap (circuit-architecture diagrams + V1/V2/V3 config locks)"
  artifacts:
    - path: "run_circuit_diagrams.py"
      provides: "Render-only circuit-diagram emitter for the 5 production quantum circuits (default_75, iqp_sel_55, V1, V2, V3) using qml.draw_mpl; emits PNG+PDF+companion JSON each; also a build_config_locks step that emits V1/V2/V3 + default_75 config-lock JSONs from the source dicts (pure aggregator — importing run_matched2000 does not trigger torch training)"
      min_lines: 200
    - path: "results/v1_config_lock.json"
      provides: "V1 ansatz config lock — circuit_id=default_75, num_qubits=5, num_layers=4, topology=range, param_count=75, gate_layout breakdown (IQP encoding 5 + 4*SEL 15 + final RX+RY 10), native_pipeline=B, source_path=run_matched2000.py:118 (mirrors canonical_config_lock.json schema)"
    - path: "results/v2_config_lock.json"
      provides: "V2 ansatz config lock — circuit_id=default_75, num_qubits=5, num_layers=8, topology=range, param_count=135 (5+8*15+10), native_pipeline=B, source_path=run_matched2000.py:120"
    - path: "results/v3_config_lock.json"
      provides: "V3 ansatz config lock — circuit_id=default_75, num_qubits=5, num_layers=4, topology=LINEAR (q→q+1 chain), param_count=75, native_pipeline=B, source_path=run_matched2000.py:122"
    - path: "results/default_75_config_lock.json"
      provides: "default_75 byte-frozen v1.0/v1.1 config lock — circuit_id=default_75, num_qubits=5, num_layers=4, topology=range, param_count=75, gate_layout breakdown (IQP 5 + 4*SEL 15 + RX+RY 10), native_pipeline=B, source_path=core/__init__.py (NUM_QUBITS=5, NUM_LAYERS=4) + core/models/quantum.py (default_75 branch); derived from the existing core constants — supersedes the untracked singleton `circuit_diagram.png` at the repo root by giving the default circuit a tracked, JSON-companion-backed render"
    - path: "results/figures/circuits/"
      provides: "15 emitted render-only artifacts: {default_75, iqp_sel_55, V1, V2, V3}.{png,pdf,json} — each PNG drawn via qml.draw_mpl at dpi=150 bbox_inches=tight; each companion JSON records (figure name, circuit_id, source_config_lock_path, n_params, depth, topology, generation_timestamp) so every figure is independently re-derivable"
      min_files: 15
    - path: "docs/circuit_atlas.md"
      provides: "Copy-paste PAPER-03 circuit atlas — one section per circuit with embedded ![](circuits/<name>.png) image, spec table (param count, depth, topology, encoding, variational block, final rotations) with every numeric literal read from the corresponding config-lock JSON, a 2-3 sentence 'what this circuit does differently' contrastive paragraph, and a final cross-comparison table (rows = circuits, cols = depth/topology/param_count); PASSES verify_number_provenance.py unmodified"
      min_lines: 80
  key_links:
    - from: "run_circuit_diagrams.py"
      to: "run_matched2000.py:118-122 (_QUANTUM_ANSATZ dict)"
      via: "import-only aggregator — read V1/V2/V3 spec dicts, never trigger training; pure-aggregator-safe per the 14-03 yaml.safe_load precedent (PyYAML / dict import is not torch/pennylane training import)"
      pattern: "from revision.run_matched2000 import _QUANTUM_ANSATZ"
    - from: "run_circuit_diagrams.py"
      to: "core/models/quantum.QuantumGenerator(circuit_id=...)"
      via: "construct a placeholder QNode by instantiating QuantumGenerator with the locked (num_qubits, num_layers, topology, circuit_id); pass a deterministic torch.zeros(num_params) noise + the model's own params_pqc — never `.fit(`, never `model.sample(`, never `model.forward(` in a training loop"
      pattern: "QuantumGenerator(num_qubits=..., num_layers=..., topology=..., circuit_id=...)"
    - from: "run_circuit_diagrams.py"
      to: "qml.draw_mpl(qnode, style=\"pennylane\")"
      via: "render-only — qml.draw_mpl returns (fig, ax); fig.savefig dual PNG+PDF dpi=150 bbox tight + plt.close(fig); NEVER hand-rolled matplotlib gate art"
      pattern: "qml.draw_mpl"
    - from: "results/figures/circuits/<name>.png"
      to: "results/<name>_config_lock.json"
      via: "companion JSON records source_config_lock_path field so every figure is traceable to the gated JSON (mirrors the 14-04 / 14-08 companion-JSON pattern)"
      pattern: "source_config_lock_path"
    - from: "docs/circuit_atlas.md"
      to: "verify_number_provenance.py --target docs/circuit_atlas.md"
      via: "every numeric literal traces (via the gate's `results/*.json` rglob) to one of the 5 config-lock JSONs (canonical + default_75 + v1 + v2 + v3); zero verifier edit, zero hand-typed numbers"
      pattern: "verify_number_provenance.py"
    - from: "run_circuit_diagrams.py"
      to: "param_count assertion gate"
      via: "explicit raise AssertionError (python -O safe) — computed param_count from formula MUST equal the writeable param_count per variant (V1=75, V2=135, V3=75, default_75=75, iqp_sel_55=55); loud-fail lists every offending variant"
      pattern: "raise AssertionError"
---

<objective>
Add `run_circuit_diagrams.py` — a render-only emitter that produces
PAPER-03's missing circuit-architecture visualizations for ALL 5 production
quantum circuits (`default_75`, `iqp_sel_55`, V1, V2, V3) using
`qml.draw_mpl(..., style="pennylane")`, plus the three missing
config-lock JSONs (`v1_config_lock.json`, `v2_config_lock.json`,
`v3_config_lock.json`) and a derived `default_75_config_lock.json` so every
numeric literal in the circuit-atlas doc is gated by the existing
`verify_number_provenance.py`. Then author `docs/circuit_atlas.md`
(copy-paste-ready) — one section per circuit + a cross-comparison table —
sourced solely from the 5 config-lock JSONs.

Purpose: every cross-model figure in `results/figures/` references
`iqp_sel_55` (the canonical paper circuit recovered in 14-01) and the
matched-budget V1/V2/V3 ansatz variants, but currently NONE of them has a
rendered diagram tracked in the repository — the only existing circuit PNG is
the untracked singleton `circuit_diagram.png` at the repo root, which covers
only `default_75` and has no JSON provenance companion. PAPER-03 (Circuit
Design Rationale) explicitly references the visualization gap. This plan
closes it without retraining, without touching `core/` (D-14-22
byte-freeze), and without changing the existing number-provenance gate.

Output: `run_circuit_diagrams.py`, 4 new config-lock JSONs
(v1/v2/v3/default_75), 15 figure artifacts under
`results/figures/circuits/` (5 PNG + 5 PDF + 5 companion JSON), and
`docs/circuit_atlas.md` — every artifact gated by the existing
number-provenance gate, no retraining, no sampling, no checkpoint reload.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-paper-revision-release-freeze/14-CONTEXT.md
@.planning/phases/14-paper-revision-release-freeze/14-RESEARCH.md
@.planning/phases/14-paper-revision-release-freeze/14-PATTERNS.md
@.planning/phases/14-paper-revision-release-freeze/14-01-SUMMARY.md
@.planning/phases/14-paper-revision-release-freeze/14-04-PLAN.md
@.planning/phases/14-paper-revision-release-freeze/14-08-PLAN.md

<interfaces>
<!-- The render-only contract this plan mirrors comes from
     run_figure_suite.py (14-04). The config-lock JSON schema this
     plan extends comes from results/canonical_config_lock.json
     (14-01). Both are load-bearing — copy the shapes, do not invent new ones. -->

run_figure_suite.py render-only contract (the EXACT idiom to mirror):
- Headless: `import matplotlib; matplotlib.use("Agg")` BEFORE pyplot import (lines 45-49).
- `_bootstrap_repo_on_path()` walks up to `core/preprocessing.py` (lines 54-69) so both `python run_circuit_diagrams.py` and `python -m revision.run_circuit_diagrams` work.
- `_find_repo_root()` resolver (lines 144-150).
- `_require(path, what)` raises `FileNotFoundError` with a "render-only (no training/sampling/recompute)" message on any missing artifact (lines 153-168) — NEVER a silent partial render.
- `_load_json(path, what)` calls `_require` + `json.loads(path.read_text())` (lines 171-174).
- `_save(fig, figures_dir, stem, companion)` writes `<stem>.png` + `<stem>.pdf` + `<stem>.json` (dpi=150, bbox_inches="tight", plt.close, json.dumps default=float) (lines 177-193).
- CLI: argparse `--figures-dir` default `results/figures/circuits`, print every written path (lines 1044-1135).

results/canonical_config_lock.json schema (mirror it for V1/V2/V3 + default_75):
{
  "schema": "canonical-config-lock v1 (Phase 14 D-14-05/06/07)",  # for v1/v2/v3 use "ansatz-config-lock v1 (Phase 14 D-14-04, plan 14-09)"
  "locked_circuit_id": "<circuit_id>",
  "decomposition": {
    "num_qubits": <int>,
    "num_layers": <int>,
    "param_count": <int>,
    "gate_layout": { "hadamard_init": true, "iqp_encoding_params_per_qubit": 1,
                     "sel_rot_params_per_qubit_per_layer": 3,
                     "entangler": "<range|linear>",
                     "final_rotation": "<RX_only|RX_plus_RY>" }
  },
  "param_count": <int>,            # mirror at top level too (canonical does it)
  "native_pipeline": "B",
  "source_path": "<file>:<line>",  # NEW field for v1/v2/v3/default_75 — records the source dict location
  "topology": "<range|linear>"
}

core/models/quantum.py constructor (the only path used to build QNodes):
- `QuantumGenerator(num_qubits=5, num_layers=4, window_length=10, topology="range", circuit_id="default_75")` — eagerly validates topology and circuit_id; computes num_params via `num_qubits + num_layers*(num_qubits*3) + num_qubits*_final_rot_factor` where `_final_rot_factor = 2 if circuit_id == "default_75" else 1`.
- `QuantumGenerator.qnode` is the bound torch QNode — pass this directly to `qml.draw_mpl`.
- `QuantumGenerator.params_pqc` is the trainable PQC tensor; pass `model.params_pqc` (or `torch.zeros(model.num_params)`) as the params arg.
- `QuantumGenerator.encoding_layer(noise)` accepts a 1D `(num_qubits,)` tensor; pass `torch.zeros(num_qubits)` as the noise arg for drawing.

run_matched2000.py:118-122 (the V1/V2/V3 source-of-truth dicts):
_QUANTUM_ANSATZ = {
    "V1": {"num_layers": 4, "topology": "range",  "circuit_id": "default_75", "parameter_count": 75},
    "V2": {"num_layers": 8, "topology": "range",  "circuit_id": "default_75", "parameter_count": 135},
    "V3": {"num_layers": 4, "topology": "linear", "circuit_id": "default_75", "parameter_count": 75},
}
# Importing run_matched2000 to grab this dict is pure-aggregator-safe:
# the module's top-level imports are stdlib + numpy + yaml + revision.core
# constants — NO model `.fit(` is triggered by import alone (verified by 14-03
# precedent for yaml.safe_load).

core/__init__.py (source for default_75 lock):
NUM_QUBITS = 5; NUM_LAYERS = 4; WINDOW_LENGTH = 10  (== 2*NUM_QUBITS)
# default_75 param_count = 5 + 4*15 + 10 = 75 (RX+RY final rotation, _final_rot_factor=2).

PennyLane `qml.draw_mpl` API (the renderer — NOT custom matplotlib):
fig, ax = qml.draw_mpl(qnode, style="pennylane")(*args, **kwargs)
# `qml.draw_mpl(qnode)` returns a CALLABLE; calling it with the QNode's
# expected args (noise_params, params_pqc) returns (fig, ax). The fig is a
# standard matplotlib.figure.Figure — call fig.savefig(out, dpi=150,
# bbox_inches="tight") then plt.close(fig) (mirrors run_figure_suite._save).
# `style="pennylane"` is the recommended publication style.

verify_number_provenance.py auto-coverage:
- The gate's `_json_blobs()` rglobs `results/*.json` (line 99) — so
  the moment `v1_config_lock.json` / `v2_config_lock.json` / `v3_config_lock.json`
  / `default_75_config_lock.json` are written under `results/`,
  they are automatically in the gate's resolution corpus. ZERO verifier edit.
- `circuit_atlas.md` literals (param counts 55/75/135, qubit count 5, layer
  counts 3/4/8) all appear verbatim in those config-lock JSONs so the gate
  passes via exact-substring or float-precision match.

Existing untracked `circuit_diagram.png` at repo root:
- Lives at `/Users/shawngibford/dev/phd/qGAN/circuit_diagram.png` (untracked
  per `git status`). Produced ad-hoc via
  `qml.draw_mpl(qgan.generator, style="pennylane")` in qgan_pennylane.ipynb.
- This plan SUPERSEDES it by emitting a tracked, JSON-companion-backed render
  at `results/figures/circuits/default_75.{png,pdf,json}`. The
  singleton at repo root is NOT deleted by this plan (leave it for the 14-07
  release-freeze gate to triage); the canonical replacement lives under
  results/figures/circuits/.

D-14-10 (headline vs reproduction conflation) scope note:
- D-14-10 governs RESULTS figures (numeric distributions, EMD bars, moments)
  where conflating the frozen-checkpoint headline with the 2000ep reproduction
  would misrepresent a load-bearing number. This plan diagrams circuit
  ARCHITECTURE only — there are NO generation numbers in a circuit diagram —
  so D-14-10 does not impose a conflation guard here. iqp_sel_55 is still
  labelled "canonical paper circuit (frozen checkpoint epoch 1969)" in
  caption + atlas to preserve framing continuity with 14-01.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Emit V1/V2/V3 + default_75 config-lock JSONs (pure aggregator)</name>
  <files>run_circuit_diagrams.py, results/v1_config_lock.json, results/v2_config_lock.json, results/v3_config_lock.json, results/default_75_config_lock.json</files>
  <read_first>
    - results/canonical_config_lock.json (THE schema to mirror — schema string, locked_circuit_id, decomposition.{num_qubits,num_layers,param_count,gate_layout.{hadamard_init,iqp_encoding_params_per_qubit,sel_rot_params_per_qubit_per_layer,entangler,final_rotation}}, native_pipeline, top-level param_count)
    - run_matched2000.py:117-124 (_QUANTUM_ANSATZ dict — the source-of-truth for V1/V2/V3 num_layers/topology/circuit_id/parameter_count)
    - core/models/quantum.py:31-145 (the QuantumGenerator constructor + count_params() — the param-count formula `num_qubits + num_layers*(num_qubits*3) + num_qubits*_final_rot_factor` and `_final_rot_factor = 2 if circuit_id == "default_75" else 1`)
    - core/__init__.py (NUM_QUBITS=5, NUM_LAYERS=4 constants — the source for the default_75 lock)
    - .planning/phases/14-paper-revision-release-freeze/14-01-SUMMARY.md (the iqp_sel_55 recovery framing — locked_circuit_id, native_pipeline=B, equivalence_gate idiom — to mirror in the new locks)
    - .planning/phases/14-paper-revision-release-freeze/14-03-SUMMARY.md (the pure-aggregator-safety precedent: yaml.safe_load / PyYAML import is not a torch training import, mirrored here for `from revision.run_matched2000 import _QUANTUM_ANSATZ`)
  </read_first>
  <action>
    Create `run_circuit_diagrams.py` as the canonical PAPER-03
    circuit emitter. Begin with the standard repo bootstrap +
    headless-matplotlib idiom copied from `run_figure_suite.py:1-69`
    (module docstring noting render-only / PAPER-03 / D-14-22 byte-freeze;
    `import matplotlib; matplotlib.use("Agg")` BEFORE pyplot;
    `_bootstrap_repo_on_path()` walking to `core/preprocessing.py`;
    stdlib + numpy + torch + pennylane + matplotlib.pyplot imports;
    `_find_repo_root()`). Add a `build_config_locks(repo: Path) -> list[Path]`
    step that:
    (1) `from revision.run_matched2000 import _QUANTUM_ANSATZ` — pure
        aggregator import (verified safe per 14-03 SUMMARY deviation #1; the
        module's top-level imports are stdlib + numpy + yaml + revision.core
        constants, NO model `.fit(` is triggered by import alone).
    (2) For each variant in {"V1","V2","V3"}, read the source dict and
        compute the expected param_count via the EXACT formula from
        `core/models/quantum.py:104-109`:
        `num_qubits + num_layers*(num_qubits*3) + num_qubits * (2 if circuit_id == "default_75" else 1)`
        (V1: 5+4*15+10=75; V2: 5+8*15+10=135; V3: 5+4*15+10=75). Then
        `raise AssertionError(f"V?: computed param_count {computed} != source-dict parameter_count {parameter_count}")`
        if computed != source-dict parameter_count (python -O safe gate).
        Loud-fail listing EVERY offending variant in a single raise, not the
        first one only (collect mismatches into a list, raise after the loop
        if non-empty).
    (3) Write each of `v1_config_lock.json`, `v2_config_lock.json`,
        `v3_config_lock.json` to `results/` mirroring
        `canonical_config_lock.json`'s schema with these fields:
        `{ "schema": "ansatz-config-lock v1 (Phase 14 D-14-04, plan 14-09)",
           "locked_circuit_id": "default_75",
           "decomposition": { "num_qubits": 5, "num_layers": <L>, "param_count": <p>,
                              "gate_layout": { "hadamard_init": true,
                                               "iqp_encoding_params_per_qubit": 1,
                                               "sel_rot_params_per_qubit_per_layer": 3,
                                               "entangler": "<range|linear>",
                                               "final_rotation": "RX_plus_RY" } },
           "param_count": <p>,
           "topology": "<range|linear>",
           "native_pipeline": "B",
           "source_path": "run_matched2000.py:<line>",
           "ansatz_name": "<V1|V2|V3>",
           "gate_layout_breakdown": "IQP encoding (5) + <L>*SEL layers (15 each) + final RX+RY (10) = <p>" }
        (V1 -> source_path line 118; V2 -> 120; V3 -> 122; mirror the canonical
        lock's idiom of carrying `param_count` at both top level AND under
        `decomposition`).
    (4) Also emit `results/default_75_config_lock.json` derived from
        `core/__init__.py` constants (`NUM_QUBITS=5`, `NUM_LAYERS=4`)
        + the `default_75` branch of `quantum.py` (final RX+RY per qubit,
        range topology): same schema, `locked_circuit_id="default_75"`,
        `ansatz_name="default_75"`, `param_count=75`, `topology="range"`,
        `source_path="core/__init__.py + core/models/quantum.py (default_75 branch)"`,
        `gate_layout_breakdown="IQP encoding (5) + 4*SEL layers (15 each) + final RX+RY (10) = 75"`.
        Same `raise AssertionError` gate (computed 75 == written 75; this is
        a regression guard against future edits, NOT a tautology — if the
        core formula ever changes the lock will loud-fail).
    (5) Use `json.dumps(obj, indent=2, sort_keys=True)` to write so the
        emitted JSON has a stable, diffable form. Return the list of written
        paths and print each (mirroring `run_figure_suite.py`'s
        print-every-written-path).
    Expose `build_config_locks` as a CLI sub-action: `argparse` with a
    `--config-locks-only` flag (when set, only the lock-build runs; default
    behavior is "build locks AND render diagrams" — Task 2 wires the rendering
    half). This task's commit lands the locks ONLY (and the script skeleton
    with the rendering half stubbed under `def render_diagrams(...): raise
    NotImplementedError("filled in by plan 14-09 Task 2")` so the file is
    atomically committable with the locks). No training, no sampling, no
    checkpoint reload, no `core/` edit
    (`git diff --stat core/` stays empty).
  </action>
  <verify>
    <automated>./qgan_env/bin/python run_circuit_diagrams.py --config-locks-only && ./qgan_env/bin/python -c "import json,sys; paths=['results/v1_config_lock.json','results/v2_config_lock.json','results/v3_config_lock.json','results/default_75_config_lock.json']; locks=[json.load(open(p)) for p in paths]; exp=[('V1',4,'range',75),('V2',8,'range',135),('V3',4,'linear',75),('default_75',4,'range',75)]; ok=all(L['decomposition']['num_qubits']==5 and L['decomposition']['num_layers']==nl and L['topology']==t and L['param_count']==p and L['param_count']==L['decomposition']['param_count'] and L['native_pipeline']=='B' and 'source_path' in L and 'gate_layout_breakdown' in L for L,(name,nl,t,p) in zip(locks,exp)); print('locks_ok',ok,[L['ansatz_name'] for L in locks]); sys.exit(0 if ok else 1)" &amp;&amp; grep -q 'matplotlib.use("Agg")' run_circuit_diagrams.py &amp;&amp; grep -q 'raise AssertionError' run_circuit_diagrams.py &amp;&amp; grep -q 'from revision.run_matched2000 import _QUANTUM_ANSATZ' run_circuit_diagrams.py &amp;&amp; ! grep -qE '\.fit\(|def train_|model\.sample\(|best_checkpoint\.pt' run_circuit_diagrams.py &amp;&amp; [ -z "$(git diff --stat core/)" ]</automated>
  </verify>
  <acceptance_criteria>
    - `results/v1_config_lock.json`, `v2_config_lock.json`, `v3_config_lock.json`, and `default_75_config_lock.json` all exist under `results/`
    - Each lock mirrors the `canonical_config_lock.json` schema (decomposition.{num_qubits,num_layers,param_count,gate_layout}, top-level param_count, native_pipeline="B", topology, source_path, ansatz_name, gate_layout_breakdown)
    - Computed values match the source dicts: V1=(5,4,range,75), V2=(5,8,range,135), V3=(5,4,linear,75), default_75=(5,4,range,75)
    - `grep 'raise AssertionError'` present (param-count gate, python -O safe)
    - `grep 'from revision.run_matched2000 import _QUANTUM_ANSATZ'` present (pure aggregator import, not a training import)
    - `grep 'matplotlib.use("Agg")'` present BEFORE any pyplot import (headless render contract)
    - No training/sampling/checkpoint-reload path: `grep` finds NO `.fit(` / `def train_` / `model.sample(` / `best_checkpoint.pt` in the script
    - `git diff --stat core/` is empty (byte-freeze, D-14-22)
    - Re-running `--config-locks-only` is idempotent (overwrites locks cleanly, identical bytes on second run)
  </acceptance_criteria>
  <done>Four new config-lock JSONs (v1/v2/v3/default_75) emitted with mirrored schema + explicit-raise param_count gate + pure-aggregator import of the source dict, render skeleton stubbed, core untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Render 5 circuit diagrams via qml.draw_mpl (render-only)</name>
  <files>run_circuit_diagrams.py, results/figures/circuits/default_75.{png,pdf,json}, results/figures/circuits/iqp_sel_55.{png,pdf,json}, results/figures/circuits/V1.{png,pdf,json}, results/figures/circuits/V2.{png,pdf,json}, results/figures/circuits/V3.{png,pdf,json}</files>
  <read_first>
    - run_figure_suite.py (THE render-only contract: headless matplotlib lines 45-49, `_bootstrap_repo_on_path` 54-69, `_find_repo_root` 144-150, `_require` loud-fail 153-168, `_load_json` 171-174, `_save` dual PNG+PDF + companion JSON 177-193, CLI/print 1044-1135)
    - results/canonical_config_lock.json (load this for iqp_sel_55 — locked_circuit_id, num_qubits=5, num_layers=3, topology defaulted to range, final_rotation="RX_only")
    - results/v1_config_lock.json + v2_config_lock.json + v3_config_lock.json + default_75_config_lock.json (Task 1 — load these for V1/V2/V3 and default_75; loud-fail FileNotFoundError if absent)
    - core/models/quantum.py (the constructor + bound qnode — `QuantumGenerator(num_qubits, num_layers, window_length=2*num_qubits, topology, circuit_id).qnode` is the callable to pass to `qml.draw_mpl`)
    - qgan_pennylane.ipynb (the singleton-rendering call site `qml.draw_mpl(qgan.generator, style="pennylane")` whose output is the existing untracked `circuit_diagram.png` — confirms the API we mirror)
    - .planning/phases/14-paper-revision-release-freeze/14-01-SUMMARY.md (iqp_sel_55 framing as "canonical paper circuit (frozen checkpoint epoch 1969)" for caption continuity)
  </read_first>
  <action>
    Fill in `def render_diagrams(repo: Path, figures_dir: Path) -> list[Path]`
    in `run_circuit_diagrams.py` (replacing the Task-1 stub). Render
    behavior:
    (1) Define the variant table inline as a small dict (Claude's discretion
        on data structure):
        {
          "default_75": "results/default_75_config_lock.json",
          "iqp_sel_55": "results/canonical_config_lock.json",
          "V1":         "results/v1_config_lock.json",
          "V2":         "results/v2_config_lock.json",
          "V3":         "results/v3_config_lock.json",
        }
        — order matters for output stability (use this exact order).
    (2) For each (name, lock_path): call `_load_json(repo / lock_path, f"{name} config lock")`
        (the `_require` loud-fail FileNotFoundError + json.loads idiom from
        run_figure_suite.py:153-174). Extract `num_qubits`, `num_layers`,
        `topology`, `locked_circuit_id`, `param_count` from the lock
        (decomposition + top-level — read top-level when both exist; assert
        the two match if both present, else use whichever exists). NEVER
        hand-pick numbers — every value comes from the lock JSON.
    (3) Construct the placeholder QNode via the public constructor path,
        INSIDE a `with torch.no_grad():` block to honor the render-only
        contract (mirrors `QuantumGenerator.introspect` at
        `core/models/quantum.py:344`; without it, 5 forward passes
        through `default.qubit` register in PyTorch's autograd graph — wasteful
        and inconsistent with the no-train guarantee). The `with torch.no_grad():`
        block opens here and STAYS OPEN through step (4) so the `qml.draw_mpl`
        execution is also gradient-isolated:
        `import torch; from revision.core.models.quantum import QuantumGenerator`
        then under `with torch.no_grad():`:
        `model = QuantumGenerator(num_qubits=num_qubits, num_layers=num_layers,
        window_length=2*num_qubits, topology=topology, circuit_id=locked_circuit_id)`.
        Build a deterministic placeholder noise tensor: `noise = torch.zeros(num_qubits)`.
        Use `model.params_pqc` (the constructor-initialized random tensor) OR
        an explicit `torch.zeros(model.num_params)` — Claude's discretion;
        `qml.draw_mpl` does NOT need real param values (param numerical
        values do not affect the drawn tape structure). Hard-assert
        `model.num_params == param_count` (explicit raise, python -O safe).
    (4) Render (STILL inside the `with torch.no_grad():` block from step 3):
        `fig, ax = qml.draw_mpl(model.qnode, style="pennylane")(noise, model.params_pqc)`.
        Set a caption-as-suptitle that includes the circuit name and
        spec, e.g. `fig.suptitle(f"{name} — {num_qubits} qubits × {num_layers} layers × {topology} topology — {param_count} parameters", fontsize=11)`.
        For `iqp_sel_55` ONLY, suffix the suptitle with
        " (canonical paper circuit, frozen checkpoint epoch 1969)" — this
        preserves continuity with 14-01's framing (no D-14-10 conflation
        concern because there are no generation numbers in an architecture
        diagram).
    (5) Save via the `_save` dual PNG+PDF idiom from
        `run_figure_suite.py:177-193` — write into a new subdir
        `results/figures/circuits/` (mkdir parents=True exist_ok=True).
        Each variant produces `<name>.png`, `<name>.pdf`, and a companion
        `<name>.json` recording:
        { "figure": "circuit_diagram", "circuit_id": locked_circuit_id,
          "ansatz_name": name, "source_config_lock_path": str(lock_path),
          "n_params": param_count, "depth": num_layers,
          "topology": topology, "num_qubits": num_qubits,
          "render_only": true,
          "renderer": "qml.draw_mpl(style=\"pennylane\")",
          "generation_timestamp": <ISO 8601 UTC string from datetime.datetime.utcnow().isoformat()+\"Z\"> }
    (6) Wire it into `main()`: default behavior is "build locks then render
        diagrams"; the `--config-locks-only` flag remains (Task 1) for
        partial runs; add `--diagrams-only` for the symmetric partial
        (assumes locks already exist; loud-fail FileNotFoundError if any
        lock is absent). Print every written path (15 total: 5 PNG + 5 PDF
        + 5 JSON) under "[run_circuit_diagrams] wrote:".
    The renderer is `qml.draw_mpl` — NEVER hand-rolled matplotlib gate art,
    NEVER a custom `plt.Rectangle`/`plt.Circle` mini-DSL. No training, no
    sampling, no `.fit(`, no `model.sample(`, no `best_checkpoint.pt` load.
    `core/` byte-freeze preserved (`git diff --stat core/`
    stays empty).
  </action>
  <verify>
    <automated>./qgan_env/bin/python run_circuit_diagrams.py &amp;&amp; ./qgan_env/bin/python -c "import pathlib,json,sys; d=pathlib.Path('results/figures/circuits'); names=['default_75','iqp_sel_55','V1','V2','V3']; pngs=[d/f'{n}.png' for n in names]; pdfs=[d/f'{n}.pdf' for n in names]; jsons=[d/f'{n}.json' for n in names]; exist=all(p.exists() for p in pngs+pdfs+jsons); js=[json.load(open(j)) for j in jsons]; expected_params={'default_75':75,'iqp_sel_55':55,'V1':75,'V2':135,'V3':75}; expected_layers={'default_75':4,'iqp_sel_55':3,'V1':4,'V2':8,'V3':4}; expected_topology={'default_75':'range','iqp_sel_55':'range','V1':'range','V2':'range','V3':'linear'}; meta_ok=all(j['ansatz_name']==n and j['n_params']==expected_params[n] and j['depth']==expected_layers[n] and j['topology']==expected_topology[n] and j['num_qubits']==5 and j['render_only'] is True and 'qml.draw_mpl' in j['renderer'] and 'source_config_lock_path' in j for j,n in zip(js,names)); print('files_ok',exist,'meta_ok',meta_ok,'count',len(pngs+pdfs+jsons)); sys.exit(0 if exist and meta_ok and len(pngs+pdfs+jsons)==15 else 1)" &amp;&amp; grep -q 'qml.draw_mpl' run_circuit_diagrams.py &amp;&amp; grep -q 'style="pennylane"' run_circuit_diagrams.py &amp;&amp; ! grep -qE 'plt\.Rectangle|plt\.Circle|hand.?rolled' run_circuit_diagrams.py &amp;&amp; ! grep -qE '\.fit\(|def train_|model\.sample\(|best_checkpoint\.pt' run_circuit_diagrams.py &amp;&amp; [ -z "$(git diff --stat core/)" ] &amp;&amp; ./qgan_env/bin/python verify_number_provenance.py --target results/figures/circuits/default_75.json</automated>
  </verify>
  <acceptance_criteria>
    - 15 artifacts exist under `results/figures/circuits/`: {default_75, iqp_sel_55, V1, V2, V3}.{png, pdf, json}
    - Every companion JSON records the canonical metadata (figure, circuit_id, ansatz_name, source_config_lock_path, n_params, depth, topology, num_qubits, render_only=true, renderer with "qml.draw_mpl", generation_timestamp)
    - n_params per variant matches the locked count: default_75=75, iqp_sel_55=55, V1=75, V2=135, V3=75
    - depth per variant matches the locked layer count: default_75=4, iqp_sel_55=3, V1=4, V2=8, V3=4
    - topology per variant matches the lock: default_75=range, iqp_sel_55=range, V1=range, V2=range, V3=linear
    - `grep 'qml.draw_mpl'` present AND `grep 'style="pennylane"'` present — renderer is the PennyLane canonical drawer, not custom matplotlib
    - No hand-rolled gate art: grep finds NO `plt.Rectangle` / `plt.Circle` / "hand-rolled" in the script
    - No training/sampling/checkpoint-reload path: grep finds NO `.fit(` / `def train_` / `model.sample(` / `best_checkpoint.pt`
    - `git diff --stat core/` is empty (D-14-22 byte-freeze)
    - The `default_75.json` companion (a JSON under `results/figures/circuits/`) is auto-discoverable by `verify_number_provenance.py`'s rglob — re-running the gate on it returns 0
    - Re-running the script is idempotent (overwrites figures cleanly, no retraining triggered)
  </acceptance_criteria>
  <done>5 circuit diagrams rendered via qml.draw_mpl as PNG+PDF+companion-JSON triples under results/figures/circuits/, every metadata field sourced from the config-lock JSONs, no training/sampling/core edits, iqp_sel_55 captioned as canonical paper circuit.</done>
</task>

<task type="auto">
  <name>Task 3: Author docs/circuit_atlas.md (PAPER-03 copy-paste atlas)</name>
  <files>docs/circuit_atlas.md</files>
  <read_first>
    - results/canonical_config_lock.json + v1_config_lock.json + v2_config_lock.json + v3_config_lock.json + default_75_config_lock.json (Tasks 1+2 — the SOLE numeric source for every literal in the atlas; loud-fail equivalent in this doc-author task = if any lock is absent, abort and surface the missing path)
    - results/figures/circuits/ (Task 2 — the PNG paths to embed; relative path from circuit_atlas.md is `circuits/<name>.png` since the atlas lives in docs/ and the figures live in results/figures/circuits/ — use a relative-from-doc path, NOT an absolute or repo-root-relative path)
    - verify_number_provenance.py (the gate that this doc MUST pass; confirm its `results/*.json` rglob covers the 5 lock JSONs — no verifier edit)
    - docs/ (existing doc dir; mirror its prose conventions if present — Claude's discretion on tone/voice)
    - .planning/phases/14-paper-revision-release-freeze/14-PATTERNS.md (phase paper-blocks / copy-paste-doc convention, if present)
  </read_first>
  <action>
    Author `docs/circuit_atlas.md` as a copy-paste-ready PAPER-03
    visualization atlas. The doc has 7 sections, each authored so every
    numeric literal resolves verbatim into one of the 5 config-lock JSONs
    (canonical + default_75 + v1 + v2 + v3):

    Document structure (Claude's discretion on exact prose; structure below
    is mandatory):

    # Section 0 — Front-matter
    A short opening paragraph stating the doc's purpose (visualize the 5
    production quantum circuits used in the manuscript), the provenance
    discipline (every number traces to a config-lock JSON under
    `results/`, gated by `verify_number_provenance.py`),
    and the render method (`qml.draw_mpl(qnode, style="pennylane")` via
    `run_circuit_diagrams.py`).

    # Section 1 — default_75 (the byte-frozen v1.0/v1.1 baseline)
    Subsections:
    - Embedded image: `![default_75 circuit](../results/figures/circuits/default_75.png)`
      (the relative path from `docs/circuit_atlas.md` is
      `../results/figures/circuits/default_75.png` — confirm by computing
      from the file path layout)
    - Spec table (markdown):
      | spec | value |
      | --- | --- |
      | num_qubits | 5 |
      | num_layers | 4 |
      | topology | range |
      | encoding | IQP (Hadamard + RZ per qubit) |
      | variational block | SEL — Rot(phi, theta, omega) per qubit per layer |
      | final rotations | RX + RY per qubit |
      | param_count | 75 |
      Every numeric literal (5, 4, 75) appears verbatim in `default_75_config_lock.json`.
    - "What this circuit does differently" — 2-3 sentence prose paragraph
      contrasting with the other 4 variants (it is the v1.0/v1.1 byte-frozen
      baseline that Phases 8-13 trained against; it differs from iqp_sel_55
      by carrying final RX AND RY per qubit (factor 2) instead of RX-only).

    # Section 2 — iqp_sel_55 (canonical paper circuit, frozen checkpoint epoch 1969)
    - Embedded image: `![iqp_sel_55 circuit](../results/figures/circuits/iqp_sel_55.png)`
    - Spec table — values pulled from `canonical_config_lock.json`:
      num_qubits=5, num_layers=3, topology=range, encoding=IQP, variational block=SEL,
      final rotations=RX only, param_count=55
    - Prose: explicit "canonical paper circuit (frozen checkpoint epoch 1969)" framing
      from 14-01; note it is RX-only final rotation (vs default_75's RX+RY) and
      shallower (3 layers vs 4) — that is what gives it 55 params instead of 75.

    # Section 3 — V1 (matched-budget ansatz: range, depth 4, 75 params)
    - Embedded image: `![V1 circuit](../results/figures/circuits/V1.png)`
    - Spec table from `v1_config_lock.json`: num_qubits=5, num_layers=4,
      topology=range, encoding=IQP, variational block=SEL, final rotations=RX+RY,
      param_count=75
    - Prose: structurally IDENTICAL to default_75 (same depth, topology, gate
      layout, param count) — it is the matched-budget label for the v1.0
      ansatz in `run_matched2000.py`. The contrast is operational
      (multi-seed 2000ep sweep), not architectural.

    # Section 4 — V2 (matched-budget ansatz: range, depth 8, 135 params)
    - Embedded image: `![V2 circuit](../results/figures/circuits/V2.png)`
    - Spec table from `v2_config_lock.json`: num_qubits=5, num_layers=8,
      topology=range, encoding=IQP, variational block=SEL, final rotations=RX+RY,
      param_count=135
    - Prose: doubled depth (8 vs 4) → 135 params (5 + 8*15 + 10 = 135).
      Probes whether deeper SEL stacks at the same topology pay off vs the
      baseline.

    # Section 5 — V3 (matched-budget ansatz: linear, depth 4, 75 params)
    - Embedded image: `![V3 circuit](../results/figures/circuits/V3.png)`
    - Spec table from `v3_config_lock.json`: num_qubits=5, num_layers=4,
      topology=linear, encoding=IQP, variational block=SEL, final rotations=RX+RY,
      param_count=75
    - Prose: same depth + param count as V1/default_75 but linear entangler
      (q → q+1 chain) instead of range (wrap-around r=(layer%4)+1). Isolates
      the topology variable.

    # Section 6 — Cross-comparison table (at-a-glance)
    A single markdown table with the 5 circuits as rows and columns =
    {num_qubits, num_layers, topology, final_rotation, param_count}.
    EVERY cell value is a literal lifted from the corresponding config-lock
    JSON. Add a final short paragraph noting that all 5 share num_qubits=5
    and IQP+SEL structure; the variation axes are (num_layers, topology,
    final_rotation) and these cover the matched-budget sweep.

    # Section 7 — Provenance footer
    A short footer stating:
    - All numeric literals in this doc resolve to one of:
      `results/canonical_config_lock.json`,
      `results/default_75_config_lock.json`,
      `results/v1_config_lock.json`,
      `results/v2_config_lock.json`,
      `results/v3_config_lock.json`.
    - All figures were emitted by `run_circuit_diagrams.py` using
      `qml.draw_mpl(qnode, style="pennylane")`.
    - Gated by `./qgan_env/bin/python verify_number_provenance.py --target docs/circuit_atlas.md`.

    Numeric-discipline rule: every number (5, 4, 3, 8, 55, 75, 135) MUST
    appear verbatim in at least one of the 5 lock JSONs so the gate's
    substring resolver finds it (the gate also auto-strips decision IDs
    like D-14-NN, so "D-14-22" / "D-14-10" / "D-14-01" prose references
    do not need provenance). The number `1969` (frozen checkpoint epoch)
    is allowed because it appears in `canonical_config_lock.json`'s
    `checkpoint_epoch` field. The decimal `2*15=30` parenthetical maths
    (e.g. "5 + 4*15 + 10 = 75") are also fine because each summand
    (5, 4, 15, 10, 75) is in the lock's `gate_layout_breakdown` string.

    NO training/sampling/checkpoint-reload language in the doc beyond the
    canonical-paper-circuit framing for iqp_sel_55. NO numbers from other
    artifacts (no EMD, no moments, no seeds, no epochs other than 1969 —
    this is an ARCHITECTURE atlas, not a results atlas).

    After writing, run
    `./qgan_env/bin/python verify_number_provenance.py --target docs/circuit_atlas.md`
    and it MUST PASS. If a literal fails to resolve, the fix is either
    (a) adjust the doc's literal to match the lock's exact spelling, or
    (b) widen the lock's `gate_layout_breakdown` string so the literal
    appears verbatim — NEVER edit `verify_number_provenance.py`.
  </action>
  <verify>
    <automated>[ -f docs/circuit_atlas.md ] &amp;&amp; ./qgan_env/bin/python verify_number_provenance.py --target docs/circuit_atlas.md &amp;&amp; ./qgan_env/bin/python -c "import pathlib,sys; t=pathlib.Path('docs/circuit_atlas.md').read_text(); needed=['default_75','iqp_sel_55','V1','V2','V3','canonical_config_lock.json','v1_config_lock.json','v2_config_lock.json','v3_config_lock.json','default_75_config_lock.json','qml.draw_mpl','verify_number_provenance.py','circuits/default_75.png','circuits/iqp_sel_55.png','circuits/V1.png','circuits/V2.png','circuits/V3.png','canonical paper circuit','frozen checkpoint epoch 1969']; missing=[n for n in needed if n not in t]; print('missing',missing); sys.exit(0 if not missing else 1)" &amp;&amp; [ -z "$(git diff --stat core/ verify_number_provenance.py)" ]</automated>
  </verify>
  <acceptance_criteria>
    - `docs/circuit_atlas.md` exists and PASSES `verify_number_provenance.py --target docs/circuit_atlas.md`
    - One section per circuit (5 sections) + cross-comparison + provenance footer (7 total content sections)
    - Each circuit section contains: embedded image (`![](circuits/<name>.png)` relative path that resolves from `docs/circuit_atlas.md` to `results/figures/circuits/<name>.png`), spec table (param count, depth, topology, encoding, variational block, final rotations), and a 2-3 sentence contrast paragraph
    - Cross-comparison table has 5 rows (one per circuit) × columns including num_layers, topology, param_count for at-a-glance comparison
    - Every numeric literal traces to one of the 5 config-lock JSONs (the gate enforces this)
    - iqp_sel_55 is labelled "canonical paper circuit" AND "frozen checkpoint epoch 1969" (continuity with 14-01 framing)
    - The doc references `qml.draw_mpl` as the renderer (not "custom matplotlib") and `verify_number_provenance.py` as the gate
    - `verify_number_provenance.py` itself is NOT modified to make the doc pass (gate unchanged; `git diff --stat verify_number_provenance.py` empty)
    - `git diff --stat core/` is empty (D-14-22 byte-freeze preserved)
  </acceptance_criteria>
  <done>circuit_atlas.md authored with one section per circuit + cross-comparison + provenance footer, every numeric literal gated by the existing number-provenance verifier unmodified, every figure cross-linked, iqp_sel_55 labelled canonical paper circuit (frozen checkpoint epoch 1969).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| run_matched2000._QUANTUM_ANSATZ Python dict → config-lock JSON | the V1/V2/V3 dicts live in code only; a wrong import or a mistyped variant would emit a misleading lock and silently misrepresent the paper architecture |
| config-lock JSON → rendered circuit diagram | a placeholder param tensor of the WRONG shape (or a misread num_layers/topology) would draw a misleading circuit and the visualization-vs-architecture gap would survive |
| circuit_atlas.md ↔ config-lock JSONs | a hand-typed numeric literal in the atlas would bypass the number-provenance gate and put an unverifiable number in PAPER-03 |
| importing revision.run_matched2000 / revision.core.models.quantum | importing for QNode-construction-only must NOT trigger any model `.fit(` / sampling / checkpoint reload path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-18 | Tampering | wrong param_count in a V1/V2/V3 config-lock JSON | mitigate | Task 1 explicit `raise AssertionError` (python -O safe) — computed param_count from the source formula MUST equal the writeable param_count per variant; loud-fail lists every offending variant before any lock is written |
| T-14-19 | Tampering | placeholder QNode built from a wrong (num_layers, topology, circuit_id) tuple → diagram misrepresents architecture | mitigate | Task 2 reads num_qubits/num_layers/topology/circuit_id EXCLUSIVELY from the loaded config-lock JSON (the same JSON that Task 1's assertion guarded), and hard-asserts `model.num_params == lock.param_count` before draw_mpl; the rendered tape is therefore the exact tape the lock describes |
| T-14-20 | Tampering | silent partial render on a missing config-lock JSON | mitigate | Task 2 `_load_json` calls `_require` which raises `FileNotFoundError` with a "render-only (no training/sampling/recompute)" message on any absent lock — never a silent partial figure |
| T-14-21 | Spoofing | hand-typed numeric literal in circuit_atlas.md (bypassing the lock JSONs) | mitigate | Task 3's `verify_number_provenance.py --target docs/circuit_atlas.md` is the EXECUTABLE gate; every literal must resolve to a `results/*.json` value via the gate's rglob — verifier unmodified |
| T-14-22 | Tampering | importing run_matched2000 silently triggers a torch training import path | mitigate | Pure-aggregator-safety precedent (14-03 SUMMARY deviation #1): run_matched2000's top-level imports are stdlib + numpy + yaml + revision.core constants. The Task-1 import grabs only the `_QUANTUM_ANSATZ` module-level dict; no function call triggers training. Verified by `! grep -qE '\.fit\(|def train_|model\.sample\(|best_checkpoint\.pt'` in the script |
| T-14-23 | Tampering | hand-rolled matplotlib gate art instead of `qml.draw_mpl` (would diverge from the canonical PennyLane drawer used in qgan_pennylane.ipynb) | mitigate | Task 2 grep gate: `grep -q 'qml.draw_mpl'` AND `grep -q 'style="pennylane"'` AND `! grep -qE 'plt\.Rectangle\|plt\.Circle\|hand.?rolled'` — renderer is `qml.draw_mpl` only |
| T-14-24 | Tampering | `core/` edit slips in (would break D-14-22 byte-freeze and the 7 frozen-default reproducibility plans) | mitigate | Every task's verify gate asserts `[ -z "$(git diff --stat core/)" ]` |
</threat_model>

<verification>
- `run_circuit_diagrams.py` runs `--config-locks-only` and emits 4 config-lock JSONs (v1/v2/v3/default_75) with mirrored canonical schema + explicit-raise param-count gate (python -O safe), pure-aggregator import of the source dict, no training/sampling/checkpoint-reload, `core/` untouched.
- The full run emits 15 artifacts under `results/figures/circuits/` (5 PNG + 5 PDF + 5 companion JSON) via `qml.draw_mpl(qnode, style="pennylane")`; every companion JSON records circuit_id / source_config_lock_path / n_params / depth / topology / num_qubits and is auto-discoverable by `verify_number_provenance.py`.
- `docs/circuit_atlas.md` exists, embeds the 5 PNGs via relative paths, contains a spec table per circuit + a cross-comparison table + a provenance footer, and PASSES `verify_number_provenance.py` with the verifier unmodified.
- iqp_sel_55 is labelled "canonical paper circuit (frozen checkpoint epoch 1969)" in caption + atlas (continuity with 14-01 framing).
- `core/` byte-freeze (D-14-22) preserved across all 3 tasks.
</verification>

<success_criteria>
PAPER-03 (Circuit Design Rationale) now has a complete, JSON-traceable
architecture visualization atlas for every production quantum circuit:
default_75 (baseline), iqp_sel_55 (canonical paper circuit, frozen checkpoint
epoch 1969), and the V1/V2/V3 matched-budget ansatz variants. Every numeric
literal in the new doc + figure companions resolves to one of 5 config-lock
JSONs under `results/` and is gated by the existing
`verify_number_provenance.py` unmodified. Rendered via `qml.draw_mpl(style="pennylane")`,
no retraining, no sampling, no checkpoint reload, `core/` untouched
(D-14-22). The previously-untracked singleton `circuit_diagram.png` at the
repo root is superseded by the tracked, JSON-companion-backed
`results/figures/circuits/default_75.{png,pdf,json}` triple.
</success_criteria>

<output>
Create `.planning/phases/14-paper-revision-release-freeze/14-09-SUMMARY.md` when done
</output>
