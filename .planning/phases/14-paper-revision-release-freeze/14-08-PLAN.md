---
phase: 14-paper-revision-release-freeze
plan: 08
type: execute
wave: 6
depends_on: ["14-02", "14-03", "14-04"]
files_modified:
  - revision/run_matched2000_dualscale.py
  - revision/run_figure_suite.py
  - revision/results/matched2000_dualscale.json
  - revision/results/figures/
autonomous: true
requirements: [PAPER-09]
must_haves:
  truths:
    - "No retraining and no resampling: every number is computed from the already-saved revision/results/matched2000/runs/<model>/<seed>/samples.npy bundles (45 reused, byte-frozen)"
    - "Both scales (OD AND log_return) are present for all 9 matched-2000ep models, with a mean±std-over-5-seeds aggregate per (model, scale, metric)"
    - "Every emitted number is computed via revision.core.eval helpers ONLY (compute_emd/compute_moments/compute_acf/compute_dtw) — no hand-typed numbers, no re-implemented metric math (D-11-10, D-14-16)"
    - "The frozen-checkpoint headline is carried as its OWN distinct model_kind row-set (source=frozen_checkpoint_epoch_1969), numerically and visually separate from iqp_sel_55_repro (source=matched2000_reproduction) — never merged (D-14-10)"
    - "Cross-artifact data_hash equality is enforced by an explicit raise (raise AssertionError, python -O safe); recomputed hash and every consumed bundle must equal 91e447d4624e25b3 (D-14-16)"
    - "Any missing samples.npy / inverse_kwargs.npz fails loudly (FileNotFoundError) — never a silent partial aggregate"
    - "revision/results/matched2000_dualscale.json is the single source of truth for these matched-budget dual-scale numbers; the side-by-side figure and comparison table render only from it (no second numeric origin)"
  artifacts:
    - path: "revision/run_matched2000_dualscale.py"
      provides: "eval-only matched-2000ep dual-scale aggregator over the 9 saved sample sets + frozen headline; long-form rows[] + per-(model,scale,metric) mean±std aggregate; explicit-raise data_hash gate; loud-fail on missing samples"
      min_lines: 200
    - path: "revision/results/matched2000_dualscale.json"
      provides: "long-form dual-scale rows[] (model_kind, pipeline, seed, metric_name, scale, value) + aggregates[] (model_kind, scale, metric_name, mean, std, n_seeds) + a distinct frozen-headline row-set + data_hash 91e447d4624e25b3"
      contains: "matched2000_reproduction"
    - path: "revision/results/figures/"
      provides: "render-only matched2000 dual-scale side-by-side figure set (matched2000_dualscale_sidebyside.{png,pdf,json}) + copy-paste comparison table doc, all sourced solely from matched2000_dualscale.json"
  key_links:
    - from: "revision/run_matched2000_dualscale.py"
      to: "revision/results/matched2000/runs/<model>/<seed>/samples.npy + inverse_kwargs.npz"
      via: "verbatim run_dualscale_fidelity reconstruct_od Pipeline-B path, loud-fail on missing bundle"
      pattern: "FileNotFoundError|np.load"
    - from: "revision/run_matched2000_dualscale.py"
      to: "revision.core.eval (compute_emd/compute_moments/compute_acf/compute_dtw)"
      via: "import-only, reused UNCHANGED — no metric math re-implemented (D-11-10)"
      pattern: "from revision.core.eval import"
    - from: "revision/run_matched2000_dualscale.py"
      to: "data_hash == 91e447d4624e25b3"
      via: "explicit raise AssertionError cross-artifact equality gate (python -O safe)"
      pattern: "raise AssertionError"
    - from: "revision/results/figures/matched2000_dualscale_sidebyside.png"
      to: "revision/results/matched2000_dualscale.json"
      via: "render-only JSON->figure, loud-fail on missing companion JSON"
      pattern: "FileNotFoundError|_load_json"
---

<objective>
Add `revision/run_matched2000_dualscale.py` — a pure RE-EVALUATION aggregator
that, from the 45 already-saved `revision/results/matched2000/runs/<model>/<seed>/samples.npy`
bundles (9 models × seeds 42–46, all accepted by the 14-02 strict gate) plus the
frozen-checkpoint headline, recomputes EMD + moments (+ every metric the existing
audited `run_dualscale_fidelity.py` emits) at BOTH the `OD` and `log_return`
scales via `revision.core.eval` ONLY, and emits the matched-2000ep counterpart
of `fidelity_dualscale.json`. Then render a render-only side-by-side
quantum-vs-classical, OD-vs-log_return comparison figure set + copy-paste
comparison table — all sourced solely from the new JSON.

Purpose: the resubmission needs a FAIR side-by-side dual-scale comparison at the
matched 2000-epoch budget. Current matched-budget numeric artifacts report EMD
primarily on the OD scale; the original submission headlined a log-return-scale
EMD. `revision/results/fidelity_dualscale.json` is the OLD 1000-epoch artifact
(60 frozen Phase-11 bundles). This plan produces the matched-2000ep dual-scale
artifact so reviewers see both scales on the same matched budget, with the
frozen-checkpoint headline kept numerically and visually distinct from the
2000ep reproduction (D-14-10).

Output: `revision/run_matched2000_dualscale.py`,
`revision/results/matched2000_dualscale.json` (the single source of truth for
these numbers), and a render-only `revision/results/figures/`
`matched2000_dualscale_sidebyside.{png,pdf,json}` set + comparison table doc,
all gated by `verify_number_provenance.py`.

NO retraining. NO resampling. The 45 `samples.npy` bundles are read-only; this
is metric re-emission + render only — exactly mirroring the audited dual-scale
evaluator's reconstruction + `revision.core.eval` math (D-11-10).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-paper-revision-release-freeze/14-CONTEXT.md
@.planning/phases/14-paper-revision-release-freeze/14-RESEARCH.md
@.planning/phases/14-paper-revision-release-freeze/14-PATTERNS.md
@.planning/phases/14-paper-revision-release-freeze/14-02-SUMMARY.md
@.planning/phases/14-paper-revision-release-freeze/14-03-SUMMARY.md
@.planning/phases/14-paper-revision-release-freeze/14-04-PLAN.md

<interfaces>
<!-- run_dualscale_fidelity.py is the EXACT audited pattern to mirror — copy
     its reconstruction + scale-tagged metric math verbatim, only swap the
     run-dir resolver to the matched2000 layout and add the seed-aggregate. -->

revision/run_dualscale_fidelity.py contract to mirror VERBATIM:
- repo bootstrap: `_bootstrap_repo_on_path()` (walks to revision/core/preprocessing.py); `_find_repo_root()`.
- `EXPECTED_DATA_HASH = "91e447d4624e25b3"`; `SEEDS = [42, 43, 44, 45, 46]`; `NLAGS = 9`; `DTW_N_PAIRS = 100`.
- `_compute_data_hash(csv_path)` = sha256(load_and_preprocess(csv)['OD'].tobytes())[:16].
- `reconstruct_od(model_kind, pipeline, seed)` — Pipeline-B path is load-bearing: `np.random.default_rng(seed*7919+1)` od_start draw, `inverse_logreturns`, the `od[:, :10]` 11->10 trim. COPY VERBATIM; do not change the RNG seed expression.
- `_od_scale_rows()` / `_log_return_rows()` — the EXACT metric suite (emd, moment_{mean,std,skewness,kurtosis}, acf_lag{0..9}_{mean,std}, dtw_{mean,median,std}) on each scale. DTW recipe: `np.random.default_rng(s*31)`, 100 synth pairs, 64 real, verbatim. COPY VERBATIM — no new metric math (D-11-10).
- metric helpers imported UNCHANGED: `from revision.core.eval import compute_acf, compute_dtw, compute_emd, compute_moments`; `from revision.core.preprocessing import inverse_logreturns`; `from revision.core.data import load_and_preprocess, rolling_window`; `from revision.core import WINDOW_LENGTH`.
- `build_real_references(csv_path)` — real_OD_flat (rolling_window OD step 2 flat), real_windowed_OD, real_log_delta (= d_real["log_delta"]). COPY VERBATIM so numbers reconcile with fidelity_dualscale.json.
- output idiom: write a single JSON with `schema`, `data_hash`, `metric_helpers`, `rows`.

matched2000 run-dir layout (verified on disk — THIS is the only thing that
changes from run_dualscale_fidelity's `_run_base`):
- 9 model dirs under `revision/results/matched2000/runs/`: `iqp_sel_55_repro`, `V1`, `V2`, `V3`, `wgan_mlp`, `wgan_cnn`, `wgan_lstm`, `vae`, `ar`.
- per (model, seed) dir: `config.yaml`, `inverse_kwargs.npz`, `metrics.json`, `samples.npy` (the 5-seed set is {42,43,44,45,46}).
- EVERY matched2000 model is `pipeline: B` (verified: iqp_sel_55_repro/V1/V2/V3 are model_kind=quantum pipeline=B; wgan_*/vae/ar are model_kind=<name> pipeline=B). So the matched2000 sweep is Pipeline-B only — mirror run_dualscale_fidelity's Pipeline-B branch for OD AND log_return; there is NO Pipeline-A run dir in matched2000 (do NOT fabricate one, do NOT emit Pipeline-A null rows for models that have no A bundle).
- config.yaml carries `data_hash` (= 91e447d4624e25b3), `model_kind`, `pipeline`, `source` (iqp_sel_55_repro -> matched2000_reproduction). Read via `yaml.safe_load` (PyYAML 6.0.3 in qgan_env; not torch/pennylane/core — pure-aggregator-safe, per 14-03 SUMMARY deviation #1).

frozen-checkpoint headline (the 10th, DISTINCT row-set — D-14-10):
- `revision/results/headline_canonical.json`: keys include `source` (= "frozen_checkpoint_epoch_1969"), `data_hash` (= 91e447d4624e25b3), `scales` (["OD","log_return"]), `rows` (56 long-form rows: model_kind=quantum, pipeline=B, scale in {OD,log_return}, same metric_name vocabulary). It is ALREADY a dual-scale eval emitted by revision.core.eval in 14-02. Do NOT recompute it (no checkpoint reload here — 14-02 owns that). RE-EMIT its rows verbatim under a distinct `model_kind="frozen_checkpoint_headline"` AND carry an explicit `source="frozen_checkpoint_epoch_1969"` field on each headline row so it is a separate, never-merged row-set with its own aggregate (n_seeds=1, single generation seed).

number-provenance gate (14-03 — RESEARCH Pattern 3):
- `revision/verify_number_provenance.py --target <file>` scans `revision/results/*.json` recursively (`RESULTS.rglob("*.json")`) — so the new `matched2000_dualscale.json` is AUTOMATICALLY in the gate's resolution corpus the moment it is written. The comparison-table doc's literals are therefore gated by the EXISTING verifier with no verifier edit; the verify step asserts this coverage rather than modifying the gate.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Eval-only matched-2000ep dual-scale aggregator + JSON</name>
  <files>revision/run_matched2000_dualscale.py, revision/results/matched2000_dualscale.json</files>
  <read_first>
    - revision/run_dualscale_fidelity.py (THE audited pattern: copy _bootstrap_repo_on_path, _find_repo_root, _compute_data_hash, reconstruct_od Pipeline-B branch VERBATIM, _od_scale_rows/_log_return_rows VERBATIM, build_real_references VERBATIM, the output-write idiom; only _run_base / MODEL_KINDS and the new seed-aggregate change)
    - revision/results/matched2000/runs/ (the 9 model dirs × seeds 42-46 — the read-only inputs; confirm samples.npy + inverse_kwargs.npz + config.yaml present per dir)
    - revision/results/matched2000/sweep_status.json (45/45 accepted; the model/seed matrix)
    - revision/results/headline_canonical.json (the frozen-checkpoint dual-scale row-set to carry distinctly; source=frozen_checkpoint_epoch_1969, 56 rows, scales OD+log_return — DO NOT recompute it)
    - revision/results/fidelity_dualscale.json (the OLD 1000-epoch artifact — schema reference ONLY; the matched-2000ep JSON is its 2000ep counterpart, NOT an edit of it)
    - .planning/phases/14-paper-revision-release-freeze/14-03-SUMMARY.md (yaml.safe_load is pure-aggregator-safe; data_hash explicit-raise gate idiom; D-14-10 headline != reproduction)
  </read_first>
  <action>
    Create `revision/run_matched2000_dualscale.py`. Copy `run_dualscale_fidelity.py`
    end-to-end and change ONLY what the matched2000 layout requires:
    (1) `_run_base`/`_resolve_run_dir`: resolve
    `revision/results/matched2000/runs/<model>/<seed>` (loud-fail
    `FileNotFoundError` with a "no retraining/resampling — D-14-08" message if a
    `samples.npy` or `inverse_kwargs.npz` is absent; never a silent partial).
    (2) `MODEL_KINDS = ["iqp_sel_55_repro","V1","V2","V3","wgan_mlp","wgan_cnn",
    "wgan_lstm","vae","ar"]`; matched2000 is Pipeline-B ONLY (verified) — call
    the VERBATIM-copied `reconstruct_od` Pipeline-B branch (the
    `np.random.default_rng(seed*7919+1)` od_start draw and `od[:, :10]` trim are
    load-bearing, do NOT alter) and emit both `_od_scale_rows` and
    `_log_return_rows` (real values — Pipeline B has a log_return space; do NOT
    emit Pipeline-A null rows, there is no Pipeline-A matched2000 run).
    (3) Metric math is `revision.core.eval` ONLY — import
    `compute_emd/compute_moments/compute_acf/compute_dtw` UNCHANGED; reuse the
    VERBATIM `_od_scale_rows`/`_log_return_rows` bodies and
    `build_real_references` (real_OD_flat / real_windowed_OD / real_log_delta) so
    every metric reconciles with the audited dualscale recipe (D-11-10). Do NOT
    re-implement any metric.
    (4) data_hash gate: recompute via `_compute_data_hash`; `raise
    AssertionError` (NOT bare assert — python -O safe) if recomputed !=
    `91e447d4624e25b3`, and `raise AssertionError` if ANY consumed
    `matched2000/runs/<model>/<seed>/config.yaml` `data_hash` != that value
    (yaml.safe_load — pure-aggregator-safe per 14-03). Loud-fail listing every
    mismatching (model, seed).
    (5) Frozen-checkpoint headline as its OWN distinct row-set (D-14-10): load
    `headline_canonical.json` and re-emit its existing dual-scale rows VERBATIM
    (do NOT recompute — 14-02 owns the checkpoint reload) under a distinct
    `model_kind="frozen_checkpoint_headline"` AND an explicit
    `source="frozen_checkpoint_epoch_1969"` field on every headline row. For the
    9 matched2000 models, each row's `source` field is READ VERBATIM from that
    run's `config.yaml['source']` (never hardcoded) — iqp_sel_55_repro will carry
    `source="matched2000_reproduction"`, V1/V2/V3 will carry
    `source="matched2000_ansatz"`, and wgan_mlp/wgan_cnn/wgan_lstm/vae/ar will
    carry `source="matched2000_baseline"`. The headline row-set is the strict
    separation D-14-10 mandates, never merged into iqp_sel_55_repro.
    (6) Add a per-(model_kind, scale, metric_name) aggregate: emit an
    `aggregates` array of {model_kind, scale, metric_name, mean, std, n_seeds}
    where mean/std are over the 5 matched2000 seeds (n_seeds=5); the frozen
    headline's aggregate is n_seeds=1 (single generation seed) and stays a
    distinct model_kind. Use `statistics`/`numpy` for mean/std — these are
    aggregations of `revision.core.eval` outputs, not new metric math.
    Write `revision/results/matched2000_dualscale.json` with the same top-level
    idiom as `fidelity_dualscale.json`: `schema` (literal noting "matched-2000ep
    dual-scale rows[] + per-(model,scale,metric) seed-aggregate; frozen headline
    DISTINCT, D-14-10"), `model_kinds`, `pipelines: ["B"]`,
    `seeds: [42,43,44,45,46]`, `data_hash: "91e447d4624e25b3"`, `metric_helpers`
    (the four `revision.core.eval` fn names + "reused unchanged, D-11-10"),
    `rows` (all 9 models × 5 seeds × both scales + the distinct frozen-headline
    rows), `aggregates`. CLI: `argparse --out` (default `revision/results`) +
    `--csv-path` (default `./data.csv`, resolved absolute). This is a renderer of
    SAVED samples — NO model `.fit(`, NO sampling, NO checkpoint reload, NO
    `revision/core/` edit (`git diff --stat revision/core/` stays empty).
  </action>
  <verify>
    <automated>./qgan_env/bin/python revision/run_matched2000_dualscale.py && ./qgan_env/bin/python -c "import json,sys; o=json.load(open('revision/results/matched2000_dualscale.json')); rows=o['rows']; aggs=o['aggregates']; mk=set(r['model_kind'] for r in rows); base={'iqp_sel_55_repro','V1','V2','V3','wgan_mlp','wgan_cnn','wgan_lstm','vae','ar'}; head=[r for r in rows if r['model_kind']=='frozen_checkpoint_headline' or r.get('source')=='frozen_checkpoint_epoch_1969']; repro=[r for r in rows if r['model_kind']=='iqp_sel_55_repro']; both_all=all(any(r['model_kind']==m and r['scale']==sc and r['metric_name']=='emd' for r in rows) for m in base for sc in ('OD','log_return')); aggok=all(any(a['model_kind']==m and a['scale']==sc and a['metric_name']=='emd' and a['n_seeds']==5 for a in aggs) for m in base for sc in ('OD','log_return')); head_agg_ok=any(a['model_kind']=='frozen_checkpoint_headline' and a['n_seeds']==1 for a in aggs); ok = base.issubset(mk) and len(head)>0 and len(repro)>0 and (set(id(r) for r in head) & set(id(r) for r in repro))==set() and both_all and aggok and head_agg_ok and o['data_hash']=='91e447d4624e25b3'; print('models',sorted(mk),'headline_rows',len(head),'repro_rows',len(repro),'both_scales',both_all,'agg_ok',aggok,'head_agg_ok',head_agg_ok); sys.exit(0 if ok else 1)" && grep -q 'from revision.core.eval import' revision/run_matched2000_dualscale.py && grep -q 'raise AssertionError' revision/run_matched2000_dualscale.py && ! grep -qE '\.fit\(|def train_|model\.sample\(' revision/run_matched2000_dualscale.py && [ -z "$(git diff --stat revision/core/)" ]</automated>
  </verify>
  <acceptance_criteria>
    - `revision/results/matched2000_dualscale.json` exists with `data_hash` == `91e447d4624e25b3`, `pipelines == ["B"]`, `seeds == [42,43,44,45,46]`
    - `rows[]` contains both `scale="OD"` AND `scale="log_return"` `emd` rows for ALL 9 models (iqp_sel_55_repro, V1, V2, V3, wgan_mlp, wgan_cnn, wgan_lstm, vae, ar)
    - The frozen-checkpoint headline rows are a DISTINCT row-set (model_kind=frozen_checkpoint_headline AND source=frozen_checkpoint_epoch_1969), with ZERO row-object overlap with the iqp_sel_55_repro rows (D-14-10)
    - `aggregates[]` has a {mean,std,n_seeds} entry per (model, scale, metric) with n_seeds=5 for the 9 matched2000 models and n_seeds=1 for the frozen headline
    - `grep 'from revision.core.eval import'` present; metric math NOT re-implemented (D-11-10)
    - `grep 'raise AssertionError'` present (explicit-raise data_hash gate, python -O safe)
    - No training/sampling/checkpoint-reload path: grep shows no `.fit(` / `def train_` / `model.sample(` in the script (render of saved samples only)
    - `git diff --stat revision/core/` is empty (eval module untouched)
    - Re-running the script is idempotent (overwrites the JSON cleanly, no new training)
  </acceptance_criteria>
  <done>matched2000_dualscale.json emitted from the 45 saved sample sets via revision.core.eval only, both scales × all 9 models + distinct frozen headline + per-seed aggregate, explicit-raise data_hash gate, loud-fail on missing samples, core untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Render-only dual-scale side-by-side figure + comparison table doc</name>
  <files>revision/run_figure_suite.py (or revision/run_matched2000_dualscale_figure.py), revision/results/figures/matched2000_dualscale_sidebyside.{png,pdf,json}, revision/results/figures/matched2000_dualscale_comparison.md</files>
  <read_first>
    - revision/run_figure_suite.py (14-04 — THE render-only pattern: headless `matplotlib.use("Agg")` before pyplot, `_load_json()` loud-fail FileNotFoundError on missing companion, dual PNG+PDF `savefig(dpi=150, bbox_inches="tight")` + `plt.close`, `_find_repo_root()`, CLI + print-written-paths)
    - revision/run_introspect_figures.py (the original render-only contract 14-04 itself copied — secondary reference if run_figure_suite.py styling differs)
    - revision/results/matched2000_dualscale.json (Task 1 — the SOLE numeric source for this figure + table; loud-fail if absent)
    - revision/run_model_info.py (14-03 — the JSON->markdown `_agg()`/`_fmt()` render idiom: every table cell value pulled from JSON, formatted so its textual form appears verbatim in the JSON the number-provenance gate checks)
    - revision/verify_number_provenance.py (14-03 — the gate; confirm it rglob-scans revision/results/*.json so matched2000_dualscale.json is auto-covered, no verifier edit)
    - .planning/phases/14-paper-revision-release-freeze/14-PATTERNS.md (the phase's paper-blocks / copy-paste-doc convention, if present)
  </read_first>
  <action>
    Add a render-only side-by-side figure routine + a copy-paste comparison-table
    doc, BOTH sourced solely from `revision/results/matched2000_dualscale.json`
    (Task 1) — no recomputation, no second numeric origin. Two acceptable
    structures (Claude's discretion): (a) extend `revision/run_figure_suite.py`
    with a `matched2000_dualscale_sidebyside` routine, or (b) a small dedicated
    `revision/run_matched2000_dualscale_figure.py` that copies the
    `run_figure_suite.py` render-only contract. Either way:
    - Headless: `import matplotlib; matplotlib.use("Agg")` BEFORE pyplot.
    - Loud-fail: a `_load_json()` that `raise FileNotFoundError` with a
      "render-only (no training/eval) — matched2000_dualscale.json missing; run
      run_matched2000_dualscale.py first" message if the JSON is absent. NEVER a
      silent partial figure.
    - Render `revision/results/figures/matched2000_dualscale_sidebyside.{png,pdf}`:
      a side-by-side comparison of all 9 matched-2000ep models — quantum
      (iqp_sel_55_repro, V1, V2, V3) vs classical (wgan_mlp/cnn/lstm, vae, ar) —
      with OD vs log_return EMD (mean±std over 5 seeds from `aggregates[]`) as
      the primary panel and at least the key moments (moment_mean / moment_std)
      as a secondary panel. The frozen-checkpoint headline is plotted as a
      VISUALLY DISTINCT marker/series (distinct color + label, e.g. a separate
      "frozen checkpoint (epoch 1969)" reference marker) — NEVER merged into the
      iqp_sel_55_repro series (D-14-10). Subplot composition / color scheme /
      bar-vs-point styling is Claude's discretion (D-14 discretion).
    - Write a same-stem companion
      `revision/results/figures/matched2000_dualscale_sidebyside.json`
      (reproducibility: the exact (model, scale, metric, mean, std) values
      plotted + the source artifact path `revision/results/matched2000_dualscale.json`)
      — every figure traceable to a JSON value.
    - Emit a copy-paste comparison-table doc
      `revision/results/figures/matched2000_dualscale_comparison.md` (or the
      14-PATTERNS paper-block path/format if that convention exists) rendered
      ENTIRELY from `matched2000_dualscale.json` via the
      `run_model_info.py` `_fmt()`-style render (every cell pulled from the JSON,
      formatted so its textual form appears verbatim in the JSON the
      number-provenance gate checks; zero hand-typed numbers) — a 9-model × {OD,
      log_return} EMD (+ key moments) markdown/LaTeX table with the frozen
      headline as its own clearly-labeled row, distinct from iqp_sel_55_repro.
    - Print every written path. NO training, NO sampling, NO metric
      recomputation (this is a renderer of Task-1's JSON only).
    Finally, assert the EXISTING number-provenance gate covers these new
    numbers: run `./qgan_env/bin/python revision/verify_number_provenance.py
    --target revision/results/figures/matched2000_dualscale_comparison.md` and it
    MUST PASS (every literal resolves to `matched2000_dualscale.json`, which the
    gate auto-includes via its `revision/results/*.json` rglob — no verifier
    edit; if a literal fails to resolve, the fix is the renderer's `_fmt()`
    precision, NOT editing the gate or hand-typing).
  </action>
  <verify>
    <automated>./qgan_env/bin/python $(grep -ql 'matched2000_dualscale_sidebyside' revision/run_figure_suite.py 2>/dev/null && echo revision/run_figure_suite.py || echo revision/run_matched2000_dualscale_figure.py) && ./qgan_env/bin/python -c "import pathlib,sys; d=pathlib.Path('revision/results/figures'); png=d/'matched2000_dualscale_sidebyside.png'; pdf=d/'matched2000_dualscale_sidebyside.pdf'; cj=d/'matched2000_dualscale_sidebyside.json'; md=d/'matched2000_dualscale_comparison.md'; ok=all(p.exists() for p in (png,pdf,cj,md)); sys.exit(0 if ok else 1)" && ./qgan_env/bin/python revision/verify_number_provenance.py --target revision/results/figures/matched2000_dualscale_comparison.md && grep -qE 'matplotlib.use\("Agg"\)' $(grep -ql 'matched2000_dualscale_sidebyside' revision/run_figure_suite.py 2>/dev/null && echo revision/run_figure_suite.py || echo revision/run_matched2000_dualscale_figure.py) && grep -qE 'FileNotFoundError|render-only' $(grep -ql 'matched2000_dualscale_sidebyside' revision/run_figure_suite.py 2>/dev/null && echo revision/run_figure_suite.py || echo revision/run_matched2000_dualscale_figure.py)</automated>
  </verify>
  <acceptance_criteria>
    - `revision/results/figures/matched2000_dualscale_sidebyside.png` AND `.pdf` AND same-stem `.json` companion all exist
    - `revision/results/figures/matched2000_dualscale_comparison.md` exists and PASSES `verify_number_provenance.py` (every literal resolves to `matched2000_dualscale.json`)
    - The figure shows quantum vs classical, OD vs log_return EMD (mean±std over 5 seeds) + key moments; the frozen-checkpoint headline is a visually distinct series/marker, never merged with iqp_sel_55_repro (D-14-10)
    - Renderer uses headless `matplotlib.use("Agg")` before pyplot and a `_load_json` loud-fail on the missing source JSON (grep)
    - No training/sampling/metric-recompute path in the renderer (it reads only `matched2000_dualscale.json`)
    - The companion JSON records the source artifact path `revision/results/matched2000_dualscale.json` (figure->data provenance)
    - The existing `verify_number_provenance.py` was NOT modified to make the doc pass (gate auto-covers the new JSON via its rglob)
  </acceptance_criteria>
  <done>Render-only matched2000 dual-scale side-by-side figure (PNG+PDF+companion JSON) + copy-paste comparison-table doc, sourced solely from matched2000_dualscale.json, frozen headline visually distinct, comparison doc passes the existing number-provenance gate unmodified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| saved samples.npy → re-emitted metric | a stale/wrong-hash/missing sample silently re-evaluated would put an unfair or fabricated number in the resubmission |
| matched2000_dualscale.json → figure/table | a figure/table not strictly sourced from the gated JSON would introduce a second, unverifiable numeric origin |
| frozen headline ↔ 2000ep reproduction | conflating them would misrepresent the load-bearing headline as a multi-seed reproduction (D-14-10) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-13 | Tampering | wrong-hash / mixed-budget sample silently aggregated | mitigate | Explicit `raise AssertionError` (python -O safe) if recomputed data_hash != 91e447d4624e25b3 OR any consumed config.yaml data_hash mismatches; loud-fail lists every offending (model, seed) |
| T-14-14 | Tampering | silent partial aggregate on a missing sample bundle | mitigate | `_resolve_run_dir` raises `FileNotFoundError` with a no-retraining/resampling message on any absent samples.npy / inverse_kwargs.npz — never a silent partial |
| T-14-15 | Spoofing | hand-typed / re-implemented metric number | mitigate | Metric math is `revision.core.eval` import-only, VERBATIM `_od_scale_rows`/`_log_return_rows`; `git diff --stat revision/core/` empty; the comparison-table doc is gated by the existing `verify_number_provenance.py` |
| T-14-16 | Tampering | frozen headline merged into iqp_sel_55_repro | mitigate | Headline carried as a distinct model_kind + explicit `source=frozen_checkpoint_epoch_1969`; verify asserts zero row-object overlap with iqp_sel_55_repro; figure plots it as a visually distinct series (D-14-10) |
| T-14-17 | Repudiation | figure ↔ data provenance | mitigate | Same-stem companion JSON records every plotted value + the source artifact path; comparison doc passes the number-provenance gate that auto-covers matched2000_dualscale.json |
</threat_model>

<verification>
- `run_matched2000_dualscale.py` runs eval-only over the 45 saved bundles + frozen headline; emits `matched2000_dualscale.json` (both scales × 9 models + distinct headline + per-seed aggregate); explicit-raise data_hash gate; loud-fail on missing samples; `revision/core/` untouched.
- The side-by-side figure (PNG+PDF+companion JSON) + comparison-table doc render solely from that JSON; frozen headline visually + numerically distinct from iqp_sel_55_repro.
- The comparison-table doc PASSES the existing `verify_number_provenance.py` with no verifier modification (gate auto-covers the new JSON via its rglob).
</verification>

<success_criteria>
A fair matched-2000ep dual-scale (OD + log_return) side-by-side comparison
across all 9 models exists, computed via `revision.core.eval` only from the
already-saved samples (no retraining/resampling), with the frozen-checkpoint
headline kept numerically and visually distinct from the 2000ep reproduction,
gated by the explicit-raise data_hash check and the existing number-provenance
gate — the resubmission's PAPER-09 dual-scale comparison is now traceable to a
single JSON source of truth.
</success_criteria>

<output>
Create `.planning/phases/14-paper-revision-release-freeze/14-08-SUMMARY.md` when done
</output>
