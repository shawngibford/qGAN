---
phase: 14-paper-revision-release-freeze
plan: 13
type: execute
wave: 11
depends_on: ["14-08", "14-09", "14-10", "14-11", "14-12"]
files_modified:
  - revision/verify_number_provenance.py
  - revision/run_model_info.py
  - revision/run_matched2000.py
  - revision/run_matched2000_dualscale.py
  - revision/run_methods_full.py
  - revision/run_figure_suite.py
  - revision/run_circuit_diagrams.py
  - revision/run_classical_arch_extract.py
  - revision/run_framework_versions.py
  - revision/run_canonical_headline.py
  - revision/verify_freeze_ready.py
  - revision/requirements-pinned.txt
  - revision/checkpoints/best_checkpoint.pt
  - .gitignore
  - revision/docs/reconciliation_note.md
  - revision/docs/methods_full.md
  - revision/docs/training_protocol.md
  - revision/docs/paper_blocks_framing.md
  - revision/docs/reviewer_response.md
  - revision/docs/completeness_sweep_manifest.md
  - revision/docs/peer_review_remediation.md
  - revision/results/model_info.json
  - revision/results/matched2000_dualscale.json
  - revision/results/methods_full.json
  - revision/results/figures/cross_model_emd.png
  - revision/results/figures/cross_model_emd.pdf
  - revision/results/figures/cross_model_emd.json
  - revision/results/figures/param_efficiency_pareto.json
  - revision/results/figures/failure_modes_summary.json
  - revision/results/figures/seed_variance_per_model.json
  - revision/results/figures/training_convergence_all_models.json
  - revision/results/figures/matched2000_dualscale_sidebyside.json
  - revision/results/figures/timeseries_default_75.png
  - revision/results/figures/timeseries_iqp_sel_55.png
  - revision/results/figures/timeseries_V1.png
  - revision/results/figures/timeseries_V2.png
  - revision/results/figures/timeseries_V3.png
  - revision/results/figures/timeseries_wgan_mlp.png
  - revision/results/figures/timeseries_wgan_cnn.png
  - revision/results/figures/timeseries_wgan_lstm.png
  - revision/results/figures/timeseries_vae.png
autonomous: true
requirements: [PAPER-01, PAPER-02, PAPER-03, PAPER-08, PAPER-09, INFRA-03]
must_haves:
  truths:
    - "A 5-agent peer-review pass (code-quality, math/statistics, quantum-circuit, provenance/numerical-claims, methods/reproducibility) on the completed Phase 14 work surfaced 12 CRITICAL + 16 HIGH findings; this plan closes 27 of the 28 (HI-9 explicitly OUT OF SCOPE per the reviewer's own annotation as perf-only with no correctness impact); the core science (WGAN-GP loss, gradient penalty, parameter counts, EMD via `scipy.stats.wasserstein_distance`, seed independence, quantum circuit correctness, classical architecture extraction) remains sound — fixes are presentation-layer corrections, additive disclosures, and gate/aggregator hardening with NO retraining"
    - "D-14-16 (gate byte-freeze) LIFTED for Task 2 only; the gate is rewritten to v2 with (a) exact-token boundary match `re.search(rf\"(?<![\\d.]){re.escape(token)}(?![\\d])\", blob)` eliminating substring coincidence (e.g. `0.6843` in `0.03006843578…`), (b) tighter float precision `abs(cval - val) <= 10**(-prec) / 2` instead of `f\"{cval:.{prec}f}\" == f\"{val:.{prec}f}\"`, (c) exclusion of `render_only: true` JSONs from the resolution corpus, (d) narrower `_ID_PATTERNS` (year strip `\\b(?:19|20)\\d{2}\\b(?=\\s*\\))`; file:line strip requires a file-extension prefix), (e) a new `--manifest` flag printing the resolved JSON path + key for each PASSing literal; schema string bumped to `\"v2 (Phase 14 plan 14-13 — boundary-strict resolution + render-only exclusion)\"`"
    - "D-14-22 (`revision/core/` byte-freeze) PRESERVED across all 7 tasks; M-2 (AR `ddof=p` vs `ddof=0` convention), M-3 (`compute_moments` ddof / Fisher kurtosis / biased ACF), MD-1 (stale `real_log_returns` reference in `training.py:418`), MD-7 (`mps.is_available` monkey-patch threadsafety), and LO-1 (bare `assert` in `quantum.py:87`) are DOCUMENTED in `methods_full.md` Task 6 rather than CHANGED; `git diff --stat revision/core/` returns empty at every task close"
    - "D-14-13 (strict-accept gate) EXTENDED to include a `training_time_device` equality check across all matched-budget models (forward-only); historical asymmetry — that the matched-2000ep classical runs reported in this manuscript executed on Apple-Silicon MPS at float32 while quantum runs executed on CPU at float64 via the `_train_quantum` MPS-disable hook — is disclosed in narrative (`methods_full.md` + `reviewer_response.md`, Task 5) rather than retroactively corrected via a full classical sweep re-run (user opted for disclose + future-gate, sparing ~3–6 h re-run wall time)"
    - "CR-4 handling is split per the user's locked decision: the future-gate (the MPS-disable hook applied symmetrically to `_train_wgan` and `_train_vae` + the `training_time_device` strict-accept equality check) lands in Task 4; the historical-asymmetry disclosure paragraph lands in Task 5 in both `methods_full.md` and `reviewer_response.md`. There is NO classical sweep re-run."
    - "`best_checkpoint.pt` (~6 MB) is committed directly via a `!revision/checkpoints/best_checkpoint.pt` exception in `.gitignore` (Task 1); NOT via git-lfs, NOT via Zenodo-dependent deferral; its sha256 already matches `canonical_config_lock.json#checkpoint_sha256` at `f7cceb52…`; the `.gitignore` exception line is the first intentional `.gitignore` edit and must be applied at the executor side, NOT stashed across the merge"
    - "`revision/requirements-pinned.txt` (NEW, Task 1) uses `==` pins (NOT `≥`) for every package recorded in `revision/results/framework_versions.json` (pennylane==0.43.0, torch==2.9.0, numpy==2.3.4, scipy==1.16.2, matplotlib==3.10.7, PyYAML==6.0.3, plus an explicit python-version line); `methods_full.md §4.1` references the pinned file as the resubmission-canonical environment"
    - "`methods_full.md §5.1` no longer claims `bit-identical training trajectories`; the wording is softened to `trajectories agree to ~1e-6 EMD on the same CPU+BLAS+pinned-pip-freeze stack; bit-determinism would require torch.use_deterministic_algorithms(True) which is not set` (METHODS-HIGH-1 resolved; D-14-22 honored — no `revision/core/training.py` edit)"
    - "After Task 3, `reconciliation_note.md` reads its NEW column from the audited OD-scale aggregate mean (`matched2000_dualscale.json#aggregates[*, metric_name='emd', scale='OD']`) instead of `metrics.json[\"emd_avg\"][-1]` (the log-return-standardized training-loop metric); deltas are ≈ 0 for every model (iqp_sel_55_repro: -0.00006, wgan_mlp: -0.00163, wgan_cnn: -0.05871, wgan_lstm: -0.00104); the narrative inverts from the previous `+0.127 degradation` framing to `the matched 2000-epoch budget recovers OD-scale EMD within seed variance of the 1000-epoch baseline`; C-1 / PROV-CRIT-1 resolved"
    - "After Task 3, `reconciliation_note.md` carries an appended metric-redefinition disclosure paragraph explicitly stating the v1.0 `revision/core/eval.py:25-36` switch from histogram-density Wasserstein to raw-sample Wasserstein; the old `0.0015` headline and the new `0.121` log-return EMD are explicitly marked NOT commensurate; C-3 resolved"
    - "After Task 3, `cross_model_emd.png/pdf/json` is re-rendered on OD scale (NOT log-return) sourcing per-seed EMD from `matched2000_dualscale.json#aggregates`, taking MEAN over seeds 42-46 (NOT `np.min` over the 201-point trajectory), with the frozen headline reference line on the SAME OD scale, and the `best EMD` framing dropped; companion JSON caption is `OD scale, final-eval mean ± std over 5 seeds`; CR-2 / C-2 resolved"
    - "After Task 3, every figure that aggregates per-seed metrics switches from population std (ddof=0) to sample std (ddof=1) — `revision/run_matched2000_dualscale.py:522` (`statistics.pstdev` → `statistics.stdev`); `revision/run_figure_suite.py:634, 1129, 1892` (`np.std(...)` → `np.std(..., ddof=1)`); error bars widen by ~12% (factor √(5/4)); cross_model_emd (already redone), param_efficiency_pareto, failure_modes_summary, seed_variance_per_model, and matched2000_dualscale_sidebyside are re-rendered against the updated aggregates; H-2 resolved"
    - "After Task 3, `methods_full.md §2.k` (Pareto narrative) carries the explicit critic-included parameter count `250881` plus a `total adversarial parameter budget` supplementary subsection comparing generator-only and generator+critic counts; `param_efficiency_pareto` caption (companion JSON) discloses that the x-axis is generator-only with the shared critic ~250k applying to all adversarial models alike; H-3 resolved"
    - "After Task 3, `matched2000_dualscale.json#aggregates` has the `n` field populated per row (`5` for matched-budget aggregates; `1` for the headline) — replacing the previously-null values flagged by MED-4"
    - "After Task 4, `revision/run_methods_full.py:466-468` reads `generator_to_compute_dtype` and `mps_dtype_block` line numbers via the existing programmatic `_first_lineno` / `_citations` pattern instead of the hardcoded `training.py:347` and `training.py:259-268` literals; CR-3 resolved and the anti-drift contract is no longer defeated by hand-typed line numbers"
    - "After Task 4, `revision/run_model_info.py` (a) sets `optimizer_betas = None` for non-WGAN families (VAE, AR) instead of the hardcoded `[0.0, 0.9]` (HI-2), (b) declares `EXPECTED_DATA_HASH = \"91e447d4624e25b3\"` and raises explicitly on inequality (not just mutual-equality) at the cross-artifact gate (HI-3 / PROV-HIGH-2), (c) renames `model_info.json#models[].dtype` → `dtype_samples` and adds `dtype_params` alongside; `training_protocol.md` row 34 is split into `dtype_params` / `dtype_samples` rows OR references `methods_full.md §4.b` (PROV-HIGH-3 / HIGH-3)"
    - "After Task 4, `revision/run_matched2000.py` (a) reads `topology` from the lock dict at line 344 instead of hardcoding `\"range\"` (HI-4), (b) patches `_train_wgan` and `_train_vae` with the same `torch.backends.mps.is_available = lambda: False` hook `_train_quantum` already uses (CR-4 future-gate part — ensures all FUTURE matched-budget runs are CPU-only), (c) adds `np.random.seed(seed)` and `random.seed(seed)` to `_train_vae` top mirroring `train_wgan_gp` (HI-7), (d) records `training_time_device` (NOT just `sample_generation_device`) in `_device_manifest` by inspecting the generator/critic AFTER training, and (e) extends `_strict_accept` to require `training_time_device` equality across all models in a sweep (D-14-13 extension)"
    - "After Task 4, `revision/run_matched2000.py` and `revision/run_figure_suite.py:191` replace `default=float` in `json.dumps` with a `_finite_sanitize` helper that converts non-finite floats to `None` and emits an explicit-raise if too many metrics are non-finite (HI-8); `revision/run_matched2000_dualscale.py` includes `HEADLINE_MODEL_KIND` in the `model_kinds` list with a `headline_model_kind` top-level documentation field (HI-5); `revision/run_canonical_headline.py` threads `generation_seed` through instead of the hardcoded `42*31` DTW seed (HI-1)"
    - "After Task 4, `revision/run_circuit_diagrams.py`, `revision/run_classical_arch_extract.py`, `revision/run_framework_versions.py` each emit `\"data_hash\": \"91e447d4624e25b3\"` in their JSON outputs, and the figure-companion-JSON schema in `run_figure_suite.py` is extended to include `data_hash` (PROV-HIGH-2 / HIGH-2 — `data_hash` corpus consistency)"
    - "After Task 4, `revision/run_figure_suite.py` (a) corrects the `training_convergence_all_models` axis label from `OD scale` to `EMD (in-loop training metric, log-return-standardized scale)` reflecting what `emd_avg` actually is (H-1), and (b) replaces the hardcoded `head_epoch != 1969` assertion at line 1117 with `head_epoch != lock[\"checkpoint_epoch\"]` reading the lock JSON once at module init (MD-3)"
    - "After Task 5, `revision/run_figure_suite.py:382` replaces the non-deterministic `np.random.default_rng(model.__hash__() & 0xFFFF)` (Python's randomized string hash) with `np.random.default_rng(int(hashlib.sha256(model.encode()).hexdigest()[:8], 16))` (CR-1); all 9 `timeseries_<model>.png/pdf/json` are re-rendered with stable `real_window_idx` / `fake_window_idx` across invocations (verifiable by re-emit + diff)"
    - "After Task 5, `revision/docs/paper_blocks_framing.md` is corrected: line 119 BEFORE quotation no longer contains the phantom `0.6843` (the AFTER block already supersedes the original sentence; the BEFORE block now quotes the manuscript WITHOUT the phantom number), and the misleading provenance footer at :520 that admitted the substring coincidence is removed (PROV-CRIT-2)"
    - "After Task 5, `revision/verify_freeze_ready.py:82, 116` use `RESULTS_DIR.rglob(\"*.json\")` instead of `RESULTS_DIR.glob(\"*.json\")` (HI-6, matching the verifier's recursive walk) and the negation-self-heal writes `!revision/results/**/*.json` when needed"
    - "After Task 5, both `methods_full.md` and `reviewer_response.md` carry a one-paragraph HONEST DISCLOSURE of the historical CR-4 MPS asymmetry: `The matched-2000ep classical runs reported in this manuscript executed on Apple-Silicon MPS at float32 precision (the runtime default), while the quantum runs executed on CPU at float64 (the _train_quantum MPS-disable hook). This asymmetry was discovered post-execution; future runs invoke the MPS-disable hook in all training paths, and the strict-accept gate now records training_time_device. Numerical impact: MPS-float32 vs CPU-float64 on these small (74–250k param) classical generators is empirically within seed variance, but the asymmetry is disclosed here for completeness.`"
    - "After Task 6, `methods_full.md` gains a new `§3.x — Metric conventions` subsection documenting (a) `compute_moments` uses `np.std(..., ddof=0)` (population std), Fisher excess kurtosis via `scipy.stats.kurtosis(bias=True)`, sample skew via `scipy.stats.skew(bias=True)` — cite `revision/core/eval.py:42-58` + v1.0 notebook-parity rationale; (b) `compute_acf` uses `statsmodels.tsa.stattools.acf(s, nlags=20, fft=True)` (biased divisor `n`); (c) AR(p) `sigma^2` estimator uses `resid.var(ddof=0)` (ML estimator, biased by `(n-p)/n`, ~0.26% downward bias for n=777, p=2) — cite `revision/core/models/nonadversarial.py:157`; (d) the actual VAE ELBO uses per-element-mean MSE + per-element-mean KLD with β=1 which is equivalent to a re-weighted ELBO with implicit β ≈ 0.4 vs the canonical formulation — cite `revision/run_baselines.py:315`; the actual loss expression is added next to the canonical LaTeX in `methods_full.md §2.i` and §2.j (AR) is annotated with the convention notes (M-2, M-3, M-4 documented — D-14-22 honored)"
    - "After Task 7, every paper-facing JSON downstream of Task 3-4 changes is re-emitted (`model_info.json`, `matched2000_dualscale.json`, `methods_full.json`, `training_protocol.md`, `reconciliation_note.md`, `methods_full.md`, all figure companion JSONs); the v2 gate runs on all 9 paper-facing docs (`paper_blocks_framing.md`, `paper_blocks_refs_methods.md`, `reviewer_response.md`, `reconciliation_note.md`, `methods_full.md`, `circuit_atlas.md`, `completeness_sweep_manifest.md`, `training_protocol.md`, `dataset_stats.md`) and ALL PASS with the tightened resolution — any new failure becomes a follow-up data fix in this same plan, NOT a gate weakening"
    - "After Task 7, `revision/docs/completeness_sweep_manifest.md` is updated to add the 14-13 deliverables (one new section per task tag); `revision/docs/peer_review_remediation.md` is created as a NEW reviewer-facing index mapping each of the 12 CRITICAL + 16 HIGH findings to the commit SHA(s) that resolved it, with file:line citations from the 5 peer-review reports; HI-9 is explicitly marked OUT OF SCOPE with rationale"
    - "Files explicitly NOT modified by this plan: `revision/core/` (D-14-22 byte-freeze); `revision/core/training.py`, `revision/core/eval.py`, `revision/core/models/*.py`, `revision/core/__init__.py` (all under the byte-freeze); the read-only Overleaf-canonical `main (4) copy.tex` / `supp_material.tex` (D-14-18); `revision/docs/circuit_atlas.md` (owned by 14-09, byte-stable); `revision/docs/paper_blocks_refs_methods.md` (owned by 14-12, byte-stable — Task 5's paper-blocks cleanup is paper_blocks_framing.md only); `revision/docs/dataset_stats.md` (gate target only, not edited)"
    - "After Task 7, the only outstanding Phase 14 plan is 14-07 (Zenodo deposit + tag + DOI wiring + release.md, deferred manual gate); Phase 14 plan inventory shows incomplete = [14-07]; ROADMAP progress row `14. Paper Revision & Release Freeze | v2.0 | 11/13 | In Progress` after this plan commits"
  artifacts:
    - path: "revision/requirements-pinned.txt"
      provides: "NEW pinned-environment file (Task 1). Every package recorded in `revision/results/framework_versions.json` uses `==` pins (pennylane==0.43.0, torch==2.9.0, numpy==2.3.4, scipy==1.16.2, matplotlib==3.10.7, PyYAML==6.0.3) plus an explicit python-version line. Referenced from `methods_full.md §4.1` as the resubmission-canonical environment. METHODS-BLOCKER-1 resolved."
    - path: ".gitignore"
      provides: "Adds `!revision/checkpoints/best_checkpoint.pt` exception line (Task 1) so the byte-frozen headline checkpoint is tracked in-repo (~6 MB). This is the first intentional `.gitignore` edit in this session — must be applied at the executor side, NOT stashed across the merge. The rest of the pre-existing dirty `.gitignore` modifications (the user's unrelated local edits) stay on the stash."
    - path: "revision/checkpoints/best_checkpoint.pt"
      provides: "NEW tracked binary (Task 1, ~6 MB). The headline checkpoint identified by `canonical_config_lock.json#checkpoint_sha256` at `f7cceb52…`. METHODS-BLOCKER-2 resolved. Reviewers can checkout the repo and rerun the headline-evaluation pipeline without external downloads."
      contains: "<bytes; sha256 matches canonical_config_lock.json#checkpoint_sha256>"
    - path: "revision/verify_number_provenance.py"
      provides: "Provenance gate v2 (Task 2; D-14-16 LIFTED for this plan only). End-to-end rewrite: (a) exact-token boundary match replaces substring `if token in blob`; (b) tighter float precision `abs(cval - val) <= 10**(-prec) / 2`; (c) render-only JSON exclusion from resolution corpus (but render-only docs can still be GATED); (d) narrower `_ID_PATTERNS`; (e) new `--manifest` flag printing resolved JSON path + key per PASSing literal. Schema string bumped to `\"v2 (Phase 14 plan 14-13 — boundary-strict resolution + render-only exclusion)\"`. CR-5, PROV-HIGH-1, PROV-MED-1, PROV-MED-2, PROV-MED-3 resolved."
      min_lines: 200
    - path: "revision/docs/reconciliation_note.md"
      provides: "Re-emitted by `revision/run_model_info.py` (Task 3) reading NEW column from `matched2000_dualscale.json#aggregates[*, metric_name='emd', scale='OD']` (NOT `metrics.json[\"emd_avg\"][-1]`); deltas ≈ 0 per model; narrative inverts from `+0.127 degradation` to `matched 2000-epoch budget recovers OD-scale EMD within seed variance`. Appended metric-redefinition disclosure paragraph documents the v1.0 `revision/core/eval.py:25-36` switch from histogram-density Wasserstein to raw-sample Wasserstein. C-1 / PROV-CRIT-1 / C-3 resolved."
    - path: "revision/results/figures/cross_model_emd.{png,pdf,json}"
      provides: "Re-rendered by `revision/run_figure_suite.py` (Task 3) on OD scale (NOT log-return), source = `matched2000_dualscale.json#aggregates`, MEAN over seeds 42-46 (NOT `np.min` over trajectory), frozen headline reference line on SAME OD scale, `best EMD` framing dropped. Companion JSON caption: `OD scale, final-eval mean ± std over 5 seeds`. CR-2 / C-2 resolved."
    - path: "revision/results/matched2000_dualscale.json"
      provides: "Re-emitted by `revision/run_matched2000_dualscale.py` (Task 3 + Task 4) after `statistics.pstdev` → `statistics.stdev` switch at line 522, with `HEADLINE_MODEL_KIND` included in `model_kinds`, `headline_model_kind` top-level field added, and `n` field populated per aggregate row (`5` for matched-budget, `1` for headline). H-2 / HI-5 / MED-4 resolved."
    - path: "revision/results/model_info.json"
      provides: "Re-emitted by `revision/run_model_info.py` (Task 4) with (a) `optimizer_betas = None` for non-WGAN families (HI-2), (b) `EXPECTED_DATA_HASH = \"91e447d4624e25b3\"` with explicit-raise inequality check (HI-3 / PROV-HIGH-2), (c) `dtype` field renamed to `dtype_samples` with `dtype_params` alongside (PROV-HIGH-3 / HIGH-3)."
    - path: "revision/docs/training_protocol.md"
      provides: "Re-emitted by `revision/run_model_info.py` (Task 4) with row 34 split into `dtype_params` / `dtype_samples` rows OR referencing `methods_full.md §4.b`. PROV-HIGH-3 / HIGH-3 resolved."
    - path: "revision/results/methods_full.json"
      provides: "Re-emitted by `revision/run_methods_full.py` (Task 4) after the hardcoded `training.py:347` and `training.py:259-268` line literals are replaced with programmatic `_first_lineno` citations using new `_citations[\"generator_to_compute_dtype\"]` and `_citations[\"mps_dtype_block\"]` patterns. CR-3 resolved."
    - path: "revision/docs/methods_full.md"
      provides: "Multiple edits across Tasks 3, 4, 5, 6: §2.k (Pareto narrative + 250881 critic-included param count + total-adversarial-budget supplementary subsection, Task 3 / H-3); §4.1 references `requirements-pinned.txt` (Task 1); §5.1 softened wording (Task 1 / METHODS-HIGH-1); CR-4 historical-asymmetry disclosure paragraph (Task 5); new §3.x — Metric conventions subsection (Task 6 / M-2, M-3, M-4) documenting compute_moments ddof / Fisher kurtosis / biased ACF / AR sigma² ddof / VAE β=1 ELBO; §2.i (VAE) and §2.j (AR) annotated with convention notes."
    - path: "revision/docs/paper_blocks_framing.md"
      provides: "Surgical edits (Task 5): line 119 BEFORE quotation no longer contains phantom `0.6843`; provenance footer at :520 admitting the substring coincidence is removed. PROV-CRIT-2 doc-side resolution (gate-side fix is Task 2)."
    - path: "revision/docs/reviewer_response.md"
      provides: "Appended one-paragraph CR-4 historical-asymmetry disclosure (Task 5) explicitly stating the MPS-float32 vs CPU-float64 asymmetry, its post-execution discovery, the future-gate fix, and the empirical-within-seed-variance numerical impact. CR-4 disclosure portion."
    - path: "revision/results/figures/timeseries_{default_75,iqp_sel_55,V1,V2,V3,wgan_mlp,wgan_cnn,wgan_lstm,vae}.{png,pdf,json}"
      provides: "All 9 timeseries figures re-rendered by `revision/run_figure_suite.py` (Task 5) after `np.random.default_rng(model.__hash__() & 0xFFFF)` → `np.random.default_rng(int(hashlib.sha256(model.encode()).hexdigest()[:8], 16))` replacement; companion JSONs now have stable `real_window_idx` / `fake_window_idx` across re-renders. CR-1 resolved."
    - path: "revision/verify_freeze_ready.py"
      provides: "Surgical edits (Task 5): lines 82, 116 switch `RESULTS_DIR.glob(\"*.json\")` → `RESULTS_DIR.rglob(\"*.json\")` matching the verifier's recursive walk; negation-self-heal writes `!revision/results/**/*.json`. HI-6 resolved."
    - path: "revision/docs/completeness_sweep_manifest.md"
      provides: "Updated (Task 7) with a new `## Plan 14-13 — Peer-review remediation sweep` section enumerating the 7 task-tag commits + their resolved findings + the updated/created artifacts; preserves the existing 14-09 / 14-10 / 14-11 / 14-12 sections verbatim."
    - path: "revision/docs/peer_review_remediation.md"
      provides: "NEW reviewer-facing index (Task 7) mapping each of the 12 CRITICAL + 16 HIGH findings to the commit SHA(s) that resolved it, with file:line citations from the 5 peer-review reports (`code-review.md`, `math-review.md`, `quantum-review.md`, `provenance-review.md`, `methods-reproducibility-review.md`). HI-9 explicitly marked OUT OF SCOPE with the reviewer's own perf-only rationale. M-2, M-3, MD-1, MD-7, LO-1 explicitly marked DOCUMENTED-NOT-CHANGED per D-14-22."
      min_lines: 100
  key_links:
    - from: "revision/verify_number_provenance.py (gate v2)"
      to: "all 9 paper-facing docs"
      via: "Task 7 runs the v2 gate against `paper_blocks_framing.md`, `paper_blocks_refs_methods.md`, `reviewer_response.md`, `reconciliation_note.md`, `methods_full.md`, `circuit_atlas.md`, `completeness_sweep_manifest.md`, `training_protocol.md`, `dataset_stats.md`; ALL must PASS with the tightened resolution; any new failure becomes a follow-up data fix in this same plan, NOT a gate weakening"
      pattern: "verify_number_provenance.py --target|v2 \\(Phase 14 plan 14-13"
    - from: "revision/run_model_info.py (Task 3 NEW-column source switch)"
      to: "revision/results/matched2000_dualscale.json#aggregates[*, metric_name='emd', scale='OD']"
      via: "the reconciliation NEW column is read from the audited OD-scale aggregate mean instead of `metrics.json[\"emd_avg\"][-1]` (the log-return-standardized training-loop metric); deltas ≈ 0 per model; narrative inverts"
      pattern: "matched2000_dualscale.json#aggregates|OD scale|emd_avg"
    - from: "revision/run_figure_suite.py (cross_model_emd render, Task 3)"
      to: "revision/results/matched2000_dualscale.json#aggregates (OD scale, mean over seeds)"
      via: "cross_model_emd is re-rendered on OD scale sourcing per-seed EMD from the audited aggregates, MEAN over seeds (NOT min over trajectory), frozen headline reference line on SAME OD scale, `best EMD` framing dropped"
      pattern: "cross_model_emd|OD scale, final-eval mean"
    - from: "revision/run_matched2000.py:_strict_accept (Task 4 D-14-13 extension)"
      to: "training_time_device equality across all matched-budget models"
      via: "`_train_wgan` and `_train_vae` get the same `torch.backends.mps.is_available = lambda: False` hook `_train_quantum` already uses; `training_time_device` is recorded in `_device_manifest` by inspecting generator/critic AFTER training; `_strict_accept` requires equality across all models in a sweep (future-gate)"
      pattern: "training_time_device|mps.is_available = lambda|_strict_accept"
    - from: "revision/run_figure_suite.py:382 (Task 5 CR-1 fix)"
      to: "stable real_window_idx / fake_window_idx across re-renders"
      via: "`np.random.default_rng(model.__hash__() & 0xFFFF)` (Python's randomized string hash) → `np.random.default_rng(int(hashlib.sha256(model.encode()).hexdigest()[:8], 16))`; verifiable by re-emit + diff"
      pattern: "hashlib.sha256\\(model.encode|real_window_idx"
    - from: "revision/docs/methods_full.md (Task 5 CR-4 disclosure + Task 6 §3.x)"
      to: "revision/docs/reviewer_response.md (Task 5 CR-4 disclosure)"
      via: "both docs carry the same one-paragraph CR-4 historical-asymmetry disclosure; methods_full.md additionally gains §3.x Metric conventions documenting M-2 / M-3 / M-4 (D-14-22 honored — documented, not changed)"
      pattern: "matched-2000ep classical runs|MPS at float32|training_time_device|Metric conventions"
    - from: "revision/docs/peer_review_remediation.md (Task 7 NEW)"
      to: "12 CRITICAL + 16 HIGH findings from the 5 peer-review reports"
      via: "every finding maps to at least one commit SHA from this plan; HI-9 explicitly marked OUT OF SCOPE; M-2/M-3/MD-1/MD-7/LO-1 explicitly marked DOCUMENTED-NOT-CHANGED per D-14-22"
      pattern: "CR-1|CR-2|CR-3|CR-4|CR-5|C-1|C-2|C-3|PROV-CRIT-1|PROV-CRIT-2|METHODS-BLOCKER-1|METHODS-BLOCKER-2|HI-[0-9]+|H-[0-9]+|PROV-HIGH-[0-9]+|PROV-MED-[0-9]+|METHODS-HIGH-1"
    - from: ".gitignore (Task 1)"
      to: "revision/checkpoints/best_checkpoint.pt (NEW tracked binary)"
      via: "`!revision/checkpoints/best_checkpoint.pt` exception line tracks the ~6 MB headline checkpoint directly (NOT git-lfs, NOT Zenodo-dependent); sha256 already matches `canonical_config_lock.json#checkpoint_sha256` at `f7cceb52…`"
      pattern: "!revision/checkpoints/best_checkpoint.pt|f7cceb52"
---

<objective>
Close 27 of the 28 actionable findings (12 CRITICAL + 16 HIGH minus HI-9 perf-only)
surfaced by the 5-agent peer-review pass on the completed Phase 14 work. Tighten
the provenance gate to v2 (lifting D-14-16 for this plan only); rebuild the
scale-correct reconciliation table and cross-model EMD figure; switch every
seed-aggregate from population std to sample std and re-render every dependent
figure; harden 9 aggregator emitters for integrity (programmatic line citations,
optimizer_betas family-specific, data_hash explicit-raise gate, dtype split,
MPS-disable future-gate for `_train_wgan`/`_train_vae` with `training_time_device`
strict-accept, finite sanitization, headline-model-kind inclusion, axis label
correctness, lock-driven head-epoch); make timeseries renders deterministic
across invocations; ship pinned-environment + tracked-checkpoint reproducibility;
disclose the historical CR-4 MPS asymmetry honestly while future-gating it;
document the math reviewer's small statistical convention findings in
`methods_full.md §3.x` (honoring D-14-22 — documented, not changed); re-emit
every paper-facing JSON downstream of the data fixes; verify the v2 gate passes
across all 9 paper-facing docs; emit a reviewer-facing remediation index. NO
retraining; NO classical sweep re-run; NO new figures beyond the re-renders;
`revision/core/` byte-frozen throughout (D-14-22).

Purpose: 14-09 / 14-10 / 14-11 / 14-12 landed substantial new artifacts and
integration but a fresh 5-agent peer-review pass surfaced 28 actionable findings
(12 CRITICAL + 16 HIGH; HI-9 explicitly OOS). Three of the CRITICAL findings
were independently triangulated by three reviewers (the cross_model_emd
scale-mix, the reconciliation_note scale-collision, and the provenance-gate
substring/float false positives). The core science is sound — these are
presentation-layer corrections, additive disclosures, and gate/aggregator
hardening. After this plan completes, only 14-07 (Zenodo deposit + tag + DOI
wiring + release.md) remains in Phase 14.

Output: 1 gate-v2 rewrite + 9 hardened aggregator/emitter scripts + 9 re-emitted
JSONs + 9 re-rendered timeseries figures + 5 re-rendered seed-aggregated
figures + 7 edited/new docs (`reconciliation_note.md`, `methods_full.md`,
`training_protocol.md`, `paper_blocks_framing.md`, `reviewer_response.md`,
`completeness_sweep_manifest.md`, `peer_review_remediation.md`) + 1 new pinned
requirements file + 1 newly tracked checkpoint binary + 1 `.gitignore` exception
line. All gated by the v2 provenance gate on all 9 paper-facing docs;
`revision/core/` byte-frozen (D-14-22); no retraining.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-paper-revision-release-freeze/14-CONTEXT.md
@.planning/phases/14-paper-revision-release-freeze/14-PATTERNS.md
@.planning/phases/14-paper-revision-release-freeze/14-08-PLAN.md
@.planning/phases/14-paper-revision-release-freeze/14-09-PLAN.md
@.planning/phases/14-paper-revision-release-freeze/14-10-PLAN.md
@.planning/phases/14-paper-revision-release-freeze/14-11-PLAN.md
@.planning/phases/14-paper-revision-release-freeze/14-12-PLAN.md
@.planning/phases/14-paper-revision-release-freeze/peer-review/code-review.md
@.planning/phases/14-paper-revision-release-freeze/peer-review/math-review.md
@.planning/phases/14-paper-revision-release-freeze/peer-review/quantum-review.md
@.planning/phases/14-paper-revision-release-freeze/peer-review/provenance-review.md
@.planning/phases/14-paper-revision-release-freeze/peer-review/methods-reproducibility-review.md

<contract_changes>
Explicit deviations from prior Phase 14 contracts:

- **D-14-16 (gate byte-freeze) LIFTED for Task 2 only.** The gate is rewritten
  end-to-end to v2: exact-token boundary match, float resolution within
  `±10⁻ᵖ/2`, exclusion of `render_only: true` companions from the resolution
  corpus, narrower `_ID_PATTERNS`, and a new `--manifest` output. Rationale:
  4 CRITICAL/HIGH findings (CR-5, PROV-HIGH-1, PROV-MED-1/2/3 + the root cause
  of PROV-CRIT-2's substring coincidence) cannot be fixed any other way. After
  Task 2 the gate is back in byte-freeze; the schema string change documents
  the bump.

- **D-14-22 (`revision/core/` byte-freeze) PRESERVED.** M-2 (AR `ddof=p` vs
  `ddof=0` convention) and M-3 (`compute_moments` ddof / Fisher kurtosis /
  biased ACF) are DOCUMENTED in `methods_full.md §3.x Metric conventions`
  rather than CHANGED. MD-1 (stale `real_log_returns` reference in
  `training.py:418`), MD-7 (`mps.is_available` monkey-patch threadsafety), and
  LO-1 (bare `assert` in `quantum.py:87`) are likewise DOCUMENTED, not patched.

- **D-14-13 (strict-accept gate) EXTENDED** to include a `training_time_device`
  equality check across all matched-budget models (forward-only). Historical
  asymmetry — the matched-2000ep classical runs reported in this manuscript
  executed on Apple-Silicon MPS at float32 while quantum runs executed on CPU
  at float64 via the `_train_quantum` MPS-disable hook — is DISCLOSED in
  narrative (Task 5) rather than retroactively corrected via a full classical
  sweep re-run (user opted for disclose + future-gate per the locked decision).
</contract_changes>

<interfaces>
<!-- All upstream artifacts (Phases 9-13 + Phase 14 plans 01-12) are
     EXISTING and AUDITED on disk; this plan only modifies emitters,
     re-emits JSONs, re-renders figures, and edits/adds docs. -->

revision/verify_number_provenance.py (the gate being rewritten in Task 2):
- Current contract: rglobs `revision/results/*.json`; strips identifier-like
  tokens (D-14-NN, R1-MN, plan IDs, source line citations file:NNN); accepts
  substring match `if token in blob` (line 119) and float-precision-equality
  `f"{cval:.{prec}f}" == f"{val:.{prec}f}"` (the false-positive sources for
  CR-5 / PROV-HIGH-1 / PROV-CRIT-2 root cause).
- After Task 2: exact-token boundary regex `re.search(rf"(?<![\d.]){re.escape(token)}(?![\d])", blob)`;
  float ε-neighborhood `abs(cval - val) <= 10**(-prec) / 2`; render-only JSON
  exclusion from resolution corpus; narrower `_ID_PATTERNS` (year strip
  `\b(?:19|20)\d{2}\b(?=\s*\))`; file:line strip requires file-extension
  prefix `(?:\.py|\.md|\.json|\.tex):\d+(?:-\d+)?\b`); new `--manifest` flag
  printing resolved JSON path + key per PASSing literal; schema string bumped
  to `"v2 (Phase 14 plan 14-13 — boundary-strict resolution + render-only exclusion)"`.

revision/run_model_info.py (Task 3 + Task 4):
- Lines 220-302 are the reconciliation NEW-column site; Task 3 switches the
  source from `metrics.json["emd_avg"][-1]` to `matched2000_dualscale.json#aggregates[*, metric_name='emd', scale='OD']`.
- Task 4 sets `optimizer_betas = None` for non-WGAN families (HI-2); declares
  `EXPECTED_DATA_HASH = "91e447d4624e25b3"` with explicit-raise inequality
  check (HI-3 / PROV-HIGH-2); renames `model_info.json#models[].dtype` →
  `dtype_samples` and adds `dtype_params` alongside (PROV-HIGH-3 / HIGH-3);
  re-emits `training_protocol.md` row 34 split or referencing
  `methods_full.md §4.b`.

revision/run_matched2000.py (Task 4):
- Lines 117-127: ansatz dict (V1/V2/V3 entries; topology read from lock at
  line 344 instead of hardcoded `"range"`).
- Lines 337-347, 372-389, 434-492, 503: `_train_quantum`, `_train_wgan`,
  `_train_vae`, `_strict_accept`. Task 4 patches `_train_wgan` and `_train_vae`
  with `torch.backends.mps.is_available = lambda: False` (CR-4 future-gate
  symmetry); adds `np.random.seed(seed)` + `random.seed(seed)` to `_train_vae`
  (HI-7); records `training_time_device` in `_device_manifest` by inspecting
  generator/critic AFTER training; extends `_strict_accept` to require
  `training_time_device` equality across all models in a sweep (D-14-13
  extension). Replaces `default=float` in `json.dumps` with `_finite_sanitize`
  helper (HI-8).

revision/run_matched2000_dualscale.py (Task 3 + Task 4):
- Line 522 (`statistics.pstdev` → `statistics.stdev`, H-2 sample-std switch).
- HEADLINE_MODEL_KIND included in `model_kinds` list (HI-5); `headline_model_kind`
  top-level documentation field added; `n` populated per aggregate row
  (5 for matched-budget, 1 for headline; MED-4).

revision/run_methods_full.py (Task 4):
- Lines 466-468: replace hardcoded `training.py:347` and `training.py:259-268`
  literals with programmatic `_first_lineno` citations using new
  `_citations["generator_to_compute_dtype"]` and `_citations["mps_dtype_block"]`
  patterns (CR-3).

revision/run_figure_suite.py (Task 3 + Task 4 + Task 5):
- Line 191: `default=float` → `_finite_sanitize` (HI-8 mirror).
- Lines 620-654: cross_model_emd render (Task 3 OD-scale + mean-over-seeds rebuild).
- Line 382: `np.random.default_rng(model.__hash__() & 0xFFFF)` →
  `np.random.default_rng(int(hashlib.sha256(model.encode()).hexdigest()[:8], 16))` (CR-1).
- Line 634, 1129, 1892: `np.std(...)` → `np.std(..., ddof=1)` (H-2 ddof switch
  across aggregator sites).
- Line 1117: `head_epoch != 1969` → `head_epoch != lock["checkpoint_epoch"]`
  reading lock JSON once at module init (MD-3).
- Line 1146 (training_convergence_all_models axis label): `OD scale` →
  `EMD (in-loop training metric, log-return-standardized scale)` (H-1).

revision/run_circuit_diagrams.py, revision/run_classical_arch_extract.py,
revision/run_framework_versions.py (Task 4):
- Each emitter extended to record `"data_hash": "91e447d4624e25b3"` in the
  JSONs it produces (HIGH-2 / PROV-HIGH-2 data_hash corpus consistency).

revision/run_canonical_headline.py (Task 4):
- Thread `generation_seed` through instead of the hardcoded `42*31` DTW seed
  (HI-1).

revision/verify_freeze_ready.py (Task 5):
- Lines 82, 116: `RESULTS_DIR.glob("*.json")` → `RESULTS_DIR.rglob("*.json")`
  matching the verifier's recursive walk; negation-self-heal writes
  `!revision/results/**/*.json` (HI-6).

revision/results/canonical_config_lock.json (read-only reference for Tasks 1 + 4):
- `checkpoint_epoch` (drives the MD-3 hardcode replacement at run_figure_suite.py:1117).
- `checkpoint_sha256 = "f7cceb52…"` (drives Task 1 binary commit identity).

revision/docs/methods_full.md (byte-stable from 14-11; THIS PLAN edits it):
- This plan opens the doc for surgical edits in Tasks 1 (§4.1 +
  requirements-pinned.txt reference), 3 (§2.k Pareto narrative + 250881
  critic-included count + total-adversarial-budget subsection), 5 (CR-4
  disclosure paragraph + §5.1 softened wording), and 6 (NEW §3.x Metric
  conventions subsection + §2.i and §2.j convention annotations).

revision/docs/paper_blocks_framing.md (owned by 14-05 / 14-12 integration):
- Task 5 surgical edits ONLY: line 119 BEFORE quotation no longer contains
  phantom `0.6843`; misleading provenance footer at :520 removed. All
  existing AFTER LaTeX blocks, locked-decomposition citations, PAPER-03
  Companion artifact atlas subsection (14-12), and PAPER-01/02b
  param_efficiency_pareto footnote (14-12) are PRESERVED VERBATIM.

revision/docs/reviewer_response.md (owned by 14-06 / 14-12 integration):
- Task 5 appends ONE paragraph: the CR-4 historical-asymmetry disclosure.
  All existing reviewer-table rows, Cross-Cutting Provenance Note, and the
  appended Completeness sweep section from 14-12 are PRESERVED VERBATIM.

revision/docs/completeness_sweep_manifest.md (owned by 14-12):
- Task 7 appends a new `## Plan 14-13 — Peer-review remediation sweep` section
  at the bottom; all existing 14-09 / 14-10 / 14-11 / 14-12 sections are
  PRESERVED VERBATIM.

Peer-review source reports (read-only references; do NOT modify):
- .planning/phases/14-paper-revision-release-freeze/peer-review/code-review.md
- .planning/phases/14-paper-revision-release-freeze/peer-review/math-review.md
- .planning/phases/14-paper-revision-release-freeze/peer-review/quantum-review.md
- .planning/phases/14-paper-revision-release-freeze/peer-review/provenance-review.md
- .planning/phases/14-paper-revision-release-freeze/peer-review/methods-reproducibility-review.md

Critical files to read before each task (per the approved plan):
- revision/verify_number_provenance.py:1-180 — the gate being rewritten in T2
- revision/run_model_info.py:220-302 — the reconciliation NEW-column site (T3)
- revision/run_figure_suite.py:620-654, 382, 1117, 1146 — the figure defects (T3 + T4 + T5)
- revision/run_methods_full.py:466-468 — the hardcoded citations (T4)
- revision/run_matched2000.py:117-127, 337-347, 372-389, 434-492, 503 — ansatz dict, repro path, _train_quantum/_train_wgan/_train_vae (T4)
- revision/results/matched2000_dualscale.json#aggregates — audited OD-scale source for T3's reconciliation rebuild
- revision/results/canonical_config_lock.json#checkpoint_epoch + checkpoint_sha256 — headline checkpoint identity (T1 binary commit + T4 MD-3 lock-driven hardcode replacement)
- The 5 peer-review reports under .planning/phases/14-paper-revision-release-freeze/peer-review/*.md — full file:line citations for each finding
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reproducibility infrastructure — requirements-pinned.txt + tracked best_checkpoint.pt + §5.1 wording softening (3 findings: METHODS-BLOCKER-1, METHODS-BLOCKER-2, METHODS-HIGH-1)</name>
  <files>revision/requirements-pinned.txt, .gitignore, revision/checkpoints/best_checkpoint.pt, revision/docs/methods_full.md</files>
  <read_first>
    - revision/results/framework_versions.json (the audited, currently-installed `qgan_env` version set — every package recorded here will become a `==` pin in `requirements-pinned.txt`)
    - revision/results/canonical_config_lock.json (the `checkpoint_sha256 = "f7cceb52…"` and `checkpoint_epoch` fields; the binary committed under `revision/checkpoints/best_checkpoint.pt` MUST match this sha256)
    - .gitignore (the WHOLE existing file — the new `!revision/checkpoints/best_checkpoint.pt` exception line is the FIRST intentional `.gitignore` edit in this session; the pre-existing dirty modifications on the working tree stay on the stash and are NOT touched by this task)
    - revision/docs/methods_full.md §4 (Hardware & Software) + §5.1 (Reproducibility — the `bit-identical training trajectories` claim being softened) (this doc was byte-stable from 14-11; this task opens it for surgical edits in §4.1 + §5.1 only)
    - .planning/phases/14-paper-revision-release-freeze/peer-review/methods-reproducibility-review.md — section labelled METHODS-BLOCKER-1, METHODS-BLOCKER-2, METHODS-HIGH-1 with file:line citations
    - Locate the existing `best_checkpoint.pt` on disk (it currently sits under the gitignored path used by Phase 14-01); the executor MUST verify the file's sha256 matches `canonical_config_lock.json#checkpoint_sha256` before moving/copying it into `revision/checkpoints/`
  </read_first>
  <action>
    Step A — Create `revision/requirements-pinned.txt`:
    Read `revision/results/framework_versions.json` and produce a one-package-per-line
    file with EXACT `==` pins (METHODS-BLOCKER-1):

    ```
    # Resubmission-canonical environment for Phase 14 manuscript revision.
    # Generated from revision/results/framework_versions.json (Plan 14-11).
    # Pinned via Phase 14 plan 14-13 (Task 1) — methods_full.md §4.1.
    pennylane==0.43.0
    torch==2.9.0
    numpy==2.3.4
    scipy==1.16.2
    matplotlib==3.10.7
    PyYAML==6.0.3
    # python-version recorded for the running interpreter (cross-check at install time).
    # python==<the value from framework_versions.json#python_version>
    ```

    Use the exact version strings from `framework_versions.json` — do NOT
    invent versions; if a package is listed in `framework_versions.json` but
    not in the snippet above, include it in alphabetical order using its
    audited version. Explicitly emit the python-version line using the
    `python_version` field from the JSON.

    Step B — `.gitignore` exception line:
    Append `!revision/checkpoints/best_checkpoint.pt` to `.gitignore`
    (METHODS-BLOCKER-2). Place it at the bottom under a new comment header
    `# Phase 14 plan 14-13: track headline checkpoint in-repo (~6 MB)`. This
    is the FIRST intentional `.gitignore` edit; the pre-existing dirty
    modifications on the working tree stay on the stash and are NOT touched
    by this task.

    Step C — Move/copy `best_checkpoint.pt` into `revision/checkpoints/`:
    Create the directory `revision/checkpoints/` if it does not exist. Locate
    the existing `best_checkpoint.pt` on disk (the one currently sitting under
    the gitignored path from Phase 14-01) and copy it to
    `revision/checkpoints/best_checkpoint.pt`. Verify the copied file's sha256
    via `sha256sum revision/checkpoints/best_checkpoint.pt` matches
    `canonical_config_lock.json#checkpoint_sha256` at `f7cceb52…` (it must;
    this is the already-locked headline checkpoint identity from Plan 14-01).

    Step D — Soften the `bit-identical` claim in `methods_full.md §5.1`
    (METHODS-HIGH-1):
    Open `revision/docs/methods_full.md`. Locate the §5.1 paragraph claiming
    "bit-identical training trajectories" (or the equivalent wording emitted
    by `run_methods_full.py`). Replace with:

    > Trajectories agree to ~1e-6 EMD on the same CPU+BLAS+pinned-pip-freeze
    > stack (`revision/requirements-pinned.txt`); bit-determinism would
    > require `torch.use_deterministic_algorithms(True)` which is not set in
    > the byte-frozen `revision/core/training.py` (D-14-22). The pinned-env
    > + tracked-checkpoint contract (`revision/checkpoints/best_checkpoint.pt`,
    > sha256 = `<the f7cceb52… prefix from canonical_config_lock.json>`)
    > delivers reproducibility-within-numerical-tolerance, not bit-determinism.

    (The preferred alternative — adding `torch.use_deterministic_algorithms(True, warn_only=True)`
    + `os.environ.setdefault("PYTHONHASHSEED", "0")` near the top of
    `train_wgan_gp` — touches `revision/core/training.py` which is byte-frozen
    under D-14-22; default to the wording fix.)

    Step E — Reference `requirements-pinned.txt` from `methods_full.md §4.1`:
    Open the §4.1 (Hardware & Software / Pip-freeze) paragraph. Append one
    sentence at the end:

    > The resubmission-canonical environment is committed at
    > `revision/requirements-pinned.txt` (Phase 14 plan 14-13, Task 1) with
    > exact `==` pins for every package recorded in
    > `revision/results/framework_versions.json`; reviewers can rerun the
    > pipeline against this exact environment with `python -m venv <env> &&
    > pip install -r revision/requirements-pinned.txt`.

    The rest of `methods_full.md` (sections §1, §2, §3, §4.2-§4.N, §5.2+, §6, §7)
    is PRESERVED VERBATIM by Task 1 (Task 3 + Task 5 + Task 6 edit other
    sections downstream).

    Step F — Commit and gate verify:
    Stage all four files (`revision/requirements-pinned.txt`, `.gitignore`,
    `revision/checkpoints/best_checkpoint.pt`, `revision/docs/methods_full.md`).
    Run the existing provenance gate against `methods_full.md` (the gate is
    pre-v2 at this point — Task 2 hasn't lifted yet — so the pre-v2 gate is
    the integration check at Task 1 close). Confirm PASS.

    Commit message: `feat(14-13): repro infrastructure (requirements-pinned + tracked checkpoint + softened §5.1 + §4.1 reference)`.
  </action>
  <verify>
    <automated>./qgan_env/bin/python -c "import pathlib,hashlib,json,sys; req=pathlib.Path('revision/requirements-pinned.txt'); fw=json.loads(pathlib.Path('revision/results/framework_versions.json').read_text()); lock=json.loads(pathlib.Path('revision/results/canonical_config_lock.json').read_text()); ckpt=pathlib.Path('revision/checkpoints/best_checkpoint.pt'); gi=pathlib.Path('.gitignore').read_text(); md=pathlib.Path('revision/docs/methods_full.md').read_text(); errs=[]; (req.exists() and '==' in req.read_text()) or errs.append('requirements-pinned missing or not ==-pinned'); ckpt.exists() or errs.append('best_checkpoint.pt not tracked at revision/checkpoints/'); ckpt.exists() and hashlib.sha256(ckpt.read_bytes()).hexdigest().startswith(lock['checkpoint_sha256'][:8]) or errs.append('checkpoint sha256 mismatch with canonical_config_lock.json'); ('!revision/checkpoints/best_checkpoint.pt' in gi) or errs.append('.gitignore missing !revision/checkpoints/best_checkpoint.pt exception'); ('bit-identical' not in md) or errs.append('methods_full.md still claims bit-identical'); ('revision/requirements-pinned.txt' in md) or errs.append('methods_full.md does not reference requirements-pinned.txt'); print('errors',errs); sys.exit(0 if not errs else 1)" &amp;&amp; ./qgan_env/bin/python revision/verify_number_provenance.py --target revision/docs/methods_full.md &amp;&amp; [ -z "$(git diff --stat revision/core/)" ]</automated>
  </verify>
  <acceptance_criteria>
    - `revision/requirements-pinned.txt` exists, uses `==` pins for every package in `framework_versions.json`, plus an explicit python-version line
    - `revision/checkpoints/best_checkpoint.pt` is tracked (`git ls-files | grep best_checkpoint.pt` returns the path) and its sha256 matches `canonical_config_lock.json#checkpoint_sha256`
    - `.gitignore` contains the `!revision/checkpoints/best_checkpoint.pt` exception line under a `# Phase 14 plan 14-13` header
    - `methods_full.md §5.1` no longer claims `bit-identical training trajectories`; carries the softened wording referencing `requirements-pinned.txt` + `torch.use_deterministic_algorithms(True)` (not set) + the tracked checkpoint
    - `methods_full.md §4.1` references `revision/requirements-pinned.txt` with the `pip install -r` rerun command
    - `methods_full.md` other sections (§1, §2, §3, §4.2+, §5.2+, §6, §7) PRESERVED VERBATIM
    - `git diff --stat revision/core/` is empty (D-14-22 byte-freeze)
    - The pre-v2 provenance gate PASSES on `methods_full.md`
  </acceptance_criteria>
  <done>Pinned requirements file + tracked headline checkpoint binary + `.gitignore` exception + softened §5.1 wording + §4.1 reference are in place; sha256 matches canonical_config_lock.json; methods_full.md other sections preserved verbatim; D-14-22 honored.</done>
</task>

<task type="auto">
  <name>Task 2: Provenance gate hardening to v2 — boundary-strict resolution + render-only exclusion + --manifest output (5 findings: CR-5, PROV-HIGH-1, PROV-MED-1, PROV-MED-2, PROV-MED-3) — LIFTS D-14-16</name>
  <files>revision/verify_number_provenance.py</files>
  <read_first>
    - revision/verify_number_provenance.py (the WHOLE existing file, lines 1-180; this is the gate being rewritten end-to-end under the explicit D-14-16 lift for Task 2 only)
    - .planning/phases/14-paper-revision-release-freeze/peer-review/provenance-review.md — sections labelled CR-5, PROV-HIGH-1, PROV-MED-1, PROV-MED-2, PROV-MED-3
    - .planning/phases/14-paper-revision-release-freeze/peer-review/code-review.md — CR-5 cross-reference
    - revision/docs/paper_blocks_framing.md (the doc carrying the phantom `0.6843` which currently "passes" via substring coincidence; Task 5 fixes the doc-side, this task fixes the gate-side)
    - Existing JSONs under `revision/results/*.json` (the resolution corpus — figure-companion JSONs carry `"render_only": true` flags that Task 2 must exclude from the resolution corpus while still allowing them to be GATED as documents)
    - .planning/phases/14-paper-revision-release-freeze/14-CONTEXT.md — locate D-14-16 (the byte-freeze decision being lifted) and confirm the lift is for THIS PLAN ONLY (after Task 2 closes, the gate is back in byte-freeze under the new schema string `v2 (Phase 14 plan 14-13 — boundary-strict resolution + render-only exclusion)`)
  </read_first>
  <action>
    Rewrite `revision/verify_number_provenance.py` end-to-end with the 5
    targeted contract changes:

    Step A — Exact-token boundary match (CR-5, PROV-CRIT-2 root cause):
    Locate line 119 (or the equivalent substring-acceptance site). Replace
    `if token in blob` with:

    ```python
    import re
    if re.search(rf"(?<![\d.]){re.escape(token)}(?![\d])", blob):
    ```

    The lookbehind `(?<![\d.])` ensures the token is not preceded by a digit
    or decimal point (eliminating `0.6843` matching inside `0.03006843578…`);
    the lookahead `(?![\d])` ensures the token is not followed by a digit
    (eliminating prefix-substring matches). The boundary class is intentionally
    NOT `\b` (word boundary) because `\b` does not catch the digit-after-decimal
    case.

    Step B — Tighter float precision (CR-5, PROV-HIGH-1):
    Locate the float-resolution site (the `f"{cval:.{prec}f}" == f"{val:.{prec}f}"`
    comparison). Replace with a real ε-neighborhood:

    ```python
    if abs(cval - val) <= 10**(-prec) / 2:
    ```

    This is the actual mathematical definition of "matches to `prec` decimal
    places" rather than the formatting-string coincidence (which can match
    e.g. `0.121` against `0.124` if both format-round to `0.12`).

    Step C — Exclude render-only JSONs from resolution corpus (PROV-MED-3):
    In the `_json_blobs()` walker (the function that rglobs
    `revision/results/*.json` and builds the resolution corpus), filter out
    any JSON whose top-level dict carries `"render_only": true`. Render-only
    companions can still be *gated* (i.e. passed to `--target`), just not used
    as a resolution *source*:

    ```python
    def _json_blobs():
        for p in RESULTS_DIR.rglob("*.json"):
            try:
                data = json.loads(p.read_text())
                if isinstance(data, dict) and data.get("render_only") is True:
                    continue  # render-only companions are not resolution sources
                yield p, data
            except Exception:
                continue
    ```

    Step D — Narrow `_ID_PATTERNS` (PROV-MED-1, PROV-MED-2):
    Locate `_ID_PATTERNS`. Restrict the year-strip pattern to:

    ```python
    YEAR_STRIP = re.compile(r"\b(?:19|20)\d{2}\b(?=\s*\))")  # only 1900-2099 with closing-paren lookahead
    ```

    Restrict the file:line-strip pattern to require a file-extension prefix:

    ```python
    FILE_LINE_STRIP = re.compile(r"(?:\.py|\.md|\.json|\.tex):\d+(?:-\d+)?\b")
    ```

    Step E — `--manifest` flag (new output):
    Add a CLI flag `--manifest` (mutually compatible with `--target`). When
    set, for each PASSing literal, print the resolved JSON path + the key path
    inside the JSON that resolved it. Format: `<doc>:<literal> -> <json_path>#<key_path>`.
    This is the resolution-trace artifact reviewers need to spot semantic
    mismatches (e.g. a value that resolves to the WRONG JSON key by
    coincidence).

    Step F — Schema-string bump:
    Locate the `_SCHEMA = "..."` string at the top of the file (or wherever
    the gate's self-identifying version is declared). Change it to:

    ```python
    _SCHEMA = "v2 (Phase 14 plan 14-13 — boundary-strict resolution + render-only exclusion)"
    ```

    Step G — Self-verify and surface new failures:
    Run the v2 gate against all 9 paper-facing docs:

    ```
    paper_blocks_framing.md
    paper_blocks_refs_methods.md
    reviewer_response.md
    reconciliation_note.md
    methods_full.md
    circuit_atlas.md
    completeness_sweep_manifest.md
    training_protocol.md
    dataset_stats.md
    ```

    Document any NEW failures that appear (the gate WILL find new failures
    because it's tighter). DO NOT weaken the gate to make failures disappear —
    those failures are the substantive data problems Tasks 3-5 then fix.
    Surface the new failure list in the commit body of Task 2 so Tasks 3-5
    have the exact reviewer-grade failure manifest to work against.

    Commit message: `feat(14-13): gate v2 (boundary-strict resolution + render-only exclusion + --manifest)`.
  </action>
  <verify>
    <automated>./qgan_env/bin/python -c "import re,pathlib,sys; g=pathlib.Path('revision/verify_number_provenance.py').read_text(); errs=[]; ('v2 (Phase 14 plan 14-13' in g) or errs.append('schema string not v2-bumped'); (re.search(r'\(\?\<\!\[\\\\d\.\]\)', g) or '(?<![\\\\d.])' in g or '(?<![\\d.])' in g) or errs.append('exact-token boundary regex not present'); ('10\\*\\*(-prec) / 2' in g or '10**(-prec) / 2' in g) or errs.append('float epsilon neighborhood not present'); ('render_only' in g) or errs.append('render_only exclusion not present'); ('--manifest' in g) or errs.append('--manifest flag not present'); print('errors',errs); sys.exit(0 if not errs else 1)" &amp;&amp; ./qgan_env/bin/python revision/verify_number_provenance.py --help 2>&amp;1 | grep -q -- '--manifest' &amp;&amp; ./qgan_env/bin/python -c "import sys; sys.path.insert(0,'revision'); import verify_number_provenance as v; assert '0.6843' not in '0.03006843578' or True; import re; blob='emd_avg final 0.03006843578901'; token='0.6843'; assert not re.search(rf'(?<![\\d.]){re.escape(token)}(?![\\d])', blob), 'boundary regex should NOT match substring'; blob2='value 0.6843 reference'; assert re.search(rf'(?<![\\d.]){re.escape(token)}(?![\\d])', blob2), 'boundary regex SHOULD match standalone token'; print('boundary regex sanity OK')" &amp;&amp; [ -z "$(git diff --stat revision/core/)" ]</automated>
  </verify>
  <acceptance_criteria>
    - `revision/verify_number_provenance.py` schema string is `"v2 (Phase 14 plan 14-13 — boundary-strict resolution + render-only exclusion)"`
    - Exact-token boundary regex `(?<![\d.])<token>(?![\d])` replaces the substring `if token in blob`
    - Float resolution uses `abs(cval - val) <= 10**(-prec) / 2` (real ε-neighborhood)
    - `_json_blobs()` excludes JSONs with `"render_only": true` at the top level
    - `_ID_PATTERNS` year-strip is `\b(?:19|20)\d{2}\b(?=\s*\))`; file:line strip requires `(?:\.py|\.md|\.json|\.tex):\d+(?:-\d+)?\b`
    - `--manifest` CLI flag prints resolved JSON path + key path per PASSing literal
    - Running the v2 gate against the 9 paper-facing docs may surface NEW failures (the substring/precision coincidences that previously passed); the failure list is documented in the commit body for Tasks 3-5
    - Boundary-regex unit sanity: `0.6843` does NOT match `0.03006843578` but DOES match the standalone literal `0.6843` in `value 0.6843 reference`
    - `git diff --stat revision/core/` is empty (D-14-22)
  </acceptance_criteria>
  <done>Provenance gate rewritten to v2 with boundary-strict resolution, ε-neighborhood float matching, render-only corpus exclusion, narrower `_ID_PATTERNS`, and `--manifest` flag; schema string bumped; new failures (if any) surfaced in commit body for Tasks 3-5 to address; D-14-16 lift documented; D-14-22 preserved.</done>
</task>

<task type="auto">
  <name>Task 3: Scale-correct reconciliation + cross_model_emd figure rebuild + ddof=0→ddof=1 sample-std switch (5 findings: C-1/PROV-CRIT-1, C-2/CR-2, C-3, H-2, H-3)</name>
  <files>revision/run_model_info.py, revision/run_matched2000_dualscale.py, revision/run_figure_suite.py, revision/docs/reconciliation_note.md, revision/docs/methods_full.md, revision/results/matched2000_dualscale.json, revision/results/figures/cross_model_emd.png, revision/results/figures/cross_model_emd.pdf, revision/results/figures/cross_model_emd.json, revision/results/figures/param_efficiency_pareto.json, revision/results/figures/failure_modes_summary.json, revision/results/figures/seed_variance_per_model.json, revision/results/figures/matched2000_dualscale_sidebyside.json</files>
  <read_first>
    - revision/run_model_info.py:220-302 (the reconciliation NEW-column site; this task replaces the `metrics.json["emd_avg"][-1]` read with the audited OD-scale aggregate mean from `matched2000_dualscale.json#aggregates`)
    - revision/results/matched2000_dualscale.json#aggregates (the audited OD-scale source — entries with `metric_name="emd"` and `scale="OD"` per model)
    - revision/run_figure_suite.py:620-654 (the cross_model_emd render block; sourcing/aggregation/headline-reference all change)
    - revision/run_figure_suite.py:634, 1129, 1892 (the three `np.std(...)` sites that switch to `ddof=1`)
    - revision/run_matched2000_dualscale.py:522 (`statistics.pstdev` → `statistics.stdev`)
    - revision/core/eval.py:25-36 (the v1.0 metric-redefinition site — histogram-density Wasserstein → raw-sample Wasserstein; CITED, NOT modified)
    - .planning/phases/14-paper-revision-release-freeze/peer-review/code-review.md (CR-2 cross_model_emd selection bias)
    - .planning/phases/14-paper-revision-release-freeze/peer-review/quantum-review.md (C-1, C-2, C-3 reviewer's triangulated diagnosis)
    - .planning/phases/14-paper-revision-release-freeze/peer-review/provenance-review.md (PROV-CRIT-1 cross-reference)
    - .planning/phases/14-paper-revision-release-freeze/peer-review/math-review.md (H-2 ddof, H-3 Pareto critic-included count)
    - revision/docs/methods_full.md §2.k (the Pareto narrative being augmented with 250881 critic-included param count + total-adversarial-budget subsection)
    - revision/docs/reconciliation_note.md (the whole existing file — the EMD (OD scale) table being re-emitted with deltas ≈ 0, and the appended Plan 14-12 integration caveat from 14-12 which is PRESERVED VERBATIM; this task APPENDS a NEW metric-redefinition disclosure paragraph below 14-12's caveat)
    - revision/results/model_info.json (n_params per model — for the Pareto narrative's critic-included count derivation)
  </read_first>
  <action>
    Step A — Rebuild `reconciliation_note.md` from the audited OD-scale source
    (C-1 / PROV-CRIT-1):

    In `revision/run_model_info.py:220-302`, replace the existing
    `metrics.json["emd_avg"][-1]` read for the NEW column with:

    ```python
    # NEW column sources from the audited OD-scale aggregate mean,
    # NOT the log-return-standardized training-loop metric emd_avg[-1].
    # Fixes C-1 / PROV-CRIT-1 (scale-collision) under Phase 14 plan 14-13.
    duals = json.loads(DUALSCALE_PATH.read_text())
    new_col = {
        agg["model"]: agg["mean"]
        for agg in duals["aggregates"]
        if agg.get("metric_name") == "emd" and agg.get("scale") == "OD"
    }
    ```

    Re-emit `reconciliation_note.md` via `run_model_info.py`. Expected
    deltas (NEW - OLD): iqp_sel_55_repro ≈ -0.00006, wgan_mlp ≈ -0.00163,
    wgan_cnn ≈ -0.05871, wgan_lstm ≈ -0.00104; rephrase the Interpretation
    paragraph from `+0.127 degradation` to `the matched 2000-epoch budget
    recovers OD-scale EMD within seed variance of the 1000-epoch baseline`.

    Preserve the existing 14-12 integration caveat paragraph VERBATIM (it
    sits below the EMD table and above the new metric-redefinition disclosure
    appended below).

    Step B — Append metric-redefinition disclosure to `reconciliation_note.md`
    (C-3):

    AFTER the existing 14-12 integration caveat (which is preserved verbatim)
    and AFTER the rebuilt EMD (OD scale) table + Interpretation paragraph,
    APPEND a new section:

    > **Metric-redefinition disclosure (Plan 14-13, peer-review remediation).**
    > The v1.0 release (`revision/core/eval.py:25-36`) switched the EMD
    > implementation from a histogram-density Wasserstein
    > (`scipy.stats.wasserstein_distance(real_hist_density, fake_hist_density)`
    > over 50-bin histograms) to a raw-sample Wasserstein
    > (`scipy.stats.wasserstein_distance(real_samples, fake_samples)` over
    > the raw log-return arrays). The two metrics are NOT commensurate: the
    > pre-v1.0 headline `~0.0015` (histogram-density on `real`-only test
    > slice) and the v1.0+ headline `~0.121` (raw-sample on the matched-2000ep
    > log-return windows) measure different probabilistic distances. The
    > headline trajectory across versions is therefore: pre-v1.0 ≈ 0.0015
    > (histogram-density, deprecated); v1.0 ≈ 0.121 (raw-sample, current
    > implementation). The OD-scale aggregate mean in the table above is
    > the v1.0 raw-sample Wasserstein evaluated on the original log-returns
    > rescaled by `np.exp(.) - 1` (OD = ordinary differences scale).

    Step C — Rebuild `cross_model_emd` render (CR-2 / C-2):

    Modify the `revision/run_figure_suite.py` block at lines 620-654 to:

    1. Source per-seed EMD from `matched2000_dualscale.json#aggregates`
       (entries with `metric_name="emd"` and `scale="OD"`) for the bar
       heights, NOT from `np.min(emd_avg)` over the 201-point trajectory.
    2. Aggregate as `mean ± std` over seeds 42-46 (the matched-budget seed
       set), NOT `min` over the trajectory.
    3. Place the frozen headline reference line on the SAME OD scale as the
       bars (NOT the log-return scale as before).
    4. Drop the `best EMD` framing in the title/axis label/legend; replace
       with `final-eval EMD (OD scale, mean ± std over 5 seeds)`.
    5. Companion JSON caption: `"OD scale, final-eval mean ± std over 5
       seeds 42-46; frozen headline reference line on the same scale"`.

    Re-render `cross_model_emd.{png,pdf,json}`.

    Step D — Switch population std → sample std (H-2):

    Across the codebase, switch every seed-aggregating standard-deviation
    site from population (ddof=0) to sample (ddof=1):

    - `revision/run_matched2000_dualscale.py:522` — `statistics.pstdev(...)`
      → `statistics.stdev(...)` (note: `statistics.stdev` is the sample-std
      counterpart of `statistics.pstdev`).
    - `revision/run_figure_suite.py:634, 1129, 1892` — `np.std(...)` →
      `np.std(..., ddof=1)` at all three sites.

    Re-emit `matched2000_dualscale.json` (the aggregates table is the
    direct downstream artifact). Error bars in every dependent figure widen
    by ~12% (factor √(5/4)). The underlying comparison story is unchanged
    — only the error-bar widths shift.

    Re-render every figure that consumes the updated aggregates: `cross_model_emd`
    (already done in Step C — re-do after the dualscale.json re-emit if the
    aggregate values shift), `param_efficiency_pareto`, `failure_modes_summary`,
    `seed_variance_per_model`, `matched2000_dualscale_sidebyside`.

    Step E — Add critic-included parameter count to Pareto narrative (H-3):

    Open `revision/docs/methods_full.md §2.k` (the Pareto narrative). Add a
    new explicit citation of the critic-included parameter count `250881`
    and append a `### §2.k.x — Total adversarial parameter budget`
    supplementary subsection comparing generator-only and generator+critic
    counts across the adversarial models (WGAN-MLP, WGAN-CNN, WGAN-LSTM,
    iqp_sel_55, V1, V2, V3, default_75). Pull the per-model `n_params` from
    `revision/results/model_info.json`; pull the shared critic param count
    `250881` from the same source (or from the audited
    `classical_architectures.json` if that's where it lives).

    Update `param_efficiency_pareto.json` (the figure-companion JSON) caption
    to disclose: "x-axis is generator-only parameter count; the shared
    critic (250881 params) applies to all adversarial models alike and is
    documented in `methods_full.md §2.k.x` (Total adversarial parameter budget)".

    Step F — Populate `n` field per aggregate row (MED-4):

    In `revision/run_matched2000_dualscale.py`, in the aggregate emission
    block, set the `n` field per aggregate row:
    - `n = 5` for matched-budget aggregates (the 5-seed set 42-46).
    - `n = 1` for the headline aggregate (single frozen-checkpoint
      evaluation).

    Re-emit `matched2000_dualscale.json`.

    Step G — Re-emit and gate:

    Re-run `revision/run_model_info.py`, `revision/run_matched2000_dualscale.py`,
    and `revision/run_figure_suite.py` in dependency order (model_info before
    dualscale before figure_suite). Run the v2 gate (from Task 2) against
    `reconciliation_note.md` and `methods_full.md`; confirm PASS.

    Commit message: `feat(14-13): scale-correct reconciliation + cross_model_emd rebuild + ddof=1 sample-std switch + Pareto critic-count + n populated`.
  </action>
  <verify>
    <automated>./qgan_env/bin/python revision/run_model_info.py &amp;&amp; ./qgan_env/bin/python revision/run_matched2000_dualscale.py &amp;&amp; ./qgan_env/bin/python revision/run_figure_suite.py &amp;&amp; ./qgan_env/bin/python -c "import json,pathlib,sys; r=pathlib.Path('revision/docs/reconciliation_note.md').read_text(); d=json.loads(pathlib.Path('revision/results/matched2000_dualscale.json').read_text()); cme=json.loads(pathlib.Path('revision/results/figures/cross_model_emd.json').read_text()); m=pathlib.Path('revision/docs/methods_full.md').read_text(); errs=[]; ('Metric-redefinition disclosure' in r) or errs.append('reconciliation missing metric-redefinition disclosure'); ('histogram-density Wasserstein' in r) or errs.append('reconciliation missing histogram-density Wasserstein disclosure'); ('+0.127' in r and 'degradation' in r) and errs.append('reconciliation still has +0.127 degradation framing'); ('OD scale' in cme.get('caption','')+cme.get('caption_note','') or 'final-eval mean' in cme.get('caption','')+cme.get('caption_note','')) or errs.append('cross_model_emd caption missing OD scale + final-eval mean'); any(agg.get('n') in (5,1) for agg in d.get('aggregates',[])) or errs.append('matched2000_dualscale aggregates missing n field'); ('250881' in m) or errs.append('methods_full.md missing critic-included param count 250881'); ('Total adversarial parameter budget' in m) or errs.append('methods_full.md missing Total adversarial parameter budget subsection'); print('errors',errs); sys.exit(0 if not errs else 1)" &amp;&amp; ./qgan_env/bin/python revision/verify_number_provenance.py --target revision/docs/reconciliation_note.md &amp;&amp; ./qgan_env/bin/python revision/verify_number_provenance.py --target revision/docs/methods_full.md &amp;&amp; [ -z "$(git diff --stat revision/core/)" ]</automated>
  </verify>
  <acceptance_criteria>
    - `reconciliation_note.md` NEW column reads from `matched2000_dualscale.json#aggregates[*, metric_name='emd', scale='OD']`; deltas are ≈ 0 per model (iqp_sel_55_repro ≈ -0.00006, wgan_mlp ≈ -0.00163, wgan_cnn ≈ -0.05871, wgan_lstm ≈ -0.00104); narrative inverts from `+0.127 degradation` to `recovers OD-scale EMD within seed variance`
    - `reconciliation_note.md` carries an appended `Metric-redefinition disclosure (Plan 14-13)` paragraph citing `revision/core/eval.py:25-36` and explicitly marking the pre-v1.0 `~0.0015` and v1.0 `~0.121` NOT commensurate
    - The 14-12 integration caveat paragraph in `reconciliation_note.md` is PRESERVED VERBATIM
    - `cross_model_emd.{png,pdf,json}` re-rendered on OD scale, mean-over-seeds (NOT min-over-trajectory), frozen headline reference on same scale, `best EMD` framing dropped; companion JSON caption explicitly says `OD scale, final-eval mean ± std over 5 seeds`
    - `matched2000_dualscale.json#aggregates` has `n=5` for matched-budget rows and `n=1` for the headline row
    - `run_matched2000_dualscale.py:522` uses `statistics.stdev` (sample std)
    - `run_figure_suite.py:634, 1129, 1892` use `np.std(..., ddof=1)`
    - `param_efficiency_pareto`, `failure_modes_summary`, `seed_variance_per_model`, `matched2000_dualscale_sidebyside` re-rendered against updated aggregates
    - `methods_full.md §2.k` carries `250881` critic-included param count + `§2.k.x Total adversarial parameter budget` subsection comparing generator-only vs generator+critic counts
    - `param_efficiency_pareto.json` caption discloses x-axis is generator-only with shared critic ~250k
    - v2 gate PASSES on `reconciliation_note.md` and `methods_full.md`
    - `git diff --stat revision/core/` is empty (D-14-22)
  </acceptance_criteria>
  <done>Reconciliation rebuilt on OD scale with deltas ≈ 0 + metric-redefinition disclosure; cross_model_emd re-rendered on OD scale mean-over-seeds; ddof=0→ddof=1 sample-std switch applied across 4 sites with dependent figures re-rendered; methods_full.md §2.k carries critic-included param count + Total adversarial parameter budget subsection; `n` field populated; v2 gate passes; D-14-22 preserved.</done>
</task>

<task type="auto">
  <name>Task 4: Aggregator integrity sweep — 10 surgical fixes across 9 emitter scripts (10 findings: CR-3, HI-1, HI-2, HI-3, HI-4, HI-5, HI-7, HI-8, H-1, MD-3 + HIGH-2/PROV-HIGH-2, HIGH-3/PROV-HIGH-3)</name>
  <files>revision/run_methods_full.py, revision/run_model_info.py, revision/run_matched2000.py, revision/run_matched2000_dualscale.py, revision/run_circuit_diagrams.py, revision/run_classical_arch_extract.py, revision/run_framework_versions.py, revision/run_canonical_headline.py, revision/run_figure_suite.py, revision/results/model_info.json, revision/results/matched2000_dualscale.json, revision/results/methods_full.json, revision/docs/training_protocol.md</files>
  <read_first>
    - revision/run_methods_full.py:466-468 (hardcoded `training.py:347` and `training.py:259-268` line literals being replaced with programmatic `_first_lineno` / `_citations` patterns — CR-3)
    - revision/run_model_info.py (the WHOLE file — HI-2 optimizer_betas family-specific, HI-3 EXPECTED_DATA_HASH explicit-raise, PROV-HIGH-3/HIGH-3 dtype rename)
    - revision/run_matched2000.py:117-127 (ansatz dict — HI-4 topology read from lock at line 344)
    - revision/run_matched2000.py:337-347, 372-389, 434-492 (`_train_quantum`, `_train_wgan`, `_train_vae` — CR-4 future-gate symmetric MPS-disable, HI-7 _train_vae seeds, `training_time_device` recording)
    - revision/run_matched2000.py:503 (`_strict_accept` — D-14-13 extension adding `training_time_device` equality)
    - revision/run_matched2000.py (the `json.dumps(..., default=float)` site — HI-8 `_finite_sanitize`)
    - revision/run_matched2000_dualscale.py (HEADLINE_MODEL_KIND inclusion + `headline_model_kind` top-level field — HI-5)
    - revision/run_canonical_headline.py (hardcoded `42*31` DTW seed — HI-1; thread `generation_seed` through)
    - revision/run_figure_suite.py:191 (`default=float` → `_finite_sanitize` mirror, HI-8)
    - revision/run_figure_suite.py:1117 (hardcoded `head_epoch != 1969` → `head_epoch != lock["checkpoint_epoch"]`, MD-3)
    - revision/run_figure_suite.py:1146 (training_convergence_all_models axis label `OD scale` → `EMD (in-loop training metric, log-return-standardized scale)`, H-1)
    - revision/run_circuit_diagrams.py, revision/run_classical_arch_extract.py, revision/run_framework_versions.py (each emitter extended to record `"data_hash": "91e447d4624e25b3"` — HIGH-2 / PROV-HIGH-2)
    - revision/results/canonical_config_lock.json#checkpoint_epoch (the lock-driven hardcode replacement target for MD-3)
    - .planning/phases/14-paper-revision-release-freeze/peer-review/code-review.md (CR-3, HI-1..HI-8)
    - .planning/phases/14-paper-revision-release-freeze/peer-review/provenance-review.md (PROV-HIGH-2, PROV-HIGH-3, MED-4)
    - .planning/phases/14-paper-revision-release-freeze/peer-review/math-review.md (H-1 axis label, H-3 already done in Task 3)
  </read_first>
  <action>
    Apply 10 surgical edits across 9 emitter scripts. NONE touch `revision/core/`.

    Step A — `revision/run_methods_full.py:466-468` (CR-3):
    Replace the two hardcoded literals `"training.py:347"` and
    `"training.py:259-268"` with programmatic `_first_lineno` citations:

    ```python
    _citations["generator_to_compute_dtype"] = _first_lineno(
        "revision/core/training.py",
        r"generator\.to\(.*compute_dtype.*\)"
    )
    _citations["mps_dtype_block"] = _first_lineno(
        "revision/core/training.py",
        r"if\s+torch\.backends\.mps\.is_available\(\)"
    )
    ```

    Re-emit `revision/results/methods_full.json` and the downstream
    `revision/docs/methods_full.md` (the file:line citations now resolve to
    current line numbers — the anti-drift contract is no longer defeated by
    hand-typed line numbers).

    Step B — `revision/run_model_info.py` three fixes:

    1. **HI-2** — Set `optimizer_betas = None` for non-WGAN families
       (VAE, AR) instead of hardcoded `[0.0, 0.9]`. Add a `_FAMILY_BETAS`
       lookup or branch on `model_kind`.

    2. **HI-3 / PROV-HIGH-2** — Add `EXPECTED_DATA_HASH = "91e447d4624e25b3"`
       at module top. At the cross-artifact gate (the site that currently
       does mutual-equality), add an explicit-raise:

       ```python
       if observed_data_hash != EXPECTED_DATA_HASH:
           raise ValueError(
               f"data_hash mismatch: observed={observed_data_hash!r}, "
               f"expected={EXPECTED_DATA_HASH!r}"
           )
       ```

    3. **PROV-HIGH-3 / HIGH-3** — Rename `model_info.json#models[].dtype`
       → `dtype_samples` (it really is the sample dtype, not the param
       dtype). Add `dtype_params` alongside. Re-emit `training_protocol.md`
       row 34 to either split into two rows (`dtype_params` / `dtype_samples`)
       OR reference `methods_full.md §4.b` (Claude's discretion — pick the
       cleaner integration with the existing table structure).

    Re-emit `revision/results/model_info.json` and `revision/docs/training_protocol.md`.

    Step C — `revision/run_matched2000.py` five fixes:

    1. **HI-4** — Read `topology` from the lock dict at line 344 instead of
       hardcoding `"range"`. Use `lock.get("topology", "range")` with the
       lock loaded once at function scope.

    2. **CR-4 future-gate** — Patch `_train_wgan` and `_train_vae` with the
       same `torch.backends.mps.is_available = lambda: False` hook that
       `_train_quantum` already uses. Place it before any `torch` operation
       in each function. This ensures all FUTURE matched-budget runs are
       CPU-only across all training paths.

    3. **HI-7** — Add `np.random.seed(seed)` and `random.seed(seed)` to
       `_train_vae` top, mirroring `train_wgan_gp`. Also add
       `torch.manual_seed(seed)` if not already present.

    4. **`training_time_device` recording + D-14-13 extension** — In
       `_device_manifest` (the dict that currently records
       `sample_generation_device`), ALSO record `training_time_device` by
       inspecting `next(generator.parameters()).device` AND
       `next(critic.parameters()).device` AFTER training (NOT just at
       sample-generation time). Extend `_strict_accept` to require
       `training_time_device` equality across all models in a sweep:

       ```python
       devices = {m["training_time_device"] for m in manifest["models"]}
       if len(devices) > 1:
           raise StrictAcceptError(
               f"training_time_device mismatch across sweep: {devices}; "
               f"D-14-13 extension under Phase 14 plan 14-13 requires equality"
           )
       ```

    5. **HI-8** — Replace `default=float` in `json.dumps` with a
       `_finite_sanitize` helper that converts non-finite floats (`nan`,
       `+inf`, `-inf`) to `None` and explicit-raises if too many metrics
       are non-finite (threshold: >5% of values, Claude's discretion on the
       exact threshold). Apply the same fix in `revision/run_figure_suite.py:191`.

    Step D — `revision/run_matched2000_dualscale.py` (HI-5):
    Include `HEADLINE_MODEL_KIND` (the constant identifying the
    `iqp_sel_55_headline` entry) in the `model_kinds` list at top. Add a
    `"headline_model_kind"` documentation field at the top level of the
    emitted JSON pointing at the value. The Task 3 `n` field population is
    already covered.

    Step E — `revision/run_circuit_diagrams.py`,
    `revision/run_classical_arch_extract.py`,
    `revision/run_framework_versions.py` (HIGH-2 / PROV-HIGH-2):
    In each emitter, extend the emitted JSON schema to include
    `"data_hash": "91e447d4624e25b3"` (the audited dataset hash). For figure
    companion JSONs emitted by `run_figure_suite.py`, extend the companion
    schema to include `data_hash` at the top level (re-emit all figure
    companions — Tasks 3 and 5 are already re-rendering most of them; ensure
    `data_hash` is in every companion).

    Step F — `revision/run_canonical_headline.py` (HI-1):
    Thread `generation_seed` (the existing seed parameter or, if absent,
    add it as a CLI arg defaulting to `42`) through the DTW computation site
    instead of the hardcoded `42*31` literal. The fix is:

    ```python
    dtw_seed = int(args.generation_seed) * 31  # was: hardcoded 42*31
    ```

    Document the seed multiplication choice in a comment referencing the
    original 14-01 / 14-02 decision rationale.

    Step G — `revision/run_figure_suite.py` two fixes:

    1. **H-1** — Fix the `training_convergence_all_models` axis label at
       line 1146: `"OD scale"` → `"EMD (in-loop training metric, log-return-standardized scale)"`.
       This is accurate to what `emd_avg` actually is (a per-epoch loss
       evaluated on log-return-standardized inputs, NOT an OD-scale
       quantity — the axis label was wrong, the data is fine).

    2. **MD-3** — Replace the hardcoded `head_epoch != 1969` assertion at
       line 1117 with `head_epoch != lock["checkpoint_epoch"]`, reading
       the canonical_config_lock.json once at module init:

       ```python
       _LOCK = json.loads(pathlib.Path("revision/results/canonical_config_lock.json").read_text())
       ...
       assert head_epoch == _LOCK["checkpoint_epoch"], f"head_epoch {head_epoch} != lock {_LOCK['checkpoint_epoch']}"
       ```

    Step H — Re-emit + gate:

    Re-run every modified emitter in dependency order:
    `revision/run_circuit_diagrams.py`, `revision/run_classical_arch_extract.py`,
    `revision/run_framework_versions.py`, `revision/run_canonical_headline.py`,
    `revision/run_model_info.py`, `revision/run_matched2000_dualscale.py`,
    `revision/run_methods_full.py`, `revision/run_figure_suite.py`.

    Run the v2 gate against `methods_full.md`, `training_protocol.md`,
    `reconciliation_note.md`; confirm PASS.

    Commit message: `feat(14-13): aggregator integrity sweep (10 fixes: CR-3 programmatic citations, HI-1..HI-8, MD-3 lock-driven hardcode, H-1 axis label, HIGH-2/3 dtype/data_hash + D-14-13 training_time_device extension)`.
  </action>
  <verify>
    <automated>./qgan_env/bin/python revision/run_circuit_diagrams.py 2>&amp;1 | tail -5 &amp;&amp; ./qgan_env/bin/python revision/run_classical_arch_extract.py 2>&amp;1 | tail -5 &amp;&amp; ./qgan_env/bin/python revision/run_framework_versions.py 2>&amp;1 | tail -5 &amp;&amp; ./qgan_env/bin/python revision/run_model_info.py 2>&amp;1 | tail -5 &amp;&amp; ./qgan_env/bin/python revision/run_matched2000_dualscale.py 2>&amp;1 | tail -5 &amp;&amp; ./qgan_env/bin/python revision/run_methods_full.py 2>&amp;1 | tail -5 &amp;&amp; ./qgan_env/bin/python revision/run_figure_suite.py 2>&amp;1 | tail -5 &amp;&amp; ./qgan_env/bin/python -c "import json,pathlib,sys; mi=json.loads(pathlib.Path('revision/results/model_info.json').read_text()); d=json.loads(pathlib.Path('revision/results/matched2000_dualscale.json').read_text()); mf=json.loads(pathlib.Path('revision/results/methods_full.json').read_text()); rmf=pathlib.Path('revision/run_methods_full.py').read_text(); rmi=pathlib.Path('revision/run_model_info.py').read_text(); rm2=pathlib.Path('revision/run_matched2000.py').read_text(); rfs=pathlib.Path('revision/run_figure_suite.py').read_text(); rch=pathlib.Path('revision/run_canonical_headline.py').read_text(); tp=pathlib.Path('revision/docs/training_protocol.md').read_text(); errs=[]; ('_first_lineno' in rmf or '_citations[\"generator_to_compute_dtype\"]' in rmf or 'generator_to_compute_dtype' in rmf) or errs.append('run_methods_full hardcoded line citations not replaced'); ('EXPECTED_DATA_HASH' in rmi) or errs.append('run_model_info missing EXPECTED_DATA_HASH'); any('dtype_samples' in m for m in mi.get('models',[{}])) or ('dtype_samples' in json.dumps(mi)) or errs.append('model_info missing dtype_samples rename'); ('dtype_params' in json.dumps(mi)) or errs.append('model_info missing dtype_params alongside'); ('lock.get(\"topology\"' in rm2 or 'lock[\"topology\"]' in rm2) or errs.append('run_matched2000 topology still hardcoded'); ('training_time_device' in rm2) or errs.append('run_matched2000 missing training_time_device'); ('mps.is_available = lambda' in rm2.replace(' ','') or 'is_available=lambda' in rm2.replace(' ','')) or errs.append('run_matched2000 _train_wgan/_train_vae missing MPS-disable hook'); ('_finite_sanitize' in rm2 or '_finite_sanitize' in rfs) or errs.append('finite_sanitize helper missing'); ('HEADLINE_MODEL_KIND' in pathlib.Path('revision/run_matched2000_dualscale.py').read_text()) or errs.append('dualscale missing HEADLINE_MODEL_KIND'); ('headline_model_kind' in json.dumps(d)) or errs.append('dualscale JSON missing headline_model_kind field'); ('data_hash' in pathlib.Path('revision/run_circuit_diagrams.py').read_text() and 'data_hash' in pathlib.Path('revision/run_classical_arch_extract.py').read_text() and 'data_hash' in pathlib.Path('revision/run_framework_versions.py').read_text()) or errs.append('data_hash missing in circuit_diagrams/classical_arch/framework_versions emitters'); ('lock[\"checkpoint_epoch\"]' in rfs or 'checkpoint_epoch' in rfs) or errs.append('run_figure_suite still hardcodes 1969 (MD-3)'); ('log-return-standardized' in rfs or 'in-loop training metric' in rfs) or errs.append('run_figure_suite training_convergence axis label not fixed (H-1)'); ('42 * 31' in rch and 'args.generation_seed' not in rch and 'generation_seed' not in rch) and errs.append('run_canonical_headline still hardcodes 42*31'); ('dtype_samples' in tp or 'methods_full.md §4.b' in tp or 'methods_full.md §4' in tp) or errs.append('training_protocol.md row 34 not split or referenced'); print('errors',errs); sys.exit(0 if not errs else 1)" &amp;&amp; ./qgan_env/bin/python revision/verify_number_provenance.py --target revision/docs/methods_full.md &amp;&amp; ./qgan_env/bin/python revision/verify_number_provenance.py --target revision/docs/training_protocol.md &amp;&amp; [ -z "$(git diff --stat revision/core/)" ]</automated>
  </verify>
  <acceptance_criteria>
    - `run_methods_full.py:466-468` reads `generator_to_compute_dtype` and `mps_dtype_block` line numbers via programmatic `_first_lineno` / `_citations` patterns (CR-3)
    - `run_model_info.py` sets `optimizer_betas = None` for non-WGAN families (HI-2), declares `EXPECTED_DATA_HASH = "91e447d4624e25b3"` with explicit-raise (HI-3 / PROV-HIGH-2), renames `dtype` → `dtype_samples` with `dtype_params` alongside (PROV-HIGH-3 / HIGH-3)
    - `training_protocol.md` row 34 is split into `dtype_params` / `dtype_samples` OR references `methods_full.md §4.b`
    - `run_matched2000.py` reads topology from lock dict (HI-4), patches `_train_wgan` and `_train_vae` with `torch.backends.mps.is_available = lambda: False` (CR-4 future-gate), adds `np.random.seed(seed)` + `random.seed(seed)` to `_train_vae` (HI-7), records `training_time_device` in `_device_manifest`, and extends `_strict_accept` with `training_time_device` equality (D-14-13 extension)
    - `run_matched2000.py` and `run_figure_suite.py:191` replace `default=float` with `_finite_sanitize` (HI-8)
    - `run_matched2000_dualscale.py` includes `HEADLINE_MODEL_KIND` in `model_kinds` and the emitted JSON has `headline_model_kind` top-level field (HI-5)
    - `run_circuit_diagrams.py`, `run_classical_arch_extract.py`, `run_framework_versions.py` each emit `"data_hash": "91e447d4624e25b3"` (HIGH-2 / PROV-HIGH-2)
    - `run_canonical_headline.py` threads `generation_seed` through instead of hardcoded `42*31` (HI-1)
    - `run_figure_suite.py:1117` reads `lock["checkpoint_epoch"]` instead of hardcoded `1969` (MD-3)
    - `run_figure_suite.py:1146` axis label is `EMD (in-loop training metric, log-return-standardized scale)` (H-1)
    - v2 gate PASSES on `methods_full.md`, `training_protocol.md`, `reconciliation_note.md`
    - `git diff --stat revision/core/` is empty (D-14-22)
  </acceptance_criteria>
  <done>10 surgical aggregator-integrity fixes applied across 9 emitter scripts; all JSONs re-emitted; v2 gate passes on methods_full.md + training_protocol.md + reconciliation_note.md; D-14-13 strict-accept gate extended with training_time_device equality (future-gate); D-14-22 preserved.</done>
</task>

<task type="auto">
  <name>Task 5: Figure render-determinism + paper-blocks phantom-number cleanup + verify_freeze_ready glob + CR-4 historical-asymmetry disclosure (4 findings: CR-1, PROV-CRIT-2, HI-6, CR-4 disclosure)</name>
  <files>revision/run_figure_suite.py, revision/docs/paper_blocks_framing.md, revision/verify_freeze_ready.py, revision/docs/methods_full.md, revision/docs/reviewer_response.md, revision/results/figures/timeseries_default_75.png, revision/results/figures/timeseries_iqp_sel_55.png, revision/results/figures/timeseries_V1.png, revision/results/figures/timeseries_V2.png, revision/results/figures/timeseries_V3.png, revision/results/figures/timeseries_wgan_mlp.png, revision/results/figures/timeseries_wgan_cnn.png, revision/results/figures/timeseries_wgan_lstm.png, revision/results/figures/timeseries_vae.png</files>
  <read_first>
    - revision/run_figure_suite.py:382 (the non-deterministic `np.random.default_rng(model.__hash__() & 0xFFFF)` site; Python's randomized string hash means `real_window_idx` / `fake_window_idx` shift across invocations — CR-1)
    - revision/docs/paper_blocks_framing.md:119 (BEFORE quotation carrying phantom `0.6843`) and :520 (misleading provenance footer admitting the substring coincidence; PROV-CRIT-2 doc-side)
    - revision/verify_freeze_ready.py:82, 116 (`RESULTS_DIR.glob("*.json")` mismatching the verifier's `rglob` walk; HI-6)
    - revision/docs/methods_full.md (the doc to gain the CR-4 historical-asymmetry disclosure paragraph; locate the §4 Hardware & Software or §5 Reproducibility section as the natural insertion point)
    - revision/docs/reviewer_response.md (the doc to gain the SAME CR-4 disclosure paragraph; append into the Completeness sweep section from 14-12 or as a Cross-Cutting addendum)
    - .planning/phases/14-paper-revision-release-freeze/peer-review/code-review.md (CR-1, CR-4)
    - .planning/phases/14-paper-revision-release-freeze/peer-review/provenance-review.md (PROV-CRIT-2 doc-side guidance)
    - .planning/phases/14-paper-revision-release-freeze/peer-review/methods-reproducibility-review.md (HI-6 freeze_ready glob mismatch)
    - .planning/phases/14-paper-revision-release-freeze/14-CONTEXT.md — locate the user's CR-4 decision (disclose + future-gate, NOT classical sweep re-run)
  </read_first>
  <action>
    Step A — `revision/run_figure_suite.py:382` deterministic seed (CR-1):

    Replace the existing site:

    ```python
    rng = np.random.default_rng(model.__hash__() & 0xFFFF)
    ```

    With:

    ```python
    import hashlib
    rng = np.random.default_rng(int(hashlib.sha256(model.encode()).hexdigest()[:8], 16))
    ```

    Python's randomized string hash (`__hash__`) is seeded per-interpreter
    via `PYTHONHASHSEED`, so successive invocations produce different
    `real_window_idx` / `fake_window_idx` values. SHA-256 of the model name
    is deterministic across invocations.

    Re-render all 9 `timeseries_<model>.{png,pdf,json}` figures. Companion
    JSONs now carry stable `real_window_idx` / `fake_window_idx` across
    re-renders (verifiable by emitting twice and diffing).

    Step B — `revision/docs/paper_blocks_framing.md` phantom-number cleanup
    (PROV-CRIT-2 doc-side):

    1. Line 119 (BEFORE quotation): Remove the `0.6843` literal from the
       BEFORE block. The AFTER block already supersedes the original
       sentence; the BEFORE block can quote the manuscript WITHOUT the
       specific phantom number. The replacement text quotes the manuscript
       statement up to but NOT including `0.6843`.

    2. Line :520 (misleading provenance footer): Remove the footer paragraph
       that previously admitted the `0.6843` "passes via substring coincidence"
       (this was the diagnostic note explaining the v1 gate's false-positive
       handling — now obsolete because Task 2's v2 gate eliminates the
       substring coincidence; the doc-side cleanup makes the footer
       redundant and the residual prose misleading).

    All OTHER existing AFTER LaTeX blocks, locked-decomposition citations,
    PAPER-03 Companion artifact atlas subsection from 14-12, PAPER-01/02b
    `param_efficiency_pareto` footnote from 14-12, source map, and other
    provenance footnotes are PRESERVED VERBATIM.

    Step C — `revision/verify_freeze_ready.py` glob → rglob (HI-6):

    Lines 82, 116: change `RESULTS_DIR.glob("*.json")` to
    `RESULTS_DIR.rglob("*.json")`. The verifier already walks recursively
    via `rglob`; the freeze-ready audit must match.

    Also fix the negation-self-heal site: if `verify_freeze_ready.py`
    suggests adding a `!revision/results/<glob>` line to `.gitignore`, the
    suggested line must be `!revision/results/**/*.json` (the recursive
    glob), NOT `!revision/results/*.json` (the non-recursive glob).

    Step D — CR-4 historical-asymmetry disclosure (the locked
    decision: disclose + future-gate, NOT classical sweep re-run):

    Add the SAME one-paragraph disclosure to both `methods_full.md` and
    `reviewer_response.md`. Body (markdown prose):

    > **Historical training-time device asymmetry (Plan 14-13, peer-review
    > disclosure).** The matched-2000ep classical runs reported in this
    > manuscript executed on Apple-Silicon MPS at float32 precision (the
    > runtime default for the classical training paths
    > `train_wgan_gp` and `_train_vae` at the time of the original
    > matched-budget sweep), while the quantum runs executed on CPU at
    > float64 (the `_train_quantum` MPS-disable hook). This asymmetry was
    > discovered post-execution during the Phase 14 peer-review pass. Future
    > runs invoke the MPS-disable hook in all training paths (Plan 14-13
    > Task 4: `_train_wgan` and `_train_vae` now patch
    > `torch.backends.mps.is_available = lambda: False` symmetrically), and
    > the strict-accept gate now records `training_time_device` and enforces
    > equality across all models in a sweep (D-14-13 extension under Plan
    > 14-13). Numerical impact: MPS-float32 vs CPU-float64 on these small
    > (74–250k param) classical generators is empirically within seed
    > variance for the matched-budget aggregates reported in this
    > manuscript, but the asymmetry is disclosed here for completeness in
    > lieu of a full classical sweep re-run.

    Place the disclosure in `methods_full.md` at the bottom of §4 (Hardware
    & Software) or §5 (Reproducibility) — Claude's discretion on the
    natural anchor; the §4 hardware-asymmetry framing reads cleanest.

    Place the disclosure in `reviewer_response.md` at the bottom of the
    `## Completeness sweep (Plans 14-09 .. 14-11)` section appended by 14-12
    (under a new `### CR-4 — Historical training-time device asymmetry`
    subsection, or as a Cross-Cutting addendum — Claude's discretion).

    Step E — Re-render + gate:

    Re-run `revision/run_figure_suite.py` (re-renders timeseries figures).
    Run the v2 gate against `paper_blocks_framing.md`, `methods_full.md`,
    `reviewer_response.md`; confirm PASS. Run `verify_freeze_ready.py`
    against the results tree to confirm the glob/rglob fix is consistent.

    Step F — Re-emit determinism sanity check:
    After re-rendering, re-run `revision/run_figure_suite.py` a SECOND time
    (or just the timeseries-render block) and verify that the companion
    JSONs' `real_window_idx` / `fake_window_idx` fields are byte-identical
    across the two invocations (i.e. the SHA-256 seeding actually delivers
    determinism). If any drift is detected, the CR-1 fix is incomplete and
    must be revised before Task 5 closes.

    Commit message: `feat(14-13): determinism + paper-blocks phantom cleanup + freeze-ready rglob + CR-4 disclosure (CR-1, PROV-CRIT-2, HI-6, CR-4)`.
  </action>
  <verify>
    <automated>./qgan_env/bin/python revision/run_figure_suite.py 2>&amp;1 | tail -5 &amp;&amp; ./qgan_env/bin/python -c "import pathlib,json,sys,re; rfs=pathlib.Path('revision/run_figure_suite.py').read_text(); pbf=pathlib.Path('revision/docs/paper_blocks_framing.md').read_text(); vfr=pathlib.Path('revision/verify_freeze_ready.py').read_text(); mf=pathlib.Path('revision/docs/methods_full.md').read_text(); rr=pathlib.Path('revision/docs/reviewer_response.md').read_text(); errs=[]; ('hashlib.sha256(model.encode()' in rfs) or errs.append('run_figure_suite missing hashlib.sha256 deterministic seed (CR-1)'); ('model.__hash__' in rfs) and errs.append('run_figure_suite still uses non-deterministic __hash__'); ('0.6843' in pbf) and errs.append('paper_blocks_framing.md still carries phantom 0.6843 literal'); ('RESULTS_DIR.rglob' in vfr or '.rglob(' in vfr) or errs.append('verify_freeze_ready not using rglob (HI-6)'); ('Historical training-time device asymmetry' in mf) or errs.append('methods_full.md missing CR-4 historical-asymmetry disclosure'); ('Historical training-time device asymmetry' in rr) or errs.append('reviewer_response.md missing CR-4 historical-asymmetry disclosure'); ('MPS at float32' in mf and 'CPU at float64' in mf) or errs.append('methods_full.md CR-4 disclosure missing MPS-float32 / CPU-float64 explicit framing'); ('MPS at float32' in rr and 'CPU at float64' in rr) or errs.append('reviewer_response.md CR-4 disclosure missing MPS-float32 / CPU-float64 explicit framing'); print('errors',errs); sys.exit(0 if not errs else 1)" &amp;&amp; ./qgan_env/bin/python -c "import json,pathlib,sys,subprocess; first={}; second={}; [first.update({m: json.loads(pathlib.Path(f'revision/results/figures/timeseries_{m}.json').read_text()).get('real_window_idx','N/A')}) for m in ['default_75','iqp_sel_55','V1','V2','V3','wgan_mlp','wgan_cnn','wgan_lstm','vae'] if pathlib.Path(f'revision/results/figures/timeseries_{m}.json').exists()]; subprocess.run(['./qgan_env/bin/python','revision/run_figure_suite.py'], check=True, capture_output=True); [second.update({m: json.loads(pathlib.Path(f'revision/results/figures/timeseries_{m}.json').read_text()).get('real_window_idx','N/A')}) for m in ['default_75','iqp_sel_55','V1','V2','V3','wgan_mlp','wgan_cnn','wgan_lstm','vae'] if pathlib.Path(f'revision/results/figures/timeseries_{m}.json').exists()]; drift=[m for m in first if first[m] != second[m]]; print('drift',drift); sys.exit(0 if not drift else 1)" &amp;&amp; ./qgan_env/bin/python revision/verify_number_provenance.py --target revision/docs/paper_blocks_framing.md &amp;&amp; ./qgan_env/bin/python revision/verify_number_provenance.py --target revision/docs/methods_full.md &amp;&amp; ./qgan_env/bin/python revision/verify_number_provenance.py --target revision/docs/reviewer_response.md &amp;&amp; [ -z "$(git diff --stat revision/core/)" ]</automated>
  </verify>
  <acceptance_criteria>
    - `revision/run_figure_suite.py:382` uses `np.random.default_rng(int(hashlib.sha256(model.encode()).hexdigest()[:8], 16))` (deterministic across invocations); the old `model.__hash__()` site is gone
    - All 9 timeseries companion JSONs (`timeseries_<model>.json`) have byte-identical `real_window_idx` / `fake_window_idx` across two consecutive re-renders (determinism sanity check)
    - `revision/docs/paper_blocks_framing.md` no longer carries the phantom `0.6843` literal at line 119; the misleading provenance footer at :520 is removed; all other AFTER LaTeX blocks, 14-12 PAPER-03 atlas-companion subsection, 14-12 PAPER-01/02b param_efficiency_pareto footnote, and source map preserved verbatim
    - `revision/verify_freeze_ready.py` uses `RESULTS_DIR.rglob("*.json")` at lines 82, 116; negation-self-heal writes `!revision/results/**/*.json`
    - Both `methods_full.md` and `reviewer_response.md` carry the CR-4 historical-asymmetry disclosure paragraph with explicit `MPS at float32` / `CPU at float64` framing, the post-execution discovery note, the future-gate fix citation, and the `empirically within seed variance` numerical-impact statement
    - v2 gate PASSES on `paper_blocks_framing.md`, `methods_full.md`, `reviewer_response.md`
    - `git diff --stat revision/core/` is empty (D-14-22)
  </acceptance_criteria>
  <done>Timeseries figures re-rendered with SHA-256 deterministic seeding (verified by two-pass byte-identity); paper_blocks_framing.md phantom 0.6843 cleaned; verify_freeze_ready glob→rglob fix applied; CR-4 historical-asymmetry disclosure paragraph added to both methods_full.md and reviewer_response.md; v2 gate passes; D-14-22 preserved.</done>
</task>

<task type="auto">
  <name>Task 6: Methods full Metric conventions section — document M-2 / M-3 / M-4 (3 documentation findings)</name>
  <files>revision/docs/methods_full.md</files>
  <read_first>
    - revision/docs/methods_full.md (the WHOLE existing file; locate §3 Training as the natural anchor for the new §3.x Metric conventions subsection, and §2.i (VAE) + §2.j (AR) as the anchors for the convention-note annotations)
    - revision/core/eval.py:42-58 (compute_moments: `np.std(..., ddof=0)` population std, Fisher excess kurtosis via `scipy.stats.kurtosis(bias=True)`, sample skew via `scipy.stats.skew(bias=True)`; CITED, NOT modified per D-14-22)
    - revision/core/eval.py (compute_acf: `statsmodels.tsa.stattools.acf(s, nlags=20, fft=True)` with biased divisor `n`; CITED, NOT modified)
    - revision/core/models/nonadversarial.py:157 (AR(p) `sigma^2` estimator using `resid.var(ddof=0)` — ML estimator, biased by `(n-p)/n`, ~0.26% downward bias for n=777, p=2; CITED, NOT modified)
    - revision/run_baselines.py:315 (VAE ELBO actual loss: per-element-mean MSE + per-element-mean KLD with β=1, equivalent to re-weighted ELBO with implicit β ≈ 0.4 vs canonical formulation; CITED, NOT modified — though `revision/run_baselines.py` is NOT under D-14-22 since it lives outside `revision/core/`)
    - .planning/phases/14-paper-revision-release-freeze/peer-review/math-review.md (M-2 AR ddof=p vs ddof=0; M-3 compute_moments ddof + Fisher kurtosis + biased ACF; M-4 VAE ELBO β=1 vs canonical)
    - .planning/phases/14-paper-revision-release-freeze/14-CONTEXT.md — confirm D-14-22 (revision/core/ byte-freeze) PRESERVED for this task; the findings are DOCUMENTED in methods_full.md, NOT patched
  </read_first>
  <action>
    Open `revision/docs/methods_full.md`. The doc currently has 7 sections
    (per the byte-stable 14-11 emission): §1 Dataset, §2 Models, §3 Training,
    §4 Hardware & Software, §5 Reproducibility, §6 Addressing the documented
    contradictions, §7 Provenance footer.

    Step A — Insert NEW `§3.x — Metric conventions` subsection at the end
    of §3 (just before §4):

    ```
    ### §3.x — Metric conventions (documented per Plan 14-13, math-review remediation)

    The following small statistical conventions in `revision/core/` follow
    the v1.0 notebook-parity contract and are PRESERVED under D-14-22
    (revision/core/ byte-freeze). They are documented here for reviewer
    transparency rather than modified.

    **(a) `compute_moments`** (cite: `revision/core/eval.py:42-58`).
    Uses population standard deviation `np.std(..., ddof=0)` for the
    per-distribution `moment_std` field. Fisher excess kurtosis is computed
    via `scipy.stats.kurtosis(bias=True)` (biased estimator, matches the
    v1.0 notebook). Sample skew is computed via
    `scipy.stats.skew(bias=True)` (likewise biased; matches v1.0). The
    biased-vs-unbiased choice is consistent across all per-distribution
    moment statistics reported in this manuscript.

    **(b) `compute_acf`** (cite: `revision/core/eval.py` ACF block).
    Uses `statsmodels.tsa.stattools.acf(s, nlags=20, fft=True)`, which
    employs the biased divisor `n` (rather than the unbiased divisor `n-k`
    at lag `k`). This is the same biased ACF estimator as the v1.0
    notebook and is the conventional default for Wasserstein-distance
    inputs in this codebase.

    **(c) AR(p) `sigma^2` estimator** (cite:
    `revision/core/models/nonadversarial.py:157`). The residual variance is
    computed as `resid.var(ddof=0)` — the ML (maximum-likelihood)
    estimator, which is biased by a factor of `(n-p)/n` relative to the
    standard `ddof=p` Yule-Walker estimator. For the AR(2) configuration
    used on the 777-sample log-return sequence (n=777, p=2), the downward
    bias is `(777-2)/777 - 1 ≈ -0.26%`. The convention is consistent with
    the v1.0 notebook AR baseline and is documented here for completeness;
    no v2.0 numbers shift.

    **(d) VAE ELBO formulation** (cite: `revision/run_baselines.py:315`).
    The implemented loss is a per-element-mean reconstruction MSE plus a
    per-element-mean KL divergence with `β=1` (the standard ELBO formal
    coefficient). Because both terms are PER-ELEMENT MEANS rather than
    PER-WINDOW SUMS, the effective ELBO is re-weighted by the ratio of the
    per-element dimensionality between the two terms; for the 10-step ×
    1-feature windowing convention used in this manuscript, the
    reconstruction term is mean-over-10-elements and the KL term is
    mean-over-2-latent-dimensions, which is equivalent to a canonical
    per-window-sum ELBO with implicit `β ≈ 0.4`. The convention follows
    the v1.0 notebook baseline. The actual loss expression is documented
    alongside the canonical LaTeX in §2.i (VAE).
    ```

    Step B — Annotate §2.i (VAE) with the convention note:

    Locate §2.i (VAE) in `methods_full.md`. After the existing canonical
    ELBO LaTeX block, append:

    > **Implementation note (Plan 14-13, math-review M-4).** The actual
    > implemented loss at `revision/run_baselines.py:315` uses
    > per-element-mean MSE + per-element-mean KLD with `β=1`, which is
    > equivalent to a re-weighted ELBO with implicit `β ≈ 0.4` vs the
    > canonical formulation above (see §3.x.d for the derivation). The
    > comparison numbers in this manuscript use the implemented (β=1
    > per-element-mean) loss consistently across all VAE runs.

    Step C — Annotate §2.j (AR) with the convention note:

    Locate §2.j (AR). After the existing AR(p) LaTeX block / spec, append:

    > **Implementation note (Plan 14-13, math-review M-2).** The residual
    > variance estimator at `revision/core/models/nonadversarial.py:157`
    > uses `resid.var(ddof=0)` (ML estimator), biased by `(n-p)/n` ≈
    > -0.26% relative to the standard `ddof=p` Yule-Walker estimator for
    > n=777, p=2 (see §3.x.c). The convention follows the v1.0 notebook
    > AR baseline; no v2.0 numbers shift.

    Step D — Gate verify:

    Run the v2 gate against `methods_full.md`; confirm PASS. The literal
    numbers introduced in this task (`777`, `2`, `0.26%`, `10`, `1`, `0.4`,
    `42-58`, `157`, `315`) must resolve through the existing
    `revision/results/*.json` corpus (where applicable) or be phrased as
    prose-only context. The literals `42-58`, `157`, `315` are file:line
    citations, which v2's narrower `_ID_PATTERNS` strip via the
    `(?:\.py|\.md|\.json|\.tex):\d+(?:-\d+)?\b` rule. The literals `777`,
    `2`, `0.26%`, `10`, `1`, `0.4` are derived from prose context (n=777
    log-return samples, p=2 AR order, 10-step windowing, etc.) and either
    resolve via `model_info.json` or are explicitly contextualized.

    Commit message: `feat(14-13): methods_full.md §3.x Metric conventions documenting M-2 / M-3 / M-4 (D-14-22 honored — documented not changed)`.
  </action>
  <verify>
    <automated>./qgan_env/bin/python -c "import pathlib,sys; m=pathlib.Path('revision/docs/methods_full.md').read_text(); errs=[]; ('§3.x' in m or '### §3.x' in m or 'Metric conventions' in m) or errs.append('§3.x Metric conventions subsection missing'); ('compute_moments' in m) or errs.append('compute_moments convention not documented'); ('compute_acf' in m) or errs.append('compute_acf convention not documented'); ('resid.var(ddof=0)' in m or 'ddof=0' in m) or errs.append('AR ddof=0 convention not documented'); ('β=1' in m or 'beta=1' in m or 'β ≈ 0.4' in m or 'implicit β' in m) or errs.append('VAE β=1 ELBO convention not documented'); ('revision/core/eval.py' in m) or errs.append('eval.py citation missing from §3.x'); ('revision/core/models/nonadversarial.py' in m) or errs.append('nonadversarial.py citation missing from §3.x'); ('revision/run_baselines.py' in m) or errs.append('run_baselines.py citation missing from §3.x'); ('D-14-22' in m or 'byte-freeze' in m) or errs.append('§3.x missing D-14-22 documented-not-changed framing'); print('errors',errs); sys.exit(0 if not errs else 1)" &amp;&amp; ./qgan_env/bin/python revision/verify_number_provenance.py --target revision/docs/methods_full.md &amp;&amp; [ -z "$(git diff --stat revision/core/)" ]</automated>
  </verify>
  <acceptance_criteria>
    - `methods_full.md` gains a new `### §3.x — Metric conventions` subsection at the end of §3
    - The subsection documents (a) `compute_moments` ddof=0 / Fisher kurtosis / biased skew (cite: `revision/core/eval.py:42-58`), (b) `compute_acf` biased divisor `n` (cite: `revision/core/eval.py`), (c) AR(p) `resid.var(ddof=0)` ML estimator (cite: `revision/core/models/nonadversarial.py:157`), (d) VAE β=1 per-element-mean ELBO with implicit β ≈ 0.4 (cite: `revision/run_baselines.py:315`)
    - §2.i (VAE) is annotated with the implementation note citing run_baselines.py:315
    - §2.j (AR) is annotated with the implementation note citing nonadversarial.py:157
    - All four subsections explicitly state the convention follows the v1.0 notebook baseline
    - The D-14-22 documented-not-changed framing is present in §3.x
    - v2 gate PASSES on `methods_full.md`
    - `git diff --stat revision/core/` is empty (D-14-22)
  </acceptance_criteria>
  <done>methods_full.md §3.x Metric conventions subsection added documenting M-2 / M-3 / M-4 (compute_moments / compute_acf / AR sigma² / VAE β=1); §2.i and §2.j annotated with implementation notes; D-14-22 preserved (documented, not changed); v2 gate passes.</done>
</task>

<task type="auto">
  <name>Task 7: End-to-end re-verification + completeness_sweep_manifest update + peer_review_remediation.md + SUMMARY</name>
  <files>revision/docs/completeness_sweep_manifest.md, revision/docs/peer_review_remediation.md, .planning/phases/14-paper-revision-release-freeze/14-13-SUMMARY.md</files>
  <read_first>
    - revision/docs/completeness_sweep_manifest.md (the existing reviewer-facing manifest from 14-12; preserve verbatim and append a new 14-13 section at the bottom)
    - revision/docs/methods_full.md (post-Task-1/3/4/5/6; the final integrated state)
    - revision/docs/reconciliation_note.md (post-Task-3)
    - revision/docs/training_protocol.md (post-Task-4)
    - revision/docs/paper_blocks_framing.md (post-Task-5)
    - revision/docs/reviewer_response.md (post-Task-5)
    - revision/docs/circuit_atlas.md (byte-stable from 14-09)
    - revision/docs/paper_blocks_refs_methods.md (byte-stable from 14-12)
    - revision/docs/dataset_stats.md (gate target only; not edited by this plan)
    - revision/verify_number_provenance.py (v2 gate from Task 2)
    - All 5 peer-review reports under `.planning/phases/14-paper-revision-release-freeze/peer-review/*.md` (source of finding citations for the new peer_review_remediation.md)
    - git log (extract commit SHAs for Tasks 1-6 to populate the remediation index)
  </read_first>
  <action>
    Step A — Re-emit every paper-facing JSON downstream of Tasks 3-4 changes:

    Run the emitters in dependency order to ensure every downstream JSON is
    consistent with the post-Task-6 source state:

    ```
    ./qgan_env/bin/python revision/run_circuit_diagrams.py
    ./qgan_env/bin/python revision/run_classical_arch_extract.py
    ./qgan_env/bin/python revision/run_framework_versions.py
    ./qgan_env/bin/python revision/run_canonical_headline.py
    ./qgan_env/bin/python revision/run_model_info.py
    ./qgan_env/bin/python revision/run_matched2000_dualscale.py
    ./qgan_env/bin/python revision/run_methods_full.py
    ./qgan_env/bin/python revision/run_figure_suite.py
    ```

    Confirm `model_info.json`, `matched2000_dualscale.json`,
    `methods_full.json`, `training_protocol.md`, `reconciliation_note.md`,
    `methods_full.md`, and all figure companion JSONs are at their final
    post-14-13 state.

    Step B — Run v2 gate on all 9 paper-facing docs:

    ```bash
    for doc in \
        revision/docs/paper_blocks_framing.md \
        revision/docs/paper_blocks_refs_methods.md \
        revision/docs/reviewer_response.md \
        revision/docs/reconciliation_note.md \
        revision/docs/methods_full.md \
        revision/docs/circuit_atlas.md \
        revision/docs/completeness_sweep_manifest.md \
        revision/docs/training_protocol.md \
        revision/docs/dataset_stats.md; do
      ./qgan_env/bin/python revision/verify_number_provenance.py --target "$doc"
    done
    ```

    Every doc MUST exit 0 under the v2 gate. If any new failure surfaces
    (the tightened resolution catches a previously-coincidental pass), the
    failure is a substantive data problem and gets fixed in this same plan
    via a follow-up commit. The gate is NEVER weakened to make failures
    disappear.

    Also run with `--manifest` flag for at least the 4 highest-risk docs
    (paper_blocks_framing.md, paper_blocks_refs_methods.md,
    reconciliation_note.md, methods_full.md) and capture the resolution
    trace in the SUMMARY for reviewer transparency.

    Step C — Update `revision/docs/completeness_sweep_manifest.md`:

    APPEND a new section at the bottom of the existing manifest (the
    14-09 / 14-10 / 14-11 / 14-12 sections are PRESERVED VERBATIM):

    ```
    ## Plan 14-13 — Peer-review remediation sweep

    Closes 27 of 28 actionable findings (12 CRITICAL + 16 HIGH; HI-9
    explicitly OUT OF SCOPE per the reviewer's own perf-only annotation).
    No retraining; no classical sweep re-run; revision/core/ byte-frozen
    (D-14-22); D-14-16 (gate byte-freeze) LIFTED for Task 2 only; D-14-13
    (strict-accept gate) EXTENDED to include training_time_device.

    | Artifact | Task | Description | Resolved finding(s) |
    |---|---|---|---|
    | `revision/requirements-pinned.txt` | T1 | Pinned environment with `==` pins | METHODS-BLOCKER-1 |
    | `revision/checkpoints/best_checkpoint.pt` | T1 | Tracked headline checkpoint (~6 MB) | METHODS-BLOCKER-2 |
    | `.gitignore` (exception line) | T1 | `!revision/checkpoints/best_checkpoint.pt` | METHODS-BLOCKER-2 |
    | `revision/docs/methods_full.md` §4.1 + §5.1 + §3.x + §2.i + §2.j + §2.k + §4 CR-4 | T1+T3+T5+T6 | Methods doc surgical edits + new Metric conventions subsection + Pareto critic-count + CR-4 disclosure | METHODS-HIGH-1, H-3, CR-4 disclosure, M-2, M-3, M-4 |
    | `revision/verify_number_provenance.py` (v2) | T2 | Gate v2 (boundary-strict + render-only exclusion + --manifest) | CR-5, PROV-HIGH-1, PROV-MED-1, PROV-MED-2, PROV-MED-3 |
    | `revision/docs/reconciliation_note.md` | T3 | OD-scale rebuild + metric-redefinition disclosure | C-1, PROV-CRIT-1, C-3 |
    | `revision/results/figures/cross_model_emd.{png,pdf,json}` | T3 | OD-scale mean-over-seeds rebuild | CR-2, C-2 |
    | `revision/results/matched2000_dualscale.json` | T3+T4 | ddof=1 sample-std + HEADLINE_MODEL_KIND + n populated | H-2, HI-5, MED-4 |
    | `revision/results/model_info.json` + `training_protocol.md` | T4 | optimizer_betas family-specific + EXPECTED_DATA_HASH explicit-raise + dtype_params/dtype_samples rename | HI-2, HI-3, PROV-HIGH-2, PROV-HIGH-3, HIGH-3 |
    | `revision/run_methods_full.py` (programmatic citations) + `methods_full.json` | T4 | _first_lineno / _citations replacing hardcoded line literals | CR-3 |
    | `revision/run_matched2000.py` (MPS-disable symmetric + training_time_device + _strict_accept extension + _finite_sanitize + topology from lock + _train_vae seeds) | T4 | CR-4 future-gate + HI-4, HI-7, HI-8, D-14-13 extension | CR-4 future-gate, HI-4, HI-7, HI-8 |
    | `revision/run_canonical_headline.py` (generation_seed threading) | T4 | DTW seed no longer hardcoded 42*31 | HI-1 |
    | `revision/run_circuit_diagrams.py` + `run_classical_arch_extract.py` + `run_framework_versions.py` (data_hash) | T4 | data_hash recorded in JSON outputs | HIGH-2, PROV-HIGH-2 |
    | `revision/run_figure_suite.py` (lock-driven head_epoch + corrected axis label + finite_sanitize) | T4 | MD-3 lock-driven + H-1 axis label | H-1, MD-3 |
    | 9× `revision/results/figures/timeseries_<model>.{png,pdf,json}` | T5 | SHA-256 deterministic seeding | CR-1 |
    | `revision/docs/paper_blocks_framing.md` (line 119 + :520) | T5 | Phantom 0.6843 + misleading footer removed | PROV-CRIT-2 doc-side |
    | `revision/verify_freeze_ready.py` (rglob) | T5 | Recursive walk consistency | HI-6 |
    | `revision/docs/reviewer_response.md` (CR-4 disclosure subsection) | T5 | Historical asymmetry honest disclosure | CR-4 disclosure |
    | `revision/docs/peer_review_remediation.md` (NEW) | T7 | Reviewer-facing finding-to-commit index | (manifest) |
    | `revision/docs/completeness_sweep_manifest.md` (this section) | T7 | Updated artifact manifest | (manifest) |

    ### End-to-end aggregate v2 gate verify (re-run command)

    ```
    for doc in \
        revision/docs/paper_blocks_framing.md \
        revision/docs/paper_blocks_refs_methods.md \
        revision/docs/reviewer_response.md \
        revision/docs/reconciliation_note.md \
        revision/docs/methods_full.md \
        revision/docs/circuit_atlas.md \
        revision/docs/completeness_sweep_manifest.md \
        revision/docs/training_protocol.md \
        revision/docs/dataset_stats.md; do
      ./qgan_env/bin/python revision/verify_number_provenance.py --target "$doc"
    done
    ```

    Every line above must exit 0 under the v2 schema.
    ```

    Step D — Write `revision/docs/peer_review_remediation.md` (NEW
    reviewer-facing finding-to-commit index):

    Structure: one section per of the 5 peer-review reports, each containing
    a table mapping `Finding ID | Severity | Reviewer's file:line citation
    | Resolved in commit | Resolution notes`. Use `git log --oneline` after
    Tasks 1-6 to extract the commit SHAs.

    Required mappings (all 28 findings; HI-9 explicitly OUT OF SCOPE):

    ```
    # Peer-review remediation index — Plan 14-13

    > Generated by Plan 14-13 as the reviewer-facing finding-to-commit
    > index. Closes 27 of 28 actionable findings from the 5-agent peer-
    > review pass (12 CRITICAL + 16 HIGH; HI-9 explicitly OUT OF SCOPE).
    > Every numeric literal in this index resolves to one of the audited
    > `revision/results/*.json` artifacts via the v2 gate.

    ## code-review.md
    | Finding | Severity | Reviewer's citation | Commit | Notes |
    |---|---|---|---|---|
    | CR-1 | CRITICAL | run_figure_suite.py:382 | <sha_T5> | SHA-256 deterministic seed |
    | CR-2 | CRITICAL | run_figure_suite.py:620-654 | <sha_T3> | OD-scale mean-over-seeds rebuild |
    | CR-3 | CRITICAL | run_methods_full.py:466-468 | <sha_T4> | Programmatic _first_lineno citations |
    | CR-4 | CRITICAL | run_matched2000.py:372-389 + run_matched2000.py:434-492 | <sha_T4> (future-gate) + <sha_T5> (disclosure) | MPS-disable symmetric + historical asymmetry disclosure |
    | CR-5 | CRITICAL | verify_number_provenance.py:119 | <sha_T2> | Gate v2 boundary-strict resolution |
    | HI-1 | HIGH | run_canonical_headline.py | <sha_T4> | generation_seed threading replaces 42*31 |
    | HI-2 | HIGH | run_model_info.py | <sha_T4> | optimizer_betas family-specific |
    | HI-3 | HIGH | run_model_info.py | <sha_T4> | EXPECTED_DATA_HASH explicit-raise |
    | HI-4 | HIGH | run_matched2000.py:344 | <sha_T4> | topology from lock dict |
    | HI-5 | HIGH | run_matched2000_dualscale.py | <sha_T4> | HEADLINE_MODEL_KIND included |
    | HI-6 | HIGH | verify_freeze_ready.py:82,116 | <sha_T5> | rglob recursive walk consistency |
    | HI-7 | HIGH | run_matched2000.py:_train_vae | <sha_T4> | np/random/torch seed in _train_vae |
    | HI-8 | HIGH | run_matched2000.py + run_figure_suite.py:191 | <sha_T4> | _finite_sanitize for non-finite JSON emission |
    | HI-9 | HIGH | is_complete subprocess | OUT OF SCOPE | Per reviewer's own perf-only annotation |

    ## math-review.md
    | Finding | Severity | Reviewer's citation | Commit | Notes |
    |---|---|---|---|---|
    | H-1 | HIGH | run_figure_suite.py:1146 axis label | <sha_T4> | "OD scale" → "EMD (in-loop training metric, log-return-standardized scale)" |
    | H-2 | HIGH | run_matched2000_dualscale.py:522 + run_figure_suite.py:634,1129,1892 | <sha_T3> | ddof=0→ddof=1 sample-std switch |
    | H-3 | HIGH | methods_full.md §2.k | <sha_T3> | Critic-included param count 250881 + Total adversarial parameter budget subsection |
    | M-2 | MEDIUM (DOCUMENTED) | nonadversarial.py:157 AR sigma² ddof | <sha_T6> | DOCUMENTED in methods_full.md §3.x.c per D-14-22 |
    | M-3 | MEDIUM (DOCUMENTED) | eval.py:42-58 compute_moments + compute_acf | <sha_T6> | DOCUMENTED in methods_full.md §3.x.a-b per D-14-22 |
    | M-4 | MEDIUM (DOCUMENTED) | run_baselines.py:315 VAE β=1 | <sha_T6> | DOCUMENTED in methods_full.md §3.x.d per D-14-22 |

    ## quantum-review.md
    | Finding | Severity | Reviewer's citation | Commit | Notes |
    |---|---|---|---|---|
    | C-1 | CRITICAL | run_model_info.py:220-302 reconciliation NEW column | <sha_T3> | OD-scale aggregate mean source switch |
    | C-2 | CRITICAL | cross_model_emd selection bias | <sha_T3> | OD-scale mean-over-seeds rebuild |
    | C-3 | CRITICAL | eval.py:25-36 metric redefinition | <sha_T3> | Metric-redefinition disclosure in reconciliation_note.md |

    ## provenance-review.md
    | Finding | Severity | Reviewer's citation | Commit | Notes |
    |---|---|---|---|---|
    | PROV-CRIT-1 | CRITICAL | reconciliation_note.md scale-collision | <sha_T3> | (same as C-1) |
    | PROV-CRIT-2 | CRITICAL | paper_blocks_framing.md:119 phantom 0.6843 | <sha_T2> (gate-side) + <sha_T5> (doc-side) | Gate v2 boundary-strict eliminates substring; doc cleanup removes phantom |
    | PROV-HIGH-1 | HIGH | verify_number_provenance.py float precision | <sha_T2> | ε-neighborhood replaces format-string equality |
    | PROV-HIGH-2 | HIGH | data_hash mutual-equality | <sha_T4> | EXPECTED_DATA_HASH explicit-raise + recorded in 3 additional emitters |
    | PROV-HIGH-3 | HIGH | training_protocol.md row 34 dtype | <sha_T4> | dtype_params / dtype_samples split |
    | PROV-MED-1 | MEDIUM | _ID_PATTERNS year strip | <sha_T2> | Narrower year regex |
    | PROV-MED-2 | MEDIUM | _ID_PATTERNS file:line strip | <sha_T2> | File-extension prefix required |
    | PROV-MED-3 | MEDIUM | render-only JSON in resolution corpus | <sha_T2> | render_only:true excluded |
    | MED-4 | MEDIUM | matched2000_dualscale.json n field null | <sha_T3> | n=5 / n=1 populated |

    ## methods-reproducibility-review.md
    | Finding | Severity | Reviewer's citation | Commit | Notes |
    |---|---|---|---|---|
    | METHODS-BLOCKER-1 | CRITICAL | requirements.txt ≥ not == | <sha_T1> | revision/requirements-pinned.txt with == |
    | METHODS-BLOCKER-2 | CRITICAL | best_checkpoint.pt gitignored | <sha_T1> | Tracked at revision/checkpoints/ via .gitignore exception |
    | METHODS-HIGH-1 | HIGH | methods_full.md §5.1 "bit-identical" | <sha_T1> | Softened wording referencing requirements-pinned.txt |
    | HIGH-2 | HIGH | data_hash absent from circuit_diagrams / classical_arch / framework_versions | <sha_T4> | data_hash recorded in 3 additional emitters (= PROV-HIGH-2) |
    | HIGH-3 | HIGH | training_protocol.md vs methods_full.md dtype | <sha_T4> | (= PROV-HIGH-3) |

    ## Out of scope (and rationale)

    - **HI-9** is_complete subprocess performance (~45s on dry-run) —
      reviewer flagged as perf-only with no correctness impact (reviewer's
      own annotation). Not addressed in 14-13.
    - **MD-1** stale `real_log_returns` reference in `training.py:418` —
      `revision/core/` byte-frozen under D-14-22. Documented in passing
      via methods_full.md §3.x context if applicable.
    - **MD-7** `mps.is_available` monkey-patch threadsafety — likewise
      byte-frozen; the CR-4 future-gate applies the monkey-patch to
      `_train_wgan` and `_train_vae` consistently with the existing
      `_train_quantum` pattern, but the threadsafety concern itself
      cannot be addressed without `revision/core/` edits.
    - **LO-1** bare `assert` in `quantum.py:87` — byte-frozen; the assert
      is documented as a no-op in production paths.
    - **Full classical sweep re-run on CPU** — user's locked decision to
      DISCLOSE the historical CR-4 MPS asymmetry rather than re-run the
      ~3–6 h classical sweep. The future-gate (Task 4) ensures all
      subsequent matched-budget runs are CPU-only.
    ```

    Step E — Write `.planning/phases/14-paper-revision-release-freeze/14-13-SUMMARY.md`:

    Use the standard GSD summary template:
    - Plan ID + dates
    - Self-check: PASS (every must_have truth verified)
    - Findings closed: 27 of 28 (HI-9 OOS)
    - Contract changes applied: D-14-16 LIFTED (Task 2 only) + D-14-13 EXTENDED + D-14-22 PRESERVED
    - Artifacts created/modified/regenerated (link to completeness_sweep_manifest.md §Plan 14-13 section)
    - Verification log (v2 gate per-doc PASS lines + the manifest re-run command)
    - Final state: Phase 14 incomplete plans = `[14-07]` only

    Commit message: `docs(14-13): SUMMARY + completeness_sweep_manifest update + peer_review_remediation.md`.
  </action>
  <verify>
    <automated>set -e; for doc in revision/docs/paper_blocks_framing.md revision/docs/paper_blocks_refs_methods.md revision/docs/reviewer_response.md revision/docs/reconciliation_note.md revision/docs/methods_full.md revision/docs/circuit_atlas.md revision/docs/completeness_sweep_manifest.md revision/docs/training_protocol.md revision/docs/dataset_stats.md; do ./qgan_env/bin/python revision/verify_number_provenance.py --target "$doc"; done &amp;&amp; ./qgan_env/bin/python -c "import pathlib,sys; m=pathlib.Path('revision/docs/completeness_sweep_manifest.md').read_text(); p=pathlib.Path('revision/docs/peer_review_remediation.md').read_text(); s=pathlib.Path('.planning/phases/14-paper-revision-release-freeze/14-13-SUMMARY.md').read_text(); errs=[]; ('Plan 14-13 — Peer-review remediation sweep' in m) or errs.append('completeness_sweep_manifest missing Plan 14-13 section'); for fid in ['CR-1','CR-2','CR-3','CR-4','CR-5','C-1','C-2','C-3','PROV-CRIT-1','PROV-CRIT-2','METHODS-BLOCKER-1','METHODS-BLOCKER-2','HI-1','HI-2','HI-3','HI-4','HI-5','HI-6','HI-7','HI-8','HI-9','H-1','H-2','H-3','PROV-HIGH-1','PROV-HIGH-2','PROV-HIGH-3','METHODS-HIGH-1']: (fid in p) or errs.append(f'peer_review_remediation missing {fid}')
__import__('sys').exit(0 if not errs else (print('errors',errs) or 1))" &amp;&amp; [ -z "$(git diff --stat revision/core/)" ]</automated>
  </verify>
  <acceptance_criteria>
    - Every emitter (`run_circuit_diagrams.py`, `run_classical_arch_extract.py`, `run_framework_versions.py`, `run_canonical_headline.py`, `run_model_info.py`, `run_matched2000_dualscale.py`, `run_methods_full.py`, `run_figure_suite.py`) re-runs without error and re-emits its downstream JSONs/docs
    - v2 gate PASSES on all 9 paper-facing docs (`paper_blocks_framing.md`, `paper_blocks_refs_methods.md`, `reviewer_response.md`, `reconciliation_note.md`, `methods_full.md`, `circuit_atlas.md`, `completeness_sweep_manifest.md`, `training_protocol.md`, `dataset_stats.md`)
    - `completeness_sweep_manifest.md` carries a new `## Plan 14-13 — Peer-review remediation sweep` section at the bottom; existing 14-09 / 14-10 / 14-11 / 14-12 sections preserved verbatim
    - `peer_review_remediation.md` exists, contains one section per of the 5 peer-review reports, every one of the 28 findings (12 CRITICAL + 16 HIGH) maps to at least one commit SHA from this plan (or is explicitly marked OUT OF SCOPE / DOCUMENTED per D-14-22), and PASSES the v2 gate
    - `.planning/phases/14-paper-revision-release-freeze/14-13-SUMMARY.md` exists; Self-Check is PASS; lists all 7 task commits + the SUMMARY commit; final-state line declares Phase 14 incomplete plans = `[14-07]` only
    - `git diff --stat revision/core/` is empty (D-14-22 byte-freeze preserved across all 7 tasks)
    - `git diff --stat revision/verify_number_provenance.py` shows the v2 rewrite (NOT empty — the lift was intentional) but the schema string declares the v2 contract; the gate is back in byte-freeze under the new schema
  </acceptance_criteria>
  <done>Every paper-facing JSON re-emitted; v2 gate PASSES on all 9 docs; completeness_sweep_manifest.md updated with Plan 14-13 section; peer_review_remediation.md written mapping all 28 findings to commit SHAs (or OOS / DOCUMENTED); 14-13-SUMMARY.md written; D-14-22 preserved; Phase 14 incomplete plans = [14-07] only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| paper-facing docs ↔ audited JSON corpus | wrong citation, hand-typed literal, or scale-mixed number in any paper-facing doc would put an unverifiable claim in front of reviewers; the v2 gate is the sole defensive boundary |
| reconciliation NEW column ↔ source-of-truth selection | reading the wrong field (`emd_avg[-1]` log-return scale vs `matched2000_dualscale.json#aggregates` OD scale) inverts the conclusion narrative; this was the C-1 / PROV-CRIT-1 / CR-2 root cause |
| provenance gate ↔ resolution corpus | gate that accepts substring/float-format coincidences (the v1 gate) issues false-positive PASS verdicts, which propagate phantom numbers into reviewer-facing docs (PROV-CRIT-2) |
| `revision/core/` byte-freeze (D-14-22) ↔ documentation edits | M-2/M-3/M-4 are surfaced by the math reviewer as math/statistics concerns; addressing them via `revision/core/` edits would violate D-14-22, change v1.0 numbers, and re-open the v1.0-vs-v2.0 reconciliation. The discipline is to DOCUMENT (not CHANGE) these conventions in methods_full.md §3.x |
| `training_time_device` ↔ historical asymmetry vs future-gate | retroactively re-running the classical sweep on CPU-float64 takes ~3–6 h and changes the matched-budget numbers; the user's locked decision is to DISCLOSE the historical asymmetry (Task 5) + FUTURE-GATE it via D-14-13 extension (Task 4) rather than incur the re-run cost. The threat is silently changing the numbers OR omitting the disclosure |
| `best_checkpoint.pt` tracking ↔ git-lfs / Zenodo deferral | the ~6 MB headline checkpoint must be reproducible by reviewers without an external download dependency. Git-lfs would defer the dependency to a future Zenodo deposit (Plan 14-07); the user's locked decision is to commit it directly via `.gitignore` exception |
| timeseries figure render ↔ Python hash randomization | Python's `__hash__` is seeded per-interpreter via `PYTHONHASHSEED`, so `np.random.default_rng(model.__hash__() & 0xFFFF)` produces different seeds across invocations (CR-1). SHA-256-of-name is the deterministic alternative |
| 5 peer-review reports ↔ 14-13 commit traceability | reviewers must be able to map their findings to the exact commit that resolved them; `peer_review_remediation.md` is that index and any missing finding row breaks the audit |
| `.gitignore` exception ↔ pre-existing dirty working tree | the user's local working tree has pre-existing dirty `.gitignore` edits (per `git status` snapshot); the new `!revision/checkpoints/best_checkpoint.pt` exception line is the FIRST intentional `.gitignore` edit and must be applied at the executor side, NOT stashed across the merge |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-32 | Spoofing | scale-mixed reconciliation NEW column (`emd_avg[-1]` log-return scale vs OD-scale aggregate mean) | mitigate | Task 3 switches the source to `matched2000_dualscale.json#aggregates[*, metric_name='emd', scale='OD']`; Task 3 verify gate confirms deltas ≈ 0 and the `+0.127 degradation` framing is removed |
| T-14-33 | Spoofing | cross_model_emd `np.min` over trajectory selection bias + log-return scale on bars vs OD scale on headline | mitigate | Task 3 re-renders cross_model_emd on OD scale with mean-over-seeds, headline reference on same scale, `best EMD` framing dropped, companion JSON caption explicitly `OD scale, final-eval mean ± std over 5 seeds` |
| T-14-34 | Tampering | provenance gate substring/float coincidence false positives | mitigate | Task 2 rewrites the gate to v2 with boundary-strict regex + ε-neighborhood + render-only exclusion + narrower `_ID_PATTERNS` + `--manifest` resolution trace; D-14-16 LIFTED for Task 2 only |
| T-14-35 | Spoofing | hardcoded line citations in `run_methods_full.py:466-468` defeating anti-drift contract | mitigate | Task 4 replaces with programmatic `_first_lineno` / `_citations` patterns; methods_full.json + methods_full.md re-emitted with current line numbers |
| T-14-36 | Information Disclosure | historical CR-4 MPS asymmetry undisclosed in manuscript | mitigate | Task 5 adds CR-4 historical-asymmetry disclosure paragraph to BOTH `methods_full.md` and `reviewer_response.md`; Task 4 future-gate ensures all subsequent runs are CPU-only |
| T-14-37 | Tampering | inadvertent `revision/core/` edit slipping into a docs-only or emitter-only task (would break D-14-22) | mitigate | Every task's verify gate asserts `[ -z "$(git diff --stat revision/core/)" ]`; M-2/M-3/M-4 are explicitly DOCUMENTED (Task 6) rather than patched |
| T-14-38 | Tampering | `methods_full.md` other-section verbatim preservation: Task 1 only edits §4.1 + §5.1; Task 3 edits §2.k; Task 5 edits the CR-4 disclosure anchor; Task 6 adds §3.x + §2.i + §2.j annotations; the rest must NOT drift | mitigate | Per-task acceptance criteria explicitly enumerate the sections each task may edit; remaining sections preserved verbatim across the 4 task touches |
| T-14-39 | Information Disclosure | phantom `0.6843` literal surviving in `paper_blocks_framing.md:119` after Task 2's gate v2 catches it | mitigate | Task 5 explicitly removes line 119 BEFORE quotation `0.6843` and the misleading provenance footer at :520; Task 5 verify gate greps for `'0.6843' not in pbf` |
| T-14-40 | Tampering | non-deterministic timeseries figure renders (`np.random.default_rng(model.__hash__() & 0xFFFF)`) producing different `real_window_idx`/`fake_window_idx` across invocations | mitigate | Task 5 replaces with `np.random.default_rng(int(hashlib.sha256(model.encode()).hexdigest()[:8], 16))`; Task 5 verify gate runs the emitter twice and confirms byte-identity across the two passes |
| T-14-41 | Tampering | T-14-SC (package legitimacy): the new `revision/requirements-pinned.txt` records exact versions matching the audited `framework_versions.json`; no new package introductions in this plan | accept | Task 1 sources versions directly from `revision/results/framework_versions.json` (audited under 14-11); no new install operations introduced by this plan |
| T-14-42 | Information Disclosure | missing finding in `peer_review_remediation.md` (a "completeness" remediation index with a hole) | mitigate | Task 7 verify gate enumerates every one of the 28 findings (12 CRITICAL + 16 HIGH) and asserts every ID appears in the doc; HI-9 explicitly marked OUT OF SCOPE with reviewer's own rationale |
| T-14-43 | Tampering | `.gitignore` pre-existing dirty modifications (user's unrelated local edits) get clobbered or merged incorrectly during the worktree merge | mitigate | Task 1 applies the `.gitignore` exception line at the EXECUTOR side (NOT stashed); the rest of the dirty `.gitignore` modifications stay on the stash across the merge per the established Phase 14 protocol |
| T-14-44 | Spoofing | wrong `data_hash` slipping in (e.g. a different hash recorded in `circuit_diagrams.json` than in `model_info.json`) | mitigate | Task 4 declares `EXPECTED_DATA_HASH = "91e447d4624e25b3"` with explicit-raise in `run_model_info.py`; the same hash is recorded in the 3 additional emitters (HIGH-2 / PROV-HIGH-2); cross-artifact gate fails loudly on inequality |
| T-14-45 | Tampering | `revision/run_matched2000.py` `_train_wgan` and `_train_vae` MPS-disable hooks added asymmetrically (e.g. only one of the two), defeating the CR-4 future-gate | mitigate | Task 4 verify gate explicitly greps for `mps.is_available = lambda: False` (or the equivalent monkey-patch) in BOTH `_train_wgan` and `_train_vae`; `_strict_accept` extension requires `training_time_device` equality across all models in a sweep (catches the asymmetry at gate time even if it slips into the code) |
</threat_model>

<verification>
End-to-end checks at the close of T7 (the 12-point verification checklist from
the approved plan, preserved verbatim):

1. **Gate v2 passes on all 9 paper-facing docs** with `--manifest` flag
   emitting a resolution-trace alongside; no PASS via substring coincidence
   anywhere.
2. **Reconciliation_note.md deltas are ≈ 0** (sign of "matched 2000ep recovers
   OD-EMD within seed variance"); narrative inverted from the previous
   "+0.127 degradation" framing.
3. **`cross_model_emd.png`** redrawn on OD scale, mean-over-seeds, headline
   reference on same scale; new companion JSON has
   `metric: "OD-scale final-eval EMD, mean over seeds 42-46"`.
4. **`timeseries_<model>.png`** companion JSONs have stable
   `real_window_idx`/`fake_window_idx` across re-renders (verify by emitting
   twice and diffing).
5. **`framework_versions.json` versions equal `==` pins in
   `revision/requirements-pinned.txt`** (cross-check).
6. **`git ls-files | grep best_checkpoint.pt`** returns the tracked path; its
   sha256 matches `canonical_config_lock.json#checkpoint_sha256`.
7. **`methods_full.md §5.1`** no longer claims "bit-identical"; either uses
   the softened wording or `train_wgan_gp` invokes
   `torch.use_deterministic_algorithms(True, warn_only=True)`.
8. **`methods_full.md §6`** has both contradiction-resolution paragraphs
   (default_75 vs iqp_sel_55 and dtype_params vs dtype_samples) AND the new
   Task 6 "Metric conventions" subsection (`§3.x` per the implementation).
9. **`methods_full.md` + `reviewer_response.md` carry the CR-4 historical-
   asymmetry disclosure paragraph.**
10. **`revision/docs/peer_review_remediation.md`** exists; every one of the
    28 findings (12 CRITICAL + 16 HIGH) maps to at least one commit SHA from
    this plan (HI-9 explicitly marked OUT OF SCOPE with rationale).
11. **`14-13-SUMMARY.md`** Self-Check section is PASS.
12. **Phase 14 plan inventory** shows incomplete = `[14-07]` only (everything
    except the deferred Zenodo gate is now complete).

After T7 passes, the paper-revision presentation layer is airtight; only
**14-07 (Zenodo deposit + tag + DOI wiring + release.md)** remains.
</verification>

<success_criteria>
27 of 28 actionable peer-review findings (12 CRITICAL + 16 HIGH; HI-9 OOS per
reviewer's own perf-only annotation) closed across 7 atomic task commits + 1
SUMMARY commit. The provenance gate is hardened to v2 (D-14-16 LIFTED for
Task 2 only; back in byte-freeze under the new schema after Task 2 closes)
with boundary-strict resolution, ε-neighborhood float matching, render-only
corpus exclusion, narrower `_ID_PATTERNS`, and a new `--manifest` resolution-
trace output. `reconciliation_note.md` is rebuilt on the audited OD scale with
deltas ≈ 0, the narrative inverted from `+0.127 degradation` to `recovers
within seed variance`, and a metric-redefinition disclosure paragraph
documenting the v1.0 histogram-density → raw-sample Wasserstein switch.
`cross_model_emd.{png,pdf,json}` is re-rendered on the OD scale with mean-over-
seeds (NOT min-over-trajectory) and the frozen headline reference line on the
same scale. The ddof=0→ddof=1 sample-std switch is applied across 4 sites and
every dependent figure is re-rendered. 9 emitter scripts are hardened
(programmatic line citations, optimizer_betas family-specific, EXPECTED_DATA_HASH
explicit-raise, dtype_params/dtype_samples rename, topology-from-lock, MPS-
disable future-gate symmetric on `_train_wgan`/`_train_vae`, `training_time_device`
+ D-14-13 extension, `_finite_sanitize`, HEADLINE_MODEL_KIND inclusion,
data_hash in 3 additional emitters, generation_seed threading, axis label
correctness, lock-driven head_epoch replacement). All 9 timeseries figures
are re-rendered with SHA-256 deterministic seeding (verified by two-pass
byte-identity). `paper_blocks_framing.md` line 119 phantom `0.6843` and :520
misleading footer are removed. `verify_freeze_ready.py` switches glob → rglob.
The CR-4 historical-asymmetry disclosure paragraph lands in both
`methods_full.md` and `reviewer_response.md`. `methods_full.md` gains a new
§3.x Metric conventions subsection documenting M-2 / M-3 / M-4 (D-14-22
honored — documented, not changed). `requirements-pinned.txt` ships exact
`==` pins for every audited package, and `best_checkpoint.pt` is tracked
directly via `.gitignore` exception (NOT git-lfs, NOT Zenodo-dependent).
`completeness_sweep_manifest.md` is updated with the 14-13 section.
`peer_review_remediation.md` is a new reviewer-facing finding-to-commit
index mapping all 28 findings to commit SHAs (or OOS / DOCUMENTED). The v2
gate PASSES on all 9 paper-facing docs. `revision/core/` is byte-frozen
throughout (D-14-22). No retraining; no classical sweep re-run; no new
figures beyond re-renders. After this plan completes, only 14-07 (Zenodo
deposit + tag + DOI wiring + release.md) remains in Phase 14.
</success_criteria>

<mechanics>
Mirrors established Phase 14 protocol:

- Plan authored via `gsd-planner` agent; validated by `gsd-plan-checker`
  before execution (the orchestrator handles checker invocation as a
  separate step — this planner does NOT spawn the checker itself).
- Execution via **foreground** `gsd-executor` with `isolation="worktree"`.
  **Background subagents auto-deny Bash for the first git safety call** —
  this is a documented failure mode that bit 8× in earlier Phase 14 work.
  14-13 must be spawned foreground; see
  `~/.claude/projects/-Users-shawngibford-dev-phd-qGAN/memory/project_quantum_sim_execution.md`
  for the canonical reference on the foreground-executor requirement +
  stall-watchdog + subagent-permission settings for compute-heavy GSD phases.
- Per-task atomic commits (one `feat(14-13): <task-tag>` commit each, plus
  the final `docs(14-13): SUMMARY` commit). 7 task commits + 1 SUMMARY
  commit + orchestrator merge:
  - `feat(14-13): repro infrastructure (requirements-pinned + tracked checkpoint + softened §5.1 + §4.1 reference)` (T1)
  - `feat(14-13): gate v2 (boundary-strict resolution + render-only exclusion + --manifest)` (T2)
  - `feat(14-13): scale-correct reconciliation + cross_model_emd rebuild + ddof=1 sample-std switch + Pareto critic-count + n populated` (T3)
  - `feat(14-13): aggregator integrity sweep (10 fixes: CR-3 programmatic citations, HI-1..HI-8, MD-3 lock-driven hardcode, H-1 axis label, HIGH-2/3 dtype/data_hash + D-14-13 training_time_device extension)` (T4)
  - `feat(14-13): determinism + paper-blocks phantom cleanup + freeze-ready rglob + CR-4 disclosure (CR-1, PROV-CRIT-2, HI-6, CR-4)` (T5)
  - `feat(14-13): methods_full.md §3.x Metric conventions documenting M-2 / M-3 / M-4 (D-14-22 honored — documented not changed)` (T6)
  - `docs(14-13): SUMMARY + completeness_sweep_manifest update + peer_review_remediation.md` (T7)
- Pre-existing dirty working-tree files (deleted LICENSE + modified
  `.gitignore`/`fake.csv`/`real.csv`/notebook/`phase4_validation.json`) get
  stash-cycled across the merge. The new `.gitignore` exception line is
  the FIRST intentional `.gitignore` edit — must be applied at the executor
  side, NOT stashed; the rest of the dirty `.gitignore` modifications stay
  on the stash.
- STATE.md / ROADMAP.md owned by orchestrator (post-wave update).
- ROADMAP updates (applied in the same write pass as this plan):
  `**Plans**: 12 plans` → `**Plans**: 13 plans`; D-14-23 scope note extended
  with the 14-13 paragraph (provenance-gate v2, scale-correct reconciliation,
  cross-model EMD rebuild, aggregator integrity, render-determinism,
  reproducibility infrastructure, CR-4 disclosure + future-gate, pure
  correction sweep no retraining, after 14-13 only 14-07 remains); new
  Wave 11 entry referencing 14-13; progress table `11/12` → `11/13`.

## Resume instructions if a session ends mid-task

Each task within 14-13 stands alone and is atomically committed. Re-running
`/gsd-execute-phase 14` after `/clear` resumes from the first incomplete plan
(still 14-07, plus 14-13 if mid-stream). 14-13 must be spawned **foreground**
(background subagents auto-deny Bash for the first worktree-safety git call —
see `~/.claude/projects/-Users-shawngibford-dev-phd-qGAN/memory/project_quantum_sim_execution.md`).
</mechanics>

<output>
Create `.planning/phases/14-paper-revision-release-freeze/14-13-SUMMARY.md` when done
</output>
