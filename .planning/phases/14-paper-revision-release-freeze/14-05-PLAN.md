---
phase: 14-paper-revision-release-freeze
plan: 05
type: execute
wave: 4
depends_on: ["14-03", "14-04"]
files_modified:
  - revision/docs/paper_blocks_framing.md
autonomous: true
requirements: [PAPER-01, PAPER-02, PAPER-03, PAPER-04, PAPER-05]
must_haves:
  truths:
    - "Hypothesis reframed in Section 1 and the quantum-necessity transition softened (PAPER-01)"
    - "All overclaiming language removed or explicitly softened (PAPER-02, LOCKED regardless of result direction per D-14-20)"
    - "A 'Circuit Design Rationale' subsection block exists (why 5 qubits, ansatz expressibility/trainability, classical critic + quantum generator) (PAPER-03)"
    - "Log-returns justified in bioprocess growth-rate framing, not finance (PAPER-04)"
    - "Decision-tree + Hybrid-GAN material moved to an Outlook block; Supp Table A2 caveated/removed; 20L/300L mismatch fixed (PAPER-05)"
    - "Every numeric literal in the blocks resolves to a revision/results/*.json value (number-provenance gate passes)"
  artifacts:
    - path: "revision/docs/paper_blocks_framing.md"
      provides: "copy-paste LaTeX blocks for PAPER-01..05 keyed to label/anchor + per-block reviewer rationale"
      min_lines: 60
  key_links:
    - from: "revision/docs/paper_blocks_framing.md"
      to: "main (4) copy.tex / supp_material.tex anchors"
      via: "each block keyed to a label or anchor sentence + line ref"
      pattern: "label|anchor|line"
    - from: "revision/docs/paper_blocks_framing.md"
      to: "revision/results/*.json"
      via: "every cited number annotated with its JSON source; verify_number_provenance.py passes"
      pattern: "revision/results"
---

<objective>
Produce `revision/docs/paper_blocks_framing.md` — copy-paste LaTeX blocks for
PAPER-01 (reframe hypothesis), PAPER-02 (de-overclaim, LOCKED per D-14-20),
PAPER-03 (Circuit Design Rationale subsection), PAPER-04 (log-returns bioprocess
justification), PAPER-05 (move decision-tree + Hybrid-GAN to Outlook, caveat
Supp Table A2, fix 20L/300L) — each keyed to a label/anchor sentence with a
one-line reviewer-comment rationale, every number sourced from a
`revision/results/*.json` artifact. The in-repo `.tex` is READ-ONLY (D-14-18) —
never edited.

Purpose: These five requirements are the claim-calibration + circuit-rationale
core of the reviewer response (R1-M5, R2-1, R2-2, R2-5b). PAPER-02 is a LOCKED
reviewer requirement regardless of which way the 2000ep numbers fall (D-14-20).

Output: A single keyed, JSON-traceable LaTeX-blocks file that passes the
number-provenance gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-paper-revision-release-freeze/14-CONTEXT.md
@.planning/phases/14-paper-revision-release-freeze/14-RESEARCH.md
@.planning/phases/14-paper-revision-release-freeze/14-PATTERNS.md
@.planning/phases/14-paper-revision-release-freeze/14-03-SUMMARY.md
@.planning/phases/14-paper-revision-release-freeze/14-04-SUMMARY.md

<interfaces>
<!-- Exact manuscript anchors from RESEARCH "Phase Requirements" — use these verbatim -->

PAPER-01: `main (4) copy.tex` section 1 Introduction line 61, section 1.4 "Quantum Generative Adversarial Networks" line 90, section 2.4 lines 146-151. Block keyed to anchor sentence at line 92/151. Reframed hypothesis text: "Can a PQC generator, operating in an exponentially large Hilbert space with O(poly(n)) parameters, match or exceed a classical generator of equivalent parameter count on a low-data bioprocess task?" Soften quantum-necessity transition.

PAPER-02 (LOCKED, D-14-20): targets in `main (4) copy.tex` — "industrial bioprocess engineering" (Concluding Remarks line 296), "high fidelity ... industrial bioprocesses" (line 266), "exponentially more compactly" (line 151). Block-replace each anchor sentence. Also remove/soften "computational advantages", "exponential representational compactness", "reduced mode collapse".

PAPER-03: new subsection after section 3.1 "QWGAN-GP Architecture Overview" line 155. Three sub-points (R2-5b): why 5 qubits, ansatz expressibility/trainability tradeoff, classical critic + quantum generator. Numbers from `ansatz_comparison` (2000ep, D-14-10) + `model_info.json` (55-param decomposition).

PAPER-04: `supp_material.tex` "Data Transformation Details" line 352 (finance-framed rationale lines 358-365 "highly favored in quantitative analysis"). Rewrite to growth-rate/bioprocess framing; block keyed to line 352.

PAPER-05: Hybrid-GAN material `supp_material.tex` "Hybrid-GAN Framework for Future Work" line 142 label fig:qgan_hybrid_appraoch; decision-tree label fig:qgan_schemcatic line 340; aspirational table label tbl:various_approaches line 226 (Hybrid-GAN "Proposed" row line 242); main-text future-work block line 286; 20L/300L mismatch `main (4) copy.tex` line 178 (label fig:lucy text "300L configuration of the 20L version") + supp caption line 346.

Number-provenance gate: `revision/verify_number_provenance.py --target <file>` (built in Plan 03) — every numeric literal must resolve to a `revision/results/*.json` value. `revision/docs/reconciliation_note.md` is the authoritative source for any number that changed 1000ep→2000ep (Pitfall 5).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: PAPER-01/02/04/05 keyed LaTeX blocks (framing + calibration)</name>
  <read_first>
    - main (4) copy.tex (READ-ONLY reference — line 61, line 90, lines 146-151, line 92/151 anchors; PAPER-02 targets lines 266/296/151; line 178 label fig:lucy 20L/300L; main future-work line 286)
    - supp_material.tex (READ-ONLY — "Data Transformation Details" line 352 + finance rationale 358-365; Hybrid-GAN line 142; decision-tree line 340; Table A2 line 226 / row 242; supp caption line 346)
    - QGAN_Review_Response_Plan.md.pdf (R1-M5, R2-1, R2-2, R2-3, R2-5a verbatim reviewer concerns for the one-line rationale per block)
    - revision/results/model_info.json (Plan 03 — 55-param decomposition + per-model numbers for PAPER-03)
    - revision/docs/reconciliation_note.md (Plan 03 — authoritative source for any number changed 1000ep→2000ep)
    - .planning/phases/14-paper-revision-release-freeze/14-RESEARCH.md (Phase Requirements PAPER-01/02/04/05 exact anchors; Pitfall 5; D-14-18 read-only .tex; D-14-20 PAPER-02 LOCKED)
  </read_first>
  <action>
    Create `revision/docs/paper_blocks_framing.md`. Do NOT edit any `.tex`
    file (D-14-18 — read-only Overleaf reference). For PAPER-01: a copy-paste
    LaTeX block reframing the hypothesis to the parameter-count-parity question
    (verbatim text in the interfaces block) keyed to `main (4) copy.tex` line
    92/151 / section 1.4, plus a softened quantum-necessity transition block
    for section 2.4 lines 146-151; one-line R1-M5/R2-1/R2-2 rationale each. For
    PAPER-02 (LOCKED, D-14-20 — applies regardless of which way the 2000ep
    numbers fall): block-replace each overclaim anchor sentence — "industrial
    bioprocess engineering" (line 296), "high fidelity ... industrial
    bioprocesses" (line 266), "exponentially more compactly" (line 151) — plus
    removal/softening of "computational advantages", "exponential
    representational compactness", "reduced mode collapse"; each a keyed
    before→after block with the R1-M5 rationale. For PAPER-04: a block
    rewriting the finance-framed log-return rationale (`supp_material.tex`
    lines 358-365) to bioprocess growth-rate framing, keyed to "Data
    Transformation Details" line 352, R1-M3 rationale. For PAPER-05: blocks
    (a) moving the Hybrid-GAN material (label fig:qgan_hybrid_appraoch line
    142) + decision-tree (label fig:qgan_schemcatic line 340) into a new
    explicit "Outlook" section out of main claims (main future-work line 286),
    (b) caveating or removing Supp Table A2 (label tbl:various_approaches line
    226, "Proposed" row 242) as aspirational, (c) fixing the 20L/300L mismatch
    at `main (4) copy.tex` line 178 (note the malformed mid-sentence label
    fig:lucy) and supp caption line 346; R2-3/R2-5a rationale. Every numeric
    literal in any block MUST carry an inline `% source:
    revision/results/<file>.json#<path>` annotation resolvable by
    `verify_number_provenance.py`; numbers that changed at 2000ep come from
    `reconciliation_note.md`. Each block format: target file + label/anchor +
    line ref → before snippet → after (revised) LaTeX → one-line
    reviewer-comment rationale.
  </action>
  <verify>
    <automated>test -f revision/docs/paper_blocks_framing.md && grep -q 'PAPER-01' revision/docs/paper_blocks_framing.md && grep -q 'PAPER-02' revision/docs/paper_blocks_framing.md && grep -q 'PAPER-04' revision/docs/paper_blocks_framing.md && grep -q 'PAPER-05' revision/docs/paper_blocks_framing.md && grep -q 'fig:lucy' revision/docs/paper_blocks_framing.md && git diff --stat -- "main (4) copy.tex" supp_material.tex | grep -cv . | grep -q '^0$' && echo TEX_UNTOUCHED</automated>
  </verify>
  <acceptance_criteria>
    - `revision/docs/paper_blocks_framing.md` exists and contains keyed blocks tagged `PAPER-01`, `PAPER-02`, `PAPER-04`, `PAPER-05`
    - PAPER-01 block contains the verbatim reframed hypothesis sentence ("match or exceed a classical generator of equivalent parameter count")
    - PAPER-02 blocks contain before→after replacements for each of: "industrial bioprocess engineering", "high fidelity ... industrial bioprocesses", "exponentially more compactly" (grep each target phrase)
    - PAPER-05 block references `fig:qgan_hybrid_appraoch`, `fig:qgan_schemcatic`, `tbl:various_approaches`, and `fig:lucy` and proposes a new "Outlook" section
    - Each block carries a target file + label/anchor + line ref and a one-line reviewer-comment rationale (grep for `R1-M` / `R2-` rationale tags)
    - `git diff --stat -- "main (4) copy.tex" supp_material.tex` shows zero changes (the .tex files are untouched, D-14-18)
  </acceptance_criteria>
  <done>PAPER-01/02/04/05 delivered as keyed before→after LaTeX blocks with reviewer rationale and JSON-source annotations; .tex untouched.</done>
</task>

<task type="auto">
  <name>Task 2: PAPER-03 Circuit Design Rationale subsection block + provenance gate pass</name>
  <read_first>
    - main (4) copy.tex (READ-ONLY — section 3.1 "QWGAN-GP Architecture Overview" line 155, the insertion anchor for the new subsection)
    - revision/results/model_info.json (Plan 03 — the locked 55-param IQP:SEL decomposition: qubits, layers, gate layout, parameter_count)
    - revision/results/canonical_config_lock.json (Plan 01 — 55-param decomposition + pinned native pipeline for the "why this ansatz" argument)
    - revision/results/matched2000/ ansatz_comparison artifact (Plan 02 — 2000ep V1/V2/V3 numbers for the expressibility/trainability tradeoff argument, D-14-10)
    - revision/docs/reconciliation_note.md (Plan 03 — authoritative for any 1000ep→2000ep changed number used in the rationale)
    - revision/verify_number_provenance.py (Plan 03 — the gate this file must pass)
    - QGAN_Review_Response_Plan.md.pdf (R2-5b — the three required sub-points)
    - .planning/phases/14-paper-revision-release-freeze/14-RESEARCH.md (Phase Requirements PAPER-03 anchor + sources)
  </read_first>
  <action>
    Append to `revision/docs/paper_blocks_framing.md` a PAPER-03 block: a new
    LaTeX `\subsection{Circuit Design Rationale}` to be inserted after
    `main (4) copy.tex` section 3.1 "QWGAN-GP Architecture Overview" (line
    155), covering the three R2-5b sub-points — (1) why 5 qubits (window
    length / Hilbert-space sizing argument, driven by the locked 55-param
    decomposition in `model_info.json`/`canonical_config_lock.json`, NOT a
    hard-coded layer count), (2) the ansatz expressibility–trainability
    tradeoff (cite the 2000ep `ansatz_comparison` V1/V2/V3 numbers), (3) why a
    classical critic + quantum generator. Every numeric literal carries an
    inline `% source: revision/results/<file>.json#<path>` annotation; any
    number that changed at 2000ep is taken from `reconciliation_note.md`. Then
    run `revision/verify_number_provenance.py --target
    revision/docs/paper_blocks_framing.md` and resolve every failure until the
    gate passes (numbers must come from JSON artifacts, never hand-typed —
    success-criterion 5, Pitfall 5). Do NOT edit `main (4) copy.tex`.
  </action>
  <verify>
    <automated>grep -q 'PAPER-03' revision/docs/paper_blocks_framing.md && grep -q 'Circuit Design Rationale' revision/docs/paper_blocks_framing.md && ./qgan_env/bin/python revision/verify_number_provenance.py --target revision/docs/paper_blocks_framing.md && git diff --stat -- "main (4) copy.tex" | grep -cv . | grep -q '^0$' && echo PROVENANCE_OK</automated>
  </verify>
  <acceptance_criteria>
    - `revision/docs/paper_blocks_framing.md` contains a `PAPER-03` block with a `\subsection{Circuit Design Rationale}` keyed to insert after section 3.1 line 155
    - The block addresses all three R2-5b sub-points (why 5 qubits, expressibility/trainability tradeoff, classical critic + quantum generator) — grep for each
    - The 55-qubit/param rationale is sourced from `model_info.json` / `canonical_config_lock.json` (annotation present), not a hard-coded layer count
    - `revision/verify_number_provenance.py --target revision/docs/paper_blocks_framing.md` exits 0 (every numeric literal resolves to a `revision/results/*.json` value)
    - `git diff --stat -- "main (4) copy.tex"` shows zero changes
  </acceptance_criteria>
  <done>PAPER-03 subsection block written and the entire framing-blocks file passes the executable number-provenance gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 2000ep JSON → LaTeX block | a hand-typed number in a manuscript block fabricates a published result (integrity of the frozen artifact) |
| revision package → in-repo .tex | accidental edit of the read-only Overleaf-canonical .tex would desync the manuscript of record |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-13 | Tampering | hand-typed number in published manuscript | mitigate | Every numeric literal annotated with a JSON source; `verify_number_provenance.py` explicit-raise gate is a hard pass criterion (success-criterion 5) |
| T-14-15 | Tampering | overclaiming language reintroduction | mitigate | PAPER-02 LOCKED (D-14-20) — before→after blocks for each named overclaim phrase, grep-verified present |
| T-14-16 | Tampering | read-only .tex edited | mitigate | `git diff --stat` on `main (4) copy.tex` / `supp_material.tex` must be empty in verify (D-14-18) |
| T-14-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan; RESEARCH Package Legitimacy Audit is a documented no-op for Phase 14 |
</threat_model>

<verification>
- `paper_blocks_framing.md` contains PAPER-01..05 keyed blocks with reviewer rationale + JSON-source annotations
- PAPER-02 LOCKED overclaim before→after replacements present (grep each phrase)
- PAPER-03 subsection covers all three R2-5b sub-points, sourced from model_info.json (not hard-coded)
- `verify_number_provenance.py --target paper_blocks_framing.md` exits 0
- `.tex` files byte-untouched (git diff empty)
</verification>

<success_criteria>
PAPER-01..05 are delivered as keyed, reviewer-rationaled, JSON-traceable
copy-paste LaTeX blocks that pass the executable number-provenance gate, with
the read-only manuscript source untouched — the claim-calibration + circuit
-rationale core of the reviewer response is complete.
</success_criteria>

<output>
Create `.planning/phases/14-paper-revision-release-freeze/14-05-SUMMARY.md` when done
</output>
