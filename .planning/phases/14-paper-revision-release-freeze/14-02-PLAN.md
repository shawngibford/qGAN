---
phase: 14-paper-revision-release-freeze
plan: 02
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - run_canonical_headline.py
  - run_matched2000.py
  - run_matched2000_sweep.sh
  - results/headline_canonical.json
  - results/matched2000/sweep_status.json
autonomous: true
requirements: [PAPER-03]
must_haves:
  truths:
    - "The headline quantum result is generated from the frozen best_checkpoint.pt (epoch 1969) loaded into the 55-param config, using stored mu/sigma and a fixed generation seed"
    - "Every model (55-param IQP:SEL reproduction, ansatz V1/V2/V3, wgan_mlp/cnn/lstm, VAE/AR) re-runs at a matched 2000-epoch budget"
    - "Each run emits a device/dtype manifest and hard-asserts its actual backend (fails loudly on silent CPU/dtype fallback)"
    - "The strict accept gate rejects any artifact whose device manifest, data_hash, seed set {42..46}, schema, or 2000ep-completion fails"
    - "The sweep is resumable (skip-already-done) and tiered T1/T2/T3 with run-to-completion (no hard time-box)"
  artifacts:
    - path: "run_canonical_headline.py"
      provides: "frozen-checkpoint headline generator (stored mu/sigma + fixed seed)"
      min_lines: 50
    - path: "run_matched2000_sweep.sh"
      provides: "xargs -P2 resumable 2000ep sweep harness (parallel >=3 rejected)"
      min_lines: 80
    - path: "results/headline_canonical.json"
      provides: "headline metrics from frozen checkpoint (distinct from reproduction)"
    - path: "results/matched2000/sweep_status.json"
      provides: "resumable tiered sweep state"
  key_links:
    - from: "run_canonical_headline.py"
      to: "best_checkpoint.pt"
      via: "load epoch-1969 params_pqc into 55-param config, stored mu/sigma, fixed gen seed"
      pattern: "best_checkpoint|params_pqc"
    - from: "run_matched2000_sweep.sh"
      to: "xargs -P 2"
      via: "resumable dispatch, parallel>=3 hard-rejected"
      pattern: "xargs -P 2"
    - from: "per-run accept logic"
      to: "device/dtype manifest"
      via: "hard-assert actual backend"
      pattern: "device|dtype|manifest"
---

<objective>
Re-execute every model at a matched 2000-epoch budget (D-14-08/09/10) behind
the D-14-13 strict accept gate, and generate the canonical headline result from
the FROZEN `best_checkpoint.pt` (epoch 1969) — NOT a fresh retrain (D-14-03/05).
The clean 2000ep retrain of the 55-param IQP:SEL runs as a non-load-bearing
reproducibility instance reported distinctly from the headline (D-14-10).
Tier-2/T3 of the D-14-22 strict gated pipeline.

Purpose: The originating bug was 1000ep comparison runs vs a 2000ep headline
checkpoint AND a 75-param vs 55-param mismatch. This plan closes the
unfair-comparison gap: identical 2000ep budget, seeds {42..46}, frozen
data_hash, device-honest manifests across every model.

Output: A frozen-checkpoint headline generator, a resumable tiered 2000ep
sweep harness following the established `xargs -P2` pattern, and accepted
2000ep artifacts for every model.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-paper-revision-release-freeze/14-CONTEXT.md
@.planning/phases/14-paper-revision-release-freeze/14-RESEARCH.md
@.planning/phases/14-paper-revision-release-freeze/14-PATTERNS.md
@.planning/phases/14-paper-revision-release-freeze/14-01-SUMMARY.md

<interfaces>
<!-- Established sweep + headline idioms — copy verbatim, change only the matrix -->

run_ansatz_sweep.sh skeleton (THE pattern to copy — change only VARIANTS/SEEDS/EPOCHS + artifact-bundle def):
- Thermal guardrail (lines 147-157): `--parallel` must be 1 or 2; `>=3` → `exit 3`.
- Dispatch (lines 394-409): `< "$WORKLIST" xargs -P 2 -L 1 bash -c 'run_one "$0" "$1"'` — NEVER multiprocessing.Pool.
- Resume: `is_complete()` (lines 175-183) checks all artifacts exist & non-empty; `run_one()` short-circuits complete pairs (294-300); `sweep_status.json` atomic tmp+`os.rename` under `flock` (264-281).
- Venv binary: invoke `./qgan_env/bin/python` directly (activate script has a stale hardcoded path), lines 98-108.

Backend lock (D-14-11): `default.qubit` + `diff_method="backprop"`. NO `lightning.qubit`. PennyLane sims are CPU-only — the quantum row is CORRECTLY CPU; classical rows must not silently fall back.

Headline reproduction landmines (D-14-05): use the checkpoint's STORED scalar `mu`/`sigma` and a FIXED generation seed — never freshly-computed stats — or the 16 canonical figures will not reproduce.

Strict accept gate (D-14-13): artifact trusted ONLY if device-manifest assertion passed + `data_hash` matches the frozen Phase-09.1 hash across ALL artifacts + seed set == {42,43,44,45,46} + long-form JSON schema conforms + full 2000ep completed (no early-stop on the headline path). Use explicit-raise (run_multiseed_rollup.py:86-92), not bare assert.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Frozen-checkpoint canonical headline generator</name>
  <read_first>
    - best_checkpoint.pt (epoch 1969, params_pqc (55,), stored mu/sigma — the headline source, NOT a retrain)
    - results/canonical_config_lock.json (Plan 01 locked 55-param selector + pinned native pipeline + stored mu/sigma + checkpoint_sha256)
    - core/models/quantum.py (the 55-param config-selectable circuit added in Plan 01)
    - core/training.py (device auto-select cuda→mps→cpu, compute_dtype float32-on-mps, generation path)
    - run_introspect_figures.py (repo-root resolver + CLI idiom lines 51-57)
    - .planning/phases/14-paper-revision-release-freeze/14-RESEARCH.md (D-14-03/05 reproduction landmines; Code Examples checkpoint oracle)
  </read_first>
  <action>
    Create `scripts/run_canonical_headline.py`. Resolve repo root + RESULTS
    anchor via the `run_multiseed_rollup.py:42-59` idiom. Verify
    `best_checkpoint.pt`'s sha256 equals the `checkpoint_sha256` recorded in
    `results/canonical_config_lock.json` (explicit-raise on mismatch
    — same ground-truth tensor as Plan 01, T-14-14). Construct the locked
    55-param IQP:SEL circuit (selector value from
    `canonical_config_lock.json`), load `params_pqc` from the checkpoint
    (epoch 1969), generate samples using the checkpoint's STORED scalar
    `mu`/`sigma` (read verbatim from the lock JSON / checkpoint — never
    recompute, D-14-05) and a FIXED generation seed recorded in the output.
    Evaluate metrics via `revision.core.eval` helpers ONLY (D-10-20 — never
    re-implement EMD/ACF/DTW/moments) on the pinned native pipeline (D-14-06)
    AND dual-scale (OD + log_return). Emit
    `results/headline_canonical.json` in the long-form schema
    (`schema`, `data_hash`, `metric_helpers`, `models`, `rows`) with a
    `device`/`dtype` manifest field and an explicit `source:
    "frozen_checkpoint_epoch_1969"` marker so it is NEVER conflated with the
    reproduction instance (D-14-10). Hard-assert (explicit-raise) the
    generation seed is fixed and the mu/sigma match the checkpoint.
  </action>
  <verify>
    <automated>./qgan_env/bin/python scripts/run_canonical_headline.py && ./qgan_env/bin/python -c "import json,sys; d=json.load(open('results/headline_canonical.json')); sys.exit(0 if d['source']=='frozen_checkpoint_epoch_1969' and 'device' in d and d.get('generation_seed') is not None else 1)"</automated>
  </verify>
  <acceptance_criteria>
    - `results/headline_canonical.json` exists with `source == "frozen_checkpoint_epoch_1969"`, a `device`/`dtype` manifest field, and a non-null `generation_seed`
    - The script verifies `best_checkpoint.pt` sha256 against `canonical_config_lock.json` with `raise AssertionError(` (grep) on mismatch
    - The mu/sigma used equal the checkpoint's stored scalars (grep the JSON for the stored values; no recompute path present)
    - `metric_helpers` field == `"revision.core.eval ONLY (D-10-20)"`
  </acceptance_criteria>
  <done>Headline metrics generated from the frozen checkpoint with stored mu/sigma + fixed seed, schema-conformant, device-manifested, distinct from any reproduction.</done>
</task>

<task type="auto">
  <name>Task 2: Resumable tiered 2000ep matched-budget sweep + strict accept gate</name>
  <read_first>
    - run_ansatz_sweep.sh (THE skeleton: thermal guardrail 147-157, xargs -P2 dispatch 394-409, is_complete 175-183, sweep_status atomic write 264-281, venv binary 98-108)
    - run_ansatz.py (per-run CLI driver shape to mirror for the per-model run)
    - run_multiseed_rollup.py (lines 42-59 repo-root resolver; lines 86-92 explicit-raise cross-artifact data_hash gate)
    - results/baselines/sweep_status.json (resumable sweep-state schema to reuse)
    - results/canonical_config_lock.json (locked 55-param selector + pinned pipeline — the quantum entrant for every comparison, D-14-04)
    - core/training.py (device auto-select + compute_dtype — the manifest source)
    - .planning/phases/14-paper-revision-release-freeze/14-CONTEXT.md (D-14-08..14 matched-budget + tiering + strict gate)
  </read_first>
  <action>
    Create `run_matched2000.py` (per-(model,seed) CLI driver) and
    `run_matched2000_sweep.sh` by copying the
    `scripts/run_ansatz_sweep.sh` skeleton end-to-end and changing ONLY the matrix and
    artifact-bundle definition. Matrix: models = {55-param IQP:SEL
    reproduction, ansatz V1, V2, V3, wgan_mlp, wgan_cnn, wgan_lstm, vae, ar} ×
    seeds {42,43,44,45,46}, EPOCHS=2000 for all. The 55-param IQP:SEL
    (selector from `canonical_config_lock.json`) is the quantum entrant in
    every comparison (D-14-04); its 2000ep run is the NON-load-bearing
    reproduction instance, tagged distinctly from the headline (D-14-10).
    Keep the thermal guardrail verbatim (`--parallel` 1|2, `>=3` → exit 3,
    D-14-12), the `xargs -P 2 -L 1` dispatch (NEVER multiprocessing.Pool),
    the resumable `is_complete()` skip-already-done semantics writing
    `results/matched2000/sweep_status.json` atomically under flock,
    and the `./qgan_env/bin/python` direct invocation. Backend is
    `default.qubit` + `diff_method="backprop"` (D-14-11 — do NOT add
    `lightning.qubit`). Each per-run emits a device/dtype manifest into its
    artifact bundle and hard-asserts the actual backend with the explicit-raise
    idiom — fail loudly on silent CPU/dtype fallback (e.g. CPU-float64 when
    MPS-float32 expected, D-14-12); the quantum rows are CORRECTLY CPU. Add a
    strict accept gate (explicit-raise, run_multiseed_rollup.py:86-92) applied
    per-artifact: device-manifest assertion passed, `data_hash` equal to the
    frozen Phase-09.1 hash across all artifacts, seed set == {42..46},
    long-form schema conforms, full 2000ep completed (no early-stop on the
    headline/reproduction path). Implement tiered acceptance — T1 (this is
    Plan 01's recovery, already done), T2 (reproduction + baseline-bearing
    runs), T3 (sensitivity/ansatz variants) — each tier independently
    acceptable so the sweep is resumable and run-to-completion with no hard
    time-box (D-14-14). Use background execution + stall-watchdog per the
    compute-heavy phase memory.
  </action>
  <verify>
    <automated>bash -n run_matched2000_sweep.sh && ./qgan_env/bin/python -c "import ast; ast.parse(open('run_matched2000.py').read())" && grep -q 'xargs -P 2' run_matched2000_sweep.sh && grep -qE 'parallel.*-gt 2|gt 2.*exit 3' run_matched2000_sweep.sh && ! grep -q 'lightning.qubit' run_matched2000.py && ! grep -q 'multiprocessing' run_matched2000_sweep.sh run_matched2000.py && echo HARNESS_OK</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n run_matched2000_sweep.sh` passes; `run_matched2000.py` parses
    - `grep 'xargs -P 2' run_matched2000_sweep.sh` matches; `--parallel >=3` rejection branch present (`exit 3`)
    - No `lightning.qubit` and no `multiprocessing` in either new file (D-14-11/12)
    - The strict accept gate uses `raise AssertionError(` (explicit-raise) for data_hash / seed-set / device-manifest / 2000ep-completion checks — grep confirms no bare `assert` guards the gate
    - After a sweep run, `results/matched2000/sweep_status.json` exists and every accepted artifact JSON has a `device`/`dtype` manifest field, `seeds` == `[42,43,44,45,46]`, and an `epochs` field == 2000
    - The 55-param reproduction artifact is tagged distinctly from `headline_canonical.json` (e.g. `source: "matched2000_reproduction"`) — never conflated (D-14-10)
  </acceptance_criteria>
  <done>Tiered resumable 2000ep sweep harness built on the proven xargs -P2 pattern; every model regenerated at 2000ep behind the strict accept gate with device-honest manifests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| training run → device manifest | a silent CPU/dtype fallback would put a false device in the paper table (R1-M5 honesty) |
| regenerated artifact → strict accept gate | an untrusted (wrong hash/seed/budget) artifact would poison the headline numbers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-04 | Spoofing | device/dtype manifest | mitigate | Per-run hard-assert of actual backend (explicit-raise); strict gate rejects artifacts whose manifest assertion failed (D-14-12/13) |
| T-14-05 | Tampering | mixed-budget / mixed-seed artifacts | mitigate | Strict accept gate: data_hash equality + seed set {42..46} + full-2000ep completion, explicit-raise; resumable status prevents silent mixed-budget contamination |
| T-14-06 | Tampering | headline vs reproduction conflation | mitigate | Distinct `source` markers; headline from frozen checkpoint only, reproduction tagged separately (D-14-10) |
| T-14-14 | Spoofing | checkpoint identity reuse | mitigate | sha256 of best_checkpoint.pt re-verified against Plan 01's lock JSON before headline generation |
</threat_model>

<verification>
- Headline JSON from frozen checkpoint (source marker, stored mu/sigma, fixed seed, device manifest)
- Sweep harness syntactically valid, xargs -P2, no lightning.qubit, no multiprocessing, parallel>=3 rejected
- Strict accept gate explicit-raise on data_hash / seed / device / 2000ep
- Accepted artifacts carry device/dtype manifest, seeds {42..46}, epochs 2000
</verification>

<success_criteria>
Every model has an accepted 2000ep artifact and the headline is generated from
the frozen checkpoint, all behind a device-honest strict accept gate — the
matched-budget re-execution (Tier-2/3 of D-14-22) is complete and the paper's
numbers are now coherent and traceable.
</success_criteria>

<output>
Create `.planning/phases/14-paper-revision-release-freeze/14-02-SUMMARY.md` when done
</output>
