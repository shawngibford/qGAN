---
context: phase
phase: 14-paper-revision-release-freeze
plan: 14-21-post
task: submission-ready (visual figure-batch fixes complete)
total_tasks: open-ended (cleanup arc complete)
status: paused_ready_to_submit
last_updated: 2026-06-16T16:55:00.000Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] **CONSTRAINT: post-hoc outlier removal is p-hacking** — Any future "should we drop X" question on outlier data must first answer: "where was the pre-registered exclusion criterion?" If the answer is "we noticed X after seeing the results", the answer to dropping is no. Use the robustness-check pattern instead.
- [ ] **CONSTRAINT: compile-clean does NOT mean visual-clean** — Promoted from advisory to blocking this session. User found 6 distinct visual issues (Fig 5 label collisions, Fig A4 5-sentence-title-bar, page-54 column-overlap, reconstruction explosion, DTW verbose titles, A25/A26 overlap) that all gates passed. The freeze + provenance gates do NOT inspect figure aesthetics or 2-column LaTeX float behavior. Before declaring any figure or layout change done: open the rendered PNG/PDF and look at it as a reviewer would. Use `pdftotext -layout -f N -l N` to spot-check column structure.
- [ ] **CONSTRAINT: \\begin{table} vs. \\begin{table*} in 2-column journals** — Wide tables in single-column \\begin{table} force prose wrap-around that interleaves text into table cells (page 54 here was Artifact reference table mid-collision with §A.6 prose). Any supp table with > 4 columns or > 0.5 \\linewidth must be \\begin{table*}.

**Do not proceed until all boxes above are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Compile-clean ≠ visual-clean | Promoted to blocking. 6 visual issues found this session that all freeze + provenance gates passed. None detectable from automated gate output. | blocking | Before declaring done: open rendered PNG/PDF, inspect visually. Use pdftotext -layout for 2-column spot-checks. |
| matplotlib suptitle abuse for caption text | Figures A4/A25/A26 had multi-sentence caption text embedded in fig.suptitle, rendering as huge title bars. Captions belong in LaTeX \\caption{}. | advisory | Keep matplotlib suptitle to one short line. Push explanatory text to \\caption{}. |
| \\begin{table} in 2-column AIChE layout | Single-column table block forces wrap-around in 2-col layout — interleaves prose into table cells. | blocking | Wide supp tables (>4 cols or >0.5 linewidth) MUST be \\begin{table*}. |
| Cumulative-integration display explosion | Compound bias in log-return sequences cumulative-integrates to OD values 40+ orders of magnitude off-scale (WGAN-CNN reaches 1e46), crushing the real-data reference trace at any autoscaled axis. | advisory | Clip display window from real-data scale; annotate overflow explicitly. |
| Post-hoc outlier removal would be p-hacking | Carried forward from session-3 pause. | blocking | Robustness-check pattern; never drop data without pre-registered criterion. |

<current_state>
Paper submission-ready at HEAD `29d15d2`. 83-page PDF, all gates green. Bundle synced at `~/Desktop/aiche_upload_post14-21/`. Deadline 2026-06-17 (~1 day remaining). Awaiting external portal upload (cannot be done by Claude).

Session-3b continuation after the prior pause at `d26c21e`. User did a visual page-by-page review of the supp PDF and surfaced 6 visual/layout regressions that the gates do not catch. All 6 fixed in commits `13b07bf` (the figure batch) + `29d15d2` (companion JSON sidecars from the render pass).
</current_state>

<completed_work>

This session (continuation after d26c21e):

- **Fig 5 (cross_model_emd) label collisions**: annotations now anchored to (mean + std), va='bottom', 1.6× y headroom. No more overlap with error-bar whiskers.
- **Fig A4 (tstr_crossmodel_matched2000)**: figsize 15×5.2 → 13×6.5, short panel titles, R² y-axis clamped to [-0.05, 1.10] with real-only baseline annotated as text box (was stretching to -13.85).
- **Fig A25 (shot_noise_robustness)**: 10×5.6 → 9×6.0, single-line title; multi-sentence caption text removed from title bar.
- **Fig A26 (noise_robustness_quantum)**: 13×5.4 → 12×6.0, panel titles shortened from "Depolarizing — monotonic↑ (A: monotonic_increase, B: monotonic_increase)" to just "Depolarizing" / "Amplitude Damping".
- **Page-54 LaTeX overlap**: Artifact reference table promoted from `\begin{table}` to `\begin{table*}` (2-column span). §A.6 prose no longer wraps into table cells. PDF +1 page (82→83).
- **Reconstruction figures**: linear-OD y-axis clipped to `[0, real_max × 3]` with "off-scale" text annotation when generated peak exceeds the window; log-OD clipped to `[real_min/10, real_max × 100]`. WGAN-CNN's 1e46 explosion no longer crushes the real-OD trace.
- **DTW alignment titles**: "DTW alignment — {model label} (seed 42, window=500, psi=2)" → just "{model label}". Protocol metadata stays in companion JSON + atlas LaTeX caption. Note: regen required `qgan_env/bin/python3` because dtaidistance isn't in /opt/homebrew Python.

PDF: 82 → 83 pages (+1 from table* widening). 0 errors, 0 undefined refs/cites. main 164 + supp 338 literals trace to JSON. Freeze gates a/b/c PASS.
</completed_work>

<remaining_work>

External actions only — Claude cannot complete these:

- **EXT-1 (blocking, deadline 2026-06-17)**: Zip + upload to AIChE portal
  - `cd ~/Desktop && zip -r aiche_upload_post14-21.zip aiche_upload_post14-21/`
  - Upload via AIChE submission portal
- **EXT-2 (blocking, deadline 2026-06-17)**: Compose cover letter in portal text box (talking points in bundle's UPLOAD-CHECKLIST.md)
- **14-07 (deferred to acceptance)**: Zenodo DOI mint + tag v1.2.5+ + release.md authoring + manuscript Data Availability update from "Zenodo upon acceptance" to real DOI
</remaining_work>

<decisions_made>

This session:

- **Reconstruction y-axis clip + off-scale annotation** instead of autoscale: WGAN-CNN's 1e46 peak made real-OD invisible at any autoscaled axis. Clipping to [0, real_max × 3] keeps real visible; annotation acknowledges catastrophe honestly.
- **Artifact reference table → \begin{table*}**: Cost +1 page; required for column layout correctness.
- **DTW renderer needs qgan_env**: dtaidistance not in /opt/homebrew. Documented for future re-renders.
</decisions_made>

<blockers>
- **External portal upload** (only user can do): Bundle ready. Deadline 2026-06-17.
</blockers>

## Required Reading (in order)

1. `.planning/HANDOFF.json` — machine-readable handoff with full session-3 + 3b state
2. `.planning/STATE.md` — project-level state (last updated session-2; resume should refresh if working further)
3. `revision/docs/reviewer_response.md` — current rebuttal letter aligned with the paper

## Critical Anti-Patterns (do NOT repeat these)

- **Compile-clean ≠ visual-clean (BLOCKING)**: Always eyeball rendered PNG/PDF after any figure or layout change. Use pdftotext for 2-col spot-checks.
- **matplotlib suptitle abuse**: Keep to one short line. Push caption text to LaTeX \caption{}.
- **\\begin{table} in 2-col AIChE layout**: Wide tables MUST be \\begin{table*}.
- **Cumulative-integration display explosion**: Clip to real-data scale; annotate overflow.
- **Post-hoc outlier removal**: Never drop data without pre-registered criterion.

## Infrastructure State

- **Bundle**: `~/Desktop/aiche_upload_post14-21/` synced at HEAD `29d15d2`. 83-page PDF (3.73 MB), 66+ files.
- **Git tags**: v1.2 through v1.2.4 on origin immutable. Next tag v1.2.5+ at AIChE acceptance via 14-07.
- **ama.bst**: tracked in git (session-2 fix). Do not remove.
- **dtaidistance**: required for DTW alignment renderer; use `qgan_env/bin/python3`, NOT /opt/homebrew python3.

<context>
Continuation of session-3 after a /gsd-pause-work checkpoint. The user resumed by opening the rendered supp PDF page-by-page and reporting visual problems that the gates had certified as clean. Six distinct fixes applied, all visual / layout regressions that the freeze + provenance gates do not detect.

The headline cluster-dominance claim and all numeric content are unchanged from the prior pause. This session is pure visual polish: figure aesthetics, LaTeX float behavior, title verbosity.

Final assessment unchanged from prior pause:
- ~40% major-revision-then-accept
- ~30% reject-with-resubmission
- ~20% reject outright
- ~10% accept with minor

The v2.1 follow-up (second cultivation + classical HPO sweep + clean the ×0.1 vestigial scaling) remains the paper that gets cited; this one earns the right to be reviewed.
</context>

<next_action>
Paper is submission-ready. No further code/prose edits recommended.

**On resume**:
1. Verify HEAD is still `29d15d2` and bundle is intact at `~/Desktop/aiche_upload_post14-21/`
2. Zip bundle: `cd ~/Desktop && zip -r aiche_upload_post14-21.zip aiche_upload_post14-21/`
3. Upload via AIChE submission portal (deadline 2026-06-17)
4. After AIChE acceptance: run Plan 14-07

If user requests further paper edits before submission:
- FIRST acknowledge the BLOCKING constraints at top
- For any figure regen or LaTeX layout change: render → eyeball → spot-check with pdftotext
- For wide tables: use \\begin{table*}
- Re-run a co-author audit swarm to identify regression risk before any structural changes
</next_action>
