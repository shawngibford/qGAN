---
context: phase
phase: 14-paper-revision-release-freeze
task: 22
total_tasks: 22
status: complete
last_updated: 2026-06-12T00:12:32.258Z
plan: 14-21-post (peer-review remediation cycle — NOT a formal plan artifact)
plan_status: complete
phase_status: partial (20/21 plans formally complete; 14-07 deferred to journal acceptance)
deadline: 2026-06-17 (AIChE resubmission, 5 days from session pause)
head_at_pause: 20c38ad
session_pivot_note: Session pivoted out of the formal GSD plan/execute framework. The 22-commit body of work between f85fd38 and 20c38ad was driven by 4 peer-review subagent reports + the user's co-author comment thread, NOT by a 14-22-PLAN.md scaffold. ROADMAP/STATE still show 20/21 progress; commits are in the audit trail but the plan-level artifact is missing.
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

- [ ] **CONSTRAINT: Training-time ×0.1 sites are byte-frozen.** Do NOT edit `revision/run_matched2000.py:281`, `revision/core/training.py:347,381,416`, `revision/run_baselines.py:205`. The 14-21 fix is INFERENCE-ONLY at the `samples.npy` load boundary via `revision/_wgan_unscale.py`. Editing the training-side sites would invalidate every checkpoint and every saved sample bundle.
- [ ] **CONSTRAINT: VAE and AR(2) samples must NOT be passed through `_unscale_wgan_samples`.** They are deliberately excluded from `_WGAN_KINDS` per Pitfall 3 — their on-disk samples are already in the canonical [-1, +1] space. The differential test on every regenerated JSON confirms VAE+AR(2) rows are bit-identical.
- [ ] **CONSTRAINT: Hard prohibitions §6 #1, #3, #4 remain in force; §6 #2 remains AMENDED.** (1) VAE = degenerate generation regime, NEVER posterior/variance collapse; (3) Real-data lag-1 ACF reference = −0.0641; (4) Pipeline B has NO Lambert W. Only §6 #2 was amended at the 14-21 T05 R3 checkpoint — quantum cluster dominates LR-EMD by ~15× on per-model means under the corrected pipeline. New constraint: do not over-claim per-SEED LR-EMD dominance.
- [ ] **CONSTRAINT: `revision/core/` is byte-frozen (D-14-22).** The 14-21 helper module is at `revision/_wgan_unscale.py` (top-level `revision/`, NOT inside `revision/core/`). Verify `git diff revision/core/` is empty before any commit.
- [ ] **CONSTRAINT: Tags v1.2 through v1.2.4 on origin must NOT be rewritten.** They're historical reference. New work caps at v1.2.5+ (14-07 cuts the tag at journal acceptance).

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Worktree base mismatch | Spawned a parallel agent (Agent B) via `isolation: "worktree"`; worktree was rooted at a pre-14-21 ancestor, not at current main HEAD. Agent committed clean work but the branch couldn't merge — its supp_material.tex baseline was missing all the 14-21 + Wave 1 fixes. Per-seed sensitivity tables had stale pre-x10-fix WGAN values. | blocking | Before spawning a worktree-isolated agent for content that depends on recent state, verify the worktree root with `git -C <worktree_path> rev-parse HEAD` matches expected. If mismatched, abort and re-spawn after fast-forwarding the worktree. |
| Session drift out of GSD framework | Started in `/gsd:resume-work`, drifted into ad-hoc peer-review-driven remediation without authoring a 14-22 PLAN.md scaffold. 22 commits landed in the phase's audit trail but the milestone progress counter (20/21) doesn't reflect them; ROADMAP doesn't list the remediation wave; SUMMARY artifact is absent. | advisory | When the user introduces a substantial new scope mid-session, pause to ask: "should this be a new plan (14-22)?" before executing. Even if the user prefers ad-hoc execution, drop a one-line marker in ROADMAP or STATE so the next session sees the work. |
| LaTeX text-mode underscore | Used `wgan_cnn` (underscore) in supp Welch table caption prose — would have broken LaTeX compile. | advisory | When writing LaTeX prose that includes underscored identifiers, use display names (`WGAN-CNN`) or escape underscores (`wgan\_cnn`). Compile-test after caption rewrites. |

<current_state>
Phase 14 sub-plan 14-21 + peer-review-remediation cycle are **COMPLETE end-to-end** at HEAD `20c38ad`. All BLOCKING items from the 4-agent peer-review swarm are closed; all 6 co-author comments addressed; ~28 numeric trims executed across Abstract/Contributions/§5; ~30 caption file-paths stripped; Figs 2-6 promoted to full-width; supp A6/A7/A11/A39/A40 sized for legibility; §A.7 disclosure extended with R1/R5 paragraphs from PR-4; main §5 has the preemptive "convenient-inversion" subsection; per-seed sensitivity tables for OD-EMD/OD-DTW/LR-EMD added to supp; Bonferroni multi-test correction footnote added.

**Phase 14 progress (formal)**: 20/21 plans complete. 14-07 (Zenodo DOI + tag v1.2.5+ + release.md) intentionally deferred to AIChE journal acceptance. The 22-commit remediation cycle between f85fd38 and 20c38ad is NOT counted in the milestone progress because no 14-22-PLAN.md was authored.

**AIChE resubmission state**: Camera-ready post-remediation bundle is clean — provenance gates PASS on all 6 reviewer-facing docs (main 157, supp 347, paper_blocks_framing 23, paper_blocks_refs_methods 49, reviewer_response 178, methods_full 133); LaTeX compile clean (**105 pages**, 0 errors, 0 undefined refs/cites — post 2026-06-12 `ama.bst`-restore commit; the 14-21-post PDF originally had 60 [?] in-text citations + missing References section because `ama.bst` was never git-tracked, freeze-gate didn't catch it because gates don't render PDF visually); freeze gates a/b/c PASS, gate d expected-deferred. Bundle assembled at `~/Desktop/aiche_upload_post14-21/` (65 files, 7.9 MB).

**Deadline**: 2026-06-17 (5 days from session pause at 2026-06-12).
</current_state>

<completed_work>

This session executed two distinct phases of work:

## Part 1 — Initial resume from 14-21 pause (4 commits, Waves 1.1–1.4)

Resumed from pause-work commit `34b7bff` (Phase 14 sub-plan 14-21 closed). Identified 3 BLOCKING items via 4-agent peer-review swarm (PR-1 citations, PR-2 figures, PR-3 prose, PR-4 scientific rigor):

- **a70959e** — refresh supp Welch tables to post-fix data + invert caption framing (BLOCKING-1 smoking gun: tbl:welch_od_emd caption literally said "no quantum-vs-classical pair is statistically resolved" contradicting main paper headline OD-EMD p=0.019)
- **87c6ebb** — Abstract "three of four" → "all four" matched-budget metrics (BLOCKING-2 internal inconsistency)
- **9ec0c37** — `revision/docs/reviewer_response.md` narrative drift closed: lines 211-216 "LR-DTW is the sole quantum-distinguishing result we claim" + lines 421-437 "no statistically detectable OD-scale EMD difference" rewritten to cluster-dominance reading (BLOCKING-3)
- **f85fd38** — strip `% [N] removed: ... (R1-m1)` audit-trail LaTeX comments from main.tex and supp_material.tex (PR-1 source-comment leakage)

## Part 2 — Full-scope remediation (18 commits, Waves 2–6)

User opted into full-scope remediation. Two parallel git-worktree agents (Agent A: main.tex + bib.bib; Agent B: supp_material.tex) executed Waves 2-5; Agent A merged cleanly; Agent B's branch was rooted at a pre-14-21 ancestor and couldn't merge — salvaged its high-value content templates and re-executed its task list directly on main.

- **3b481c1** — swap ref [42] `dimoudis2023utilizing` → `bergmeir2012rolling` in main.tex (rolling-window context)
- **c5b2fca** — swap ref [28] `Mugel2022` → `niu2022entangling` (a real QGAN paper) in main.tex
- **b541d0a** — add McClean 2018 + Holmes 2022 barren-plateau citations + defend "55-param PQC beats classical" claim in §3.2
- **e4f1136** — normalize main.tex citation style to grouped before-period
- **ab592e6** — Wave 4 prose polish in main.tex: trim Abstract/Contributions/§5 finding-numerics; add log-returns specific-growth-rate one-liner at §3; rewrite Plain Language Summary to 238 chars
- **524f89f** — rigor paragraphs in main.tex: OD-EMD inversion footnote at §4.1 (PR-4 R3); preemptive "Note on the inference-pipeline correction" subsection at §5 (PR-4 R9 — single highest-leverage addition for surviving hostile review)
- **10eae71** — Wave 3 main figures: strip 7 caption file-path traces; promote Figs 2-6 from `figure` to `figure*` + `\linewidth`; expand main Data Availability section
- **ece127a** — merge Agent A branch (7 atomic commits) into main
- **d8c64cd** — supp §A.7 R1/R5 disclosure additions: "Note for reviewers comparing to v1.2.4" + "Relative-fidelity impact" + "Acceptance-gate amendment" paragraphs
- **884320b** — swap dimoudis → bergmeir/sakoeChiba in supp_material.tex (3 sites: supp:287 DTW intro → sakoeChiba1978; supp:833 + supp:1442 rolling-window → bergmeir2012rolling)
- **eabfd3f** — strip supp caption file paths + 5 GSD phase IDs ("Phase 13 INTRO-01 deliverable" etc.)
- **850ada0** — supp figure sizing: promote A6/A7/A39/A40 to full-width; restructure A11 (per_model_loss_grid) from 4x2 at 0.24linewidth to 2x4 at 0.48linewidth + add explicit (a)-(h) subpanel labels + fix broken body cross-references at supp:905, 911, 926
- **2ee8ecc** — expand supp §A.5 Data Availability with per-figure provenance mapping
- **8e37dc2** — per-seed sensitivity tables for OD-EMD/OD-DTW/LR-EMD + multiplicity-correction note + Bonferroni-corrected counterparts (PR-4 R4 + R2.2 — the deferrable items the user opted into)
- **20c38ad** — commit the 4 PR audit reports (`.planning/peer-review-2026-06-11/PR-{1-citations,2-figures,3-prose,4-rigor}.md`) for audit trail

## Verification + bundle (Wave 6)

- LaTeX compile: 105 pages, 0 errors, 0 undefined refs/cites (post 2026-06-12 `ama.bst`-restore rebuild; originally 99 pages with 60 [?] citations and no References section because `ama.bst` was never git-tracked)
- Provenance gates: main 157, supp 347, paper_blocks_framing 23, paper_blocks_refs_methods 49, reviewer_response 178, methods_full 133 — all PASS
- Freeze gates a/b/c PASS; gate d expected-deferred to 14-07
- Bundle rebuilt at `~/Desktop/aiche_upload_post14-21/` with refreshed UPLOAD-CHECKLIST.md

</completed_work>

<remaining_work>

**External human actions (BLOCKING for AIChE submission):**

1. **Zip bundle and upload to AIChE portal.** Bundle ready at `~/Desktop/aiche_upload_post14-21/`. Command: `cd ~/Desktop && zip -r aiche_upload_post14-21.zip aiche_upload_post14-21/`. Deadline 2026-06-17. UPLOAD-CHECKLIST.md inside the bundle has portal upload sequence + cover-letter talking points.

2. **Compose cover letter on portal.** Cover letter is composed in the portal's cover-letter text box, not as a file. Talking points in the bundle's UPLOAD-CHECKLIST.md (inference-pipeline correction disclosure, all 5 reviewers addressed, co-author 6 comments closed, reproducibility via GitHub + Zenodo placeholder).

**Optional cleanups (non-blocking):**

3. **Retrospectively author 14-22-PLAN.md + 14-22-SUMMARY.md** for the peer-review remediation cycle. ROADMAP.md + STATE.md still reference 20/21 plans complete; the 22 remediation commits between f85fd38 and 20c38ad are not captured as a discrete plan artifact. Can be reverse-engineered from commit log + the 4 PR reports in `.planning/peer-review-2026-06-11/`.

4. **(DONE — completed in pause-work + ama.bst-restore commits)** STATE.md frontmatter + body now cite HEAD `20c38ad` (+ ama.bst-restore HEAD) / 105-page PDF / 157+347 provenance counts.

**Held until journal acceptance:**

5. **Plan 14-07** — Zenodo DOI mint + tag v1.2.5+ + release.md authoring. Long-standing user deferral. Freeze gate (d) is expected-deferred until 14-07 lands.

</remaining_work>

<decisions_made>

- **Full-scope remediation** (per-seed sensitivity tables + Bonferroni footnote + leave-wgan_cnn-out sensitivity) — user opted into the strongest-possible-submission scope at the AskUserQuestion checkpoint.
- **Refresh process docs (peer_review_remediation.md, reconciliation_note.md) with supersession banner** rather than full narrative rewrite — preserves audit history; docs don't ship in AIChE bundle anyway.
- **Strip R1-m1 audit-trail comments** — PR-1 smoking gun: the comment leak showed the author noticed the ref [42] issue during R1, removed something else, and left the actual offender in place.
- **bergmeir2012rolling for rolling-window + sakoeChiba1978 for DTW canon** — context-fit per use site (supp DTW intro needed a canonical DTW citation, not a rolling-window citation).
- **niu2022entangling instead of dropping the Mugel2022 clause** — Agent A judgment call: keeping a real QGAN reference strengthens the §1/§2.2 prior-art framing.
- **Bundle on Desktop at `aiche_upload_post14-21/`** — user chose Desktop assembly. Name avoids `v1.2.5` (reserved for 14-07 tag at AIChE acceptance).
- **Two parallel git-worktree agents** for Waves 2-5 — user requested swarm parallelism with inline bash. Agent A merged cleanly. Agent B's worktree base was pre-14-21, branch couldn't merge — salvaged content templates and re-executed directly.
- **Per-seed sensitivity tables regenerated from current matched2000_dualscale.json#rows** (post-x10-fix) — NOT lifted from Agent B's branch which had stale pre-fix WGAN values (would have reintroduced the bug).
- **Fig A11 restructure 4×2 → 2×4 + add explicit (a)-(h) subpanel labels** — PR-2 confirmed illegibility at print size AND broken body cross-references at supp:905, 911, 926.

</decisions_made>

<blockers>

- **No technical blockers**. All gates pass at HEAD `20c38ad`; bundle is ready to upload.
- **AIChE resubmission deadline**: 2026-06-17 (5 days from session pause). The remaining work is external (zip + portal upload).

</blockers>

## Required Reading (in order)

1. `.planning/STATE.md` — currently STALE (2026-06-01 / v1.2.4 / 60pp); helpful for project shape but numbers are outdated.
2. `.planning/HANDOFF.json` — machine-readable mirror of this doc with full task ledger.
3. `.planning/peer-review-2026-06-11/PR-{1-citations,2-figures,3-prose,4-rigor}.md` — the 4 audit reports that drove the remediation plan. Cite these if a reviewer asks about specific decisions.
4. `~/Desktop/aiche_upload_post14-21/UPLOAD-CHECKLIST.md` — portal upload sequence + cover-letter talking points + 5 pre-upload sanity checks.
5. `supp_material.tex` §A.7 (lines 737–800) — the inverse-pipeline disclosure paragraph chain (Note for reviewers + Convention/origin + Inference-side correction + VAE/AR(2) exclusion + Relative-fidelity impact + Residual mean-bias + Acceptance-gate amendment). Load-bearing for surviving hostile review.
6. `main (4) copy.tex` §5 "Note on the inference-pipeline correction and the v1.2.4 narrative" — preemptive defense against the "convenient finding-inversion" attack line (PR-4 R9 verbatim).
7. `.planning/phases/14-paper-revision-release-freeze/14-21-SUMMARY.md` — the 14-21 fix audit trail (origin of the WGAN sample-space convention; the helper module; differential test results).

## Infrastructure State

- **Branch**: `main`, multiple commits ahead of `origin/main`, NOT pushed.
- **HEAD**: `20c38ad` — `docs(14-21-post): commit peer-review-2026-06-11 reports for audit trail`.
- **Tags on origin**: v1.2.4 is the most recent. New fix work caps at v1.2.5+ (cut by 14-07 at acceptance).
- **Working tree at pause**: clean.
- **PDF state**: `main (4) copy.pdf` at 105 pages, 3.9 MB, clean compile (post 2026-06-12 `ama.bst`-restore rebuild; originally 99 pages with 60 [?] citations + no References section).
- **Bundle state**: `~/Desktop/aiche_upload_post14-21/` ready to zip + upload.
- **Worktrees from this session**:
  - `.claude/worktrees/agent-af66716675fa24d3b/` (Agent A — merged via ece127a, can be cleaned)
  - `.claude/worktrees/agent-a2cc81c0e4e021515/` (Agent B — NOT merged; pre-14-21 base; can be deleted since its work was salvaged manually)
- **Gate state at pause**: provenance gates PASS on 6 reviewer-facing docs; v2.1 differential test PASSED (last run at 14-21 close); freeze gates a/b/c PASS; gate d expected-deferred.

<context>

Mental state at pause: 22 atomic commits of post-peer-review remediation, executed across 5 waves. The work is technically complete and the bundle is ready to upload — the only remaining action is the human portal interaction. The session ran ~6 hours of wall clock.

The session pivoted out of the formal GSD plan/execute framework partway through. Started as a resume-work pickup; the user introduced co-author comments + requested a peer-review subagent swarm; the resulting body of work landed without a 14-22-PLAN.md scaffold. ROADMAP/STATE counter still shows 20/21 plans complete. Two options for the next session:

- **Option A (recommended)**: retrospectively author 14-22-PLAN.md + 14-22-SUMMARY.md from the commit log + the 4 PR reports. Bump milestone progress to 21/22. Phase 14 closes when 14-07 lands at AIChE acceptance.
- **Option B**: fold the remediation into a 14-23 plan that wraps Phase 14 completion (a single plan covering "post-acceptance + camera-ready", scope to be defined when acceptance arrives).

Bundle is byte-clean. Provenance gates pass. Freeze gates a/b/c pass; gate d is the expected Zenodo deferral.

</context>

<next_action>
1. Read `.planning/HANDOFF.json` + this `.continue-here.md`.
2. Surface AIChE bundle upload status with user — has it been uploaded? (Deadline 2026-06-17.)
3. Run `qgan_env/bin/python revision/verify_freeze_ready.py` inline to confirm state still PASSes (expect gates 0/a/b/c PASS, gate d expected-deferred).
4. Decide on Option A (retrospectively author 14-22 plan artifact) vs Option B (fold into 14-23) — the user's preference here drives whether to spend the next ~30 min on documentation cleanup.
5. **(DONE)** STATE.md frontmatter + body now cite the current HEAD / 105-page PDF / 157+347 provenance counts.
6. If user confirms upload succeeded, can transition Phase 14 status to PARTIAL-AWAITING-ACCEPTANCE.
</next_action>
