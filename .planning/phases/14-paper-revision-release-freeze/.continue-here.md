---
context: phase
phase: 14-paper-revision-release-freeze
task: 0
total_tasks: 7
status: paused_pre_execution
last_updated: 2026-06-10T17:57:08.508Z
plan_artifact: /Users/shawngibford/.claude/plans/cached-tumbling-starfish.md
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These were discovered through actual failure in this session. Acknowledge each one before proceeding.

- [ ] **CONSTRAINT: x0.1 attenuation is NOT a code error to "rip out" — it is the saved-sample contract.** Training-time and sample-export sites apply `* 0.1` (`revision/run_matched2000.py:281`, `revision/core/training.py:347,381,416`, `revision/run_baselines.py:205`). `samples.npy` on disk for all WGAN-trained models is in `[-0.1, +0.1]` space. The fix is INFERENCE-ONLY (multiply by 10 inside `reconstruct_od` and downstream renderers). Do NOT remove the `* 0.1` from training/export — that would invalidate all existing checkpoints.

- [ ] **CONSTRAINT: VAE and AR samples must NOT have x10 applied.** Per RESEARCH Pitfall 3 (`revision/run_baselines.py:28`), VAE and AR were trained WITHOUT the `* 0.1`. Their `samples.npy` is already in `[-1, +1]` space. The fix gate must check model_kind ∈ {iqp_sel_55_repro, V1, V2, V3, wgan_mlp, wgan_cnn, wgan_lstm}.

- [ ] **CONSTRAINT: Paper's bifurcated finding (`quantum > classical on LR-DTW`) may shift qualitatively after the fix.** Empirically: undoing x0.1 takes IQP:SEL gen std from 0.085x real → 0.848x real, and changes LR-EMD from published 0.0149 → 0.0034. DTW slightly worsens. The published v1.2.4 headline numbers are derived from the bugged inverse pipeline. After fix, re-assess §4.1, §4.2, §5 narrative. User has accepted this risk but said "surface the bifurcated-finding shift before tagging."

- [ ] **CONSTRAINT: Hard prohibitions from `CONTEXT-HANDOFF-2026-06-02.md` §6 remain in force.** Specifically: (1) VAE is a degenerate generation regime, NEVER posterior/variance collapse; (2) LR-DTW (not LR-EMD) is the surviving quantum-distinguishing signal — do not reframe even if numbers shift; (3) lag-1 ACF reference is −0.0641; (4) Pipeline B = NO Lambert W.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|----------------------|
| Blindly trust agent attributions | First Explore agent attributed the historical 3-panel figure to `archive/qwxlstmgan_pennylane.py`. User corrected: it was IQP:SEL (`archive/qgan_pennylane_SEL.py`). | blocking | When any agent attributes historical code, verify by reading the file directly before acting on it. |
| Assume visualization choices fix model issues | First reconstruction overlay attempt used "drift correction" (subtract gen mean, add real mean) as a visualization band-aid. The real issue was the x0.1 inverse-pipeline bug. Drift correction masked it. | blocking | Before applying any post-hoc correction, compute the gen-vs-real moment ratios. If std ratio is far from 1.0, investigate the inverse pipeline before patching the visualization. |
| Treat "VERBATIM from cell 26" as immutable | The current code preserves the x0.1 verbatim with provenance comments. But cell 26's x0.1 was designed for a different pipeline (raw log-returns at [-0.08, +0.09]). When Pipeline B changed the real-sample range to [-1, +1], the x0.1 should have been removed. Verbatim preservation across pipeline changes is dangerous. | advisory | Any code with "VERBATIM from cell N" comment should be re-validated against the current pipeline. |
| Skip the architecture-vs-training distinction | Initial hypothesis blamed the under-dispersion on reduced architecture (iqp_sel_55 vs default_75). Disproven by V1 (original architecture) showing identical 13x std deficit. | advisory | When all variants of a quantum/classical family show uniform pathology, the cause is shared infrastructure (training loop, inverse pipeline), not architecture. |

<current_state>
Investigation complete; execution not started. The bug is fully diagnosed and the user approved a 7-step fix plan. We are about to convert the plan into a GSD sub-plan and execute it in a fresh session.

Last commit on `main`: `8f7b019 fix(paper-rewrite): repair abstract math rendering + clarify power phrasing`. Branch is 4 commits ahead of `origin/main` (the four §A.6/§A.10/§A.11+§A.12/abstract commits from this session). Working tree is clean. Tag at `v1.2.4`.

The §A.10 reconstruction overlays committed at c4f28b5 are LOAD-BEARING for the bug visibility — they exposed the variance attenuation. Whether to keep them after the fix or replace them with a different visualization is a Step 3 decision.
</current_state>

<completed_work>

This session's diagnostic work (all results captured in the approved plan and in HANDOFF.json):

- **Verified dual-basis (X+Z) measurement matches between original `qgan_pennylane.ipynb` Cell 26 and current `revision/core/models/quantum.py:257-259`.** Both return interleaved `[<X_0>, <Z_0>, ..., <X_4>, <Z_4>]`. User's hypothesis that this was the bug — disproven.

- **Located the smoking-gun x0.1 origin at `archive/qgan_pennylane_SEL.py:661-663`.** The comment makes the intent explicit: "Real data is roughly in [-0.08, 0.09], so scale by ~0.1". The x0.1 was designed for RAW log-returns. Current Pipeline B standardizes real to [-1, +1] but preserved the x0.1 verbatim. This is the structural bug.

- **Empirically confirmed.** Multiplying samples by 10 before applying `((s+1)/2)*(r_max-r_min)+r_min`:
  - IQP:SEL: gen std 0.085x → 0.848x of real
  - V1: 0.073x → 0.726x
  - V2: 0.071x → 0.748x
  - V3: 0.115x → 1.160x

- **Verified the published metrics were computed in the bugged sample space.** IQP:SEL seed-42 LR-EMD published value 0.0149 matches my AS-IS (x0.1-attenuated) computation EXACTLY. With fix: 0.0034.

- **Inventoried all consumers of the buggy inverse pipeline.** Two `reconstruct_od` call sites (`run_dualscale_fidelity.py:206`, `run_figure_suite.py:261`) plus six per-model renderers in `run_figure_suite.py`.

- **Identified secondary issue (NOT fixed by this plan).** Even after x10 fix, gen samples mean in [-1,+1] space is ≈ 0 while real's Pipeline-B mean is ≈ −0.03. Causes residual ~2x OD drift. Fixable only by retraining; not in scope.

- **User approved the inference-only x10 fix plan** at `/Users/shawngibford/.claude/plans/cached-tumbling-starfish.md`.
</completed_work>

<remaining_work>

Per the approved plan (7 steps):

1. **Step 1 — Patch inverse pipeline (2 call sites + 6 renderers).** Add `_WGAN_KINDS` set and `_unscale_wgan_samples(samples_pm1, model_kind)` helper. Wire into `reconstruct_od` at `run_dualscale_fidelity.py:206` and `run_figure_suite.py:261`. Wire into all renderers that load `samples.npy` directly.

2. **Step 2 — Re-run all matched-budget metric JSONs.** `matched2000_dualscale.json` (~30 min), `welch_pairwise.json` (~5 min), `cross_model_emd.json` + `cross_model_dtw_dualscale.json` + `cross_model_acf_overlay.json` (~15 min), `tstr_matched2000.json` (~10 min).

3. **Step 3 — Regenerate all figure assets.** Full figure suite + §A.10/§A.11/§A.12 specifically. About 200+ PDF/PNG/JSON files.

4. **Step 4 — Update paper literals.** Use `verify_number_provenance.py` failure list as the punch list. Expected 20-50 in main + 50-100 in supp.

5. **Step 5 — Re-assess bifurcated finding.** If LR-DTW dominance shrinks/disappears, rewrite §4.1, §4.2, §5 narrative. **HIGH RISK — surface to user before locking in narrative changes.**

6. **Step 6 — Update abstract + contributions list.** Lines 49 + 105 of `main (4) copy.tex`. Likely a whole rewrite of the bifurcated-finding paragraph.

7. **Step 7 — Disclosure paragraph.** Add to `supp_material.tex` explaining the x0.1 + x10 inverse correction + Pitfall 3 asymmetry.
</remaining_work>

<decisions_made>

- **Inference-only fix (no retraining).** User accepted x10 at inverse vs multi-day retrain. Mean-drift residual disclosed but out of scope.
- **Gate by model_kind ∈ WGAN set.** VAE and AR excluded per Pitfall 3 design.
- **Accept that v1.2.4 headline numbers will change.** User explicitly chose "fix everywhere" over the narrower "fix only visualizations" option.
- **Use GSD framework (not direct execution) for the fix.** Atomic commits + checkpoint protocol needed; current context is exhausted.
- **Tag direction:** the fix will roll into `v1.2.5` or higher once the post-fix metrics are confirmed. v1.2 through v1.2.4 stay on origin as historical reference.
</decisions_made>

<blockers>

- **Bifurcated finding may shift qualitatively** — `scientific_uncertainty`. Workaround: pause execution at Step 5 if shift is dramatic, confirm direction with user before continuing to Step 6 (abstract rewrite).
- **Frozen pre-v1.0 headline checkpoint sample storage unknown** — open question: does `revision/results/headline_canonical.json`'s samples have x0.1 applied? If yes, frozen-checkpoint headline OD-DTW=0.6843 also shifts. Verify in Step 2.
- **No active GSD sub-plan for this fix yet.** Phase 14's latest sub-plan is 14-20 (complete). The resuming agent must create a new sub-plan (likely 14-21) or argue for a new Phase 15.
</blockers>

## Required Reading (in order)

1. `~/.claude/plans/cached-tumbling-starfish.md` — the approved 7-step plan. THIS IS THE SOURCE OF TRUTH.
2. `.planning/HANDOFF.json` — machine-readable state mirror of this doc.
3. `.planning/CONTEXT-HANDOFF-2026-06-02.md` — project-wide context (load-bearing prohibitions, gate suite, current state at v1.2.4).
4. `.planning/phases/14-paper-revision-release-freeze/14-20-SUMMARY.md` — latest completed sub-plan in Phase 14; provides the immediately preceding context.
5. `archive/qgan_pennylane_SEL.py:661-663` — the smoking-gun comment that proves the x0.1 was for a different pipeline.
6. `revision/run_dualscale_fidelity.py:194-236` and `revision/run_figure_suite.py:261-296` — the two `reconstruct_od` call sites that need patching.
7. `revision/run_baselines.py:28` — RESEARCH Pitfall 3 documentation defining which models inherit x0.1.

## Critical Anti-Patterns (do NOT repeat these)

- **ANTI-PATTERN: Trusting agent attributions without verifying** → Read the file directly before acting on any historical-code claim from an Explore agent.
- **ANTI-PATTERN: Applying drift correction as a visualization band-aid** → Diagnose the inverse pipeline before patching the visualization. If gen-vs-real std ratio is far from 1.0, the bug is upstream.
- **ANTI-PATTERN: "VERBATIM from cell N" comments as immutable** → Verify the verbatim code is still correct in the current pipeline. The x0.1 is the canonical example of this failure.

## Infrastructure State

- **Branch**: `main`, 4 commits ahead of `origin/main`, NOT pushed.
- **Tag**: `v1.2.4` is the most recent tag on origin. New fix work will cap at `v1.2.5` or higher.
- **Working tree**: clean (no uncommitted changes).
- **PDF state**: `main (4) copy.pdf` was last compiled at the end of this session, 93 pages, includes all four added supp subsections (A.6 circuit family, A.10 reconstruction, A.11 stat grids, A.12 DTW alignment) and the abstract fix.
- **Gate state at pause**: provenance PASS (main 150 literals, supp 210 literals), differential test PASS, freeze gates a/b/c PASS, gate d expected-deferred to plan 14-07.

## Pre-Execution Critique Required

- **Design artifact**: `/Users/shawngibford/.claude/plans/cached-tumbling-starfish.md`
- **Critique focus**:
  - Confirm the `_WGAN_KINDS` set is complete and correct (no missing model_kind).
  - Confirm the helper signature handles both shaped arrays (single sample, batched) correctly.
  - Confirm Step 2's re-run list covers ALL downstream consumers (search for `samples_pm1`, `samples.npy`, `np.load.*samples` to be sure).
  - Confirm Step 5's bifurcated-finding re-assessment has a clear early-exit if the finding fundamentally inverts (e.g. classical now dominates LR-DTW).
- **Gate**: Do NOT begin Step 1 implementation until plan-phase has validated goal-backward and the gsd-plan-checker agent has signed off.

<context>
Mental state at pause: the diagnosis is rock-solid (smoking gun in code comments, two empirical confirmations, three agent investigations converging on the same conclusion), the plan is detailed, the user is informed and committed. The work that remains is mechanical execution + paper rewriting. The hardest part — figuring out WHAT was wrong — is done. The risky part — re-assessing the bifurcated finding — is Step 5 and has a built-in pause point.

The user is in the middle of a paper-submission cycle for AIChE Journal aic-4719598 with a ~2026-06-17 resubmission deadline (7 days out as of 2026-06-10). Whether to ship the fix BEFORE the resubmission or AFTER is itself a decision point — surface this immediately when resuming so the user can plan timing.
</context>

<next_action>
1. Read this `.continue-here.md` end-to-end and acknowledge all blocking constraints.
2. Read `/Users/shawngibford/.claude/plans/cached-tumbling-starfish.md` — the approved 7-step plan.
3. Surface the deadline-timing question to the user: "AIChE resubmission deadline is ~2026-06-17 (7 days). Do you want this fix in the resubmission, or do you want to ship v1.2.4 as-is and submit the fix as a corrigendum post-acceptance?"
4. After user answers timing, invoke `/gsd:phase add` to create sub-plan 14-21 (or new Phase 15) with title "Fix x0.1 WGAN inverse-pipeline bug; re-run matched-budget metrics + figures + paper updates" and reference the plan file.
5. Then `/gsd:plan-phase` to formalize the plan into a Phase 14-21 PLAN.md with goal-backward validation.
6. Then `/gsd:execute-phase` with checkpoint protocol enabled.
</next_action>
