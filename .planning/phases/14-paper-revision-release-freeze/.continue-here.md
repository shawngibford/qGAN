---
context: phase
phase: 14-paper-revision-release-freeze
plan: 14-21-post
task: figure-size-clarification
total_tasks: open-ended (figure-clarification + section-walkthrough + optional Tier 3)
status: paused_mid_clarification
last_updated: 2026-06-12T18:08:21.969Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure
> this session or in earlier 14-21 work. Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: **The freeze gate does not visually render the PDF.** `revision/verify_freeze_ready.py` checks git cleanliness + gitignore + provenance-md-trace + tag-scope. It does NOT load the PDF and grep for `[?]` markers. The post-14-21 bundle PDF (HEAD `20c38ad`) had 60 `[?]` citation placeholders + no References section because `ama.bst` was never git-tracked, yet all freeze gates a/b/c PASSED. Discovered this session by user noticing `[?]` visually. Structural mitigation: never trust the gate's "clean compile" claim alone — when in doubt, render the PDF and pdftotext-grep for `[?]`.
- [ ] CONSTRAINT: **`ama.bst` MUST stay tracked in git.** It was never tracked, bibtex silently failed, .bbl was 0 bytes, all 60 citations rendered as `[?]`, the entire References section was absent — yet the freeze gate passed and STATE/HANDOFF/ROADMAP all claimed "0 undefined refs/cites". The file is now at the repo root and committed (md5 `1e72abd07faa985b9b8e0bebf4952b7c`, identical to the v1.2.1/v1.2.4 bundle copies). Do NOT remove it.
- [ ] CONSTRAINT: **`% source:` gate-trace comments in §A.8 critic paragraph are load-bearing.** Specifically the two lines above the `250881` literal in the critic-architecture section of `supp_material.tex`. The provenance gate v2.2 mechanism reads these comments to resolve every numeric literal to its JSON source. Editing/removing them silently breaks the provenance gate without an error message at the supp.tex parse level (you'll only see the failure when the gate runs).

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Untracked LaTeX assets | `ama.bst` (and any other externally-sourced .bst/.cls/.sty) silently absent from git. bibtex fails opaquely, citations render as `[?]`, no error surfaces in the pdflatex log. | blocking | Track every non-vendored TeX asset in git from day one. Add a CI/precommit check: `find . -name '*.bst' -o -name '*.cls' | xargs git check-ignore` should return empty. |
| Gate-passes-broken-output | The freeze gate's "PASS" result certified the broken PDF for 12 days before the user noticed. Meta-doc gates that don't render the artifact will lie. | blocking | The gate must also do: `pdftotext --layout main.pdf - | grep -c '\\[\\?'` and fail if > 0. Out of scope for this session but flagged for 14-07 or a follow-up commit. |
| Inline path strings as cite mechanism | 24 inline `\texttt{revision/...}` paths cluttered prose, made the doc hard to read, and inflated reviewer cognitive load. | advisory | Now resolved: artifact registry table at §A.5 with R/C/D ID system. Future authors should add new artifacts as R<N>/C<N>/D<N> rows to Table A3 and use the ID in body prose. |
| Hyphens as bullets | Wave-4.4 swarm restructured §1.4 Principal Contributions but used literal `- ` at line start, which rendered as paragraph-leading hyphens, not bullets. | advisory | Always use `\begin{itemize}[nosep, leftmargin=*]` + `\item` + `\textbf{label}` (the convention already used at §4.4 "Scope of the matched-budget finding"). |

<current_state>

Session-2 of post-14-21 work. Started as a `/gsd:resume-work` pickup of `9ba37f8` (pause-work state where bundle was already on Desktop). Discovered the bundle PDF was silently broken: 60 `[?]` citation placeholders + missing References section because `ama.bst` was never git-tracked. Fixed across 4 commits:

1. **`76e0916`** — Restored `ama.bst`, rebuilt PDF (99→105 pages with References restored), updated 5 stale meta-docs.
2. **`91702f3`** — §1.4 Principal Contributions: literal hyphens → proper `\begin{itemize}` bullets.
3. **`b13f38a`** — Supp Tier 1 reduction (5 LOW-risk moves): 105 → 95 pages.
4. **`543489b`** — Supp Tier 2 (6 structural moves) + JSON pointer consolidation (Table A3 artifact registry + 24 inline-pointer replacements): 95 → 84 pages.

**Total session reduction: 105 → 84 pages (-21 pp, 20%).** All gates PASS at HEAD `543489b`. Bundle at `~/Desktop/aiche_upload_post14-21/` synced.

**Paused mid-AskUserQuestion**: User said "we also need to make sure that all of the figures/plots in main all large enough to be seen and read properly, we should make them span both columns" — but **all 6 main figures are already `\begin{figure*}`** (full-width). I posed a clarification question (which figures? Fig 1 normalisation? supp atlas re-layout? specific figure?) and user invoked `/gsd:pause-work` instead of answering.

</current_state>

<completed_work>

This session (4 commits, ~3-4h wall clock):

**Commit `76e0916` — `ama.bst` restore + PDF rebuild + meta-doc refresh**
- Tracked `ama.bst` in git (was never tracked)
- pdflatex → bibtex → pdflatex → pdflatex sequence: 99 pp (broken) → 105 pp (clean)
- 60 `[?]` placeholders → 0; References section now rendered (142 numeric citations resolved)
- Updated stale "99 pages / 0 undefined cites" claims in STATE.md / HANDOFF.json / ROADMAP.md / `.continue-here.md` / bundle UPLOAD-CHECKLIST.md (5 meta-docs)
- Bundle PDF overwritten

**Commit `91702f3` — §1.4 bullets fix**
- 4 contributions were `- text` (rendered as paragraph-leading hyphens) → `\begin{itemize}[nosep, leftmargin=*]` + `\item \textbf{Label:}` pattern
- No content change, only formatting

**Commit `b13f38a` — Supp Tier 1: 105 → 95 pages, -10 pp**
- CUT-2: §A.7 Rolling Window subsubsection (duplicate of main §3)
- CUT-3: §A.13 per-layer critic specs → JSON pointer (250881 literal preserved)
- CUT-1 light: §A.3 Hybrid-GAN integration/regularization prose (Fig A3 mech_rep KEPT, Table A2 KEPT)
- TRIM-5: §A.9 loss diagnostic prose tightening (hedging cut, substantive findings preserved)
- MERGE-3: §A.12 DTW alignment 9 figs → single 3×3 atlas at 0.32\linewidth (PILOT)

**Commit `543489b` — Supp Tier 2 + JSON pointer consolidation: 95 → 84 pages, -11 pp**
- CUT-5: §A.2.3 quantum-fundamentals textbook content → 1 paragraph
- MERGE-6: §A.3 subsubsection headers → `\paragraph{}` labels
- TRIM-4: §A.6 four per-ansatz `\paragraph{}` blocks → 1 summary sentence + Fig A4 pointer
- TRIM-6: §A.10 and §A.11 intro prose tightened; panel-layout details moved into first-figure captions
- MERGE-1: §A.10 9 reconstruction figs → 3×3 atlas (3-row internal layout per panel — MEDIUM risk)
- MERGE-5: §A.13 critic content relocated before §A.8, new "Model Architecture and Quantum Circuit Introspection" subsection
- **JSON pointer consolidation:** new artifact reference table at §A.5 with 33 IDs (R1-R21 results, C1-C7 code, D1-D4 docs); 24 inline `\texttt{revision/...}` references across main + supp replaced with table IDs; main Data Availability section condensed from 25-line bullet list to 4-line summary; `% source:` gate-mechanism comments preserved

</completed_work>

<remaining_work>

**BLOCKING: User must clarify figure-size concern before any figure edits.**

All 6 main figures are already `\begin{figure*}` (full-width). The user's request "make figures span both columns" is technically already satisfied for main.tex. Three plausible scopes:

1. **(a) Main Fig 1 normalisation only.** Concept_diagram uses fixed `[height=7cm, width=17cm]` instead of `\linewidth`. If AIChE's text-width is ≠ 17 cm, this could marginally under- or over-fit. Trivial defensive fix.
2. **(b) Supp atlas re-layout.** The new 3×3 atlases (§A.12 DTW + §A.10 reconstruction) have each subpanel at 0.32\linewidth. The reconstruction atlas in particular has internal 3-row stacked sub-plots per panel — Agent A flagged MEDIUM legibility risk. 2-page spread fallback at 0.45\linewidth would save ~5 pages instead of ~7 (recovers ~2 pp of saving). Requires visual confirmation.
3. **(c) Specific figure** the user noticed looks small but I haven't identified.

When resuming, re-pose the clarification, then act on the chosen scope.

**Other open items (non-blocking):**

- **Section walkthrough** (Tasks #7–#10): §2 Background, §3 Methods, §4 Results, §5 Conclusion. Paused before §2. Each section needs the same treatment Task #5 (Abstract) + Task #6 (§1) got: what's said + what's the receipt + anything off.
- **Orlandi-future-work paragraph** at §4.4 Limitations or Outlook — DEFERRED from §1.3 review per joint decision earlier this session. Plan: "Direct cross-protocol replication. The present matched-budget protocol holds the WGAN-GP training machinery fixed […]. It does not include direct replication of published QGAN protocols with their own hyperparameter conventions (e.g., the Orlandi et al. finance protocol with λ=10, n_critic=5). Cross-protocol replication […] is identified as immediate future work."
- **References review** (Task #12): 62/64 cite keys used; 2 unused (`Mugel2022`, `dimoudis2023utilizing`) are pre-existing peer-review-swap leftovers — could delete for cleanliness.
- **Tier 3 supp reduction** (optional): ~6–10 more pages possible. MERGE-2 stat_grid atlas + MERGE-4 §A.2 + CUT-4 Wasserstein + TRIM-1/2/3. MEDIUM-HIGH risk on MERGE-2 (6-panel insets at small size). CUT-4 requires coordinated `main.tex:145` edit. Only run if 84 pages is unacceptable.
- **AIChE bundle upload** (BLOCKING, deadline 2026-06-17): zip + portal upload + cover letter. Manual.
- **Plan 14-07** (held until AIChE acceptance): Zenodo DOI mint + tag `v1.2.5+` + `release.md`.

</remaining_work>

<decisions_made>

This session:

- **Orlandi replication is a follow-up paper, not this revision.** Cost/benefit asymmetric under 5-day deadline. Adding it now risks (B) Orlandi-beats-current claim or (C) Orlandi-fails-to-replicate which forces high-conflict "we couldn't replicate" subsection. Strategic two-paper play: submit current paper as-is, do Orlandi replication during the 2–3 months between submission and decision.
- **Tier 1 first, then Tier 2 + JSON pointer consolidation.** User chose Tier 1 pilot first; then "proceed with tier 2" + "JSON pointers into table(s)". Combined to single Tier 2 commit (`543489b`). Tier 3 held for user visual review.
- **DTW atlas was the pilot.** Agent A flagged MERGE-3 as lowest-risk atlas pattern; ran it in Tier 1 to validate before applying same pattern to MERGE-1 reconstruction atlas in Tier 2.
- **MERGE-2 stat_grid atlas held for Tier 3.** Each stat_grid is itself a 6-panel inset; 3×3 atlas would yield 54 sub-sub-panels at ~40mm. Too risky for Tier 2.
- **JSON pointer consolidation done in same commit as Tier 2.** New artifact reference table at §A.5 with 33 IDs serves as the registry; body prose now uses IDs (R<N>/C<N>/D<N>) instead of inline path strings.

Inherited from earlier sessions (still in force):

- §6 #2 amendment per T05 R3 (LR-EMD per-model dominance OK, per-seed not)
- All `revision/core/` byte-frozen (D-14-22)
- Training-time ×0.1 sites byte-frozen; 14-21 fix INFERENCE-ONLY at samples.npy load boundary via C1 (`_wgan_unscale.py`)
- Plan 14-07 intentionally deferred to AIChE acceptance

</decisions_made>

<blockers>

- **Figure-size scope clarification (BLOCKING):** User said "make figures span both columns" but all 6 main figures are already `\begin{figure*}`. Need disambiguation: Fig 1 normalisation / supp atlas re-layout / specific figure / something else. Workaround: re-pose the AskUserQuestion on resume.
- **AIChE bundle upload (BLOCKING, deadline 2026-06-17):** Manual portal action. Bundle ready at `~/Desktop/aiche_upload_post14-21/`.

</blockers>

## Required Reading (in order)

1. `.planning/HANDOFF.json` — structured handoff including artifact registry IDs (R1-R21, C1-C7, D1-D4) and bundle state.
2. `.planning/STATE.md` — current project status (note: was refreshed in previous-session commit `9ba37f8`, may need another refresh post-session-2 to reflect the 84-page state).
3. **This file** — full audit trail of session-2 work + blocking clarification needed.
4. `~/Desktop/aiche_upload_post14-21/UPLOAD-CHECKLIST.md` — portal upload + cover-letter talking points (still cites 99 pages from session-1 update; may need refresh to 84 pages if the bundle's UPLOAD-CHECKLIST is part of the submission package).
5. `/tmp/supp-merge-recs.md` + `/tmp/supp-trim-recs.md` — the two agent reports that drove Tier 1+2 (contains Tier 3 recommendations not yet executed).

## Critical Anti-Patterns (do NOT repeat these)

- **ANTI-PATTERN:** Trust the freeze gate's "PASS" claim without visually verifying the PDF → 12 days of "submission-ready" with `[?]` everywhere. **STRUCTURAL MITIGATION:** Always pdftotext-grep the bundle PDF for `[?]` markers before claiming submission-ready.
- **ANTI-PATTERN:** Add new LaTeX style/class files without tracking them in git → silent bibtex failure. **STRUCTURAL MITIGATION:** Every non-vendored TeX asset goes in git from day one.
- **ANTI-PATTERN:** Use literal `- ` at line start to emulate bullets → renders as paragraph-leading hyphens in pdfTeX. **STRUCTURAL MITIGATION:** Always use `\begin{itemize}[nosep, leftmargin=*]` + `\item \textbf{Label:}` (the convention used at §4.4 scope-conditions list).
- **ANTI-PATTERN:** Inline `\texttt{revision/path/to/file.json}` in body prose → reviewer cognitive load + clutter. **STRUCTURAL MITIGATION:** Add to Table A3 (§A.5 artifact registry) with R/C/D ID, reference by ID in body.

## Infrastructure State

- **HEAD:** `543489b` — `fix(14-21-post): supp Tier 2 + JSON pointer consolidation — 95 -> 84 pages`
- **Working tree at pause:** clean.
- **Tags on origin:** `v1.2.4` is the most recent. New fix work caps at `v1.2.5+` (cut by 14-07 at acceptance).
- **PDF state:** `main (4) copy.pdf` at **84 pages**, 3.88 MB, clean compile (0 errors, 0 warnings, 0 undefined refs).
- **Bundle state:** `~/Desktop/aiche_upload_post14-21/` — 65 files, 7.8 MB, fully synced with HEAD `543489b`.
- **Provenance gate v2.2:** main 157 PASS / supp 343 PASS (decreased from 347→343 over Tier 1+2 due to incidental prose cuts; every remaining literal still traces).
- **Citation hygiene:** 62/64 cite keys used; 0 missing; 2 unused (`Mugel2022`, `dimoudis2023utilizing` — peer-review-swap leftovers).
- **Freeze gates:** a/b/c PASS, gate d expected-deferred to 14-07.
- **Background processes:** none.
- **Worktrees:** stale from previous session (`agent-a2cc81c0e4e021515` and `agent-af66716675fa24d3b`) — can be cleaned anytime.

<context>

Mental state at pause: focused, efficient. The session ran a clear sequence — diagnose-broken-PDF → fix → walkthrough §1 → big supp reduction across two tiers + JSON cleanup → started on figures → paused for clarification. The user's interrupt happened right when I was asking which figures looked small, since all 6 main figures are already `\begin{figure*}`. The user's instinct may be more about the new 3×3 supp atlases (whose panels at 0.32\linewidth could indeed be tight at print size for the reconstruction atlas's internal 3-row sub-plots), but I'm not certain — hence the clarification.

The strategic frame for this paper revision: maximize defensive posture under tight deadline. Every move this session has been load-bearing-preservation-first, page-count-second. The artifact registry table is the single highest-impact UX improvement of the session — body prose is now significantly more readable.

The paper is in a strong place at HEAD `543489b`:
- 84 pages (was 105)
- All citations resolve, References section rendered
- Provenance gates green, freeze gates green (except deferred d)
- 33-row artifact registry table replaces inline path clutter
- Five waves of peer-review remediation still in force unchanged
- ×0.1 inverse-pipeline disclosure (highest-leverage hostile-review defense) still in §A.7 and main §5

The remaining work is finishing-touches + the section walkthrough + the user's figure-size concern. None of it is structural blocker territory.

</context>

## Pre-Execution Critique Required

Not applicable — no design artifact pending execution. The figure-size question needs user clarification before any new figure edits.

<next_action>

Start with: **re-pose the figure-size clarification question.** Specifically the user-facing question is:

> All 6 main figures (Fig 1 concept_diagram, Fig 2 training_convergence, Fig 3 cross_model_dtw_dualscale, Fig 4 cross_model_acf_overlay, Fig 5 cross_model_emd, Fig 6 param_efficiency_pareto) are already `\begin{figure*}` (full-width, spanning both columns). Tables 1 and 2 are also `table*`. The only main-text candidates for any change are:
>
> (a) **Main Fig 1** uses fixed `[height=7cm, width=17cm]` instead of `\linewidth` — defensive normalisation only;
> (b) **Supp atlas figures** (the new §A.12 DTW + §A.10 reconstruction 3×3 atlases) have panels at 0.32\linewidth — Agent A flagged MEDIUM legibility risk on the reconstruction atlas because each subpanel has internal 3-row stacked sub-plots. A 2-page spread fallback at 0.45\linewidth would address this at the cost of ~2 pages of saving;
> (c) **Specific figure** you noticed looks small — point me at it.
>
> Which scope did you have in mind?

After the user answers, execute the chosen scope. Then resume the section walkthrough at §2.

</next_action>
