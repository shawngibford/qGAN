---
context: phase
phase: 14-paper-revision-release-freeze
task: 7
total_tasks: 7
status: complete
last_updated: 2026-06-11T19:27:45.462Z
plan: 14-21
plan_status: complete
phase_status: partial (20/21; 14-07 deferred to journal acceptance)
deadline: 2026-06-17 (AIChE resubmission, 6 days out)
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

- [ ] **CONSTRAINT: Training-time ×0.1 sites are byte-frozen.** Do NOT edit `revision/run_matched2000.py:281`, `revision/core/training.py:347,381,416`, `revision/run_baselines.py:205`. The fix is inference-only at the `samples.npy` load boundary via `revision/_wgan_unscale.py`. Editing the training-side sites would invalidate every checkpoint and every saved sample bundle.
- [ ] **CONSTRAINT: VAE and AR(2) samples must NOT be passed through `_unscale_wgan_samples`.** They are deliberately excluded from `_WGAN_KINDS` per Pitfall 3 — their on-disk samples are already in the canonical [-1, +1] space. The differential test on every regenerated JSON confirms VAE+AR(2) rows are bit-identical.
- [ ] **CONSTRAINT: Hard prohibitions §6 #1, #3, #4 remain in force.** (1) VAE = degenerate generation regime, NEVER posterior/variance collapse; (3) Real-data lag-1 ACF reference = −0.0641; (4) Pipeline B has NO Lambert W. Only §6 #2 was amended (per R3 at T05 checkpoint).
- [ ] **CONSTRAINT: `revision/core/` is byte-frozen (D-14-22).** Helper module at `revision/_wgan_unscale.py` (top-level revision/), NOT inside `revision/core/`. Verify `git diff revision/core/` is empty before any commit.
- [ ] **CONSTRAINT: Tags v1.2 through v1.2.4 on origin must NOT be rewritten.** They're historical reference. New work caps at v1.2.5+ (14-07 cuts the tag at journal acceptance).

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Blindly trust agent attributions | First Explore agent mis-attributed historical figure to wrong archive file; would have caused the smoking gun to be missed. | blocking | Before any code change based on a subagent's claim about file/line, open the file directly with Read and verify the cited content exists. |
| Visualization fixes for model issues | The §A.10 reconstruction overlay's earlier "drift correction" (mean-shift band-aid) masked the actual ×0.1 attenuation bug for an entire session. | blocking | Before applying any post-hoc visual correction, compute gen-vs-real moment ratios (mean, std). If std ratio is outside [0.5, 2.0], investigate the inverse pipeline before patching the renderer. |
| Treating prohibition text as immutable when its empirical premise is bugged | §6 #2 was authored from the bugged LR-EMD column ("every classical adversarial baseline outperforms every quantum variant"). The corrected data falsifies that empirical premise. Honoring the prohibition strictly would have meant hiding a real finding. | advisory | When a hard prohibition's text contains an empirical claim, check whether that claim survives the current data. If not, surface the tension to the user (Rule 4 escalation) rather than silently honoring the now-falsified prohibition. |

<current_state>
Phase 14 sub-plan 14-21 (×0.1 WGAN inverse-pipeline bug fix) is **COMPLETE end-to-end** at HEAD `4a5954f`. The bug has been fixed; the matched-budget metrics and figures have been regenerated; the paper narrative + abstract + contributions have been reframed per R3 to lead with quantum cluster dominance over the parameter-matched WGAN cluster on all four matched-budget fidelity metrics; the disclosure paragraph in supp §A.7 makes the WGAN sample-space convention transparent for reviewers; the narrative-pass cleanup committed at `4a5954f` removed "pre-fix"/"post-fix" historical framings throughout main, supp, reviewer_response.md, and methods_full.md.

**Phase 14 progress**: 20/21 plans complete. Only 14-07 (Zenodo DOI + tag v1.2.5+ + release.md) remains, intentionally deferred to AIChE journal acceptance.

**AIChE resubmission deadline**: 2026-06-17 (6 days from today, 2026-06-11). Camera-ready bundle is technically clean at HEAD `4a5954f`: provenance gates PASS on all 6 reviewer-facing docs (main, supp, paper_blocks_framing, paper_blocks_refs_methods, reviewer_response, methods_full); LaTeX compile clean (0 errors, 0 undefined refs, 96 pages); freeze gates (0), (a), (b), (c) PASS; gate (d) expected-deferred to 14-07.

Working tree at pause: clean (apart from this handoff doc + HANDOFF.json which will be committed as the pause-work WIP).
</current_state>

<completed_work>

This session executed Phase 14 sub-plan 14-21 from start to finish (10 atomic commits):

- **T01** (`b367e52`): Diagnosed and patched the ×0.1 WGAN inverse-pipeline bug. Added new shared helper module `revision/_wgan_unscale.py` (top-level revision/, NOT inside revision/core/, preserving D-14-22 byte-freeze). 9 verified `samples.npy` load sites across 7 producer files wired through the helper (`run_matched2000_dualscale.py:189`, `run_distribution_emd.py:217`, `run_utility.py:170`, `run_timegan_scores.py:165`, `run_ansatz_comparison.py:159`, `run_canonical_headline.py:232`, `run_figure_suite.py:272`+843+3118). `_WGAN_KINDS` set contains 7 WGAN-trained model_kinds plus 3 headline-row labels (`frozen_checkpoint_headline`, `iqp_sel_55_headline`, `quantum`). VAE+AR(2) deliberately excluded per Pitfall 3. Mean-match drift-correction band-aid removed from `render_reconstruction_overlay` (lines 859-863). Training-side ×0.1 sites verified byte-unchanged.

- **T05 (pulled forward)** (`9391b1a`): Executor agent pulled forward the T05 human checkpoint at T02 step (7) when `run_welch_aggregator.py` hard-aborted on the OD-EMD H2 strong-claim threshold (post-fix Welch p=0.0188 does not clear the 0.36 threshold from 14-18). Structured directional-shift report written to `.continue-here-t05.md` with pre/post tables for OD-EMD, LR-EMD, OD-DTW, LR-DTW + explicit Branch A/B + the §6 #2 hard-prohibition tension + 5 R-options. **User chose R3** (relax §6 #2; rewrite to "quantum dominates OD-DTW + LR-DTW + LR-EMD at the matched budget").

- **§6 #2 amendment + welch threshold patch** (`ede7aa4` + `30a6cb4`): CONTEXT-HANDOFF §6 #2 rewritten — prior "LR-DTW NOT LR-EMD is the surviving quantum-distinguishing signal — never re-claim quantum advantage on LR-EMD" replaced with the post-fix multi-signal cluster-dominance statement + new per-seed-not-per-model caveat. `run_welch_aggregator.py` strong-claim acceptance gates converted from hard-abort to soft-fail with R3 rationale comment + `_status: 'H2_DROPPED_PER_14-21_R3'` field; thresholds preserved for historical traceability.

- **T02** (`e05553e`): 8 matched-budget JSONs regenerated post-fix (`matched2000_dualscale.json`, `distribution_emd.json`, `ansatz_comparison.json`, `headline_canonical.json`, `welch_pairwise.json`, `tstr_matched2000.json`, `predictive_discriminative_matched2000.json`, `augmentation_matched2000.json`) plus the 3 `figures/cross_model_*.json`. Differential test confirms every WGAN-kind row changed and every VAE+AR(2) row bit-identical.

- **T03** (`a70a5d8`): Full figure suite regenerated (~200 PDF/PNG/JSON triples). All per-model figure families (11 prefixes × 9 models each) and all cross-model + headline figures freshly regenerated using the patched code path. §A.10 reconstruction overlays now show orange synthetic line at real-matching amplitude (not the pre-fix near-flat trace). §A.11 + §A.12 backed by post-fix data. The 22 "stale" files (16 circuit architecture diagrams, 4 circuit-introspection JSONs, 3 training-time captured-state JSONs) are intentionally not regenerated — circuits/introspection are independent of samples; training-time captured data would require retraining (out of scope per plan).

- **T04** (`99fef8e`): Paper literals refreshed in main (4) copy.tex (164 distinct literals) and supp_material.tex (214 distinct literals). Headline + Cross-Model + Per-seed LR-DTW + LR-EMD pairwise Welch tables refreshed. Abstract + Plain Language Summary + Contributions list reframed per R3. New `revision/results/lr_dtw_dominance_gap.json` adds 5 per-seed gap literals for provenance traceability (worst-quantum V3 vs best-classical-WGAN wgan_lstm per-seed gap, derived from `matched2000_dualscale.json`).

- **T06 + T07** (`6c3da1d`): Main: §4.1 cross_model_emd figure caption rewritten; §4.1 OD-marginal paragraph rewritten ("non-significant difference under low power" → "quantum cluster dominates the WGAN cluster"); §4.2 VAE characterization de-coupled from "bifurcated finding" language (mechanism description unchanged per §6 #1); §5 Theoretical and Practical Implications rewritten to lead with cluster dominance on all four matched-budget metrics; §5 Scope-of-finding paragraph rewritten with metric scope + per-seed-vs-per-model distinction. Supp: new disclosure paragraph in §A.7 Data Transformation Details covering ×0.1 origin + Pipeline B preservation + ×10 inference-only correction + Pitfall 3 asymmetry + residual mean-drift; §A.10 + §A.11 drift-correction band-aid language removed.

- **T07-ext** (`f67766a`): `reviewer_response.md` utility-battery table refreshed with post-fix TSTR/predictive/discriminative/+100% augmented R²; R1-M2 "honest reading" paragraph reframed (partial generator discrimination now visible on the utility battery — quantum + VAE + AR(2) form a tight high-utility group, WGAN cluster falls away on TSTR R², predictive score, and +100% augmented R²); R1-M3 DTW addendum updated to post-fix numbers; per-baseline Welch table refreshed; cluster-floor reading paragraph added; outlier-seed disclosure for wgan_cnn restated against the cluster narrative.

- **SUMMARY + STATE/ROADMAP close** (`b59f918`): `14-21-SUMMARY.md` written with per-task commit ledger, bifurcated-finding pre/post matched-budget table, final verification gate state, hard prohibitions honored list, authorised R3 deviations list, and open items intentionally deferred. STATE.md updated (frontmatter progress 23/24→24/25, 51%→53%; Session Continuity block refreshed; AIChE resubmission state recorded). ROADMAP.md Wave 18 marked `[x]`.

- **Narrative pass** (`4a5954f`): Per user direction ("no need to mention the pre-post fix thing, these are just the results"), removed "pre-fix"/"post-fix"/"post-correction"/"pre-correction"/"pre-revision"/"v1.2.4 framing"/"was an artifact" framings across main (4) copy.tex (§4.1 OD-marginal closer + §5 lead-in), supp_material.tex (§A.7 disclosure rewritten as "WGAN sample-space convention" + §A.10 + §A.11), reviewer_response.md (R1-M2 narrative + R1-M1 main claim section + per-baseline Welch table + outlier-seed disclosure + aggregate summary), and methods_full.md (§3.x (f) + §3 DTW historical context). All numeric literals refreshed against current matched-budget aggregates. Methodology disclosure of the WGAN sample-space convention preserved in supp §A.7 for reviewer reproducibility but framed as a forward-looking convention.

**Headline result (final, as it currently stands):**

On a parameter-matched, matched-epoch (2000), multi-seed (n=5) comparison against three classical adversarial WGAN-GP baselines (73-78 generator parameters each, sharing a 250,881-parameter critic) on a single laboratory-scale photobioreactor cultivation dataset, a **55-parameter quantum WGAN-GP generator** (and three matched-budget variants at 75-135 parameters) **dominates the classical adversarial WGAN cluster on all four matched-budget fidelity metrics**:
- LR-DTW: per-seed dominance, 25/25 cells no overlap (Q 6.09-9.48 vs WGAN 18.23-69.02)
- OD-DTW: Welch cluster-floor p ≈ 0.002 (Q 0.33-0.41 vs WGAN 0.60-6.99; ~5× under the Orlandi et al. 1.954 reference)
- LR-EMD: ~15× quantum advantage on per-model means (Q 0.0040-0.0050 vs WGAN 0.0244-0.1286)
- OD-EMD: Welch cluster-floor p = 0.019 (Q 0.028-0.031 vs WGAN 0.077-0.799)

The quantum cluster's mean lag-1 autocorrelation (−0.0895 to −0.0997) is closer to the real-data reference (−0.064) than any classical-baseline mean. VAE (562 params, ELBO) and AR(2) (3 closed-form params) are included as non-adversarial reference points; the quantum-vs-WGAN dominance claim is scoped to the parameter-matched adversarial sub-family.

</completed_work>

<remaining_work>

**Optional cleanup (not blocking, not gate-blocking):**

1. **Narrative cleanup of `peer_review_remediation.md` + `reconciliation_note.md`.** These process docs still carry the 14-16-era "pre-fix/post-fix" framing in the historical Plan 14-16 r3-process retraction sections. The numbers cited in those sections are 14-16-era values that are now superseded by 14-21. Provenance gates pass (the values still exist in some JSONs), but the narrative is internally contradictory with the current reviewer_response.md + methods_full.md framings. Estimated ~1-2 hours work to do a comprehensive pass; the freeze gate's hard-block subset only includes paper_blocks_framing.md + paper_blocks_refs_methods.md + reviewer_response.md (all already clean), so this is a consistency-of-narrative concern, not a gate-blocking issue.

**External human actions (BLOCKING for AIChE submission):**

2. **Rebuild AIChE submission bundle from HEAD `4a5954f` and upload to portal.** Deadline 2026-06-17 (6 days). Bundle should include: `main (4) copy.pdf` (96pp, 3.8MB), bibliography, figures, revised `reviewer_response.md`, cover letter referencing the corrected matched-budget findings. The supp content is included via `\input{supp_material}` from main — so the compiled main.pdf is the single document the editor + reviewers will read.

**Held until journal acceptance:**

3. **Plan 14-07 — Zenodo DOI mint + tag v1.2.5+ + release.md authoring.** Long-standing user deferral. Freeze gate (d) is expected-deferred until 14-07 lands; all other freeze gates pass at HEAD `4a5954f`.
4. **GitHub release notes draft.** Held until acceptance.

</remaining_work>

<decisions_made>

- **R3 (T05 checkpoint, user-authorized)**: Relax CONTEXT-HANDOFF §6 #2. The prohibition's empirical premise ("On LR-EMD, every classical adversarial baseline outperforms every quantum variant") was authored from the bugged data and is falsified by the corrected pipeline. Replaced with the multi-signal cluster-dominance statement + new per-seed-not-per-model caveat (~15× advantage is on per-model means, not on every individual seed).

- **Drop OD-EMD H2 parametric-equivalence claim from 14-18**: Post-fix data inverts the "no statistically detectable difference, underpowered null" framing. Welch cluster-floor p drops from 0.37 to 0.019; quantum cluster mean ~11× below WGAN cluster mean.

- **Relax `run_welch_aggregator.py` strong-claim thresholds** (Rule 4 deviation, user-authorized): `floor_welch_p_OD: 0.36` and `ceiling_abs_cohen_d_OD: 0.65` → soft-fail with R3 rationale comment. Thresholds preserved in `strong_claim_thresholds` dict for traceability + `_status: 'H2_DROPPED_PER_14-21_R3'`.

- **Inference-only fix via shared helper at `revision/_wgan_unscale.py`**: Preserves D-14-22 `revision/core/` byte-freeze. Helper imported by 7 producer files; `_WGAN_KINDS` gating is single source of truth for the model_kind asymmetry.

- **Frozen-checkpoint headline (`run_canonical_headline.py:210`) also patched unconditionally**: The bug applied ×0.1 to fresh samples generated from `best_checkpoint.pt`, so `headline_canonical.json` is also affected. Q1 from the source plan ("verify") collapsed to "regenerate unconditionally". Patched at line 232 via Option B (preserve historical `* 0.1` at line 210 + undo at inverse boundary via helper with `model_kind="frozen_checkpoint_headline"`).

- **Drop "pre-fix/post-fix" historical framing across reviewer-facing docs** (user direction at end of narrative-pass commit `4a5954f`): The methodology disclosure of the WGAN sample-space convention remains in supp §A.7 (necessary for code reproducibility — reviewers will see `revision/_wgan_unscale.py` in the codebase) but is rewritten as a forward-looking convention note rather than a "the prior version was wrong" disclosure. Results stated directly throughout.

- **Tag direction caps at v1.2.5+**: Tags v1.2 through v1.2.4 stay on origin as historical reference. New work caps at v1.2.5 or higher once 14-07 mints the Zenodo DOI at acceptance.

</decisions_made>

<blockers>

- **AIChE resubmission deadline**: 2026-06-17 (6 days from today). Camera-ready bundle is technically clean at HEAD `4a5954f`; remaining work is external (bundle rebuild + portal upload).

- **No technical blockers**. All gates pass at HEAD `4a5954f`; phase is 20/21 complete with the only outstanding plan (14-07) intentionally deferred.

</blockers>

## Required Reading (in order)

1. `.planning/STATE.md` — current phase + session state.
2. `.planning/phases/14-paper-revision-release-freeze/14-21-SUMMARY.md` — full sub-plan summary with per-task commit ledger, gate state, hard prohibitions honored, R3 deviations, deferred items.
3. `.planning/HANDOFF.json` — machine-readable state mirror of this doc.
4. `.planning/CONTEXT-HANDOFF-2026-06-02.md` §5 + §6 — load-bearing hard prohibitions (§6 #1, #3, #4 unchanged; §6 #2 amended per R3).
5. `revision/_wgan_unscale.py` — the shared helper module + `_WGAN_KINDS` set + Pitfall 3 asymmetry documentation.
6. `supp_material.tex` §A.7 "WGAN sample-space convention and inverse-pipeline scaling" paragraph — the reviewer-facing methodology disclosure.

## Infrastructure State

- **Branch**: `main`, ahead of `origin/main` by 11 commits (8c5380c through 4a5954f), NOT pushed.
- **Tags on origin**: v1.2.4 is the most recent. New fix work caps at v1.2.5+ (cut by 14-07 at acceptance).
- **Working tree at pause**: clean apart from this handoff doc + HANDOFF.json (will be committed as pause-work WIP).
- **PDF state**: `main (4) copy.pdf` at 96 pages, 3.8MB, clean compile (0 errors, 0 undefined refs).
- **Gate state at pause**: all 6 reviewer-facing docs PASS provenance gate (main 164 literals, supp 214, paper_blocks_framing 23, paper_blocks_refs_methods 49, reviewer_response 177, methods_full 133); v2.1 differential test PASSED; freeze gates (0), (a), (b), (c) PASS; gate (d) expected-deferred to 14-07.

<context>
Mental state at pause: 14-21 is technically complete with a substantial reframing of the AIChE submission's headline result. The reframing is well-supported empirically (8 regenerated JSONs + ~200 figure triples + differential test confirms WGAN-only changes + VAE/AR(2) bit-identical) and was user-authorized via the structured R3 path at the T05 checkpoint with full disclosure of the §6 #2 prohibition tension. The user explicitly opted to lead with the corrected cluster-dominance finding rather than honor a prohibition whose empirical premise was bugged. The narrative-pass cleanup ensures reviewer-facing docs report results directly without "before/after" historical scaffolding.

The user is in a tight resubmission window (6 days) but the technical work is done. Remaining work is external (bundle rebuild + portal upload) plus the long-standing 14-07 Zenodo deferral.

The two process-doc cleanups (peer_review_remediation.md + reconciliation_note.md) are flagged but not in the freeze-gate hard-block set; they can be done in the next session for narrative consistency, deferred to post-acceptance corrigendum, or skipped entirely.
</context>

<next_action>
1. Read `.planning/STATE.md` + `14-21-SUMMARY.md` + this `.continue-here.md` + the HANDOFF.json.
2. Surface the AIChE timing question: bundle rebuild + portal upload before 2026-06-17 is the critical-path human action. Confirm the user's plan.
3. Optional: clean up `peer_review_remediation.md` + `reconciliation_note.md` for narrative consistency across the AIChE bundle (~1-2 hours). User can decide on resume.
4. If user wants comprehensive consistency before submission, do the process-doc cleanup THEN prep the bundle. Otherwise, proceed directly to bundle prep.
5. 14-07 Zenodo + tag work stays deferred until journal acceptance.
</next_action>
