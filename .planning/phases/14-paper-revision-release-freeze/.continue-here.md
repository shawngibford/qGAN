---
context: phase
phase: 14-paper-revision-release-freeze
plan: 14-21-post
task: submission-ready
total_tasks: open-ended (cleanup arc complete)
status: paused_ready_to_submit
last_updated: 2026-06-16T14:28:22.694Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] **CONSTRAINT: post-hoc outlier removal is p-hacking** — Any future "should we drop X" question on outlier data must first answer: "where was the pre-registered exclusion criterion?" If the answer is "we noticed X after seeing the results", the answer to dropping is no. Use the robustness-check pattern instead (report headline with all seeds + show what would happen excluding).

**Do not proceed until the box above is checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Compile-clean ≠ visual-clean | Fig A10 was rendering with 0 LaTeX errors but inconsistent y-axes across rows, no real-data overlay despite caption claiming one, spike-like bins. Freeze + provenance gates passed; figure was a hostile-reviewer magnet. Caught only when user eyeballed the supp PDF on page 54. | advisory | Before declaring a figure "done", open the rendered PNG and look at it as a reviewer would. Gate output does not inspect aesthetics. |
| Over-disclosure creates hedge-heavy reading | After Tier-2 swarm added HPO + Bonferroni + single-cultivation disclosures across 5+ sites each, the prose became verbal-tic-heavy. Required dedicated cohesiveness sweep (11 fixes) to restore readability. Reviewers spot "every claim is hedged twice" as defensive overcompensation. | advisory | After any disclosure-addition swarm, run a redundancy/cohesiveness audit before declaring done. Goal: each disclosure appears exactly once at the strongest layer (Abstract / Limitations / Section A) and is referenced elsewhere. |
| Post-hoc outlier removal would be p-hacking | Session attempted to ask "can we drop seed-42" as path to Bonferroni significance. Computation showed: (a) would NOT improve Bonferroni p (floor pair unchanged); (b) would create a worse attack vector than the outlier itself; (c) violates pre-registration. | blocking | Robustness-check pattern: report headline with all seeds + show what would happen excluding. NEVER drop data post-hoc without pre-registered criterion. |

<current_state>
Paper submission-ready at HEAD `bec0e50`. 81-page PDF, all gates green. Bundle synced at `~/Desktop/aiche_upload_post14-21/`. Deadline 2026-06-17 (~1 day remaining). Awaiting external portal upload (cannot be done by Claude).
</current_state>

<completed_work>

Session-3 completed 14 atomic commits between `67a8630` and `bec0e50`. Major arcs:

- **Figure modernization**: Fig 5 (cross_model_emd) log y-axis + value annotations; Fig 6 (param_efficiency_pareto) modernized then removed entirely; Fig A10 (training_progression) regenerated with shared axes + real-data overlay + out-of-range counts
- **Content removal per user**: §5 inference-pipeline-correction subsection (44 lines), all x10 references in main+supp, reviewer-tagged footnotes, ZENODO placeholders, inline file paths, Fig 6
- **Co-author swarm pass 1** (6 BLOCK + tier-1/2): LR-EMD Welch contradiction, WGAN-CNN 3.4338 vs 0.1587 mismatch, fig:circuit_atlas dangling ref, A5/A6/A7 supp-fig renumber drift, frozen-headline 0.0224, "exposes the collapse" VAE-prohibition risk, supp §3.2→§3.3, two captions LR-EMD→OD-EMD outlier, R3 Welch pointer added, 7 §4.2→§4.1 site fixes, "per-model means" qualifier, redundant sentence delete, orphaned mean-bias paragraph delete
- **Co-author swarm pass 2** (regression): Bonferroni propagation to 4 sites, |d|>3 factual error corrected to |d|=2.34, §4.4 +2 scope bullets (Single-cultivation + HPO asymmetry), AR(2) Yule-Walker framing in §4.2 + §5, "uniformly" replaced
- **Tier-2 disclosures**: HPO asymmetry paragraph; Abstract single-cultivation prefix; Bonferroni nuance (OD-EMD dissolves under family-wise correction, OD-DTW survives)
- **Robustness paragraph**: §4.1 documents cluster-floor pair (V2 vs wgan_mlp; p=0.0188) does NOT depend on wgan_cnn seed-42 outlier
- **Cohesiveness sweep**: 11 redundancy/seam cleanups (AR(2) trim, HPO bullet collapse + promotion, Bonferroni demotion, parallel cleanup, metric re-list compress)
- **x0.1/x10 disclosure restored** (slimmed): supp Data Transformation Details, no v1.2.4 comparison narrative
- **Fig A10 modernized**: shared x/y axes, real OD log-returns overlaid as gray fill in every panel, out-of-range counts (Quantum 0; WGAN-MLP +26-38; WGAN-CNN +94-116; WGAN-LSTM +12-13)

Page count: 84 (session start) → 81 (current). 0 LaTeX errors. main 164 + supp 338 literals trace to JSON. Freeze gates a/b/c PASS.
</completed_work>

<remaining_work>

External actions only — Claude cannot complete these:

- **EXT-1 (blocking, deadline 2026-06-17)**: Zip the bundle and upload to AIChE portal
  - `cd ~/Desktop && zip -r aiche_upload_post14-21.zip aiche_upload_post14-21/`
  - Then upload via AIChE submission portal
- **EXT-2 (blocking, deadline 2026-06-17)**: Compose cover letter in portal text box (talking points in bundle's UPLOAD-CHECKLIST.md)
- **14-07 (deferred to acceptance)**: Zenodo DOI mint + tag v1.2.5+ + release.md authoring + manuscript Data Availability update from "Zenodo upon acceptance" to real DOI
</remaining_work>

<decisions_made>

Major decisions logged this session (all reflected in HANDOFF.json):

- **Removed §5 "Note on the inference-pipeline correction"** + all x10 references per user explicit decision. Trade-off accepted: the 44-line subsection was the highest-leverage hostile-review defense per prior session memory.
- **Removed Fig 6 (param_efficiency_pareto)** entirely. Restated Table 2 + Fig 5 without new evidence; "Pareto frontier" framing actively misled.
- **Did NOT drop wgan_cnn seed-42 outlier**. Post-hoc removal would be p-hacking. Used robustness-check disclosure pattern.
- **Restored slimmed sample-space-convention disclosure** after user reconsideration. Without v1.2.4 comparison narrative.
- **Tier 2 proactive disclosures** (HPO + single-cultivation + Bonferroni) over leaving silent. Preempts hostile-reviewer attacks.
- **Cohesiveness sweep** collapsed 11 redundancies after Tier-2 created hedge-heavy reading. No content cuts; prose tightening only.
</decisions_made>

<blockers>
- **External portal upload** (only user can do): Bundle ready. Deadline 2026-06-17.
</blockers>

## Required Reading (in order)

1. `.planning/HANDOFF.json` — machine-readable handoff with full session state, decisions, anti-patterns, and load-bearing invariants
2. `.planning/STATE.md` — project-level state (last updated session-2; resume should refresh)
3. `revision/docs/reviewer_response.md` — current rebuttal letter aligned with the slimmed paper

## Critical Anti-Patterns (do NOT repeat these)

- **Compile-clean ≠ visual-clean**: Before declaring a figure done, open the rendered PNG and eyeball it. Gates do not inspect aesthetics.
- **Disclosure proliferation**: After adding any defensive disclosure, run redundancy audit. Goal: each disclosure appears once at strongest layer, referenced elsewhere.
- **Post-hoc outlier removal**: Never drop data without pre-registered criterion. Use robustness-check pattern.

## Infrastructure State

- **Bundle**: `~/Desktop/aiche_upload_post14-21/` synced at every commit boundary; HEAD `bec0e50`. 81-page PDF (3.85 MB), 66 files including ama.bst, bib.bib, all figures, both .tex sources, reviewer_response.md, UPLOAD-CHECKLIST.md.
- **Git tags**: v1.2 through v1.2.4 on origin are immutable historical reference points. Next tag is v1.2.5+ (cut at AIChE acceptance via plan 14-07).
- **ama.bst**: tracked in git as of session-2 fix. Do not remove.

<context>
Session ran ~6+ hours wall clock. The work was end-to-end: resumed from a paused state, resolved that ambiguity, then ran the largest cleanup arc of the milestone. The headline cluster-dominance claim survived intact across ~35 fixes, but the hedging around it grew substantially via Tier-2 disclosures (HPO, Bonferroni, single-cultivation). Net effect: the paper is more honest about caveats and harder to attack on statistical-rigor or scope grounds. The empirical basis (n=1 cultivation, local Mac simulator) is unchanged — no amount of editing can address that.

Final honest probability estimate delivered to user as session summary:
- ~40% major-revision-then-accept
- ~30% reject-with-resubmission
- ~20% reject outright
- ~10% accept with minor

The v2.1 follow-up (second cultivation + classical HPO sweep + clean the x0.1 vestigial scaling) is the paper that gets cited; this one earns the right to be reviewed.
</context>

<next_action>
Paper is submission-ready. No further code/prose edits recommended.

**On resume**:
1. Verify HEAD is still `bec0e50` and bundle is intact at `~/Desktop/aiche_upload_post14-21/`
2. Zip bundle: `cd ~/Desktop && zip -r aiche_upload_post14-21.zip aiche_upload_post14-21/`
3. Upload via AIChE submission portal (deadline 2026-06-17)
4. After AIChE acceptance: run Plan 14-07 (Zenodo DOI mint + tag v1.2.5+ + release.md authoring + manuscript Data Availability update)

If user requests further paper edits before submission, FIRST acknowledge the blocking constraint at top (post-hoc outlier removal is p-hacking). Then re-run a co-author audit swarm to identify regression risk before any changes — the paper has already been through 2 such swarms and is in the strongest defensible position the current evidence supports.
</next_action>
