---
phase: 14-paper-revision-release-freeze
plan: 14
type: execute
wave: 12
depends_on: ["14-13"]
files_modified:
  - verify_number_provenance.py
  - run_matched2000.py
  - run_methods_full.py
  - run_framework_versions.py
  - docs/methods_full.md
  - docs/reconciliation_note.md
  - docs/reviewer_response.md
  - docs/completeness_sweep_manifest.md
  - docs/peer_review_remediation.md
  - requirements-pinned.txt
  - results/figures/_introspect_quantum.json
  - results/figures/_introspect_wgan_cnn.json
  - results/figures/_introspect_wgan_lstm.json
  - results/figures/_introspect_wgan_mlp.json
  - results/noise_model_sensitivity.json
  - results/shot_noise_sensitivity.json
  - results/manuscript_apparatus_constants.json
  - results/framework_versions.json
  - REPRODUCE.md
autonomous: true
requirements: [PAPER-01, PAPER-02, PAPER-08, PAPER-09, INFRA-03]
must_haves:
  truths:
    - "A 5-agent r2 peer-review pass on Phase 14 plan 14-13's remediation work surfaced 0 CRITICAL, 5 HIGH, 4 MEDIUM, 6 LOW findings; every r2 reviewer's final recommendation was `ready for paper resubmission YES` (no blockers); this plan closes 15 of the r2 items (3 triangulated HIGHs that direct file verification confirmed + 12 lower-severity items) in one atomic plan; the core science remains sound — fixes are gate hardening (one-character regex), capture-site correction (device manifest), doc-only math derivation, and a 12-item documentation cleanup batch"
    - "D-14-16 (gate byte-freeze) LIFTED for Task 1 only; one-character regex fix to `verify_number_provenance.py:193` — the boundary lookbehind class `(?<![\\d.])` is changed to `(?<![-\\d.])` so a positive token `0.0001` in a doc no longer resolves against a JSON value `-0.0001` (particularly important because `reconciliation_deltas.json` is dense with small negative deltas); schema string bumps to `\"v2.1 (Phase 14 plan 14-14 — negative-sign-aware lookbehind)\"`; gate is back in byte-freeze under v2.1 after Task 1"
    - "D-14-22 (`core/` byte-freeze) PRESERVED across all 5 tasks; all math-doc corrections are doc-only; `git diff --stat core/` returns empty at every task close"
    - "D-14-13 (strict-accept gate) PRESERVED — Task 2 corrects the CAPTURE SITE of `training_time_device` without changing gate semantics; the `_strict_accept` equality check added in 14-13 T4 is unchanged; it just gets a value that actually reflects training device (the manifest currently always sees `cpu` because `generate_wgan_samples` at `run_matched2000.py:270` moves the model to CPU before `_device_manifest` reads `next(generator.parameters()).device` at lines 333–335; the MPS-disable monkey-patch IS the real protection, the gate that's supposed to CERTIFY it is non-functional in current shape)"
    - "After Task 1, `verify_number_provenance.py:193` carries the negative-sign-aware lookbehind `(?<![-\\d.])`; schema string is `\"v2.1 (Phase 14 plan 14-14 — negative-sign-aware lookbehind)\"`; gate docstring/comments include a regression block showing an example positive token + negative JSON value that v2 (broken) would pass and v2.1 must reject; all 4 `results/figures/_introspect_{quantum,wgan_cnn,wgan_lstm,wgan_mlp}.json` files carry `\"render_only\": true` at top level; `results/noise_model_sensitivity.json`, `results/shot_noise_sensitivity.json`, and `results/ansatz_comparison.json` all carry `\"data_hash\": \"91e447d4624e25b3\"`"
    - "After Task 2, `run_matched2000.py` captures `training_time_device` IMMEDIATELY after the training loop returns, BEFORE `generate_wgan_samples(generator, ...)` (or `vae.sample(...).cpu()`) moves the model to CPU; stored on `extra_cfg[\"training_time_device\"]` (or passed through `_device_manifest`); the fix is applied symmetrically to `_train_quantum`, `_train_wgan`, `_train_vae` (quantum already trains on CPU under the hook; the value is `\"cpu\"` in all current paths but the mechanism is identical); `_device_manifest` accepts an optional pre-captured device, falling back to the current `next(parameters()).device` read if absent (backward compat); the `_strict_accept` equality check (added in 14-13 T4) is UNCHANGED — it now actually verifies what it claims to verify"
    - "After Task 3, `docs/methods_full.md §3.x.d` (lines 316–327) carries the corrected VAE β derivation: per-element-mean MSE = sum_MSE/N_recon (N_recon = window × features = 10), per-element-mean KLD = sum_KLD/N_kld (N_kld = latent_dim = 4); `loss = recon + 1·kld = sum_MSE/10 + sum_KLD/4 = (1/10)·(sum_MSE + (10/4)·sum_KLD)`, so the canonical-sum-form equivalent is **β_eff = 2.5 (KL up-weighted)**, NOT 0.4 (the inverted figure propagated from r1 M-4 through 14-13); a semantic interpretation sentence explains the latent space is more strongly regularized toward the unit Gaussian prior than a canonical β=1 ELBO would impose; cite `run_baselines.py:315-319` for the per-element-mean MSE and per-element-mean KLD code"
    - "After Task 3, `docs/methods_full.md §2.i` (VAE architecture) carries the explicit caveat: `The VAE is not parameter-matched to the WGAN-GP variants (~75k–135k generator params) or the IQP:SEL headline (55 params); at 74 trainable params, the VAE is closer to the IQP:SEL scale and is included as a non-adversarial low-data baseline, not as a head-to-head adversarial comparator.`"
    - "After Task 3, `docs/reconciliation_note.md` interpretation paragraph carries the softened framing: `All deltas are within seed variance of the 1000-epoch baseline (Welch t-test p ≥ 0.37 for every model). The largest absolute delta is wgan_cnn (-0.059, a ~50% reduction off a small base); seed-42 outliers drive that variance, not a uniform near-zero effect across seeds.` All table rows are preserved verbatim; only the interpretation paragraph rewords"
    - "After Task 4, `docs/reviewer_response.md` R1-m4 row reads: `Tagged release + Zenodo DOI deposit pending under Plan 14-07 (only outstanding Phase 14 plan); release.md will carry the resolved DOI and a frozen-tag commit SHA upon deposit. The provenance gate v2.1 + tracked checkpoint + pinned requirements provide the reproducibility surface today; Zenodo adds the citable DOI on top.` — replacing the implication that the DOI is already delivered"
    - "After Task 4, `run_methods_full.py:152, 265, 524` all carry `1-69` line-range labels in the docstring-slicer (replacing the inaccurate `1-80` labels — the actual `run_matched2000.py` docstring ends at line 69, an off-by-11 error); mentions in `docs/methods_full.md:431, 455, 536` also flip from `1-80` to `1-69`"
    - "After Task 4, `run_methods_full.py`'s `_citations[\"generator_to_compute_dtype\"]` pattern is changed from `\"generated_samples = generator\"` (which resolves to `core/training.py:346`, the generator call) to `\"generated_samples = generated_samples.to(compute_dtype)\"` (which resolves to `core/training.py:347`, the actual dtype cast site that the original CR-3 finding was about); the file:line citation now points to the line the finding intended"
    - "After Task 4, `results/manuscript_apparatus_constants.json` is restructured: `apparatus_dimensions_mm: {depth_or_height_880, ancillary_120, ancillary_6, ancillary_10}` is replaced with `apparatus_dimensions: {tube_outer_diameter_cm: 6, tube_length_cm: 120, data_logging_interval_min: 10, ir_led_wavelength_nm: 880}` (or whatever the LaTeX `main (4) copy.tex:176-180` context dictates — verify by reading the LaTeX in T4 and matching the field semantics exactly); the units are no longer lumped under a single `_mm` label that misrepresents the 880-nm IR LED wavelength; the v2.1 gate corpus is updated accordingly and the gate re-runs PASS against `paper_blocks_framing.md`"
    - "After Task 4, `requirements-pinned.txt` carries `statsmodels==0.14.5` (the live version, currently missing from the pin file); `results/framework_versions.json` records the same (and `run_framework_versions.py` is extended to probe statsmodels if it does not already do so)"
    - "After Task 4, a top-level `REPRODUCE.md` exists at the repo root (~50–80 lines) with: (a) one-paragraph orientation; (b) clone + venv + `pip install -r requirements-pinned.txt`; (c) checkout the manuscript-revision tag (currently `main` until 14-07 lands `v2.0-revision`); (d) commands to regenerate the headline metric, sweep aggregates, figures, and the methods doc, in dependency order; (e) link to `docs/methods_full.md §5.2` for the full per-script breakdown and `docs/completeness_sweep_manifest.md` for the cross-plan navigation"
    - "After Task 5, the v2.1 gate runs against all 10 paper-facing docs (the existing 9 + `peer_review_remediation.md` and `REPRODUCE.md` if any paper-facing claims appear in it); all must PASS; differential test added to the gate's `__main__` self-test: positive token `0.0001` in a doc does NOT resolve to a JSON `-0.0001`; the `--manifest` flag remains for human spot-checks"
    - "After Task 5, `docs/peer_review_remediation.md` carries TWO new appended sections: (a) `## Gate v2.1 known limitations` documenting the R2-code-HIGH-2 ε-neighborhood low-precision coincidence risk (e.g. `-0.26` AR-bias factor resolving to a kurtosis value `-0.2570036` at low precision) — mitigated by `--manifest` human-spot-check, NOT eliminated; (b) `## R2 follow-up sweep` mapping each r2 finding to its 14-14 commit SHA (same structure as the existing 14-13 finding→commit table)"
    - "After Task 5, `docs/completeness_sweep_manifest.md` carries a new `## Plan 14-14 — r2 peer-review punch list (Wave 12)` section enumerating the T1–T5 deliverables (preserving the existing 14-09 / 14-10 / 14-11 / 14-12 / 14-13 sections verbatim)"
    - "After Task 5, `.planning/phases/14-paper-revision-release-freeze/14-14-SUMMARY.md` exists with the standard GSD self-check PASS + all 5 task commit SHAs + a final-state line declaring Phase 14 incomplete plans = `[14-07]` only"
    - "R2-code-HIGH-2 (ε-neighborhood broad coincidences) is DISCLOSED as a known gate limitation in T5's `peer_review_remediation.md` addendum; the gate is NOT tightened further (locked decision — the user accepts the limitation in exchange for the simpler v2.1 contract)"
    - "Files explicitly NOT modified by this plan: `core/` (D-14-22 byte-freeze, asserted by every task verify gate); `core/training.py`, `core/eval.py`, `core/models/*.py`, `core/__init__.py` (all under the byte-freeze); the read-only Overleaf-canonical `main (4) copy.tex` / `supp_material.tex` (D-14-18; T4 READS `:176-180` for field semantics but does not write); the byte-stable upstream docs `circuit_atlas.md` (14-09), `paper_blocks_framing.md` and `paper_blocks_refs_methods.md` (14-12 / 14-13 — except the v2.1 gate re-run touches `paper_blocks_framing.md` only for the apparatus-units re-verification; no doc text edit), `dataset_stats.md` (gate target only)"
    - "After Task 5, the only outstanding Phase 14 plan is 14-07 (Zenodo deposit + tag + DOI wiring + release.md, deferred manual gate); Phase 14 plan inventory shows incomplete = [14-07]; ROADMAP progress row `14. Paper Revision & Release Freeze | v2.0 | 13/14 | In Progress` after this plan commits (orchestrator flips from `12/14` → `13/14` post-merge)"
  artifacts:
    - path: "verify_number_provenance.py"
      provides: "Provenance gate v2.1 (Task 1; D-14-16 LIFTED for this plan only). One-character fix at line 193: boundary lookbehind class `(?<![\\d.])` → `(?<![-\\d.])` adding `-` to the lookbehind so a positive token does not resolve against a negative JSON value. Schema string bumped to `\"v2.1 (Phase 14 plan 14-14 — negative-sign-aware lookbehind)\"`. Docstring/comments include a regression-block example. Differential test (positive `0.0001` vs JSON `-0.0001`) added to the `__main__` self-test in Task 5."
      min_lines: 200
    - path: "results/figures/_introspect_quantum.json"
      provides: "Re-emitted (Task 1) with `\"render_only\": true` added at top level — flags the file as a render-only companion (not a resolution source). Mirrors the convention already in place for figure-companion JSONs from 14-04 / 14-10."
    - path: "results/figures/_introspect_wgan_cnn.json"
      provides: "Re-emitted (Task 1) with `\"render_only\": true` at top level. Same convention as quantum introspect."
    - path: "results/figures/_introspect_wgan_lstm.json"
      provides: "Re-emitted (Task 1) with `\"render_only\": true` at top level."
    - path: "results/figures/_introspect_wgan_mlp.json"
      provides: "Re-emitted (Task 1) with `\"render_only\": true` at top level."
    - path: "results/noise_model_sensitivity.json"
      provides: "Re-emitted (Task 1) with `\"data_hash\": \"91e447d4624e25b3\"` at top level — corpus consistency with the audited dataset hash already used across `model_info.json`, `methods_full.json`, `circuit_diagrams.json`, `classical_architectures.json`, `framework_versions.json` (14-13 T4). MED finding closed."
    - path: "results/shot_noise_sensitivity.json"
      provides: "Re-emitted (Task 1) with `\"data_hash\": \"91e447d4624e25b3\"` at top level. Same convention as noise_model_sensitivity."
    - path: "results/ansatz_comparison.json"
      provides: "Re-emitted (Task 1) with `\"data_hash\": \"91e447d4624e25b3\"` at top level. Closes the third REPRO-LOW-1 sensitivity JSON flagged by methods-reproducibility-review-r2.md:269."
    - path: "run_matched2000.py"
      provides: "Capture-before-`.to(cpu)` fix (Task 2). `_train_quantum`, `_train_wgan`, `_train_vae` each capture `training_time_device` IMMEDIATELY after their training loop returns, BEFORE `generate_wgan_samples(generator, ...)` (line 270) or `vae.sample(...).cpu()` moves the model to CPU. Value stored on `extra_cfg[\"training_time_device\"]` (or passed through `_device_manifest`). `_device_manifest` accepts an optional pre-captured device, falling back to `next(parameters()).device` for backward compat. The `_strict_accept` equality check from 14-13 T4 is UNCHANGED — it now actually verifies what it claims to verify (the D-14-13 future-gate becomes structurally sound)."
    - path: "docs/methods_full.md"
      provides: "Multiple edits (Tasks 3, 4): §3.x.d (lines 316–327) rewritten with corrected VAE β derivation showing β_eff = 2.5 (KL up-weighted) with the canonical-sum-form math + a semantic interpretation sentence (Task 3 / M-4 correction); §2.i (VAE architecture, lines 190–200) gains the VAE-not-param-matched caveat sentence (Task 3); §5.2 references retain their existing structure (Task 4 only flips `1-80` → `1-69` line-range labels at :431, :455 and :536). All other sections preserved verbatim."
    - path: "docs/reconciliation_note.md"
      provides: "Interpretation paragraph reworded (Task 3) to the seed-variance + wgan_cnn-honesty framing. All table rows (including the 14-12 caveat paragraph and 14-13 metric-redefinition disclosure) preserved verbatim."
    - path: "docs/reviewer_response.md"
      provides: "R1-m4 row updated (Task 4) to the DOI-pending wording explicitly stating `Zenodo DOI deposit pending under Plan 14-07`. All other rows + Cross-Cutting + Completeness sweep + 14-13 CR-4 disclosure preserved verbatim."
    - path: "run_methods_full.py"
      provides: "Three coordinated edits (Task 4): (a) line-range labels in the docstring-slicer change from `1-80` to `1-69` at lines 152, 265, 524 — the actual `run_matched2000.py` docstring ends at line 69; (b) `_citations[\"generator_to_compute_dtype\"]` pattern changes from `\"generated_samples = generator\"` (resolves to `training.py:346`, the generator call) to `\"generated_samples = generated_samples.to(compute_dtype)\"` (resolves to `training.py:347`, the actual dtype cast site that the original CR-3 finding intended); (c) re-emit `results/methods_full.json` and `docs/methods_full.md` via the existing emit path."
    - path: "results/manuscript_apparatus_constants.json"
      provides: "Restructured (Task 4): `apparatus_dimensions_mm: {depth_or_height_880, ancillary_120, ancillary_6, ancillary_10}` → `apparatus_dimensions: {tube_outer_diameter_cm: 6, tube_length_cm: 120, data_logging_interval_min: 10, ir_led_wavelength_nm: 880}` (precise field assignment verified during T4 execution by reading `main (4) copy.tex:176-180`). The 880-nm IR LED wavelength is no longer lumped under a `_mm` label."
    - path: "requirements-pinned.txt"
      provides: "Extended (Task 4) with `statsmodels==0.14.5` (the live version, currently missing from the pin file). Matches the audited package set across the running interpreter."
    - path: "results/framework_versions.json"
      provides: "Re-emitted (Task 4) with `statsmodels` recorded at version `0.14.5`. The emitter `run_framework_versions.py` is extended to probe statsmodels if it does not already do so."
    - path: "REPRODUCE.md"
      provides: "NEW top-level reproducibility entry-point (Task 4, ~50–80 lines): (a) one-paragraph orientation; (b) clone + venv + `pip install -r requirements-pinned.txt`; (c) checkout the manuscript-revision tag (currently `main` until 14-07 lands `v2.0-revision`); (d) commands to regenerate the headline metric, sweep aggregates, figures, and the methods doc, in dependency order; (e) link to `docs/methods_full.md §5.2` (full per-script breakdown) and `docs/completeness_sweep_manifest.md` (cross-plan navigation)."
      min_lines: 50
    - path: "docs/peer_review_remediation.md"
      provides: "Appended two new sections (Task 5): `## Gate v2.1 known limitations` documenting the R2-code-HIGH-2 ε-neighborhood low-precision coincidence risk (mitigated by `--manifest`, not eliminated); `## R2 follow-up sweep` mapping each r2 finding to its 14-14 commit SHA. The existing 14-13 finding-to-commit tables (code-review.md, math-review.md, quantum-review.md, provenance-review.md, methods-reproducibility-review.md) and the OOS section are preserved verbatim."
    - path: "docs/completeness_sweep_manifest.md"
      provides: "Updated (Task 5) with a new `## Plan 14-14 — r2 peer-review punch list (Wave 12)` section at the bottom enumerating the T1–T5 deliverables; existing 14-09 / 14-10 / 14-11 / 14-12 / 14-13 sections preserved verbatim."
  key_links:
    - from: "verify_number_provenance.py:193 (v2.1 lookbehind class)"
      to: "all 10 paper-facing docs"
      via: "Task 5 runs the v2.1 gate against `paper_blocks_framing.md`, `paper_blocks_refs_methods.md`, `reviewer_response.md`, `reconciliation_note.md`, `methods_full.md`, `circuit_atlas.md`, `completeness_sweep_manifest.md`, `training_protocol.md`, `dataset_stats.md`, `peer_review_remediation.md` (+ `REPRODUCE.md` if any paper-facing claims appear in it); all must PASS under the v2.1 schema; differential test added to `__main__` confirms positive `0.0001` does not resolve against JSON `-0.0001`"
      pattern: "verify_number_provenance.py --target|v2.1 \\(Phase 14 plan 14-14|--differential-test"
    - from: "run_matched2000.py (Task 2 capture-before-.to(cpu))"
      to: "`training_time_device` reflects the actual training-time device (not the post-.to(cpu) device)"
      via: "each of `_train_quantum`, `_train_wgan`, `_train_vae` captures `next(generator.parameters()).device` (and `next(critic.parameters()).device` where applicable) IMMEDIATELY after the training loop returns, BEFORE `generate_wgan_samples(generator, ...)` or `vae.sample(...).cpu()` moves the model to CPU; the captured value is passed through `_device_manifest` (which accepts an optional pre-captured device); the `_strict_accept` equality check from 14-13 T4 now actually verifies what it claims"
      pattern: "training_time_device|extra_cfg\\[.training_time_device|capture.*before"
    - from: "docs/methods_full.md §3.x.d (Task 3 corrected derivation)"
      to: "run_baselines.py:315-319 (per-element-mean MSE + per-element-mean KLD code)"
      via: "the doc derivation cites the actual loss code; `loss = recon + 1·kld = sum_MSE/10 + sum_KLD/4 = (1/10)·(sum_MSE + 2.5·sum_KLD)`, so β_eff = 2.5 (KL up-weighted), NOT 0.4 (the inverted figure propagated from r1 M-4 through 14-13)"
      pattern: "β_eff = 2.5|sum_MSE/10|sum_KLD/4|run_baselines.py:315"
    - from: "results/manuscript_apparatus_constants.json (Task 4 restructured fields)"
      to: "main (4) copy.tex:176-180 (the LUCY apparatus / IR LED context)"
      via: "T4 reads the LaTeX context (`main (4) copy.tex:176-180` — 6 cm tube OD, 120 cm tube length, 10-minute data logging interval, 880 nm IR LED wavelength) to assign field semantics exactly: `tube_outer_diameter_cm: 6`, `tube_length_cm: 120`, `data_logging_interval_min: 10`, `ir_led_wavelength_nm: 880`; the 880-nm IR LED wavelength is no longer lumped under a `_mm` label, and the 10-minute logging interval is no longer mislabeled as a dimension; v2.1 gate corpus updated; gate re-runs PASS against `paper_blocks_framing.md`"
      pattern: "apparatus_dimensions|ir_led_wavelength_nm|tube_outer_diameter_cm|tube_length_cm|data_logging_interval_min"
    - from: "REPRODUCE.md (Task 4 NEW top-level entry-point)"
      to: "docs/methods_full.md §5.2 + docs/completeness_sweep_manifest.md"
      via: "REPRODUCE.md orients the reviewer at the repo root and links to methods_full.md §5.2 (full per-script breakdown) and completeness_sweep_manifest.md (cross-plan navigation), giving a one-stop reproducibility entrypoint that complements the methods doc"
      pattern: "REPRODUCE\\.md|methods_full\\.md §5\\.2|completeness_sweep_manifest\\.md"
    - from: "docs/peer_review_remediation.md (Task 5 R2 follow-up sweep section)"
      to: "15 r2 findings (3 triangulated HIGHs + 12 lower-severity)"
      via: "every r2 finding maps to a 14-14 commit SHA in the new `## R2 follow-up sweep` section; the R2-code-HIGH-2 ε-neighborhood limitation is explicitly DISCLOSED in the new `## Gate v2.1 known limitations` section as accepted with `--manifest` mitigation"
      pattern: "R2 follow-up sweep|Gate v2.1 known limitations|R2-code-HIGH-2"
---

<objective>
Close 15 of the 15 actionable findings (3 triangulated HIGHs + 12 lower-severity)
from the 5-agent r2 peer-review pass on Phase 14 plan 14-13's remediation work
in a single atomic plan. Apply the one-character gate fix that lifts v2 to v2.1
(negative-sign-aware lookbehind, eliminating the positive→negative false
positive that was particularly concerning around `reconciliation_deltas.json`'s
small negative deltas). Correct the `training_time_device` capture site so the
D-14-13 future-gate from 14-13 T4 becomes structurally sound instead of
silently always reading `cpu`. Rewrite the VAE β derivation in `methods_full.md
§3.x.d` from the inverted `β_eff ≈ 0.4` propagated from r1 M-4 through 14-13 to
the correct **β_eff = 2.5** (KL up-weighted) backed by per-element-mean math
citing `run_baselines.py:315-319`. Add the VAE-not-param-matched caveat to §2.i.
Soften the `reconciliation_note.md` interpretation paragraph to the wgan_cnn
seed-variance-honest framing. Update `reviewer_response.md` R1-m4 to the
DOI-pending wording. Flip the `run_methods_full.py` docstring-slicer line range
from the inaccurate `1-80` to the actual `1-69`. Re-target the CR-3 citation
pattern from `training.py:346` (the generator call) to `training.py:347` (the
actual dtype cast site that CR-3 was about). Restructure
`manuscript_apparatus_constants.json` to per-unit subfields so the 880-nm IR
LED wavelength stops being lumped under an `_mm` label. Pin `statsmodels==0.14.5`
in `requirements-pinned.txt` + `framework_versions.json`. Create a top-level
`REPRODUCE.md` reviewer entry-point. Add `"render_only": true` to the 4
`_introspect_*.json` files. Add `"data_hash": "91e447d4624e25b3"` to
`noise_model_sensitivity.json`, `shot_noise_sensitivity.json`, and `ansatz_comparison.json`. Re-run the
v2.1 gate on all 10 paper-facing docs. Disclose the R2-code-HIGH-2
ε-neighborhood low-precision coincidence as a known gate limitation
(`--manifest` mitigation, no further tightening — locked decision). Update
`completeness_sweep_manifest.md` + `peer_review_remediation.md` with the new
14-14 sections. NO retraining; NO new figures; NO classical sweep re-run;
`core/` byte-frozen throughout (D-14-22).

Purpose: 14-13 closed 27 of 28 actionable findings from the r1 peer-review pass
and was rated `ready for paper resubmission YES` by every r2 reviewer. The r2
pass surfaced 0 CRITICAL, 5 HIGH, 4 MEDIUM, 6 LOW findings; three of the HIGHs
are triangulated by direct file verification (the Explore-agent verification
disagreed on two of them and was wrong — the source code matches the r2
reviewers' analyses). Fixing them before 14-07 (Zenodo deposit + tag) tightens
the paper-revision presentation layer one more click and makes 14-07 the only
remaining outstanding Phase 14 plan. Pure correction sweep, no retraining.

Output: 1 one-character gate fix (v2.1) + 6 introspect/sensitivity JSON
re-emits + 1 device-capture correction in run_matched2000.py + 3 doc corrections
in methods_full.md (§3.x.d β derivation, §2.i VAE caveat) + reconciliation_note
interpretation rewording + reviewer_response R1-m4 DOI-pending wording + 4
surgical run_methods_full.py edits (line ranges + CR-3 citation pattern) + 1
restructured apparatus JSON + 1 statsmodels pin in 2 files + 1 new REPRODUCE.md
at repo root + 2 new sections in peer_review_remediation.md + 1 new section in
completeness_sweep_manifest.md + 1 SUMMARY. All gated by the v2.1 provenance
gate on all 10 paper-facing docs; `core/` byte-frozen (D-14-22); no
retraining.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-paper-revision-release-freeze/14-CONTEXT.md
@.planning/phases/14-paper-revision-release-freeze/14-PATTERNS.md
@.planning/phases/14-paper-revision-release-freeze/14-13-PLAN.md
@.planning/phases/14-paper-revision-release-freeze/14-13-SUMMARY.md
@.planning/phases/14-paper-revision-release-freeze/peer-review-r2/code-review-r2.md
@.planning/phases/14-paper-revision-release-freeze/peer-review-r2/math-review-r2.md
@.planning/phases/14-paper-revision-release-freeze/peer-review-r2/quantum-review-r2.md
@.planning/phases/14-paper-revision-release-freeze/peer-review-r2/provenance-review-r2.md
@.planning/phases/14-paper-revision-release-freeze/peer-review-r2/methods-reproducibility-review-r2.md

<contract_changes>
Explicit deviations from prior Phase 14 contracts (mirrors 14-13's contract
changes section but narrower in scope — 14-14 is the punch list, not the
remediation sweep):

- **D-14-16 (gate byte-freeze) LIFTED for Task 1 only.** One-character fix to
  the gate's boundary regex at `verify_number_provenance.py:193`:
  lookbehind class `(?<![\d.])` → `(?<![-\d.])` (adding `-` so a positive
  token does not resolve against a negative JSON value). Schema string bumps
  to `"v2.1 (Phase 14 plan 14-14 — negative-sign-aware lookbehind)"`. After
  Task 1 the gate is back in byte-freeze under v2.1.

- **D-14-22 (`core/` byte-freeze) PRESERVED across all 5 tasks.** All
  math-doc corrections (M-4 VAE β derivation, VAE-not-param-matched caveat,
  reconciliation_note interpretation wording, reviewer_response R1-m4 DOI
  wording) are doc-only. `git diff --stat core/` is empty at every
  task close.

- **D-14-13 (strict-accept gate) PRESERVED.** Task 2 corrects the CAPTURE SITE
  of `training_time_device` without changing gate semantics. The
  `_strict_accept` equality check added in 14-13 T4 is unchanged — it just
  gets a value that actually reflects training device (the manifest currently
  always sees `cpu` because `generate_wgan_samples` at line 270 moves the
  model to CPU before `_device_manifest` reads `next(generator.parameters()).device`
  at lines 333–335). The MPS-disable monkey-patch IS the real protection;
  the gate that's supposed to CERTIFY it is non-functional in current shape
  and Task 2 makes it functional.

- **R2-code-HIGH-2 (ε-neighborhood broad coincidences) is DISCLOSED, NOT
  TIGHTENED.** Locked decision: the gate is not made tighter. The
  `--manifest` human-spot-check flag is the mitigation. Task 5 documents the
  limitation in `peer_review_remediation.md`.
</contract_changes>

<interfaces>
<!-- All upstream artifacts (Phases 9-14 through 14-13) are EXISTING and
     AUDITED on disk; this plan only patches 1 gate line + 6 JSONs + 1
     emitter + 3 docs + 4 surgical script edits + 2 new docs (REPRODUCE.md
     + appended sections in peer_review_remediation.md and
     completeness_sweep_manifest.md). -->

verify_number_provenance.py (the gate being one-char-patched in Task 1):
- Line 193: boundary lookbehind class `(?<![\d.])` is missing `-`; a positive
  token resolves against a negative JSON value (e.g. positive `0.0001` in a
  doc against JSON `-0.0001`). After Task 1: `(?<![-\d.])`. Schema string
  bumps to `"v2.1 (Phase 14 plan 14-14 — negative-sign-aware lookbehind)"`.
- Task 5 adds a differential-test assertion to the `__main__` self-test:
  positive `0.0001` in a synthetic doc + JSON corpus carrying `-0.0001`
  must FAIL to resolve (the regression that motivated the v2.1 bump).

run_matched2000.py (Task 2 capture-before-.to(cpu) fix):
- Lines 260-285 are `generate_wgan_samples` with `generator = generator.to("cpu")`
  at line 270 — this is the migration that defeats the current
  `_device_manifest` read.
- Lines 320-345 are `_device_manifest` (reads `next(generator.parameters()).device`
  at lines 333–335; sees `cpu` after the migration).
- Lines 387-506 are `_train_quantum`; Lines 509-579 are `_train_wgan`; Lines
  582-672 are `_train_vae`. Each captures `training_time_device` IMMEDIATELY
  after training, BEFORE the migration, and passes it through `_device_manifest`.
- `_strict_accept` (from 14-13 T4) — the equality check is UNCHANGED; only the
  value it sees changes.

docs/methods_full.md (Task 3 + Task 4 surgical edits):
- Lines 316-327 (§3.x.d): the VAE β derivation currently claims β_eff ≈ 0.4
  (= L/W = 4/10, the INVERTED ratio propagated from r1 M-4 through 14-13).
  Task 3 rewrites to the correct β_eff = 2.5 (= W/L) with per-element-mean
  math derivation.
- Lines 190-200 (§2.i VAE architecture): Task 3 appends the VAE-not-param-matched
  caveat sentence.
- Lines 431, 455, 536: `1-80` mentions → `1-69` (Task 4, mirroring the
  run_methods_full.py docstring-slicer fix).

docs/reconciliation_note.md (Task 3 interpretation rewording):
- The interpretation paragraph (just below the EMD OD-scale table from
  14-13) currently reads: `Deltas are ≈ 0 across the board: the matched
  2000-epoch budget recovers OD-scale EMD within seed variance of the
  1000-epoch baseline.` Task 3 replaces with the Welch-t-test + wgan_cnn
  seed-variance honesty wording. All table rows preserved.

docs/reviewer_response.md (Task 4 R1-m4 DOI-pending rewording):
- The R1-m4 row currently implies the Zenodo DOI is delivered while 14-07 is
  still open. Task 4 rewords to explicit pending-under-14-07 language.

run_methods_full.py (Task 4 four edits):
- Lines 152, 265, 524: `1-80` line-range labels in the docstring-slicer
  → `1-69` (the actual `run_matched2000.py` docstring ends at line
  69, an off-by-11 error).
- The `_citations["generator_to_compute_dtype"]` pattern: from
  `"generated_samples = generator"` (resolves to `training.py:346`, the
  generator call) → `"generated_samples = generated_samples.to(compute_dtype)"`
  (resolves to `training.py:347`, the actual dtype cast site).
- Re-emit `results/methods_full.json` and the downstream
  `docs/methods_full.md` line-range mentions.

results/manuscript_apparatus_constants.json (Task 4 restructure):
- Current: `apparatus_dimensions_mm: {depth_or_height_880, ancillary_120,
  ancillary_6, ancillary_10}` — lumps 880 (NIR wavelength, nm) with 120/6/10
  (likely mm) under a `_mm` label.
- After: `apparatus_dimensions: {tube_outer_diameter_cm: 6,
  tube_length_cm: 120, data_logging_interval_min: 10,
  ir_led_wavelength_nm: 880}` (per LaTeX `main (4) copy.tex:176-180`:
  6 cm OD, 120 cm length, 10-min logging interval, 880 nm IR LED;
  `main (4) copy.tex:176-180` dictates — verified at T4 execution time).

requirements-pinned.txt + results/framework_versions.json
(Task 4 statsmodels pin):
- `statsmodels==0.14.5` is the live version. Currently missing from both
  files. Task 4 adds the pin to `requirements-pinned.txt` and extends
  `run_framework_versions.py` to probe statsmodels if needed.

REPRODUCE.md (Task 4 NEW top-level file at repo root):
- ~50–80 lines. (a) one-paragraph orientation; (b) clone + venv + `pip install
  -r requirements-pinned.txt`; (c) checkout the manuscript-revision
  tag (currently `main` until 14-07 lands `v2.0-revision`); (d) commands to
  regenerate the headline metric, sweep aggregates, figures, and the methods
  doc, in dependency order; (e) link to `docs/methods_full.md §5.2`
  + `docs/completeness_sweep_manifest.md`.

results/figures/_introspect_{quantum,wgan_cnn,wgan_lstm,wgan_mlp}.json
(Task 1):
- Each gains `"render_only": true` at top level — flags as a render-only
  companion (not a resolution source). Mirrors the convention already in
  place for figure-companion JSONs from 14-04 / 14-10.

results/noise_model_sensitivity.json + shot_noise_sensitivity.json + ansatz_comparison.json
(Task 1):
- Each gains `"data_hash": "91e447d4624e25b3"` at top level — corpus
  consistency with the audited dataset hash from 14-13 T4.

docs/peer_review_remediation.md (Task 5 append two sections):
- `## Gate v2.1 known limitations` documents the R2-code-HIGH-2
  ε-neighborhood low-precision coincidence (e.g. `-0.26` AR-bias factor
  resolving to a kurtosis value `-0.2570036` at low precision); mitigated by
  `--manifest`, NOT eliminated; the locked decision is to disclose, not
  tighten.
- `## R2 follow-up sweep` maps each r2 finding to its 14-14 commit SHA
  (same structure as the existing 14-13 finding-to-commit tables).

docs/completeness_sweep_manifest.md (Task 5 append one section):
- `## Plan 14-14 — r2 peer-review punch list (Wave 12)` enumerating the T1–T5
  deliverables. Existing 14-09 / 14-10 / 14-11 / 14-12 / 14-13 sections
  preserved verbatim.

Peer-review source reports (read-only references; do NOT modify):
- .planning/phases/14-paper-revision-release-freeze/peer-review-r2/code-review-r2.md
- .planning/phases/14-paper-revision-release-freeze/peer-review-r2/math-review-r2.md
- .planning/phases/14-paper-revision-release-freeze/peer-review-r2/quantum-review-r2.md
- .planning/phases/14-paper-revision-release-freeze/peer-review-r2/provenance-review-r2.md
- .planning/phases/14-paper-revision-release-freeze/peer-review-r2/methods-reproducibility-review-r2.md

Critical files to read before each task (per the approved plan):

- **T1:** `verify_number_provenance.py:185-220` (the gate `_resolves`
  function); 4 `_introspect_*.json` top-levels; 2 `*_sensitivity.json` top-levels.
- **T2:** `run_matched2000.py:260-285` (`generate_wgan_samples` with
  `.to("cpu")` at line 270); `:320-345` (`_device_manifest`); `:387-506`
  (`_train_quantum`); `:509-579` (`_train_wgan`); `:582-672` (`_train_vae`).
- **T3:** `docs/methods_full.md` §3.x.d (lines 316–327) + §2.i (lines
  190–200); `run_baselines.py:305-325` (the actual VAE ELBO code);
  `docs/reconciliation_note.md` (the interpretation paragraph + the
  wgan_cnn table row).
- **T4:** `docs/reviewer_response.md` R1-m4 row;
  `run_methods_full.py:115-180, 524`; `core/training.py:340-360`
  (line 346 vs 347 cast site); `results/manuscript_apparatus_constants.json`;
  `main (4) copy.tex:176-180` (the LUCY/IR-LED context).
- **T5:** All 10 paper-facing docs; `docs/peer_review_remediation.md`;
  `docs/completeness_sweep_manifest.md`; the 5 r2 reports at
  `.planning/phases/14-paper-revision-release-freeze/peer-review-r2/*.md`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gate v2.1 (negative-sign-aware lookbehind, LIFTS D-14-16 one char) + introspect render_only marks + sensitivity data_hash propagation</name>
  <files>verify_number_provenance.py, results/figures/_introspect_quantum.json, results/figures/_introspect_wgan_cnn.json, results/figures/_introspect_wgan_lstm.json, results/figures/_introspect_wgan_mlp.json, results/noise_model_sensitivity.json, results/shot_noise_sensitivity.json, results/ansatz_comparison.json</files>
  <read_first>
    - verify_number_provenance.py:185-220 (the gate's `_resolves` function — line 193 carries the boundary lookbehind class `(?<![\d.])` that this task patches to `(?<![-\d.])` by adding `-`; schema-string declaration is near the top of the file, locate it before editing)
    - results/figures/_introspect_quantum.json, _introspect_wgan_cnn.json, _introspect_wgan_lstm.json, _introspect_wgan_mlp.json (the 4 introspect JSONs that gain `"render_only": true` at top level — verify each is a top-level dict before adding the field; if any is an array, fall back to a wrapper dict policy and surface in commit body)
    - results/noise_model_sensitivity.json, results/shot_noise_sensitivity.json, results/ansatz_comparison.json (the 3 sensitivity JSONs flagged by methods-reproducibility-review-r2.md REPRO-LOW-1 that gain `"data_hash": "91e447d4624e25b3"`; verify each is a top-level dict; the audited dataset hash `91e447d4624e25b3` is identical to the value already recorded across `model_info.json`, `methods_full.json`, `circuit_diagrams.json`, `classical_architectures.json`, `framework_versions.json` from 14-13 T4)
    - .planning/phases/14-paper-revision-release-freeze/peer-review-r2/provenance-review-r2.md (the r2 reviewer's gate sign-flip false-positive finding — the triangulated HIGH that motivates the v2.1 lookbehind fix)
    - .planning/phases/14-paper-revision-release-freeze/peer-review-r2/code-review-r2.md (cross-reference for the gate fix; R2-code-HIGH-2 ε-neighborhood limitation is DISCLOSED later in T5, not tightened here)
    - .planning/phases/14-paper-revision-release-freeze/peer-review-r2/methods-reproducibility-review-r2.md (the MEDIUM finding about `_introspect_*.json` lacking `"render_only": true` → semantic mis-provenance in `--manifest` output)
    - .planning/phases/14-paper-revision-release-freeze/14-CONTEXT.md — locate D-14-16 (gate byte-freeze) and confirm the lift is for THIS TASK ONLY (after Task 1 closes, the gate is back in byte-freeze under the new schema string `v2.1 (Phase 14 plan 14-14 — negative-sign-aware lookbehind)`)
  </read_first>
  <action>
    Apply the one-character lookbehind fix + add `render_only: true` to the 4
    introspect JSONs + add `data_hash` to the 2 sensitivity JSONs. NO other
    gate semantics change.

    Step A — One-character lookbehind fix at `verify_number_provenance.py:193`:
    Locate line 193 (the boundary-strict resolution regex from 14-13 T2 v2):

    ```python
    if re.search(rf"(?<![\d.]){re.escape(token)}(?![\d])", blob):
    ```

    Change to (add `-` inside the lookbehind character class):

    ```python
    if re.search(rf"(?<![-\d.]){re.escape(token)}(?![\d])", blob):
    ```

    This eliminates the sign-flip false positive where a positive token
    `0.0001` resolves against a JSON value `-0.0001`. The fix is one character
    (the `-` added to the lookbehind class).

    Step B — Schema-string bump:
    Locate the `_SCHEMA = "..."` string declaration at the top of the file
    (or wherever the gate's self-identifying version sits). Change from the
    v2 string (14-13 T2) to:

    ```python
    _SCHEMA = "v2.1 (Phase 14 plan 14-14 — negative-sign-aware lookbehind)"
    ```

    Step C — Add a regression block to the gate's docstring/comments:
    Just below the modified line 193, add an inline comment showing the
    failure mode that v2 (broken) would pass and v2.1 must reject. Suggested
    block:

    ```python
    # Regression (Phase 14 plan 14-14): the lookbehind class is `(?<![-\d.])`
    # NOT `(?<![\d.])` — without the `-`, a positive token `0.0001` in a doc
    # resolves against a JSON value `-0.0001` because the `-` does not appear
    # in the lookbehind. The negative sign is particularly common in
    # `reconciliation_deltas.json` (dense with small negative deltas), so the
    # bug had outsized impact on the reconciliation gate's reliability.
    # See peer-review-r2/provenance-review-r2.md (R2-prov-HIGH-1) and the
    # `__main__` differential-test assertion added in Task 5.
    ```

    Step D — Add `"render_only": true` to the 4 introspect JSONs:
    For each of `results/figures/_introspect_{quantum,wgan_cnn,wgan_lstm,wgan_mlp}.json`:
    1. Load the JSON; verify it is a top-level dict.
    2. Add the key/value `"render_only": true` at the top level.
    3. Re-emit with the same formatting (2-space indent, trailing newline).

    These files are figure-introspection companions (emitted by 14-04 / 14-10
    figure-suite renders); they are NOT resolution sources for the gate. The
    `"render_only": true` flag mirrors the convention already in place for
    figure-companion JSONs from 14-04 / 14-10 and lets the 14-13 T2 v2
    `_json_blobs()` walker exclude them from the resolution corpus
    consistently.

    Step E — Add `"data_hash"` to the 2 sensitivity JSONs:
    For each of `results/noise_model_sensitivity.json`,
    `results/shot_noise_sensitivity.json`, and
    `results/ansatz_comparison.json`:
    1. Load the JSON; verify it is a top-level dict.
    2. Add the key/value `"data_hash": "91e447d4624e25b3"` at the top level.
    3. Re-emit with the same formatting.

    The audited dataset hash `91e447d4624e25b3` is the same one already
    recorded across `model_info.json`, `methods_full.json`,
    `circuit_diagrams.json`, `classical_architectures.json`,
    `framework_versions.json` from 14-13 T4 (HIGH-2 / PROV-HIGH-2 corpus
    consistency). The 2 sensitivity JSONs were missed in the 14-13 T4 sweep
    and are added here for corpus consistency.

    Step F — Commit and gate-self-verify:
    Stage `verify_number_provenance.py` + the 6 modified JSONs.
    Run the v2.1 gate against `methods_full.md` (sanity check that the
    one-character fix did not break anything). The differential-test
    assertion for the sign-flip case is added to `__main__` in Task 5.

    Commit message: `feat(14-14): gate v2.1 (negative-sign-aware lookbehind) + introspect render_only marks + sensitivity data_hash`.
  </action>
  <verify>
    <automated>./qgan_env/bin/python -c "import json,pathlib,re,sys; g=pathlib.Path('verify_number_provenance.py').read_text(); errs=[]; ('v2.1 (Phase 14 plan 14-14' in g) or errs.append('schema string not v2.1-bumped'); ('(?<![-\\\\d.])' in g or '(?<![-\\d.])' in g) or errs.append('lookbehind class missing negative sign (no `-` in (?<![-\\d.]))'); [errs.append(f'{p} missing render_only:true') for p in ['results/figures/_introspect_quantum.json','results/figures/_introspect_wgan_cnn.json','results/figures/_introspect_wgan_lstm.json','results/figures/_introspect_wgan_mlp.json'] if not pathlib.Path(p).exists() or json.loads(pathlib.Path(p).read_text()).get('render_only') is not True]; [errs.append(f'{p} missing data_hash 91e447d4624e25b3') for p in ['results/noise_model_sensitivity.json','results/shot_noise_sensitivity.json','results/ansatz_comparison.json'] if not pathlib.Path(p).exists() or json.loads(pathlib.Path(p).read_text()).get('data_hash') != '91e447d4624e25b3']; print('errors',errs); sys.exit(0 if not errs else 1)" &amp;&amp; ./qgan_env/bin/python -c "import re; token='0.0001'; blob_neg='delta -0.0001 reference'; blob_pos='value 0.0001 reference'; assert not re.search(rf'(?<![-\\d.]){re.escape(token)}(?![\\d])', blob_neg), 'v2.1 regex should NOT match positive token against negative blob'; assert re.search(rf'(?<![-\\d.]){re.escape(token)}(?![\\d])', blob_pos), 'v2.1 regex SHOULD match positive token against positive blob'; print('v2.1 lookbehind sanity OK')" &amp;&amp; ./qgan_env/bin/python verify_number_provenance.py --target docs/methods_full.md &amp;&amp; [ -z "$(git diff --stat core/)" ]</automated>
  </verify>
  <acceptance_criteria>
    - `verify_number_provenance.py` line 193 lookbehind class is `(?<![-\d.])` (the `-` is present); schema string is `"v2.1 (Phase 14 plan 14-14 — negative-sign-aware lookbehind)"`; inline regression-block comment is present near line 193
    - All 4 introspect JSONs (`_introspect_quantum.json`, `_introspect_wgan_cnn.json`, `_introspect_wgan_lstm.json`, `_introspect_wgan_mlp.json`) carry `"render_only": true` at top level
    - `noise_model_sensitivity.json` + `shot_noise_sensitivity.json` + `ansatz_comparison.json` carry `"data_hash": "91e447d4624e25b3"` at top level
    - Lookbehind sanity (Python regex test): positive token `0.0001` does NOT match against blob `delta -0.0001 reference` but DOES match against blob `value 0.0001 reference`
    - v2.1 gate PASSES on `methods_full.md` (sanity check that the fix did not break anything)
    - `git diff --stat core/` is empty (D-14-22)
  </acceptance_criteria>
  <done>Gate v2.1 one-character fix applied (negative-sign-aware lookbehind); schema string bumped; regression-block comment added; 4 introspect JSONs marked render_only; 2 sensitivity JSONs carry data_hash; D-14-16 lift documented (limited to this task); D-14-22 preserved.</done>
</task>

<task type="auto">
  <name>Task 2: training_time_device capture-before-.to(cpu) fix — D-14-13 future-gate becomes structurally sound</name>
  <files>run_matched2000.py</files>
  <read_first>
    - run_matched2000.py:260-285 (`generate_wgan_samples`; line 270 carries `generator = generator.to("cpu")` — the migration that defeats the current `_device_manifest` read)
    - run_matched2000.py:320-345 (`_device_manifest`; reads `next(generator.parameters()).device` at lines 333–335; sees `cpu` after the migration; this is the function that gets the optional pre-captured device parameter under Task 2)
    - run_matched2000.py:387-506 (`_train_quantum`; already trains on CPU under the `torch.backends.mps.is_available = lambda: False` hook from earlier; the capture mechanism is mechanically identical to `_train_wgan` / `_train_vae`, the value will always be `"cpu"` in current paths but the fix is symmetric)
    - run_matched2000.py:509-579 (`_train_wgan`; captures `training_time_device` IMMEDIATELY after the `train_wgan_gp` training loop returns, BEFORE `generate_wgan_samples(generator, ...)` is called)
    - run_matched2000.py:582-672 (`_train_vae`; captures `training_time_device` IMMEDIATELY after the VAE training loop returns, BEFORE the parallel `vae.sample(...).cpu()` migration happens)
    - .planning/phases/14-paper-revision-release-freeze/peer-review-r2/code-review-r2.md (the r2 reviewer's triangulated HIGH finding on `training_time_device` future-gate being structurally broken at run_matched2000.py:556 / lines 333–335 read sees `cpu` regardless of training device)
    - .planning/phases/14-paper-revision-release-freeze/peer-review-r2/provenance-review-r2.md (cross-reference for the same finding)
    - .planning/phases/14-paper-revision-release-freeze/14-CONTEXT.md — locate D-14-13 (strict-accept gate) extension from 14-13 T4 (the equality check is PRESERVED — only the capture site changes here so the value the gate sees actually reflects training device)
    - results/matched2000.json (most recent emission; verify `training_time_device` field is currently present with the post-`.to(cpu)` value `"cpu"` so Task 2's correctness can be confirmed by diffing against a subsequent emit)
  </read_first>
  <action>
    Capture `training_time_device` IMMEDIATELY after each training loop
    returns, BEFORE the model moves to CPU. Apply symmetrically across all
    three training paths. The `_strict_accept` equality check (added in 14-13
    T4) is UNCHANGED — only the value it sees changes.

    Step A — Update `_device_manifest` to accept an optional pre-captured device:
    Locate `_device_manifest` at `run_matched2000.py:320-345`. Add
    an optional parameter `training_time_device` (or similarly named) with
    default `None`. If provided, use it for the `training_time_device` field
    in the manifest; otherwise fall back to the current
    `next(parameters()).device` read (backward compat with any other caller).

    Conceptual signature change:

    ```python
    def _device_manifest(generator, *, critic=None, training_time_device=None):
        ...
        manifest["training_time_device"] = (
            training_time_device
            if training_time_device is not None
            else str(next(generator.parameters()).device)
        )
        return manifest
    ```

    Step B — Capture `training_time_device` in `_train_wgan` BEFORE `generate_wgan_samples`:
    Locate `_train_wgan` at lines 509-579. Find the spot RIGHT AFTER
    `train_wgan_gp(...)` returns (the WGAN-GP training loop's terminal
    return) and BEFORE `generate_wgan_samples(generator, ...)` is called
    (which at line 270 does `generator = generator.to("cpu")`).

    At that capture point, record the device by inspecting
    `next(generator.parameters()).device` (and `next(critic.parameters()).device`
    if applicable — they should agree). Store the captured device string
    on `extra_cfg["training_time_device"]` (or whatever local context dict
    is passed through to `_device_manifest`).

    Pass the captured value through to `_device_manifest` when the manifest
    is built.

    Step C — Capture `training_time_device` in `_train_vae` BEFORE the sample-generation migration:
    Locate `_train_vae` at lines 582-672. Find the spot RIGHT AFTER the VAE
    training loop returns and BEFORE the parallel `vae.sample(...).cpu()`
    migration happens. Apply the same capture mechanism as in Step B.

    Step D — Capture `training_time_device` in `_train_quantum` symmetrically:
    Locate `_train_quantum` at lines 387-506. Apply the same capture
    mechanism. The quantum path already trains on CPU under the
    `torch.backends.mps.is_available = lambda: False` hook from 14-13 T4, so
    the value will always be `"cpu"` in current paths; the fix is mechanically
    identical (symmetry across all three training paths) and protects against
    future drift if the quantum path ever moves to MPS.

    Step E — `_strict_accept` is UNCHANGED:
    The `_strict_accept` equality check added in 14-13 T4 (which requires
    `training_time_device` equality across all models in a sweep) is
    UNCHANGED. The function body, error message, and gate semantics stay as
    written. The change in Task 2 is purely the capture site — what value
    the gate's equality check actually sees.

    Step F — Re-emit and verify:
    Re-run `run_matched2000.py` (or the smallest invocation path
    that exercises a sample matched-budget emit — a single seed across a
    single model is sufficient to confirm the capture-before-`.to(cpu)`
    correctness by inspecting the emitted `matched2000.json` manifest's
    `training_time_device` field). Confirm the manifest field reflects the
    actual training-time device (not just `cpu`).

    NOTE: A full MPS-hardware test is NOT required (the existing CR-4
    future-gate forces CPU-only training in all paths). The structural-soundness
    check is sufficient: the captured value flows through `_device_manifest`
    correctly and `_strict_accept` sees what it claims to see.

    Commit message: `feat(14-14): training_time_device captured pre-.to(cpu) — D-14-13 future-gate now structurally sound`.
  </action>
  <verify>
    <automated>./qgan_env/bin/python -c "import pathlib,re,sys; rm=pathlib.Path('run_matched2000.py').read_text(); errs=[]; ('training_time_device' in rm) or errs.append('run_matched2000 missing training_time_device'); m_dm=re.search(r'def _device_manifest\\([^)]*\\)', rm); (m_dm and 'training_time_device' in m_dm.group(0)) or errs.append('_device_manifest signature missing training_time_device optional param'); rm.count('training_time_device') >= 4 or errs.append(f'training_time_device only appears {rm.count(\"training_time_device\")}× in run_matched2000.py — expected ≥4 (3 capture sites + _device_manifest signature)'); print('errors',errs); sys.exit(0 if not errs else 1)" &amp;&amp; ./qgan_env/bin/python verify_number_provenance.py --target docs/methods_full.md &amp;&amp; [ -z "$(git diff --stat core/)" ]</automated>
  </verify>
  <acceptance_criteria>
    - `_device_manifest` accepts an optional pre-captured `training_time_device` parameter (kw-only or positional), falling back to `next(parameters()).device` if absent (backward compat)
    - `_train_wgan` captures `training_time_device` IMMEDIATELY after `train_wgan_gp` returns and BEFORE `generate_wgan_samples(generator, ...)` is called (which at line 270 does `generator = generator.to("cpu")`); the captured value flows through to `_device_manifest`
    - `_train_vae` captures `training_time_device` IMMEDIATELY after the VAE training loop returns and BEFORE the parallel `vae.sample(...).cpu()` migration
    - `_train_quantum` applies the same capture mechanism symmetrically (value will be `"cpu"` in current paths under the existing CR-4 MPS-disable hook, but the mechanism is identical and protects against future drift)
    - `_strict_accept` equality check from 14-13 T4 is UNCHANGED (function body, error message, gate semantics stay as written)
    - `training_time_device` appears at least 4× in `run_matched2000.py` (3 capture sites + the `_device_manifest` signature)
    - `git diff --stat core/` is empty (D-14-22)
  </acceptance_criteria>
  <done>training_time_device captured pre-.to(cpu) in all three training paths (_train_quantum, _train_wgan, _train_vae); _device_manifest accepts optional pre-captured value with backward-compat fallback; _strict_accept equality check unchanged but now sees the actual training device; D-14-13 future-gate becomes structurally sound; D-14-22 preserved.</done>
</task>

<task type="auto">
  <name>Task 3: VAE β derivation correction (β_eff = 2.5, not 0.4) + VAE-not-param-matched caveat + reconciliation_note wgan_cnn seed-variance honesty</name>
  <files>docs/methods_full.md, docs/reconciliation_note.md</files>
  <read_first>
    - docs/methods_full.md §3.x.d (lines 316–327; this is the section being rewritten from the inverted β_eff ≈ 0.4 to the correct β_eff = 2.5; the math derivation must cite run_baselines.py:315-319 for the per-element-mean MSE and per-element-mean KLD code)
    - docs/methods_full.md §2.i (VAE architecture, lines 190–200; gains the VAE-not-param-matched caveat sentence appended at the end of the section)
    - run_baselines.py:305-325 (the actual VAE ELBO code; verify the loss expression is `loss = recon + 1·kld` with `recon = sum_MSE/N_recon` and `kld = sum_KLD/N_kld`; verify N_recon = window × features = 10 and N_kld = latent_dim = 4)
    - docs/reconciliation_note.md (the WHOLE existing file; the interpretation paragraph below the EMD OD-scale table is the target of the rewording; all table rows + 14-12 caveat paragraph + 14-13 metric-redefinition disclosure are preserved verbatim)
    - results/matched2000_dualscale.json#aggregates (verify the wgan_cnn entry's actual delta value `-0.059` and the seed-variance framing — Welch t-test p-values per model)
    - .planning/phases/14-paper-revision-release-freeze/peer-review-r2/math-review-r2.md (the r2 reviewer's triangulated HIGH on VAE β implicit derivation inverted; the original r1 M-4 already contained this contradiction `cited 2.5 ratio but named 0.4 equivalent`; 14-13 propagated rather than corrected it)
    - .planning/phases/14-paper-revision-release-freeze/peer-review-r2/methods-reproducibility-review-r2.md (the MEDIUM finding on `methods_full.md §2.i` (VAE arch) lacking the VAE-not-param-matched caveat)
    - .planning/phases/14-paper-revision-release-freeze/14-CONTEXT.md — locate D-14-22 (core/ byte-freeze) PRESERVED for this task; the corrections are doc-only
  </read_first>
  <action>
    Three doc-only edits across two files. No `core/` touched.

    Step A — Rewrite `methods_full.md §3.x.d` β derivation (M-4 correction):
    Locate §3.x.d at lines 316–327. The existing text claims β_eff ≈ 0.4
    (= L/W = 4/10), which is the INVERTED ratio propagated from r1 M-4
    through 14-13. Replace with the corrected derivation:

    > **(d) VAE ELBO formulation (corrected per Plan 14-14, math-review-r2 M-4
    > correction).** The implemented VAE loss at `run_baselines.py:315-319`
    > uses per-element-mean reconstruction MSE plus per-element-mean KL
    > divergence with the standard `β = 1` ELBO coefficient. With
    > per-element-mean MSE = sum_MSE / N_recon (where N_recon = window ×
    > features = 10 × 1 = 10) and per-element-mean KLD = sum_KLD / N_kld
    > (where N_kld = latent_dim = 4), the implemented loss factors as:
    >
    > ```
    > loss = recon + 1·kld
    >      = sum_MSE/10 + sum_KLD/4
    >      = (1/10) · (sum_MSE + (10/4)·sum_KLD)
    >      = (1/10) · (sum_MSE + 2.5·sum_KLD)
    > ```
    >
    > Hence the canonical-sum-form equivalent is **β_eff = 2.5 (KL up-weighted)**,
    > NOT 0.4 (the inverted figure propagated from r1 M-4 through 14-13).
    > Semantic interpretation: the latent space is more strongly regularized
    > toward the unit Gaussian prior than a canonical β=1 ELBO would impose;
    > the implementation is closer to a β-VAE on the upper-β side than to a
    > vanilla VAE.

    Step B — Append VAE-not-param-matched caveat to `methods_full.md §2.i`:
    Locate §2.i (VAE architecture) at lines 190–200. After the existing
    architecture text, append one sentence:

    > **Implementation note (Plan 14-14, methods-reproducibility-review-r2
    > caveat).** The VAE is **not** parameter-matched to the WGAN-GP variants
    > (~75k–135k generator params) or the IQP:SEL headline (55 params); at
    > 74 trainable params, the VAE is closer to the IQP:SEL scale and is
    > included as a non-adversarial low-data baseline, not as a head-to-head
    > adversarial comparator.

    Step C — Rewrite `reconciliation_note.md` interpretation paragraph:
    Locate the interpretation paragraph below the EMD OD-scale table (this
    is the paragraph rewritten in 14-13 T3 from the `+0.127 degradation`
    framing to the `recovers within seed variance` framing). Currently
    reads (or similar):

    > Deltas are ≈ 0 across the board: the matched 2000-epoch budget recovers
    > OD-scale EMD within seed variance of the 1000-epoch baseline.

    Replace with:

    > All deltas are within seed variance of the 1000-epoch baseline (Welch
    > t-test p ≥ 0.37 for every model). The largest absolute delta is
    > wgan_cnn (-0.059, a ~50% reduction off a small base); seed-42 outliers
    > drive that variance, not a uniform near-zero effect across seeds.

    The table rows (per-model OLD / NEW / DELTA EMD values) + the 14-12
    integration caveat paragraph + the 14-13 metric-redefinition disclosure
    section are all preserved VERBATIM. ONLY the interpretation paragraph
    rewords.

    Step D — Re-run the v2.1 gate against the modified docs:
    Run the v2.1 gate against `methods_full.md` and `reconciliation_note.md`;
    confirm PASS. The new literal numbers introduced in §3.x.d (`10`, `4`,
    `2.5`, `1`, `0.4`, `74`, `0.37`, `-0.059`, `42`) should either resolve
    through the existing `results/*.json` corpus (where applicable,
    e.g. `74` resolves through `model_info.json#models[].n_params` for the
    VAE entry; `-0.059` resolves through `matched2000_dualscale.json#aggregates`
    for wgan_cnn) or be phrased as prose context. Surface any new failures
    immediately and fix the wording (NOT the gate).

    Commit message: `feat(14-14): VAE β_eff=2.5 derivation correction + VAE-not-param-matched caveat + wgan_cnn seed-variance honesty`.
  </action>
  <verify>
    <automated>./qgan_env/bin/python -c "import pathlib,sys; mf=pathlib.Path('docs/methods_full.md').read_text(); rn=pathlib.Path('docs/reconciliation_note.md').read_text(); errs=[]; ('β_eff = 2.5' in mf or 'β_eff=2.5' in mf or 'beta_eff = 2.5' in mf) or errs.append('methods_full.md §3.x.d missing β_eff = 2.5 correction'); ('β ≈ 0.4' in mf or 'β_eff ≈ 0.4' in mf or 'β_eff = 0.4' in mf) and errs.append('methods_full.md still carries inverted β_eff ≈ 0.4 (must be removed)'); ('sum_MSE/10' in mf or 'sum_MSE / 10' in mf) or errs.append('methods_full.md §3.x.d missing sum_MSE/10 derivation'); ('sum_KLD/4' in mf or 'sum_KLD / 4' in mf) or errs.append('methods_full.md §3.x.d missing sum_KLD/4 derivation'); ('run_baselines.py:315' in mf or 'run_baselines.py:315' in mf) or errs.append('methods_full.md §3.x.d missing run_baselines.py:315-319 citation'); ('not parameter-matched' in mf or 'not param-matched' in mf or 'not\\xa0parameter-matched' in mf or 'NOT parameter-matched' in mf or '**not**' in mf.lower() or 'not\\u00a0' in mf) or ('parameter-matched' in mf and '74' in mf) or errs.append('methods_full.md §2.i missing VAE-not-param-matched caveat (74 trainable params)'); ('Welch t-test p' in rn or 'Welch' in rn) or errs.append('reconciliation_note missing Welch t-test wording'); ('wgan_cnn' in rn and '-0.059' in rn) or errs.append('reconciliation_note missing wgan_cnn -0.059 explicit reference'); ('seed-42 outliers' in rn or 'seed-42' in rn) or errs.append('reconciliation_note missing seed-42 outliers framing'); print('errors',errs); sys.exit(0 if not errs else 1)" &amp;&amp; ./qgan_env/bin/python verify_number_provenance.py --target docs/methods_full.md &amp;&amp; ./qgan_env/bin/python verify_number_provenance.py --target docs/reconciliation_note.md &amp;&amp; [ -z "$(git diff --stat core/)" ]</automated>
  </verify>
  <acceptance_criteria>
    - `methods_full.md §3.x.d` (lines 316–327) carries the corrected β derivation: `β_eff = 2.5 (KL up-weighted)`, NOT `β_eff ≈ 0.4`; the derivation shows `sum_MSE/10`, `sum_KLD/4`, the (10/4) ratio, and the (1/10)·(sum_MSE + 2.5·sum_KLD) factoring
    - §3.x.d cites `run_baselines.py:315-319` for the per-element-mean MSE and per-element-mean KLD code
    - §3.x.d carries a semantic interpretation sentence on what β_eff = 2.5 means (latent space more strongly regularized toward unit Gaussian prior)
    - `methods_full.md §2.i` (lines 190–200) carries the VAE-not-param-matched caveat explicitly mentioning `74 trainable params`, the WGAN-GP `~75k–135k generator params` reference, the IQP:SEL `55 params` reference, and the `non-adversarial low-data baseline, not head-to-head adversarial comparator` framing
    - `reconciliation_note.md` interpretation paragraph carries the Welch t-test + wgan_cnn -0.059 + seed-42-outliers framing; the previous `recovers OD-scale EMD within seed variance of the 1000-epoch baseline` blanket framing is replaced
    - All table rows + 14-12 integration caveat paragraph + 14-13 metric-redefinition disclosure are preserved verbatim in reconciliation_note.md
    - v2.1 gate PASSES on `methods_full.md` and `reconciliation_note.md`
    - `git diff --stat core/` is empty (D-14-22)
  </acceptance_criteria>
  <done>methods_full.md §3.x.d β derivation corrected to β_eff = 2.5 (KL up-weighted) with full math derivation citing run_baselines.py:315-319; §2.i carries VAE-not-param-matched caveat; reconciliation_note.md interpretation paragraph rewritten with Welch t-test + wgan_cnn -0.059 + seed-42-outliers framing; all other content preserved verbatim; v2.1 gate passes; D-14-22 preserved.</done>
</task>

<task type="auto">
  <name>Task 4: Documentation cleanup batch — R1-m4 DOI-pending honesty + docstring 1-80→1-69 + CR-3 line 346→347 + apparatus units split + statsmodels pin + REPRODUCE.md</name>
  <files>docs/reviewer_response.md, run_methods_full.py, run_framework_versions.py, results/manuscript_apparatus_constants.json, requirements-pinned.txt, results/framework_versions.json, REPRODUCE.md, docs/methods_full.md</files>
  <read_first>
    - docs/reviewer_response.md R1-m4 row (currently implies the Zenodo DOI is delivered while 14-07 is still open; the rewording must explicitly say `pending under Plan 14-07`)
    - run_methods_full.py:115-180 (the docstring-slicer code that emits the `1-80` line-range labels at lines 152, 265, 524 — find and fix all three; cross-check by reading the actual `run_matched2000.py` docstring length to confirm it ends at line 69)
    - run_methods_full.py (the `_citations["generator_to_compute_dtype"]` pattern; currently `"generated_samples = generator"` which resolves to `training.py:346` (the generator call); change to `"generated_samples = generated_samples.to(compute_dtype)"` which resolves to `training.py:347` (the actual dtype cast site that CR-3 was about))
    - core/training.py:340-360 (read-only verification — confirm line 346 is the generator call `generated_samples = generator(...)` and line 347 is the cast `generated_samples = generated_samples.to(compute_dtype)`; if line numbers differ, adjust the pattern to match the actual cast site)
    - results/manuscript_apparatus_constants.json (current structure: `apparatus_dimensions_mm: {depth_or_height_880, ancillary_120, ancillary_6, ancillary_10}`; restructure to per-unit subfields with LaTeX-correct semantics — `tube_outer_diameter_cm: 6`, `tube_length_cm: 120`, `data_logging_interval_min: 10`, `ir_led_wavelength_nm: 880` — confirmed against `main (4) copy.tex:176-180`)
    - main (4) copy.tex:176-180 (read-only — the LUCY apparatus + IR LED context: "six vertical borosilicate glass tubes (6 cm OD, 120 cm length) ... data logged at 10-minute intervals ... OD sensor operates at 880 nm"; the 4 numeric constants map 1-to-1 to the 4 per-unit subfields above)
    - requirements-pinned.txt (the existing pinned file from 14-13 T1; check whether `statsmodels` is currently missing; the live version is `0.14.5`)
    - results/framework_versions.json (the audited package set from 14-11; check whether `statsmodels` is currently probed; if not, extend `run_framework_versions.py` to probe it)
    - run_framework_versions.py (the emitter; extend to probe statsmodels if not already)
    - docs/methods_full.md:431, 455, 536 (the doc mentions of `1-80` that also flip to `1-69` to match the run_methods_full.py docstring-slicer fix)
    - .planning/phases/14-paper-revision-release-freeze/peer-review-r2/methods-reproducibility-review-r2.md (the R1-m4 DOI-pending finding + apparatus units split finding + statsmodels pin finding + REPRODUCE.md finding)
    - .planning/phases/14-paper-revision-release-freeze/peer-review-r2/code-review-r2.md (the docstring 1-80 vs 1-69 off-by-11 finding + the CR-3 line 346 vs 347 finding)
    - .planning/phases/14-paper-revision-release-freeze/14-CONTEXT.md — confirm D-14-22 (core/ byte-freeze) PRESERVED (no `core/` edits) and D-14-18 (Overleaf-canonical `main (4) copy.tex` read-only) PRESERVED (T4 reads but does not write)
  </read_first>
  <action>
    Six doc/JSON cleanup edits + 1 new file at repo root. NONE touch
    `core/`. The `main (4) copy.tex` is READ-only (D-14-18).

    Step A — `docs/reviewer_response.md` R1-m4 DOI-pending wording:
    Locate the R1-m4 row (the row labelled `R1-m4` or whatever the
    reviewer-response table uses; typically near a Zenodo / DOI / Data
    Availability mention). The current cell implies the DOI is delivered.
    Replace with:

    > Tagged release + Zenodo DOI deposit **pending under Plan 14-07** (only
    > outstanding Phase 14 plan); release.md will carry the resolved DOI and
    > a frozen-tag commit SHA upon deposit. The provenance gate v2.1 +
    > tracked checkpoint + pinned requirements provide the reproducibility
    > surface today; Zenodo adds the citable DOI on top.

    All other rows + the Cross-Cutting Provenance Note + the appended
    Completeness sweep section from 14-12 + the 14-13 CR-4 disclosure
    subsection are PRESERVED VERBATIM.

    Step B — `run_methods_full.py:152, 265, 524` docstring-slicer 1-80 → 1-69:
    Locate the three sites carrying the `1-80` line-range label. Verify the
    actual `run_matched2000.py` docstring length ends at line 69
    by reading it first. Change `1-80` → `1-69` at all three sites (or to the
    correct value if the verification shows a different actual endpoint —
    do NOT trust the `1-80` literal; trust the actual docstring length).

    Also update the corresponding `docs/methods_full.md` mentions
    at `:455` and `:536` (the doc emits the line-range as a citation; flip
    both from `1-80` to `1-69`).

    Step C — `run_methods_full.py` CR-3 citation pattern flip (line 346 → 347):
    Locate the `_citations["generator_to_compute_dtype"]` pattern declaration
    in `run_methods_full.py` (introduced in 14-13 T4). The current
    pattern is:

    ```python
    _citations["generator_to_compute_dtype"] = _first_lineno(
        "core/training.py",
        r"generator\.to\(.*compute_dtype.*\)"
    )
    ```

    OR (depending on the actual pattern used in 14-13):

    ```python
    _citations["generator_to_compute_dtype"] = _first_lineno(
        "core/training.py",
        r"generated_samples = generator"
    )
    ```

    The r2 reviewer's finding is that the pattern currently matches
    `training.py:346` (the line `generated_samples = generator(...)`, the
    generator CALL site) but the original CR-3 finding's intent was
    `training.py:347` (the line `generated_samples = generated_samples.to(compute_dtype)`,
    the actual dtype CAST site). Change the pattern to:

    ```python
    _citations["generator_to_compute_dtype"] = _first_lineno(
        "core/training.py",
        r"generated_samples = generated_samples\.to\(compute_dtype\)"
    )
    ```

    Verify by reading `core/training.py:340-360` and confirming
    line 347 is the dtype cast site. If the actual cast site is on a
    different line, document the actual line number in the commit body and
    note that the citation now resolves to the correct semantic site
    regardless of the literal line number.

    Re-emit `results/methods_full.json` and `docs/methods_full.md`
    by running `./qgan_env/bin/python run_methods_full.py`. The
    citation in the emitted doc now points to the cast line, not the
    generator call.

    Step D — `results/manuscript_apparatus_constants.json` restructure:
    Open the file and the current structure:

    ```json
    {
        ...,
        "apparatus_dimensions_mm": {
            "depth_or_height_880": 880,
            "ancillary_120": 120,
            "ancillary_6": 6,
            "ancillary_10": 10
        },
        ...
    }
    ```

    Read `main (4) copy.tex:176-180` (the LUCY apparatus + IR LED context)
    to confirm the field semantics. The 880 is the NIR wavelength in nm
    (the IR LED in the LUCY apparatus); 120 / 6 / 10 are likely the
    physical apparatus dimensions in mm. Verify by reading the LaTeX and
    matching the field semantics EXACTLY (do NOT guess — read the LaTeX).

    Replace with the per-unit-split structure (exact field assignment per
    LaTeX context):

    ```json
    {
        ...,
        "apparatus_dimensions": {
            "tube_outer_diameter_cm": 6,
            "tube_length_cm": 120,
            "data_logging_interval_min": 10,
            "ir_led_wavelength_nm": 880
        },
        ...
    }
    ```

    Update any v2.1 gate corpus references that consumed
    `apparatus_dimensions_mm` to the new key path (`apparatus_dimensions`).
    Re-run the v2.1 gate against `docs/paper_blocks_framing.md`
    (the doc most likely to reference these apparatus numbers); confirm PASS.

    Step E — `requirements-pinned.txt` + `framework_versions.json` statsmodels pin:
    Open `requirements-pinned.txt` (from 14-13 T1). Append
    `statsmodels==0.14.5` (the live version) in alphabetical order with
    other `==` pins. Run `./qgan_env/bin/python -c "import statsmodels; print(statsmodels.__version__)"`
    first to CONFIRM the live version is `0.14.5`; if different, pin to the
    actual live version.

    Cross-check `run_framework_versions.py` — if `statsmodels` is
    NOT currently probed by the emitter, extend the probe to include it
    (one new line in the `framework_versions` dict the script emits). Run
    `run_framework_versions.py` to re-emit
    `results/framework_versions.json` carrying the statsmodels
    version.

    Step F — Create top-level `REPRODUCE.md`:
    Create `REPRODUCE.md` at the repo root (~50–80 lines) with the structure:

    ```markdown
    # REPRODUCE.md — Manuscript revision reproducibility entry-point

    > One-stop reviewer entry-point for reproducing the figures, numbers, and
    > methods doc emitted by Phase 14 (manuscript revision). For the full
    > per-script breakdown see `docs/methods_full.md §5.2`; for
    > cross-plan navigation see `docs/completeness_sweep_manifest.md`.

    ## 1 — Setup

    ```bash
    git clone <repo URL>
    cd qGAN
    python -m venv qgan_env
    source qgan_env/bin/activate
    pip install -r requirements-pinned.txt
    ```

    ## 2 — Checkout the manuscript-revision tag

    The repository is frozen at the tag `v2.0-revision` (which carries the
    Zenodo DOI cited in the manuscript). Until Plan 14-07 lands the tag,
    use the current `main` branch — every commit between 14-13's
    `docs(phase-14): update tracking after wave 11` and the eventual
    `v2.0-revision` tag is part of the revision package.

    ```bash
    git checkout v2.0-revision  # or `main` until 14-07 lands
    ```

    ## 3 — Regenerate the canonical artifacts in dependency order

    ```bash
    # Headline metric (single frozen-checkpoint evaluation)
    ./qgan_env/bin/python run_canonical_headline.py

    # Matched-budget sweep aggregates
    ./qgan_env/bin/python run_matched2000_dualscale.py

    # Methods doc + companion JSON
    ./qgan_env/bin/python run_methods_full.py

    # Figure suite
    ./qgan_env/bin/python run_figure_suite.py

    # Provenance gate
    ./qgan_env/bin/python verify_number_provenance.py --target docs/methods_full.md
    ```

    ## 4 — Further reading

    - `docs/methods_full.md §5.2` — full per-script breakdown
    - `docs/completeness_sweep_manifest.md` — cross-plan artifact navigation
    - `docs/peer_review_remediation.md` — finding-to-commit index for the
      r1 + r2 peer-review passes
    - `docs/release.md` — frozen-tag commit SHA + Zenodo DOI (post-14-07)
    ```

    Adjust the exact command list and prose to match the actual emitter
    order from `docs/methods_full.md §5.2` (the per-script
    dependency breakdown). The file is targeted at 50–80 lines; do not pad
    with verbose narrative.

    Step G — Re-emit + gate verify:
    Re-run `run_methods_full.py` (re-emits methods_full.json +
    methods_full.md docstring-slicer mentions). Re-run
    `run_framework_versions.py`. Run the v2.1 gate against
    `methods_full.md`, `reviewer_response.md`, `paper_blocks_framing.md`
    (the apparatus-units re-verification target); confirm all PASS.

    Commit message: `docs(14-14): R1-m4 DOI-pending honesty + docstring 1-80→1-69 + CR-3 line 346→347 + apparatus units split + statsmodels pin + REPRODUCE.md`.
  </action>
  <verify>
    <automated>./qgan_env/bin/python run_methods_full.py 2>&amp;1 | tail -5 &amp;&amp; ./qgan_env/bin/python run_framework_versions.py 2>&amp;1 | tail -5 &amp;&amp; ./qgan_env/bin/python -c "import json,pathlib,re,sys; rr=pathlib.Path('docs/reviewer_response.md').read_text(); rmf=pathlib.Path('run_methods_full.py').read_text(); mf=pathlib.Path('docs/methods_full.md').read_text(); ac=json.loads(pathlib.Path('results/manuscript_apparatus_constants.json').read_text()); rp=pathlib.Path('requirements-pinned.txt').read_text(); fv=json.loads(pathlib.Path('results/framework_versions.json').read_text()); rep=pathlib.Path('REPRODUCE.md'); errs=[]; ('pending under Plan 14-07' in rr or 'pending under plan 14-07' in rr.lower()) or errs.append('reviewer_response.md R1-m4 not flipped to DOI-pending wording'); (rmf.count('1-80') == 0) or errs.append(f'run_methods_full.py still contains {rmf.count(\"1-80\")}× `1-80` literal'); (rmf.count('1-69') == 3) or errs.append(f'run_methods_full.py has {rmf.count(\"1-69\")}× `1-69` — expected exactly 3 (lines 152, 265, 524)'); ('1-80' in mf) and errs.append('methods_full.md still contains 1-80 mention (should be 1-69)'); (lambda m: m and 'generated_samples.to(compute_dtype)' in m.group(1))(re.search(r'\"generator_to_compute_dtype\":\s*\"([^\"]+)\"', rmf)) or errs.append('run_methods_full.py _citations[\"generator_to_compute_dtype\"] pattern not re-pointed to dtype cast site `generated_samples = generated_samples.to(compute_dtype)` (training.py:347)'); ('apparatus_dimensions' in ac and 'ir_led_wavelength_nm' in ac.get('apparatus_dimensions', {})) or errs.append('manuscript_apparatus_constants.json not restructured to per-unit subfields (ir_led_wavelength_nm missing)'); ('apparatus_dimensions_mm' in ac) and ('depth_or_height_880' in str(ac)) and errs.append('manuscript_apparatus_constants.json still has old apparatus_dimensions_mm with depth_or_height_880 lump'); ('statsmodels==0.14.5' in rp or 'statsmodels==' in rp) or errs.append('requirements-pinned.txt missing statsmodels pin'); ('statsmodels' in json.dumps(fv)) or errs.append('framework_versions.json missing statsmodels'); rep.exists() or errs.append('REPRODUCE.md missing at repo root'); rep.exists() and ('pip install -r requirements-pinned.txt' in rep.read_text()) or errs.append('REPRODUCE.md missing pip install -r requirements-pinned.txt line'); rep.exists() and ('methods_full.md §5.2' in rep.read_text() or 'methods_full.md' in rep.read_text()) or errs.append('REPRODUCE.md missing methods_full.md §5.2 reference'); rep.exists() and ('completeness_sweep_manifest.md' in rep.read_text()) or errs.append('REPRODUCE.md missing completeness_sweep_manifest.md reference'); print('errors',errs); sys.exit(0 if not errs else 1)" &amp;&amp; ./qgan_env/bin/python verify_number_provenance.py --target docs/methods_full.md &amp;&amp; ./qgan_env/bin/python verify_number_provenance.py --target docs/reviewer_response.md &amp;&amp; ./qgan_env/bin/python verify_number_provenance.py --target docs/paper_blocks_framing.md &amp;&amp; [ -z "$(git diff --stat core/)" ]</automated>
  </verify>
  <acceptance_criteria>
    - `reviewer_response.md` R1-m4 row carries `pending under Plan 14-07` explicit wording; the DOI-delivered implication is removed; all other rows + Cross-Cutting + Completeness sweep + 14-13 CR-4 disclosure preserved verbatim
    - `run_methods_full.py:152, 265, 524` all carry `1-69` (not `1-80`); zero `1-80` literals remain in the file
    - `methods_full.md:431, 455, 536` mentions of `1-80` are flipped to `1-69`
    - `run_methods_full.py` `_citations["generator_to_compute_dtype"]` pattern matches the dtype cast site `generated_samples = generated_samples.to(compute_dtype)` at `training.py:347` (not the generator call at `training.py:346`)
    - `results/methods_full.json` and `docs/methods_full.md` re-emitted; the file:line citation for `generator_to_compute_dtype` now resolves to line 347 (or the actual current cast site if line numbers differ)
    - `manuscript_apparatus_constants.json` restructured: `apparatus_dimensions` is a per-unit-split dict with `tube_outer_diameter_cm: 6`, `tube_length_cm: 120`, `data_logging_interval_min: 10`, `ir_led_wavelength_nm: 880` (per LaTeX `main (4) copy.tex:176-180`); the old `apparatus_dimensions_mm: {depth_or_height_880, ancillary_120, ancillary_6, ancillary_10}` lump is removed
    - `requirements-pinned.txt` carries `statsmodels==0.14.5` (or the actual live version)
    - `results/framework_versions.json` records `statsmodels` at the same version
    - `REPRODUCE.md` exists at repo root with: pip install line referencing `requirements-pinned.txt`, `methods_full.md §5.2` reference, `completeness_sweep_manifest.md` reference, regeneration commands in dependency order
    - v2.1 gate PASSES on `methods_full.md`, `reviewer_response.md`, `paper_blocks_framing.md`
    - `git diff --stat core/` is empty (D-14-22)
  </acceptance_criteria>
  <done>R1-m4 row flipped to DOI-pending wording; run_methods_full.py docstring-slicer flipped 1-80→1-69 at 3 sites; CR-3 citation pattern re-pointed to the dtype cast site (training.py:347); manuscript_apparatus_constants.json restructured to per-unit-split subfields with ir_led_wavelength_nm explicitly separated from mm dimensions; statsmodels==0.14.5 pinned in requirements + framework_versions.json; REPRODUCE.md created at repo root with full reviewer entry-point; methods_full.md re-emitted; v2.1 gate passes; D-14-22 + D-14-18 preserved.</done>
</task>

<task type="auto">
  <name>Task 5: End-to-end re-verification (v2.1 gate on all 10 paper-facing docs + differential test) + peer_review_remediation.md addenda + completeness_sweep_manifest update + SUMMARY</name>
  <files>docs/peer_review_remediation.md, docs/completeness_sweep_manifest.md, verify_number_provenance.py, .planning/phases/14-paper-revision-release-freeze/14-14-SUMMARY.md</files>
  <read_first>
    - docs/peer_review_remediation.md (the existing reviewer-facing finding-to-commit index from 14-13 T7; preserve verbatim and append two NEW sections at the bottom: `## Gate v2.1 known limitations` and `## R2 follow-up sweep`)
    - docs/completeness_sweep_manifest.md (the existing manifest from 14-12 + 14-13; preserve verbatim and append a new `## Plan 14-14 — r2 peer-review punch list (Wave 12)` section at the bottom)
    - verify_number_provenance.py (the v2.1 gate from Task 1; this task adds a differential-test assertion to the `__main__` self-test confirming positive `0.0001` does NOT resolve against JSON `-0.0001`)
    - All 10 paper-facing docs (the existing 9 from 14-13 T7 + `peer_review_remediation.md`): paper_blocks_framing.md, paper_blocks_refs_methods.md, reviewer_response.md, reconciliation_note.md, methods_full.md, circuit_atlas.md, completeness_sweep_manifest.md, training_protocol.md, dataset_stats.md, peer_review_remediation.md
    - `REPRODUCE.md` (from Task 4; check whether any paper-facing claims appear in it — if so, include it as an 11th gate target; if it's pure scaffolding/commands, exclude)
    - .planning/phases/14-paper-revision-release-freeze/peer-review-r2/code-review-r2.md, math-review-r2.md, quantum-review-r2.md, provenance-review-r2.md, methods-reproducibility-review-r2.md (source of the 15 r2 findings; the `## R2 follow-up sweep` table cites each finding's reviewer file:line and maps to its 14-14 commit SHA)
    - git log (extract commit SHAs for Tasks 1-4 to populate the `## R2 follow-up sweep` table)
  </read_first>
  <action>
    End-to-end re-verification + two new appended sections in
    `peer_review_remediation.md` + one new section in
    `completeness_sweep_manifest.md` + the gate's `__main__` differential
    test + SUMMARY. NO new emitters; NO new figures.

    Step A — Re-run the v2.1 gate against all 10 paper-facing docs:

    ```bash
    for doc in \
        docs/paper_blocks_framing.md \
        docs/paper_blocks_refs_methods.md \
        docs/reviewer_response.md \
        docs/reconciliation_note.md \
        docs/methods_full.md \
        docs/circuit_atlas.md \
        docs/completeness_sweep_manifest.md \
        docs/training_protocol.md \
        docs/dataset_stats.md \
        docs/peer_review_remediation.md; do
      ./qgan_env/bin/python verify_number_provenance.py --target "$doc"
    done
    ```

    Every doc MUST exit 0 under the v2.1 schema. If `REPRODUCE.md` (from Task 4)
    contains any paper-facing claims, include it as the 11th target. The
    expected outcome is all PASS; if a NEW failure surfaces (the v2.1
    lookbehind catches a previously-undetected sign-flip), the failure is a
    substantive data problem and gets fixed in this same plan via a follow-up
    commit. The gate is NEVER weakened.

    Also run with `--manifest` flag for at least the 4 highest-risk docs
    (paper_blocks_framing.md, paper_blocks_refs_methods.md,
    reconciliation_note.md, methods_full.md) and capture the resolution
    trace in the SUMMARY for reviewer transparency.

    Step B — Add the differential-test assertion to the v2.1 gate's `__main__`:
    Locate the `__main__` block (or equivalent self-test entry point) in
    `verify_number_provenance.py`. Add a pytest-style assertion
    block (or a small standalone if-block executed when called with a
    `--differential-test` CLI flag — Claude's discretion on the integration
    pattern; the key requirement is the assertion runs as part of the gate's
    sanity check):

    ```python
    # Differential test (Phase 14 plan 14-14 — v2.1 lookbehind regression):
    # positive token 0.0001 in a doc must NOT resolve against a JSON value -0.0001.
    if __name__ == "__main__":
        import sys, re
        if "--differential-test" in sys.argv:
            token = "0.0001"
            neg_blob = "delta -0.0001 reference"
            pos_blob = "value 0.0001 reference"
            neg_match = re.search(rf"(?<![-\d.]){re.escape(token)}(?![\d])", neg_blob)
            pos_match = re.search(rf"(?<![-\d.]){re.escape(token)}(?![\d])", pos_blob)
            assert not neg_match, (
                "v2.1 differential test FAILED: positive token 0.0001 resolved "
                "against negative JSON value -0.0001 (the v2 sign-flip false positive)"
            )
            assert pos_match, (
                "v2.1 differential test FAILED: positive token 0.0001 did NOT resolve "
                "against positive JSON value 0.0001 (regression in the v2.1 lookbehind)"
            )
            print("v2.1 differential test PASSED")
            sys.exit(0)
    ```

    Run `./qgan_env/bin/python verify_number_provenance.py --differential-test`
    to confirm it exits 0 with `v2.1 differential test PASSED`.

    Step C — Append `## Gate v2.1 known limitations` to `peer_review_remediation.md`:
    AFTER the existing 14-13 finding-to-commit tables and OOS section
    (preserved verbatim), APPEND a new section:

    ```markdown
    ## Gate v2.1 known limitations (Plan 14-14)

    The v2.1 gate's ε-neighborhood float resolution `abs(cval - val) <= 10**(-prec) / 2`
    (from 14-13 T2 v2) still admits semantically-unrelated coincidences at
    low precision. Concrete example surfaced in r2 peer review:

    - Token `-0.26` (AR-bias factor from `methods_full.md §3.x.c`) resolves
      against a kurtosis value `-0.2570036` from `model_info.json` at low
      precision (prec=1 → both round to `-0.3`, within the ε-neighborhood).

    The resolution is semantically wrong (an AR-bias-factor literal should
    NOT resolve via a kurtosis value), even though both numbers exist in
    the audited JSON corpus.

    **Disposition (locked decision under Plan 14-14):** the gate is NOT
    tightened further. The mitigation is the `--manifest` CLI flag (added
    in 14-13 T2): reviewers and developers can spot semantic mismatches by
    inspecting the JSON-path-per-literal resolution trace it emits. The
    locked decision balances simplicity-of-gate-contract against
    completeness-of-semantic-binding; the `--manifest` flag is sufficient
    for the reviewer-facing audit standard targeted by this manuscript.

    See `.planning/phases/14-paper-revision-release-freeze/peer-review-r2/code-review-r2.md`
    (R2-code-HIGH-2) for the full reviewer finding.
    ```

    Step D — Append `## R2 follow-up sweep` to `peer_review_remediation.md`:
    AFTER the v2.1-limitations section, APPEND a new section mapping each
    r2 finding to its 14-14 commit SHA. Structure mirrors the existing 14-13
    finding-to-commit tables: one table per r2 review report.

    ```markdown
    ## R2 follow-up sweep (Plan 14-14)

    Closes 15 of 15 actionable findings from the 5-agent r2 peer-review pass
    on Phase 14 plan 14-13's remediation work (3 triangulated HIGHs + 12
    lower-severity items; every r2 reviewer's final recommendation was
    `ready for paper resubmission YES` — no blockers, but the items below
    are closed in one atomic sweep before 14-07 Zenodo deposit).

    ### code-review-r2.md
    | Finding | Severity | Reviewer's citation | Commit | Notes |
    |---|---|---|---|---|
    | R2-code-HIGH-1 | HIGH | run_matched2000.py:556 + 333–335 (training_time_device structurally broken) | <sha_T2> | Capture-before-.to(cpu) in _train_quantum/_train_wgan/_train_vae; _device_manifest accepts optional pre-captured device |
    | R2-code-HIGH-2 | HIGH | verify_number_provenance.py ε-neighborhood low-prec broad coincidence (-0.26 → -0.2570036) | DISCLOSED | Locked decision: --manifest mitigation, gate NOT tightened (see `## Gate v2.1 known limitations` above) |
    | R2-code-MED-1 | MEDIUM | run_methods_full.py:152,265,524 (1-80 vs actual 1-69 docstring length) | <sha_T4> | Docstring-slicer line range flipped at 4 sites + methods_full.md:455,536 mentions |
    | R2-code-MED-2 | MEDIUM | run_methods_full.py CR-3 citation matches training.py:346 (generator call) but intent was :347 (dtype cast site) | <sha_T4> | Citation pattern re-pointed to `generated_samples = generated_samples.to(compute_dtype)` |
    | R2-code-LOW-1 | LOW | _introspect_*.json (4 files) lack `"render_only": true` → semantic mis-provenance in --manifest output | <sha_T1> | All 4 files updated |

    ### math-review-r2.md
    | Finding | Severity | Reviewer's citation | Commit | Notes |
    |---|---|---|---|---|
    | R2-math-HIGH-1 | HIGH | methods_full.md §3.x.d (lines 316–327) — β_eff ≈ 0.4 inverted; correct value is β_eff = 2.5 (KL up-weighted) | <sha_T3> | §3.x.d rewritten with full derivation citing run_baselines.py:315-319 |

    ### quantum-review-r2.md
    | Finding | Severity | Reviewer's citation | Commit | Notes |
    |---|---|---|---|---|
    | (no HIGH or MED findings in scope) | — | — | — | quantum-review-r2.md surfaced MINOR items below the 14-14 threshold; the 14-13 quantum-circuit reviews remain valid |

    ### provenance-review-r2.md
    | Finding | Severity | Reviewer's citation | Commit | Notes |
    |---|---|---|---|---|
    | R2-prov-HIGH-1 | HIGH | verify_number_provenance.py:193 boundary lookbehind missing `-` → positive→negative sign-flip false positive | <sha_T1> | Gate v2.1 one-character fix: `(?<![\d.])` → `(?<![-\d.])`; schema bumped to v2.1; regression block + differential test (in <sha_T5>) added |
    | R2-prov-MED-1 | MEDIUM | noise_model_sensitivity.json + shot_noise_sensitivity.json + ansatz_comparison.json missing `data_hash` (REPRO-LOW-1 enumerates all three) | <sha_T1> | All three files updated with `data_hash: 91e447d4624e25b3` |

    ### methods-reproducibility-review-r2.md
    | Finding | Severity | Reviewer's citation | Commit | Notes |
    |---|---|---|---|---|
    | R2-methods-HIGH-1 | HIGH | reviewer_response.md R1-m4 implies Zenodo DOI delivered while 14-07 is open | <sha_T4> | Row reworded to `pending under Plan 14-07` |
    | R2-methods-MED-1 | MEDIUM | methods_full.md §2.i (VAE arch) lacks not-param-matched caveat | <sha_T3> | Caveat sentence appended citing 74 vs 75k–135k vs 55 param counts |
    | R2-methods-MED-2 | MEDIUM | manuscript_apparatus_constants.json#apparatus_dimensions_mm lumps 880 (nm) with 120/6/10 (mm) | <sha_T4> | Restructured to per-unit subfields with ir_led_wavelength_nm explicit |
    | R2-methods-LOW-1 | LOW | statsmodels==0.14.5 (live) not in requirements-pinned.txt | <sha_T4> | Pin added; framework_versions.json updated |
    | R2-methods-LOW-2 | LOW | no top-level REPRODUCE.md | <sha_T4> | Created at repo root linking to methods_full.md §5.2 + completeness_sweep_manifest.md |

    ### Out of scope (and rationale)

    - **R2-code-HIGH-2** (ε-neighborhood low-prec broad coincidences) —
      DISCLOSED as a known v2.1 gate limitation in the section above; locked
      decision to disclose rather than tighten (the `--manifest` flag is the
      mitigation).
    - **Any quantum-review-r2.md MINOR items** — below the 14-14 severity
      threshold; the 14-13 quantum-circuit reviews remain valid.
    ```

    Step E — Append `## Plan 14-14 — r2 peer-review punch list (Wave 12)` to `completeness_sweep_manifest.md`:
    AFTER the existing 14-13 section (preserved verbatim), APPEND:

    ```markdown
    ## Plan 14-14 — r2 peer-review punch list (Wave 12)

    Closes 15 of 15 actionable findings from the 5-agent r2 peer-review pass
    on Phase 14 plan 14-13's remediation work (3 triangulated HIGHs + 12
    lower-severity items). No retraining; no new figures;
    `core/` byte-frozen (D-14-22); D-14-16 (gate byte-freeze)
    LIFTED for Task 1 only (one-character lookbehind fix); D-14-13
    (strict-accept gate) PRESERVED (capture site corrected; equality check
    unchanged).

    | Artifact | Task | Description | Resolved finding(s) |
    |---|---|---|---|
    | `verify_number_provenance.py` (v2.1) | T1 | Boundary lookbehind class `(?<![\d.])` → `(?<![-\d.])` (one char); schema bumped to v2.1 | R2-prov-HIGH-1 |
    | `results/figures/_introspect_{quantum,wgan_cnn,wgan_lstm,wgan_mlp}.json` | T1 | `"render_only": true` added at top level (4 files) | R2-code-LOW-1 |
    | `results/noise_model_sensitivity.json` + `shot_noise_sensitivity.json` + `ansatz_comparison.json` | T1 | `"data_hash": "91e447d4624e25b3"` added at top level | R2-prov-MED-1 |
    | `run_matched2000.py` (training_time_device capture-before-.to(cpu)) | T2 | `_train_quantum`, `_train_wgan`, `_train_vae` capture `training_time_device` immediately after training, before .to(cpu); `_device_manifest` accepts optional pre-captured device; `_strict_accept` equality check unchanged | R2-code-HIGH-1 |
    | `docs/methods_full.md §3.x.d` | T3 | VAE β derivation rewritten: β_eff = 2.5 (KL up-weighted), NOT 0.4 (inverted figure from r1 M-4 → 14-13) | R2-math-HIGH-1 |
    | `docs/methods_full.md §2.i` | T3 | VAE-not-param-matched caveat appended (74 vs 75k–135k vs 55 param counts) | R2-methods-MED-1 |
    | `docs/reconciliation_note.md` interpretation paragraph | T3 | Reworded to Welch t-test + wgan_cnn -0.059 + seed-42-outliers honesty; table rows + 14-12 caveat + 14-13 disclosure preserved verbatim | (additive honesty correction) |
    | `docs/reviewer_response.md` R1-m4 row | T4 | Reworded to "pending under Plan 14-07" DOI-pending explicit | R2-methods-HIGH-1 |
    | `run_methods_full.py` (docstring slicer + CR-3 citation) | T4 | `1-80` → `1-69` at 4 sites; CR-3 citation pattern re-pointed to `training.py:347` cast site | R2-code-MED-1, R2-code-MED-2 |
    | `results/manuscript_apparatus_constants.json` | T4 | Restructured to per-unit subfields (`tube_outer_diameter_cm: 6`, `tube_length_cm: 120`, `data_logging_interval_min: 10`, `ir_led_wavelength_nm: 880`); old `apparatus_dimensions_mm` lump removed | R2-methods-MED-2 |
    | `requirements-pinned.txt` + `framework_versions.json` | T4 | `statsmodels==0.14.5` pin added; framework_versions.json updated | R2-methods-LOW-1 |
    | `REPRODUCE.md` (NEW, repo root) | T4 | One-stop reviewer entry-point linking to methods_full.md §5.2 + completeness_sweep_manifest.md | R2-methods-LOW-2 |
    | `verify_number_provenance.py` (`--differential-test`) | T5 | `__main__` differential-test assertion: positive `0.0001` does NOT resolve against JSON `-0.0001` | (regression test for R2-prov-HIGH-1) |
    | `docs/peer_review_remediation.md` (`## Gate v2.1 known limitations` + `## R2 follow-up sweep`) | T5 | r2 finding-to-commit index + ε-neighborhood limitation disclosed | R2-code-HIGH-2 (DISCLOSED) |
    | `docs/completeness_sweep_manifest.md` (this section) | T5 | Updated artifact manifest | (manifest) |

    ### End-to-end v2.1 gate re-run command

    ```bash
    for doc in \
        docs/paper_blocks_framing.md \
        docs/paper_blocks_refs_methods.md \
        docs/reviewer_response.md \
        docs/reconciliation_note.md \
        docs/methods_full.md \
        docs/circuit_atlas.md \
        docs/completeness_sweep_manifest.md \
        docs/training_protocol.md \
        docs/dataset_stats.md \
        docs/peer_review_remediation.md; do
      ./qgan_env/bin/python verify_number_provenance.py --target "$doc"
    done
    ./qgan_env/bin/python verify_number_provenance.py --differential-test
    ```

    Every line above must exit 0 under the v2.1 schema.
    ```

    Step F — Write `.planning/phases/14-paper-revision-release-freeze/14-14-SUMMARY.md`:
    Use the standard GSD summary template:
    - Plan ID + dates
    - Self-check: PASS (every must_have truth verified)
    - Findings closed: 15 of 15 (R2-code-HIGH-2 DISCLOSED per locked decision)
    - Contract changes applied: D-14-16 LIFTED (Task 1 only, one-character
      lookbehind) + D-14-13 PRESERVED (capture site corrected, gate semantics
      unchanged) + D-14-22 PRESERVED
    - Artifacts created/modified/regenerated (link to
      `completeness_sweep_manifest.md §Plan 14-14` section)
    - Verification log (v2.1 gate per-doc PASS lines + differential-test
      PASS + the manifest re-run command)
    - Final state: Phase 14 incomplete plans = `[14-07]` only

    Commit message: `docs(14-14): SUMMARY + completeness_sweep_manifest update + peer_review_remediation r2 follow-up + gate v2.1 limitations section`.

    Step G — Final 12-point verification checklist (mirrors 14-13's structure,
    preserved verbatim from the approved plan):

    1. Gate v2.1 PASSES on all 10 paper-facing docs with `--manifest`.
    2. Differential test: positive token `0.0001` in a doc does NOT resolve
       to a JSON `-0.0001`; explicit pytest-style assertion added to gate's
       `__main__` self-test.
    3. `training_time_device` field on every `device_manifest` reflects the
       device the model was trained on at the moment training finished, not
       the post-`.to("cpu")` device.
    4. `methods_full.md §3.x.d` states β_eff = 2.5 (KL up-weighted) with
       the derivation; §2.i carries the VAE-not-param-matched caveat.
    5. `reconciliation_note.md` interpretation paragraph mentions wgan_cnn
       explicitly and the Welch t-test seed-variance framing.
    6. `reviewer_response.md` R1-m4 explicitly says "Zenodo DOI pending
       under Plan 14-07".
    7. `_introspect_*.json` (all 4) carry `"render_only": true`.
    8. `noise_model_sensitivity.json` + `shot_noise_sensitivity.json` + `ansatz_comparison.json` carry
       `data_hash: "91e447d4624e25b3"`.
    9. `requirements-pinned.txt` includes `statsmodels==0.14.5`.
    10. `REPRODUCE.md` exists at repo root and links to `methods_full.md
        §5.2` + `completeness_sweep_manifest.md`.
    11. `peer_review_remediation.md` carries both the v2.1-limitations and
        r2-follow-up sections; every r2 finding maps to a 14-14 commit SHA.
    12. `git diff` against pre-14-14 main shows zero changes to
        `core/` (D-14-22 preserved).
  </action>
  <verify>
    <automated>set -e; for doc in docs/paper_blocks_framing.md docs/paper_blocks_refs_methods.md docs/reviewer_response.md docs/reconciliation_note.md docs/methods_full.md docs/circuit_atlas.md docs/completeness_sweep_manifest.md docs/training_protocol.md docs/dataset_stats.md docs/peer_review_remediation.md; do ./qgan_env/bin/python verify_number_provenance.py --target "$doc"; done &amp;&amp; ./qgan_env/bin/python verify_number_provenance.py --differential-test &amp;&amp; ./qgan_env/bin/python -c "import pathlib,sys; p=pathlib.Path('docs/peer_review_remediation.md').read_text(); c=pathlib.Path('docs/completeness_sweep_manifest.md').read_text(); s=pathlib.Path('.planning/phases/14-paper-revision-release-freeze/14-14-SUMMARY.md').read_text(); errs=[]; ('## Gate v2.1 known limitations' in p) or errs.append('peer_review_remediation missing Gate v2.1 known limitations section'); ('## R2 follow-up sweep' in p) or errs.append('peer_review_remediation missing R2 follow-up sweep section'); ('R2-code-HIGH-2' in p) or errs.append('peer_review_remediation missing R2-code-HIGH-2 disclosure'); [errs.append(f'peer_review_remediation R2 follow-up missing {fid}') for fid in ['R2-code-HIGH-1','R2-code-MED-1','R2-code-MED-2','R2-code-LOW-1','R2-math-HIGH-1','R2-prov-HIGH-1','R2-prov-MED-1','R2-methods-HIGH-1','R2-methods-MED-1','R2-methods-MED-2','R2-methods-LOW-1','R2-methods-LOW-2'] if fid not in p]; ('## Plan 14-14' in c or 'Plan 14-14 — r2 peer-review punch list' in c) or errs.append('completeness_sweep_manifest missing Plan 14-14 section'); ('PASS' in s and 'Self-check' in s.lower() or 'Self-Check' in s) or errs.append('SUMMARY missing self-check PASS'); ('14-07' in s) or errs.append('SUMMARY missing final-state Phase 14 incomplete plans = [14-07] line'); print('errors',errs); sys.exit(0 if not errs else 1)" &amp;&amp; [ -z "$(git diff --stat core/)" ]</automated>
  </verify>
  <acceptance_criteria>
    - v2.1 gate PASSES on all 10 paper-facing docs (`paper_blocks_framing.md`, `paper_blocks_refs_methods.md`, `reviewer_response.md`, `reconciliation_note.md`, `methods_full.md`, `circuit_atlas.md`, `completeness_sweep_manifest.md`, `training_protocol.md`, `dataset_stats.md`, `peer_review_remediation.md`)
    - `verify_number_provenance.py --differential-test` PASSES (positive `0.0001` does NOT match against blob `-0.0001`; positive `0.0001` DOES match against blob `0.0001`)
    - `peer_review_remediation.md` carries new `## Gate v2.1 known limitations` section disclosing R2-code-HIGH-2 ε-neighborhood low-precision coincidences (with the `-0.26` → `-0.2570036` concrete example) and the locked decision (gate NOT tightened, `--manifest` mitigation)
    - `peer_review_remediation.md` carries new `## R2 follow-up sweep` section with 5 tables (one per r2 review report); every r2 finding ID (R2-code-HIGH-1, R2-code-MED-1, R2-code-MED-2, R2-code-LOW-1, R2-math-HIGH-1, R2-prov-HIGH-1, R2-prov-MED-1, R2-methods-HIGH-1, R2-methods-MED-1, R2-methods-MED-2, R2-methods-LOW-1, R2-methods-LOW-2) appears in the doc and maps to a 14-14 commit SHA (or `DISCLOSED` for R2-code-HIGH-2)
    - `completeness_sweep_manifest.md` carries new `## Plan 14-14 — r2 peer-review punch list (Wave 12)` section at the bottom; existing 14-09 / 14-10 / 14-11 / 14-12 / 14-13 sections preserved verbatim
    - `.planning/phases/14-paper-revision-release-freeze/14-14-SUMMARY.md` exists; Self-Check is PASS; lists all 5 task commits + the SUMMARY commit; final-state line declares Phase 14 incomplete plans = `[14-07]` only
    - All 12 verification checks at the end of Step G pass
    - `git diff --stat core/` is empty (D-14-22 byte-freeze preserved across all 5 tasks)
  </acceptance_criteria>
  <done>v2.1 gate PASSES on all 10 paper-facing docs; differential-test PASS confirms the sign-flip regression is closed; peer_review_remediation.md carries the v2.1-limitations + R2-follow-up-sweep sections mapping all 15 r2 findings (or DISCLOSED for R2-code-HIGH-2); completeness_sweep_manifest.md updated with Plan 14-14 section; 14-14-SUMMARY.md written; all 12 verification checks pass; Phase 14 incomplete plans = [14-07] only; D-14-22 preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| paper-facing docs ↔ audited JSON corpus | wrong citation, hand-typed literal, or sign-flipped number resolution in any paper-facing doc would put an unverifiable claim in front of reviewers; the v2.1 gate is the sole defensive boundary |
| positive token ↔ negative JSON value | the v2 lookbehind `(?<![\d.])` does NOT include `-`, so a positive token resolves against a negative JSON value (concrete: positive `0.0001` resolves against JSON `-0.0001`); `reconciliation_deltas.json` is dense with small negative deltas, so the bug had outsized impact |
| `training_time_device` capture site ↔ `_strict_accept` equality check | the manifest currently reads `next(generator.parameters()).device` AFTER `generate_wgan_samples` has moved the model to CPU, so the gate that's supposed to CERTIFY the MPS-disable hook is always reading `cpu` and never actually validates the hook fired; the D-14-13 future-gate is non-functional in current shape |
| `core/` byte-freeze (D-14-22) ↔ math-doc corrections | M-4 (VAE β derivation), VAE-not-param-matched caveat, reconciliation interpretation reword, and R1-m4 DOI-pending wording are ALL doc-only; addressing them via `core/` edits would violate D-14-22 |
| `main (4) copy.tex` Overleaf-canonical read-only (D-14-18) ↔ apparatus-units restructure | T4 READS `:176-180` for field semantics but does not write; the apparatus restructure is JSON-only (`manuscript_apparatus_constants.json`); the LaTeX is the authoritative semantic source |
| `--manifest` mitigation ↔ ε-neighborhood broad coincidence | the v2.1 gate's ε-neighborhood float resolution still admits low-precision broad coincidences (e.g. `-0.26` resolves to `-0.2570036`); the locked decision is to DISCLOSE this in `peer_review_remediation.md` rather than tighten the gate further (which would require a much larger redesign) |
| 5 r2 peer-review reports ↔ 14-14 commit traceability | reviewers must be able to map each r2 finding to the exact commit that resolved it; the `## R2 follow-up sweep` section in `peer_review_remediation.md` is that index and any missing finding row breaks the audit |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-46 | Spoofing | gate v2 lookbehind missing `-` resolves positive token against negative JSON value (positive `0.0001` against `-0.0001`) | mitigate | Task 1 one-character fix at line 193: `(?<![\d.])` → `(?<![-\d.])`; schema bumped to v2.1; Task 5 differential-test assertion in `__main__` verifies the regression is closed |
| T-14-47 | Tampering | `training_time_device` capture site reads `next(parameters()).device` AFTER `.to("cpu")` migration → manifest always sees `cpu` → D-14-13 future-gate non-functional | mitigate | Task 2 captures `training_time_device` IMMEDIATELY after each training loop returns, BEFORE the migration; `_device_manifest` accepts an optional pre-captured device; `_strict_accept` equality check unchanged but now sees the actual training device |
| T-14-48 | Information Disclosure | inverted VAE β derivation (`β_eff ≈ 0.4`) propagated through 14-13 misrepresents the latent-space regularization to readers | mitigate | Task 3 rewrites §3.x.d with the corrected `β_eff = 2.5 (KL up-weighted)` derivation citing `run_baselines.py:315-319` per-element-mean MSE + KLD code; the semantic interpretation sentence explains the upper-β-side regularization |
| T-14-49 | Information Disclosure | VAE not-param-matched relative to WGAN-GP variants is implicit (a reader might compare 74-param VAE against 75k–135k WGAN-GP generators head-to-head) | mitigate | Task 3 appends the explicit caveat sentence to §2.i: VAE is closer to the IQP:SEL 55-param scale and is a non-adversarial low-data baseline, NOT a head-to-head adversarial comparator |
| T-14-50 | Information Disclosure | `reconciliation_note.md` interpretation paragraph's blanket "recovers within seed variance" framing hides the wgan_cnn -0.059 outlier (a ~50% reduction off a small base) | mitigate | Task 3 rewrites with Welch t-test p ≥ 0.37 + wgan_cnn -0.059 explicit + seed-42 outliers driving variance |
| T-14-51 | Information Disclosure | `reviewer_response.md` R1-m4 row implies Zenodo DOI is delivered while 14-07 is still open | mitigate | Task 4 rewords to explicit "pending under Plan 14-07" wording stating release.md will carry the resolved DOI + frozen-tag commit SHA upon deposit |
| T-14-52 | Tampering | `run_methods_full.py:152, 265, 524` carries `1-80` line-range labels but the actual `run_matched2000.py` docstring ends at line 69 (off-by-11) | mitigate | Task 4 flips all 3 sites to `1-69`; `methods_full.md:431, 455, 536` mentions also flip |
| T-14-53 | Tampering | CR-3 citation pattern resolves to `training.py:346` (generator call) but the original CR-3 finding's intent was `:347` (dtype cast site) | mitigate | Task 4 changes the pattern from `"generated_samples = generator"` to `"generated_samples = generated_samples.to(compute_dtype)"`; the citation now resolves to the intended semantic site |
| T-14-54 | Spoofing | `manuscript_apparatus_constants.json#apparatus_dimensions_mm` lumps 880 (NIR wavelength, nm), 10 (logging interval, min), and 6/120 (tube OD/length, cm) under a "mm" label | mitigate | Task 4 restructures to per-unit subfields per the LaTeX `main (4) copy.tex:176-180` semantics: `tube_outer_diameter_cm: 6`, `tube_length_cm: 120`, `data_logging_interval_min: 10`, `ir_led_wavelength_nm: 880` |
| T-14-55 | Tampering | `statsmodels==0.14.5` is live but missing from `requirements-pinned.txt` (gap in the pinned environment) | mitigate | Task 4 adds the pin to `requirements-pinned.txt`; `run_framework_versions.py` extended to probe statsmodels; `framework_versions.json` records the version |
| T-14-56 | Information Disclosure | no top-level `REPRODUCE.md` — reviewers landing on the repo root have no orientation document | mitigate | Task 4 creates `REPRODUCE.md` at repo root (~50–80 lines) linking to `methods_full.md §5.2` + `completeness_sweep_manifest.md` |
| T-14-57 | Tampering | 4 `_introspect_*.json` files lack `"render_only": true` flag → they get pulled into the gate's resolution corpus and cause semantic mis-provenance in `--manifest` output | mitigate | Task 1 adds the flag to all 4 files (consistent with 14-13's render-only exclusion design from T2 v2) |
| T-14-58 | Tampering | `noise_model_sensitivity.json` + `shot_noise_sensitivity.json` + `ansatz_comparison.json` lack `data_hash` (gap in 14-13 T4 HIGH-2 / PROV-HIGH-2 data_hash corpus consistency sweep; REPRO-LOW-1 enumerates all three) | mitigate | Task 1 adds `data_hash: "91e447d4624e25b3"` to all three files; matches the value across `model_info.json`, `methods_full.json`, `circuit_diagrams.json`, `classical_architectures.json`, `framework_versions.json` from 14-13 T4 |
| T-14-59 | Repudiation | R2-code-HIGH-2 (ε-neighborhood low-prec broad coincidence) is a known gate limitation NOT addressed by v2.1; if undisclosed, a reviewer could later challenge a coincidental resolution as a false positive | mitigate | Task 5 adds `## Gate v2.1 known limitations` section to `peer_review_remediation.md` with the concrete `-0.26` → `-0.2570036` example + the locked decision (gate NOT tightened, `--manifest` mitigation) |
| T-14-60 | Tampering | inadvertent `core/` edit slipping into a docs-only or aggregator-only task (would break D-14-22) | mitigate | Every task's verify gate asserts `[ -z "$(git diff --stat core/)" ]`; M-4 (VAE β derivation) is explicitly doc-only (methods_full.md edit, NOT core/) |
| T-14-61 | Information Disclosure | missing finding in `## R2 follow-up sweep` (a remediation-index hole) | mitigate | Task 5 verify gate enumerates every one of the 12 finding IDs (R2-code-HIGH-1, R2-code-MED-1, R2-code-MED-2, R2-code-LOW-1, R2-math-HIGH-1, R2-prov-HIGH-1, R2-prov-MED-1, R2-methods-HIGH-1, R2-methods-MED-1, R2-methods-MED-2, R2-methods-LOW-1, R2-methods-LOW-2) and asserts every ID appears in the doc; R2-code-HIGH-2 is DISCLOSED (not commit-mapped) |
| T-14-62 | Tampering | `methods_full.md` other-section verbatim preservation: Task 3 only edits §3.x.d + §2.i; Task 4 only flips line-range labels at :455 and :536; the rest must NOT drift | mitigate | Per-task acceptance criteria explicitly enumerate the sections each task may edit; remaining sections preserved verbatim across the task touches |
</threat_model>

<verification>
End-to-end checks at the close of T5 (the 12-point verification checklist
from the approved plan, preserved verbatim):

1. **Gate v2.1 PASSES on all 10 paper-facing docs** with `--manifest`.
2. **Differential test**: positive token `0.0001` in a doc does NOT resolve
   to a JSON `-0.0001`; explicit pytest-style assertion added to gate's
   `__main__` self-test.
3. **`training_time_device` field on every `device_manifest`** reflects the
   device the model was trained on at the moment training finished, not the
   post-`.to("cpu")` device.
4. **`methods_full.md §3.x.d`** states β_eff = 2.5 (KL up-weighted) with the
   derivation; §2.i carries the VAE-not-param-matched caveat.
5. **`reconciliation_note.md`** interpretation paragraph mentions wgan_cnn
   explicitly and the Welch t-test seed-variance framing.
6. **`reviewer_response.md`** R1-m4 explicitly says "Zenodo DOI pending under
   Plan 14-07".
7. **`_introspect_*.json`** (all 4) carry `"render_only": true`.
8. **`noise_model_sensitivity.json`** + `shot_noise_sensitivity.json` + `ansatz_comparison.json` carry
   `data_hash: "91e447d4624e25b3"`.
9. **`requirements-pinned.txt`** includes `statsmodels==0.14.5`.
10. **`REPRODUCE.md`** exists at repo root and links to `methods_full.md §5.2`
    + `completeness_sweep_manifest.md`.
11. **`peer_review_remediation.md`** carries both the v2.1-limitations and
    r2-follow-up sections; every r2 finding maps to a 14-14 commit SHA.
12. **`git diff` against pre-14-14 main** shows zero changes to
    `core/` (D-14-22 preserved).

After T5 passes, the paper-revision presentation layer is airtight under r2
scrutiny too; only **14-07 (Zenodo deposit + tag + DOI wiring + release.md)**
remains.
</verification>

<success_criteria>
15 of 15 actionable r2 peer-review findings (3 triangulated HIGHs + 12
lower-severity items; R2-code-HIGH-2 DISCLOSED per locked decision) closed
across 5 atomic task commits + 1 SUMMARY commit. The provenance gate is
hardened to v2.1 (D-14-16 LIFTED for Task 1 only; back in byte-freeze under
the v2.1 schema after Task 1 closes) with a one-character lookbehind fix
adding `-` to the boundary class. The `training_time_device` capture site
is corrected in `run_matched2000.py` so the D-14-13 future-gate
becomes structurally sound (capture before `.to(cpu)` in all three training
paths; `_device_manifest` accepts an optional pre-captured device; the
`_strict_accept` equality check is unchanged but now sees the actual
training device). `docs/methods_full.md §3.x.d` carries the
corrected VAE β derivation (β_eff = 2.5 KL up-weighted, NOT 0.4) with full
per-element-mean math citing `run_baselines.py:315-319`; §2.i
carries the VAE-not-param-matched caveat (74 vs 75k–135k vs 55 param
counts). `docs/reconciliation_note.md` interpretation paragraph
carries the Welch t-test + wgan_cnn -0.059 + seed-42-outliers honesty
framing. `docs/reviewer_response.md` R1-m4 explicitly states
"pending under Plan 14-07". `run_methods_full.py` carries `1-69`
line-range labels at 4 sites (replacing the inaccurate `1-80`) and the CR-3
citation pattern resolves to the dtype cast site at `training.py:347` (not
the generator call at `:346`). `results/manuscript_apparatus_constants.json`
is restructured to per-unit subfields with `ir_led_wavelength_nm: 880`
explicitly separated from `tube_outer_diameter_cm`/`tube_length_cm`/`data_logging_interval_min`.
`requirements-pinned.txt` carries `statsmodels==0.14.5`;
`results/framework_versions.json` records it. `REPRODUCE.md` exists
at the repo root linking to `methods_full.md §5.2` + `completeness_sweep_manifest.md`.
The 4 `_introspect_*.json` files carry `"render_only": true`; the 2
sensitivity JSONs carry `"data_hash": "91e447d4624e25b3"`. The v2.1 gate
runs against all 10 paper-facing docs (including `peer_review_remediation.md`)
and all PASS; the differential-test assertion in the gate's `__main__`
confirms the positive→negative sign-flip false positive is closed.
`docs/peer_review_remediation.md` carries two new appended
sections: `## Gate v2.1 known limitations` (disclosing R2-code-HIGH-2
ε-neighborhood low-precision broad coincidences with the `-0.26` →
`-0.2570036` concrete example + the locked decision to disclose-not-tighten)
and `## R2 follow-up sweep` (mapping all 15 r2 findings to 14-14 commit
SHAs or DISCLOSED). `docs/completeness_sweep_manifest.md` carries
a new `## Plan 14-14 — r2 peer-review punch list (Wave 12)` section at the
bottom. `core/` is byte-frozen throughout (D-14-22). No
retraining; no new figures; no classical sweep re-run. After this plan
completes, only 14-07 (Zenodo deposit + tag + DOI wiring + release.md)
remains in Phase 14.
</success_criteria>

<mechanics>
Mirrors established Phase 14 protocol:

- Plan authored via `gsd-planner` agent; validated by `gsd-plan-checker`
  before execution (the orchestrator handles checker invocation as a
  separate step — this planner does NOT spawn the checker itself).
- Execution via **foreground** `gsd-executor` with `isolation="worktree"`.
  **Background subagents auto-deny Bash for the first git safety call** —
  this is a documented failure mode that bit 8× in earlier Phase 14 work.
  14-14 must be spawned foreground; see
  `~/.claude/projects/-Users-shawngibford-dev-phd-qGAN/memory/project_quantum_sim_execution.md`
  for the canonical reference on the foreground-executor requirement +
  stall-watchdog + subagent-permission settings for compute-heavy GSD phases.
- Per-task atomic commits (one `feat(14-14): <task-tag>` commit each, plus
  the final `docs(14-14): SUMMARY` commit). 5 task commits + 1 SUMMARY
  commit + orchestrator merge:
  - `feat(14-14): gate v2.1 (negative-sign-aware lookbehind) + introspect render_only marks + sensitivity data_hash` (T1)
  - `feat(14-14): training_time_device captured pre-.to(cpu) — D-14-13 future-gate now structurally sound` (T2)
  - `feat(14-14): VAE β_eff=2.5 derivation correction + VAE-not-param-matched caveat + wgan_cnn seed-variance honesty` (T3)
  - `docs(14-14): R1-m4 DOI-pending honesty + docstring 1-80→1-69 + CR-3 line 346→347 + apparatus units split + statsmodels pin + REPRODUCE.md` (T4)
  - `docs(14-14): SUMMARY + completeness_sweep_manifest update + peer_review_remediation r2 follow-up + gate v2.1 limitations section` (T5)
- Pre-existing dirty working-tree files (deleted LICENSE + modified
  `.gitignore`/`fake.csv`/`real.csv`/notebook/`phase4_validation.json`) get
  stash-cycled across the merge. None of T1–T5 touches `.gitignore`, so it
  stays in the stash this round.
- STATE.md / ROADMAP.md owned by orchestrator (post-wave update).
- ROADMAP updates (applied in the same write pass as this plan):
  `**Plans**: 13 plans` → `**Plans**: 14 plans`; D-14-23 scope note
  extended with the 14-14 paragraph (r2 peer-review punch list, gate v2.1,
  training_time_device capture site fix, VAE β_eff=2.5 correction, 12-item
  doc/JSON cleanup batch); new Wave 12 entry referencing 14-14; progress
  table `12/13` → `12/14` (and post-merge flip to `13/14`).

## Resume instructions if a session ends mid-task

Each task within 14-14 stands alone and is atomically committed. Re-running
`/gsd-execute-phase 14` after `/clear` resumes from the first incomplete plan
(still 14-07, plus 14-14 if mid-stream). 14-14 must be spawned **foreground**
(background subagents auto-deny Bash for the first worktree-safety git call —
see `~/.claude/projects/-Users-shawngibford-dev-phd-qGAN/memory/project_quantum_sim_execution.md`).
</mechanics>

<output>
Create `.planning/phases/14-paper-revision-release-freeze/14-14-SUMMARY.md` when done
</output>
</content>
</invoke>