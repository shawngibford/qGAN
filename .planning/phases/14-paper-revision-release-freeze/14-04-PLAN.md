---
phase: 14-paper-revision-release-freeze
plan: 04
type: execute
wave: 3
depends_on: ["14-02"]
files_modified:
  - run_figure_suite.py
  - results/figures/
autonomous: true
requirements: [PAPER-09]
must_haves:
  truths:
    - "A complete per-model + cross-model + analysis figure suite is generated from the 2000ep artifacts (PNG + PDF + reproducibility JSON each)"
    - "The figure count matches or exceeds the verified 16 canonical Figure_*.png set (not 20)"
    - "Every figure has an underlying companion JSON; the renderer fails loudly on a missing companion (render-only)"
    - "Figures cover the canonical figure types for every model plus cross-model comparison and introspection"
  artifacts:
    - path: "run_figure_suite.py"
      provides: "render-only PNG+PDF+JSON figure suite generator"
      min_lines: 80
    - path: "results/figures/"
      provides: "complete figure set >= 16 canonical figures"
  key_links:
    - from: "run_figure_suite.py"
      to: "results/*.json (2000ep) + model_info.json"
      via: "render-only JSON->PNG/PDF, loud-fail on missing companion"
      pattern: "FileNotFoundError|_load_json"
    - from: "run_figure_suite.py"
      to: "results/figures/*.{png,pdf}"
      via: "dual save dpi=150 bbox tight"
      pattern: "savefig"
---

<objective>
Add `run_figure_suite.py` — a render-only figure module that generates,
from the accepted 2000ep artifacts, a COMPLETE per-model + cross-model +
analysis figure suite (PNG + PDF + reproducibility JSON each, D-14-17). The
completeness bar is the VERIFIED 16-figure canonical set in `Final Results from
2000 epochs - IQP:SEL circuit/` (NOT 20 — the context/D-14-17 "20" figure is a
discrepancy; only 16 `Figure_*.png` exist on disk, gaps at 14/16/17/18).
Provides the PAPER-09 dual-scale figure source.

Purpose: `results/figures/` currently holds only
trajectory/introspection + transform-ablation figures — the full per-model
suite is missing and must be added so the manuscript has a complete, coherent,
JSON-traceable figure set at a single 2000ep budget.

Output: `run_figure_suite.py` and a `results/figures/` set that meets
or exceeds the 16 canonical figures, every figure backed by a companion JSON.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-paper-revision-release-freeze/14-CONTEXT.md
@.planning/phases/14-paper-revision-release-freeze/14-RESEARCH.md
@.planning/phases/14-paper-revision-release-freeze/14-PATTERNS.md
@.planning/phases/14-paper-revision-release-freeze/14-02-SUMMARY.md

<interfaces>
<!-- run_introspect_figures.py is the EXACT pattern D-14-17 names — copy it -->

run_introspect_figures.py render-only contract:
- Headless: `import matplotlib; matplotlib.use("Agg")` BEFORE pyplot import (lines 24-29).
- Loud-fail on missing companion JSON (lines 60-73): `_load_json()` raises `FileNotFoundError` with a "render-only (no training/sampling)" message — never a silent partial figure.
- Dual save (lines 76-84): for ext in ("png","pdf"): `fig.savefig(out, bbox_inches="tight", dpi=150)`; `plt.close(fig)`.
- CLI + repo-root + print-written-paths (lines 51-57, 291-325): `argparse --figures-dir` default `results/figures`, `_find_repo_root()` to `core/preprocessing.py`, print every written path.

Canonical figure types to port for EVERY model (from D-14-17 + notebook ~11 savefig routines): distribution_comparison, acf_comparison (dual-scale OD + log_return), qq_plot, time_series_comparison, loss_curves, emd_over_training, od_reconstruction, stylized_facts_trajectory. PLUS cross-model comparison figures and the existing introspection figures (training_progression, param_trajectory, entanglement_trajectory) already in results/figures/.

Completeness bar (RESEARCH Runtime State + Open Q3 + Assumption A2): the VERIFIED 16 `Figure_*.png` in `Final Results from 2000 epochs - IQP:SEL circuit/` (Figure_2..21 with 14,16,17,18 absent) — NOT 20. The acceptance criterion is ">= 16 figures", never ">= 20".
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Render-only per-model + cross-model + analysis figure suite</name>
  <read_first>
    - run_introspect_figures.py (THE pattern D-14-17 names: headless 24-29, loud-fail _load_json 60-73, dual-save 76-84, CLI/repo-root/print 51-57 + 291-325 — copy this shape end-to-end)
    - results/headline_canonical.json + results/matched2000/ accepted artifacts (Plan 02 — the 2000ep companion data for every figure)
    - results/model_info.json (Plan 03 — the per-model registry to iterate; OPTIONAL ordering only, do not block on it)
    - results/figures/ (existing introspection figures: training_progression/param_trajectory/entanglement_trajectory + their JSON — extend, do not overwrite)
    - qgan_pennylane.ipynb (the ~11 savefig plot routines to port: distribution_comparison, acf, qq, time_series, loss_curves, emd_over_training, od_reconstruction, stylized_facts)
    - Final Results from 2000 epochs - IQP:SEL circuit/ (the 16-figure visual quality + completeness bar to match)
    - .planning/phases/14-paper-revision-release-freeze/14-RESEARCH.md (Runtime State: 16 not 20; Open Q3 / Assumption A2)
  </read_first>
  <action>
    Create `run_figure_suite.py` copying the
    `run_introspect_figures.py` shape end-to-end: headless matplotlib
    (`matplotlib.use("Agg")` before pyplot), the `_load_json()` loud-fail
    pattern (raise `FileNotFoundError` with a render-only message if a
    companion JSON is missing — NEVER emit a silent partial figure), the
    `_save()` dual PNG+PDF at `dpi=150, bbox_inches="tight"` + `plt.close`,
    the `_find_repo_root()` resolver, the `argparse --figures-dir` default of
    `results/figures`, and print-every-written-path. Port the
    notebook's canonical plot routines (distribution_comparison, acf_comparison
    dual-scale OD + log_return, qq_plot, time_series_comparison, loss_curves,
    emd_over_training, od_reconstruction, stylized_facts_trajectory) and render
    them FOR EVERY MODEL from the accepted 2000ep artifacts
    (`headline_canonical.json` + `results/matched2000/` bundle), PLUS
    cross-model comparison figures (distribution / metric side-by-side across
    all models) and keep the existing introspection figures
    (training_progression, param_trajectory, entanglement_trajectory). Each
    rendered figure writes a companion reproducibility JSON alongside the
    PNG+PDF (same stem). The 55-param IQP:SEL is the quantum entrant in every
    cross-model figure (D-14-04); the frozen-checkpoint headline and the 2000ep
    reproduction are visually labeled distinctly, never merged (D-14-10). This
    is a renderer ONLY — no training, no sampling, no metric re-computation
    (metrics come from the artifacts; figure-derived stats use
    `revision.core.eval` helpers only, never re-implement). Figure styling /
    subplot composition / file naming / which legacy routines to port-vs-rewrite
    is Claude's discretion (D-14 discretion).
  </action>
  <verify>
    <automated>./qgan_env/bin/python run_figure_suite.py && ./qgan_env/bin/python -c "import pathlib,sys; d=pathlib.Path('results/figures'); pngs=sorted(d.glob('*.png')); ok=len(pngs)>=16 and all((p.with_suffix('.pdf').exists() and p.with_suffix('.json').exists()) for p in pngs); sys.exit(0 if ok else 1)" && grep -q 'matplotlib.use(\"Agg\")' run_figure_suite.py && grep -qE 'FileNotFoundError|render-only' run_figure_suite.py</automated>
  </verify>
  <acceptance_criteria>
    - `results/figures/` contains >= 16 `*.png` files (the verified canonical-set bar — NOT 20)
    - Every `*.png` has a matching `*.pdf` AND a matching `*.json` companion (same stem)
    - `run_figure_suite.py` calls `matplotlib.use("Agg")` before `import matplotlib.pyplot` (grep)
    - The renderer raises `FileNotFoundError` (or equivalent loud-fail) on a missing companion JSON — grep for the render-only loud-fail message
    - No training/sampling import path: grep shows no model `.fit(`/training-loop call in `run_figure_suite.py` (render-only)
    - Per-model figures exist for the canonical types AND at least one cross-model comparison figure is present (grep figure stems / inspect printed paths)
    - The frozen-checkpoint headline and the 2000ep reproduction are labeled distinctly in any figure containing both (D-14-10)
  </acceptance_criteria>
  <done>Complete render-only figure suite generated from 2000ep artifacts, >=16 figures each with PNG+PDF+JSON, loud-fail on missing companion, headline/reproduction distinct.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 2000ep artifact → figure | a figure rendered from a stale/partial artifact would misrepresent results in the manuscript |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-10 | Tampering | silent partial / fabricated figure | mitigate | Render-only `_load_json` loud-fail (raise `FileNotFoundError`) — no figure is ever produced without its backing companion JSON (success-criterion 4) |
| T-14-11 | Repudiation | figure ↔ data provenance | mitigate | Every figure writes a same-stem reproducibility JSON so each manuscript figure is traceable to a `results/*.json` value |
| T-14-12 | Tampering | headline vs reproduction conflation in figures | mitigate | Distinct visual labels for frozen-checkpoint headline vs 2000ep reproduction (D-14-10) |
</threat_model>

<verification>
- `run_figure_suite.py` runs render-only and writes >=16 PNG+PDF+JSON triples
- Headless Agg backend; loud-fail on missing companion JSON
- Per-model + cross-model + introspection coverage; headline/reproduction labeled distinctly
</verification>

<success_criteria>
A complete, JSON-traceable figure suite at the single 2000ep budget meeting or
exceeding the verified 16-figure canonical bar, render-only with loud-fail on
missing data — the manuscript's figure set is now coherent and reproducible.
</success_criteria>

<output>
Create `.planning/phases/14-paper-revision-release-freeze/14-04-SUMMARY.md` when done
</output>
