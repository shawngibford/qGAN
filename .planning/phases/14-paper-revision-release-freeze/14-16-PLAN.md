---
phase: 14-paper-revision-release-freeze
plan: 16
type: execute
wave: 14
depends_on: ["14-13", "14-14", "14-15"]
files_modified:
  - run_matched2000_dualscale.py
  - results/matched2000_dualscale.json
  - run_distribution_emd.py
  - results/distribution_emd.json
  - run_welch_aggregator.py
  - results/welch_pairwise.json
  - run_model_info.py
  - docs/reconciliation_note.md
  - docs/reviewer_response.md
  - docs/methods_full.md
  - docs/peer_review_remediation.md
  - run_figure_suite.py
  - figures/cross_model_emd.png
  - figures/cross_model_emd.pdf
  - figures/cross_model_emd.json
  - figures/qq_overlay.png
  - figures/qq_overlay.pdf
  - figures/qq_overlay.json
  - docs/completeness_sweep_manifest.md
  - .planning/phases/14-paper-revision-release-freeze/14-16-SUMMARY.md
autonomous: true
requirements: [PAPER-01, PAPER-02, PAPER-08, PAPER-09, INFRA-03]
must_haves:
  truths:
    - "Two CRITICAL metric bugs surfaced by the r3 forensic peer-review pass (5-agent investigation committed at `961ee12`, synthesis at `.planning/phases/14-paper-revision-release-freeze/peer-review-r3/SYNTHESIS.md`) are closed by this plan via the user-selected Path 1 remediation: (R3-CR-2, HIGH/inherited from `scripts/run_dualscale_fidelity.py` since 14-08) the log-return EMD column in `run_matched2000_dualscale.py:368-372` calls `compute_emd(real_log_delta, trans_flat)` where `real_log_delta` is raw (std≈0.022) and `trans_flat = r['transformed']` is the standardized log-return (std≈1) emitted by `reconstruct_od` at `:201` via `r_norm = ((samples_pm1 + 1.0) / 2.0) * (r_max - r_min) + r_min`; this 50× scale mismatch artificially inflates every LR-EMD value AND inverts the cross-model ranking; (R3-CR-1, CRITICAL/NEW in 14-15) the histogram-density EMD in `run_distribution_emd.py:94-141` uses `np.histogram(..., density=True)` which silently renormalizes each histogram independently over the in-range portion only, dropping out-of-range fake mass and inverting rankings to reward narrow distributions (VAE posterior collapse, std=0.0004) and uncapped distributions (WGAN-CNN, 94% out-of-range)"
    - "User decision is LOCKED to Path 1 from SYNTHESIS.md (NOT Path 2 disclose-only, NOT Path 3 ship-current): fix both bugs at the metric-emitter level, regenerate the affected JSON aggregates, regenerate the cross-model figures that reference them, and update the three reviewer-facing docs (`reviewer_response.md`, `methods_full.md`, `reconciliation_note.md`) with the corrected numbers AND a strengthened claim grounded in Agent 4's statistical-honesty Welch tests; the R3-CR-1 fix variant is LOCKED to shared-edges-from-real-range + disclosed-out-of-range mass (NOT Option A counts-with-forfeit, NOT Option C drop-the-column) per SYNTHESIS.md's explicit endorsement and the comparability-preservation rationale"
    - "D-14-22 (`core/` byte-freeze) PRESERVED across all 7 tasks; the metric-emitter fixes for R3-CR-2 + R3-CR-1 are surgical edits to TOP-LEVEL emitters (`run_matched2000_dualscale.py` and `scripts/run_distribution_emd.py`), NOT to `core/eval.py`; the core `compute_emd` at `core/eval.py:25-36` (the v1.0 raw-sample Wasserstein) is BYTE-UNTOUCHED; every task's verify gate asserts `[ -z \"$(git diff --stat core/)\" ]`"
    - "D-14-13 (strict-accept gate, device-manifest equality semantics in `_strict_accept` from 14-13 T4 / 14-14 T2) PRESERVED — the corrected metric values flow through the same top-level emitter contracts; no edit to `_strict_accept` semantics; no new gate threshold introduced"
    - "D-14-16 (`scripts/verify_number_provenance.py` v2.1 byte-freeze post-14-14 T1) PRESERVED — no edit to the gate script; the corrected JSON values flow through the existing `_json_blobs()` walker (introduced 14-13 T2, lookbehind-fixed 14-14 T1) automatically; the gate runs PASSING against the updated `reviewer_response.md` after T4 lands, and against `methods_full.md` + `reconciliation_note.md` after T5 lands, and against all 10 paper-facing docs after T6 lands — all without any gate edit"
    - "D-14-18 (`main (4) copy.tex` / `supp_material.tex` Overleaf-canonical LaTeX read-only) PRESERVED — no edits to LaTeX sources; the reviewer-facing corrected numbers and strong-claim framing live in `docs/`"
    - "R3-CR-2 FIX (T1) — Path A (LR-EMD-vs-WGAN strong-claim WITHDRAWN per user decision): The canonical recipe per `pipeline-review-r3.md` §2 lines 124-135 is un-standardize the FAKE side via `r_norm_raw = r_norm * sigma + mu` before comparing against raw `real_log_delta`; both sides then live in raw log-return units. The standardize-real alternative (`norm_log_delta = (log_delta - mu) / sigma`) is scale-matched but inflates EMD by ~47× and produces values that do NOT match the §2 anchor table. T1 uses the un-standardize-fake recipe so the corrected aggregates exactly match `pipeline-review-r3.md` §2 anchors: AR 0.00294 best overall (closed-form Yule-Walker fits the linear-Gaussian marginal exactly), wgan_cnn 0.00711, wgan_mlp 0.01031, wgan_lstm 0.01272, V3 0.01432, iqp_sel_55_repro / V1 0.01497, V2 0.01502, vae 0.01583 worst. EVERY WGAN beats every quantum variant on corrected LR-EMD; the pre-fix `statistical-honesty-r3.md` §3b Welch tests (p ≤ 0.014, d ≤ -2.6) were computed on the broken pre-T1 LR-EMD column and DO NOT carry post-fix. Path A retracts the LR-EMD-vs-WGAN strong-claim half; OD-EMD equivalence (p > 0.36, |d| ≤ 0.65, n=5, byte-stable OD column) and DTW LR-scale dominance (independent metric, byte-stable JSON aggregates) survive unchanged. See `14-16-DEVIATION-NOTE.md` for executor's checkpoint forensic analysis."
    - "R3-CR-2 FIX preserves OD-scale rows BYTE-IDENTICAL: the OD-scale column in `matched2000_dualscale.json` is already correct (the OD inverse via `inverse_logreturns` at `core/preprocessing.py:49-72` already cumsum-exps to the OD price scale where there is no standardization-vs-raw mismatch); after T1, the OD-scale rows in `matched2000_dualscale.json#rows` and the per-(model_kind, scale='OD') aggregates MUST be BYTE-IDENTICAL to the prior emit (T1 verify gate asserts this by digesting the OD subset before and after and comparing SHA-256)"
    - "R3-CR-1 FIX (T2) variant LOCKED to shared-edges-from-real-range + disclosed out-of-range fake mass: replace the broken body in `compute_histogram_density_emd(real, fake, n_bins=50)` at `run_distribution_emd.py:94-141` (currently `real_hist, edges = np.histogram(real, bins=n_bins, density=True); fake_hist, _ = np.histogram(fake, bins=edges, density=True); bin_centers = ...; return wasserstein_distance(bin_centers, bin_centers, real_hist, fake_hist)`) with the corrected formulation: `real_hist, edges = np.histogram(real, bins=n_bins, density=False); fake_hist, _ = np.histogram(fake, bins=edges, density=False); fake_in_range_mass = float(fake_hist.sum() / len(fake)); real_norm = real_hist / real_hist.sum() if real_hist.sum() > 0 else real_hist; fake_norm = fake_hist / fake_hist.sum() if fake_hist.sum() > 0 else fake_hist; bin_centers = 0.5 * (edges[:-1] + edges[1:]); emd = float(wasserstein_distance(bin_centers, bin_centers, real_norm, fake_norm))`; both histograms now use the SAME edges (from the real distribution's empirical range) AND both are normalized to total-mass=1 OVER THE SAME EDGE SET; out-of-range fake mass is captured separately as the `fake_in_range_mass` disclosure stat (where 1.0 = no truncation; 0.06 = 94% out-of-range as in WGAN-CNN's case at log-return scale per `pipeline-review-r3.md`'s Pitfall 3 table)"
    - "R3-CR-1 FIX schema-bumps `results/distribution_emd.json` to schema string `\"distribution-emd v2 (Phase 14 plan 14-16)\"`; per-(model, seed, scale) rows gain a new `fake_in_range_mass` field (float in [0.0, 1.0]); per-(model, scale) aggregates gain a new `fake_in_range_mass_mean` field; top-level `metric_formulation` is rewritten to describe the shared-edges-from-real + disclosed-out-of-range formulation verbatim (the new string MUST then be reflected word-for-word in the C-3 disclosure paragraph extension that T5 writes into `reconciliation_note.md`); the function signature `compute_histogram_density_emd(real, fake, n_bins=50)` is preserved AND the function now returns a 2-tuple `(emd, fake_in_range_mass)` so the per-(model, seed, scale) rows can be built in one pass; AND T2 Step E2 ALSO applies the analogous `norm_log_delta = (log_delta - mu) / sigma` standardization fix to the sister call sites in `_real_references` at `:144-153` and `_fake_log_return_flat` at `:156-169` (the LR-scale path), so the LR-scale rows in `distribution_emd.json` v2 consume a standardized real reference against the standardized fake side — closing the R3-HI-1 sibling site that `code-review-r3.md` §H3 explicitly names alongside `run_matched2000_dualscale.py:368-372`"
    - "R3-CR-1 FIX expected ranking inversion verified post-emit per Agent 5's expectations in `code-review-r3.md`: on the corrected metric, VAE (posterior collapse std=0.0004) NO LONGER ranks #1 on histogram-density EMD (the broken metric rewarded its narrow-collapse behavior); WGAN-CNN's coincidental in-range alignment is now penalized by its low `fake_in_range_mass` (the disclosure stat surfaces the 94% truncation that the broken `density=True` silently dropped); quantum models rank competitively per the existing raw-sample OD-EMD ranking already in `matched2000_dualscale.json#aggregates[*, scale='OD', metric_name='emd']` (which is byte-identical to its prior emit per T1's OD-preservation guarantee)"
    - "NEW (T3, Path A): `results/welch_pairwise.json` is emitted by a NEW top-level script `scripts/run_welch_aggregator.py`; the script reads the per-(model_kind, seed, scale='OD'/'log_return', metric_name='emd') rows from `matched2000_dualscale.json` (post-T1 corrected), computes Welch t-test (`scipy.stats.ttest_ind(..., equal_var=False)`) and Mann-Whitney U (`scipy.stats.mannwhitneyu`) for all 40 quantum-classical pairs (4 quantum × 5 classical × 2 scales = 40 records emitted for transparency), plus Cohen's d per pair with `ddof=1` pooled SD; Path A schema includes `pairs[]` (all 40 with full per-pair stats), `summaries{OD_floor_welch_p_quantum_vs_classical, OD_ceiling_abs_cohen_d_quantum_vs_classical}` (the enforced OD-EMD anchors) plus `log_return_floor_welch_p_quantum_vs_wgan` and `log_return_extremum_cohen_d_quantum_vs_wgan` (LR-EMD summaries REPORTED ONLY, not threshold-enforced), `strong_claim_thresholds{floor_welch_p_OD: 0.36, ceiling_abs_cohen_d_OD: 0.65}` (the surviving OD-EMD literals as first-class JSON leaves — the withdrawn LR-EMD thresholds `ceiling_welch_p_LR_vs_wgan` / `extremum_cohen_d_LR_vs_wgan` are NOT emitted per Path A), and a `notes` field documenting the r3 process retraction. The pre-write gate ONLY asserts OD-EMD threshold direction satisfaction; LR-EMD pairs are emitted without pre-write threshold checks because the data does not satisfy the pre-fix `p ≤ 0.014, d ≤ -2.6` direction (those tests were computed on the broken column). The v2.1 gate resolves the surviving OD literals against `strong_claim_thresholds.{floor_welch_p_OD, ceiling_abs_cohen_d_OD}` directly"
    - "After T4, `docs/reviewer_response.md` R1-M1 row (a single markdown-table cell at `docs/reviewer_response.md:36` reading 'Added matched-parameter classical WGAN-GP (MLP/CNN/LSTM critics) and a non-adversarial VAE + AR baseline, all at matched 2000-epoch budget, identical critic/optimizer/seed set; parameter-count-controlled comparison table' — NO equivalence claim is currently made) is LEFT VERBATIM (it accurately describes what was done); a NEW H2 section `## Parametric-efficiency equivalence (post-r3 corrected metrics)` is INSERTED between the existing `## Marginal-convergence finding (post-r2 investigation)` H2 and the existing `## Completeness sweep (Plans 14-09 .. 14-11)` H2, asserting the user-approved Path A reframed claim VERBATIM: '55 quantum parameters achieve OD-scale EMD statistically equivalent to classical generators of 73-562 generator params AND the full ~2.5x10^5 adversarial budget (generator + 250881-parameter shared critic) carried by every WGAN variant (Welch p > 0.36, |d| ≤ 0.65, n=5). On log-return EMD, AR (3 params, closed-form Yule-Walker MLE) leads at 0.003; quantum/WGAN/VAE cluster in 0.007-0.016. No statistically meaningful quantum-vs-WGAN separation on marginal log-return distribution. On DTW (Dynamic Time Warping, temporal alignment), quantum dominates: all 4 variants beat every WGAN+AR on LR-DTW; best quantum beats Orlandi et al. by 6.5x on OD-DTW. This is the temporal-structure capture quantum is specifically designed for.'; the DTW addendum (T4 Step B2) is preserved VERBATIM — it was authored against byte-stable DTW JSON aggregates and survives Path A unchanged; the per-baseline OD-EMD equivalence table (5 rows × Welch p / Cohen d / parameter count) cites `welch_pairwise.json#pairs[?scale=='OD']` for the per-pair stats and `matched2000_dualscale.json#aggregates[*, scale='OD']` for the corrected (byte-stable OD) aggregates"
    - "After T3, `docs/methods_full.md` carries a new 'Log-return scale correction (Plan 14-16)' paragraph in the OD-scale evaluation section documenting the R3-CR-2 fix (the broken `compute_emd(real_log_delta, trans_flat)` call site at `run_matched2000_dualscale.py:368-372` is replaced so both sides of the EMD live in the standardized log-return space) AND a new 'Shared-edges formulation (Plan 14-16)' paragraph in the histogram-density EMD section documenting the R3-CR-1 fix (the `density=True` per-distribution renormalization is replaced with shared-edges-from-real + the `fake_in_range_mass` disclosure stat) — both paragraphs preserve the existing 14-11/14-14 §3.x text verbatim and append at the end of their respective sections"
    - "After T3, `docs/reconciliation_note.md` carries the existing OD-scale headline table from 14-13 (preserved verbatim) AND the 3-column comparable-variants table from 14-15 (preserved structurally — column 1 OD raw-sample EMD is byte-identical pre/post per T1's OD-preservation guarantee, columns 2 and 3 are REGENERATED from the corrected JSONs); the C-3 disclosure paragraph (originally from 14-13 T3, extended in 14-15 T2) gains one more disclosure sentence: 'Plan 14-16 r3 remediation: column 2 (log-return raw-sample EMD) corrected for a standardization scale mismatch in `run_matched2000_dualscale.py:368-372` (R3-CR-2); column 3 (histogram-density EMD, OD scale) reformulated with shared-edges-from-real to eliminate the per-distribution renormalization bias documented in `peer-review-r3/code-review-r3.md` §H3 (R3-CR-1). See `peer_review_remediation.md` Plan 14-16 section for full forensic disclosure.'"
    - "After T6, `figures/cross_model_emd.{png,pdf,json}` is RE-RENDERED against the corrected `matched2000_dualscale.json` aggregates; the 9-model OD-EMD bar group is BYTE-IDENTICAL to the prior render structurally (because OD aggregates are preserved per T1's invariant) but the LR-EMD bar group is REGENERATED reflecting the corrected aggregates (rankings inverted: AR ~0.003 best, quantum cluster ~0.015, WGAN cluster significantly worse); the existing CR-1 sha256-seeded determinism contract (from 14-13) is preserved: two consecutive renders produce byte-identical `model_order_color_keys` arrays in the companion JSON"
    - "After T6, `figures/qq_overlay.{png,pdf,json}` from 14-15 is cross-checked for any incidental dependence on `matched2000_dualscale.json`; the QQ overlay was rendered from `samples.npy` directly (NOT from JSON aggregates) so panels A/B are STRUCTURALLY UNCHANGED; the companion JSON `convergence_stats` numbers (8/9 pairwise median ~0.03, WGAN-CNN-vs-others ~0.69, all-9 vs-real ~0.25, WGAN-CNN vs-real ~0.81 — all from 14-15) are STRUCTURALLY UNCHANGED; T6 emits a `## Plan 14-16 verification` line in `qq_overlay.json` (additive top-level field) confirming no structural change required — IF the cross-check finds any incidental dependence (e.g., a `convergence_stats` number was sourced from `matched2000_dualscale.json` rather than from samples directly), the qq_overlay is RE-RENDERED instead and the verification line records the re-render"
    - "After T6, v2.1 gate (D-14-16 byte-frozen post-14-14 T1) runs PASSING against all 10 paper-facing docs from 14-14 T5 / 14-15 T5: `paper_blocks_framing.md`, `paper_blocks_refs_methods.md`, `reviewer_response.md`, `reconciliation_note.md`, `methods_full.md`, `circuit_atlas.md`, `completeness_sweep_manifest.md`, `training_protocol.md`, `dataset_stats.md`, `peer_review_remediation.md` (plus `REPRODUCE.md` if any paper-facing claims appear post-14-16); the corrected LR-EMD literals in `reconciliation_note.md`'s 3-column table resolve to `matched2000_dualscale.json#aggregates[*, scale='log_return']` (now corrected); the corrected histogram-density EMD literals resolve to `distribution_emd.json#aggregates[*, scale='OD']` (now under schema v2); the strong-claim OD-EMD literals in `reviewer_response.md` (`p > 0.36`, `|d| ≤ 0.65`, `n=5`) resolve to `welch_pairwise.json#summaries.OD_*` and `welch_pairwise.json#strong_claim_thresholds.{floor_welch_p_OD,ceiling_abs_cohen_d_OD}` (Path A withdraws the LR-EMD-vs-WGAN strong-claim half; the LR-EMD cluster framing literals `0.003`, `0.007`, `0.016` resolve to `matched2000_dualscale.json#aggregates[*, scale='log_return', metric_name='emd'].mean` as JSON-anchored cluster anchors); `welch_pairwise.json#pairs[*]` still carries per-pair Welch t/p + Cohen d + MWU p for ALL 40 quantum-classical pairs on BOTH OD-EMD + LR-EMD as machine-readable provenance, but the LR-EMD pairs are emitted WITHOUT threshold-direction assertions (the emitter's pre-write gate ONLY checks OD-EMD threshold satisfaction); the corrected LR-EMD literals in `reconciliation_note.md`'s 3-column table column 2 resolve to `matched2000_dualscale.json#aggregates[*, scale='log_return']` (now corrected via T1 un-standardize-fake); the methods_full.md correction paragraphs resolve to the appropriate JSON entries (matched2000_dualscale.json, distribution_emd.json v2)"
    - "After T5, `.planning/phases/14-paper-revision-release-freeze/14-16-SUMMARY.md` exists with Self-Check PASS + 7 task commit SHAs + the SUMMARY commit SHA + a final-state line declaring Phase 14 incomplete plans = `[14-07]` only (per the deferred-manual-Zenodo-DOI gate recorded in `~/.claude/projects/-Users-shawngibford-dev-phd-qGAN/memory/project_phase14_zenodo_blocker.md`); a new `## Plan 14-16 — r3 forensic remediation` section appended to `docs/peer_review_remediation.md` enumerates R3-CR-1 + R3-CR-2 with full mechanism + fix + resulting strong-claim framing (existing 14-13 + 14-14 + 14-15 sections preserved verbatim); a new `## Plan 14-16` section appended to `docs/completeness_sweep_manifest.md` enumerates T1-T7 deliverables (existing 14-09 / 14-10 / 14-11 / 14-12 / 14-13 / 14-14 / 14-15 sections preserved verbatim)"
    - "Welch statistical anchors for the Path A strong-claim framing (n=5 seeds per cell, seeds {42, 43, 44, 45, 46}): floor Welch p-value for quantum-vs-classical OD-EMD pairwise comparisons = `p > 0.36` (MINIMUM p across all 20 quantum-classical pairs from `statistical-honesty-r3.md` §3a — byte-stable OD column, unaffected by T1's LR-EMD fix); ceiling Cohen's d for quantum-vs-classical OD-EMD pairwise = `|d| ≤ 0.65` (same byte-stable OD provenance); LR-EMD-vs-WGAN strong-claim Welch thresholds (`p ≤ 0.014`, `d ≤ -2.6`) WITHDRAWN per Path A — those literals were derived from the broken pre-T1 LR-EMD column and DO NOT carry on corrected un-standardize-fake aggregates; corrected LR-EMD cluster anchors (per `pipeline-review-r3.md` §2 exact match): AR 0.003 best, quantum/WGAN/VAE cluster 0.007-0.016, no statistically meaningful quantum-vs-WGAN separation; quantum parameter count = 55 (iqp_sel_55 reference circuit per 14-01 D-14-07); classical generator parameter-equivalence range = 73-562 generator parameters (74 wgan_mlp, 73 wgan_cnn, 78 wgan_lstm, 562 vae) and ~2.5x10^5 adversarial budget (generator + 250881 shared critic) — per the per-model param counts already recorded in `results/model_info.json` from earlier phases; the DTW addendum (T4 Step B2, T5 Step E2/E3, T7 Step A2) is byte-stable and survives Path A — DTW LR-scale quantum dominance + 6.5x OD-DTW Orlandi improvement are independent of the EMD fix"
    - "Files explicitly NOT modified by this plan: `core/` (D-14-22 byte-freeze, asserted by every task verify gate); `scripts/verify_number_provenance.py` (D-14-16 byte-freeze post-14-14 T1, no gate edit); `main (4) copy.tex` / `supp_material.tex` (D-14-18 Overleaf-canonical LaTeX read-only); the existing `qq_overlay.png/pdf` binaries (only the JSON companion gains an additive `## Plan 14-16 verification` line, IF the structural cross-check confirms no re-render is required — otherwise the binaries are regenerated and the verification line records that fact); the 9 per-model `qq_{model}.json` companions (only their 14-15 `caption_note` field is preserved verbatim; no further edit); the 14-15 byte-stable upstream docs (`circuit_atlas.md`, `paper_blocks_refs_methods.md`, `training_protocol.md`, `dataset_stats.md`)"
    - "Phase 14 incomplete plans post-14-16 = `[14-07]` only (the deferred manual Zenodo DOI deposit gate); ROADMAP progress row `14. Paper Revision & Release Freeze | v2.0 | 14/15 → 15/16 (mid-plan) → 15/16 (post-merge — Plan 14-16 with 7 tasks)` (orchestrator handles ROADMAP arithmetic post-wave)"
    - "DTW historical-asymmetry disclosure (Plan 14-16 third historical-vs-current case, structurally parallel to R3-CR-1 + R3-CR-2): after T4 Step B2 + T5 Step E2 + T7 Step A2 land, the reviewer-facing docs carry load-bearing DTW assertions all gate-resolvable against `matched2000_dualscale.json#aggregates[*, metric_name='dtw_mean']`: (a) on OD-scale, all 4 quantum variants achieve DTW in the range 0.298–0.302 (V2=0.298 best, iqp_sel_55_repro=0.302 upper-bound), beating the Orlandi et al. reference DTW=1.954 by ~6.5x (best-quantum ratio 1.954/0.298=6.55x, mean-quantum ratio 1.954/0.300=6.51x — the manuscript Orlandi-improvement narrative is structurally preserved post-r3); (b) on log-return DTW, every quantum variant (range 0.940 V1 to 1.122 V3, mean ~0.99) outperforms every WGAN baseline (wgan_lstm=1.581, wgan_mlp=2.624, wgan_cnn=6.863) AND the AR baseline (7.699) — the worst-quantum LR-DTW (V3 at 1.122) still beats the best-classical LR-DTW (wgan_lstm at 1.581), so the dominance is total across the 12 quantum-vs-{WGAN,AR} pairs; (c) VAE's anomalously-low LR-DTW=0.088 reflects posterior collapse (sample std=0.0004 per Agent 5 's empirical confirmation at `peer-review-r3/code-review-r3.md` §H3 line 212, vs real std≈0.0217 — 50× narrower; same mechanism that triggered R3-CR-1 at the histogram-density EMD) and is explicitly NOT interpreted as evidence of model quality — disclosed-not-celebrated; (d) the manuscript headline DTW=0.6843 at `main (4) copy.tex:190,266` + `supp_material.tex:290` is labeled as a pre-v1.0 best-case iqp_sel_55 evaluation (NOT a current-pipeline emission), parallel to the C-3 metric-redefinition disclosure pattern; (e) all 11 numeric literals (0.298, 0.302, 1.954, 1.58, 2.62, 6.86, 7.70, 0.088, 0.6843, 0.30, 0.99) resolve under v2.1 gate ε-neighborhood (ε ≈ 0.005 for 3-decimal floats) against either `matched2000_dualscale.json` leaves or labeled historical-reference literals — no fuzzy ε-match against unrelated metrics, no claim without a JSON-anchored or labeled-historical-reference provenance"
  artifacts:
    - path: "run_matched2000_dualscale.py"
      provides: "Surgically edited (Task 1) to fix R3-CR-2 — the scale-mismatched log-return EMD call at `:368-372`. The fix builds a standardized real-log-return reference `norm_log_delta = (log_delta - mu) / sigma` from the per-seed `inverse_kwargs.npz` (the SAME mu/sigma used by `reconstruct_od` at `:198-199` to compute the standardized `r_norm` on the fake side) so both sides of the EMD live in the standardized log-return space (std≈1). The `_log_return_rows` function signature MAY gain `mu` + `sigma` parameters (or read them from the per-seed `inverse_kwargs.npz` via a small helper); the EMD call becomes `compute_emd(norm_log_delta, trans_flat)` where `trans_flat = r['transformed'].reshape(-1)` is unchanged. The moment / ACF / DTW rows on the log-return scale are LEFT UNTOUCHED (they were already comparing same-scale arrays internally — the scale mismatch was specific to the EMD call). The OD-scale `_od_scale_rows` is BYTE-UNTOUCHED. The `D-11-10` provenance comment in the function docstring is extended with: 'Plan 14-16 r3 remediation: the log-return EMD reference switched from raw `real_log_delta` (std≈0.022) to standardized `norm_log_delta = (log_delta - mu) / sigma` (std≈1) so both sides of the wasserstein_distance are on the same scale — see R3-CR-2 mechanism in `peer-review-r3/code-review-r3.md` §H3 and the corrected aggregates in `peer-review-r3/pipeline-review-r3.md` §2.' D-14-22 (`core/`) byte-untouched."
      min_lines: 8
    - path: "results/matched2000_dualscale.json"
      provides: "Re-emitted (Task 1) with R3-CR-2 corrections. Per-(model, pipeline, seed) rows for `metric_name='emd', scale='log_return'` carry the corrected (standardized-vs-standardized) value; per-(model_kind, scale='log_return') aggregates regenerated with `ddof=1` std. Per-(model_kind, scale='OD') rows AND aggregates are BYTE-IDENTICAL to the prior emit (T1 verify gate asserts this via SHA-256 over the OD subset before/after). All other metric_names on the log-return scale (moments, ACF lags, DTW) are BYTE-IDENTICAL because the scale mismatch was specific to the EMD call. The `schema` and other top-level fields are unchanged (no version bump needed — the fix is a value correction within the existing schema). The headline `iqp_sel_55_headline` rows (n=1) are corrected the same way if present. Verify against `pipeline-review-r3.md` anchors: AR LR-EMD ~0.003 best overall, V1 ~0.0145 best quantum, three quantum siblings ~0.015, VAE ~0.0163, WGAN cluster all > 0.02 with WGAN-CNN remaining the outlier."
      min_lines: 100
    - path: "run_distribution_emd.py"
      provides: "Surgically edited (Task 2) to fix R3-CR-1. The `compute_histogram_density_emd(real, fake, n_bins=50)` function at `:94-141` is replaced: returns a 2-tuple `(emd, fake_in_range_mass)`; uses `density=False` for both histograms; computes `edges` from real ONLY; normalizes both histograms to total-mass=1 OVER THE SAME EDGE SET (no per-distribution renormalization); captures `fake_in_range_mass = float(fake_hist.sum() / len(fake))` as a separate disclosure stat. The module-level `SCHEMA` constant at `:83` is bumped from `'distribution-emd v1 (Phase 14 plan 14-15)'` to `'distribution-emd v2 (Phase 14 plan 14-16)'`. The module-level `METRIC_FORMULATION` constant at `:87-91` is rewritten to describe the new formulation verbatim: 'np.histogram(real, bins=50, density=False) -> edges from real; np.histogram(fake, bins=edges, density=False); both histograms normalized to total-mass=1 over the same edge set; bin_centers = 0.5*(edges[:-1] + edges[1:]); scipy.stats.wasserstein_distance(bin_centers, bin_centers, real_norm, fake_norm); out-of-range fake mass disclosed separately as fake_in_range_mass = fake_hist.sum() / len(fake)'. The `_model_seed_rows` builder at `:172-243` updates to call the new 2-tuple-returning function and append the `fake_in_range_mass` field to each per-(model, seed, scale) row. The `_build_aggregates` step gains a `fake_in_range_mass_mean` field per (model_kind, scale). The module docstring is extended with a 'Plan 14-16 r3 remediation' paragraph referencing the R3-CR-1 mechanism in `peer-review-r3/code-review-r3.md` §H3 AND a 'Plan 14-16 R3-HI-1 sister-fix' paragraph documenting Step E2: the LR-scale call sites in `_real_references` at `:144-153` and `_fake_log_return_flat` at `:156-169` consume the standardized `norm_log_delta = (log_delta - mu) / sigma` real reference (same recipe as T1's edit to `run_matched2000_dualscale.py`), so both LR-scale pathways in both top-level emitters share the scale-matched comparison contract — the LR-scale rows in `distribution_emd.json` v2 are correct (not inherited-broken). D-14-22 (`core/`) byte-untouched (the function lives in the TOP-LEVEL emitter, not in core)."
      min_lines: 40
    - path: "results/distribution_emd.json"
      provides: "Re-emitted (Task 2) under schema string `'distribution-emd v2 (Phase 14 plan 14-16)'`. Top-level `metric_formulation` updated to the new shared-edges + disclosed-out-of-range string verbatim (matching the new `METRIC_FORMULATION` constant in `scripts/run_distribution_emd.py` word-for-word). `data_hash: '91e447d4624e25b3'` preserved (corpus consistency). `n_bins: 50` preserved. Per-(model, seed, scale) rows gain new `fake_in_range_mass` field (float in [0.0, 1.0]; e.g., WGAN-CNN log-return scale ~0.06 per the 94%-out-of-range disclosure in `code-review-r3.md`'s table; quantum models close to 1.0 because the `*0.1` cap keeps them inside the real range; VAE close to 1.0 because posterior collapse keeps it tightly inside the mean). The LR-scale rows ALSO consume a standardized real reference (post-Step-E2 fix), so the LR-scale aggregates are at the corrected magnitude (~0.003-0.02) matching `pipeline-review-r3.md` §2 anchors — they are NOT inherited-broken. Per-(model_kind, scale) aggregates gain new `fake_in_range_mass_mean` field. The corrected EMD values surface ranking inversion vs schema v1: VAE no longer #1 on histogram-density (the broken metric rewarded its narrow collapse); WGAN-CNN's `fake_in_range_mass` disclosure surfaces the 94% truncation that v1's `density=True` silently dropped; quantum models rank competitively per the byte-stable OD raw-sample EMD ranking in `matched2000_dualscale.json#aggregates[*, scale='OD', metric_name='emd']`."
      min_lines: 60
    - path: "run_welch_aggregator.py"
      provides: "NEW (Task 3). Top-level emitter that reads per-(model_kind, seed, scale, metric_name='emd') rows from `results/matched2000_dualscale.json` (post-T1 corrected) and emits `results/welch_pairwise.json` with per-(quantum, classical, scale) Welch t-test results (statistic, p-value), Cohen's d effect size, Mann-Whitney U p-value, sample sizes (n=5 per group). The script uses `scipy.stats.ttest_ind(equal_var=False)` for Welch and `scipy.stats.mannwhitneyu(alternative='two-sided')` for MWU; pooled SD for Cohen's d uses `ddof=1`. The script lives in `revision/` (TOP-LEVEL emitter, NOT in `core/`; D-14-22 preserved). The script signature: `def main(): rows = json.load(open('results/matched2000_dualscale.json'))['rows']; ...; write_json('results/welch_pairwise.json', payload)`."
      min_lines: 60
    - path: "results/welch_pairwise.json"
      provides: "NEW (Task 3, Path A). Schema: `welch-pairwise v1 (Phase 14 plan 14-16 W2)`. Per-pair records keyed by `(quantum, classical, scale)` for ALL 40 quantum-classical pairs across BOTH OD-EMD + LR-EMD. Anchors the SURVIVING Path A OD-EMD strong-claim literals (`p > 0.36`, `|d| ≤ 0.65`) cited in `docs/reviewer_response.md`'s new H2 section via JSON-anchored path lookups: `welch_pairwise.json#pairs[?scale=='OD']` for per-pair OD records AND `welch_pairwise.json#strong_claim_thresholds.{floor_welch_p_OD,ceiling_abs_cohen_d_OD}` for the threshold LITERALS as first-class JSON leaves. The LR-EMD pairs are emitted in `pairs[?scale=='log_return']` for transparency but WITHOUT corresponding threshold leaves (Path A retraction: the LR-EMD-vs-WGAN strong-claim `p ≤ 0.014, d ≤ -2.6` was computed on the broken pre-T1 column and is WITHDRAWN). The emitter aborts before writing JSON only if computed OD-EMD `summaries` values fail their threshold directions. A `notes` field provides machine-readable retraction provenance. `data_hash: 91e447d4624e25b3` (corpus consistency)."
      min_lines: 100
    - path: "run_model_info.py"
      provides: "Updated (Task 5) to surface the corrected aggregates into `reconciliation_note.md`'s 3-column comparable-variants table from 14-15. The emit logic structurally unchanged — column 1 still sources from `matched2000_dualscale.json#aggregates[*, scale='OD', metric_name='emd']` (byte-identical pre/post per T1's OD-preservation guarantee); column 2 still sources from `matched2000_dualscale.json#aggregates[*, scale='log_return', metric_name='emd']` (now corrected); column 3 still sources from `distribution_emd.json#aggregates[*, scale='OD']` (now under schema v2). The emitter is unaware of the underlying scale-fix and reformulation — it just reads the updated JSON aggregates. The optional `fake_in_range_mass_mean` column MAY be appended as a 4th disclosure column in the table (recommended for full transparency to reviewers; if appended, the table header gains `fake_in_range_mass_mean`); IF appended, sub-rows for WGAN-CNN show the 94%-out-of-range disclosure value prominently. The new H2 section in `reviewer_response.md` is added by T4 as a manual edit (NOT via this emitter) — T4 preserves the R1-M1 row at `:36` verbatim and inserts the new H2 between the existing Marginal-convergence and Completeness-sweep H2s. `scripts/run_model_info.py` is updated by T5 only to re-emit `reconciliation_note.md` against the corrected JSON columns; it does NOT write to `reviewer_response.md`."
      min_lines: 10
    - path: "docs/reconciliation_note.md"
      provides: "Re-emitted (Task 5) with the corrected 3-column comparable-variants table from 14-15 (column 1 OD raw-sample EMD byte-identical; columns 2 + 3 regenerated from corrected JSONs). The existing OD-scale headline table from 14-13 is preserved verbatim. The C-3 metric-redefinition disclosure paragraph (originally from 14-13 T3, extended in 14-15 T2) gains one more disclosure sentence: 'Plan 14-16 r3 remediation: column 2 (log-return raw-sample EMD) corrected for a standardization scale mismatch in `run_matched2000_dualscale.py:368-372` (R3-CR-2); column 3 (histogram-density EMD, OD scale) reformulated with shared-edges-from-real to eliminate the per-distribution renormalization bias documented in `peer-review-r3/code-review-r3.md` §H3 (R3-CR-1). See `peer_review_remediation.md` Plan 14-16 section for full forensic disclosure.' All prior 14-13 + 14-14 + 14-15 sections preserved verbatim."
      min_lines: 60
    - path: "docs/reviewer_response.md"
      provides: "Re-emitted (Task 4, Path A). The existing R1-M1 row (`docs/reviewer_response.md:36`, a single markdown-table cell with NO equivalence claim — reads 'Added matched-parameter classical WGAN-GP (MLP/CNN/LSTM critics) and a non-adversarial VAE + AR baseline, all at matched 2000-epoch budget, identical critic/optimizer/seed set; parameter-count-controlled comparison table') is LEFT VERBATIM. A new H2 section `## Parametric-efficiency equivalence (post-r3 corrected metrics)` is INSERTED into the doc between the existing `## Marginal-convergence finding (post-r2 investigation)` H2 and the existing `## Completeness sweep (Plans 14-09 .. 14-11)` H2, asserting the user-approved Path A reframed claim VERBATIM (three bolded paragraphs): '**55 quantum parameters achieve OD-scale EMD statistically equivalent to classical generators of 73-562 generator params AND the full ~2.5x10^5 adversarial budget (generator + 250881-parameter shared critic) carried by every WGAN variant (Welch p > 0.36, |d| ≤ 0.65, n=5).** **On log-return EMD, AR (3 params, closed-form Yule-Walker MLE) leads at 0.003; quantum/WGAN/VAE cluster in 0.007-0.016. No statistically meaningful quantum-vs-WGAN separation on marginal log-return distribution.** **On DTW (Dynamic Time Warping, temporal alignment), quantum dominates: all 4 variants beat every WGAN+AR on LR-DTW; best quantum beats Orlandi et al. by 6.5x on OD-DTW. This is the temporal-structure capture quantum is specifically designed for.**' The withdrawn LR-EMD-vs-WGAN strong-claim literals (`p ≤ 0.014`, `d ≤ -2.6`, `empirical range d in [-5.22, -2.63]`, `significantly outperform every WGAN baseline`) MUST NOT appear in the document. A subsection enumerates the per-baseline OD-EMD equivalence table (quantum 55 params ≡ wgan_mlp 74 / wgan_cnn 73 / wgan_lstm 78 / vae 562 / ar 3 on OD-EMD) with explicit citations to `welch_pairwise.json#pairs[?scale=='OD']` (Path A: OD-only) and `matched2000_dualscale.json#aggregates[*, scale='OD']` (byte-stable). The DTW addendum H3 subsection (T4 Step B2) is preserved VERBATIM — DTW JSON aggregates survive Path A unchanged. All other R1-XX / R2-XX / Cross-Cutting / Completeness sweep / 14-13 CR-4 / 14-14 R1-m4 / 14-15 Marginal-convergence finding sections are preserved verbatim."
      min_lines: 80
    - path: "docs/methods_full.md"
      provides: "Re-emitted (Task 5). The OD-scale evaluation section gains a new 'Log-return scale correction (Plan 14-16)' paragraph at its end documenting the R3-CR-2 fix verbatim: the broken `compute_emd(real_log_delta, trans_flat)` at `run_matched2000_dualscale.py:368-372` was replaced so the real reference is now the standardized `norm_log_delta = (log_delta - mu) / sigma` matching the standardized `trans_flat` on the fake side (both std≈1); corrected aggregates surface in `matched2000_dualscale.json#aggregates[*, scale='log_return']` and `reconciliation_note.md`'s 3-column table column 2. The histogram-density EMD section (from 14-15) gains a new 'Shared-edges formulation (Plan 14-16)' paragraph at its end documenting the R3-CR-1 fix verbatim: the per-distribution `density=True` renormalization was replaced with shared-edges-from-real + total-mass=1 normalization over the same edge set; out-of-range fake mass disclosed separately as `fake_in_range_mass`; corrected aggregates surface in `distribution_emd.json#aggregates[*, scale='OD']` under schema v2. All prior 14-11 / 14-14 §3.x / 14-15 marginal-convergence sections preserved verbatim."
      min_lines: 12
    - path: "docs/peer_review_remediation.md"
      provides: "Re-emitted (Task 5). A new `## Plan 14-16 — r3 forensic remediation` section appended at the bottom enumerating: (R3-CR-2 mechanism) the scale-mismatched real-log-return reference at `run_matched2000_dualscale.py:368-372`, with file-line refs, the 50× inflation evidence from `pipeline-review-r3.md` §2, the corrected aggregates' anchor values; (R3-CR-2 fix) the standardized `norm_log_delta = (log_delta - mu) / sigma` substitution; (R3-CR-1 mechanism) the per-distribution `density=True` renormalization bias in `run_distribution_emd.py:94-141`, with the empirical evidence from `code-review-r3.md` §H3 and Agent 5's ranking-inversion table; (R3-CR-1 fix) the shared-edges-from-real + disclosed-out-of-range `fake_in_range_mass` formulation; (resulting strong-claim framing) the Welch-test grounded equivalence claim now in `reviewer_response.md` R1-M1. Existing 14-13 + 14-14 + 14-15 sections preserved verbatim."
      min_lines: 30
    - path: "run_figure_suite.py"
      provides: "Surgically edited (Task 6) ONLY if the QQ overlay structural cross-check finds an incidental dependence on `matched2000_dualscale.json`; otherwise UNTOUCHED. The cross-model EMD figure rendering path (around the existing `render_cross_model_emd` function — verify path with grep) is exercised via the existing caller loop to regenerate `cross_model_emd.{png,pdf,json}` against the corrected `matched2000_dualscale.json` aggregates; no code change needed for cross_model_emd because the rendering path already reads aggregates from the JSON. The CR-1 sha256-seeded determinism contract (from 14-13) is preserved: two consecutive renders produce byte-identical `model_order_color_keys` arrays."
      min_lines: 0
    - path: "figures/cross_model_emd.png"
      provides: "Re-rendered (Task 6) against the corrected `matched2000_dualscale.json` aggregates. The 9-model OD-EMD bar group is BYTE-IDENTICAL structurally to the prior render (OD aggregates byte-stable per T1's invariant). The LR-EMD bar group is REGENERATED reflecting the corrected aggregates (rankings inverted: AR ~0.003 best, quantum cluster ~0.015 significantly beating every WGAN, WGAN-CNN remaining the outlier). PNG file timestamp will change but the OD bar pixel values for the bar tops are preserved within rendering tolerance."
      min_lines: 0
    - path: "figures/cross_model_emd.pdf"
      provides: "Re-rendered (Task 6). PDF companion of `cross_model_emd.png`; same content."
      min_lines: 0
    - path: "figures/cross_model_emd.json"
      provides: "Re-rendered (Task 6). Top-level fields: `caption` (updated to reference the Plan 14-16 LR-EMD correction), `model_order_color_keys` (preserved byte-identical across re-renders per CR-1 contract), `data_hash: '91e447d4624e25b3'`, `render_only: false`, `sha256_png` (updated), `bar_values_OD` (byte-identical to prior emit per T1's OD-preservation guarantee), `bar_values_LR` (regenerated from corrected `matched2000_dualscale.json#aggregates[*, scale='log_return']`)."
      min_lines: 12
    - path: "figures/qq_overlay.png"
      provides: "Conditionally re-rendered (Task 6): structurally UNCHANGED if the cross-check confirms no incidental dependence on `matched2000_dualscale.json` (the figure was rendered from samples directly per 14-15 design); the binary may receive a touch-update if re-render is needed (then `sha256_png` in the companion JSON updates). Default outcome per the brief: NOT re-rendered (no dependence found)."
      min_lines: 0
    - path: "figures/qq_overlay.pdf"
      provides: "Conditionally re-rendered (Task 6); default UNCHANGED — same outcome as the PNG."
      min_lines: 0
    - path: "figures/qq_overlay.json"
      provides: "Re-emitted (Task 6) with an additive top-level `plan_14_16_verification` field carrying either `{ structural_change: false, reason: 'qq_overlay was rendered from samples.npy directly per 14-15; no dependence on matched2000_dualscale.json found in the convergence_stats sources' }` or `{ structural_change: true, reason: ... , new_sha256_png: ... }` depending on the cross-check outcome. All other 14-15 fields (`caption`, `convergence_stats`, `cluster_summary`, `data_hash`, `render_only`, `model_order_color_keys`, `sha256_png`, `routing_verification`) preserved verbatim under the default no-dependence outcome."
      min_lines: 20
    - path: "docs/completeness_sweep_manifest.md"
      provides: "Re-emitted (Task 7). A new `## Plan 14-16` section appended at the bottom enumerating T1-T7 deliverables (R3-CR-2 fix + R3-CR-1 + R3-HI-1 sister-fix + welch_pairwise.json aggregator + corrected aggregates + reviewer-facing strong-claim framing + methods_full/reconciliation_note correction paragraphs + figure re-render + SUMMARY). Existing 14-09 / 14-10 / 14-11 / 14-12 / 14-13 / 14-14 / 14-15 sections preserved verbatim."
      min_lines: 20
    - path: ".planning/phases/14-paper-revision-release-freeze/14-16-SUMMARY.md"
      provides: "NEW (Task 7). Standard GSD SUMMARY structure: Self-Check section (status PASS, 14-point verification checklist enumerated — extended from the original 10-point list to cover the 4 new task scopes from the revision), Task commits section (7 task SHAs + SUMMARY commit SHA), Final-state declaration (Phase 14 incomplete plans = `[14-07]` only, post-14-16 ROADMAP progress row reflecting the new arithmetic), Cross-references section (pointers to `peer-review-r3/SYNTHESIS.md`, `14-15-SUMMARY.md`, the new `## Plan 14-16` section in `completeness_sweep_manifest.md`, and the new `## Plan 14-16 — r3 forensic remediation` section in `peer_review_remediation.md`)."
      min_lines: 50
  key_links:
    - from: "run_matched2000_dualscale.py:368-372 (R3-CR-2 fix site)"
      to: "results/matched2000_dualscale.json#aggregates[*, scale='log_return', metric_name='emd']"
      via: "Task 1 swaps the real-log-return reference from raw `real_log_delta` (std≈0.022) to standardized `norm_log_delta = (log_delta - mu) / sigma` (std≈1) so both sides of `compute_emd(real, trans_flat)` are on the SAME scale; the per-seed `mu, sigma` come from `inverse_kwargs.npz` (the SAME values used by `reconstruct_od` at `:198-199` to build `r_norm` on the fake side); the corrected aggregates surface in the JSON without any schema change"
      pattern: "norm_log_delta|standardized log-return|R3-CR-2|inverse_kwargs.npz|compute_emd\\(norm_log_delta"
    - from: "run_distribution_emd.py:94-141 (R3-CR-1 fix site)"
      to: "results/distribution_emd.json#aggregates[*, scale='OD'] (schema bumped to v2)"
      via: "Task 2 replaces the `density=True` per-distribution renormalization with shared-edges-from-real + total-mass=1 normalization over the same edge set; out-of-range fake mass is captured separately as the `fake_in_range_mass` disclosure stat; the function returns a 2-tuple `(emd, fake_in_range_mass)`; the `SCHEMA` constant bumps to `'distribution-emd v2 (Phase 14 plan 14-16)'`; the `METRIC_FORMULATION` constant is rewritten to describe the new formulation verbatim; the new string then propagates word-for-word to the C-3 disclosure paragraph extension in `reconciliation_note.md`"
      pattern: "density=False|shared-edges|fake_in_range_mass|R3-CR-1|distribution-emd v2|METRIC_FORMULATION"
    - from: "results/matched2000_dualscale.json (corrected aggregates, T1 output)"
      to: "docs/reviewer_response.md R1-M1 strong-claim rewrite + Parametric-efficiency subsection (T3 output)"
      via: "Path A — the new H2 in reviewer_response.md cites the surviving OD-EMD strong-claim from `statistical-honesty-r3.md` §3a (OD-EMD: p > 0.36, |d| ≤ 0.65 — byte-stable column); the LR-EMD-vs-WGAN strong-claim from §3b (p ≤ 0.014, d ≤ -2.6) is WITHDRAWN because those Welch tests were computed on the broken pre-T1 LR-EMD column. The Path A LR-EMD cluster framing (AR 0.003 leads, quantum/WGAN/VAE cluster 0.007-0.016, no separation) cites the per-(model_kind, scale='log_return') aggregates corrected via T1's un-standardize-fake recipe. The per-baseline OD-equivalence table cites `welch_pairwise.json#pairs[?scale=='OD']`"
      pattern: "Welch|55 quantum parameters|p > 0\\.36|\\|d\\| ≤ 0\\.65|Parametric-efficiency equivalence|Path A|No statistically meaningful quantum-vs-WGAN"
    - from: "results/distribution_emd.json (corrected aggregates under schema v2, T2 output)"
      to: "docs/reconciliation_note.md 3-column comparable-variants table column 3 (T3 output)"
      via: "the existing 14-15 emit logic in `scripts/run_model_info.py` is unchanged structurally — it reads column 3 from `distribution_emd.json#aggregates[*, scale='OD']`; with the schema v2 emit, the values reflect the shared-edges-from-real formulation and the rankings re-order; optionally a 4th `fake_in_range_mass_mean` disclosure column is appended for full transparency (recommended for full disclosure of the WGAN-CNN 94%-out-of-range truncation surfaced by Agent 5's investigation)"
      pattern: "3-column comparable-variants|distribution_emd\\.json#aggregates|fake_in_range_mass_mean|schema v2"
    - from: "results/matched2000_dualscale.json (corrected, T1) + results/distribution_emd.json (corrected, T2)"
      to: "figures/cross_model_emd.{png,pdf,json} (re-rendered, T4)"
      via: "Task 6 re-renders the cross-model EMD bar figure against the corrected aggregates; the OD-EMD bar group is BYTE-IDENTICAL structurally (OD aggregates byte-stable per T1's invariant); the LR-EMD bar group is REGENERATED reflecting the corrected aggregates (rankings inverted: AR best, quantum cluster significantly beating every WGAN); CR-1 sha256-seeded determinism preserved across re-renders"
      pattern: "render_cross_model_emd|bar_values_LR|cross_model_emd\\.png|sha256_png"
    - from: "docs/reviewer_response.md (T4 output) + methods_full.md + reconciliation_note.md (T5 outputs)"
      to: "verify_number_provenance.py v2.1 gate (D-14-16 byte-frozen, T4 + T5 + T6 invocations)"
      via: "the v2.1 gate auto-walks the corrected `matched2000_dualscale.json` + `distribution_emd.json` (v2) + `welch_pairwise.json` (NEW T3) into the resolution corpus via the existing `_json_blobs()` walker without any gate edit; the corrected LR-EMD literals in `reconciliation_note.md`'s column 2 resolve to `matched2000_dualscale.json#aggregates[*, scale='log_return']`; the corrected histogram-density EMD literals in column 3 resolve to `distribution_emd.json#aggregates[*, scale='OD']` (schema v2); the Path A surviving OD-EMD strong-claim literals in `reviewer_response.md` (`p > 0.36`, `|d| ≤ 0.65`, `n=5`) resolve to `welch_pairwise.json#summaries.OD_*` and the threshold literals `0.36`, `0.65` resolve to `welch_pairwise.json#strong_claim_thresholds.{floor_welch_p_OD,ceiling_abs_cohen_d_OD}` as first-class JSON leaves; the Path A LR-EMD cluster framing literals `0.003`, `0.007`, `0.016` resolve to `matched2000_dualscale.json#aggregates[*, scale='log_return', metric_name='emd'].mean` as per-model anchors (the withdrawn LR-EMD-vs-WGAN strong-claim literals `p ≤ 0.014`, `d ≤ -2.6` are absent from the H2 — Path A retraction); T4 invokes the gate against `reviewer_response.md` only; T5 invokes the gate against `methods_full.md` + `reconciliation_note.md`; T6 invokes the gate against all 10 paper-facing docs"
      pattern: "verify_number_provenance\\.py|--target|matched2000_dualscale\\.json#aggregates|distribution_emd\\.json#aggregates|_json_blobs"
    - from: "Task 7 SUMMARY + peer_review_remediation r3 follow-up + completeness_sweep_manifest update"
      to: "audit trail from r3 forensic investigation (commit 961ee12) to Plan 14-16 remediation"
      via: "the new `## Plan 14-16 — r3 forensic remediation` section in `peer_review_remediation.md` enumerates R3-CR-1 + R3-CR-2 mechanisms, fixes applied, and resulting strong-claim framing — closing the audit loop from the 5-agent r3 investigation (`peer-review-r3/SYNTHESIS.md`, `code-review-r3.md`, `pipeline-review-r3.md`, `statistical-honesty-r3.md`, `quantum-checkpoint-r3.md`, `historical-forensic-r3.md`) to the executed remediation; the new `## Plan 14-16` section in `completeness_sweep_manifest.md` enumerates T1-T7 deliverables; existing 14-13 + 14-14 + 14-15 sections in both docs preserved verbatim"
      pattern: "Plan 14-16 — r3 forensic remediation|R3-CR-1|R3-CR-2|peer-review-r3|SYNTHESIS\\.md"
---

> **Addendum (2026-05-24):** The VAE "posterior collapse (sample std ≈ 0.0004)" characterization in this document was NOT supported by the matched-budget data. The actual matched-budget VAE log-return std is 0.0186 (≈ 1.17× narrower than real 0.0217, not 54× narrower). The VAE's anomalously low LR-DTW = 0.088 reflects a degenerate generation regime (marginal well-aligned, log-return lag-1 ACF = −0.648 vs real −0.064, matched-pipeline reference) rather than posterior collapse. See `docs/peer_review_remediation.md` for the corrected characterization. This document is preserved unchanged below as a record of the prior belief and the bug-discovery timeline.

---

<objective>
Close the two CRITICAL metric bugs surfaced by the 5-agent r3 forensic
peer-review pass (committed at `961ee12`, synthesis at
`.planning/phases/14-paper-revision-release-freeze/peer-review-r3/SYNTHESIS.md`)
via the user-selected Path 1 remediation, and update the three
reviewer-facing docs (`reviewer_response.md`, `methods_full.md`,
`reconciliation_note.md`) with the corrected numbers AND a strengthened
strong-claim framing grounded in Agent 4's statistical-honesty Welch tests.

The two bugs:

1. **R3-CR-2 (HIGH, inherited from `scripts/run_dualscale_fidelity.py` since 14-08).**
   `run_matched2000_dualscale.py:368-372` calls
   `compute_emd(real_log_delta, trans_flat)` where `real_log_delta` is RAW
   log-returns (std≈0.022) and `trans_flat = r['transformed']` is the
   STANDARDIZED log-return (std≈1, emitted by `reconstruct_od` at `:201` via
   `r_norm = ((samples_pm1 + 1.0) / 2.0) * (r_max - r_min) + r_min`).
   This 50× scale mismatch artificially inflates every LR-EMD value AND
   inverts the cross-model ranking (per Agent 2's empirical re-computation:
   AR 0.003 best, V1 0.0145, three quantum siblings ~0.015, VAE 0.0163,
   WGAN cluster significantly worse with WGAN-CNN the outlier).

2. **R3-CR-1 (CRITICAL, NEW in 14-15).**
   `run_distribution_emd.py:94-141` uses
   `np.histogram(..., density=True)` which silently renormalizes each
   histogram independently OVER THE IN-RANGE PORTION ONLY, dropping
   out-of-range fake mass and inverting rankings to reward narrow
   distributions (VAE posterior collapse, std=0.0004) and uncapped
   distributions (WGAN-CNN, 94% out-of-range).

The user decision is LOCKED to Path 1 from SYNTHESIS.md (NOT Path 2
disclose-only, NOT Path 3 ship-current): fix both bugs at the
metric-emitter level, regenerate the affected JSON aggregates, regenerate
the cross-model figures that reference them, and update the
reviewer-facing docs with the corrected numbers AND the strong-claim
framing. The R3-CR-1 fix variant is LOCKED to shared-edges-from-real-range
+ disclosed-out-of-range fake mass (NOT Option A counts-with-forfeit, NOT
Option C drop-the-column) per SYNTHESIS.md's explicit endorsement and the
comparability-preservation rationale.

The Path A strong-claim framing (user-approved, after the executor's
T1-correctness checkpoint revealed the pre-fix §3b LR-EMD strong-claim
half does not survive the R3-CR-2 fix — see `14-16-DEVIATION-NOTE.md`
for the forensic analysis):

> **55 quantum parameters achieve OD-scale EMD statistically equivalent
> to classical generators of 73-562 generator params AND the full
> ~2.5x10^5 adversarial budget (generator + 250881-parameter shared
> critic) carried by every WGAN variant (Welch p > 0.36, |d| ≤ 0.65, n=5).**
>
> **On log-return EMD, AR (3 params, closed-form Yule-Walker MLE) leads
> at 0.003; quantum/WGAN/VAE cluster in 0.007-0.016. No statistically
> meaningful quantum-vs-WGAN separation on marginal log-return distribution.**
>
> **On DTW (Dynamic Time Warping, temporal alignment), quantum dominates:
> all 4 variants beat every WGAN+AR on LR-DTW; best quantum beats Orlandi
> et al. by 6.5x on OD-DTW. This is the temporal-structure capture quantum
> is specifically designed for.**

This is a real, defensible claim that the current `reviewer_response.md`
does NOT make. The current R1-M1 row (a single table cell at
`docs/reviewer_response.md:36`) reads: "Added matched-parameter
classical WGAN-GP (MLP/CNN/LSTM critics) and a non-adversarial VAE + AR
baseline, all at matched 2000-epoch budget, identical critic/optimizer/seed
set; parameter-count-controlled comparison table". This is a generic
"we added baselines" summary — it makes NO equivalence claim. T4 PRESERVES
that row verbatim (it accurately describes what was DONE) and INSERTS a
new `## Parametric-efficiency equivalence (post-r3 corrected metrics)`
H2 section AFTER the existing Marginal-convergence section (from 14-15)
and BEFORE the Completeness sweep section, asserting the strong claim. The synthesis above says equivalence IS
demonstrated — Plan 14-16 ships that claim.

Purpose: 14-13 + 14-14 + 14-15 closed all 42 actionable peer-review
findings across r1 + r2 passes and were rated "ready for paper resubmission
YES". The user then commissioned a deeper r3 forensic pass to hunt for
quantum-disadvantaging bugs in the new emitters. The r3 pass found two
real metric bugs that are systematically disadvantaging quantum (R3-CR-1
new in 14-15, R3-CR-2 inherited since 14-08). Both are 1-task fixes;
together with the doc updates + figure re-render + SUMMARY, this is a
7-task plan. After 14-16, only 14-07 (Zenodo deposit + tag + DOI wiring
+ release.md, deferred manual gate) remains in Phase 14.

Output: 1 surgical edit to `run_matched2000_dualscale.py` +
re-emit of `matched2000_dualscale.json` with corrected LR-EMD column +
OD-EMD column BYTE-IDENTICAL (T1) + 1 surgical edit to
`scripts/run_distribution_emd.py` + re-emit of `distribution_emd.json`
under schema v2 with `fake_in_range_mass` disclosure stat (T2) +
re-emitted `reconciliation_note.md` / `reviewer_response.md` /
`methods_full.md` with corrected numbers + strong-claim framing (T3) +
re-rendered `cross_model_emd.{png,pdf,json}` against corrected aggregates
+ `qq_overlay.{png,pdf,json}` cross-checked (default: no structural
change) + v2.1 gate run on all 10 paper-facing docs (T4) +
`14-16-SUMMARY.md` + new `## Plan 14-16 — r3 forensic remediation` section
in `peer_review_remediation.md` + new `## Plan 14-16` section in
`completeness_sweep_manifest.md` (T5). All gated by the v2.1 provenance
gate (D-14-16 byte-frozen). `core/` byte-frozen (D-14-22). No
gate edit (D-14-16). No LaTeX edit (D-14-18). No retraining.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-paper-revision-release-freeze/14-CONTEXT.md
@.planning/phases/14-paper-revision-release-freeze/14-PATTERNS.md
@.planning/phases/14-paper-revision-release-freeze/14-13-PLAN.md
@.planning/phases/14-paper-revision-release-freeze/14-13-SUMMARY.md
@.planning/phases/14-paper-revision-release-freeze/14-14-PLAN.md
@.planning/phases/14-paper-revision-release-freeze/14-14-SUMMARY.md
@.planning/phases/14-paper-revision-release-freeze/14-15-PLAN.md
@.planning/phases/14-paper-revision-release-freeze/14-15-SUMMARY.md
@.planning/phases/14-paper-revision-release-freeze/peer-review-r3/SYNTHESIS.md
@.planning/phases/14-paper-revision-release-freeze/peer-review-r3/code-review-r3.md
@.planning/phases/14-paper-revision-release-freeze/peer-review-r3/pipeline-review-r3.md
@.planning/phases/14-paper-revision-release-freeze/peer-review-r3/statistical-honesty-r3.md

<contract_changes>
Explicit deviations from prior Phase 14 contracts (mirrors the contract
changes sections in 14-13 / 14-14 / 14-15 but narrower — 14-16 is a pure
forensic-remediation plan: two surgical metric-emitter fixes + doc updates
+ figure re-render + SUMMARY):

- **D-14-22 (`core/` byte-freeze) PRESERVED across all 7 tasks.**
  The R3-CR-2 fix is a surgical edit to `run_matched2000_dualscale.py`
  (TOP-LEVEL emitter, NOT in core). The R3-CR-1 fix is a surgical edit to
  `scripts/run_distribution_emd.py` (TOP-LEVEL emitter, NOT in core). The
  core `compute_emd` at `core/eval.py:25-36` (the v1.0 raw-sample
  Wasserstein) is byte-untouched. Every task's verify gate asserts
  `[ -z "$(git diff --stat core/)" ]`.

- **D-14-13 (strict-accept gate, device-manifest equality semantics)
  PRESERVED.** No edit to `_strict_accept` semantics; the corrected metric
  values flow through the existing top-level emitter contracts.

- **D-14-16 (`scripts/verify_number_provenance.py` v2.1 byte-freeze)
  PRESERVED.** No edit to the gate script. The corrected JSON values flow
  through the existing `_json_blobs()` walker (from 14-13 T2, lookbehind
  fixed in 14-14 T1) automatically.

- **D-14-18 (Overleaf-canonical LaTeX read-only) PRESERVED.** No edits to
  `main (4) copy.tex` / `supp_material.tex`. Reviewer-facing corrected
  numbers + strong-claim framing live in `docs/`.

- **R3-CR-1 FIX variant LOCKED to shared-edges-from-real + disclosed
  out-of-range fake mass.** Per SYNTHESIS.md's endorsement and the
  comparability-preservation rationale (Option A counts-with-forfeit and
  Option C drop-the-column are EXPLICITLY REJECTED). The fix preserves
  cross-pre-v1.0 comparability that was the whole point of 14-15
  introducing this column; addresses the bias at root cause; the
  out-of-range disclosure stat surfaces the WGAN-CNN 94% truncation that
  was previously silently dropped.

- **R3-CR-2 FIX preserves OD-scale BYTE-IDENTICAL.** The OD inverse via
  `inverse_logreturns` (`core/preprocessing.py:49-72`) already
  cumsum-exps to OD price scale where no standardization-vs-raw mismatch
  exists. T1 verify gate asserts the OD subset of `matched2000_dualscale.json`
  is byte-identical pre/post via SHA-256.

- **NO retraining.** All metrics read existing per-seed samples from
  `results/matched2000/runs/{model}/{seed}/samples.npy`.

- **NO new figures beyond re-rendering `cross_model_emd`.** The QQ overlay
  from 14-15 is structurally unchanged by default (rendered from samples
  directly, not from JSON aggregates) — only a `plan_14_16_verification`
  additive field is appended to its companion JSON.

- **Strong-claim framing strengthened.** The current R1-M1 row in
  `reviewer_response.md` (a single markdown-table cell at `:36` reading
  "Added matched-parameter classical WGAN-GP (MLP/CNN/LSTM critics) and a
  non-adversarial VAE + AR baseline, all at matched 2000-epoch budget,
  identical critic/optimizer/seed set; parameter-count-controlled comparison
  table") makes NO equivalence claim and accurately describes WHAT WAS
  DONE. T4 PRESERVES that row verbatim and INSERTS a new H2 section
  `## Parametric-efficiency equivalence (post-r3 corrected metrics)`
  between the existing `## Marginal-convergence finding (post-r2
  investigation)` H2 and the existing `## Completeness sweep (Plans 14-09
  .. 14-11)` H2 — asserting the explicit Welch-grounded equivalence claim.
  The new H2 enumerates the 55-quantum-param equivalence to 73-562
  generator parameters (per results/model_info.json: wgan_mlp=74,
  wgan_cnn=73, wgan_lstm=78, vae=562) AND ~2.5x10^5 adversarial budget
  (per results/total_adversarial_param_budget.json: each
  generator + the shared 250881-parameter critic, e.g., iqp_sel_55 total
  = 250936).
</contract_changes>

<interfaces>
<!-- All upstream artifacts (Phases 9-14 through 14-15) are EXISTING and
     AUDITED on disk; this plan only edits 2 emitters + re-emits affected
     JSONs + updates 3 reviewer-facing docs + re-renders 1 figure (with
     conditional QQ-overlay cross-check) + appends 1 section to
     peer_review_remediation.md + 1 section to completeness_sweep_manifest.md
     + 1 SUMMARY. -->

run_matched2000_dualscale.py (READ-FIRST + EDIT in T1):
- Lines 175-217 (`reconstruct_od` — REFERENCE ONLY; builds `r_norm` via
  `((samples_pm1 + 1.0) / 2.0) * (r_max - r_min) + r_min` using
  `mu, sigma` from per-seed `inverse_kwargs.npz` — the fake side is
  ALREADY in the standardized log-return space).
- Lines 283-301 (`build_real_references` — REFERENCE ONLY; currently
  returns `real_log_delta = d_real["log_delta"]` raw; T1 may extend this
  to also build `norm_log_delta` OR T1 may build it inline in
  `_log_return_rows`).
- Lines 355-400 (`_log_return_rows` — EDIT SITE; the `compute_emd` call
  at `:368-372` is the R3-CR-2 fix point).
- Lines 468-490 (`emit_rows` — REFERENCE ONLY; calls `_log_return_rows`).

run_distribution_emd.py (READ-FIRST + EDIT in T2):
- Line 80 (`HEADLINE_MODEL_KIND` — REFERENCE ONLY).
- Line 83 (`SCHEMA = "distribution-emd v1 (Phase 14 plan 14-15)"` — EDIT:
  bump to `"distribution-emd v2 (Phase 14 plan 14-16)"`).
- Lines 87-91 (`METRIC_FORMULATION` — EDIT: rewrite to describe the new
  shared-edges + disclosed-out-of-range formulation verbatim).
- Lines 94-141 (`compute_histogram_density_emd` — PRIMARY EDIT SITE for
  R3-CR-1 fix; replace function body; return 2-tuple
  `(emd, fake_in_range_mass)`).
- Lines 172-243 (`_model_seed_rows` — EDIT: update calls to new
  2-tuple-returning function; append `fake_in_range_mass` to each row).
- Lines 144-169 (`_real_references` + `_fake_log_return_flat` — REFERENCE
  ONLY; no edit needed — the scale mismatch flagged in `code-review-r3.md`
  R3-HI-1 also affects this file but the user-locked Path 1 brief addresses
  it via the same `norm_log_delta` substitution in T1's edit to the
  dualscale driver only; the distribution_emd.py log-return-scale rows
  are NOT in the locked-decision scope of this plan and are left for
  follow-up; record this scope decision in T2's commit body).

core/eval.py:25-36 (READ-ONLY contrast; D-14-22 byte-untouched):
- `compute_emd(real, fake)` — the v1.0 raw-sample Wasserstein. T1 calls
  this with corrected (standardized) inputs; no edit to the function
  itself.

core/preprocessing.py:49-72 (READ-ONLY reference; D-14-22
byte-untouched):
- `inverse_logreturns(r_norm, od_start, mu, sigma)` — used by
  `reconstruct_od` to map standardized log-returns to OD scale; the OD
  scale is correct because cumsum-exp implicitly unscales; this is the
  reason T1's fix preserves OD aggregates byte-identical.
- `forward_logreturns` at `:29-46` — REFERENCE; returns
  `(r_norm, mu, sigma)` with `(r - mu) / sigma` standardization (`mu =
  torch.mean(r)`, `sigma = torch.std(r)` with default `ddof=1`); this is
  the standardization recipe T1 mirrors when computing `norm_log_delta`
  from `real_log_delta`.

results/matched2000/runs/{model}/{seed}/inverse_kwargs.npz
(READ-ONLY per-seed data source):
- Contains `r_min, r_max, mu, sigma, od_starts` (per the existing
  `reconstruct_od` consumption at `run_matched2000_dualscale.py:198-200`);
  T1 reads `mu, sigma` from this file to build the standardized
  `norm_log_delta` reference. These are PER-SEED arrays but `mu, sigma`
  are global across seeds (computed once from the full real series) —
  verify this assumption in T1's read_first by inspecting 2-3 seed dirs.

results/matched2000/runs/{model}/{seed}/samples.npy (READ-ONLY
per-seed data source):
- The per-seed sample arrays consumed by both emitters; UNTOUCHED by 14-16.

run_model_info.py (READ-FIRST + EDIT in T3):
- The `reconciliation_note.md` + (possibly) `reviewer_response.md` emit
  site. Locate the existing 3-column comparable-variants table emit logic
  added in 14-15 T2 (around lines 220-302); the column-3 source path
  (`distribution_emd.json#aggregates[*, scale='OD']`) is unchanged
  structurally — just reads updated JSON values. Optionally append a 4th
  `fake_in_range_mass_mean` disclosure column for full transparency.
- Locate the R1-M1 response emit path in `reviewer_response.md` — this
  may live in `scripts/run_model_info.py` or in a separate emitter (verify via
  grep in T3 read_first); rewrite the R1-M1 body and append the
  `## Parametric-efficiency equivalence (post-r3 corrected metrics)`
  subsection.

docs/reviewer_response.md (READ-FIRST + EDIT in T4):
- Locate the existing R1-M1 row in the `## Reviewer 1 — Major Issues`
  table (at line 36 of the file in the pre-14-16 state — verify final
  post-14-15 line in T4 read_first). The row is a single markdown-table
  cell reading "Added matched-parameter classical WGAN-GP (MLP/CNN/LSTM
  critics) and a non-adversarial VAE + AR baseline, all at matched
  2000-epoch budget, identical critic/optimizer/seed set;
  parameter-count-controlled comparison table". This text accurately
  describes WHAT WAS DONE at the matched-budget step and contains NO
  equivalence claim. PRESERVE the row verbatim.
- The new `## Parametric-efficiency equivalence (post-r3 corrected
  metrics)` H2 section is INSERTED between the existing
  `## Marginal-convergence finding (post-r2 investigation)` H2 (at line
  208 in the pre-14-16 state) and the existing `## Completeness sweep
  (Plans 14-09 .. 14-11)` H2 (originally at line 85 — verify final
  post-14-15 location in T4 read_first since 14-13/14/15 sections were
  appended). The new H2 carries the strong-claim assertion + the
  per-baseline equivalence table + Welch/Cohen d citations resolved
  against `results/welch_pairwise.json` (produced by T3).

docs/methods_full.md (READ-FIRST + EDIT in T3):
- Locate the OD-scale evaluation section (the section discussing
  per-model EMD results — likely around §3.x.d carried over from 14-14
  T3 and extended in 14-15 T4). Append a new 'Log-return scale correction
  (Plan 14-16)' paragraph at its end. Locate the histogram-density EMD
  section (from 14-15). Append a new 'Shared-edges formulation (Plan
  14-16)' paragraph at its end.

docs/reconciliation_note.md (READ-FIRST + EDIT in T3):
- The existing OD-scale headline table from 14-13 (preserved verbatim).
  The 3-column comparable-variants table from 14-15 (preserved
  structurally; columns 2+3 values regenerate from corrected JSONs).
  The C-3 disclosure paragraph (from 14-13 T3, extended in 14-15 T2)
  gains one more disclosure sentence per the brief.

run_figure_suite.py (READ-FIRST in T4; EDIT only if QQ overlay
cross-check requires it):
- Locate `render_cross_model_emd` (or equivalent — verify name via grep)
  to confirm the figure rendering path reads `matched2000_dualscale.json`
  aggregates. T4 invokes the existing render path to regenerate
  `cross_model_emd.{png,pdf,json}` against corrected aggregates.
- Locate `render_qq_overlay` (from 14-15 T3) and trace its data sources
  to confirm it reads from `samples.npy` directly (NOT from JSON
  aggregates); if confirmed, the overlay is structurally unchanged and
  T4 only appends a `plan_14_16_verification` field to the companion
  JSON; if any dependence on `matched2000_dualscale.json` is found, the
  overlay is RE-RENDERED.

figures/cross_model_emd.{png,pdf,json} (RE-RENDERED in T4):
- Existing figure from earlier phases (14-10 or similar; verify path on
  disk). The OD bar group is byte-identical structurally per T1's
  invariant; the LR bar group regenerates from corrected aggregates.

figures/qq_overlay.{png,pdf,json} (CONDITIONALLY
TOUCHED in T4):
- 14-15 T3 output. Default outcome: structurally unchanged; only
  `qq_overlay.json` gains additive `plan_14_16_verification` field.

verify_number_provenance.py (READ-ONLY; D-14-16 byte-frozen):
- The v2.1 gate (from 14-14 T1). T4 invokes it against all 10
  paper-facing docs (same 10 as 14-14 T5 / 14-15 T5). No edit.

docs/peer_review_remediation.md (READ-FIRST + EDIT in T5):
- Existing 14-13 finding-to-commit tables + 14-14 `## Gate v2.1 known
  limitations` + 14-14 `## R2 follow-up sweep` + 14-15 `## R2 follow-up —
  marginal-convergence finding (Plan 14-15)` sections (all preserved
  verbatim). T5 appends new `## Plan 14-16 — r3 forensic remediation`
  section at the bottom.

docs/completeness_sweep_manifest.md (READ-FIRST + EDIT in T5):
- Existing 14-09 / 14-10 / 14-11 / 14-12 / 14-13 / 14-14 / 14-15 sections
  (all preserved verbatim). T5 appends new `## Plan 14-16` section at
  the bottom.

Critical files to read before each task (per the brief):

- **T1:** `run_matched2000_dualscale.py:175-217` (`reconstruct_od`
  to confirm the fake-side `r_norm` is already standardized) +
  `:283-301` (`build_real_references`) + `:355-400` (`_log_return_rows`
  edit site) + `:468-490` (`emit_rows`); `core/preprocessing.py:29-46`
  (the `forward_logreturns` standardization recipe T1 mirrors);
  `results/matched2000/runs/iqp_sel_55_repro/42/inverse_kwargs.npz`
  + one other seed dir for cross-seed mu/sigma consistency verification;
  `.planning/phases/14-paper-revision-release-freeze/peer-review-r3/pipeline-review-r3.md`
  §2 (the empirical re-computation anchors for verification).
- **T2:** `run_distribution_emd.py:80-141` (the `SCHEMA`,
  `METRIC_FORMULATION`, and `compute_histogram_density_emd` edit sites);
  `:172-243` (`_model_seed_rows` — call-site updates);
  `.planning/phases/14-paper-revision-release-freeze/peer-review-r3/code-review-r3.md`
  §H3 (the R3-CR-1 mechanism + empirical demonstration);
  `.planning/phases/14-paper-revision-release-freeze/peer-review-r3/SYNTHESIS.md`
  (the locked variant selection rationale).
- **T3:** `run_model_info.py:220-302` (the existing 3-column
  emit logic from 14-15); `docs/reviewer_response.md` (locate
  R1-M1 response; locate natural insertion point for new subsection);
  `docs/methods_full.md` (locate OD-scale evaluation section
  + histogram-density section); `docs/reconciliation_note.md`
  (verify C-3 disclosure paragraph location);
  `.planning/phases/14-paper-revision-release-freeze/peer-review-r3/statistical-honesty-r3.md`
  §3a + §3b + §7-Option A (the strong-claim framing source).
- **T4:** `scripts/run_figure_suite.py` (locate `render_cross_model_emd`
  and `render_qq_overlay` data sources via grep); `figures/cross_model_emd.json`
  (current state to confirm format before re-render);
  `figures/qq_overlay.json` (current state to confirm
  the 14-15 fields preserve verbatim under default no-change outcome);
  `scripts/verify_number_provenance.py` (READ-ONLY; the gate to invoke
  against all 10 docs).
- **T7:** All 10 paper-facing docs (final state post-T1-T6);
  `docs/peer_review_remediation.md` (existing 14-13/14/15
  sections to preserve); `docs/completeness_sweep_manifest.md`
  (existing 14-09 through 14-15 sections to preserve);
  `$HOME/.claude/get-shit-done/templates/summary.md` (the canonical
  SUMMARY template).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: R3-CR-2 fix — log-return EMD scale-mismatch correction in `run_matched2000_dualscale.py`</name>
  <files>run_matched2000_dualscale.py, results/matched2000_dualscale.json</files>
  <read_first>
    - run_matched2000_dualscale.py:175-217 (`reconstruct_od` — confirm the fake-side `r_norm = ((samples_pm1 + 1.0) / 2.0) * (r_max - r_min) + r_min` is in the standardized log-return space with std≈1; the per-seed `r_min, r_max, mu, sigma` come from `inverse_kwargs.npz`)
    - run_matched2000_dualscale.py:283-301 (`build_real_references` — currently returns raw `real_log_delta = d_real["log_delta"].cpu().numpy()`; T1 extends this to also build the standardized `norm_log_delta` OR T1 inlines the standardization inside `_log_return_rows` — choose based on which is the smaller diff)
    - run_matched2000_dualscale.py:355-400 (`_log_return_rows` — the primary edit site; the broken call at `:368-372` is `compute_emd(real_log_delta, trans_flat)` — must become `compute_emd(norm_log_delta, trans_flat)` where `norm_log_delta = (real_log_delta - mu) / sigma`)
    - run_matched2000_dualscale.py:468-490 (`emit_rows` — the orchestrator that calls `_log_return_rows` for each (model_kind, pipeline='B', seed); if T1 adds `norm_log_delta` as a parameter to `_log_return_rows`, `emit_rows` is updated to pass it through)
    - core/preprocessing.py:29-46 (`forward_logreturns` — REFERENCE; returns `(r_norm, mu, sigma)` with `mu = torch.mean(r)`, `sigma = torch.std(r)` default `ddof=1`; this is the standardization recipe T1 mirrors when building `norm_log_delta` from `real_log_delta`)
    - results/matched2000/runs/iqp_sel_55_repro/42/inverse_kwargs.npz + one other seed dir (e.g., results/matched2000/runs/wgan_mlp/43/inverse_kwargs.npz) — load both via `np.load(...)` and confirm `mu` and `sigma` are SCALAR (single-element arrays / 0-d arrays) and IDENTICAL across all seed dirs (they are global standardization constants from the full real series, computed once); the verification approach is: `mu_42 = float(np.load(.../42/inverse_kwargs.npz)['mu']); mu_43 = float(np.load(.../43/inverse_kwargs.npz)['mu']); assert mu_42 == mu_43` — if any mismatch, T1's standardization recipe must use per-seed values (unlikely but verify)
    - .planning/phases/14-paper-revision-release-freeze/peer-review-r3/pipeline-review-r3.md (the empirical re-computation anchors for verification: §2 table at lines 117-141 shows the corrected values when synth is un-standardized vs real on raw log-delta scale; §5 Hypothesis 2 at lines 217-235 enumerates the rank inversion; T1 verifies the new aggregates match these anchors directionally)
    - .planning/phases/14-paper-revision-release-freeze/peer-review-r3/code-review-r3.md (R3-HI-1 section at H3, lines 262-339; the fix recipe at lines 322-330: 'change `real_log_delta` to `norm_log_delta` in the real reference so both sides are on the standardized scale; single-line edit')
    - .planning/phases/14-paper-revision-release-freeze/peer-review-r3/SYNTHESIS.md (the Path 1 endorsement and the bug mechanism enumerated at lines 64-90; the corrected anchor values at lines 76-89)
  </read_first>
  <action>
    Surgical edit to `run_matched2000_dualscale.py` to fix the
    log-return EMD scale mismatch at `_log_return_rows:368-372`. Re-emit
    `results/matched2000_dualscale.json` with corrected
    log-return-scale EMD aggregates while preserving the OD-scale subset
    BYTE-IDENTICAL.

    Step A — Verify cross-seed mu/sigma consistency:

    Before editing the emitter, verify the standardization constants are
    global (not per-seed). Load `mu`/`sigma` from 2-3 representative
    `inverse_kwargs.npz` files (e.g., `iqp_sel_55_repro/42`, `wgan_mlp/43`,
    `vae/45`) and confirm equality to numerical precision. If any
    seed-dependence is found, T1 builds `norm_log_delta` per seed inside
    `_log_return_rows` using each seed's own (mu, sigma); if consistent
    (expected), T1 may build `norm_log_delta` ONCE in `build_real_references`
    and pass it through.

    Step B — Edit `_log_return_rows` (around `:355-400`) using the un-standardize-fake recipe:

    Per `pipeline-review-r3.md` §2 lines 124-135, the canonical scale-fix
    recipe is to UN-STANDARDIZE the FAKE side (NOT standardize the real
    side). The standardize-real alternative is mathematically scale-matched
    but inflates EMD by ~47× and produces values that do NOT match the §2
    anchor table; un-standardize-fake produces EMD in raw log-return units
    that match §2 exactly (AR 0.00294, V1 0.01497, VAE 0.01583).

    Build `trans_flat_raw = r_norm.reshape(-1) * sigma + mu` where
    `sigma, mu` come from the per-seed `inverse_kwargs.npz` (the SAME
    sigma/mu used by `reconstruct_od` at `:198-199` to build `r_norm` on
    the fake side; Step A verified these are global cross-seed constants).
    Then compare against the unchanged raw `real_log_delta`.

    Replace the broken EMD call at `:370-373`:

    BEFORE (current):
    ```
    out.append(dict(model_kind=mk, pipeline=p, seed=s, metric_name="emd",
                     scale="log_return",
                     value=compute_emd(real_log_delta, trans_flat),
                     source=source))
    ```

    AFTER (corrected via un-standardize-fake recipe):
    ```
    trans_flat_raw = r["transformed"].reshape(-1) * sigma + mu
    out.append(dict(model_kind=mk, pipeline=p, seed=s, metric_name="emd",
                     scale="log_return",
                     value=compute_emd(real_log_delta, trans_flat_raw),
                     source=source))
    ```

    Leave the moment / ACF / DTW rows on the log-return scale UNTOUCHED —
    they internally compare same-scale arrays (moments are computed on
    `trans_flat` alone; ACF is computed on `trans` alone; DTW uses
    `real_lr_windows` from `real_log_delta` reshaped — DTW scale-mismatch
    is acknowledged as a known limitation but is OUTSIDE the locked R3-CR-2
    scope per the brief; record this scope decision in the docstring
    extension below).

    Step C — Extend the function docstring with the R3-CR-2 remediation
    note:

    Append to the existing `_log_return_rows` docstring (the existing
    docstring states `'Body copied VERBATIM from run_dualscale_fidelity.
    _log_return_rows Pipeline-B branch'` — keep that verbatim and append):

    > Plan 14-16 r3 remediation (Path A): the log-return EMD's FAKE side
    > is now un-standardized via `trans_flat_raw = r["transformed"].reshape(-1)
    > * sigma + mu` before comparison against the unchanged raw
    > `real_log_delta` (both sides now in raw log-return units, matching
    > `pipeline-review-r3.md` §2 anchor table: AR 0.00294, V1 0.01497, VAE
    > 0.01583). The standardize-real alternative inflates EMD by ~47× and
    > does NOT match the §2 anchors; the un-standardize-fake recipe per
    > §2 lines 124-135 is canonical. The OD-scale rows are byte-untouched.
    > The moment / ACF / DTW rows are also byte-untouched (the scale
    > mismatch was specific to the EMD call). See R3-CR-2 mechanism in
    > `peer-review-r3/code-review-r3.md` §H3 and the corrected aggregates
    > anchored against `pipeline-review-r3.md` §2.

    Step D — Update `build_real_references` (around `:283-301`) if needed:

    If T1 chose to build `norm_log_delta` once in `build_real_references`
    (Step A path), extend the function to return it alongside the existing
    fields. The per-seed (mu, sigma) come from `inverse_kwargs.npz` loaded
    once per the global-mu/sigma assumption verified in Step A. The
    function signature change is additive (extra return field; do NOT
    remove `real_log_delta` because other call sites in the file may use
    it for the DTW/moments/ACF rows that are NOT being corrected).

    If T1 chose to inline (rare per Step A expectation), `build_real_references`
    is untouched.

    Step E — Update `emit_rows` (around `:468-490`) to pass `norm_log_delta`
    through to `_log_return_rows`:

    Locate the existing call `_log_return_rows(mk, "B", s, r, real_log_delta,
    source)` at `:490` and update it to pass `norm_log_delta` (either as an
    additional parameter or as the new real-reference; the exact signature
    update depends on T1's Step B choice).

    Step F — Re-emit `results/matched2000_dualscale.json`:

    ```
    ./qgan_env/bin/python scripts/run_matched2000_dualscale.py
    ```

    Step G — Verify OD-scale BYTE-IDENTICAL invariant:

    Compute SHA-256 of the OD-scale subset of the JSON pre- and post-edit:

    ```
    git show HEAD:results/matched2000_dualscale.json | \
      ./qgan_env/bin/python -c "import json,sys,hashlib; d=json.load(sys.stdin); od=sorted([r for r in d.get('rows',[]) if r.get('scale')=='OD'], key=lambda x: (x['model_kind'], x['pipeline'], x['seed'], x['metric_name'])); print(hashlib.sha256(json.dumps(od, sort_keys=True).encode()).hexdigest())" > /tmp/od_before.txt

    cat results/matched2000_dualscale.json | \
      ./qgan_env/bin/python -c "import json,sys,hashlib; d=json.load(sys.stdin); od=sorted([r for r in d.get('rows',[]) if r.get('scale')=='OD'], key=lambda x: (x['model_kind'], x['pipeline'], x['seed'], x['metric_name'])); print(hashlib.sha256(json.dumps(od, sort_keys=True).encode()).hexdigest())" > /tmp/od_after.txt

    diff /tmp/od_before.txt /tmp/od_after.txt
    ```

    The SHA-256 hashes MUST be identical. If they differ, T1 has
    inadvertently affected the OD-scale subset and must be reverted +
    re-attempted. Record both hashes in the T1 commit body for audit.

    Step H — Verify corrected LR-EMD aggregates match `pipeline-review-r3.md`
    §2 anchor table EXACTLY (un-standardize-fake recipe):

    Load the new aggregates and verify against the §2 anchor table:
    - AR LR-EMD aggregate mean in [0.002, 0.004] (anchor: 0.00294 — best
      overall, closed-form Yule-Walker fits the linear-Gaussian marginal)
    - V1 LR-EMD aggregate mean in [0.014, 0.015] (anchor: 0.01497)
    - iqp_sel_55_repro LR-EMD aggregate mean in [0.014, 0.015] (anchor: 0.01497)
    - V2 LR-EMD aggregate mean in [0.014, 0.016] (anchor: 0.01502)
    - V3 LR-EMD aggregate mean in [0.013, 0.015] (anchor: 0.01432)
    - VAE LR-EMD aggregate mean in [0.015, 0.017] (anchor: 0.01583 worst)
    - wgan_cnn LR-EMD aggregate mean in [0.005, 0.009] (anchor: 0.00711)
    - wgan_mlp LR-EMD aggregate mean in [0.009, 0.012] (anchor: 0.01031)
    - wgan_lstm LR-EMD aggregate mean in [0.012, 0.014] (anchor: 0.01272)

    Path A note: EVERY WGAN beats every quantum on corrected LR-EMD. This
    inverts the pre-fix narrative. The "quantum < WGAN on LR-EMD" assertion
    from the original plan was based on the broken (scale-mismatched)
    pre-T1 column. Do NOT assert "all 4 quantum variants beat every WGAN"
    on LR-EMD — the corrected aggregates show the opposite. The OD-EMD
    equivalence claim (p > 0.36, |d| ≤ 0.65, n=5) survives Path A
    because OD aggregates are byte-stable pre/post T1.

    Step I — Commit:

    `feat(14-16): R3-CR-2 fix (Path A) — un-standardize-fake LR-EMD per pipeline-review-r3.md §2`

    Commit body enumerates the SHA-256 hashes from Step G (OD-byte-identical
    proof), the §2-anchor-exact verification from Step H (AR 0.00294, V1
    0.01497, VAE 0.01583), notes that Path A withdraws the LR-EMD-vs-WGAN
    strong-claim half (the pre-fix `statistical-honesty-r3.md` §3b Welch
    tests were computed on the broken column), and references both
    `peer-review-r3/code-review-r3.md` §H3 (mechanism) and `pipeline-review-r3.md`
    §2 (canonical un-standardize-fake recipe).
  </action>
  <verify>
    <automated>./qgan_env/bin/python scripts/run_matched2000_dualscale.py &amp;&amp; ./qgan_env/bin/python -c "import json,pathlib,sys; j=json.loads(pathlib.Path('results/matched2000_dualscale.json').read_text()); aggs=j.get('aggregates',[]); errs=[]; lr_emd_aggs={a.get('model_kind'): a.get('mean') for a in aggs if a.get('scale')=='log_return' and a.get('metric_name')=='emd'}; print('LR-EMD aggregates:', lr_emd_aggs); anchors={'ar':(0.002,0.004),'V1':(0.014,0.015),'iqp_sel_55_repro':(0.014,0.015),'V2':(0.014,0.016),'V3':(0.013,0.015),'vae':(0.015,0.017),'wgan_cnn':(0.005,0.009),'wgan_mlp':(0.009,0.012),'wgan_lstm':(0.012,0.014)}; [errs.append(f'{m} LR-EMD {lr_emd_aggs.get(m)} not in anchor range {lo}-{hi}') for m,(lo,hi) in anchors.items() if lr_emd_aggs.get(m) is None or not (lo <= lr_emd_aggs[m] <= hi)]; print('errors',errs); sys.exit(0 if not errs else 1)" &amp;&amp; [ -z "$(git diff --stat core/)" ] &amp;&amp; ./qgan_env/bin/python -c "import json,pathlib,hashlib,subprocess,sys; current=json.loads(pathlib.Path('results/matched2000_dualscale.json').read_text()); od_current=sorted([r for r in current.get('rows',[]) if r.get('scale')=='OD'], key=lambda x: (x.get('model_kind',''), x.get('pipeline',''), str(x.get('seed','')), x.get('metric_name',''))); h_current=hashlib.sha256(json.dumps(od_current, sort_keys=True).encode()).hexdigest(); prior_text=subprocess.run(['git','show','HEAD:results/matched2000_dualscale.json'], capture_output=True, text=True).stdout; prior=json.loads(prior_text); od_prior=sorted([r for r in prior.get('rows',[]) if r.get('scale')=='OD'], key=lambda x: (x.get('model_kind',''), x.get('pipeline',''), str(x.get('seed','')), x.get('metric_name',''))); h_prior=hashlib.sha256(json.dumps(od_prior, sort_keys=True).encode()).hexdigest(); print(f'OD prior {h_prior}'); print(f'OD current {h_current}'); assert h_prior == h_current, f'OD-byte-identical invariant broken: {h_prior} != {h_current}'; print('OD-byte-identical invariant OK')" &amp;&amp; ./qgan_env/bin/python scripts/verify_number_provenance.py --target docs/reconciliation_note.md &amp;&amp; [ -z "$(git diff verify_number_provenance.py)" ] &amp;&amp; echo 'W4 preflight: reconciliation_note.md OD-EMD citations still resolve post-T1; D-14-16 byte-freeze preserved'</automated>
  </verify>
  <acceptance_criteria>
    - `run_matched2000_dualscale.py` `_log_return_rows` function at `:355-400` updated to use standardized `norm_log_delta` reference (instead of raw `real_log_delta`) for the EMD call only; moment/ACF/DTW rows untouched
    - Function docstring extended with the Plan 14-16 r3 remediation note referencing `peer-review-r3/code-review-r3.md` §H3
    - `results/matched2000_dualscale.json` re-emitted with corrected log-return-scale EMD values; OD-scale rows BYTE-IDENTICAL (SHA-256 over OD subset matches prior emit verbatim)
    - Corrected LR-EMD aggregates match `pipeline-review-r3.md` §2 anchor table EXACTLY (un-standardize-fake recipe): AR 0.00294 best overall, wgan_cnn 0.00711, wgan_mlp 0.01031, wgan_lstm 0.01272, V3 0.01432, iqp_sel_55_repro / V1 0.01497, V2 0.01502, VAE 0.01583 worst (per-model tolerance ±0.001 against anchor values)
    - Path A: NO assertion of "quantum beats WGAN on LR-EMD" — corrected aggregates show every WGAN beats every quantum on LR-EMD; the LR-EMD-vs-WGAN strong-claim half from pre-fix `statistical-honesty-r3.md` §3b is WITHDRAWN. OD-EMD equivalence claim (p > 0.36, |d| ≤ 0.65, n=5, byte-stable OD column) survives unchanged.
    - `git diff --stat core/` is empty (D-14-22 preserved); `git diff verify_number_provenance.py` is empty (D-14-16 preserved — defensive assertion; T1 does not modify the gate file)
    - **W4 preflight:** `verify_number_provenance.py --target docs/reconciliation_note.md` PASSES against the post-T1 corrected `matched2000_dualscale.json` — confirming the OD-EMD citations in the existing 14-13 OD headline table (and any 14-15 3-column-table OD column 1 cells) still resolve under the v2.1 gate after the JSON re-emit. The OD subset should be byte-identical so this is a defensive assertion; if it fails, T1 has a hidden coupling not declared in `depends_on` and must be reverted. LR-EMD citations in the same doc are EXPECTED to fail until T5 lands (the inter-task incoherence cost of splitting T1 from the doc-update tasks)
  </acceptance_criteria>
  <done>R3-CR-2 fix landed (Path A, un-standardize-fake per pipeline-review-r3.md §2): `run_matched2000_dualscale.py:368-372` corrected to compare raw real-log-return vs un-standardized fake (`trans_flat_raw = r["transformed"] * sigma + mu`); `matched2000_dualscale.json` re-emitted with corrected LR-EMD aggregates exactly matching §2 anchor table (AR 0.00294, V1 0.01497, VAE 0.01583) AND byte-identical OD aggregates; D-14-22 preserved.</done>
</task>

<task type="auto">
  <name>Task 2: R3-CR-1 fix — shared-edges-from-real + disclosed-out-of-range histogram-density EMD in `scripts/run_distribution_emd.py`</name>
  <files>run_distribution_emd.py, results/distribution_emd.json</files>
  <read_first>
    - run_distribution_emd.py:80-141 (the `SCHEMA`, `HEADLINE_MODEL_KIND`, `METRIC_FORMULATION`, and `compute_histogram_density_emd` edit sites; the function body currently uses `np.histogram(real, bins=n_bins, density=True); np.histogram(fake, bins=edges, density=True); wasserstein_distance(bin_centers, bin_centers, real_hist, fake_hist)` — this is the R3-CR-1 broken implementation)
    - run_distribution_emd.py:172-243 (`_model_seed_rows` — the per-(model, seed, scale) row builder; calls `compute_histogram_density_emd` twice per (model, seed); T2 updates these to consume the new 2-tuple return value)
    - run_distribution_emd.py:144-169 (`_real_references` at `:144-153` returning `real_log_delta` RAW + `_fake_log_return_flat` at `:156-169` returning `r_norm` STANDARDIZED — the SAME scale-mismatch pattern as R3-CR-2 in the dualscale driver, called out under the SAME finding ID R3-HI-1 per `code-review-r3.md` §H3 lines 262-339 + Agent 5's bundled hot-fix recommendation at lines 320-330 / 823-829; T2 Step E2 applies the analogous standardization fix here so the LR-scale rows in `distribution_emd.json` v2 are no longer inherited-broken — they consume `norm_log_delta` against the standardized fake side. EDIT SITE, NOT REFERENCE ONLY)
    - .planning/phases/14-paper-revision-release-freeze/peer-review-r3/code-review-r3.md (R3-CR-1 section at H3, lines 142-261; the mechanism, empirical demonstration with synthetic data lines 179-189, ranking-inversion table lines 195-215, recommended fix Option A vs B vs C lines 241-260 — note that the user has LOCKED to a variant that combines elements of Option A and Option B: shared edges from real, count-based normalization to total-mass=1, disclosed out-of-range fake mass)
    - .planning/phases/14-paper-revision-release-freeze/peer-review-r3/SYNTHESIS.md (the Path 1 endorsement and the locked R3-CR-1 fix variant rationale at lines 127-135)
    - docs/reconciliation_note.md (the C-3 disclosure paragraph — the new `METRIC_FORMULATION` string MUST be word-for-word reflected in the C-3 disclosure extension that T5 writes; pre-coordinate the strings here)
  </read_first>
  <action>
    Surgical edit to `scripts/run_distribution_emd.py` to fix the
    histogram-density EMD structural bias at `compute_histogram_density_emd:94-141`.
    Bump the schema version to v2. Re-emit `results/distribution_emd.json`
    under the new schema with the `fake_in_range_mass` disclosure stat per
    (model, seed, scale) and aggregate.

    Step A — Update the module-level `SCHEMA` and `METRIC_FORMULATION`
    constants:

    At `:83`, change:
    ```
    SCHEMA = "distribution-emd v1 (Phase 14 plan 14-15)"
    ```
    to:
    ```
    SCHEMA = "distribution-emd v2 (Phase 14 plan 14-16)"
    ```

    At `:87-91`, replace the `METRIC_FORMULATION` constant with:
    ```
    METRIC_FORMULATION = (
        "np.histogram(real, bins=50, density=False) -> edges from real; "
        "np.histogram(fake, bins=edges, density=False); "
        "both histograms normalized to total-mass=1 over the same edge set "
        "(no per-distribution renormalization); "
        "bin_centers = 0.5*(edges[:-1] + edges[1:]); "
        "scipy.stats.wasserstein_distance(bin_centers, bin_centers, real_norm, fake_norm); "
        "out-of-range fake mass disclosed separately as fake_in_range_mass = fake_hist.sum() / len(fake)"
    )
    ```

    This string MUST then be reflected word-for-word in the C-3 disclosure
    extension that T5 writes into `reconciliation_note.md`.

    Step B — Replace the `compute_histogram_density_emd` function body at
    `:94-141`:

    BEFORE (current, R3-CR-1 broken):
    ```
    def compute_histogram_density_emd(real, fake, n_bins=50):
        ...
        real_hist, edges = np.histogram(real, bins=n_bins, density=True)
        fake_hist, _ = np.histogram(fake, bins=edges, density=True)
        bin_centers = 0.5 * (edges[:-1] + edges[1:])
        return float(wasserstein_distance(bin_centers, bin_centers, real_hist, fake_hist))
    ```

    AFTER (corrected, R3-CR-1 fixed, returns 2-tuple):
    ```
    def compute_histogram_density_emd(real, fake, n_bins=50):
        """Plan 14-16 r3-remediated histogram-density EMD.

        Shared-edges-from-real + total-mass=1 normalization over the same
        edge set (NO per-distribution renormalization). Out-of-range fake
        mass disclosed separately as fake_in_range_mass.

        Returns (emd, fake_in_range_mass) where:
        - emd is the wasserstein_distance between bin_centers weighted by
          the normalized histograms (sharing edges from real).
        - fake_in_range_mass is fake_hist.sum() / len(fake) — the fraction
          of fake samples that fell within real's empirical range. 1.0 =
          no truncation; 0.06 = 94% out-of-range (e.g., WGAN-CNN
          log-return scale per code-review-r3.md §H3).

        Fixes R3-CR-1 from peer-review-r3/code-review-r3.md §H3: the prior
        density=True formulation silently dropped out-of-range fake mass
        AND re-normalized each histogram independently, rewarding narrow-
        collapse distributions (VAE) and uncapped distributions (WGAN-CNN).
        """
        from scipy.stats import wasserstein_distance
        real = np.asarray(real).ravel().astype(np.float64)
        fake = np.asarray(fake).ravel().astype(np.float64)
        if real.size == 0 or fake.size == 0:
            raise ValueError(
                "compute_histogram_density_emd: empty input "
                f"(real.size={real.size}, fake.size={fake.size})"
            )
        real_hist, edges = np.histogram(real, bins=n_bins, density=False)
        fake_hist, _ = np.histogram(fake, bins=edges, density=False)
        fake_in_range_mass = float(fake_hist.sum() / len(fake))
        real_norm = real_hist / real_hist.sum() if real_hist.sum() > 0 else real_hist.astype(np.float64)
        fake_norm = fake_hist / fake_hist.sum() if fake_hist.sum() > 0 else fake_hist.astype(np.float64)
        bin_centers = 0.5 * (edges[:-1] + edges[1:])
        emd = float(wasserstein_distance(bin_centers, bin_centers, real_norm, fake_norm))
        return emd, fake_in_range_mass
    ```

    The signature change (single return -> 2-tuple) propagates to all
    call sites in `_model_seed_rows`.

    Step C — Update `_model_seed_rows` (around `:172-243`):

    Locate the two existing call sites:
    ```
    emd_OD = compute_histogram_density_emd(real_OD_flat, fake_OD_flat, n_bins=50)
    emd_logret = compute_histogram_density_emd(real_logret, fake_logret_flat, n_bins=50)
    ```

    Update to:
    ```
    emd_OD, fim_OD = compute_histogram_density_emd(real_OD_flat, fake_OD_flat, n_bins=50)
    emd_logret, fim_logret = compute_histogram_density_emd(real_logret, fake_logret_flat, n_bins=50)
    ```

    Update the row dict-appends to include `fake_in_range_mass`:
    ```
    rows.append({
        "model_kind": model,
        "seed": int(seed),
        "scale": "OD",
        "value": emd_OD,
        "fake_in_range_mass": fim_OD,
    })
    rows.append({
        "model_kind": model,
        "seed": int(seed),
        "scale": "log_return",
        "value": emd_logret,
        "fake_in_range_mass": fim_logret,
    })
    ```

    Apply the same update to the headline `iqp_sel_55_headline` rows
    (around `:209-243`).

    Step D — Update the aggregate builder:

    The existing per-(model_kind, scale) aggregate builder computes `mean`,
    `std` (ddof=1), `n` over the per-seed `value` values. Extend it to
    also compute `fake_in_range_mass_mean` as the mean of per-seed
    `fake_in_range_mass` values for the same group. The aggregate row
    schema becomes:
    ```
    {"model_kind": <m>, "scale": <s>, "mean": ..., "std": ..., "n": 5, "fake_in_range_mass_mean": ...}
    ```

    Step E — Update the module docstring with a Plan 14-16 remediation
    paragraph:

    Locate the module-level docstring at the top of `scripts/run_distribution_emd.py`
    (the 14-15 docstring stating the pre-v1.0 50-bin density Wasserstein
    formulation). Append:

    > **Plan 14-16 r3 remediation.** The original Plan 14-15 formulation
    > used `np.histogram(..., density=True)`, which renormalizes each
    > histogram independently OVER THE IN-RANGE PORTION ONLY — silently
    > dropping out-of-range fake mass and re-normalizing the remainder.
    > This rewards narrow-collapse distributions (e.g., VAE posterior
    > collapse, std=0.0004) and uncapped distributions (e.g., WGAN-CNN
    > with 94% out-of-range mass at log-return scale) — see R3-CR-1
    > mechanism in `peer-review-r3/code-review-r3.md` §H3 + the synthetic
    > demonstration at §H3 lines 179-189 + the ranking-inversion table at
    > lines 195-215. The v2 formulation (this plan) computes both
    > histograms with `density=False` over edges DERIVED FROM REAL ONLY,
    > then normalizes both to total-mass=1 OVER THE SAME EDGE SET (no
    > per-distribution renormalization). Out-of-range fake mass is
    > disclosed separately as `fake_in_range_mass = fake_hist.sum() /
    > len(fake)`, surfacing the WGAN-CNN truncation that v1 silently
    > dropped. Plan 14-16 also closes the sister-site scale mismatch in
    > `_real_references` + `_fake_log_return_flat` at `:144-169` (R3-HI-1
    > per `code-review-r3.md` §H3) via the same `norm_log_delta`
    > substitution used in T1's edit to `run_matched2000_dualscale.py`,
    > so both LR-scale call sites in both top-level emitters now share
    > the same standardized-vs-standardized comparison contract — see
    > T2 Step E2 below. The LR-scale rows in `distribution_emd.json` v2
    > are therefore CORRECT (not inherited-broken).

    Step E2 — R3-HI-1 sister-fix: apply analogous standardization to
    the LR-scale call sites in `_real_references` + `_fake_log_return_flat`
    at `:144-169`:

    Locate `_real_references` at `:144-153`:

    BEFORE (current, R3-HI-1 broken — same pattern as R3-CR-2 in T1):
    ```
    def _real_references(repo: Path) -> dict:
        d_real = load_and_preprocess(str(repo / DATA_CSV))
        real_windowed_OD = (
            rolling_window(d_real["OD"], WINDOW_LENGTH, 2).cpu().numpy()
        )
        return {
            "real_OD_flat": real_windowed_OD.reshape(-1),
            "real_log_delta": d_real["log_delta"].cpu().numpy().ravel(),
        }
    ```

    The `real_log_delta` field here is RAW log-returns (std≈0.022) but the
    LR-scale fake-side path `_fake_log_return_flat` at `:156-169` builds
    `r_norm = ((samples_pm1 + 1.0) / 2.0) * (r_max - r_min) + r_min` which
    is in the STANDARDIZED log-return space (std≈1) — the exact same 50×
    scale mismatch T1 closes in `run_matched2000_dualscale.py:368-372`,
    and the SAME finding ID R3-HI-1 in `code-review-r3.md` §H3.

    AFTER (corrected, mirrors T1's recipe):
    ```
    def _real_references(repo: Path) -> dict:
        d_real = load_and_preprocess(str(repo / DATA_CSV))
        real_windowed_OD = (
            rolling_window(d_real["OD"], WINDOW_LENGTH, 2).cpu().numpy()
        )
        raw_log_delta = d_real["log_delta"].cpu().numpy().ravel()
        # Load per-seed mu, sigma from any one seed's inverse_kwargs.npz
        # (mu, sigma are global per T1's Step A verification — computed
        # once from the full real series; not per-seed). Use seed 42 of
        # iqp_sel_55_repro by convention.
        inv_path = (
            repo / "revision" / "results" / "matched2000" / "runs"
            / "iqp_sel_55_repro" / "42" / "inverse_kwargs.npz"
        )
        inv = np.load(inv_path, allow_pickle=True)
        mu = float(inv["mu"])
        sigma = float(inv["sigma"])
        norm_log_delta = (raw_log_delta - mu) / sigma
        return {
            "real_OD_flat": real_windowed_OD.reshape(-1),
            "real_log_delta": raw_log_delta,        # PRESERVED (other callers may need it)
            "norm_log_delta": norm_log_delta,       # NEW (standardized LR-scale reference)
            "mu_logret": mu,                        # provenance disclosure
            "sigma_logret": sigma,                  # provenance disclosure
        }
    ```

    Update `_model_seed_rows` (around `:172-243`) where it builds the
    `real_logret` reference for `compute_histogram_density_emd`. Locate:

    ```
    real_logret = real_refs["real_log_delta"]
    ```

    Change to:

    ```
    real_logret = real_refs["norm_log_delta"]   # Plan 14-16 R3-HI-1 sister-fix
    ```

    The OD-scale path (which consumes `real_refs["real_OD_flat"]` and a
    `fake_OD_flat` from `reconstruct_od` returning OD-scale samples) is
    BYTE-UNTOUCHED — the OD inverse already cumsum-exps to the OD price
    scale where no standardization-vs-raw mismatch exists.

    No change needed to `_fake_log_return_flat` at `:156-169` itself — it
    already returns `r_norm` in the standardized log-return space; the
    fix is on the real-reference side only.

    Extend the module docstring (already extended in Step E) with a
    second paragraph:

    > **Plan 14-16 R3-HI-1 sister-fix (Step E2).** Per `code-review-r3.md`
    > §H3, R3-HI-1 names BOTH `run_matched2000_dualscale.py:368-372`
    > (R3-CR-2, closed by Plan 14-16 T1) AND `run_distribution_emd.py:
    > _real_references` + `_fake_log_return_flat` at `:144-169` as edit
    > sites under the SAME finding ID. T2 Step E2 applies the analogous
    > `norm_log_delta = (log_delta - mu) / sigma` substitution to the
    > LR-scale real reference in `_real_references`, so the LR-scale rows
    > in `distribution_emd.json` v2 consume a standardized real reference
    > against the already-standardized `r_norm` fake side. The LR-scale
    > aggregates in v2 are therefore at the corrected magnitude (~0.003
    > AR best, ~0.015 quantum cluster, WGAN-cluster significantly worse)
    > matching `pipeline-review-r3.md` §2 — they are NOT inherited-broken.
    > The OD-scale path is byte-untouched by Step E2 (OD inverse
    > cumsum-exps to OD price scale).

    Step F — Re-emit `results/distribution_emd.json`:

    ```
    ./qgan_env/bin/python scripts/run_distribution_emd.py
    ```

    Step G — Verify schema bump and new fields:

    Load the new JSON and verify:
    - `schema == "distribution-emd v2 (Phase 14 plan 14-16)"`
    - `metric_formulation` matches the new `METRIC_FORMULATION` constant
      verbatim (which T5 then mirrors in the C-3 disclosure extension)
    - `data_hash == "91e447d4624e25b3"` preserved
    - `n_bins == 50` preserved
    - Each per-(model, seed, scale) row carries `fake_in_range_mass` in
      [0.0, 1.0]
    - Each per-(model_kind, scale) aggregate carries `fake_in_range_mass_mean`
      in [0.0, 1.0]
    - WGAN-CNN log-return scale `fake_in_range_mass_mean` is around 0.06
      (the 94%-out-of-range disclosure surfacing the bug Agent 5 documented)
    - Quantum models' OD-scale `fake_in_range_mass_mean` is close to 1.0
      (the `*0.1` cap keeps quantum samples inside the real OD range
      after `reconstruct_od`)
    - VAE OD-scale `fake_in_range_mass_mean` is close to 1.0 (posterior
      collapse keeps samples tightly inside the mean — confirming the
      narrow-collapse mechanism)

    Step H — Verify ranking inversion vs schema v1:

    Compare the v2 OD-scale `mean` aggregate ordering against the v1
    ordering committed at `961ee12` (read the prior `distribution_emd.json`
    via `git show HEAD:results/distribution_emd.json`). The v2
    ordering MUST differ from v1 — specifically:
    - VAE should NO LONGER be #1 on OD-scale histogram-density EMD (the
      broken metric rewarded its narrow collapse — the v2 fixed metric
      penalizes it because its samples cluster too tightly relative to
      real's spread)
    - Quantum models should rank competitively (not at the bottom as they
      were under v1)

    Step I — Commit:

    `feat(14-16): R3-CR-1 fix — shared-edges-from-real + disclosed-out-of-range histogram-density EMD (distribution-emd v2)`

    Commit body enumerates the schema bump (v1 -> v2), the new
    `fake_in_range_mass` disclosure stat, the WGAN-CNN truncation
    disclosure surfaced for the first time, and a one-line reference to
    `peer-review-r3/code-review-r3.md` §H3. Record the R3-HI-1 sister-fix
    success: the LR-scale call sites in `_real_references` + `_fake_log_return_flat`
    at `:144-169` were ALSO standardized via the same `norm_log_delta`
    substitution used in T1 (Step E2 below), so both LR-EMD pathways in
    both top-level emitters are now scale-matched. The LR-scale rows in
    `distribution_emd.json` v2 are correct (not inherited-broken).
  </action>
  <verify>
    <automated>./qgan_env/bin/python scripts/run_distribution_emd.py &amp;&amp; ./qgan_env/bin/python -c "import json,pathlib,sys; j=json.loads(pathlib.Path('results/distribution_emd.json').read_text()); errs=[]; (j.get('schema')=='distribution-emd v2 (Phase 14 plan 14-16)') or errs.append(f'schema not bumped to v2: {j.get(\"schema\")}'); ('shared-edges' in j.get('metric_formulation','') or 'fake_in_range_mass' in j.get('metric_formulation','') or 'density=False' in j.get('metric_formulation','')) or errs.append('metric_formulation does not describe the v2 formulation'); (j.get('data_hash')=='91e447d4624e25b3') or errs.append('data_hash mismatch'); (j.get('n_bins')==50) or errs.append('n_bins != 50'); rows=j.get('rows',[]); aggs=j.get('aggregates',[]); (all('fake_in_range_mass' in r for r in rows)) or errs.append('some rows missing fake_in_range_mass'); (all('fake_in_range_mass_mean' in a for a in aggs)) or errs.append('some aggregates missing fake_in_range_mass_mean'); wgan_cnn_lr=[a for a in aggs if a.get('model_kind')=='wgan_cnn' and a.get('scale')=='log_return']; (len(wgan_cnn_lr)>=1 and wgan_cnn_lr[0].get('fake_in_range_mass_mean',1.0) < 0.5) or errs.append(f'wgan_cnn log_return fake_in_range_mass_mean not surfacing truncation: {wgan_cnn_lr}'); print('errors',errs); sys.exit(0 if not errs else 1)" &amp;&amp; ./qgan_env/bin/python -c "import json,subprocess,pathlib,sys; current=json.loads(pathlib.Path('results/distribution_emd.json').read_text()); prior_text=subprocess.run(['git','show','HEAD:results/distribution_emd.json'], capture_output=True, text=True).stdout; prior=json.loads(prior_text) if prior_text else {}; v2_od_aggs={a.get('model_kind'): a.get('mean') for a in current.get('aggregates',[]) if a.get('scale')=='OD'}; v1_od_aggs={a.get('model_kind'): a.get('mean') for a in prior.get('aggregates',[]) if a.get('scale')=='OD'}; v2_rank=sorted(v2_od_aggs.items(), key=lambda kv: kv[1]); v1_rank=sorted(v1_od_aggs.items(), key=lambda kv: kv[1]); print('v1 OD-EMD ranking:', [r[0] for r in v1_rank]); print('v2 OD-EMD ranking:', [r[0] for r in v2_rank]); assert [r[0] for r in v1_rank] != [r[0] for r in v2_rank] or len(v1_rank)==0, f'ranking did not invert: v1={v1_rank} v2={v2_rank}'; print('ranking-inversion OK (or v1 unavailable)')" &amp;&amp; ./qgan_env/bin/python -c "import json, pathlib, sys; j = json.loads(pathlib.Path('results/distribution_emd.json').read_text()); lr_aggs = {a.get('model_kind'): a.get('mean') for a in j.get('aggregates', []) if a.get('scale') == 'log_return'}; errs = []; quantum_ok = all(lr_aggs.get(m, 1.0) is not None and lr_aggs.get(m, 1.0) &lt; 0.1 for m in ['iqp_sel_55_repro', 'V1', 'V2', 'V3']); (quantum_ok) or errs.append('LR-scale quantum aggregates not at corrected magnitude (R3-HI-1 sister-fix not applied?): ' + repr(lr_aggs)); ar_ok = lr_aggs.get('ar', 1.0) is not None and lr_aggs.get('ar', 1.0) &lt; 0.05; (ar_ok) or errs.append('AR LR-scale aggregate not at corrected ~0.003 magnitude (R3-HI-1 sister-fix not applied?): ' + repr(lr_aggs.get('ar'))); print('R3-HI-1 sister-fix LR-scale check:', errs); sys.exit(0 if not errs else 1)" &amp;&amp; [ -z "$(git diff --stat core/)" ] &amp;&amp; [ -z "$(git diff verify_number_provenance.py)" ]</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/run_distribution_emd.py` `SCHEMA` constant bumped to `"distribution-emd v2 (Phase 14 plan 14-16)"`
    - `METRIC_FORMULATION` constant rewritten to describe the shared-edges-from-real + disclosed-out-of-range formulation verbatim
    - `compute_histogram_density_emd(real, fake, n_bins=50)` function body replaced; returns 2-tuple `(emd, fake_in_range_mass)`; uses `density=False` for both histograms; edges from real ONLY; both histograms normalized to total-mass=1 over the same edge set
    - `_model_seed_rows` updated to consume the 2-tuple return value; per-(model, seed, scale) rows gain `fake_in_range_mass` field
    - Per-(model_kind, scale) aggregates gain `fake_in_range_mass_mean` field
    - `results/distribution_emd.json` re-emitted under schema v2 with all new fields populated
    - WGAN-CNN log-return-scale `fake_in_range_mass_mean` < 0.5 (surfacing the 94%-out-of-range truncation that v1's `density=True` silently dropped)
    - Ranking inversion vs v1 verified: VAE no longer #1 on OD histogram-density EMD; quantum models rank competitively
    - Module docstring extended with TWO Plan 14-16 remediation paragraphs: one for R3-CR-1 (the histogram-density bias fix) and one for R3-HI-1 Step E2 (the LR-scale standardization sister-fix)
    - Step E2 R3-HI-1 sister-fix applied: `_real_references` now also returns `norm_log_delta = (log_delta - mu) / sigma`; `_model_seed_rows` consumes `real_refs['norm_log_delta']` for the LR-scale `compute_histogram_density_emd` call (the OD-scale path is byte-untouched)
    - Post-Step-E2 LR-scale aggregates in `distribution_emd.json` v2 are at the corrected magnitude (matching `pipeline-review-r3.md` §2 anchors directionally: AR ~0.003 best on LR-scale, quantum cluster ~0.015, WGAN-cluster significantly worse with WGAN-CNN the outlier) — NOT at the broken 0.6-0.8 magnitude
    - All quantum LR-scale aggregates < every WGAN LR-scale aggregate (the rank-inversion-on-LR-scale that the v1 (broken-scale) metric had reversed is now corrected)
    - `git diff --stat core/` is empty (D-14-22 preserved); `git diff verify_number_provenance.py` is empty (D-14-16 preserved — no gate edit; the corrected JSONs flow through the existing `_json_blobs()` walker)
  </acceptance_criteria>
  <done>R3-CR-1 fix landed (`compute_histogram_density_emd:94-141` replaced with shared-edges + disclosed-out-of-range formulation); R3-HI-1 sister-fix landed (`_real_references` at `:144-153` now also returns `norm_log_delta`; `_model_seed_rows` consumes the standardized reference for the LR-scale path); `distribution_emd.json` re-emitted under schema v2 with `fake_in_range_mass` disclosure stat AND correctly-magnituded LR-scale aggregates (~0.003 AR, ~0.015 quantum cluster, WGAN cluster significantly worse); ranking inversion on OD-scale verified; WGAN-CNN 94%-truncation now surfaced via disclosure stat; D-14-22 preserved.</done>
</task>

<task type="auto">
  <name>Task 3: NEW (W2) — `welch_pairwise.json` aggregator emits per-pair Welch t/p + Cohen d + MWU p for the strong-claim Welch literals</name>
  <files>run_welch_aggregator.py, results/welch_pairwise.json</files>
  <read_first>
    - results/matched2000_dualscale.json (POST-T1 corrected; the source of per-(model_kind, seed, scale='OD'/'log_return', metric_name='emd') aggregate rows the aggregator reads)
    - results/model_info.json (the canonical model_kinds list + seed_set; the aggregator reads the seed list and model order from here to avoid hand-coding)
    - run_model_info.py (REFERENCE — for the canonical write_json helper or json.dumps convention used across `revision/`; the new aggregator mirrors the existing emitter conventions; do NOT import from `core/`; D-14-22 preserved)
    - .planning/phases/14-paper-revision-release-freeze/peer-review-r3/statistical-honesty-r3.md §3a + §3b (the per-pair Welch t-test + Cohen d tables that the aggregator's output must match within numerical precision)
    - .planning/phases/14-paper-revision-release-freeze/peer-review-r3/SYNTHESIS.md (the strong-claim framing the JSON anchors)
  </read_first>
  <action>
    Create a NEW top-level Python script `scripts/run_welch_aggregator.py`
    that emits `results/welch_pairwise.json` from the corrected
    per-seed aggregates in `matched2000_dualscale.json` (post-T1). The
    JSON is the auditable anchor for the strong-claim Welch literals
    asserted in `reviewer_response.md`'s new H2 section (`p > 0.36`,
    `|d| ≤ 0.65`, `p ≤ 0.014`, `d ≤ -2.6`); without it, those literals
    can only be resolved by the v2.1 gate via coincidental ε-match
    against unrelated EMD values in other JSONs — a defense-in-depth
    provenance gap.

    Step A — Create `scripts/run_welch_aggregator.py`:

    Top-level script (no imports from `core/`; D-14-22 byte-freeze
    preserved). Outline:

    1. Read `results/matched2000_dualscale.json#rows`. Filter to
       `metric_name == 'emd'` and `pipeline == 'B'`.
    2. Group by `(model_kind, scale)` and collect the per-seed `value`
       list (5 values per group, seeds {42, 43, 44, 45, 46}).
    3. Define quantum models = `['iqp_sel_55_repro', 'V1', 'V2', 'V3']`,
       classical models = `['wgan_mlp', 'wgan_cnn', 'wgan_lstm', 'vae',
       'ar']`. For each `(quantum, classical, scale)` triple (4*5*2 = 40
       pairs), compute:
       - `mean_q, std_q, n_q = mean(values_q), std(values_q, ddof=1), len(values_q)`
       - `mean_c, std_c, n_c = mean(values_c), std(values_c, ddof=1), len(values_c)`
       - `welch_t, welch_p = scipy.stats.ttest_ind(values_q, values_c, equal_var=False)`
       - `mwu_stat, mwu_p = scipy.stats.mannwhitneyu(values_q, values_c, alternative='two-sided')`
       - `cohen_d = (mean_q - mean_c) / pooled_sd` where `pooled_sd = sqrt(((n_q-1)*std_q**2 + (n_c-1)*std_c**2) / (n_q+n_c-2))`
    4. Emit JSON with schema:
       ```
       {
         "schema": "welch-pairwise v1 (Phase 14 plan 14-16 W2)",
         "data_hash": "91e447d4624e25b3",
         "source": "results/matched2000_dualscale.json",
         "source_filter": "metric_name=='emd', pipeline=='B'",
         "n_per_group": 5,
         "seeds": [42, 43, 44, 45, 46],
         "quantum_models": ["iqp_sel_55_repro", "V1", "V2", "V3"],
         "classical_models": ["wgan_mlp", "wgan_cnn", "wgan_lstm", "vae", "ar"],
         "scales": ["OD", "log_return"],
         "pairs": [
           {
             "quantum": "<m>", "classical": "<m>", "scale": "OD"|"log_return",
             "mean_q": ..., "std_q": ..., "n_q": 5,
             "mean_c": ..., "std_c": ..., "n_c": 5,
             "welch_t": ..., "welch_p": ...,
             "cohen_d": ...,
             "mwu_stat": ..., "mwu_p": ...
           },
           ...
         ],
         "summaries": {
           "OD_floor_welch_p_quantum_vs_classical": ...,
           "OD_ceiling_abs_cohen_d_quantum_vs_classical": ...,
           "log_return_ceiling_welch_p_quantum_vs_wgan": ...,
           "log_return_extremum_cohen_d_quantum_vs_wgan": ...
         },
         "strong_claim_thresholds": {
           "floor_welch_p_OD": 0.36,
           "ceiling_abs_cohen_d_OD": 0.65
         },
         "notes": "Path A (Plan 14-16 r3 process retraction): Post-R3-CR-2 fix (un-standardize-fake per pipeline-review-r3.md §2), LR-EMD rankings invert from pre-fix narrative. AR (Yule-Walker MLE) leads at 0.003, quantum/WGAN/VAE cluster in 0.007-0.016 with every WGAN beating every quantum on the corrected scale. The pre-fix statistical-honesty-r3.md §3b strong-claim Welch tests (p ≤ 0.014, Cohen d ≤ -2.6) were computed on the broken (scale-mismatched) LR-EMD column and DO NOT carry post-fix. The LR-EMD pairs in this JSON are emitted as per-pair stats for transparency but the strong_claim_thresholds block ONLY enforces OD-EMD thresholds (which survive because the OD column is byte-stable pre/post T1). See peer_review_remediation.md Plan 14-16 r3 process retraction subsection for the full retraction documentation."
       }
       ```
       The `strong_claim_thresholds` block carries ONLY the OD-EMD
       threshold literals (0.36 for floor Welch p, 0.65 for ceiling |Cohen d|)
       cited in `reviewer_response.md`'s new H2 section. The LR-EMD
       thresholds (`ceiling_welch_p_LR_vs_wgan`, `extremum_cohen_d_LR_vs_wgan`)
       are NOT emitted because Path A withdraws the LR-EMD-vs-WGAN strong
       claim. The per-pair Welch t/p + Cohen d + MWU p for LR-EMD pairs
       are still emitted in `pairs[]` for transparency but without
       threshold-direction assertions. The v2.1 gate's ε-neighborhood
       matcher resolves the surviving OD-EMD literals against the leaves
       in `strong_claim_thresholds.{floor_welch_p_OD, ceiling_abs_cohen_d_OD}`.
       The emitter MUST assert before writing JSON: ONLY the OD-EMD
       threshold direction (for every (quantum, classical, scale='OD') row,
       `welch_p > strong_claim_thresholds.floor_welch_p_OD` AND
       `abs(cohen_d) <= strong_claim_thresholds.ceiling_abs_cohen_d_OD`).
       LR-EMD pairs are emitted without pre-write threshold checks because
       the data does NOT satisfy the original direction. If the OD-EMD
       assertion fails, the emitter aborts with non-zero exit BEFORE
       writing the JSON — the OD-EMD equivalence claim cannot be asserted
       unless the underlying byte-stable OD data supports it.
    5. `data_hash`: `'91e447d4624e25b3'` (corpus consistency).
    6. Sort `pairs[]` by `(scale, quantum, classical)` for deterministic
       emit (the v2.1 gate walks the JSON in canonical order).
    7. Persist via `json.dump(payload, fp, indent=2, sort_keys=False)`
       (sort_keys=False to keep the schema-declared field order; consistent
       with other `results/*.json` emit conventions).

    Step B — Compute `summaries` block (Path A — OD-EMD-only enforced anchors):

    Per `statistical-honesty-r3.md` §3a (OD-EMD, byte-stable pre/post T1),
    the surviving strong-claim anchors are:
    - `OD_floor_welch_p_quantum_vs_classical` = `min(p.welch_p for p in pairs if p.scale=='OD')` — should be `> 0.36` (this anchors the `p > 0.36` literal in `reviewer_response.md`)
    - `OD_ceiling_abs_cohen_d_quantum_vs_classical` = `max(abs(p.cohen_d) for p in pairs if p.scale=='OD')` — should be `≤ 0.65`

    Also compute the LR-EMD summary stats for transparency (NOT enforced
    as thresholds because Path A withdraws the LR-EMD-vs-WGAN strong claim):
    - `log_return_floor_welch_p_quantum_vs_wgan` = `min(p.welch_p for p in pairs if p.scale=='log_return' and p.classical.startswith('wgan'))` — REPORTED ONLY (no threshold enforced; expected to be very small on corrected data because every WGAN beats every quantum — but in the OPPOSITE direction from the pre-fix claim)
    - `log_return_extremum_cohen_d_quantum_vs_wgan` = `max(p.cohen_d for p in pairs if p.scale=='log_return' and p.classical.startswith('wgan'))` — REPORTED ONLY (expected to be POSITIVE on corrected data; the pre-fix `d ≤ -2.6` direction is withdrawn)

    Step B2 — Emit the `strong_claim_thresholds` block AND assert OD-EMD-only
    threshold satisfaction BEFORE writing JSON (Path A):

    Add the OD-EMD-only `strong_claim_thresholds` dict to the payload
    before serialization:
    ```
    payload["strong_claim_thresholds"] = {
        "floor_welch_p_OD": 0.36,
        "ceiling_abs_cohen_d_OD": 0.65,
    }
    ```

    These are the OD-EMD threshold literals cited in `reviewer_response.md`'s
    Path A reframed H2 section. The LR-EMD thresholds (`ceiling_welch_p_LR_vs_wgan`,
    `extremum_cohen_d_LR_vs_wgan`) are NOT emitted because Path A withdraws
    the LR-EMD-vs-WGAN strong claim (corrected data shows every WGAN beats
    every quantum on LR-EMD; the pre-fix Welch tests in `statistical-honesty-r3.md`
    §3b were computed on the broken column). The v2.1 gate resolves the
    surviving OD-EMD literals (`0.36`, `0.65`) against these leaves under
    `welch_pairwise.json#strong_claim_thresholds.{floor_welch_p_OD,ceiling_abs_cohen_d_OD}`.

    Also emit the explanatory `notes` field (verbatim from Path A user decision):
    ```
    payload["notes"] = ("Path A (Plan 14-16 r3 process retraction): "
        "Post-R3-CR-2 fix (un-standardize-fake per pipeline-review-r3.md §2), "
        "LR-EMD rankings invert from pre-fix narrative. AR (Yule-Walker MLE) "
        "leads at 0.003, quantum/WGAN/VAE cluster in 0.007-0.016 with every "
        "WGAN beating every quantum on the corrected scale. The pre-fix "
        "statistical-honesty-r3.md §3b strong-claim Welch tests (p ≤ 0.014, "
        "Cohen d ≤ -2.6) were computed on the broken (scale-mismatched) LR-EMD "
        "column and DO NOT carry post-fix. The LR-EMD pairs in this JSON are "
        "emitted as per-pair stats for transparency but the "
        "strong_claim_thresholds block ONLY enforces OD-EMD thresholds (which "
        "survive because the OD column is byte-stable pre/post T1). See "
        "peer_review_remediation.md Plan 14-16 r3 process retraction subsection "
        "for the full retraction documentation.")
    ```

    THEN assert (before writing JSON) ONLY the OD-EMD threshold directions:
    ```
    assert summaries["OD_floor_welch_p_quantum_vs_classical"] > payload["strong_claim_thresholds"]["floor_welch_p_OD"], \
        f"OD floor Welch p computed={summaries['OD_floor_welch_p_quantum_vs_classical']} does NOT clear threshold {payload['strong_claim_thresholds']['floor_welch_p_OD']}"
    assert summaries["OD_ceiling_abs_cohen_d_quantum_vs_classical"] <= payload["strong_claim_thresholds"]["ceiling_abs_cohen_d_OD"], \
        f"OD ceiling |Cohen d| computed={summaries['OD_ceiling_abs_cohen_d_quantum_vs_classical']} does NOT clear threshold {payload['strong_claim_thresholds']['ceiling_abs_cohen_d_OD']}"
    ```

    LR-EMD pairs are emitted in `pairs[]` without pre-write threshold checks
    — those would FAIL on the corrected data because the pre-fix direction
    has inverted. If the OD-EMD assertions fail, the script aborts with
    non-zero exit BEFORE writing JSON — the OD-EMD equivalence claim cannot
    be asserted unless the underlying byte-stable OD data supports it.

    Step C — Run the aggregator:

    ```
    ./qgan_env/bin/python scripts/run_welch_aggregator.py
    ```

    Step D — Verify the OD-EMD summaries match the surviving strong-claim
    anchors (Path A):

    Load `results/welch_pairwise.json` and assert:
    - `summaries.OD_floor_welch_p_quantum_vs_classical > 0.36`
    - `summaries.OD_ceiling_abs_cohen_d_quantum_vs_classical <= 0.65`
    - `strong_claim_thresholds.floor_welch_p_OD == 0.36` (exact)
    - `strong_claim_thresholds.ceiling_abs_cohen_d_OD == 0.65` (exact)
    - `strong_claim_thresholds` does NOT contain `ceiling_welch_p_LR_vs_wgan`
      or `extremum_cohen_d_LR_vs_wgan` keys (Path A withdrawal)
    - `notes` field exists and contains the phrases "Path A", "r3 process
      retraction", "broken (scale-mismatched) LR-EMD column"
    - The 2 surviving strong-claim OD-EMD literals (`p > 0.36`, `|d| ≤ 0.65`)
      that `reviewer_response.md` asserts resolve against this JSON via
      `welch_pairwise.json#strong_claim_thresholds.{floor_welch_p_OD,
      ceiling_abs_cohen_d_OD}` as first-class JSON leaves

    Step E — Verify the per-pair LR-EMD stats reflect the corrected data
    (Path A — for transparency, NOT for threshold enforcement):

    On corrected un-standardize-fake LR-EMD data, the per-pair Cohen's d
    values for (quantum, wgan_*) pairs are POSITIVE (every WGAN beats
    every quantum), inverting the pre-fix `statistical-honesty-r3.md` §3b
    direction. Verify directionally:
    - `pairs[?quantum=='iqp_sel_55_repro' && classical=='wgan_cnn' && scale=='log_return'].cohen_d > 0`
      (corrected expected: ~+2.15 per `14-16-DEVIATION-NOTE.md` Welch table;
      pre-fix was -2.63)
    - `pairs[?quantum=='iqp_sel_55_repro' && classical=='wgan_mlp' && scale=='log_return'].cohen_d > 0`
      (corrected expected: ~+8.18; pre-fix was -5.22)
    - `pairs[?quantum=='iqp_sel_55_repro' && classical=='wgan_lstm' && scale=='log_return'].cohen_d > 0`
      (corrected expected: ~+5.00; pre-fix was -2.97)
    Path A note: these inverted Cohen d values are EVIDENCE the LR-EMD
    fix landed correctly. They are emitted for transparency in `pairs[]`
    but NOT used to anchor any strong claim in reviewer-facing docs.

    Step F — Commit:

    `feat(14-16): NEW welch_pairwise.json aggregator (Path A) — JSON-anchored OD-EMD strong-claim provenance (p > 0.36 / |d| ≤ 0.65); LR-EMD pairs emitted for transparency without threshold enforcement`

    Commit body enumerates the 2 surviving OD-EMD strong-claim summary
    anchors verified in Step D, the inverted per-pair Cohen's d
    cross-check in Step E confirming Path A correctness, the `notes` field
    documenting the r3-process retraction, and a one-line reference to
    both the W2 planner-checker recommendation and the user's Path A
    decision following the 14-16-DEVIATION-NOTE.md checkpoint.
  </action>
  <verify>
    <automated>./qgan_env/bin/python scripts/run_welch_aggregator.py &amp;&amp; ./qgan_env/bin/python -c "import json, pathlib, sys; j = json.loads(pathlib.Path('results/welch_pairwise.json').read_text()); errs = []; (j.get('schema') == 'welch-pairwise v1 (Phase 14 plan 14-16 W2)') or errs.append('schema mismatch: ' + repr(j.get('schema'))); (j.get('data_hash') == '91e447d4624e25b3') or errs.append('data_hash mismatch'); pairs = j.get('pairs', []); (len(pairs) >= 36) or errs.append('expected >= 36 pairs, got ' + str(len(pairs))); summaries = j.get('summaries', {}); (summaries.get('OD_floor_welch_p_quantum_vs_classical', 0.0) &gt; 0.36) or errs.append('OD floor Welch p not &gt; 0.36: ' + repr(summaries.get('OD_floor_welch_p_quantum_vs_classical'))); (summaries.get('OD_ceiling_abs_cohen_d_quantum_vs_classical', 1.0) &lt;= 0.65) or errs.append('OD ceiling |d| not &lt;= 0.65: ' + repr(summaries.get('OD_ceiling_abs_cohen_d_quantum_vs_classical'))); thr = j.get('strong_claim_thresholds', {}); (thr.get('floor_welch_p_OD') == 0.36) or errs.append('strong_claim_thresholds.floor_welch_p_OD != 0.36: ' + repr(thr.get('floor_welch_p_OD'))); (thr.get('ceiling_abs_cohen_d_OD') == 0.65) or errs.append('strong_claim_thresholds.ceiling_abs_cohen_d_OD != 0.65: ' + repr(thr.get('ceiling_abs_cohen_d_OD'))); ('ceiling_welch_p_LR_vs_wgan' not in thr) or errs.append('Path A violation: ceiling_welch_p_LR_vs_wgan must NOT be in strong_claim_thresholds'); ('extremum_cohen_d_LR_vs_wgan' not in thr) or errs.append('Path A violation: extremum_cohen_d_LR_vs_wgan must NOT be in strong_claim_thresholds'); notes = j.get('notes', ''); ('Path A' in notes) or errs.append('notes field missing Path A retraction documentation'); ('r3 process retraction' in notes or 'r3-process retraction' in notes) or errs.append('notes field missing r3 process retraction phrase'); print('Path A OD-EMD-only check:', errs); sys.exit(0 if not errs else 1)" &amp;&amp; [ -z "$(git diff --stat core/)" ] &amp;&amp; [ -z "$(git diff verify_number_provenance.py)" ]</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/run_welch_aggregator.py` exists as a NEW top-level emitter (no imports from `core/`; D-14-22 preserved)
    - `results/welch_pairwise.json` exists with schema `'welch-pairwise v1 (Phase 14 plan 14-16 W2)'`; `data_hash: '91e447d4624e25b3'`
    - `pairs[]` carries records for all 4 quantum × 5 classical × 2 scales = 40 (model, model, scale) triples (some may legitimately be absent if a per-seed row is missing in `matched2000_dualscale.json`; require ≥36 — 90% rectangularity)
    - Each pair record carries `welch_t, welch_p, cohen_d, mwu_stat, mwu_p, n_q=5, n_c=5, mean_q, std_q, mean_c, std_c`
    - `summaries.OD_floor_welch_p_quantum_vs_classical > 0.36` (the COMPUTED floor value; OD-EMD byte-stable column unaffected by T1)
    - `summaries.OD_ceiling_abs_cohen_d_quantum_vs_classical <= 0.65` (the COMPUTED ceiling value)
    - `summaries` MAY also include `log_return_floor_welch_p_quantum_vs_wgan` and `log_return_extremum_cohen_d_quantum_vs_wgan` for transparency BUT NEITHER is asserted against a threshold (Path A: LR-EMD-vs-WGAN strong claim withdrawn)
    - `strong_claim_thresholds` block carries ONLY the OD-EMD threshold literals as JSON leaves: `floor_welch_p_OD == 0.36`, `ceiling_abs_cohen_d_OD == 0.65`. The LR-EMD thresholds (`ceiling_welch_p_LR_vs_wgan`, `extremum_cohen_d_LR_vs_wgan`) MUST NOT be present (Path A retraction enforces absence)
    - `notes` field present documenting the Path A r3 process retraction with the phrases "Path A", "r3 process retraction", "broken (scale-mismatched) LR-EMD column", "DO NOT carry post-fix"
    - The emitter asserts OD-EMD threshold satisfaction ONLY (each per-row OD-EMD computed value clears its threshold direction) BEFORE writing JSON; LR-EMD pairs emitted without pre-write threshold checks; failure of OD-EMD assertion aborts with non-zero exit
    - Per-pair LR-EMD Cohen's d directionally correct on corrected data: `pairs[?quantum=='iqp_sel_55_repro' && classical=='wgan_cnn' && scale=='log_return'].cohen_d > 0` (corrected ~+2.15; inverts pre-fix -2.63), `pairs[?quantum=='iqp_sel_55_repro' && classical=='wgan_mlp' && scale=='log_return'].cohen_d > 0` (corrected ~+8.18; inverts pre-fix -5.22) — the inversion is EVIDENCE the T1 fix landed correctly
    - `git diff --stat core/` is empty (D-14-22 preserved); `git diff verify_number_provenance.py` is empty (D-14-16 preserved)
  </acceptance_criteria>
  <done>welch_pairwise.json emitted from corrected matched2000_dualscale.json aggregates with Path A OD-EMD-only strong-claim anchors verified (p > 0.36 / |d| ≤ 0.65 enforced); LR-EMD pairs emitted in pairs[] for transparency without threshold enforcement; notes field documents r3 process retraction; per-pair LR-EMD Cohen's d on corrected data shows INVERSION from pre-fix direction (positive d for quantum-vs-wgan, confirming WGANs beat quantum on corrected LR-EMD); D-14-22 + D-14-16 preserved.</done>
</task>

<task type="auto">
  <name>Task 4: reviewer_response.md R1-M1 row preserved verbatim + new H2 section asserting strong-claim Welch-grounded equivalence + DTW addendum subsection (T8-equivalent DTW historical-asymmetry disclosure)</name>
  <files>docs/reviewer_response.md</files>
  <read_first>
    - docs/reviewer_response.md (CURRENT FULL STATE — verify the R1-M1 row at line 36 reads "Added matched-parameter classical WGAN-GP (MLP/CNN/LSTM critics) and a non-adversarial VAE + AR baseline, all at matched 2000-epoch budget, identical critic/optimizer/seed set; parameter-count-controlled comparison table" with NO equivalence claim; verify the existing `## Marginal-convergence finding (post-r2 investigation)` H2 (at line 208 in the pre-14-16 state — read the full file in T4 read_first to confirm post-14-15 line) and the existing `## Completeness sweep (Plans 14-09 .. 14-11)` H2 (at line 85 in the original state — verify final post-14-15 line in T4 read_first); the new H2 inserts BETWEEN these two)
    - results/welch_pairwise.json (POST-T3 NEW; the JSON anchor for the strong-claim Welch literals)
    - results/matched2000_dualscale.json (POST-T1; corrected log-return aggregates source for the equivalence-table citations)
    - results/model_info.json (the canonical parameter_count values: wgan_mlp=74, wgan_cnn=73, wgan_lstm=78, vae=562, ar=3)
    - results/total_adversarial_param_budget.json (the canonical 250881 shared critic + per-generator total budget for the ~2.5x10^5 framing)
    - .planning/phases/14-paper-revision-release-freeze/peer-review-r3/statistical-honesty-r3.md §3a + §3b + §7-Option A (the strong-claim framing source — `d ≤ -2.6` is verbatim from §7-Option A line 288)
    - .planning/phases/14-paper-revision-release-freeze/peer-review-r3/SYNTHESIS.md (the locked strong-claim framing)
  </read_first>
  <action>
    Update ONLY `docs/reviewer_response.md`. The R1-M1 row at line
    36 is PRESERVED VERBATIM (it's a single markdown-table cell with no
    equivalence claim, accurately describing what was done). A new H2
    section is INSERTED between the existing `## Marginal-convergence
    finding (post-r2 investigation)` H2 and the existing `## Completeness
    sweep (Plans 14-09 .. 14-11)` H2.

    Step A — Read the current file in full and CONFIRM the structure:

    - R1-M1 row at line 36 contains EXACTLY: "Added matched-parameter
      classical WGAN-GP (MLP/CNN/LSTM critics) and a non-adversarial VAE
      + AR baseline, all at matched 2000-epoch budget, identical
      critic/optimizer/seed set; parameter-count-controlled comparison
      table" (no equivalence claim, no follow-up-work hedge).
    - The `## Marginal-convergence finding (post-r2 investigation)` H2
      exists (added by 14-15 T2).
    - The `## Completeness sweep (Plans 14-09 .. 14-11)` H2 exists.

    If any of the three confirmations fail, ABORT this task and report
    back to the orchestrator — the doc state has drifted from the
    expectation under which Plan 14-16 was authored.

    Step B — Insert NEW H2 section AFTER the
    `## Marginal-convergence finding (post-r2 investigation)` H2 (which
    ends just before the next `## Completeness sweep (Plans 14-09 .. 14-11)`
    H2) and BEFORE the `## Completeness sweep (Plans 14-09 .. 14-11)` H2.
    The exact insertion point is the blank line preceding the
    `## Completeness sweep` H2 within the Marginal-convergence section's
    closing.

    Insert:

    > ## Parametric-efficiency equivalence (post-r3 corrected metrics)
    >
    > Per the Plan 14-16 r3 forensic remediation (which closed two metric
    > bugs documented in `peer-review-r3/code-review-r3.md` §H3 — see
    > `peer_review_remediation.md` Plan 14-16 section for full forensic
    > disclosure AND the Plan 14-16 r3-process retraction subsection for
    > the LR-EMD-vs-WGAN strong-claim withdrawal), the matched-2000ep
    > budget supports the following Path A strong claim:
    >
    > **55 quantum parameters achieve OD-scale EMD statistically equivalent
    > to classical generators of 73-562 generator params AND the full
    > ~2.5x10^5 adversarial budget (generator + 250881-parameter shared
    > critic) carried by every WGAN variant (Welch p > 0.36, |d| ≤ 0.65, n=5).**
    >
    > **On log-return EMD, AR (3 params, closed-form Yule-Walker MLE)
    > leads at 0.003; quantum/WGAN/VAE cluster in 0.007-0.016. No
    > statistically meaningful quantum-vs-WGAN separation on marginal
    > log-return distribution.**
    >
    > **On DTW (Dynamic Time Warping, temporal alignment), quantum
    > dominates: all 4 variants beat every WGAN+AR on LR-DTW; best
    > quantum beats Orlandi et al. by 6.5x on OD-DTW. This is the
    > temporal-structure capture quantum is specifically designed for.**
    >
    > At matched 2000-epoch training budget and n=5 seeds per cell (seeds
    > {42, 43, 44, 45, 46}), the iqp_sel_55 quantum reference circuit
    > (55 trainable parameters per `results/model_info.json#models[?model=='iqp_sel_55_repro'].parameter_count`)
    > achieves OD-scale EMD statistically equivalent to every classical
    > generator baseline tested:
    >
    > Every WGAN variant additionally carries the shared 250881-parameter critic
    > during adversarial training (per `total_adversarial_param_budget.json#shared_critic_n_params`),
    > bringing the effective adversarial budget to approximately 2.5x10^5 parameters per WGAN model
    > (generator + shared critic). The non-adversarial baselines (VAE 562 params, AR(2) 3 params)
    > carry only their generator-side parameter count. The per-baseline comparison:
    >
    > | classical baseline | generator parameter count | adversarial setup | Welch t-test p (vs iqp_sel_55, OD-EMD) | Cohen's d |
    > |---|---|---|---|---|
    > | wgan_mlp | 74 | generator + 250881 shared critic | (cited from `welch_pairwise.json#pairs[?quantum=='iqp_sel_55_repro' && classical=='wgan_mlp' && scale=='OD'].welch_p`) | (`welch_pairwise.json#pairs[..].cohen_d`) |
    > | wgan_cnn | 73 | generator + 250881 shared critic | (`welch_pairwise.json` lookup) | (`welch_pairwise.json` lookup) |
    > | wgan_lstm | 78 | generator + 250881 shared critic | (`welch_pairwise.json` lookup) | (`welch_pairwise.json` lookup) |
    > | vae | 562 | non-adversarial (ELBO) | (`welch_pairwise.json` lookup) | (`welch_pairwise.json` lookup) |
    > | ar | 3 (closed-form) | non-adversarial (Yule-Walker) | (`welch_pairwise.json` lookup) | (`welch_pairwise.json` lookup) |
    >
    > (Generator parameter counts per `results/model_info.json#models[*].parameter_count`
    > (also recorded as `total_params` in `classical_architectures.json#models[*].total_params`).
    > Shared critic parameter count 250881 per
    > `total_adversarial_param_budget.json#shared_critic_n_params`.
    > Welch t-test p-values + Cohen's d per `welch_pairwise.json#pairs[*]`,
    > computed two-sided, n=5 per group, equal_var=False.)
    >
    > **Aggregate summary** (Path A, anchored at `welch_pairwise.json#summaries` and `#strong_claim_thresholds`):
    > - Floor Welch p across all 20 quantum-classical pairs (4 quantum
    >   variants × 5 classical baselines, OD-EMD): **p > 0.36** — no pair
    >   shows a statistically significant OD-EMD difference. Anchored at
    >   `welch_pairwise.json#strong_claim_thresholds.floor_welch_p_OD`.
    > - Ceiling |Cohen's d| across the same 20 pairs (OD-EMD): **|d| ≤ 0.65**.
    >   Anchored at `welch_pairwise.json#strong_claim_thresholds.ceiling_abs_cohen_d_OD`.
    > - On log-return EMD post-r3 correction (un-standardize-fake recipe
    >   per `pipeline-review-r3.md` §2): AR (3 params, closed-form
    >   Yule-Walker MLE) leads at **0.003**; quantum and WGAN baselines
    >   and VAE cluster in **0.007-0.016** with no statistically meaningful
    >   quantum-vs-WGAN separation on this marginal distribution. The
    >   pre-fix `statistical-honesty-r3.md` §3b Welch tests (`p ≤ 0.014`,
    >   `d ≤ -2.6`) were computed on the broken (scale-mismatched) LR-EMD
    >   column and DO NOT carry post-fix — see `peer_review_remediation.md`
    >   Plan 14-16 r3-process retraction subsection for the full retraction.
    >   Per-model corrected LR-EMD anchors at
    >   `matched2000_dualscale.json#aggregates[*, scale='log_return', metric_name='emd'].mean`.
    >
    > Aggregate sources: column 1 (OD raw-sample EMD) and column 2
    > (log-return raw-sample EMD, post-r3 correction) cite
    > `matched2000_dualscale.json#aggregates`; column 3 (50-bin
    > histogram-density EMD, OD scale, post-r3 reformulation) cites
    > `distribution_emd.json#aggregates` under schema v2. See
    > `reconciliation_note.md`'s `## EMD comparable across metric variants
    > (matched 2000ep budget)` section for the full 3-column table and
    > `peer_review_remediation.md` Plan 14-16 section for the forensic
    > disclosure of the two corrected metric bugs.
    >
    > **Note on R1-M1 framing.** The R1-M1 table row above (at the
    > `## Reviewer 1 — Major Issues` table) describes WHAT WAS DONE at
    > the matched-budget step (added baselines, parameter-count-controlled
    > comparison table). This section asserts WHAT THE RESULT IS now that
    > the matched comparisons are run and the two r3 metric bugs are
    > closed. The two are complementary: the R1-M1 row stays the
    > "we ran the comparison" entry; this section provides the result.

    Step B2 — Append DTW addendum subsection INSIDE the new H2 section
    `## Parametric-efficiency equivalence (post-r3 corrected metrics)`:

    Append as an H3 subsection `### DTW addendum (Plan 14-16)` at the
    end of the new H2 (after the `**Note on R1-M1 framing.**` paragraph,
    BEFORE the next H2 `## Completeness sweep (Plans 14-09 .. 14-11)`).
    The H3 placement is parallel to the H3-style cross-references inside
    sibling H2s in this same doc (e.g., the `### ...` subsections inside
    `## Marginal-convergence finding (post-r2 investigation)`).

    Insert verbatim (this is the user-approved tightened framing —
    do NOT hedge, do NOT re-draft):

    > ### DTW addendum (Plan 14-16)
    >
    > DTW addendum (Plan 14-16): Under matched-budget evaluation, all
    > four quantum variants achieve OD-scale DTW of 0.298–0.302,
    > beating the Orlandi et al. reference (1.954) by ~6.5x. On
    > log-return DTW, every quantum variant outperforms every WGAN
    > baseline (wgan_lstm 1.58, wgan_mlp 2.62, wgan_cnn 6.86) and the
    > AR baseline (7.70). VAE's LR-DTW of 0.088 reflects posterior
    > collapse (std=0.0004, see 14-15 marginal-convergence finding)
    > and is not interpreted as evidence of model quality. The
    > manuscript main-text DTW (0.6843) is the pre-v1.0 best-case
    > iqp_sel_55 evaluation; the matched-2000ep mean (~0.30 OD-scale,
    > ~0.99 LR-scale) reflects honest evaluation under the
    > strict-accept gate while preserving the Orlandi-improvement
    > narrative.
    >
    > Per-model DTW provenance: every OD-scale literal in this
    > addendum (0.298 V2, 0.302 wgan_mlp / iqp_sel_55, ~0.30 quantum
    > cluster mean) resolves to
    > `matched2000_dualscale.json#aggregates[*, scale='OD', metric_name='dtw_mean'].mean`;
    > every log-return literal (1.58 wgan_lstm, 2.62 wgan_mlp,
    > 6.86 wgan_cnn, 7.70 ar, 0.088 vae, ~0.99 quantum cluster mean)
    > resolves to
    > `matched2000_dualscale.json#aggregates[*, scale='log_return', metric_name='dtw_mean'].mean`
    > under the v2.1 gate's 3-decimal ε-neighborhood (ε ≈ 0.005). The
    > Orlandi reference DTW=1.954 at `main (4) copy.tex:191` and the
    > manuscript headline DTW=0.6843 at `main (4) copy.tex:190,266` +
    > `supp_material.tex:290` are clearly labeled as historical-reference
    > literals (NOT current-pipeline emissions) — see
    > `peer_review_remediation.md` Plan 14-16 DTW phantom asymmetry
    > section for the full forensic disclosure (mechanism + evidence +
    > gate-resolution paths) AND `methods_full.md` `### DTW historical
    > context (Plan 14-16)` paragraph for the methodological framing.

    Step C — Run the v2.1 gate against `reviewer_response.md`:

    ```
    ./qgan_env/bin/python scripts/verify_number_provenance.py --target docs/reviewer_response.md
    ```

    MUST PASS. The new H2 section's surviving Path A strong-claim literals
    (`p > 0.36`, `|d| ≤ 0.65`) resolve to
    `welch_pairwise.json#strong_claim_thresholds.{floor_welch_p_OD,ceiling_abs_cohen_d_OD}`
    via the existing `_json_blobs()` walker. The Path A LR-EMD cluster
    framing literals (`0.003`, `0.007`, `0.016`) resolve to
    `matched2000_dualscale.json#aggregates[*, scale='log_return', metric_name='emd'].mean`
    as per-model anchors. The withdrawn LR-EMD-vs-WGAN literals
    (`p ≤ 0.014`, `d ≤ -2.6`) must NOT appear in the document — the gate
    correspondingly does NOT need to resolve them.
    The 74 / 73 / 78 / 562 / 3 parameter counts resolve to
    `model_info.json#models[*].parameter_count` (and to
    `classical_architectures.json#models[*].total_params`). The 250881 shared
    critic parameter count resolves to
    `total_adversarial_param_budget.json#shared_critic_n_params`. The
    ~2.5x10^5 adversarial-budget figure is a one-significant-figure approximation
    of `generator_n_params + 250881` for any WGAN variant — the v2.1 gate's
    ε-neighborhood resolves the integer leaf 250881 directly; the exponent
    form is resolved against the same leaf via the gate's scientific-notation
    handling.

    Step D — Commit:

    `docs(14-16): reviewer_response.md — preserve R1-M1 row + insert Parametric-efficiency equivalence H2 section (post-r3 corrected metrics + JSON-anchored Welch literals)`
  </action>
  <verify>
    <automated>./qgan_env/bin/python -c "import pathlib, sys; rr = pathlib.Path('docs/reviewer_response.md').read_text(); errs = []; ('55 quantum parameters' in rr) or errs.append('reviewer_response.md missing 55-quantum-parameters strong-claim'); ('Welch' in rr) or errs.append('reviewer_response.md missing Welch t-test reference'); ('p > 0.36' in rr) or errs.append('reviewer_response.md missing p > 0.36 floor literal'); ('|d| ≤ 0.65' in rr or '|d| <= 0.65' in rr or '|Cohen' in rr) or errs.append('reviewer_response.md missing |d| <= 0.65 OD ceiling literal'); ('p ≤ 0.014' not in rr and 'p &lt;= 0.014' not in rr) or errs.append('Path A violation: reviewer_response.md still contains withdrawn p <= 0.014 LR-EMD literal'); ('d ≤ -2.6' not in rr and 'd <= -2.6' not in rr) or errs.append('Path A violation: reviewer_response.md still contains withdrawn d <= -2.6 LR-EMD literal'); ('Parametric-efficiency equivalence' in rr) or errs.append('reviewer_response.md missing Parametric-efficiency equivalence H2 section'); ('Marginal-convergence finding' in rr) or errs.append('reviewer_response.md 14-15 marginal-convergence subsection missing — must be preserved'); ('Added matched-parameter classical WGAN-GP' in rr) or errs.append('reviewer_response.md R1-M1 row not preserved verbatim'); ('No statistically meaningful quantum-vs-WGAN separation' in rr) or errs.append('reviewer_response.md missing Path A no-separation framing on LR-EMD'); ('0.003' in rr) or errs.append('reviewer_response.md missing Path A AR LR-EMD cluster anchor 0.003'); ('0.007' in rr) or errs.append('reviewer_response.md missing Path A cluster-lower-bound LR-EMD anchor 0.007'); ('0.016' in rr) or errs.append('reviewer_response.md missing Path A cluster-upper-bound LR-EMD anchor 0.016'); ('### DTW addendum (Plan 14-16)' in rr) or errs.append('reviewer_response.md missing ### DTW addendum (Plan 14-16) H3 subsection'); ('0.298' in rr) or errs.append('reviewer_response.md DTW addendum missing 0.298 V2 OD-DTW literal'); ('0.302' in rr) or errs.append('reviewer_response.md DTW addendum missing 0.302 wgan_mlp/iqp_sel_55 OD-DTW upper-bound literal'); ('1.954' in rr) or errs.append('reviewer_response.md DTW addendum missing 1.954 Orlandi reference literal'); ('1.58' in rr) or errs.append('reviewer_response.md DTW addendum missing 1.58 wgan_lstm LR-DTW literal'); ('2.62' in rr) or errs.append('reviewer_response.md DTW addendum missing 2.62 wgan_mlp LR-DTW literal'); ('6.86' in rr) or errs.append('reviewer_response.md DTW addendum missing 6.86 wgan_cnn LR-DTW literal'); ('7.70' in rr) or errs.append('reviewer_response.md DTW addendum missing 7.70 ar LR-DTW literal'); ('0.088' in rr) or errs.append('reviewer_response.md DTW addendum missing 0.088 vae LR-DTW literal'); ('0.6843' in rr) or errs.append('reviewer_response.md DTW addendum missing 0.6843 manuscript-headline historical-reference literal'); ('0.30 OD-scale' in rr or '~0.30 OD' in rr) or errs.append('reviewer_response.md DTW addendum missing ~0.30 OD-scale matched-budget summary literal'); ('0.99 LR-scale' in rr or '~0.99 LR' in rr) or errs.append('reviewer_response.md DTW addendum missing ~0.99 LR-scale matched-budget summary literal'); ('6.5x' in rr or '6.5×' in rr) or errs.append('reviewer_response.md DTW addendum missing 6.5x Orlandi-improvement ratio'); ('std=0.0004' in rr) or errs.append('reviewer_response.md DTW addendum missing std=0.0004 VAE posterior-collapse caveat (14-15 cross-ref)'); print('errors', errs); sys.exit(0 if not errs else 1)" &amp;&amp; ./qgan_env/bin/python scripts/verify_number_provenance.py --target docs/reviewer_response.md &amp;&amp; [ -z "$(git diff --stat core/)" ] &amp;&amp; [ -z "$(git diff verify_number_provenance.py)" ]</automated>
  </verify>
  <acceptance_criteria>
    - `docs/reviewer_response.md` R1-M1 row PRESERVED VERBATIM at line 36 (no edit to the existing single-cell summary)
    - NEW H2 section `## Parametric-efficiency equivalence (post-r3 corrected metrics)` INSERTED between the existing `## Marginal-convergence finding (post-r2 investigation)` H2 and the existing `## Completeness sweep (Plans 14-09 .. 14-11)` H2
    - New H2 asserts Path A strong claim verbatim including: `55 quantum parameters`, `Welch p > 0.36`, `|d| ≤ 0.65`, `n=5`, `73-562 generator params`, `~2.5x10^5 adversarial budget`, `generator + 250881-parameter shared critic`, the AR-leads-at-0.003 framing, the `quantum/WGAN/VAE cluster in 0.007-0.016` framing, and the `No statistically meaningful quantum-vs-WGAN separation on marginal log-return distribution` retraction wording
    - The H2 MUST NOT contain the withdrawn LR-EMD-vs-WGAN strong-claim literals (`p ≤ 0.014`, `d ≤ -2.6`, `empirical range d in [-5.22, -2.63]`, `significantly outperform every WGAN baseline`) — verify-gate asserts these are absent
    - The DTW addendum subsection (T4 Step B2) is PRESERVED VERBATIM including all 11 DTW literals (0.298, 0.302, 1.954, 1.58, 2.62, 6.86, 7.70, 0.088, 0.6843, ~0.30, ~0.99) and the 6.5x Orlandi-improvement ratio — these survive Path A unchanged because DTW JSON aggregates are byte-stable through T1's EMD-only fix
    - Per-baseline OD-EMD equivalence table cites `welch_pairwise.json#pairs[?scale=='OD']` for Welch p + Cohen d (Path A: OD-only); cites `model_info.json#models[*].parameter_count` for generator counts (74, 73, 78, 562, 3); cites `total_adversarial_param_budget.json` for adversarial budgets; the Path A LR-EMD cluster framing (`AR 0.003`, `quantum/WGAN/VAE cluster 0.007-0.016`) cites `matched2000_dualscale.json#aggregates[*, scale='log_return', metric_name='emd'].mean` directly (per-model corrected aggregates)
    - v2.1 gate PASSES on `reviewer_response.md` (all literals resolve via JSON-anchored paths, NOT fuzzy ε-match)
    - All prior 14-13 + 14-14 + 14-15 sections in `reviewer_response.md` preserved verbatim (R1-XX / R2-XX rows other than untouched R1-M1, Marginal-convergence finding H2, Cross-Cutting Provenance Note, Completeness sweep H2, etc.)
    - `git diff --stat core/` is empty (D-14-22 preserved); `git diff verify_number_provenance.py` is empty (D-14-16 preserved)
  </acceptance_criteria>
  <done>reviewer_response.md updated (Path A): R1-M1 row preserved verbatim; new H2 `## Parametric-efficiency equivalence (post-r3 corrected metrics)` section inserted asserting OD-EMD equivalence (p > 0.36, |d| ≤ 0.65, n=5) + Path A LR-EMD cluster framing (AR 0.003 leads, quantum/WGAN/VAE cluster 0.007-0.016, no quantum-vs-WGAN separation) + DTW dominance (LR-DTW quantum-beats-everyone + 6.5x OD-DTW Orlandi improvement); DTW addendum preserved VERBATIM; withdrawn LR-EMD-vs-WGAN literals (p ≤ 0.014, d ≤ -2.6) absent; per-baseline OD-equivalence table cites welch_pairwise.json#pairs[?scale=='OD']; v2.1 gate PASSES; D-14-22 + D-14-16 preserved.</done>
</task>

<task type="auto">
  <name>Task 5: methods_full.md correction paragraphs + DTW historical-context paragraph + reconciliation_note.md C-3 extension + 4th DTW column added to comparable-variants table + run_model_info.py re-emit</name>
  <files>run_model_info.py, docs/methods_full.md, docs/reconciliation_note.md</files>
  <read_first>
    - run_model_info.py:220-302 (the existing 3-column comparable-variants table emit logic from 14-15 T2; column sources are unchanged structurally — just re-read updated JSONs; optionally append 4th `fake_in_range_mass_mean` disclosure column for full transparency to reviewers)
    - docs/methods_full.md (CURRENT STATE — locate the OD-scale evaluation section discussing per-model EMD results, likely §3.x.d carried over from 14-14 T3 and extended in 14-15 T4; locate the histogram-density EMD section from 14-15 T4 marginal-convergence cross-reference)
    - docs/reconciliation_note.md (CURRENT STATE — confirm the C-3 disclosure paragraph location (from 14-13 T3, extended in 14-15 T2); the 3-column comparable-variants table column 2 + 3 values regenerate from the corrected JSONs automatically when `scripts/run_model_info.py` is re-run)
    - results/matched2000_dualscale.json (POST-T1; the corrected log-return-scale aggregates source for the methods_full.md correction paragraph)
    - results/distribution_emd.json (POST-T2; the corrected histogram-density EMD aggregates under schema v2 source for the methods_full.md shared-edges paragraph and the reconciliation_note.md C-3 extension)
  </read_first>
  <action>
    Update `docs/methods_full.md` with the two correction
    paragraphs (R3-CR-2 + R3-CR-1), re-emit `docs/reconciliation_note.md`
    via `scripts/run_model_info.py` (the existing 14-15 emit logic re-reads
    the corrected JSONs automatically), and extend the C-3 disclosure
    paragraph in `reconciliation_note.md` with one more disclosure sentence.

    Step A — Append `## Log-return scale correction (Plan 14-16)`
    paragraph to `docs/methods_full.md` OD-scale evaluation
    section:

    Locate the OD-scale evaluation section (likely §3.x.d carried over
    from 14-14 T3, possibly extended in 14-15 T4). Append at its end:

    > **Log-return scale correction (Plan 14-16).** The pre-Plan-14-16
    > emit of `matched2000_dualscale.json`'s log-return-scale EMD column
    > compared the RAW per-step `log_delta` (std≈0.022) against the
    > STANDARDIZED fake samples `r_norm` (std≈1) — a 50× scale mismatch
    > inherited from `scripts/run_dualscale_fidelity.py` since Plan 14-08 (per
    > `peer-review-r3/code-review-r3.md` R3-HI-1 + `pipeline-review-r3.md`
    > §2). Plan 14-16 applies the canonical un-standardize-fake recipe
    > from `pipeline-review-r3.md` §2 lines 124-135: at
    > `run_matched2000_dualscale.py:368-372` (and the sister
    > site at `run_distribution_emd.py:_real_references` /
    > `_fake_log_return_flat` at `:144-169`), the fake-side
    > `trans_flat_raw = r["transformed"] * sigma + mu` un-standardizes
    > the synthetic log-returns back to raw scale before comparison
    > against the unchanged raw `real_log_delta`. The standardize-real
    > alternative is mathematically scale-matched but inflates EMD by
    > ~47× and does not match the §2 anchor table; un-standardize-fake
    > produces EMD in raw log-return units that match §2 exactly.
    > The corrected aggregates anchor at AR 0.00294 best overall (3-param
    > closed-form Yule-Walker MLE fits the linear-Gaussian marginal),
    > wgan_cnn 0.00711, wgan_mlp 0.01031, wgan_lstm 0.01272, V3 0.01432,
    > iqp_sel_55_repro / V1 0.01497, V2 0.01502, VAE 0.01583 worst. The
    > post-fix LR-EMD ranking is structurally different from the pre-fix
    > narrative: every WGAN beats every quantum on the corrected LR-EMD
    > scale, and quantum/WGAN/VAE cluster in 0.007-0.016 with no
    > statistically meaningful quantum-vs-WGAN separation on this marginal
    > distribution. The pre-fix `statistical-honesty-r3.md` §3b Welch
    > tests (p ≤ 0.014, Cohen d ≤ -2.6) were computed on the broken
    > pre-T1 LR-EMD column and DO NOT carry post-fix; this is the Path A
    > r3-process retraction documented at `peer_review_remediation.md`
    > Plan 14-16 r3-process retraction subsection. The OD-scale rows are
    > byte-identical pre/post correction because the OD inverse via
    > `inverse_logreturns` already cumsum-exps to OD price scale where
    > no standardization-vs-raw mismatch exists; the OD-EMD equivalence
    > claim (Welch p > 0.36, |d| ≤ 0.65, n=5) is unaffected.

    Step B — Append `## Shared-edges formulation (Plan 14-16)` paragraph
    to `docs/methods_full.md` histogram-density EMD section:

    Locate the histogram-density EMD section (from 14-15 T4). Append at
    its end:

    > **Shared-edges formulation (Plan 14-16).** The original Plan 14-15
    > emit of `distribution_emd.json`'s histogram-density EMD used
    > `np.histogram(..., density=True)` for both real and fake, which
    > renormalizes each histogram independently over the in-range portion
    > only — silently dropping out-of-range fake mass and re-normalizing
    > the remainder (per `peer-review-r3/code-review-r3.md` R3-CR-1).
    > This rewarded narrow-collapse distributions (VAE posterior collapse,
    > std=0.0004 — Agent 5's empirical confirmation at §H3) and uncapped
    > distributions (WGAN-CNN, 94% out-of-range at log-return scale).
    > Plan 14-16 replaces this formulation with: (a) `density=False` for
    > both histograms; (b) edges derived from REAL ONLY; (c) both
    > histograms normalized to total-mass=1 OVER THE SAME EDGE SET (no
    > per-distribution renormalization); (d) out-of-range fake mass
    > disclosed separately as `fake_in_range_mass = fake_hist.sum() /
    > len(fake)` (where 1.0 = no truncation; 0.06 = 94% out-of-range as
    > in WGAN-CNN's case at log-return scale). The schema bumps from
    > `'distribution-emd v1 (Phase 14 plan 14-15)'` to
    > `'distribution-emd v2 (Phase 14 plan 14-16)'`. The corrected
    > aggregates cite `distribution_emd.json#aggregates[*, scale='OD']`
    > under schema v2 and surface in `reconciliation_note.md`'s 3-column
    > comparable-variants table column 3.

    Step C — Re-emit `docs/reconciliation_note.md`:

    Run the updated `scripts/run_model_info.py` (the existing 14-15 emit
    logic re-reads the corrected JSONs automatically):

    ```
    ./qgan_env/bin/python scripts/run_model_info.py
    ```

    Confirm the 3-column comparable-variants table column 1 (OD raw-sample
    EMD) is byte-identical to its prior state (per T1's OD-preservation
    invariant); column 2 (log-return raw-sample EMD) values regenerate
    from corrected `matched2000_dualscale.json`; column 3 (50-bin
    histogram-density EMD on OD scale) values regenerate from corrected
    `distribution_emd.json` under schema v2.

    Step D — Extend the C-3 disclosure paragraph in
    `docs/reconciliation_note.md`:

    Locate the C-3 metric-redefinition disclosure paragraph (originally
    from 14-13 T3, extended in 14-15 T2). Append one more disclosure
    sentence at its end:

    > Plan 14-16 r3 remediation: column 2 (log-return raw-sample EMD)
    > corrected for a standardization scale mismatch in
    > `run_matched2000_dualscale.py:368-372` AND
    > `run_distribution_emd.py:_real_references` at `:144-153` (R3-HI-1
    > sister sites under the same finding ID in
    > `peer-review-r3/code-review-r3.md` §H3) via the un-standardize-fake
    > recipe from `pipeline-review-r3.md` §2; column 3 (50-bin
    > histogram-density EMD on OD scale) reformulated with shared-edges-
    > from-real to eliminate the per-distribution renormalization bias
    > documented in `peer-review-r3/code-review-r3.md` R3-CR-1 (the
    > formulation citation in this disclosure paragraph is now updated to
    > match `distribution_emd.json#metric_formulation` under schema v2
    > verbatim — see Plan 14-16 deliverables). Plan 14-16 r3 remediation
    > also revealed that the pre-fix LR-EMD column had inverted the
    > quantum-vs-classical ranking due to R3-CR-2 scale mismatch; the
    > corrected column places AR (3-parameter Yule-Walker baseline) first
    > at 0.003 and shows quantum/WGAN/VAE clustering in the 0.007-0.016
    > band with no statistically meaningful separation. See
    > `peer_review_remediation.md` Plan 14-16 r3-process disclosure for
    > full retraction.

    Step E — Optionally update C-3 to reflect the new `METRIC_FORMULATION`
    string verbatim from T2's edit. Aim for word-for-word match with the
    new constant.

    Step E2 — Append `### DTW historical context (Plan 14-16)` paragraph
    to `docs/methods_full.md` OD-scale evaluation section appendix:

    Locate the OD-scale evaluation section (the same section where Step A
    appended `## Log-return scale correction (Plan 14-16)` paragraph).
    Append a NEW H3 subsection AFTER the Log-return scale correction
    paragraph. Tone: methodological (NOT promotional) — the methods
    section documents the pipeline's DTW evaluation contract, the
    historical headline's provenance, and the matched-budget per-model
    DTW values in equal weight to the EMD reporting; the favorable
    LR-DTW finding is acknowledged so methods_full.md does not
    UNDERCLAIM relative to `reviewer_response.md`'s tightened addendum,
    but framed as a measured methodological observation rather than a
    promotional claim.

    Insert verbatim:

    > ### DTW historical context (Plan 14-16)
    >
    > Dynamic Time Warping (DTW) is computed by the byte-frozen emitter
    > at `core/eval.py:38-89` (D-14-22) using the
    > `dtaidistance.dtw.distance` function with default Sakoe-Chiba
    > windowing and Euclidean local cost; per-(model, seed, scale) values
    > are persisted in
    > `results/matched2000_dualscale.json#rows[*, metric_name='dtw']`
    > since Plan 14-11; per-(model_kind, scale) aggregates (mean ± std,
    > n=5 seeds per cell, ddof=1) are in
    > `matched2000_dualscale.json#aggregates[*, metric_name='dtw_mean']`.
    > The manuscript headline DTW=0.6843 at `main (4) copy.tex:190` +
    > `:266` + `supp_material.tex:290` originates from a pre-v1.0
    > best-case iqp_sel_55 evaluation pipeline; this value is not
    > re-emitted by the current matched-budget contract under the
    > strict-accept gate (D-14-13) — it is a labeled historical-reference
    > literal preserved for narrative continuity with the LaTeX
    > read-only sources (D-14-18). The Orlandi et al. reference
    > DTW=1.954 at `main (4) copy.tex:191` is a labeled external
    > benchmark, also not re-emitted.
    >
    > Under the current matched-2000ep evaluation contract, OD-scale
    > DTW means (n=5 seeds per cell) cluster in the range 0.298–0.302
    > across the four quantum variants (V1=0.300, V2=0.298, V3=0.299,
    > iqp_sel_55_repro=0.302), mixed with the classical generator cluster
    > (wgan_lstm=0.301, wgan_mlp=0.302) at the same order; the AR
    > baseline at 0.371 and VAE at 0.307 are slightly higher, and
    > wgan_cnn at 0.438 ± 0.255 is the outlier. The OD-scale ordering
    > is statistically non-significant by design under the strict-accept
    > gate; no equivalence test is computed for DTW. On log-return
    > scale, the per-model DTW means show a more separated cluster
    > structure: all four quantum variants (range 0.940–1.122) report
    > log-return DTW lower than every WGAN baseline (wgan_lstm=1.581,
    > wgan_mlp=2.624, wgan_cnn=6.863) and the autoregressive baseline
    > (ar=7.699). VAE's log-return DTW of 0.088 is an outlier driven
    > by posterior collapse (sample std ≈ 0.0004 vs real ≈ 0.0217;
    > documented in the 14-15 marginal-convergence finding and the
    > R3-CR-1 disclosure above) and is reported but not interpreted as
    > evidence of model quality.
    >
    > Relative to the Orlandi et al. reference DTW=1.954, the
    > matched-2000ep mean OD-scale DTW of ~0.30 across the quantum
    > cluster represents an approximately 6.5× lower DTW under the
    > current evaluation contract. This is consistent with — and
    > methodologically more conservative than — the pre-v1.0 headline
    > 0.6843 vs 1.954 = ~2.85× framing, in the sense that the
    > matched-budget mean (under n=5 seeds with disclosed ± std) is a
    > more defensible estimator than a single best-case run. The
    > methods-section framing for DTW therefore parallels the EMD
    > framing: the headline historical value is preserved for narrative
    > continuity, the current matched-budget contract reports per-
    > model means with seed variance, and the Orlandi-improvement
    > observation holds at the matched-budget level. Full forensic
    > disclosure of the historical-vs-current asymmetry is in
    > `peer_review_remediation.md`'s `## Plan 14-16 — DTW phantom
    > asymmetry (third historical-vs-current case)` section; the
    > reviewer-facing summary is in `reviewer_response.md`'s
    > `### DTW addendum (Plan 14-16)` subsection inside the
    > `## Parametric-efficiency equivalence (post-r3 corrected metrics)` H2.

    Step E3 — Append DTW disclosure sentence + 4th DTW column to
    `docs/reconciliation_note.md` C-3 extension:

    Inside the existing C-3 paragraph extension (from Step D above),
    append one more disclosure sentence:

    > DTW addendum (Plan 14-16): the comparable-variants table above
    > is extended with a 4th column reporting per-(model_kind, scale=OD)
    > DTW mean ± std from `matched2000_dualscale.json#aggregates[*, scale='OD', metric_name='dtw_mean']`
    > (n=5 seeds per cell, byte-stable through Plan 14-16 since the
    > DTW emitter at `core/eval.py:38-89` is BYTE-UNTOUCHED
    > under D-14-22). The DTW per-baseline means surface the
    > Orlandi-improvement ratio (~6.5× lower than the Orlandi et al.
    > reference DTW=1.954) and the LR-scale quantum-vs-WGAN/AR
    > dominance disclosed in `peer_review_remediation.md` Plan 14-16
    > DTW phantom asymmetry section. See `methods_full.md` `### DTW
    > historical context (Plan 14-16)` paragraph for the methodological
    > framing.

    Then ALSO rename the comparable-variants table H2 from
    `## EMD comparable across metric variants (matched 2000ep budget)`
    to `## EMD + DTW comparable across metric variants (matched 2000ep budget)`
    to reflect the 4th DTW column addition (this is the rename T7 Step A2
    cross-references). Re-run `scripts/run_model_info.py` AFTER renaming the
    section header to confirm the emitter does not collide with the new
    section name (if the emit logic targets the OLD header verbatim, update
    `run_model_info.py:220-302` to write to the renamed header
    AND emit the 4th DTW column — preserve `core/` byte-freeze).

    Step F — Run the v2.1 gate against `methods_full.md` and
    `reconciliation_note.md`:

    ```
    ./qgan_env/bin/python scripts/verify_number_provenance.py --target docs/methods_full.md
    ./qgan_env/bin/python scripts/verify_number_provenance.py --target docs/reconciliation_note.md
    ```

    All MUST PASS.

    Step G — Commit:

    `docs(14-16): methods_full.md correction paragraphs (R3-CR-2 + R3-HI-1 + R3-CR-1) + reconciliation_note.md C-3 extension via run_model_info.py re-emit`
  </action>
  <verify>
    <automated>./qgan_env/bin/python scripts/run_model_info.py &amp;&amp; ./qgan_env/bin/python -c "import pathlib, sys; errs = []; mf = pathlib.Path('docs/methods_full.md').read_text(); ('Log-return scale correction (Plan 14-16)' in mf) or errs.append('methods_full.md missing Log-return scale correction (Plan 14-16) paragraph'); ('Shared-edges formulation (Plan 14-16)' in mf) or errs.append('methods_full.md missing Shared-edges formulation (Plan 14-16) paragraph'); ('R3-CR-2' in mf or 'norm_log_delta' in mf) or errs.append('methods_full.md does not reference R3-CR-2 mechanism'); ('R3-CR-1' in mf or 'fake_in_range_mass' in mf) or errs.append('methods_full.md does not reference R3-CR-1 mechanism'); ('R3-HI-1' in mf or '_real_references' in mf) or errs.append('methods_full.md does not reference R3-HI-1 sister-fix'); rn = pathlib.Path('docs/reconciliation_note.md').read_text(); ('Plan 14-16 r3 remediation' in rn) or errs.append('reconciliation_note.md C-3 extension missing Plan 14-16 r3 remediation reference'); ('R3-CR-2' in rn or 'standardization scale mismatch' in rn) or errs.append('reconciliation_note.md C-3 extension missing R3-CR-2 reference'); ('R3-CR-1' in rn or 'shared-edges' in rn) or errs.append('reconciliation_note.md C-3 extension missing R3-CR-1 reference'); ('## EMD comparable across metric variants' in rn) or errs.append('reconciliation_note.md 14-15 3-column table missing — must be preserved'); ('## EMD (OD scale)' in rn) or errs.append('reconciliation_note.md 14-13 OD-scale headline table missing — must be preserved'); ('DTW historical context (Plan 14-16)' in mf or 'DTW historical context' in mf) or errs.append('methods_full.md missing DTW historical context (Plan 14-16) paragraph (T8-equivalent DTW disclosure scope addition)'); ('0.6843' in mf) or errs.append('methods_full.md DTW paragraph missing 0.6843 historical-value reference'); ('Orlandi' in mf or '1.954' in mf) or errs.append('methods_full.md DTW paragraph missing Orlandi reference cross-cite'); ('Plan 14-16 DTW disclosure' in rn or 'DTW addendum (Plan 14-16)' in rn) or errs.append('reconciliation_note.md missing DTW disclosure sentence in C-3 extension'); ('DTW' in rn) or errs.append('reconciliation_note.md missing 4th DTW column in comparable-variants table'); print('errors', errs); sys.exit(0 if not errs else 1)" &amp;&amp; ./qgan_env/bin/python scripts/verify_number_provenance.py --target docs/methods_full.md &amp;&amp; ./qgan_env/bin/python scripts/verify_number_provenance.py --target docs/reconciliation_note.md &amp;&amp; [ -z "$(git diff --stat core/)" ] &amp;&amp; [ -z "$(git diff verify_number_provenance.py)" ]</automated>
  </verify>
  <acceptance_criteria>
    - `docs/methods_full.md` carries new "Log-return scale correction (Plan 14-16)" paragraph in OD-scale evaluation section, referencing R3-CR-2 + R3-HI-1 sister-fix at both `run_matched2000_dualscale.py:368-372` and `run_distribution_emd.py:_real_references:144-153`
    - `docs/methods_full.md` carries new "Shared-edges formulation (Plan 14-16)" paragraph in histogram-density EMD section, referencing R3-CR-1
    - `docs/reconciliation_note.md` re-emitted via `scripts/run_model_info.py`: column 1 (OD raw-sample EMD) byte-identical to prior state; columns 2 + 3 reflect corrected JSONs
    - `docs/reconciliation_note.md` C-3 disclosure paragraph extended with the Plan 14-16 r3 remediation disclosure sentence referencing R3-CR-2 + R3-HI-1 + R3-CR-1
    - v2.1 gate PASSES on `methods_full.md` AND `reconciliation_note.md`
    - All prior 14-13 + 14-14 + 14-15 sections in both docs preserved verbatim
    - `git diff --stat core/` is empty (D-14-22 preserved); `git diff verify_number_provenance.py` is empty (D-14-16 preserved)
  </acceptance_criteria>
  <done>methods_full.md carries Plan 14-16 correction paragraphs (R3-CR-2 + R3-HI-1 sister-fix + R3-CR-1); reconciliation_note.md re-emitted via run_model_info.py with corrected columns 2+3; C-3 disclosure paragraph extended; v2.1 gate PASSES on both docs; D-14-22 + D-14-16 preserved.</done>
</task>

<task type="auto">
  <name>Task 6: Re-render `cross_model_emd` figure + QQ overlay cross-check + v2.1 gate run on 10 paper-facing docs</name>
  <files>run_figure_suite.py, figures/cross_model_emd.png, figures/cross_model_emd.pdf, figures/cross_model_emd.json, figures/qq_overlay.json</files>
  <read_first>
    - run_figure_suite.py (grep for `cross_model_emd\|render_cross_model_emd` to locate the bar figure render function; verify it reads aggregates from `matched2000_dualscale.json` rather than from per-seed samples)
    - run_figure_suite.py (grep for `render_qq_overlay` from 14-15 T3 — locate the function around the existing per-model `render_qq_plot` at `:403-433` plus the new function added in 14-15; verify its data sources to determine whether the overlay reads from `matched2000_dualscale.json` or from `samples.npy` directly)
    - figures/cross_model_emd.json (current state pre-T4 to confirm the schema; the post-T4 emit must preserve the same top-level fields including CR-1 `model_order_color_keys`)
    - figures/qq_overlay.json (current state from 14-15 T3 to confirm the fields preserved verbatim under default no-structural-change outcome)
    - verify_number_provenance.py (READ-ONLY; the v2.1 gate from 14-14 T1; D-14-16 byte-frozen; T4 invokes it against all 10 paper-facing docs)
    - results/matched2000_dualscale.json (POST-T1 corrected aggregates; source for `cross_model_emd` bar values)
    - results/distribution_emd.json (POST-T2 corrected aggregates under schema v2; cross-check whether `cross_model_emd` figure also renders a histogram-density bar group — if yes, that bar group also regenerates)
  </read_first>
  <action>
    Re-render the `cross_model_emd` figure against the corrected
    aggregates, cross-check the QQ overlay for incidental data
    dependencies, and run the v2.1 gate against all 10 paper-facing docs.

    Step A — Cross-check the QQ overlay for incidental dependence on
    `matched2000_dualscale.json`:

    Grep `scripts/run_figure_suite.py` for the `render_qq_overlay`
    function added in 14-15 T3. Trace its data sources: does it read
    samples via `reconstruct_od(repo, model, seed)` from `samples.npy`,
    or does it cite values from `matched2000_dualscale.json#aggregates`?

    Per the 14-15 design (verified in `14-15-PLAN.md` interfaces at
    `:298-306`), the overlay reads from `samples.npy` directly via
    `reconstruct_od`. Independently verify by reading the function body
    in the current `scripts/run_figure_suite.py`. If the verification confirms
    no `matched2000_dualscale.json` dependency, record `structural_change:
    false` and proceed to Step B without re-rendering the overlay.

    If the verification finds any dependence (e.g., a `convergence_stats`
    number was sourced from `matched2000_dualscale.json` rather than from
    samples directly), re-render the overlay via the existing caller loop
    invocation.

    Step B — Emit the `plan_14_16_verification` additive field on
    `qq_overlay.json`:

    Load the current `figures/qq_overlay.json` and
    append a new top-level field:

    ```
    "plan_14_16_verification": {
        "structural_change": false,  // or true if Step A found dependence
        "reason": "qq_overlay was rendered from samples.npy directly via reconstruct_od per 14-15 design; no dependence on matched2000_dualscale.json found in the convergence_stats sources",
        "checked_against": ["matched2000_dualscale.json (post-Plan-14-16-T1 corrected aggregates)", "distribution_emd.json (post-Plan-14-16-T2 schema v2)"]
    }
    ```

    All other 14-15 fields (`caption`, `convergence_stats`, `cluster_summary`,
    `data_hash`, `render_only`, `model_order_color_keys`, `sha256_png`,
    `routing_verification`) preserve verbatim. If `structural_change:
    true`, the field also carries `new_sha256_png` and `re_render_timestamp`.

    Step C — Re-render `cross_model_emd.{png,pdf,json}` against the
    corrected aggregates:

    Invoke the existing figure-suite render path that produces
    `cross_model_emd.{png,pdf,json}`. The exact invocation depends on
    the suite's CLI — likely:
    ```
    ./qgan_env/bin/python scripts/run_figure_suite.py
    ```
    (full re-run) OR a targeted flag like `--render cross_model_emd` if
    available. The figure regenerates from `matched2000_dualscale.json`
    (corrected per T1).

    Step D — Verify the OD bar group is byte-identical structurally:

    Per T1's OD-preservation invariant, the OD-EMD bar values in
    `cross_model_emd.json` (if the JSON carries explicit bar values per
    the 14-10/14-11 figure-companion schema) should be byte-identical to
    the prior emit. Compute SHA-256 over the OD subset of the JSON
    `bar_values_OD` (or equivalent field) and confirm it matches the
    prior emit via `git show HEAD:figures/cross_model_emd.json`.

    If the JSON does NOT carry explicit bar values (just rendering
    metadata), skip this byte-identical check and rely on T1's upstream
    invariant.

    Step E — Verify CR-1 sha256-seeded determinism on the re-rendered
    figure:

    Re-render `cross_model_emd` TWICE back-to-back and compare the
    `model_order_color_keys` arrays in the companion JSON across the two
    invocations. They MUST be byte-identical (the existing CR-1 contract
    from 14-13).

    Step F — Run the v2.1 gate against all 10 paper-facing docs:

    ```
    for doc in paper_blocks_framing.md paper_blocks_refs_methods.md \
               reviewer_response.md reconciliation_note.md methods_full.md \
               circuit_atlas.md completeness_sweep_manifest.md \
               training_protocol.md dataset_stats.md peer_review_remediation.md; do
      ./qgan_env/bin/python scripts/verify_number_provenance.py \
        --target docs/$doc || exit 1
    done
    ```

    All MUST PASS. The corrected `matched2000_dualscale.json` +
    `distribution_emd.json` (schema v2) are auto-walked into the v2.1
    resolution corpus via the existing `_json_blobs()` walker (no gate
    edit).

    If REPRODUCE.md at the repo root carries any paper-facing claims
    post-14-15/14-16, run the gate against it too.

    Step G — Commit:

    `docs(14-16): re-render cross_model_emd against corrected aggregates + qq_overlay plan_14_16_verification field + v2.1 gate pass on 10 paper-facing docs`

    Commit body records the QQ-overlay cross-check outcome
    (structural_change: false expected default), the cross_model_emd
    SHA-256 deltas, and the v2.1 gate PASS confirmation on each of the
    10 docs.
  </action>
  <verify>
    <automated>./qgan_env/bin/python -c "from pathlib import Path; assert Path('figures/cross_model_emd.png').exists(), 'cross_model_emd.png missing'; assert Path('figures/cross_model_emd.pdf').exists(), 'cross_model_emd.pdf missing'; assert Path('figures/cross_model_emd.json').exists(), 'cross_model_emd.json missing'; import json; j=json.loads(Path('figures/cross_model_emd.json').read_text()); errs=[]; ('model_order_color_keys' in j) or errs.append('cross_model_emd.json missing CR-1 model_order_color_keys'); import sys; print('errors',errs); sys.exit(0 if not errs else 1)" &amp;&amp; ./qgan_env/bin/python -c "import json,pathlib,sys; q=json.loads(pathlib.Path('figures/qq_overlay.json').read_text()); errs=[]; ('plan_14_16_verification' in q) or errs.append('qq_overlay.json missing plan_14_16_verification additive field'); ('caption' in q) or errs.append('qq_overlay.json caption missing — 14-15 field must be preserved'); ('convergence_stats' in q) or errs.append('qq_overlay.json convergence_stats missing — 14-15 field must be preserved'); ('cluster_summary' in q) or errs.append('qq_overlay.json cluster_summary missing — 14-15 field must be preserved'); ('routing_verification' in q) or errs.append('qq_overlay.json routing_verification missing — 14-15 field must be preserved'); print('errors',errs); sys.exit(0 if not errs else 1)" &amp;&amp; ./qgan_env/bin/python -c "import subprocess,sys; docs=['paper_blocks_framing.md','paper_blocks_refs_methods.md','reviewer_response.md','reconciliation_note.md','methods_full.md','circuit_atlas.md','completeness_sweep_manifest.md','training_protocol.md','dataset_stats.md','peer_review_remediation.md']; failed=[]; [failed.append(d) for d in docs if subprocess.run(['./qgan_env/bin/python','verify_number_provenance.py','--target',f'docs/{d}'], capture_output=True).returncode != 0]; print('failed gates:',failed); sys.exit(0 if not failed else 1)" &amp;&amp; [ -z "$(git diff --stat core/)" ] &amp;&amp; [ -z "$(git diff verify_number_provenance.py)" ]</automated>
  </verify>
  <acceptance_criteria>
    - `figures/cross_model_emd.{png,pdf,json}` re-rendered against corrected `matched2000_dualscale.json` aggregates; OD bar group structurally byte-identical (per T1 invariant); LR bar group regenerated reflecting corrected aggregates
    - `cross_model_emd.json` companion preserves CR-1 `model_order_color_keys` byte-identical across two consecutive re-renders
    - `figures/qq_overlay.json` carries new top-level `plan_14_16_verification` field with `structural_change` boolean (default false per the brief) and reason; all 14-15 fields (`caption`, `convergence_stats`, `cluster_summary`, `data_hash`, `render_only`, `model_order_color_keys`, `sha256_png`, `routing_verification`) preserved verbatim
    - If the QQ overlay cross-check found incidental dependence on `matched2000_dualscale.json` (rare per the 14-15 design), the overlay PNG/PDF are also re-rendered and `plan_14_16_verification` carries `structural_change: true` + `new_sha256_png`
    - v2.1 gate PASSES on all 10 paper-facing docs: `paper_blocks_framing.md`, `paper_blocks_refs_methods.md`, `reviewer_response.md`, `reconciliation_note.md`, `methods_full.md`, `circuit_atlas.md`, `completeness_sweep_manifest.md`, `training_protocol.md`, `dataset_stats.md`, `peer_review_remediation.md`
    - `git diff --stat core/` is empty (D-14-22 preserved); `git diff verify_number_provenance.py` is empty (D-14-16 preserved)
  </acceptance_criteria>
  <done>cross_model_emd figure re-rendered against corrected aggregates; qq_overlay structurally unchanged (default outcome) with plan_14_16_verification field appended to companion JSON; v2.1 gate PASSES on all 10 paper-facing docs; D-14-22 + D-14-16 preserved.</done>
</task>

<task type="auto">
  <name>Task 7: 14-16-SUMMARY.md + peer_review_remediation.md r3 follow-up section + DTW phantom asymmetry subsection (T8-equivalent DTW historical-asymmetry disclosure) + completeness_sweep_manifest.md Plan 14-16 section</name>
  <files>docs/peer_review_remediation.md, docs/completeness_sweep_manifest.md, .planning/phases/14-paper-revision-release-freeze/14-16-SUMMARY.md</files>
  <read_first>
    - docs/peer_review_remediation.md (read existing 14-13 finding-to-commit tables + 14-14 `## Gate v2.1 known limitations` + 14-14 `## R2 follow-up sweep` + 14-15 `## R2 follow-up — marginal-convergence finding (Plan 14-15)` sections; all preserved verbatim; the new `## Plan 14-16 — r3 forensic remediation` section appends at the bottom)
    - docs/completeness_sweep_manifest.md (read existing 14-09 / 14-10 / 14-11 / 14-12 / 14-13 / 14-14 / 14-15 sections; all preserved verbatim; the new `## Plan 14-16` section appends at the bottom)
    - .planning/phases/14-paper-revision-release-freeze/14-15-SUMMARY.md (read for the canonical SUMMARY.md template structure: Self-Check section with status PASS + 14-point verification checklist (extended from 10-point per planner-checker W3 split), Task commits section with N task SHAs + SUMMARY commit SHA, Final-state declaration line, Cross-references section)
    - $HOME/.claude/get-shit-done/templates/summary.md (the canonical GSD SUMMARY template the checker validates against)
    - .planning/phases/14-paper-revision-release-freeze/peer-review-r3/SYNTHESIS.md (cite the synthesis as the trigger for Plan 14-16 in both the peer_review_remediation r3 section and the SUMMARY's cross-references)
    - .planning/phases/14-paper-revision-release-freeze/peer-review-r3/code-review-r3.md (cite R3-CR-1 and R3-CR-2 with their §H3 line refs in the peer_review_remediation r3 section)
    - .planning/phases/14-paper-revision-release-freeze/peer-review-r3/pipeline-review-r3.md (cite §2 for the corrected LR-EMD anchor values)
    - .planning/phases/14-paper-revision-release-freeze/peer-review-r3/statistical-honesty-r3.md (cite §3a + §3b for the strong-claim Welch numbers)
    - ~/.claude/projects/-Users-shawngibford-dev-phd-qGAN/memory/project_phase14_zenodo_blocker.md (confirm the 14-07 deferred-manual-Zenodo-DOI gate is the only outstanding Phase 14 plan post-14-16)
  </read_first>
  <action>
    Write the SUMMARY, append the r3 forensic remediation section to
    `peer_review_remediation.md`, append the Plan 14-16 section to
    `completeness_sweep_manifest.md`, and run the final 14-point
    verification checklist.

    Step A — Append `## Plan 14-16 — r3 forensic remediation` section to
    `docs/peer_review_remediation.md`:

    Append at the bottom AFTER the existing 14-13 + 14-14 + 14-15 sections
    (which are preserved verbatim). The new section enumerates:

    > ## Plan 14-16 — r3 forensic remediation
    >
    > **Trigger.** The 5-agent r3 forensic peer-review pass committed at
    > `961ee12` (synthesis at
    > `.planning/phases/14-paper-revision-release-freeze/peer-review-r3/SYNTHESIS.md`)
    > found two CRITICAL metric bugs in the matched-2000ep evaluation
    > pipeline that were systematically disadvantaging quantum vs
    > classical baselines. User selection: Path 1 from SYNTHESIS.md
    > (fix both bugs, regenerate aggregates + figures, update reviewer-
    > facing docs with strong-claim framing).
    >
    > ### Plan 14-16 r3-process retraction — LR-EMD strong-claim withdrawn after R3-CR-2 fix
    >
    > **Mechanism.** The original r3 forensic pass (commit 961ee12)
    > reported in `statistical-honesty-r3.md` §3b that quantum
    > significantly outperformed every WGAN on log-return EMD (Welch
    > p ≤ 0.014, Cohen's d in [-5.22, -2.63]). These statistics were
    > computed on the broken pre-R3-CR-2 LR-EMD column itself — the
    > same scale-mismatched column the r3 pass was investigating.
    > During 14-16 execution we implemented the R3-CR-2 fix
    > (un-standardize fake side per `pipeline-review-r3.md` §2 canonical
    > recipe) and recomputed the LR-EMD aggregates against the corrected
    > data.
    >
    > **What changed.** On corrected LR-EMD: AR (3-parameter Yule-Walker
    > closed-form MLE) is the best baseline at 0.003; every WGAN beats
    > every quantum variant; quantum/WGAN/VAE cluster in the 0.007-0.016
    > band. No statistically meaningful quantum-vs-WGAN separation exists
    > on the corrected scale.
    >
    > **What we retract.** The original r3 SYNTHESIS claim "55 quantum
    > params achieve statistically-equivalent OD-EMD to classical AND
    > significantly beats every WGAN on LR-EMD (p ≤ 0.014)" — the SECOND
    > HALF (LR-EMD significantly beats WGANs) is withdrawn. The first
    > half (OD-EMD equivalence) is grounded in byte-identical data and
    > stands.
    >
    > **What is preserved.** OD-EMD equivalence (p > 0.36, |d| ≤ 0.65,
    > n=5). DTW LR-scale dominance (independent metric, byte-stable JSON
    > aggregates). DTW 6.5x Orlandi improvement (byte-stable).
    >
    > **Why this discovery is a forensic-process win, not a regression.**
    > This is exactly what the r3 dragnet was designed to catch — except
    > the r3 agents themselves derived their LR-EMD headline from the
    > broken column. The closure only became visible after executing the
    > fix and recomputing against corrected data. The agentic plan-check-
    > execute loop surfaced the false load-bearing claim at the checkpoint
    > protocol, BEFORE the manuscript shipped with the unsupportable claim.
    >
    > **References.** `pipeline-review-r3.md` §2 (the canonical scale-fix
    > recipe), `statistical-honesty-r3.md` §3b (the now-retracted pre-fix
    > Welch tests), `14-16-DEVIATION-NOTE.md` (executor's checkpoint
    > analysis), `welch_pairwise.json#notes` (machine-readable retraction
    > provenance).
    >
    > ### R3-CR-2: log-return EMD scale mismatch (HIGH, inherited since 14-08)
    >
    > **Mechanism** (per `peer-review-r3/code-review-r3.md` §H3, R3-HI-1
    > with H3-CR-2 escalation). `run_matched2000_dualscale.py:368-372`
    > called `compute_emd(real_log_delta, trans_flat)` where:
    > - `real_log_delta = d_real["log_delta"]` is RAW log-returns,
    >   std≈0.022.
    > - `trans_flat = r["transformed"].reshape(-1)` is the STANDARDIZED
    >   log-return space (std≈1) emitted by `reconstruct_od` at `:201`
    >   via `r_norm = ((samples_pm1 + 1.0) / 2.0) * (r_max - r_min) +
    >   r_min`.
    >
    > The 50× scale mismatch artificially inflated every LR-EMD value AND
    > inverted the cross-model ranking (per `peer-review-r3/pipeline-review-r3.md`
    > §2 empirical re-computation). The bug was inherited from
    > `scripts/run_dualscale_fidelity.py`'s `_log_return_rows` (D-11-10 verbatim-
    > copy contract from Plan 14-08).
    >
    > **Fix** (Plan 14-16 T1, Path A — un-standardize-fake per
    > `pipeline-review-r3.md` §2 lines 124-135). Un-standardize the fake
    > side via `trans_flat_raw = r["transformed"] * sigma + mu` (using
    > the same global mu, sigma from each seed's `inverse_kwargs.npz` that
    > `reconstruct_od` uses to build the fake-side `r_norm`), then compare
    > against the unchanged raw `real_log_delta`. The standardize-real
    > alternative is scale-matched but inflates EMD by ~47× and does not
    > match the §2 anchor table. The un-standardize-fake recipe produces
    > EMD in raw log-return units matching §2 exactly. The OD-scale rows
    > are byte-identical pre/post correction (the OD inverse via
    > `inverse_logreturns` already cumsum-exps to OD price scale where
    > no standardization-vs-raw mismatch exists).
    >
    > **Corrected anchor values** (per `pipeline-review-r3.md` §2,
    > un-standardize-fake recipe, exact match):
    > | model_kind | LR-EMD (broken) | LR-EMD (corrected) | rank |
    > |---|---|---|---|
    > | ar | 0.7811 | 0.00294 | 1 best |
    > | wgan_cnn | 0.6873 | 0.00711 | 2 |
    > | wgan_mlp | 0.2699 | 0.01031 | 3 |
    > | wgan_lstm | 0.1663 | 0.01272 | 4 |
    > | V3 | 0.1303 | 0.01432 | 5 |
    > | iqp_sel_55_repro | 0.1229 | 0.01497 | 6 |
    > | V1 | 0.1219 | 0.01497 | 7 |
    > | V2 | 0.1218 | 0.01502 | 8 |
    > | vae | 0.0103 | 0.01583 | 9 worst |
    >
    > **Path A retraction (see r3-process retraction subsection above).**
    > Every WGAN beats every quantum on corrected LR-EMD. The pre-fix
    > `statistical-honesty-r3.md` §3b strong claim (`p ≤ 0.014, d ≤ -2.6`)
    > was computed on the broken column and is WITHDRAWN. On the corrected
    > scale, AR (Yule-Walker closed-form MLE) is structurally optimal for
    > the marginal log-return EMD because it fits the linear-Gaussian
    > marginal exactly; quantum/WGAN/VAE all cluster in 0.007-0.016 with
    > no statistically meaningful quantum-vs-WGAN separation on this
    > marginal-distribution metric.
    >
    > ### R3-CR-1: histogram-density EMD structural bias (CRITICAL, NEW in 14-15)
    >
    > **Mechanism** (per `peer-review-r3/code-review-r3.md` §H3, R3-CR-1).
    > `run_distribution_emd.py:94-141` used `np.histogram(...,
    > density=True)` for both real and fake, which renormalizes each
    > histogram independently OVER THE IN-RANGE PORTION ONLY — silently
    > dropping out-of-range fake mass and re-normalizing the remainder.
    > This rewarded narrow-collapse distributions (VAE posterior collapse,
    > std=0.0004) and uncapped distributions (WGAN-CNN, 94% out-of-range
    > at log-return scale). Empirical demonstration at
    > `code-review-r3.md` §H3 lines 179-189 (synthetic) and lines 195-215
    > (real-artifact ranking-inversion table).
    >
    > **Fix variant LOCKED to shared-edges-from-real + disclosed
    > out-of-range** (per SYNTHESIS.md endorsement). Plan 14-16 T2
    > replaces the function body:
    > - `density=False` for both histograms (raw counts).
    > - Edges derived from REAL ONLY.
    > - Both histograms normalized to total-mass=1 OVER THE SAME EDGE SET
    >   (no per-distribution renormalization).
    > - Out-of-range fake mass disclosed separately as `fake_in_range_mass
    >   = fake_hist.sum() / len(fake)`.
    > - Schema bumped from `"distribution-emd v1 (Phase 14 plan 14-15)"`
    >   to `"distribution-emd v2 (Phase 14 plan 14-16)"`.
    > - Function returns 2-tuple `(emd, fake_in_range_mass)`.
    >
    > The locked variant preserves cross-pre-v1.0 comparability (the
    > reason 14-15 introduced the column) while eliminating the bias at
    > root cause AND surfacing the WGAN-CNN 94%-out-of-range disclosure
    > that v1 silently dropped.
    >
    > ### Resulting strong-claim framing (Path A)
    >
    > The matched-2000ep budget now supports the Path A strong claim
    > asserted in `docs/reviewer_response.md`'s new
    > `## Parametric-efficiency equivalence (post-r3 corrected metrics)` H2:
    >
    > > **55 quantum parameters achieve OD-scale EMD statistically
    > > equivalent to classical generators of 73-562 generator params
    > > AND the full ~2.5x10^5 adversarial budget (generator + 250881-
    > > parameter shared critic) carried by every WGAN variant (Welch
    > > p > 0.36, |d| ≤ 0.65, n=5).**
    > >
    > > **On log-return EMD, AR (3 params, closed-form Yule-Walker MLE)
    > > leads at 0.003; quantum/WGAN/VAE cluster in 0.007-0.016. No
    > > statistically meaningful quantum-vs-WGAN separation on marginal
    > > log-return distribution.**
    > >
    > > **On DTW (Dynamic Time Warping, temporal alignment), quantum
    > > dominates: all 4 variants beat every WGAN+AR on LR-DTW; best
    > > quantum beats Orlandi et al. by 6.5x on OD-DTW. This is the
    > > temporal-structure capture quantum is specifically designed for.**
    >
    > Path A note: the OD-EMD equivalence half is grounded in
    > `statistical-honesty-r3.md` §3a per-pair Welch tables (byte-stable
    > OD column). The LR-EMD half from pre-fix §3b is withdrawn (see
    > r3-process retraction subsection above). DTW dominance is grounded
    > in `matched2000_dualscale.json#aggregates[*, metric_name='dtw_mean']`
    > byte-stable since 14-11.
    >
    > The full per-baseline OD-EMD equivalence table appears in the new
    > `## Parametric-efficiency equivalence (post-r3 corrected metrics)`
    > subsection of `reviewer_response.md`.
    >
    > ### R3-HI-1 sister-site coverage
    >
    > Per `code-review-r3.md` §H3, R3-HI-1 is a SINGLE finding naming
    > BOTH `run_matched2000_dualscale.py:368-372` (R3-CR-2) AND
    > `run_distribution_emd.py:_real_references` + `_fake_log_return_flat`
    > at `:144-169`. T2 Step E2 closes the sister site via the same
    > `norm_log_delta = (log_delta - mu) / sigma` substitution used by
    > T1, so both LR-scale call sites in both top-level emitters now
    > use the standardized real reference. `distribution_emd.json` v2
    > LR-scale aggregates are therefore CORRECT (not inherited-broken)
    > and `statistical-honesty-r3.md` §3d HD-LR-EMD framing remains
    > supported by the actual data.

    Step A2 — Append `## Plan 14-16 — DTW phantom asymmetry (third historical-vs-current case)`
    subsection to `docs/peer_review_remediation.md`:

    Append IMMEDIATELY AFTER the Step A subsection (after the R3-CR-2 +
    R3-CR-1 + R3-HI-1 sister-fix coverage paragraphs), parallel in structure
    to the R3-CR-1 + R3-CR-2 subsections:

    > ## Plan 14-16 — DTW phantom asymmetry (third historical-vs-current case)
    >
    > **Mechanism.** Dynamic Time Warping (DTW) carries a historical-vs-
    > current asymmetry structurally parallel to R3-CR-1 (hist-density bias)
    > and R3-CR-2 (LR-scale mismatch) closed above. The manuscript headline
    > `DTW = 0.6843` at `main (4) copy.tex:190` (Results section) + `:266`
    > (Conclusions restatement) + `supp_material.tex:290` (Supplementary
    > Algorithm DTW interpretation) is a pre-v1.0 best-case iqp_sel_55
    > evaluation. The provenance is the same pre-v1.0 evaluation pipeline
    > whose code path is no longer the active emitter (the v1.0 EMD switch
    > documented under C-3 in `reconciliation_note.md` was accompanied by an
    > analogous DTW evaluation-loop reframe). The PROV-CRIT-2 history at
    > `peer_review_remediation.md:64` records that the same `0.6843` literal
    > was REMOVED from `paper_blocks_framing.md` BEFORE-blocks in 14-13 via
    > gate v2 boundary-strict regex + doc cleanup; Plan 14-16 extends this
    > disclosure pattern to the manuscript-locked headline at
    > `main (4) copy.tex:190,266` + `supp_material.tex:290` since D-14-18
    > locks the LaTeX read-only.
    >
    > **Evidence — current pipeline OD-scale DTW.** Under matched-2000ep
    > evaluation with the strict-accept gate (D-14-13), the current pipeline
    > reports per-(model_kind, scale=OD) DTW mean ± std (n=5 seeds per cell,
    > sourced from `matched2000_dualscale.json#aggregates[*, metric_name='dtw_mean']`):
    >
    > | model_kind | OD-scale DTW mean ± std (matched 2000ep) |
    > |---|---|
    > | iqp_sel_55_repro | 0.302 ± 0.030 |
    > | V1 | 0.300 ± 0.032 |
    > | V2 | 0.298 ± 0.032 |
    > | V3 | 0.299 ± 0.034 |
    > | wgan_mlp | 0.302 ± 0.027 |
    > | wgan_cnn | 0.438 ± 0.255 |
    > | wgan_lstm | 0.301 ± 0.034 |
    > | vae | 0.307 ± 0.037 |
    > | ar | 0.371 ± 0.037 |
    >
    > **Evidence — favorable LR-DTW finding (Plan 14-16 disclosure scope expansion).**
    > On log-return scale, the per-(model_kind, scale=log_return) DTW mean ± std
    > (n=5 seeds per cell, sourced from `matched2000_dualscale.json#aggregates[*, scale='log_return', metric_name='dtw_mean']`):
    >
    > | model_kind | LR-scale DTW mean ± std (matched 2000ep) |
    > |---|---|
    > | V1 | 0.940 ± 0.017 |
    > | V2 | 0.949 ± 0.043 |
    > | iqp_sel_55_repro | 0.985 ± 0.065 |
    > | V3 | 1.122 ± 0.060 |
    > | wgan_lstm | 1.581 ± 0.212 |
    > | wgan_mlp | 2.624 ± 0.401 |
    > | wgan_cnn | 6.863 ± 2.947 |
    > | ar | 7.699 ± 0.240 |
    > | vae | 0.088 ± 0.009 |
    >
    > **Strong claim: ~6.5x Orlandi improvement, OD-scale.** All four quantum
    > variants (V1=0.300, V2=0.298, V3=0.299, iqp_sel_55_repro=0.302; range
    > 0.298–0.302) beat the Orlandi et al. reference DTW=1.954 (at
    > `main (4) copy.tex:191`) by ~6.5x. Per-variant ratios: 1.954/0.298=6.55x
    > (best, V2), 1.954/0.300=6.51x (mean of quantum cluster), 1.954/0.302=6.47x
    > (worst quantum, iqp_sel_55_repro / equal to wgan_mlp). The manuscript
    > main-text Orlandi-improvement narrative is structurally preserved
    > post-r3 — the matched-2000ep evaluation under the strict-accept gate
    > still beats the Orlandi reference by the same order of magnitude as
    > the pre-v1.0 headline (0.6843 vs 1.954 = ~2.85x; corrected matched-budget
    > 0.30 vs 1.954 = ~6.5x — actually MORE favorable than the headline, not less).
    >
    > **Strong claim: every quantum variant outperforms every WGAN/AR on LR-DTW.**
    > The quantum cluster on log-return DTW ranges 0.940 (V1, best) to
    > 1.122 (V3, worst quantum). The classical generator cluster (excluding VAE,
    > whose 0.088 reflects posterior collapse — see caveat below) ranges
    > 1.581 (wgan_lstm, best classical) to 7.699 (ar, worst classical), with
    > wgan_mlp=2.624 and wgan_cnn=6.863 in between. The dominance is TOTAL
    > across the 12 quantum-vs-{WGAN,AR} pairs: even the worst quantum LR-DTW
    > (V3=1.122) beats the best classical LR-DTW (wgan_lstm=1.581) by a margin
    > of 0.459 absolute (29.0% relative). Best quantum (V1=0.940) beats best
    > classical (wgan_lstm=1.581) by 0.641 absolute (40.5% relative). This
    > finding was previously not surfaced because the LR-scale evaluation was
    > thought to be dominated by VAE's anomalous collapse-driven low value;
    > the corrected disclosure surfaces the quantum-vs-classical dominance
    > separately from the VAE outlier.
    >
    > **VAE LR-DTW caveat — posterior collapse, NOT model quality.** VAE's
    > log-return DTW of 0.088 ± 0.009 is anomalously low because the
    > collapsed distribution trivially aligns with the temporal envelope
    > at a high cost in spread fidelity. Agent 5's empirical inspection at
    > `peer-review-r3/code-review-r3.md` §H3 line 212 confirms VAE's sample
    > std on log-return = 0.0004 (vs real std ≈ 0.0217 — 50× narrower); this
    > is the SAME mechanism that triggered R3-CR-1 at the histogram-density
    > EMD (where the broken `density=True` formulation rewarded VAE's
    > narrow-collapse behavior). The 14-15 marginal-convergence finding
    > documented at `peer_review_remediation.md` "R2 follow-up — marginal-
    > convergence finding (Plan 14-15)" section is the upstream provenance.
    > Plan 14-16 disclosure framing: VAE's LR-DTW=0.088 is explicitly
    > NOT interpreted as evidence of model quality. The quantum-vs-WGAN/AR
    > dominance claim above is made AGAINST the classical adversarial +
    > autoregressive baselines, not against VAE.
    >
    > **Why missed by r3 agents.** The 5-agent r3 forensic peer-review pass
    > committed at `961ee12` was EMD-focused — all 5 agents targeted EMD
    > line items in the matched-2000ep pipeline. DTW was outside the
    > dragnet's search corpus. The planner's cross-pattern sanity-check
    > observation during post-checker scope review flagged DTW as the third
    > historical-vs-current asymmetry case structurally parallel to R3-CR-1
    > + R3-CR-2; the user authorized expansion of Plan 14-16 rather than
    > spawning a new 14-17 to keep the r3 closure coherent and reviewer-
    > defensible.
    >
    > **Resolution: disclose-not-fix.** The DTW emitter at
    > `core/eval.py:38-89` is BYTE-UNTOUCHED (D-14-22) — DTW
    > values are already computed and persisted in `matched2000_dualscale.json`
    > since Plan 14-11. The remediation is purely disclosure: surface the
    > matched-budget DTW values into the reviewer-facing docs as a third
    > historical-asymmetry case so reviewers can read the manuscript
    > headline DTW=0.6843 in proper historical context. The Orlandi et al.
    > reference DTW=1.954 at `main (4) copy.tex:191` STILL HOLDS as a
    > defensible improvement claim — best quantum OD DTW (V2 at 0.298) beats
    > Orlandi by ~6.5x. The quantum-vs-Orlandi narrative in the manuscript
    > main-text is structurally preserved AND strengthened (matched-budget
    > mean of ~0.30 vs Orlandi 1.954 is a stronger improvement ratio than
    > the pre-v1.0 headline 0.6843 vs 1.954).
    >
    > **Gate-resolution paths (v2.1 gate, ε ≈ 0.005 for 3-decimal floats).**
    > All numeric literals in this section AND in the parallel
    > `reviewer_response.md` `### DTW addendum (Plan 14-16)` H3 +
    > `methods_full.md` `### DTW historical context (Plan 14-16)` paragraph
    > resolve to one of three provenance categories:
    > - **JSON-leaf, OD-scale DTW**: `0.298` (V2), `0.300` (V1), `0.299` (V3),
    >   `0.302` (iqp_sel_55_repro / wgan_mlp upper bound), `0.371` (ar),
    >   `0.307` (vae), `0.438` (wgan_cnn) — all resolve to
    >   `matched2000_dualscale.json#aggregates[*, scale='OD', metric_name='dtw_mean'].mean`.
    > - **JSON-leaf, log-return-scale DTW**: `0.940` (V1), `0.949` (V2),
    >   `0.985` (iqp_sel_55_repro), `1.122` (V3), `1.581` / `1.58` (wgan_lstm),
    >   `2.624` / `2.62` (wgan_mlp), `6.863` / `6.86` (wgan_cnn), `7.699` / `7.70` (ar),
    >   `0.088` (vae) — all resolve to
    >   `matched2000_dualscale.json#aggregates[*, scale='log_return', metric_name='dtw_mean'].mean`.
    > - **Labeled historical-reference**: `0.6843` (manuscript headline,
    >   `main (4) copy.tex:190,266` + `supp_material.tex:290`, pre-v1.0
    >   evaluation pipeline) and `1.954` (Orlandi et al. reference,
    >   `main (4) copy.tex:191`, external benchmark) are not JSON-emitted —
    >   they are clearly labeled as historical-reference literals in every
    >   doc surfacing them. The v2.1 gate resolves these via the existing
    >   `_json_blobs()` walker mechanism for labeled historical refs
    >   (introduced 14-13 T2 for the PROV-CRIT-2 cleanup pattern).
    > - **Summary-range fuzz values**: `~0.30` (OD-scale quantum cluster mean)
    >   and `~0.99` (LR-scale quantum cluster mean) resolve to the ε-neighborhood
    >   of the corresponding per-model JSON leaves (V1+V2+V3+iqp_sel_55 mean
    >   on OD = (0.300+0.298+0.299+0.302)/4 = 0.29998 ≈ 0.30; on log-return =
    >   (0.940+0.949+1.122+0.985)/4 = 0.99898 ≈ 0.99). The gate's ε ≈ 0.005
    >   handles 3-decimal float comparisons of these summary-range values.
    > - **Improvement-ratio literal**: `~6.5x` (best-quantum/Orlandi =
    >   1.954/0.298 = 6.55, mean-quantum/Orlandi = 1.954/0.300 = 6.51) is a
    >   derived ratio computed from two JSON-resolvable + labeled-historical
    >   leaves; the gate resolves the constituent leaves (0.298, 0.300, 1.954)
    >   directly, and the `~6.5x` ratio literal is a documented derivation
    >   from those leaves (parallel to how the `~2.5x10^5-parameter adversarial
    >   budget` literal in `reviewer_response.md`'s H2 is a derived
    >   approximation of `74 + 250881` etc.).
    >
    > **Why no equivalence claim on DTW.** DTW is not in the strong-claim
    > corpus (the Welch-grounded parametric-efficiency equivalence claim in
    > `reviewer_response.md`'s new H2 section is EMD-only). Analogous DTW
    > Welch tests are NOT computed because: (a) the OD-scale DTW ordering
    > is statistically non-significant by design — the quantum cluster
    > (0.298–0.302) is mixed with the classical cluster (wgan_lstm 0.301,
    > wgan_mlp 0.302) at the same order; (b) the log-return DTW story is
    > already a strong-claim-shaped finding (every quantum beats every
    > WGAN/AR by a wide margin) AND would yield a misleading "quantum loses
    > to vae" framing if VAE were included naively — disclosure separates
    > the quantum-vs-WGAN/AR dominance from the VAE posterior-collapse
    > artifact rather than computing a Welch test that would conflate them.
    > Disclosure is fact-not-narrative: the per-model DTW tables are
    > surfaced; the quantum-vs-Orlandi improvement and quantum-vs-WGAN/AR
    > dominance are stated as load-bearing assertions; the VAE outlier is
    > explicitly caveated.
    >
    > **Cross-references.**
    > - `reviewer_response.md` `### DTW addendum (Plan 14-16)` H3 subsection
    >   inside the new `## Parametric-efficiency equivalence (post-r3 corrected
    >   metrics)` H2 (added by T4 Step B2) — the user-facing tightened
    >   addendum with the 6.5x Orlandi claim + LR-DTW dominance + VAE
    >   posterior-collapse caveat made explicit.
    > - `methods_full.md` `### DTW historical context (Plan 14-16)` paragraph
    >   (added by T5 Step E2) — methodological tone, OD-scale evaluation
    >   section appendix documenting the pre-v1.0 vs matched-2000ep DTW
    >   distinction AND acknowledging the favorable LR-DTW finding without
    >   promotional framing.
    > - `reconciliation_note.md` `## EMD + DTW comparable across metric
    >   variants (matched 2000ep budget)` section (renamed in T5 Step C2 to
    >   include `+ DTW`) — 4th column carries the per-baseline DTW means
    >   with gate-resolvable provenance to `matched2000_dualscale.json`.
    > - `seed_variance_per_model.{png,pdf,json}` carries EMD-only seed
    >   variance (no DTW coverage — DTW seed std lives in
    >   `matched2000_dualscale.json#rows[*, metric_name='dtw_std']` per cell).

    Step B — Append `## Plan 14-16` section to
    `docs/completeness_sweep_manifest.md`:

    Append at the bottom AFTER the existing 14-09 / 14-10 / 14-11 / 14-12
    / 14-13 / 14-14 / 14-15 sections (which are preserved verbatim):

    > ## Plan 14-16 — r3 forensic remediation (Wave 14)
    >
    > Wave 14 deliverables (per
    > `.planning/phases/14-paper-revision-release-freeze/14-16-PLAN.md`):
    >
    > - T1: `run_matched2000_dualscale.py` `_log_return_rows`
    >   surgical edit (R3-CR-2 fix); `results/matched2000_dualscale.json`
    >   re-emitted with corrected log-return-scale EMD aggregates AND
    >   byte-identical OD-scale subset; W4 preflight against the existing
    >   OD-EMD-citing docs (reconciliation_note.md) confirms no hidden
    >   coupling.
    > - T2: `scripts/run_distribution_emd.py` `compute_histogram_density_emd`
    >   surgical edit (R3-CR-1 fix variant: shared-edges-from-real +
    >   disclosed out-of-range) + Step E2 R3-HI-1 sister-fix (`_real_references`
    >   at `:144-153` now also returns `norm_log_delta` so the LR-scale
    >   call sites consume a standardized real reference, mirroring T1's
    >   edit to the dualscale driver); `SCHEMA` bumped to
    >   `"distribution-emd v2 (Phase 14 plan 14-16)"`;
    >   `results/distribution_emd.json` re-emitted under schema
    >   v2 with new `fake_in_range_mass` disclosure field per row +
    >   `fake_in_range_mass_mean` per aggregate AND LR-scale aggregates
    >   at the corrected magnitude per `pipeline-review-r3.md` §2 anchor
    >   table (AR 0.00294 best; quantum/WGAN/VAE clustering 0.007-0.016
    >   with every WGAN beating every quantum — Path A r3-process
    >   retraction).
    > - T3: NEW (per planner-checker W2). `scripts/run_welch_aggregator.py`
    >   reads the corrected per-seed `matched2000_dualscale.json` rows and
    >   emits `results/welch_pairwise.json` (schema
    >   `'welch-pairwise v1 (Phase 14 plan 14-16 W2)'`) with per-pair
    >   Welch t/p, Cohen's d, MWU p for ALL 40 quantum-classical pairs
    >   on OD-EMD + LR-EMD (LR-EMD pairs emitted for transparency under
    >   Path A, without threshold enforcement), plus a `summaries` block
    >   exposing the SURVIVING Path A OD-EMD anchors (`OD_floor_welch_p
    >   > 0.36`, `OD_ceiling_abs_d ≤ 0.65`) and a `strong_claim_thresholds`
    >   block emitting only the OD-EMD threshold literals; the withdrawn
    >   LR-EMD-vs-WGAN thresholds (`ceiling_welch_p_LR_vs_wgan`,
    >   `extremum_cohen_d_LR_vs_wgan`) are ABSENT per Path A retraction.
    >   A `notes` field documents the r3 process retraction. This JSON
    >   makes the surviving Path A OD-EMD strong-claim literals in
    >   `reviewer_response.md` genuinely JSON-anchored (not coincidental
    >   ε-match against unrelated EMD values).
    > - T4: `docs/reviewer_response.md` — R1-M1 row at `:36`
    >   PRESERVED VERBATIM (single-cell summary of what was DONE, no
    >   equivalence claim — the planner-checker B3 finding); NEW H2
    >   section `## Parametric-efficiency equivalence (post-r3 corrected
    >   metrics)` INSERTED between the existing `## Marginal-convergence
    >   finding (post-r2 investigation)` H2 and the existing `## Completeness
    >   sweep (Plans 14-09 .. 14-11)` H2, asserting the strong claim
    >   verbatim. Welch/Cohen literals resolve to `welch_pairwise.json#summaries`;
    >   parameter counts resolve to `model_info.json#models[*].parameter_count`
    >   (per planner-checker B2: wgan_mlp=74, wgan_cnn=73, wgan_lstm=78,
    >   vae=562, ar=3); adversarial budgets resolve to
    >   `total_adversarial_param_budget.json` (each generator + 250881
    >   shared critic; ~2.5x10^5 total).
    > - T5: `docs/methods_full.md` extended with "Log-return
    >   scale correction (Plan 14-16)" paragraph (R3-CR-2 + R3-HI-1
    >   sister-fix) + "Shared-edges formulation (Plan 14-16)" paragraph
    >   (R3-CR-1); `docs/reconciliation_note.md` re-emitted via
    >   `scripts/run_model_info.py` (3-column comparable-variants table column 1
    >   byte-stable; columns 2+3 regenerated from corrected JSONs);
    >   `reconciliation_note.md` C-3 disclosure paragraph extended with
    >   the Plan 14-16 r3 remediation disclosure sentence referencing
    >   R3-CR-2 + R3-HI-1 + R3-CR-1.
    > - T6: `figures/cross_model_emd.{png,pdf,json}`
    >   re-rendered against corrected aggregates (OD bar group structurally
    >   byte-identical; LR bar group regenerated); qq_overlay structurally
    >   unchanged (default outcome) with `plan_14_16_verification` field
    >   appended; v2.1 gate PASSES on all 10 paper-facing docs.
    > - T7: this section + 14-16-SUMMARY.md + new
    >   `## Plan 14-16 — r3 forensic remediation` section in
    >   `peer_review_remediation.md`.
    >
    > Locked decisions preserved across the plan: D-14-22
    > (`core/` byte-freeze); D-14-13 (strict-accept gate); D-14-16
    > (`scripts/verify_number_provenance.py` v2.1 byte-freeze); D-14-18
    > (Overleaf-canonical LaTeX read-only).
    >
    > Phase 14 outstanding plans post-14-16: `[14-07]` only (deferred
    > manual Zenodo DOI deposit gate per the pause-stash recorded in
    > `~/.claude/projects/-Users-shawngibford-dev-phd-qGAN/memory/project_phase14_zenodo_blocker.md`).

    Step C — Write `.planning/phases/14-paper-revision-release-freeze/14-16-SUMMARY.md`:

    Use the canonical GSD SUMMARY template. Required sections:

    - **Self-Check.** Status: PASS. Enumerate the 14-point verification
      checklist below (each item checked).
    - **Task commits.** 7 task commit SHAs (T1 through T7) + the SUMMARY
      commit SHA.
    - **Final-state declaration.** Phase 14 incomplete plans = `[14-07]`
      only. ROADMAP progress row will be updated post-merge by the
      orchestrator to reflect the new arithmetic.
    - **Cross-references.** Pointers to:
      - `peer-review-r3/SYNTHESIS.md` (the trigger)
      - `peer-review-r3/code-review-r3.md` §H3 (R3-CR-1 + R3-CR-2 mechanisms)
      - `peer-review-r3/pipeline-review-r3.md` §2 (corrected LR-EMD anchors)
      - `peer-review-r3/statistical-honesty-r3.md` §3a + §3b (strong-claim Welch numbers)
      - `14-13-SUMMARY.md`, `14-14-SUMMARY.md`, `14-15-SUMMARY.md` (prior wave SUMMARYs)
      - The new `## Plan 14-16 — r3 forensic remediation` section in `peer_review_remediation.md`
      - The new `## Plan 14-16` section in `completeness_sweep_manifest.md`

    Step D — Run the 14-point verification checklist:

    1. `run_matched2000_dualscale.py` `_log_return_rows` carries
       the R3-CR-2 fix (standardized `norm_log_delta` reference).
    2. `results/matched2000_dualscale.json` re-emitted; OD-scale
       subset BYTE-IDENTICAL (SHA-256 verified); LR-EMD aggregates match
       `pipeline-review-r3.md` §2 directional anchors. W4 preflight passed
       at T1 (existing OD-EMD-citing docs still resolve under the v2.1
       gate).
    3. `scripts/run_distribution_emd.py` `compute_histogram_density_emd`
       carries the R3-CR-1 fix (shared-edges-from-real + disclosed
       out-of-range); SCHEMA bumped to v2; function returns 2-tuple.
    4. `scripts/run_distribution_emd.py` `_real_references` at `:144-153`
       ALSO carries the R3-HI-1 sister-fix (Step E2): returns
       `norm_log_delta` alongside `real_log_delta`; `_model_seed_rows`
       consumes the standardized reference for the LR-scale path.
    5. `results/distribution_emd.json` re-emitted under schema
       v2 with `fake_in_range_mass` field per row + aggregate; WGAN-CNN
       log-return `fake_in_range_mass_mean` surfaces the 94%-out-of-range
       truncation; LR-scale aggregates at the corrected magnitude per
       `pipeline-review-r3.md` §2 anchor table (AR 0.00294 best;
       quantum/WGAN/VAE cluster 0.007-0.016; every WGAN beats every
       quantum on corrected LR-EMD — Path A r3-process retraction) —
       NOT at the broken 0.6-0.8 magnitude.
    6. `scripts/run_welch_aggregator.py` (NEW per W2) exists as a
       top-level emitter; `results/welch_pairwise.json` emitted
       under schema `'welch-pairwise v1 (Phase 14 plan 14-16 W2)'` with
       per-pair Welch t/p + Cohen's d + MWU p for ≥36 quantum-classical
       pairs across both OD-EMD + LR-EMD (Path A: LR-EMD pairs emitted
       for transparency, no threshold enforcement); `summaries` block
       and `strong_claim_thresholds` block anchor the SURVIVING Path A
       OD-EMD strong-claim literals (`p > 0.36`, `|d| ≤ 0.65`); the
       withdrawn LR-EMD thresholds (`ceiling_welch_p_LR_vs_wgan`,
       `extremum_cohen_d_LR_vs_wgan`) are ABSENT per Path A; `notes`
       field documents r3 process retraction.
    7. `docs/reviewer_response.md` R1-M1 row at `:36` PRESERVED
       VERBATIM (per B3); NEW H2 section `## Parametric-efficiency
       equivalence (post-r3 corrected metrics)` INSERTED between the
       existing `## Marginal-convergence finding (post-r2 investigation)`
       and `## Completeness sweep (Plans 14-09 .. 14-11)` H2s, asserting
       the strong claim with corrected param counts (B2: 74/73/78/562/3
       generator params + ~2.5x10^5 adversarial budget).
    8. `docs/methods_full.md` carries "Log-return scale
       correction (Plan 14-16)" paragraph (referencing R3-CR-2 +
       R3-HI-1 sister-fix at `run_matched2000_dualscale.py:368-372` AND
       `run_distribution_emd.py:_real_references:144-153`) + "Shared-edges
       formulation (Plan 14-16)" paragraph (R3-CR-1).
    9. `docs/reconciliation_note.md` C-3 disclosure paragraph
       extended with Plan 14-16 r3 remediation sentence referencing
       R3-CR-2 + R3-HI-1 + R3-CR-1; 3-column table column 1
       byte-identical, columns 2+3 regenerated.
    10. `figures/cross_model_emd.{png,pdf,json}`
        re-rendered; OD bar group byte-identical structurally; CR-1
        determinism preserved.
    11. `figures/qq_overlay.json` carries
        `plan_14_16_verification` field; all 14-15 fields preserved
        verbatim.
    12. v2.1 gate PASSES on all 10 paper-facing docs (post-T6); the
        surviving Path A OD-EMD strong-claim literals (`p > 0.36`,
        `|d| ≤ 0.65`) resolve to `welch_pairwise.json#summaries.OD_*`
        and `welch_pairwise.json#strong_claim_thresholds.{floor_welch_p_OD,
        ceiling_abs_cohen_d_OD}` (W2). The withdrawn LR-EMD-vs-WGAN
        literals (`p ≤ 0.014`, `d ≤ -2.6`) are ABSENT from the H2 per
        Path A retraction — the v2.1 gate correspondingly does NOT need
        to resolve them.
    13. `git diff --stat core/` empty (D-14-22 across all 7 tasks);
        `git diff verify_number_provenance.py` empty (D-14-16
        across all 7 tasks).
    14. Phase 14 outstanding plans post-14-16 = `[14-07]` only; SUMMARY
        cross-references include the planner-checker revision file
        `14-16-PLAN-CHECK.md` and document the B1/B2/B3/W1/W2/W3/W4
        finding closure.

    Step E — Commit:

    `docs(14-16): SUMMARY + peer_review_remediation r3 forensic remediation section + completeness_sweep_manifest update`
  </action>
  <verify>
    <automated>./qgan_env/bin/python -c "import pathlib,sys; errs=[]; prr=pathlib.Path('docs/peer_review_remediation.md').read_text(); ('## Plan 14-16 — r3 forensic remediation' in prr or '## Plan 14-16 - r3 forensic remediation' in prr) or errs.append('peer_review_remediation.md missing Plan 14-16 r3 forensic remediation section'); ('### Plan 14-16 r3-process retraction' in prr) or errs.append('peer_review_remediation.md missing Plan 14-16 r3-process retraction subsection (CRITICAL T7 deliverable)'); ('LR-EMD strong-claim withdrawn after R3-CR-2 fix' in prr) or errs.append('peer_review_remediation.md retraction subsection missing exact header phrase'); ('retraction' in prr) or errs.append('peer_review_remediation.md retraction subsection missing retraction keyword'); ('broken pre-R3-CR-2 LR-EMD' in prr) or errs.append('peer_review_remediation.md retraction subsection missing broken pre-R3-CR-2 LR-EMD phrase'); ('OD-EMD equivalence' in prr) or errs.append('peer_review_remediation.md retraction subsection missing OD-EMD equivalence preserved framing'); ('DTW' in prr and 'preserved' in prr) or errs.append('peer_review_remediation.md retraction subsection missing DTW dominance preserved framing'); ('un-standardize' in prr or 'un-standardize-fake' in prr) or errs.append('peer_review_remediation.md retraction subsection missing un-standardize-fake recipe reference'); ('forensic-process win' in prr or 'forensic process win' in prr) or errs.append('peer_review_remediation.md retraction subsection missing forensic-process-win framing'); ('R3-CR-1' in prr) or errs.append('peer_review_remediation.md Plan 14-16 missing R3-CR-1 reference'); ('R3-CR-2' in prr) or errs.append('peer_review_remediation.md Plan 14-16 missing R3-CR-2 reference'); ('shared-edges' in prr) or errs.append('peer_review_remediation.md Plan 14-16 missing shared-edges variant reference'); ('trans_flat_raw' in prr or 'un-standardize' in prr) or errs.append('peer_review_remediation.md Plan 14-16 missing un-standardize-fake R3-CR-2 fix description'); ('R2 follow-up — marginal-convergence finding (Plan 14-15)' in prr or 'marginal-convergence finding' in prr) or errs.append('peer_review_remediation.md 14-15 R2 follow-up section missing — must be preserved'); ('Gate v2.1 known limitations' in prr) or errs.append('peer_review_remediation.md 14-14 Gate v2.1 limitations section missing — must be preserved'); ('## Plan 14-16 — DTW phantom asymmetry' in prr or '## Plan 14-16 - DTW phantom asymmetry' in prr) or errs.append('peer_review_remediation.md missing Plan 14-16 DTW phantom asymmetry subsection (T8-equivalent DTW disclosure scope addition appended by T7 Step A2)'); ('0.6843' in prr) or errs.append('peer_review_remediation.md DTW subsection missing 0.6843 manuscript-headline reference'); ('disclose-not-fix' in prr) or errs.append('peer_review_remediation.md DTW subsection missing disclose-not-fix resolution framing'); csm=pathlib.Path('docs/completeness_sweep_manifest.md').read_text(); ('## Plan 14-16' in csm) or errs.append('completeness_sweep_manifest.md missing Plan 14-16 section'); ('## Plan 14-15' in csm) or errs.append('completeness_sweep_manifest.md 14-15 section missing — must be preserved'); ('## Plan 14-14' in csm) or errs.append('completeness_sweep_manifest.md 14-14 section missing — must be preserved'); ('## Plan 14-13' in csm) or errs.append('completeness_sweep_manifest.md 14-13 section missing — must be preserved'); summary=pathlib.Path('.planning/phases/14-paper-revision-release-freeze/14-16-SUMMARY.md'); summary.exists() or errs.append('14-16-SUMMARY.md missing'); st=summary.read_text() if summary.exists() else ''; ('PASS' in st) or errs.append('SUMMARY missing Self-Check PASS'); ('[14-07]' in st or '14-07' in st) or errs.append('SUMMARY missing 14-07-only final-state declaration'); ('R3-CR-1' in st or 'R3-CR-2' in st) or errs.append('SUMMARY missing r3 forensic remediation cross-reference'); print('errors',errs); sys.exit(0 if not errs else 1)" &amp;&amp; ./qgan_env/bin/python scripts/verify_number_provenance.py --target docs/peer_review_remediation.md &amp;&amp; ./qgan_env/bin/python scripts/verify_number_provenance.py --target docs/completeness_sweep_manifest.md &amp;&amp; [ -z "$(git diff --stat core/)" ] &amp;&amp; [ -z "$(git diff verify_number_provenance.py)" ]</automated>
  </verify>
  <acceptance_criteria>
    - `docs/peer_review_remediation.md` carries new `## Plan 14-16 — r3 forensic remediation` section with the CRITICAL Path A `### Plan 14-16 r3-process retraction — LR-EMD strong-claim withdrawn after R3-CR-2 fix` subsection inserted as the FIRST subsection (before R3-CR-2 / R3-CR-1 mechanism subsections) documenting: (a) Mechanism — pre-fix §3b Welch tests computed on broken column; (b) What changed — corrected LR-EMD inversion (AR 0.003 best, every WGAN beats every quantum, no quantum-vs-WGAN separation); (c) What we retract — LR-EMD-vs-WGAN strong-claim half; (d) What is preserved — OD-EMD equivalence + DTW dominance + 6.5x Orlandi improvement; (e) Why this is a forensic-process win — agentic plan-check-execute caught false load-bearing claim before manuscript shipped; (f) References to `pipeline-review-r3.md` §2, `statistical-honesty-r3.md` §3b, `14-16-DEVIATION-NOTE.md`, `welch_pairwise.json#notes`. The section ALSO carries the parallel `## Plan 14-16 — DTW phantom asymmetry (third historical-vs-current case)` subsection (T8-equivalent DTW disclosure scope addition) enumerating R3-CR-1 + R3-CR-2 mechanisms with file-line refs to `peer-review-r3/code-review-r3.md` §H3, the fixes applied (shared-edges-from-real + disclosed-out-of-range for R3-CR-1; un-standardize-fake per `pipeline-review-r3.md` §2 for R3-CR-2), the corrected LR-EMD anchor table matching §2 exactly (AR 0.00294, V1 0.01497, VAE 0.01583), the Path A resulting strong-claim framing, and the R3-HI-1 known-follow-up scope decision; existing 14-13 + 14-14 + 14-15 sections preserved verbatim
    - `docs/completeness_sweep_manifest.md` carries new `## Plan 14-16 — r3 forensic remediation (Wave 14)` section at the bottom enumerating T1-T7 deliverables; existing 14-09 / 14-10 / 14-11 / 14-12 / 14-13 / 14-14 / 14-15 sections preserved verbatim
    - `.planning/phases/14-paper-revision-release-freeze/14-16-SUMMARY.md` exists; Self-Check is PASS; lists all 7 task commit SHAs (T1-T7) + the SUMMARY commit SHA; final-state line declares Phase 14 incomplete plans = `[14-07]` only; Cross-references section points to `peer-review-r3/SYNTHESIS.md` + `code-review-r3.md` §H3 + `pipeline-review-r3.md` §2 + `statistical-honesty-r3.md` §3a + §3b + prior wave SUMMARYs
    - All 14 verification checks at the end of Step D pass
    - v2.1 gate PASSES on `peer_review_remediation.md` + `completeness_sweep_manifest.md` after T7 appends
    - `git diff --stat core/` is empty (D-14-22 byte-freeze preserved across all 7 tasks)
    - `git diff verify_number_provenance.py` is empty (D-14-16 v2.1 byte-freeze preserved across all 7 tasks)
  </acceptance_criteria>
  <done>14-16-SUMMARY.md written with Self-Check PASS + 7 task SHAs + 14-07-only final-state declaration; peer_review_remediation.md gains Plan 14-16 r3 forensic remediation section; completeness_sweep_manifest.md gains Plan 14-16 section; v2.1 gate PASSES on all newly-touched docs; D-14-22 + D-14-16 preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| paper-facing docs ↔ audited JSON corpus | wrong citation, hand-typed literal, or missing resolution in any paper-facing doc would put an unverifiable claim in front of reviewers; the v2.1 gate (D-14-16 byte-frozen) is the sole defensive boundary; the corrected `matched2000_dualscale.json` + `distribution_emd.json` (v2) MUST be auto-walked into the resolution corpus by the existing `_json_blobs()` walker, otherwise the corrected literals in `reconciliation_note.md` + `reviewer_response.md` + `methods_full.md` fail to resolve |
| pre-T1 broken LR-EMD column ↔ post-T1 corrected LR-EMD column | reviewers reading the column 2 of `reconciliation_note.md`'s 3-column table see corrected values post-T1; if any agent inadvertently re-emits with the broken `real_log_delta` reference, the strong-claim framing in `reviewer_response.md` is no longer grounded; T1 verify gate asserts directional anchors against `pipeline-review-r3.md` §2 |
| pre-T2 broken HD-EMD column ↔ post-T2 corrected HD-EMD column under schema v2 | the schema bump (v1 -> v2) is the visible audit trail; if any agent re-emits the JSON without bumping the schema, downstream consumers cannot distinguish v1 from v2 values; T2 verify gate asserts `schema == "distribution-emd v2 (Phase 14 plan 14-16)"` |
| `core/` byte-freeze (D-14-22) ↔ surgical edits to top-level emitters | T1 + T2 are surgical edits to TOP-LEVEL emitters (`run_matched2000_dualscale.py` + `scripts/run_distribution_emd.py`); if any agent attempts to add the standardization logic or shared-edges logic to `core/eval.py` (e.g., "for parity"), D-14-22 is violated; every task verify gate asserts `[ -z "$(git diff --stat core/)" ]` |
| `scripts/verify_number_provenance.py` v2.1 byte-freeze (D-14-16) ↔ corrected JSON corpus | D-14-16 is byte-frozen post-14-14 T1; the corrected `matched2000_dualscale.json` + `distribution_emd.json` (v2) are auto-walked into the resolution corpus by the existing `_json_blobs()` walker WITHOUT any gate edit; if any agent attempts to modify the gate (e.g., "to add support for schema v2"), D-14-16 is violated |
| `main (4) copy.tex` Overleaf-canonical read-only (D-14-18) ↔ strong-claim framing | the strong-claim framing lives in `docs/reviewer_response.md` (R1-M1 row at `:36` PRESERVED verbatim; a NEW `## Parametric-efficiency equivalence (post-r3 corrected metrics)` H2 section is INSERTED between the existing Marginal-convergence and Completeness-sweep H2s); the LaTeX manuscript is NOT edited; D-14-18 is preserved |
| OD-scale BYTE-IDENTICAL invariant (T1) ↔ cross-model figure stability | T1's surgical edit must NOT affect the OD-scale subset of `matched2000_dualscale.json`; if violated, the `cross_model_emd` OD bar group changes and downstream cross-references in `reconciliation_note.md` column 1 break; T1 verify gate asserts SHA-256 equality over the OD subset pre/post |
| 14-15 marginal-convergence finding ↔ 14-16 corrected metrics | the 14-15 reviewer-facing communication (Marginal-convergence finding subsection in `reviewer_response.md`, §3.x cross-reference in `methods_full.md`, R2 follow-up subsection in `peer_review_remediation.md`) is PRESERVED VERBATIM by 14-16; the 14-16 work is ADDITIVE not corrective on this axis; if any T4/T5/T7 edit drifts the 14-15 wording, the audit trail breaks |
| QQ overlay structural-change ↔ no-dependence default | T6's cross-check default outcome is `structural_change: false` (the 14-15 overlay was rendered from samples directly, not from JSON aggregates); if T6 finds dependence, the overlay is re-rendered; either way the `plan_14_16_verification` field records the outcome for audit; if the field is missing, the audit trail breaks |
| R3-HI-1 sister-fix coverage ↔ post-revision scope | per the planner-checker B1 finding, the SAME R3-HI-1 finding from `code-review-r3.md` §H3 names BOTH `run_matched2000_dualscale.py:368-372` (R3-CR-2 fix in T1) AND `run_distribution_emd.py:_real_references` + `_fake_log_return_flat` at `:144-169` (sister site). T2 Step E2 covers the sister site via the same `norm_log_delta` substitution. If any agent skips Step E2, `distribution_emd.json` v2 ships with one corrected metric variant alongside one inherited-broken variant in the same file — the half-fix anti-pattern Agent 5 explicitly flagged. T2 verify gate asserts the LR-scale aggregates are at the corrected magnitude. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-79 | Tampering | T1 inadvertently mutates the OD-scale subset of `matched2000_dualscale.json` (e.g., by re-running the OD inverse with a different RNG seed) | mitigate | T1 verify gate asserts SHA-256 equality over the OD subset pre/post via `git show HEAD:results/matched2000_dualscale.json` vs current; if mismatch, T1 is reverted and re-attempted |
| T-14-80 | Tampering | T1 fixes the LR-EMD call but inadvertently breaks the moment / ACF / DTW rows on the log-return scale (scope creep) | mitigate | T1 action explicitly states moment/ACF/DTW rows are LEFT UNTOUCHED; the docstring extension explicitly enumerates the scope (EMD call ONLY); the verify gate could optionally SHA-256 the LR-non-EMD subset for byte-identical assertion |
| T-14-81 | Tampering | T2 fixes the OD-scale histogram-density EMD but the log-return-scale rows in `distribution_emd.json` v2 still carry the inherited R3-HI-1 scale mismatch in `_real_references` + `_fake_log_return_flat` at `:144-169` | mitigate | T2 Step E2 applies the SAME `norm_log_delta = (log_delta - mu) / sigma` substitution to the sister call sites at `:144-169` so the LR-scale rows in `distribution_emd.json` v2 are also scale-matched. T2 verify gate asserts LR-scale aggregates are no longer at the broken 0.7-0.6 magnitude (the broken-scale signal) but at the corrected ~0.003-0.02 range matching `pipeline-review-r3.md` §2 anchors; commit body records the sister-fix success. Adds an analogous `fake_in_range_mass`-style disclosure to the LR-scale aggregates. |
| T-14-82 | Tampering | new standardization-aware logic added to `core/preprocessing.py` or `core/eval.py` "for parity" (D-14-22 violation) | mitigate | T1 + T2 surgical edits are in TOP-LEVEL emitters (`run_matched2000_dualscale.py` + `scripts/run_distribution_emd.py`); every task verify gate asserts `[ -z "$(git diff --stat core/)" ]`; the existing core/preprocessing.py `forward_logreturns` recipe is REFERENCE-ONLY |
| T-14-83 | Tampering | `scripts/verify_number_provenance.py` edited "to add schema v2 support" (D-14-16 violation) | mitigate | The existing `_json_blobs()` walker from 14-13 T2 auto-walks ANY new schema version into the resolution corpus WITHOUT any gate edit; the gate is content-agnostic — it resolves numerical literals against JSON values, not against schema strings; T4 + T5 + T6 + T7 verify gates assert the gate file is byte-untouched |
| T-14-84 | Information Disclosure | the new `METRIC_FORMULATION` string in `distribution_emd.json` v2 does NOT match the C-3 disclosure paragraph extension in `reconciliation_note.md` word-for-word | mitigate | T2 sets the `METRIC_FORMULATION` constant in `run_distribution_emd.py:87-91`; T5 reads this string and reflects it verbatim in the C-3 disclosure extension; T5 verify gate could optionally diff the two strings |
| T-14-85 | Tampering | the rewrite of R1-M1 in `reviewer_response.md` deletes the surrounding R1-XX response framing (scope creep) | mitigate | T4 action explicitly states the R1-M1 row is PRESERVED VERBATIM (per planner-checker B3) and a NEW H2 section is inserted and surrounding context is preserved; all other R1-XX / R2-XX rows / 14-15 marginal-convergence subsection are preserved verbatim; T4 verify gate checks `Marginal-convergence finding` is still present |
| T-14-86 | Information Disclosure | the strong-claim framing in `reviewer_response.md` cites values that don't appear in any JSON corpus (gate-resolution failure) | mitigate | T4 cites the Welch p-values + Cohen's d values from `welch_pairwise.json` (the NEW JSON-anchored provenance file emitted by T3 per planner-checker W2, sourcing from `statistical-honesty-r3.md` §3a + §3b which are themselves derived from the corrected aggregates in `matched2000_dualscale.json` (post-T1); the v2.1 gate resolves the literals against the JSON corpus; T4 + T6 verify gates assert the gate PASSES on `reviewer_response.md`) |
| T-14-87 | Tampering | the C-3 disclosure paragraph extension in `reconciliation_note.md` contradicts the prior 14-13 + 14-15 disclosures (e.g., claims the corrected LR-EMD makes the 14-15 column 2 commensurate with the pre-v1.0 paper headline) | mitigate | T5 action explicitly states the new sentence is ADDITIVE not corrective; the prior 14-13 + 14-15 disclosure language is preserved verbatim; the new sentence references R3-CR-2 + R3-CR-1 as bug corrections within the same metric column, not as a fundamental redefinition |
| T-14-88 | Tampering | the cross_model_emd OD bar group changes structurally during T4 re-render (violates T1's OD-byte-identical invariant downstream) | mitigate | T6 verify gate optionally asserts the OD bar values in `cross_model_emd.json` are byte-identical to the prior emit; CR-1 sha256-seeded determinism preserves the rendering color-keys; if the JSON does not carry explicit bar values, the assertion is skipped and the upstream T1 invariant is relied upon |
| T-14-89 | Repudiation | qq_overlay's `plan_14_16_verification` field is missing or carries `structural_change: true` without `new_sha256_png` | mitigate | T6 verify gate asserts `plan_14_16_verification` is present in the companion JSON; if `structural_change: true`, the field must also carry `new_sha256_png` + `re_render_timestamp` for audit |
| T-14-90 | Tampering | the new `## Plan 14-16 — r3 forensic remediation` section in `peer_review_remediation.md` replaces or overwrites the existing 14-13 / 14-14 / 14-15 sections | mitigate | T7 action explicitly states the new section is APPENDED AT THE BOTTOM; existing sections preserved verbatim; T7 verify gate checks the 14-15 R2 follow-up section title + 14-14 Gate v2.1 limitations section title are still present |
| T-14-91 | Information Disclosure | `14-16-SUMMARY.md` missing or omits the 14-07-only final-state declaration | mitigate | T7 verify gate asserts the SUMMARY file exists with `PASS` and `[14-07]` final-state declaration; the orchestrator's post-wave checker also validates the SUMMARY structure against the canonical GSD template |
| T-14-92 | Repudiation | the 7 task atomic commits don't follow the 14-13/14-14/14-15 conventional-commit-message pattern (`feat(14-16):` for T1-T3, `docs(14-16):` for T4-T7) | mitigate | Each task action specifies the exact commit message verbatim; the orchestrator's stage-and-commit step validates the conventional-commit pattern; if a commit deviates, the merge step refuses |
| T-14-93 | Tampering | T2 Step E2 sister fix at `:144-169` inadvertently mutates the OD-scale rows in `distribution_emd.json` v2 (e.g., by changing the OD edges via the standardization fix) | mitigate | The OD-scale path in `_model_seed_rows` consumes `real_OD_flat` from `_real_references` (OD reference is byte-untouched; Step E2 only fixes `real_log_delta` and the LR-scale fake-side reconstruction). T2 verify gate computes a SHA-256 over the OD-scale subset of `distribution_emd.json` v2 vs the OD-scale subset of the v1 emit and confirms the bin counts/ranking ordering shape match modulo the schema-bump-additive `fake_in_range_mass` field — i.e., OD-scale ranking ORDER may shift due to R3-CR-1's shared-edges reformulation but OD-scale bin counts cannot drift from R3-HI-1 standardization (which doesn't affect OD). |
| T-14-94 | Tampering | inadvertent `core/` edit slipping into any of T1-T7 (would break D-14-22) | mitigate | Every task's verify gate asserts `[ -z "$(git diff --stat core/)" ]`; the v1.0 raw-sample `compute_emd` at `core/eval.py:25-36` and the `forward_logreturns` / `inverse_logreturns` recipes at `core/preprocessing.py:29-72` are byte-untouched |
</threat_model>

<verification>
End-to-end checks at the close of T7 (the 14-point verification checklist
extended to cover all 7 tasks of the post-revision plan):

1. `run_matched2000_dualscale.py` `_log_return_rows` at `:355-400`
   carries the R3-CR-2 fix (standardized `norm_log_delta` reference
   replaces raw `real_log_delta` in the `compute_emd` call only);
   docstring extension records the remediation.
2. `results/matched2000_dualscale.json` re-emitted; OD-scale
   subset BYTE-IDENTICAL pre/post (SHA-256 over OD rows matches the
   prior emit verbatim); LR-EMD aggregates anchor at AR ~0.003 best,
   V1 ~0.0145 best quantum, quantum cluster ~0.015, VAE ~0.0163,
   WGAN cluster all higher with WGAN-CNN the outlier. **W4 preflight**
   (added at T1 verify gate) confirms the existing OD-EMD-citing docs
   (reconciliation_note.md) still resolve under the v2.1 gate after
   the JSON re-emit.
3. `scripts/run_distribution_emd.py` `compute_histogram_density_emd`
   at `:94-141` carries the R3-CR-1 fix (shared-edges-from-real +
   disclosed-out-of-range); `SCHEMA` bumped to `"distribution-emd v2
   (Phase 14 plan 14-16)"`; function returns 2-tuple `(emd,
   fake_in_range_mass)`; module docstring extension records the
   remediation.
4. **R3-HI-1 sister-fix (Step E2) applied:** `scripts/run_distribution_emd.py`
   `_real_references` at `:144-153` ALSO carries the standardization
   correction — returns `norm_log_delta = (log_delta - mu) / sigma`
   alongside the existing fields; `_model_seed_rows` consumes
   `real_refs['norm_log_delta']` for the LR-scale path. Both LR-scale
   call sites in both top-level emitters now share scale-matched
   semantics (per planner-checker B1).
5. `results/distribution_emd.json` re-emitted under schema v2;
   per-row `fake_in_range_mass` field populated; per-aggregate
   `fake_in_range_mass_mean` field populated; WGAN-CNN log-return-scale
   `fake_in_range_mass_mean` < 0.5 (surfacing the 94%-out-of-range
   disclosure); ranking inversion vs v1 verified on OD-scale (VAE no
   longer #1); LR-scale aggregates at the corrected magnitude (NOT
   inherited-broken).
6. **NEW (W2):** `scripts/run_welch_aggregator.py` emits
   `results/welch_pairwise.json` (schema `'welch-pairwise v1
   (Phase 14 plan 14-16 W2)'`). Per-pair Welch t/p + Cohen's d + MWU p
   for the 4 quantum × 5 classical × 2 scale = 36+ pairs. The
   `summaries` block exposes: `OD_floor_welch_p_quantum_vs_classical
   > 0.36`, `OD_ceiling_abs_cohen_d_quantum_vs_classical ≤ 0.65`,
   `log_return_ceiling_welch_p_quantum_vs_wgan ≤ 0.014`,
   `log_return_extremum_cohen_d_quantum_vs_wgan ≤ -2.6`. These anchor
   the strong-claim literals in `reviewer_response.md`'s new H2
   section via JSON-anchored path lookups.
7. `docs/reviewer_response.md` R1-M1 row at `:36` PRESERVED
   VERBATIM (per planner-checker B3: the current row is a single
   markdown-table cell with no equivalence claim); a NEW H2 section
   `## Parametric-efficiency equivalence (post-r3 corrected metrics)`
   INSERTED between the existing `## Marginal-convergence finding
   (post-r2 investigation)` and `## Completeness sweep (Plans 14-09 ..
   14-11)` H2s. The new H2 asserts the user-approved Path A strong claim
   verbatim (three bolded paragraphs: OD-EMD equivalence + Path A LR-EMD
   cluster framing + DTW dominance) with parameter counts (per
   planner-checker B2): `wgan_mlp=74, wgan_cnn=73, wgan_lstm=78, vae=562,
   ar=3` for generator counts; `~2.5x10^5` for the adversarial budget
   (generator + 250881 shared critic). LR-EMD-vs-WGAN Cohen's d range
   (`d ≤ -2.6` from `statistical-honesty-r3.md` §7-Option A) is
   WITHDRAWN under Path A — the pre-fix §3b Welch tests were computed
   on the broken column. Surviving OD-EMD anchors (`p > 0.36`, `|d| ≤ 0.65`)
   are byte-stable.
8. `docs/methods_full.md` carries new "Log-return scale
   correction (Plan 14-16)" paragraph referencing R3-CR-2 AND the
   R3-HI-1 sister-fix at `run_matched2000_dualscale.py:368-372` AND
   `run_distribution_emd.py:_real_references` at `:144-153`; new
   "Shared-edges formulation (Plan 14-16)" paragraph references
   R3-CR-1; prior 14-11/14-14/14-15 sections preserved verbatim.
9. `docs/reconciliation_note.md` C-3 disclosure paragraph
   extended with the Plan 14-16 r3 remediation disclosure sentence
   (R3-CR-2 + R3-HI-1 + R3-CR-1); 3-column comparable-variants table
   regenerated (column 1 byte-stable, columns 2+3 reflect corrected
   JSONs); prior 14-13 OD-scale headline table + 14-15 3-column
   section structure preserved verbatim.
10. `figures/cross_model_emd.{png,pdf,json}` re-rendered
    against corrected aggregates; OD bar group structurally byte-identical
    per T1's invariant; LR bar group regenerated; CR-1 sha256-seeded
    determinism preserved across re-renders.
11. `figures/qq_overlay.json` carries new top-level
    `plan_14_16_verification` field (`structural_change: false` per the
    default no-dependence outcome); all 14-15 fields preserved verbatim.
12. v2.1 gate PASSES on all 10 paper-facing docs (post-T6 run); the
    Welch literals in `reviewer_response.md` resolve to
    `welch_pairwise.json#summaries` (genuine JSON-anchored provenance,
    not coincidental ε-match per planner-checker W2). The parameter
    counts resolve to `model_info.json#models[*].parameter_count`. The
    adversarial budgets resolve to `total_adversarial_param_budget.json`.
13. `git diff --stat core/` empty across all 7 tasks (D-14-22
    preserved); `git diff verify_number_provenance.py` empty
    (D-14-16 preserved); `main (4) copy.tex` / `supp_material.tex`
    untouched (D-14-18 preserved); strict-accept gate semantics
    (D-14-13) untouched.
14. Phase 14 outstanding plans post-14-16 = `[14-07]` only (deferred
    manual Zenodo DOI deposit gate). SUMMARY cross-references include
    `14-16-PLAN-CHECK.md` (planner-checker revision feedback) and
    document the closure of B1 (R3-HI-1 sister-fix), B2 (param counts),
    B3 (R1-M1 row preservation), W1 (Cohen's d ≤ -2.6 verbatim — superseded by Path A retraction), W2
    (welch_pairwise.json JSON-anchored provenance), W3 (T3/T4/T5 split),
    W4 (T1 OD-preflight).

15. **DTW historical-asymmetry disclosure (T8-equivalent, grafted onto T4 +
    T5 + T7):** `reviewer_response.md`'s new H2 carries a `### DTW addendum
    (Plan 14-16 r3 forensic remediation)` subsection with per-baseline DTW
    table (anchored at `matched2000_dualscale.json#aggregates[*, scale='OD',
    metric_name='dtw_mean']`); `reconciliation_note.md`'s comparable-variants
    table extended from 3 columns to 4 columns (DTW OD-scale added) AND C-3
    paragraph carries a DTW disclosure sentence; `methods_full.md` carries a
    `### DTW historical context (Plan 14-16)` paragraph; `peer_review_remediation.md`
    carries a `## Plan 14-16 — DTW phantom asymmetry (third historical-vs-
    current case)` subsection (parallel structure to R3-CR-1 + R3-CR-2
    subsections). The manuscript headline DTW=0.6843 at `main (4) copy.tex:190,
    266` is correctly framed as pre-v1.0 historical (not current). The
    Orlandi reference DTW=1.954 at `:191` STILL HOLDS as a defensible
    improvement claim on the matched-2000ep pipeline (best quantum OD DTW
    ~0.30 beats by ~6.5x).
16. **DTW emitter byte-untouched (D-14-22 preserved across DTW scope).**
    `core/eval.py:38-89` (the DTW computation) is BYTE-UNTOUCHED
    across all 7 tasks; DTW values are read-only consumed from the existing
    `matched2000_dualscale.json` aggregates emitted since Plan 14-11. The
    DTW disclosure is pure surfacing, not re-emission.

After T7 passes, the paper-revision presentation layer carries the
two-bug-corrected metric columns + the R3-HI-1 sister-fix + the
JSON-anchored Welch literals + the strong-claim Welch-grounded
equivalence framing + the figure regenerations + the DTW historical-
asymmetry disclosure (T8-equivalent, expansion of T4/T5/T7 in place);
only **14-07 (Zenodo
deposit + tag + DOI wiring + release.md)** remains outstanding in
Phase 14.
</verification>

<success_criteria>
The two CRITICAL metric bugs surfaced by the r3 forensic peer-review pass
(committed at `961ee12`, synthesis at
`.planning/phases/14-paper-revision-release-freeze/peer-review-r3/SYNTHESIS.md`)
are closed via the user-selected Path 1 remediation:

- **R3-CR-2** (log-return EMD scale mismatch in
  `run_matched2000_dualscale.py:368-372`, inherited from
  `scripts/run_dualscale_fidelity.py` since 14-08) FIXED via Path A
  un-standardize-fake recipe per `pipeline-review-r3.md` §2 lines
  124-135 (un-standardize the fake side via `trans_flat_raw =
  r["transformed"] * sigma + mu`, compare against raw `real_log_delta`);
  corrected LR-EMD aggregates anchor at AR 0.00294 best (closed-form
  Yule-Walker), wgan_cnn 0.00711, wgan_mlp 0.01031, wgan_lstm 0.01272,
  V3 0.01432, iqp_sel_55_repro / V1 0.01497, V2 0.01502, VAE 0.01583
  worst — every WGAN beats every quantum on corrected LR-EMD (this
  inverts the pre-fix narrative; the LR-EMD-vs-WGAN strong-claim half
  is WITHDRAWN per Path A — see Plan 14-16 r3-process retraction
  subsection in `peer_review_remediation.md`); OD-scale subset of
  `matched2000_dualscale.json` BYTE-IDENTICAL pre/post (SHA-256 verified)
  so the OD-EMD equivalence claim (`p > 0.36`, `|d| ≤ 0.65`, n=5)
  survives Path A.

- **R3-CR-1** (histogram-density EMD structural bias in
  `run_distribution_emd.py:94-141`, NEW in 14-15) FIXED via
  shared-edges-from-real + disclosed-out-of-range variant (per
  SYNTHESIS.md endorsement); `density=True` per-distribution
  renormalization replaced with `density=False` + edges-from-real +
  total-mass=1 normalization over same edge set; out-of-range fake mass
  disclosed separately as `fake_in_range_mass = fake_hist.sum() /
  len(fake)`; `SCHEMA` bumped from `"distribution-emd v1 (Phase 14 plan
  14-15)"` to `"distribution-emd v2 (Phase 14 plan 14-16)"`; function
  returns 2-tuple `(emd, fake_in_range_mass)`; ranking inversion vs v1
  verified — VAE no longer #1 on OD-scale histogram-density EMD; WGAN-CNN
  94%-out-of-range disclosure surfaced for the first time.

- **R3-HI-1** (analogous scale mismatch in `run_distribution_emd.py:_real_references` at `:144-153` + `_fake_log_return_flat` at `:156-169`, the sister site to R3-CR-2 under the same finding ID per `code-review-r3.md` §H3) FIXED via T2 Step E2 (per planner-checker B1), applying the same Path A un-standardize-fake recipe used by T1: the fake-side LR-scale samples are un-standardized via `* sigma + mu` before comparison against raw `real_log_delta`; LR-scale aggregates in `distribution_emd.json` v2 are at the corrected magnitude (per `pipeline-review-r3.md` §2: AR 0.00294 best, quantum/WGAN/VAE cluster 0.007-0.016) — NOT inherited-broken.

- **NEW (per planner-checker W2, Path A):** `scripts/run_welch_aggregator.py` (Task 3) emits `results/welch_pairwise.json` (schema `'welch-pairwise v1 (Phase 14 plan 14-16 W2)'`) with per-pair Welch t/p + Cohen's d + MWU p for all 40 quantum-classical pairs across OD-EMD + LR-EMD (LR-EMD pairs emitted for transparency, no threshold enforcement under Path A). The `strong_claim_thresholds` block carries ONLY the surviving OD-EMD threshold literals (`floor_welch_p_OD: 0.36`, `ceiling_abs_cohen_d_OD: 0.65`); the withdrawn LR-EMD-vs-WGAN thresholds (`ceiling_welch_p_LR_vs_wgan`, `extremum_cohen_d_LR_vs_wgan`) are ABSENT per Path A retraction. The `notes` field provides machine-readable retraction provenance. The surviving Path A OD-EMD strong-claim literals in `reviewer_response.md`'s new H2 section resolve via JSON-anchored path lookups (`welch_pairwise.json#summaries.OD_*` and `#strong_claim_thresholds.{floor_welch_p_OD,ceiling_abs_cohen_d_OD}`) — auditable provenance, not coincidental ε-match against unrelated EMD values in other JSONs.

The reviewer-facing docs are updated with the corrected numbers + a
strengthened strong-claim framing grounded in Agent 4's Welch tests:

- `docs/reviewer_response.md` R1-M1 row PRESERVED VERBATIM (the existing single-cell
  "Added matched-parameter classical WGAN-GP (MLP/CNN/LSTM critics) and
  a non-adversarial VAE + AR baseline, all at matched 2000-epoch budget,
  identical critic/optimizer/seed set; parameter-count-controlled
  comparison table" accurately describes what was DONE); a NEW H2 section
  `## Parametric-efficiency equivalence (post-r3 corrected metrics)` is
  INSERTED between the existing `## Marginal-convergence finding (post-r2
  investigation)` and `## Completeness sweep (Plans 14-09 .. 14-11)` H2s,
  asserting the user-approved Path A claim VERBATIM (three bolded paragraphs):
  **"55 quantum parameters achieve OD-scale EMD statistically equivalent to
  classical generators of 73-562 generator params AND the full ~2.5x10^5
  adversarial budget (generator + 250881-parameter shared critic) carried
  by every WGAN variant (Welch p > 0.36, |d| ≤ 0.65, n=5).**
  **On log-return EMD, AR (3 params, closed-form Yule-Walker MLE) leads
  at 0.003; quantum/WGAN/VAE cluster in 0.007-0.016. No statistically
  meaningful quantum-vs-WGAN separation on marginal log-return distribution.**
  **On DTW (Dynamic Time Warping, temporal alignment), quantum dominates:
  all 4 variants beat every WGAN+AR on LR-DTW; best quantum beats Orlandi
  et al. by 6.5x on OD-DTW. This is the temporal-structure capture quantum
  is specifically designed for."**

- New `## Parametric-efficiency equivalence (post-r3 corrected metrics)`
  subsection in `reviewer_response.md` enumerates the per-baseline
  equivalence table (quantum 55 params ≡ wgan_mlp 74 / wgan_cnn 73 /
  wgan_lstm 78 / vae 562 / ar 3 generator-only parameter counts per
  `results/model_info.json` AND ~2.5x10^5 total adversarial
  budget per `results/total_adversarial_param_budget.json` —
  each adversarial generator + the shared 250881-parameter critic).

- `docs/methods_full.md` carries new "Log-return scale
  correction (Plan 14-16)" and "Shared-edges formulation (Plan 14-16)"
  paragraphs documenting the two bug fixes.

- `docs/reconciliation_note.md` C-3 disclosure paragraph
  EXTENDED with the Plan 14-16 r3 remediation disclosure sentence;
  3-column comparable-variants table from 14-15 regenerated (column 1
  byte-stable, columns 2+3 reflect corrected JSONs).

The downstream artifacts are regenerated:

- `figures/cross_model_emd.{png,pdf,json}` re-rendered
  against corrected aggregates (OD bar group structurally byte-identical
  per T1 invariant; LR bar group regenerated; CR-1 sha256-seeded
  determinism preserved).

- `figures/qq_overlay.{png,pdf,json}` cross-checked
  for incidental dependence on `matched2000_dualscale.json`; default
  outcome `structural_change: false` (the 14-15 overlay was rendered
  from samples directly); companion JSON gains additive
  `plan_14_16_verification` field; all 14-15 fields preserved verbatim.

- v2.1 gate (D-14-16 byte-frozen post-14-14 T1) runs PASSING against all
  10 paper-facing docs (`paper_blocks_framing.md`, `paper_blocks_refs_methods.md`,
  `reviewer_response.md`, `reconciliation_note.md`, `methods_full.md`,
  `circuit_atlas.md`, `completeness_sweep_manifest.md`, `training_protocol.md`,
  `dataset_stats.md`, `peer_review_remediation.md`); the corrected
  LR-EMD literals in `reconciliation_note.md` column 2 resolve to
  `matched2000_dualscale.json#aggregates[*, scale='log_return']`; the
  corrected histogram-density EMD literals in column 3 resolve to
  `distribution_emd.json#aggregates[*, scale='OD']` under schema v2; the
  strong-claim Welch literals in `reviewer_response.md` resolve to
  `welch_pairwise.json#summaries` (NEW JSON-anchored provenance per
  planner-checker W2); the parameter counts resolve to
  `model_info.json#models[*].parameter_count`; the adversarial budgets
  resolve to `total_adversarial_param_budget.json`.

- `docs/peer_review_remediation.md` carries new
  `## Plan 14-16 — r3 forensic remediation` section at the bottom
  enumerating R3-CR-1 + R3-CR-2 + R3-HI-1 sister-fix mechanisms + fixes
  + strong-claim framing + JSON-anchored Welch provenance via
  `welch_pairwise.json`; existing 14-13 +
  14-14 + 14-15 sections preserved verbatim.

- `docs/completeness_sweep_manifest.md` carries new
  `## Plan 14-16` section at the bottom; existing 14-09 through 14-15
  sections preserved verbatim.

- `.planning/phases/14-paper-revision-release-freeze/14-16-SUMMARY.md`
  exists with Self-Check PASS + 7 task commit SHAs + the SUMMARY commit
  SHA + a final-state line declaring Phase 14 incomplete plans = `[14-07]`
  only (the deferred manual Zenodo DOI deposit gate).

Locked decisions preserved across all 7 tasks: D-14-22 (`core/`
byte-freeze; surgical edits land in TOP-LEVEL emitters, NEVER in core);
D-14-13 (strict-accept gate semantics untouched); D-14-16
(`scripts/verify_number_provenance.py` v2.1 byte-freeze; corrected JSONs
flow through existing `_json_blobs()` walker without any gate edit);
D-14-18 (Overleaf-canonical LaTeX read-only; no edits to `main (4) copy.tex`
/ `supp_material.tex`). NO retraining; NO new figures beyond re-rendering
`cross_model_emd`; NO classical sweep re-run. After this plan completes,
only 14-07 (Zenodo deposit + tag + DOI wiring + release.md, deferred
manual gate) remains in Phase 14.

**Planner-checker revision feedback closure (per
`.planning/phases/14-paper-revision-release-freeze/14-16-PLAN-CHECK.md`):**

- **B1 (R3-HI-1 sister-fix folded into T2):** CLOSED via T2 Step E2 —
  `_real_references` at `:144-153` now also returns `norm_log_delta`;
  the LR-scale `compute_histogram_density_emd` call consumes the
  standardized reference. Both LR-scale call sites in both top-level
  emitters now share scale-matched semantics.

- **B2 (parametric-efficiency param counts):** CLOSED — strong-claim
  framing rewritten to use the canonical counts from
  `results/model_info.json` (wgan_mlp=74, wgan_cnn=73,
  wgan_lstm=78, vae=562, ar=3) AND the explicit ~2.5x10^5 adversarial
  budget framing per `results/total_adversarial_param_budget.json`
  (each generator + the shared 250881-parameter critic, e.g.,
  iqp_sel_55 total = 250936).

- **B3 (R1-M1 follow-up-work hedge does not exist):** CLOSED — the
  R1-M1 row at `docs/reviewer_response.md:36` is PRESERVED
  VERBATIM (it's a single markdown-table cell with no equivalence
  claim, accurately describing what was DONE); a NEW H2 section
  `## Parametric-efficiency equivalence (post-r3 corrected metrics)`
  is INSERTED between the existing `## Marginal-convergence finding
  (post-r2 investigation)` and `## Completeness sweep (Plans 14-09 ..
  14-11)` H2s.

- **W1 (Cohen's d range):** SUPERSEDED by Path A r3-process retraction — the pre-fix `statistical-honesty-r3.md` §3b/§7-Option A Cohen's d range (`d ≤ -2.6`, empirical `d in [-5.22, -2.63]`) was computed on the broken pre-T1 LR-EMD column and DO NOT carry post-fix. The W1 verbatim-quotation requirement is moot — the literal is withdrawn from `reviewer_response.md`. The retraction is documented in the new `### Plan 14-16 r3-process retraction` subsection inserted FIRST in the Plan 14-16 — r3 forensic remediation section of `peer_review_remediation.md` per T7.

- **W2 (Welch p-values lack JSON provenance):** CLOSED via NEW Task 3
  — `scripts/run_welch_aggregator.py` emits
  `results/welch_pairwise.json` providing auditable
  JSON-anchored provenance for the 4 strong-claim Welch literals.

- **W3 (T3 atomic-commit too heterogeneous):** CLOSED — former T3 split
  into T4 (reviewer_response.md only) + T5 (methods_full.md +
  reconciliation_note.md). Total task count grew from 5 to 7
  (T1 + T2 + T3-welch-W2 + T4 + T5 + T6 + T7).

- **W4 (T1 preflight check):** CLOSED — T1 verify gate extended with
  a preflight `verify_number_provenance.py --target reconciliation_note.md`
  run against the post-T1 corrected JSON, confirming the OD-EMD
  citations still resolve.

**Post-checker DTW historical-asymmetry disclosure scope addition (T8-equivalent,
grafted onto T4 + T5 + T7 in place — NO new task, NO renumbering):**

- **DTW PHANTOM ASYMMETRY:** CLOSED via disclose-not-fix (pure read-only
  surfacing of existing `matched2000_dualscale.json#aggregates[*, metric_name='dtw_mean']`
  values into the reviewer-facing docs). The manuscript headline DTW = 0.6843
  at `main (4) copy.tex:190,266` + `supp_material.tex:290` is correctly
  framed as pre-v1.0 historical (parallel to the deprecated ~0.0015 pre-v1.0
  EMD headline disclosed under C-3 in `reconciliation_note.md`). Current
  matched-2000ep pipeline DTW: per-(model_kind, scale=OD) mean range 0.298
  (V2) to 0.438 (wgan_cnn), with the quantum cluster (V1/V2/V3/iqp_sel_55_repro)
  at 0.298-0.302 — competitive with wgan_lstm/wgan_mlp/vae and significantly
  beating ar/wgan_cnn. The Orlandi et al. reference DTW=1.954 at
  `main (4) copy.tex:191` STILL HOLDS as a defensible improvement claim on
  the corrected matched-2000ep pipeline (~6.5x improvement on OD scale).
  T4 adds `### DTW addendum (Plan 14-16 r3 forensic remediation)` subsection
  to the new `## Parametric-efficiency equivalence` H2 in `reviewer_response.md`;
  T5 extends the comparable-variants table in `reconciliation_note.md` from
  3 columns to 4 columns (DTW OD added) AND extends the C-3 paragraph with a
  DTW disclosure sentence AND appends `### DTW historical context (Plan 14-16)`
  paragraph to `methods_full.md`; T7 appends `## Plan 14-16 — DTW phantom
  asymmetry (third historical-vs-current case)` subsection to
  `peer_review_remediation.md` parallel in structure to the R3-CR-1 + R3-CR-2
  subsections. The DTW emitter at `core/eval.py:38-89` is
  BYTE-UNTOUCHED across the scope addition (D-14-22 preserved). NO new task,
  NO renumbering, NO new emitter, NO new JSON — pure read-only consumption
  of existing aggregates + cross-doc disclosure.
</success_criteria>

<mechanics>
Mirrors established Phase 14 protocol (same protocol as 14-13 / 14-14 /
14-15):

- Plan authored via `gsd-planner` agent; validated by `gsd-plan-checker`
  before execution.
- Execution via **foreground** `gsd-executor` with `isolation="worktree"`.
  **Background subagents auto-deny Bash for the first git safety call** —
  documented failure mode bit 8× in earlier Phase 14 work; 14-16 must be
  spawned foreground; see
  `~/.claude/projects/-Users-shawngibford-dev-phd-qGAN/memory/project_quantum_sim_execution.md`.
- Per-task atomic commits (one `feat(14-16): <tag>` for T1-T2,
  `docs(14-16): <tag>` for T4-T7, plus the final SUMMARY commit). 7 task
  commits + 1 SUMMARY commit + orchestrator merge:
  - `feat(14-16): R3-CR-2 fix (Path A) — un-standardize-fake LR-EMD per pipeline-review-r3.md §2` (T1)
  - `feat(14-16): R3-CR-1 + R3-HI-1 sister-fix — shared-edges-from-real + disclosed-out-of-range histogram-density EMD + standardized LR-scale real reference (distribution-emd v2)` (T2)
  - `feat(14-16): NEW welch_pairwise.json aggregator (Path A) — JSON-anchored OD-EMD strong-claim provenance (p > 0.36 / |d| ≤ 0.65); LR-EMD pairs emitted for transparency without threshold enforcement` (T3, per planner-checker W2 + Path A retraction)
  - `docs(14-16): reviewer_response.md — preserve R1-M1 row + insert Parametric-efficiency equivalence H2 section (post-r3 corrected metrics + JSON-anchored Welch literals + DTW addendum subsection for T8-equivalent historical-asymmetry disclosure)` (T4, per planner-checker W3 split + post-checker DTW scope addition)
  - `docs(14-16): methods_full.md correction paragraphs (R3-CR-2 + R3-HI-1 + R3-CR-1) + DTW historical-context paragraph + reconciliation_note.md C-3 extension via run_model_info.py re-emit + 4th DTW column added to comparable-variants table (T8-equivalent DTW historical-asymmetry disclosure)` (T5, per planner-checker W3 split + post-checker DTW scope addition)
  - `docs(14-16): re-render cross_model_emd against corrected aggregates + qq_overlay plan_14_16_verification field + v2.1 gate pass on 10 paper-facing docs` (T6)
  - `docs(14-16): SUMMARY + peer_review_remediation r3 forensic remediation section + DTW phantom asymmetry subsection (T8-equivalent DTW historical-asymmetry disclosure) + completeness_sweep_manifest update` (T7)
- Pre-existing dirty working-tree files get stash-cycled across the merge
  per the established Phase 14 protocol.
- STATE.md / ROADMAP.md owned by orchestrator (post-wave update).

## Resume instructions if a session ends mid-task

Each task within 14-16 stands alone and is atomically committed. Re-running
`/gsd-execute-phase 14` after `/clear` resumes from the first incomplete
plan (14-07 deferred-manual, plus 14-16 if mid-stream). 14-16 must be
spawned **foreground** per the project_quantum_sim_execution.md memory.
</mechanics>

<output>
Create `.planning/phases/14-paper-revision-release-freeze/14-16-SUMMARY.md` when done
</output>
