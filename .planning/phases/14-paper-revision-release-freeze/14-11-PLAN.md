---
phase: 14-paper-revision-release-freeze
plan: 11
type: execute
wave: 9
depends_on: ["14-03", "14-09"]
files_modified:
  - run_classical_arch_extract.py
  - run_methods_full.py
  - run_framework_versions.py
  - results/classical_architectures.json
  - results/framework_versions.json
  - results/methods_full.json
  - docs/methods_full.md
autonomous: true
requirements: [PAPER-08, PAPER-09]
must_haves:
  truths:
    - "Pure aggregator pipeline: NO retraining, NO sampling, NO checkpoint reload — `revision.core.training` is OPENED ONLY AS A TEXT FILE (grep for `manual_seed`/`np.random.seed`/`random.seed`/`generator_loss = -torch.mean`/`critic_loss = ` to capture file:line citations); classical model classes (WGANMLPGenerator, WGANCNNGenerator, WGANLSTMGenerator, VAEBaseline, ARBaseline, Critic) imported ONLY to walk `module.named_modules()` for structure — never `.fit(`, never `train_`, never `.sample(` in any of the 3 new scripts"
    - "`core/` byte-frozen (D-14-22): `git diff --stat core/` returns empty after Tasks 1-4 complete"
    - "`scripts/verify_number_provenance.py` UNMODIFIED (D-14-16, mirrors 14-09 / 14-10 contract): `git diff --stat verify_number_provenance.py` returns empty; the gate auto-discovers the 3 new JSONs via its existing `results/*.json` rglob"
    - "`docs/methods_full.md` PASSES `./qgan_env/bin/python scripts/verify_number_provenance.py --target docs/methods_full.md` unmodified — every numeric literal resolves to one of {model_info.json, canonical_config_lock.json, default_75_config_lock.json, v1_config_lock.json, v2_config_lock.json, v3_config_lock.json, classical_architectures.json, framework_versions.json, methods_full.json}"
    - "Cross-artifact `data_hash` gate (explicit raise AssertionError, python -O safe — run_multiseed_rollup.py:86-92 idiom) in run_methods_full.py: every consumed JSON carrying a `data_hash` field MUST equal `91e447d4624e25b3`"
    - "The classical-architecture extractor hard-asserts (explicit `raise AssertionError`, python -O safe) that the walked `total_params` for each classical model (wgan_mlp/cnn/lstm/vae/ar) equals the corresponding `parameter_count` in `results/model_info.json` — this is a drift guard against the extractor going stale"
    - "Param-dtype vs sample-dtype conflation is RESOLVED in §4 of methods_full.md: `dtype_params` (torch.float32 from classical.py `nn.Parameter(...dtype=torch.float32)`) and `dtype_samples` (torch.float64 from training.py:268 `compute_dtype = torch.float32 if device.type == 'mps' else torch.float64`) appear as DISTINCT fields in methods_full.json buckets.4_hardware_software, NEVER merged into one"
    - "default_75 (4 layers, baseline byte-frozen v1.0/v1.1) vs iqp_sel_55 (3 layers, canonical paper circuit recovered from frozen checkpoint epoch 1969) contradiction is EXPLICITLY clarified in §6 of methods_full.md: both circuits are valid, used in different contexts, distinct config-lock JSONs (default_75_config_lock.json vs canonical_config_lock.json); neither is 'wrong'"
    - "Plan 14-11 marked complete in the ROADMAP progress table (10/11, not 9/10); ROADMAP Wave 9 section added after Wave 8 referencing 14-11; D-14-23 scope note extended with one sentence covering Plan 11"
    - "`run_matched2000.py:1-80` docstring text is preserved VERBATIM as the rerun-instructions block inside methods_full.json buckets.5_reproducibility.rerun_command_template (rendered as a fenced code block in methods_full.md §5) — never paraphrased"
  artifacts:
    - path: "run_classical_arch_extract.py"
      provides: "Pure-introspection emitter — imports classical generator/critic/VAE classes ONLY for `module.named_modules()` walking + ARBaseline plain-class introspection; emits results/classical_architectures.json; explicit-raise `total_params == model_info.json parameter_count` drift gate per model"
      min_lines: 120
    - path: "run_framework_versions.py"
      provides: "Pure-introspection emitter — reads installed-package versions via `importlib.metadata.version(...)` for {pennylane, torch, numpy, scipy, matplotlib, PyYAML}; records python_version + platform; emits results/framework_versions.json"
      min_lines: 40
    - path: "run_methods_full.py"
      provides: "Pure aggregator — consumes {model_info.json, canonical_config_lock.json, default_75_config_lock.json, v1/v2/v3_config_lock.json, classical_architectures.json, framework_versions.json} + text-only file:line greps over core/training.py (loss + seed call sites) + verbatim docstring read of run_matched2000.py:1-80; emits results/methods_full.json with the 5-bucket structure; cross-artifact data_hash gate (explicit-raise)"
      min_lines: 200
    - path: "results/classical_architectures.json"
      provides: "Per-model layer-tree JSON for the 5 classical baselines (wgan_mlp, wgan_cnn, wgan_lstm, vae, ar) — each layer_spec carries layer_type / in_features / out_features (or kernel_size + in/out_channels + stride / hidden_size + num_layers) / activation / param_count; per-model `total_params` matches model_info.json parameter_count"
    - path: "results/framework_versions.json"
      provides: "Pinned versions of the running qgan_env interpreter at methods-doc emit time (pennylane, torch, numpy, scipy, matplotlib, PyYAML) + python_version + platform + captured_at_iso"
    - path: "results/methods_full.json"
      provides: "The paper-ready Methods aggregator JSON with 5 buckets: 1_dataset (from model_info.json.dataset verbatim), 2_models (per-model family + n_params + architecture + training_objective with formula name + core/training.py:N citation + LaTeX equation string), 3_training (optimizer/lr/betas/n_critic/lambda_gp/batch/epochs/early_stop), 4_hardware_software (device + DISTINCT dtype_params/dtype_samples + backend + framework_versions), 5_reproducibility (data_hash + seed_set + determinism_contract with file:line citations for manual_seed / np.random.seed / random.seed + verbatim rerun_command_template from run_matched2000.py:1-80 docstring)"
    - path: "docs/methods_full.md"
      provides: "Paper-ready Methods document with 6 sections (Dataset / Models / Training / Hardware-Software / Reproducibility / Address-the-contradictions). EVERY numeric literal renders FROM methods_full.json or the 5 config-lock JSONs; the loss-equation LaTeX strings render verbatim from methods_full.json (not hand-typed). PASSES verify_number_provenance.py unmodified."
      min_lines: 200
  key_links:
    - from: "run_classical_arch_extract.py"
      to: "core/models/classical.py + core/models/nonadversarial.py + core/models/critic.py"
      via: "import-only introspection: `from revision.core.models.classical import WGANMLPGenerator, WGANCNNGenerator, WGANLSTMGenerator` + `from revision.core.models.nonadversarial import VAEBaseline, ARBaseline` + optional `from revision.core.models.critic import Critic`; instantiate each nn.Module with its default constructor, walk `model.named_modules()` — never `.fit(`, never `.sample(`, never `.forward(` in a training loop"
      pattern: "named_modules|WGANMLPGenerator|WGANCNNGenerator|WGANLSTMGenerator|VAEBaseline|ARBaseline"
    - from: "run_classical_arch_extract.py"
      to: "results/model_info.json"
      via: "drift gate — explicit `raise AssertionError` (python -O safe) when extractor's walked total_params != model_info.json models[].parameter_count for the same model kind"
      pattern: "raise AssertionError"
    - from: "run_framework_versions.py"
      to: "importlib.metadata.version(...) over {pennylane, torch, numpy, scipy, matplotlib, PyYAML}"
      via: "pure introspection of running interpreter — no network, no install, no subprocess"
      pattern: "importlib.metadata.version"
    - from: "run_methods_full.py"
      to: "core/training.py (TEXT READ ONLY, never imported as a module)"
      via: "open(training.py).read() + line-number greps for `torch.manual_seed`, `np.random.seed`, `random.seed`, `critic_loss = `, `generator_loss = `; record results as file:line citations in methods_full.json — never executes training code"
      pattern: "training.py.*read_text|manual_seed|np.random.seed"
    - from: "run_methods_full.py"
      to: "run_matched2000.py:1-80 docstring"
      via: "open(run_matched2000.py).read() and slice the leading docstring verbatim into methods_full.json buckets.5_reproducibility.rerun_command_template — NEVER paraphrased; the docstring already contains the canonical `--model {…} --seed N --epochs 2000` template and the `--accept` strict-gate command"
      pattern: "rerun_command_template|run_matched2000"
    - from: "run_methods_full.py"
      to: "data_hash gate (run_multiseed_rollup.py:86-92 idiom)"
      via: "explicit `raise AssertionError` if any consumed JSON's data_hash != '91e447d4624e25b3' (model_info.json + all 5 config-lock JSONs that carry data_hash); classical_architectures.json and framework_versions.json carry NO data_hash (introspection artifacts) so they are excluded from the gate"
      pattern: "raise AssertionError.*data_hash|91e447d4624e25b3"
    - from: "docs/methods_full.md"
      to: "verify_number_provenance.py --target docs/methods_full.md"
      via: "every numeric literal (param counts, layer counts, qubit counts, LR, lambda_gp, batch_size, epochs, seeds, framework versions) traces via the gate's `results/*.json` rglob to one of methods_full.json / model_info.json / config-lock JSONs / classical_architectures.json / framework_versions.json; zero verifier edit"
      pattern: "verify_number_provenance.py"
    - from: "docs/methods_full.md §4 (Hardware/Software)"
      to: "methods_full.json buckets.4_hardware_software.dtype_params + buckets.4_hardware_software.dtype_samples"
      via: "two DISTINCT fields rendered as two distinct rows in the doc — never merged, never paraphrased as 'the dtype'; explicit clarification paragraph resolves the conflation contradiction"
      pattern: "dtype_params|dtype_samples"
    - from: "docs/methods_full.md §6 (Address-the-contradictions)"
      to: "default_75_config_lock.json + canonical_config_lock.json"
      via: "one explicit paragraph names both circuits, cites both lock JSONs, explains default_75 (4 layers, byte-frozen v1.0/v1.1 baseline used in matched-budget sweep V1/V2/V3 ansatz studies) vs iqp_sel_55 (3 layers, canonical paper circuit recovered from frozen checkpoint epoch 1969 — the manuscript's headline quantum entrant); neither is mislabeled, they serve different purposes"
      pattern: "default_75|iqp_sel_55|contradiction"
---

<objective>
Add a paper-ready Methods document built entirely FROM audited JSON sources +
text-only file:line citations into the byte-frozen `core/`. The Methods
section is currently fragmented across `docs/training_protocol.md`,
`docs/dataset_stats.md`, `docs/circuit_atlas.md`,
`results/model_info.json`, and the source code of
`core/models/classical.py` / `core/training.py`. The paper
needs ONE consolidated Methods document. To deliver it without violating the
D-14-22 byte-freeze and the D-14-16 no-hand-typed-numbers contract, this plan
introduces three pure-introspection emitters that fill the gaps in the audited
JSON corpus (classical architectures, framework versions, the methods
aggregator), then renders `methods_full.md` from those JSONs.

Purpose: PAPER-08 (Methods completeness — dataset details + model specs) and
PAPER-09 (per-metric evaluation scale + training protocol) both require the
manuscript Methods section to be coherent, complete, and JSON-traceable. The
documented contradictions (default_75 vs iqp_sel_55 "which is default", and
param-dtype vs sample-dtype conflation) MUST be explicitly addressed.

Output: 3 new pure-aggregator/introspection scripts + 3 new audited JSONs under
`results/` + 1 paper-ready Methods document under `docs/`,
all gated by the existing `scripts/verify_number_provenance.py` unmodified, no
retraining, no sampling, no checkpoint reload, no `core/` edit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-paper-revision-release-freeze/14-CONTEXT.md
@.planning/phases/14-paper-revision-release-freeze/14-RESEARCH.md
@.planning/phases/14-paper-revision-release-freeze/14-PATTERNS.md
@.planning/phases/14-paper-revision-release-freeze/14-03-SUMMARY.md
@.planning/phases/14-paper-revision-release-freeze/14-09-SUMMARY.md

<interfaces>
<!-- All interfaces below are already on disk; this plan only consumes them.
     No new contracts created in core/. -->

run_model_info.py pure-aggregator template (lines 60-100 + 627-797):
- `_find_repo_root()` walks up to `core/preprocessing.py` and is the
  ONLY anchoring idiom; copy it verbatim into each of the 3 new scripts.
- `RESULTS = REPO / "revision/results"`; `DOCS = REPO / "revision/docs"`.
- Cross-artifact `data_hash` gate idiom (run_model_info.py:638-649): collect
  hashes from every consumed JSON, `if len(set(hashes.values())) != 1: raise
  AssertionError(...)`. The `data_hash` is `91e447d4624e25b3` (a constant the
  whole repo agrees on; matched2000 declares it as `EXPECTED_DATA_HASH` in
  run_matched2000.py:106).
- Output write idiom: `out = {"schema": ..., "data_hash": ..., ...};
  out_path.write_text(json.dumps(out, indent=2))`; print row count + data_hash.
- NO `import torch` / `import pennylane` / `revision.core` import in the pure
  aggregator path (run_methods_full.py and run_framework_versions.py).
  run_classical_arch_extract.py IS allowed to import torch + the model classes
  because `named_modules()` requires the live nn.Module; the import is
  introspection-only and the script never calls `.fit(` / `.sample(` /
  `train_*` — verified by the same grep-gate idiom run_circuit_diagrams.py
  uses (14-09 Task 1 verify gate).

run_figure_suite.py loud-fail loader idiom (lines 144-174):
- `_find_repo_root()` resolver.
- `_require(path, what)` raises `FileNotFoundError` with a "render-only (no
  training/sampling/recompute)" message on any missing artifact — copy this
  shape into the 3 new scripts so every consumed JSON loud-fails on absence.
- `_load_json(path, what) = json.loads(_require(path, what).read_text())`.

run_circuit_diagrams.py pure-aggregator-safety precedent (14-09):
- `from revision.run_matched2000 import _QUANTUM_ANSATZ` is verified
  pure-aggregator-safe because run_matched2000's top-level imports are stdlib
  + numpy + yaml + revision.core constants — NO `.fit(` is triggered by
  import. The same is true of `from revision.core.models.classical import
  WGANMLPGenerator` (the module's top-level imports are torch + torch.nn +
  torch.nn.functional; importing the class does NOT instantiate it; even
  instantiating it constructs an `nn.Parameter` but runs no forward pass).
- 14-09 verify gates: `! grep -qE '\.fit\(|def train_|model\.sample\(|best_checkpoint\.pt'` — this plan reuses the same gate verbatim.
- `qgan_env` interpreter path: `./qgan_env/bin/python` (every existing
  driver's verify block uses this — same here).

results/model_info.json schema (the per-model registry to align to):
- Top-level: schema, metric_helpers, data_hash, dataset, consumed_artifacts,
  seed_set, model_kinds, models[], rows[]:
- models[] record carries: model, kind, parameter_count, family, source,
  circuit_id, ansatz, depth, topology, epochs, early_stop, optimizer,
  lr_critic, lr_generator, optimizer_betas, batch_size, n_critic, lambda_gp,
  seeds, num_qubits, num_layers, window_length, n_real_windows, device, dtype,
  pennylane_device, diff_method, backend_assertion, data_hash, tier,
  train_protocol_notes, wall_seconds, (+ headline-only: checkpoint_epoch,
  checkpoint_sha256).
- The "kind" field is one of: quantum, wgan_mlp, wgan_cnn, wgan_lstm, vae, ar.
- The 9 sweep models + headline are listed; iqp_sel_55_headline + iqp_sel_55_repro
  are TWO DISTINCT rows (D-14-10). For classical-architectures coverage, the
  five classical kinds (wgan_mlp, wgan_cnn, wgan_lstm, vae, ar) are the
  models[] kinds to extract architecture for; the 4 quantum rows
  (iqp_sel_55_headline, iqp_sel_55_repro, V1, V2, V3) carry their architecture
  in the existing config-lock JSONs.

core/models/classical.py (the 3 WGAN-GP generator classes — see
classical.py:58-95 for WGANMLPGenerator, 98-141 for WGANCNNGenerator, 144-212
for WGANLSTMGenerator). Each has `count_params() -> int` and a single
`nn.Parameter(torch.randn(N, dtype=torch.float32) * 0.5)` named `params_pqc`.
The `forward(noise)` slices `params_pqc` into per-layer weight/bias views
applied via `torch.nn.functional` — there are no submodules to walk for these
3 classes; the extractor must record the FUNCTIONAL layer sequence from the
forward method's structure (documented in each class's docstring at
classical.py:58-69 / 98-111 / 144-160).

core/models/nonadversarial.py (VAEBaseline at nonadversarial.py:52-117
+ ARBaseline at nonadversarial.py:120-184):
- VAEBaseline IS an nn.Module with 5 submodules (enc, fc_mu, fc_logvar, dec_h,
  dec_out) — `named_modules()` walks the submodules; `parameters()` flattens
  the 5 `nn.Linear` weights+biases. The constructor at nonadversarial.py:67-73
  declares LATENT_DIM=4, WINDOW=10, HIDDEN=16; param count 562.
- ARBaseline is a PLAIN class (not nn.Module); `named_modules()` cannot walk
  it. Its architecture is `{type: "AR(p)", order_p: 2, params_per_seed: 3 (p+1
  for p AR coefficients + 1 noise variance), total_params: 3 (default p=2)}`.
  Record it as a hand-encoded layer_spec in classical_architectures.json with
  the explicit note that ARBaseline is a closed-form lstsq fit
  (nonadversarial.py:139-158), not a torch nn.Module.

core/models/critic.py (Critic at critic.py:19-77):
- Critic is an nn.Module — Conv1d×3 + LeakyReLU + AdaptiveAvgPool1d + Flatten
  + Linear(128,32) + LeakyReLU + Dropout + Linear(32,1). Cast to .double()
  at critic.py:67. This is the WGAN-GP critic shared across iqp_sel_55_repro
  + V1/V2/V3 + wgan_mlp/cnn/lstm — record its architecture in
  classical_architectures.json under a "shared_critic" key (NOT a model in
  the model_info.json sense; one critic instance per training run).

core/training.py grep target lines (text-read only — never imported):
- `torch.manual_seed(seed)` at training.py:245
- `np.random.seed(seed)` at training.py:246
- `random.seed(seed)` at training.py:247
- `if torch.cuda.is_available(): torch.cuda.manual_seed_all(seed)` at training.py:248-249
- Critic loss: `critic_loss = fake_score_mean - real_score_mean + lambda_gp * gp` at training.py:364 (WGAN-GP critic objective with gradient penalty)
- Generator loss: `generator_loss = -torch.mean(fake_scores)` at training.py:385 (WGAN generator objective)
- Gradient penalty: `gp = ((gradients.norm(2, dim=1) - 1) ** 2).mean()` at training.py:72 (two-sided GP)
- dtype split: `compute_dtype = torch.float32 if device.type == "mps" else torch.float64` at training.py:268 — params stay torch.float32 (classical.py:78 nn.Parameter dtype), critic + sample-space cast to compute_dtype (training.py:285)
- The VAE ELBO loop and AR lstsq orchestration are in run_baselines.py (NOT
  training.py); training.py covers WGAN-GP only. record_methods_full.json
  must cite this distinction.

run_matched2000.py:1-80 docstring (the verbatim rerun-instructions
source):
- Lines 1-69 contain the full module docstring including the CLI usage block:
  `./qgan_env/bin/python -m revision.run_matched2000 --model {iqp_sel_55_repro|V1|V2|V3|wgan_mlp|wgan_cnn|wgan_lstm|vae|ar} --seed N [--epochs 2000] [--out-root results/matched2000] [--csv-path ./data.csv]`
- Plus the `--accept` strict-gate variant.
- methods_full.json reads `run_matched2000.py.read_text()`, slices the
  triple-quoted docstring (between the first `"""` and the second `"""`),
  stores it as a single string in
  `buckets.5_reproducibility.rerun_command_template` — NEVER paraphrased,
  NEVER summarized. methods_full.md renders it as a fenced code block.

results/canonical_config_lock.json + default_75_config_lock.json +
v1/v2/v3_config_lock.json (from 14-01 + 14-09):
- canonical_config_lock.json: iqp_sel_55, num_qubits=5, num_layers=3,
  param_count=55, topology=range, final_rotation=RX_only, native_pipeline=B,
  checkpoint_epoch=1969.
- default_75_config_lock.json: default_75, num_qubits=5, num_layers=4,
  param_count=75, topology=range, final_rotation=RX_plus_RY,
  source_path=core/__init__.py + core/models/quantum.py.
- v1_config_lock.json (75p, range, 4 layers, source line 118).
- v2_config_lock.json (135p, range, 8 layers, source line 120).
- v3_config_lock.json (75p, linear, 4 layers, source line 122).
- Every quantum architecture spec in methods_full.md §2 reads from these.

verify_number_provenance.py auto-coverage (lines 92-107):
- `_json_blobs()` rglobs `results/*.json` — so the moment
  classical_architectures.json + framework_versions.json + methods_full.json
  land under `results/`, they are automatically in the gate's
  resolution corpus. ZERO verifier edit. (Mirrors 14-09 + 14-10 contracts.)

importlib.metadata.version API (Python 3.8+):
- `from importlib.metadata import version, PackageNotFoundError`
- `version("pennylane")` returns a `str` like "0.36.0"; raises
  PackageNotFoundError on a missing package. Wrap each call in try/except so
  a missing optional (e.g. PyYAML if not installed) records None rather than
  crashing the emitter — but PyYAML IS a dependency of the running interpreter
  (run_model_info.py:58 imports yaml) so it MUST be present; recording None
  is the missing-package fallback for completeness only.
- The canonical case-sensitive distribution names: "pennylane" (lowercase
  per pypi), "torch", "numpy", "scipy", "matplotlib", "PyYAML" (case-sensitive
  for PyPI metadata; `version("PyYAML")` is the correct invocation).
- python_version: `sys.version.split()[0]`; platform: `platform.platform()`.
- captured_at_iso: `datetime.datetime.utcnow().isoformat() + "Z"` (matches
  run_circuit_diagrams.py 14-09 Task 2 companion JSON convention).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: run_classical_arch_extract.py + classical_architectures.json</name>
  <files>run_classical_arch_extract.py, results/classical_architectures.json</files>
  <read_first>
    - core/models/classical.py (the 3 WGAN-GP generator classes: WGANMLPGenerator lines 58-95, WGANCNNGenerator 98-141, WGANLSTMGenerator 144-212 — each has `params_pqc: nn.Parameter` and `count_params() -> int`; the forward methods reveal the layer sequence — extract from the docstring + forward body)
    - core/models/nonadversarial.py (VAEBaseline lines 52-117 — IS an nn.Module with 5 submodules; ARBaseline lines 120-184 — plain class with `count_params() -> p+1`, default p=2)
    - core/models/critic.py (Critic lines 19-77 — shared WGAN-GP critic; cast to .double() at line 67)
    - results/model_info.json (the parameter_count drift basis — models[] kind ∈ {wgan_mlp, wgan_cnn, wgan_lstm, vae, ar} carries the ground-truth parameter_count; the extractor's walked total_params MUST equal it)
    - run_model_info.py:60-100 (the pure-aggregator template: `_find_repo_root()` 69-81, REPO + RESULTS anchoring 84-90, output write idiom 765)
    - run_figure_suite.py:144-174 (the loud-fail _require/_load_json idiom — copy for loading model_info.json)
    - run_circuit_diagrams.py (14-09 — the precedent for "import revision.core.models.* for introspection only, never .fit(/.sample(/train_"; reuse the same grep-gate idiom)
  </read_first>
  <action>
    Create `scripts/run_classical_arch_extract.py` as a pure-introspection
    emitter. Open with the standard repo bootstrap idiom copied from
    `run_model_info.py:60-100`: module docstring noting
    introspection-only / D-14-22 byte-freeze / pure-aggregator constraint;
    `_find_repo_root()` walking to `core/preprocessing.py`;
    `REPO + RESULTS` anchoring; the `_require` / `_load_json` loud-fail loader
    copied from `run_figure_suite.py:144-174`.

    Implementation steps:

    (1) Import the 5 classical model classes via the public path (Claude's
        discretion on import grouping):
        `from revision.core.models.classical import WGANMLPGenerator, WGANCNNGenerator, WGANLSTMGenerator`
        `from revision.core.models.nonadversarial import VAEBaseline, ARBaseline`
        `from revision.core.models.critic import Critic`
        Pure-aggregator-safety precedent: the 14-09 plan already verified that
        importing `revision.core.models.*` at top level is safe — the modules'
        own top-level imports are torch + torch.nn + torch.nn.functional + numpy,
        and class instantiation runs no forward pass. The 14-09 verify gate
        `! grep -qE '\.fit\(|def train_|model\.sample\(|best_checkpoint\.pt'`
        applies verbatim here.

    (2) For each nn.Module class in {WGANMLPGenerator, WGANCNNGenerator,
        WGANLSTMGenerator, VAEBaseline, Critic}: instantiate with default
        constructor (each has a no-arg constructor; Critic has
        `window_length=10, dropout_rate=0.2` defaults), then walk
        `model.named_modules()` to record each submodule with its identity
        (type name, in/out features or channels, kernel/stride/padding for
        conv layers, hidden_size/num_layers for any nn.LSTM, activations
        flowing between layers). For the 3 WGAN-GP generators which carve
        weights from `params_pqc` directly (no nn submodules), record the
        FUNCTIONAL layer sequence from the class docstring's flat-layout
        annotation (classical.py:60-65 / 104-109 / 152-160) — each line of
        the docstring layout becomes one layer_spec entry; the layer_type
        and shape come from the docstring + the `params_pqc` slice indices.

    (3) For ARBaseline (plain class, not nn.Module): record a single
        layer_spec entry encoding it as
        `{layer_type: "AR(p)", order_p: 2, params_per_seed: 3, fit_method: "np.linalg.lstsq (closed-form, no training loop)", source_path: "core/models/nonadversarial.py:139-158"}`.

    (4) Emit `results/classical_architectures.json` with structure:
        ```
        {
          "schema": "classical-architectures v1 (Phase 14 plan 14-11)",
          "extracted_at_iso": "<datetime.datetime.utcnow().isoformat()+'Z'>",
          "extractor": "module.named_modules() walk + functional-layout docstring parse",
          "models": {
            "wgan_mlp": {
              "class": "WGANMLPGenerator",
              "source_path": "core/models/classical.py:58-95",
              "generator": [
                {"layer_type": "Linear", "in_features": 5, "out_features": 4, "bias": true, "activation": "tanh", "param_count": 24},
                {"layer_type": "Linear", "in_features": 4, "out_features": 10, "bias": true, "activation": null, "param_count": 50}
              ],
              "total_params": 74
            },
            "wgan_cnn": { ... ConvTranspose1d + Conv1d structure ... "total_params": 73 },
            "wgan_lstm": { ... functional LSTM + Linear ... "total_params": 78 },
            "vae": {
              "class": "VAEBaseline",
              "source_path": "core/models/nonadversarial.py:52-117",
              "encoder": [...],
              "decoder": [...],
              "objective": "ELBO",
              "latent_dim": 4,
              "hidden_dim": 16,
              "window": 10,
              "total_params": 562
            },
            "ar": {
              "class": "ARBaseline",
              "source_path": "core/models/nonadversarial.py:120-184",
              "type": "AR(p)",
              "order_p": 2,
              "fit_method": "np.linalg.lstsq (closed-form)",
              "total_params": 3
            },
            "shared_critic": {
              "class": "Critic",
              "source_path": "core/models/critic.py:19-77",
              "layers": [ ... Conv1d×3, LeakyReLU, AdaptiveAvgPool1d, Flatten, Linear(128,32), Dropout, Linear(32,1) ... ],
              "dtype": "torch.float64",
              "note": "Shared across iqp_sel_55_repro + V1/V2/V3 + wgan_mlp/cnn/lstm — one critic instance per training run"
            }
          }
        }
        ```

    (5) Drift gate — for each of the 5 models in models{} (wgan_mlp/cnn/lstm/
        vae/ar), load `results/model_info.json` via `_load_json`,
        look up the matching `models[].parameter_count` by `models[].kind ==
        <key>` (the kind values match the classical_architectures.json key
        names verbatim: wgan_mlp/wgan_cnn/wgan_lstm/vae/ar). Collect every
        mismatch into a list; after the loop:
        `if mismatches: raise AssertionError(f"classical_architectures.json drift: {mismatches} — extractor walked total_params != model_info.json parameter_count")`
        — explicit raise (python -O safe), lists every offender (not just
        the first), mirrors 14-09 Task 1 idiom.

    (6) Use `json.dumps(out, indent=2, sort_keys=True)` to write so the
        emitted JSON is stable + diffable. Print written path on success.

    NO training, no sampling, no `.fit(`, no `model.sample(`, no checkpoint
    reload, no `core/` edit. Idempotent — re-running overwrites
    cleanly with byte-identical output.
  </action>
  <verify>
    <automated>./qgan_env/bin/python scripts/run_classical_arch_extract.py &amp;&amp; ./qgan_env/bin/python -c "import json,sys; ca=json.load(open('results/classical_architectures.json')); mi=json.load(open('results/model_info.json')); kinds={'wgan_mlp':74,'wgan_cnn':73,'wgan_lstm':78,'vae':562,'ar':3}; ok=all(k in ca['models'] for k in kinds) and 'shared_critic' in ca['models']; expected_by_kind={m['kind']:m['parameter_count'] for m in mi['models'] if m['kind'] in kinds}; drift=[k for k,v in kinds.items() if ca['models'][k]['total_params'] != expected_by_kind.get(k)]; print('ok',ok,'drift',drift); sys.exit(0 if ok and not drift else 1)" &amp;&amp; grep -q 'raise AssertionError' run_classical_arch_extract.py &amp;&amp; grep -q 'named_modules' run_classical_arch_extract.py &amp;&amp; ! grep -qE '\.fit\(|def train_|model\.sample\(|best_checkpoint\.pt' run_classical_arch_extract.py &amp;&amp; [ -z "$(git diff --stat core/)" ]</automated>
  </verify>
  <acceptance_criteria>
    - `results/classical_architectures.json` exists with schema header `"classical-architectures v1 (Phase 14 plan 14-11)"` and 5 model entries (wgan_mlp/cnn/lstm/vae/ar) + a shared_critic entry
    - Each model entry carries `total_params` matching the corresponding `parameter_count` in `results/model_info.json` (wgan_mlp=74, wgan_cnn=73, wgan_lstm=78, vae=562, ar=3)
    - Each layer_spec carries layer_type + relevant shape fields (in_features/out_features OR kernel_size/in_channels/out_channels/stride OR hidden_size/num_layers) + activation + param_count
    - VAE entry includes the ELBO objective marker + latent_dim=4 + hidden_dim=16 + window=10
    - AR entry includes type="AR(p)" + order_p=2 + fit_method citation
    - shared_critic entry includes dtype="torch.float64" + the source_path citation
    - `grep 'raise AssertionError'` present (drift gate, python -O safe)
    - `grep 'named_modules'` present (the introspection idiom)
    - No training/sampling path: grep finds NO `.fit(` / `def train_` / `model.sample(` / `best_checkpoint.pt`
    - `git diff --stat core/` is empty (D-14-22 byte-freeze)
    - Re-running is idempotent (byte-identical output on second invocation)
  </acceptance_criteria>
  <done>classical_architectures.json emitted with per-model layer trees + total_params drift gate against model_info.json, pure introspection, core byte-frozen.</done>
</task>

<task type="auto">
  <name>Task 2: run_framework_versions.py + framework_versions.json</name>
  <files>run_framework_versions.py, results/framework_versions.json</files>
  <read_first>
    - run_model_info.py:60-100 (the `_find_repo_root()` + REPO/RESULTS anchoring template)
    - revision/requirements.txt (the dependency list: pennylane, torch, numpy, scipy, matplotlib + transitively PyYAML — these are the packages the emitter MUST query)
    - run_model_info.py:58 (`import yaml` — proves PyYAML is in the runtime; framework_versions MUST include it)
  </read_first>
  <action>
    Create `scripts/run_framework_versions.py` as a pure-introspection
    emitter. Use the standard repo bootstrap from `run_model_info.py:60-100`
    (module docstring noting introspection-only + no network; `_find_repo_root()`
    + REPO/RESULTS anchoring).

    Implementation:

    (1) Import:
        `from importlib.metadata import version, PackageNotFoundError`
        `import sys, platform, datetime, json`

    (2) Define the package list as a constant (Claude's discretion on naming
        but stable order matters for diffable output):
        `PACKAGES = ["pennylane", "torch", "numpy", "scipy", "matplotlib", "PyYAML"]`
        — these are the runtime dependencies actually used by Phase 14's
        pipeline (pennylane: quantum.py; torch: every model; numpy: data +
        AR; scipy: core/eval helpers; matplotlib: figure suite;
        PyYAML: every run_*.py reading config.yaml). torchvision is in
        requirements.txt but unused by Phase 14 — Claude's discretion whether
        to include it; default: include if importable (record None on
        PackageNotFoundError); preserves coverage without crashing.

    (3) For each package: try `version(name)`, catch PackageNotFoundError,
        record `{name: <version-string-or-null>}` in a `packages` dict.

    (4) Emit `results/framework_versions.json`:
        ```
        {
          "schema": "framework-versions v1 (Phase 14 plan 14-11)",
          "captured_at_iso": "<datetime.datetime.utcnow().isoformat()+'Z'>",
          "python_version": "<sys.version.split()[0]>",
          "platform": "<platform.platform()>",
          "packages": { "pennylane": "0.X.Y", "torch": "2.Y.Z", "numpy": "...", "scipy": "...", "matplotlib": "...", "PyYAML": "..." },
          "note": "Recorded at methods-doc emit time from the running qgan_env interpreter; requirements.txt carries >= constraints, this JSON carries the exact installed pin. Re-emit on environment change."
        }
        ```

    (5) `json.dumps(out, indent=2, sort_keys=True)` write; print path.

    NO network calls, NO `pip install`, NO subprocess — pure introspection of
    the running interpreter. NO `core/` edit.
  </action>
  <verify>
    <automated>./qgan_env/bin/python scripts/run_framework_versions.py &amp;&amp; ./qgan_env/bin/python -c "import json,sys; fv=json.load(open('results/framework_versions.json')); needed=['pennylane','torch','numpy','scipy','matplotlib','PyYAML']; ok=all(k in fv['packages'] for k in needed) and 'python_version' in fv and 'platform' in fv and 'captured_at_iso' in fv and fv['schema'].startswith('framework-versions v1'); print('ok',ok,'pkgs',{k:fv['packages'].get(k) for k in needed}); sys.exit(0 if ok else 1)" &amp;&amp; grep -q 'importlib.metadata' run_framework_versions.py &amp;&amp; ! grep -qE 'subprocess|requests\.|urllib|pip install' run_framework_versions.py &amp;&amp; [ -z "$(git diff --stat core/)" ]</automated>
  </verify>
  <acceptance_criteria>
    - `results/framework_versions.json` exists with schema header `"framework-versions v1 (Phase 14 plan 14-11)"`, `captured_at_iso`, `python_version`, `platform`, and a `packages` dict
    - The `packages` dict contains keys for at least {pennylane, torch, numpy, scipy, matplotlib, PyYAML} (each either a version string or null on PackageNotFoundError)
    - `grep 'importlib.metadata'` present (the introspection API)
    - No network/install path: grep finds NO `subprocess` / `requests` / `urllib` / `pip install`
    - `git diff --stat core/` is empty
    - Re-running is idempotent under stable env (writes identical bytes for the same installed versions)
  </acceptance_criteria>
  <done>framework_versions.json emitted from importlib.metadata introspection of the running qgan_env interpreter — pinned versions for paper reproducibility.</done>
</task>

<task type="auto">
  <name>Task 3: run_methods_full.py + methods_full.json</name>
  <files>run_methods_full.py, results/methods_full.json</files>
  <read_first>
    - run_model_info.py:60-100 + 627-797 (the pure-aggregator template + cross-artifact data_hash gate at lines 638-649 — copy these idioms verbatim)
    - results/model_info.json (the per-model metadata source for buckets.2_models + buckets.3_training; dataset for buckets.1_dataset)
    - results/canonical_config_lock.json + default_75_config_lock.json + v1/v2/v3_config_lock.json (the gate-level quantum architectures for buckets.2_models[<quantum>])
    - results/classical_architectures.json (Task 1 output — the layer trees for buckets.2_models[<classical>])
    - results/framework_versions.json (Task 2 output — pinned versions for buckets.4_hardware_software.framework_versions)
    - core/training.py (TEXT READ ONLY — grep for line numbers of torch.manual_seed/np.random.seed/random.seed at lines 245-249; critic_loss at line 364; generator_loss at line 385; gradient_penalty at line 72; dtype split at line 268)
    - run_matched2000.py:1-80 (the docstring slice that becomes buckets.5_reproducibility.rerun_command_template — read verbatim, do NOT paraphrase)
    - run_figure_suite.py:144-174 (the loud-fail _require/_load_json idiom)
  </read_first>
  <action>
    Create `scripts/run_methods_full.py` as a pure aggregator emitting
    `results/methods_full.json` — the paper-ready Methods bucket
    JSON the doc renders from.

    Open with the standard bootstrap from `run_model_info.py:60-100`:
    module docstring (purpose, pure-aggregator constraint, NO torch import,
    NO pennylane import, NO `revision.core.*` import — `core/training.py`
    and `run_matched2000.py` are opened via `path.read_text()` ONLY,
    never imported as Python modules); `_find_repo_root()`; REPO/RESULTS/DOCS
    anchoring; `_require` / `_load_json` from run_figure_suite.py idiom.

    Implementation steps:

    (1) Load every consumed JSON via `_load_json` (loud-fail FileNotFoundError
        on absence — mirrors 14-09 / 14-10 contract):
        - `model_info.json` (the per-model registry)
        - `canonical_config_lock.json` (iqp_sel_55)
        - `default_75_config_lock.json`
        - `v1_config_lock.json`, `v2_config_lock.json`, `v3_config_lock.json`
        - `classical_architectures.json` (Task 1)
        - `framework_versions.json` (Task 2)

    (2) Cross-artifact data_hash gate (run_model_info.py:638-649 idiom).
        Collect data_hash fields from every consumed JSON that carries one
        (model_info.json and all 5 config-lock JSONs carry data_hash;
        classical_architectures.json and framework_versions.json carry NO
        data_hash — they are introspection artifacts not data artifacts —
        so exclude them from the gate). If the set of collected hashes
        differs from `{"91e447d4624e25b3"}`:
        `raise AssertionError(f"data_hash mismatch across consumed artifacts: {hashes}")`
        Explicit raise, python -O safe.

    (3) Read `core/training.py` as text:
        `training_src = (REPO / "core/training.py").read_text()`
        For each grep target — `torch.manual_seed`, `np.random.seed`,
        `random.seed`, `critic_loss = `, `generator_loss = `, `gp = (`,
        `compute_dtype = ` — split training_src on `\n`, enumerate lines, find
        the first matching line per pattern, record as `"core/training.py:<lineno>"`.
        Do NOT execute training.py.

    (4) Read `run_matched2000.py:1-80` (the leading module docstring)
        as text:
        `matched_src = (REPO / "run_matched2000.py").read_text()`
        Slice between the first `"""` and the second `"""` (the module
        docstring); store as a single string in
        `buckets.5_reproducibility.rerun_command_template`. NEVER paraphrase,
        NEVER summarize, NEVER strip leading/trailing whitespace (preserve
        the docstring verbatim).

    (5) Build the 5-bucket output dict:
        ```
        out = {
          "schema": "methods-full v1 (Phase 14 plan 14-11)",
          "data_hash": "91e447d4624e25b3",
          "consumed_artifacts": { ... map of artifact path -> data_hash or "introspection" ... },
          "buckets": {
            "1_dataset": <verbatim model_info.json.dataset block>,
            "2_models": {
              "iqp_sel_55_headline": {
                "family": "adversarial-quantum",
                "n_params": 55,
                "architecture": {"source": "results/canonical_config_lock.json", "num_qubits": 5, "num_layers": 3, "topology": "range", "encoding": "IQP (Hadamard + RZ per qubit)", "variational_block": "SEL (Rot(phi,theta,omega) per qubit per layer)", "final_rotation": "RX_only", "checkpoint_epoch": 1969},
                "training_objective": {
                  "name": "WGAN-GP (Wasserstein with two-sided gradient penalty)",
                  "source_path": "core/training.py:364 (critic loss) + core/training.py:385 (generator loss) + core/training.py:72 (gradient penalty)",
                  "equation_latex_critic": "L_C = \\\\mathbb{E}_{\\\\tilde x \\\\sim P_g}[C(\\\\tilde x)] - \\\\mathbb{E}_{x \\\\sim P_r}[C(x)] + \\\\lambda_{gp}\\\\, \\\\mathbb{E}_{\\\\hat x \\\\sim P_{\\\\hat x}}\\\\big[(\\\\|\\\\nabla_{\\\\hat x} C(\\\\hat x)\\\\|_2 - 1)^2\\\\big]",
                  "equation_latex_generator": "L_G = - \\\\mathbb{E}_{\\\\tilde x \\\\sim P_g}[C(\\\\tilde x)]"
                }
              },
              "iqp_sel_55_repro":  { ... mirrors headline but source=canonical_config_lock.json + epochs=2000 + no checkpoint_epoch ... },
              "V1":  { ... reads v1_config_lock.json: num_layers=4, topology=range, n_params=75, final_rotation=RX_plus_RY ... },
              "V2":  { ... v2: num_layers=8, n_params=135 ... },
              "V3":  { ... v3: topology=linear, n_params=75 ... },
              "wgan_mlp":  {
                "family": "adversarial-classical",
                "n_params": 74,
                "architecture": {"source": "results/classical_architectures.json", "layers": <classical_architectures.models.wgan_mlp.generator>},
                "training_objective": {"name": "WGAN-GP (same as quantum)", "source_path": "core/training.py:364 (critic loss) + core/training.py:385 (generator loss)", "equation_latex_critic": "(same as above)", "equation_latex_generator": "(same as above)"}
              },
              "wgan_cnn":  { ... },
              "wgan_lstm": { ... },
              "vae": {
                "family": "non-adversarial",
                "n_params": 562,
                "architecture": {"source": "results/classical_architectures.json", "encoder": <...>, "decoder": <...>, "latent_dim": 4, "hidden_dim": 16, "window": 10},
                "training_objective": {
                  "name": "ELBO (variational autoencoder)",
                  "source_path": "run_baselines.py (ELBO training loop — NOT in core/training.py which is WGAN-GP only; D-10-13)",
                  "equation_latex": "\\\\mathcal{L}_{ELBO}(x) = \\\\mathbb{E}_{q_\\\\phi(z|x)}[\\\\log p_\\\\theta(x|z)] - D_{KL}(q_\\\\phi(z|x) \\\\| p(z))"
                }
              },
              "ar": {
                "family": "non-adversarial",
                "n_params": 3,
                "architecture": {"source": "results/classical_architectures.json", "type": "AR(p)", "order_p": 2, "fit_method": "np.linalg.lstsq (closed-form)"},
                "training_objective": {
                  "name": "Conditional Gaussian likelihood (closed-form least squares)",
                  "source_path": "core/models/nonadversarial.py:139-158 (lstsq fit, no training loop)",
                  "equation_latex": "x_t = \\\\sum_{k=1}^{p} \\\\phi_k\\\\, x_{t-k} + \\\\varepsilon_t,\\\\quad \\\\varepsilon_t \\\\sim \\\\mathcal{N}(0, \\\\sigma^2);\\\\quad \\\\hat\\\\phi = \\\\arg\\\\min_\\\\phi \\\\|X\\\\phi - y\\\\|_2^2"
                }
              }
            },
            "3_training": {
              "optimizer": "Adam",
              "optimizer_betas": [0.0, 0.9],
              "lr_critic": <from model_info.json iqp_sel_55_repro lr_critic>,
              "lr_generator": <from model_info.json iqp_sel_55_repro lr_generator>,
              "n_critic": <from model_info.json>,
              "lambda_gp": 2.16,
              "batch_size": <from model_info.json>,
              "epochs": 2000,
              "early_stopping": "OFF for matched-budget reproduction (D-14-13); ON during original training that produced the frozen-checkpoint headline at epoch 1969",
              "seeds": [42, 43, 44, 45, 46]
            },
            "4_hardware_software": {
              "device": "cpu (PennyLane default.qubit is CPU-only; classical runs also pinned to CPU for parity)",
              "pennylane_device": "default.qubit",
              "diff_method": "backprop",
              "dtype_params": "torch.float32 (classical: nn.Parameter constructed with dtype=torch.float32 — core/models/classical.py:78; quantum: params_pqc nn.Parameter dtype=torch.float32)",
              "dtype_samples": "torch.float64 (sample-generation pipeline: core/training.py:268 compute_dtype = torch.float64 on CPU/CUDA; core/training.py:347 generator output cast .to(compute_dtype) * 0.1; MPS path falls back to float32 because MPS lacks float64 — see training.py:259-268)",
              "dtype_note": "dtype_params and dtype_samples are DISTINCT — params live in float32 (the trainable nn.Parameter), samples are cast to float64 at generation time on CPU/CUDA to match the float64 critic (core/models/critic.py:67 .double()). The two fields MUST NOT be conflated.",
              "framework_versions": <verbatim framework_versions.json.packages>,
              "python_version": <verbatim framework_versions.json.python_version>,
              "platform": <verbatim framework_versions.json.platform>
            },
            "5_reproducibility": {
              "data_hash": "91e447d4624e25b3",
              "seed_set": [42, 43, 44, 45, 46],
              "determinism_contract": {
                "manual_seed": "core/training.py:245 (torch.manual_seed(seed))",
                "numpy_seed":  "core/training.py:246 (np.random.seed(seed))",
                "random_seed": "core/training.py:247 (random.seed(seed))",
                "cuda_seed":   "core/training.py:248-249 (torch.cuda.manual_seed_all(seed) if cuda available)",
                "note": "Seeds are set ONCE at the top of train_wgan_gp before optimizer/data construction (training.py:244-249). The seed is one of {42,43,44,45,46}; the same seed produces bit-identical training trajectories on the same device/dtype path."
              },
              "rerun_command_template": <verbatim run_matched2000.py module docstring slice>,
              "rerun_command_template_source": "run_matched2000.py:1-80 (module docstring; preserved verbatim — never paraphrased)"
            }
          }
        }
        ```

    (6) `json.dumps(out, indent=2, sort_keys=False)` (preserve bucket order;
        do NOT sort keys for this artifact — bucket ordering is semantic).
        Print written path + data_hash on success.

    No `import torch`, no `import pennylane`, no `from revision.core ...`
    import in this file (pure aggregator); the loss-equation LaTeX strings
    and the citation file:line numbers come from TEXT reads of training.py
    + nonadversarial.py + run_matched2000.py, never from executing them.
  </action>
  <verify>
    <automated>./qgan_env/bin/python scripts/run_methods_full.py &amp;&amp; ./qgan_env/bin/python -c "import json,sys; mf=json.load(open('results/methods_full.json')); b=mf['buckets']; req_buckets=['1_dataset','2_models','3_training','4_hardware_software','5_reproducibility']; req_models=['iqp_sel_55_headline','iqp_sel_55_repro','V1','V2','V3','wgan_mlp','wgan_cnn','wgan_lstm','vae','ar']; ok=mf['data_hash']=='91e447d4624e25b3' and all(k in b for k in req_buckets) and all(m in b['2_models'] for m in req_models) and 'dtype_params' in b['4_hardware_software'] and 'dtype_samples' in b['4_hardware_software'] and b['4_hardware_software']['dtype_params']!=b['4_hardware_software']['dtype_samples'] and 'manual_seed' in b['5_reproducibility']['determinism_contract'] and 'training.py:245' in b['5_reproducibility']['determinism_contract']['manual_seed'] and len(b['5_reproducibility']['rerun_command_template'])>200; print('ok',ok); sys.exit(0 if ok else 1)" &amp;&amp; grep -q 'raise AssertionError' run_methods_full.py &amp;&amp; ! grep -qE 'import torch|import pennylane|from revision\.core' run_methods_full.py &amp;&amp; ! grep -qE '\.fit\(|def train_|model\.sample\(|best_checkpoint\.pt' run_methods_full.py &amp;&amp; [ -z "$(git diff --stat core/ verify_number_provenance.py)" ]</automated>
  </verify>
  <acceptance_criteria>
    - `results/methods_full.json` exists with schema `"methods-full v1 (Phase 14 plan 14-11)"`, `data_hash == "91e447d4624e25b3"`, and 5 buckets (1_dataset, 2_models, 3_training, 4_hardware_software, 5_reproducibility)
    - `buckets.2_models` contains the 10 model keys: iqp_sel_55_headline, iqp_sel_55_repro, V1, V2, V3, wgan_mlp, wgan_cnn, wgan_lstm, vae, ar
    - Each model entry carries `family`, `n_params`, `architecture` (with a `source` field pointing to a JSON path), and `training_objective` (with `name`, `source_path` file:line citation, and at least one `equation_latex*` field)
    - `buckets.4_hardware_software` has TWO DISTINCT fields `dtype_params` and `dtype_samples` with different values; a `dtype_note` paragraph explicitly states they MUST NOT be conflated
    - `buckets.5_reproducibility.determinism_contract` carries file:line citations matching `core/training.py:245` (manual_seed), `:246` (numpy_seed), `:247` (random_seed)
    - `buckets.5_reproducibility.rerun_command_template` is a multi-line string ≥ 200 characters (the verbatim run_matched2000.py:1-80 docstring slice)
    - `grep 'raise AssertionError'` present (the cross-artifact data_hash gate)
    - No torch/pennylane/core import: grep finds NO `import torch` / `import pennylane` / `from revision.core` in run_methods_full.py
    - No training/sampling path: grep finds NO `.fit(` / `def train_` / `model.sample(` / `best_checkpoint.pt`
    - `git diff --stat core/ verify_number_provenance.py` is empty (byte-freeze + gate unmodified)
    - Re-running is idempotent (byte-identical output on second invocation under stable inputs)
  </acceptance_criteria>
  <done>methods_full.json emitted as the 5-bucket aggregator over model_info.json + 5 config-lock JSONs + classical_architectures.json + framework_versions.json + text-only citations into training.py + verbatim run_matched2000.py docstring; dtype contradiction resolved; cross-artifact data_hash gate explicit-raise.</done>
</task>

<task type="auto">
  <name>Task 4: Author docs/methods_full.md (paper-ready, gated)</name>
  <files>docs/methods_full.md</files>
  <read_first>
    - results/methods_full.json (Task 3 — the SOLE numeric + equation source for every literal + LaTeX block in the doc)
    - results/model_info.json + canonical_config_lock.json + default_75_config_lock.json + v1/v2/v3_config_lock.json + classical_architectures.json + framework_versions.json (the auto-rglob'd JSONs the gate resolves literals against)
    - docs/training_protocol.md + docs/dataset_stats.md + docs/circuit_atlas.md (existing prose conventions to mirror — "Source of truth" callout, `| Constant | Value | Source |` table layout, provenance footer)
    - verify_number_provenance.py (the gate that this doc MUST pass with the verifier unmodified)
    - .planning/phases/14-paper-revision-release-freeze/14-09-SUMMARY.md (the 14-09 pattern for render-from-JSON paper-ready docs and the "contradiction-addressing" framing convention)
  </read_first>
  <action>
    Author `docs/methods_full.md` as a paper-ready, copy-paste-ready
    Methods document. EVERY numeric literal must resolve via the existing
    `scripts/verify_number_provenance.py` rglob to one of:
    `results/methods_full.json`, `model_info.json`,
    `canonical_config_lock.json`, `default_75_config_lock.json`,
    `v1/v2/v3_config_lock.json`, `classical_architectures.json`,
    `framework_versions.json`.

    Document structure (Claude's discretion on exact prose; structure below
    is mandatory):

    # Section 0 — Front-matter (preamble)
    Short opening paragraph: this doc consolidates the Methods section for the
    revised manuscript (PAPER-08 + PAPER-09); every number traces to a JSON
    artifact under `results/` gated by
    `scripts/verify_number_provenance.py`; the LaTeX equation strings are
    rendered verbatim from `methods_full.json` (never hand-typed); the doc
    explicitly addresses two documented contradictions (default_75 vs
    iqp_sel_55, dtype_params vs dtype_samples) in §6.

    # Section 1 — Dataset
    Render the dataset block from `methods_full.json.buckets.1_dataset` (which
    is itself a verbatim copy of `model_info.json.dataset`). Use the existing
    `| Quantity | Value | Source |` table layout from
    `docs/dataset_stats.md` (mirror, do not invent). Cite both
    `methods_full.json` AND `model_info.json` as sources. Counts: raw_csv_rows,
    od_rows_after_fillna_dropna, log_return_rows, window_length, window_stride,
    rolling_windows, independent_campaigns, train/val/test_windows. Append a
    one-sentence "single-campaign limitation" remark.

    # Section 2 — Models (per-model subsection)
    One subsection per model in `buckets.2_models`. For each:
    - Header naming the model + its family (quantum / classical / non-adversarial)
    - Architecture spec table:
      * For quantum (iqp_sel_55_headline, iqp_sel_55_repro, V1, V2, V3):
        ``| Property | Value | Source |`` with rows for num_qubits, num_layers,
        topology, encoding, variational_block, final_rotation, n_params,
        sourced from the corresponding config-lock JSON.
        For iqp_sel_55_headline ONLY also include a `checkpoint_epoch=1969` row.
      * For classical (wgan_mlp, wgan_cnn, wgan_lstm): a layer-tree table
        rendered FROM `classical_architectures.json.models.<name>.generator`,
        plus a `total_params` row sourced from `classical_architectures.json`
        + `model_info.json`.
      * For vae: encoder + decoder layer trees from
        `classical_architectures.json.models.vae`, plus latent_dim=4, hidden_dim=16,
        window=10, total_params=562.
      * For ar: a small table with type=AR(p), order_p=2, fit_method, total_params=3.
    - Training-objective block: name (e.g. "WGAN-GP", "ELBO", "AR(p) conditional
      Gaussian likelihood"), source_path file:line citation, and the LaTeX
      equation string rendered VERBATIM from
      `methods_full.json.buckets.2_models.<name>.training_objective.equation_latex*`
      inside a fenced ```latex ... ``` block (no manual editing of the
      equation strings — the doc renders them as-is).

    # Section 3 — Training
    Single table sourced from `methods_full.json.buckets.3_training`:
    ``| Property | Value | Source |`` with rows for optimizer, optimizer_betas,
    lr_critic, lr_generator, n_critic, lambda_gp, batch_size, epochs,
    early_stopping, seeds.
    Append a one-sentence note that the matched-budget reproduction
    (`iqp_sel_55_repro` + V1/V2/V3 + classicals) trained the FULL 2000 epochs
    with early-stopping OFF (D-14-13), while the frozen-checkpoint headline
    (`iqp_sel_55_headline`) is the best-EMD checkpoint at epoch 1969 from the
    original early-stopping-ON campaign.

    # Section 4 — Hardware & Software
    Render from `methods_full.json.buckets.4_hardware_software`. THIS SECTION
    MUST contain TWO DISTINCT rows in the spec table:
    ``| Property | Value | Source |``
    | Compute device | cpu | methods_full.json buckets.4 device |
    | PennyLane backend | default.qubit | methods_full.json buckets.4 pennylane_device |
    | Differentiation | backprop | methods_full.json buckets.4 diff_method |
    | **Param dtype (`dtype_params`)** | torch.float32 | methods_full.json buckets.4 dtype_params |
    | **Sample dtype (`dtype_samples`)** | torch.float64 (CPU/CUDA path; float32 on MPS) | methods_full.json buckets.4 dtype_samples |
    | Python version | <from framework_versions.json> | methods_full.json buckets.4 python_version |
    | Platform | <from framework_versions.json> | methods_full.json buckets.4 platform |
    Sub-table for framework_versions: one row per package (pennylane, torch,
    numpy, scipy, matplotlib, PyYAML), each cell read from
    `framework_versions.json.packages.<name>`.
    Append a paragraph explicitly stating that `dtype_params` (the trainable
    nn.Parameter dtype, float32) and `dtype_samples` (the sample-generation
    dtype, float64 on CPU/CUDA) are DISTINCT — params are kept in float32
    because the optimizer is parameterized in float32, samples are cast to
    float64 at generation time to match the float64 critic
    (core/models/critic.py:67 .double()).

    # Section 5 — Reproducibility
    Render from `methods_full.json.buckets.5_reproducibility`:
    - data_hash row (== 91e447d4624e25b3)
    - seed_set row (== [42, 43, 44, 45, 46])
    - Determinism contract table:
      ``| Seed call | Source |``
      | torch.manual_seed(seed) | core/training.py:245 |
      | np.random.seed(seed)    | core/training.py:246 |
      | random.seed(seed)       | core/training.py:247 |
      | torch.cuda.manual_seed_all(seed) | core/training.py:248-249 |
    - Exact rerun-command block (fenced ``` … ``` code block) — render
      VERBATIM from `methods_full.json.buckets.5_reproducibility.rerun_command_template`
      (which is the run_matched2000.py:1-80 docstring slice). Do NOT
      paraphrase. Do NOT strip indentation. Append a one-line citation:
      "Source: run_matched2000.py:1-80 (module docstring)".

    # Section 6 — Addressing the documented contradictions
    Two explicit one-paragraph clarifications, each cross-linked to the
    config-lock JSONs and source files that resolve the contradiction:

    (a) **default_75 vs iqp_sel_55 — which is "default"?** Both are valid,
        production quantum circuits used in the manuscript. `default_75`
        (4 layers, range topology, 75 parameters, final RX+RY per qubit) is
        the byte-frozen v1.0/v1.1 baseline encoded in
        `core/__init__.py` (NUM_QUBITS=5, NUM_LAYERS=4) and is the
        underlying circuit for the matched-budget ansatz variants V1/V2/V3
        — see `results/default_75_config_lock.json` and
        `results/v1_config_lock.json` / `v2_config_lock.json` /
        `v3_config_lock.json`. `iqp_sel_55` (3 layers, range topology, 55
        parameters, final RX-only per qubit) is the **canonical paper
        circuit** recovered from the frozen checkpoint at epoch 1969 (D-14-01)
        — see `results/canonical_config_lock.json`. The manuscript's
        headline quantum entrant in every cross-model comparison is
        `iqp_sel_55`; `default_75` underlies the matched-budget ansatz study
        that demonstrates `iqp_sel_55` is competitive at lower parameter
        count. Neither is "wrong" — they serve different purposes and live
        in different config-lock JSONs.

    (b) **`dtype_params` vs `dtype_samples` — distinct, never the same.**
        Trainable parameters live in `torch.float32` for every model (every
        `nn.Parameter` in `core/models/classical.py` is constructed
        with `dtype=torch.float32`; the quantum generator's `params_pqc` is
        also float32 — see `core/models/classical.py:78` and
        analogous lines for CNN/LSTM). Generated samples are cast to
        `torch.float64` at generation time on the CPU/CUDA path
        (`core/training.py:268` `compute_dtype = torch.float32 if
        device.type == "mps" else torch.float64`, then
        `core/training.py:347` `generated_samples.to(compute_dtype)
        * 0.1`). The MPS path falls back to `torch.float32` because MPS lacks
        float64 — explicit `compute_dtype` branching at training.py:259-268.
        These two dtypes are DISTINCT fields in
        `methods_full.json.buckets.4_hardware_software`; a previous internal
        document conflated them, which this Methods doc resolves.

    # Section 7 — Provenance footer
    Short footer:
    - All numeric literals in this document resolve to one of:
      `methods_full.json`, `model_info.json`, `canonical_config_lock.json`,
      `default_75_config_lock.json`, `v1_config_lock.json`,
      `v2_config_lock.json`, `v3_config_lock.json`,
      `classical_architectures.json`, `framework_versions.json` — all under
      `results/`.
    - Gated by `./qgan_env/bin/python scripts/verify_number_provenance.py --target docs/methods_full.md`.
    - LaTeX equation strings are rendered verbatim from `methods_full.json`
      (never hand-typed in this document).

    Numeric-discipline rule: every number in this doc MUST appear verbatim
    (or as a float-precision match) in at least one of the gate's resolution
    JSONs. The gate auto-strips decision IDs (D-14-NN), phase numbers, and
    line-citations (`.py:NN`) so prose references like "D-14-22", "epoch
    1969", "core/training.py:245" do not need provenance for the
    "D-14-NN" / "training.py:245" portion — the digit `1969` IS a literal
    that must resolve (and it does, via `canonical_config_lock.json.checkpoint_epoch`).
    Framework version strings (e.g. "0.36.0", "2.3.1") resolve via
    `framework_versions.json.packages`.

    After writing, run:
    `./qgan_env/bin/python scripts/verify_number_provenance.py --target docs/methods_full.md`
    — MUST pass. If a literal fails to resolve, the fix is either to widen
    the JSON's stored string (rerun the emitter with an adjusted
    `gate_layout_breakdown`-style breakdown field) or to adjust the doc's
    literal to match an already-stored spelling. NEVER edit
    `scripts/verify_number_provenance.py`.
  </action>
  <verify>
    <automated>[ -f docs/methods_full.md ] &amp;&amp; ./qgan_env/bin/python scripts/verify_number_provenance.py --target docs/methods_full.md &amp;&amp; ./qgan_env/bin/python -c "import pathlib,sys; t=pathlib.Path('docs/methods_full.md').read_text(); needed=['methods_full.json','classical_architectures.json','framework_versions.json','canonical_config_lock.json','default_75_config_lock.json','v1_config_lock.json','v2_config_lock.json','v3_config_lock.json','model_info.json','dtype_params','dtype_samples','torch.manual_seed','training.py:245','training.py:246','training.py:247','default_75','iqp_sel_55','canonical paper circuit','frozen checkpoint','run_matched2000.py:1-80','verify_number_provenance.py','ELBO','WGAN-GP','AR(p)']; missing=[n for n in needed if n not in t]; print('missing',missing); sys.exit(0 if not missing else 1)" &amp;&amp; [ -z "$(git diff --stat core/ verify_number_provenance.py)" ]</automated>
  </verify>
  <acceptance_criteria>
    - `docs/methods_full.md` exists with the 7 content sections (Front-matter, Dataset, Models, Training, Hardware-Software, Reproducibility, Address-the-contradictions, Provenance footer)
    - The doc PASSES `./qgan_env/bin/python scripts/verify_number_provenance.py --target docs/methods_full.md`
    - §1 (Dataset) renders the counts from `methods_full.json.buckets.1_dataset` / `model_info.json.dataset`
    - §2 (Models) has one subsection per model (10 models total) with an architecture spec table and a training-objective block carrying both file:line citations and verbatim LaTeX equation strings inside fenced code blocks
    - §3 (Training) renders optimizer/lr/betas/n_critic/lambda_gp/batch_size/epochs/early_stopping/seeds from `methods_full.json.buckets.3_training`
    - §4 (Hardware/Software) has TWO DISTINCT rows for `dtype_params` (torch.float32) and `dtype_samples` (torch.float64), plus a clarifying paragraph explicitly stating they MUST NOT be conflated
    - §5 (Reproducibility) carries the data_hash, seed_set, determinism contract table with the four file:line citations (`training.py:245/246/247/248-249`), and a fenced code block containing the VERBATIM `run_matched2000.py:1-80` docstring rerun-template (NOT paraphrased)
    - §6 (Address-the-contradictions) has two one-paragraph clarifications: one for default_75 vs iqp_sel_55 (cites both `default_75_config_lock.json` and `canonical_config_lock.json`), one for `dtype_params` vs `dtype_samples` (cites `core/training.py:268` and `core/models/classical.py:78`)
    - §7 (Provenance footer) cites the 9 JSON sources and the gate command
    - `git diff --stat core/ verify_number_provenance.py` is empty (D-14-22 byte-freeze + gate unmodified)
  </acceptance_criteria>
  <done>methods_full.md authored as paper-ready, JSON-traceable Methods document with 7 sections; passes verify_number_provenance.py unmodified; both contradictions explicitly addressed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `core/models/*` class definitions → `classical_architectures.json` | a stale extractor or a refactor that changes layer shapes without updating the extractor would emit a misleading architecture JSON and put a wrong layer table in the paper |
| installed-package versions (importlib.metadata) → `framework_versions.json` | a non-deterministic environment (e.g. unpinned PyPI install since the manuscript runs) would record a version different from the one the paper claims; the captured_at_iso timestamp is the audit trail |
| audited JSON inputs → `methods_full.json` | a `data_hash` drift across consumed JSONs (model_info / config-locks) would put incoherent numbers into the paper Methods bucket |
| `core/training.py` source text → `methods_full.json.buckets.5_reproducibility.determinism_contract` | a refactor that moves the seed calls to new line numbers without updating the citation would put a wrong file:line into the paper |
| `run_matched2000.py:1-80` docstring → `methods_full.json.buckets.5_reproducibility.rerun_command_template` | paraphrasing or truncating the docstring would emit non-runnable rerun instructions |
| `methods_full.json` → `docs/methods_full.md` | a hand-typed numeric literal in the doc would bypass the number-provenance gate and put an unverifiable number in PAPER-08/09 |
| `core/models/classical.py` import (for named_modules()) | importing for introspection-only must NOT trigger any `.fit(` / `.sample(` / training-loop path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-25 | Tampering | `classical_architectures.json` total_params drift relative to `model_info.json` | mitigate | Task 1 explicit `raise AssertionError` (python -O safe) — extractor's walked total_params for each of {wgan_mlp,cnn,lstm,vae,ar} MUST equal `model_info.json` models[].parameter_count for the same kind; loud-fail lists every offender |
| T-14-26 | Tampering | importing `revision.core.models.classical` silently triggers a training path | mitigate | Pure-introspection-safety precedent (14-09 verified the same for revision.core.models.quantum): the module's top-level imports are torch + torch.nn + torch.nn.functional + numpy; class instantiation runs no forward pass. Task 1 verify gate `! grep -qE '\.fit\(|def train_|model\.sample\(|best_checkpoint\.pt'` in the script |
| T-14-27 | Tampering | `framework_versions.json` recorded from a non-deterministic environment | accept (with audit trail) | `captured_at_iso` + `python_version` + `platform` fields are recorded so any version drift is visibly attributable to a documented env change; re-emit on environment change. The JSON is regenerated per release-freeze cycle (14-07 gate). |
| T-14-28 | Tampering | `data_hash` drift across consumed JSONs would put incoherent numbers into `methods_full.json` | mitigate | Task 3 cross-artifact `raise AssertionError` (python -O safe) gate over every consumed JSON that carries a `data_hash` field; expected value `91e447d4624e25b3` matches `run_matched2000.py:106 EXPECTED_DATA_HASH` and `run_model_info.py:649` canonical hash |
| T-14-29 | Tampering | training.py refactor moves seed/loss lines, citation in methods_full.json goes stale | mitigate | Task 3 reads `core/training.py` as text and greps for the call-site patterns at emit time — citations are regenerated on every emitter run, so a refactor cannot leave stale citations without a regeneration (the determinism_contract is itself audit-tracked via `git diff results/methods_full.json` between emissions) |
| T-14-30 | Spoofing | hand-typed numeric literal in methods_full.md (bypassing the JSON corpus) | mitigate | Task 4 verify gate: `./qgan_env/bin/python scripts/verify_number_provenance.py --target docs/methods_full.md` — every literal must resolve to a `results/*.json` value via the gate's rglob; verifier UNMODIFIED |
| T-14-31 | Tampering | LaTeX equation string hand-typed in methods_full.md (could diverge from training.py's actual loss expression) | mitigate | Task 4 contract: equation_latex strings live ONLY in `methods_full.json.buckets.2_models.<name>.training_objective.equation_latex*` and are rendered VERBATIM into fenced ```latex blocks in the doc; the doc itself contains no equation authoring, only equation rendering |
| T-14-32 | Tampering | rerun_command_template paraphrased instead of verbatim from run_matched2000.py docstring | mitigate | Task 3 contract: text-slice the leading `"""..."""` docstring of run_matched2000.py and store as a single string; Task 4 renders it as a fenced code block verbatim; verify gate asserts the template length > 200 chars (full docstring, not a one-line summary) |
| T-14-33 | Tampering | `core/` edit slips in (would break D-14-22 byte-freeze and the 11 frozen-default reproducibility plans) | mitigate | Every task's verify gate asserts `[ -z "$(git diff --stat core/)" ]` |
| T-14-34 | Tampering | `scripts/verify_number_provenance.py` edited to make methods_full.md pass | mitigate | Tasks 3 + 4 verify gates assert `[ -z "$(git diff --stat verify_number_provenance.py)" ]` — the gate is the executable success-criterion-5 enforcement; widening it to accept hand-typed numbers would defeat the contract |
</threat_model>

<verification>
- `scripts/run_classical_arch_extract.py` emits `classical_architectures.json` with 5 model entries (wgan_mlp/cnn/lstm/vae/ar) + shared_critic; total_params drift gate passes against model_info.json; pure introspection (no training/sampling); core byte-frozen.
- `scripts/run_framework_versions.py` emits `framework_versions.json` with pinned versions for {pennylane, torch, numpy, scipy, matplotlib, PyYAML} + python_version + platform + captured_at_iso; no network calls.
- `scripts/run_methods_full.py` emits `methods_full.json` with 5 buckets, cross-artifact `data_hash` explicit-raise gate, file:line citations into `core/training.py` from text-only reads, verbatim docstring slice from `run_matched2000.py:1-80`, DISTINCT `dtype_params` vs `dtype_samples` fields; no torch/pennylane/core import; no training/sampling path.
- `docs/methods_full.md` exists with 7 content sections, PASSES `scripts/verify_number_provenance.py` unmodified, addresses both documented contradictions in §6, renders LaTeX equation strings verbatim from methods_full.json.
- `core/` byte-freeze (D-14-22) preserved; `scripts/verify_number_provenance.py` UNMODIFIED (D-14-16).
- ROADMAP.md Wave 9 added after Wave 8 referencing 14-11; progress table updated 9/10 → 10/11 (plans denominator increments from 10 to 11, completed count from 9 to 10); plans header updated `10 plans` → `11 plans`; D-14-23 scope note extended one sentence.
</verification>

<success_criteria>
The paper Methods section now has ONE consolidated, JSON-traceable, paper-ready
document (`docs/methods_full.md`) backed by 3 new audited JSONs
(`classical_architectures.json`, `framework_versions.json`, `methods_full.json`).
Classical model architectures are programmatically extracted (no hand-typed
layer trees); framework versions are pinned for reproducibility (no
`>=` constraints); the determinism contract carries exact file:line citations
into `core/training.py`; rerun instructions are verbatim from
`run_matched2000.py:1-80`. Both documented contradictions are
explicitly resolved in §6. Every numeric literal in the doc traces to a JSON
artifact under `results/`; the doc passes
`scripts/verify_number_provenance.py` unmodified. `core/` is byte-frozen.
PAPER-08 + PAPER-09 are now Methods-complete.
</success_criteria>

<output>
Create `.planning/phases/14-paper-revision-release-freeze/14-11-SUMMARY.md` when done
</output>
</content>
</invoke>