---
phase: 01-foundation-and-correctness-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [qgan_pennylane.ipynb]
autonomous: true
requirements: [BUG-01, BUG-07, QUAL-04, QUAL-05, QUAL-09]

must_haves:
  truths:
    - "Checkpoint save uses 'critic_state' key (not 'discriminator')"
    - "Checkpoint load uses 'critic_state' key and restores params_pqc as nn.Parameter with gradient tracking"
    - "torch.load uses weights_only=True"
    - "No exit() call exists in the notebook"
    - "No eval() call used for variable lookup"
    - "self.measurements is not defined or referenced anywhere in the class"
  artifacts:
    - path: "qgan_pennylane.ipynb"
      provides: "Safe checkpoint system and removal of unsafe code patterns"
  key_links:
    - from: "save_checkpoint (cell 23)"
      to: "load_checkpoint (cell 23)"
      via: "matching dictionary keys: critic_state, params_pqc, c_optimizer, g_optimizer, epoch"
      pattern: "critic_state"
---

<objective>
Fix the checkpoint save/load system and remove unsafe code patterns (exit(), eval(), unused self.measurements) from the notebook.

Purpose: Establish a safe, correct checkpoint system that preserves gradient tracking on load, and eliminate code patterns that crash the kernel or pose security risks. These are prerequisites for all subsequent Phase 1 work.

Output: Modified qgan_pennylane.ipynb with correct checkpoint save/load and all unsafe patterns removed.
</objective>

<execution_context>
@/Users/shawngibford/.claude/get-shit-done/workflows/execute-plan.md
@/Users/shawngibford/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-correctness-infrastructure/01-RESEARCH.md

<interfaces>
<!-- The notebook is a single file. Key structures the executor needs to understand: -->

Cell 23 contains the qGAN class with:
- save_checkpoint method (currently saves 'discriminator' key, uses detach().clone() for params_pqc)
- load_checkpoint method (currently loads 'discriminator' key, does NOT wrap in nn.Parameter)
- self.measurements list populated in __init__ (lines 945-949) but never read
- torch.load call without weights_only parameter

Cell 29 contains exit() call (line 2829)

Cell 33 contains eval(var_name) for variable existence checking (line 3094)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite checkpoint save/load system</name>
  <files>qgan_pennylane.ipynb</files>
  <action>
In cell 23 (the qGAN class), rewrite the checkpoint save and load methods:

**Save checkpoint method:**
1. Change the saved dictionary keys from `discriminator` to `critic_state` using `model.critic.state_dict()`
2. Save full training state: `params_pqc` (via `detach().clone()`), `critic_state`, `c_optimizer` state_dict, `g_optimizer` state_dict, `epoch`, `critic_loss` (latest value), `generator_loss` (latest value)
3. Use timestamped filenames: `checkpoint_YYYY-MM-DD_HH-MM.pt` using `datetime.now().strftime('%Y-%m-%d_%H-%M')`
4. Keep only the latest checkpoint — before saving, delete all existing `checkpoint_*.pt` files in the checkpoint directory using `Path(checkpoint_dir).glob('checkpoint_*.pt')`
5. Use `Path(checkpoint_dir).mkdir(exist_ok=True)` to ensure directory exists
6. Add `from datetime import datetime` and `from pathlib import Path` to the imports cell (cell 2) if not already present

**Load checkpoint method:**
1. Find the latest checkpoint file using `sorted(Path(checkpoint_dir).glob('checkpoint_*.pt'))[-1]`
2. Use `torch.load(filepath, weights_only=True)` — this is QUAL-05
3. Restore `params_pqc` as `torch.nn.Parameter(checkpoint['params_pqc'])` — this preserves requires_grad=True
4. Restore critic via `model.critic.load_state_dict(checkpoint['critic_state'])`
5. Restore critic optimizer via `model.c_optimizer.load_state_dict(checkpoint['c_optimizer'])`
6. CRITICAL: After assigning new params_pqc, update the generator optimizer's parameter reference: `model.g_optimizer.param_groups[0]['params'] = [model.params_pqc]` THEN `model.g_optimizer.load_state_dict(checkpoint['g_optimizer'])`. This ensures the optimizer tracks the new parameter object, not the old one.
7. Return the loaded epoch number

**Do NOT change:** Any training logic, loss functions, hyperparameter values, or model architecture. Only fix the checkpoint serialization/deserialization.

**Do NOT delete cells** — replace code in place per user decision.
  </action>
  <verify>
    <automated>cd /Users/shawngibford/dev/phd/qGAN && python3 -c "
import json
with open('qgan_pennylane.ipynb') as f:
    nb = json.load(f)
src = ''.join([''.join(c['source']) for c in nb['cells']])
assert 'critic_state' in src, 'Missing critic_state key'
assert 'discriminator' not in src or 'discriminator' in src.split('critic')[0][:100], 'Old discriminator key still present in checkpoint code'
assert 'weights_only=True' in src, 'Missing weights_only=True'
assert 'nn.Parameter' in src or 'torch.nn.Parameter' in src, 'Missing nn.Parameter wrapper for params_pqc load'
assert 'param_groups' in src, 'Missing optimizer param_groups re-registration'
print('PASS: Checkpoint system verified')
"</automated>
  </verify>
  <done>Checkpoint save uses critic_state key with timestamped filename; checkpoint load uses weights_only=True and restores params_pqc as nn.Parameter with optimizer re-registration</done>
</task>

<task type="auto">
  <name>Task 2: Remove unsafe code patterns</name>
  <files>qgan_pennylane.ipynb</files>
  <action>
Three localized fixes in the notebook:

**BUG-07 — Remove exit() (Cell 29):**
Replace the `exit()` call with a descriptive print statement or comment. Do NOT delete the cell — replace the exit() line in place. Example replacement: `print("Training complete. Proceed to analysis cells below.")` or a comment explaining the cell's original purpose.

**QUAL-04 — Replace eval() (Cell 33):**
In the `debug_and_fix_generation()` function (or wherever `eval(var_name)` appears around line 3094), replace the eval-based variable existence check with `globals().get()` or `var_name in globals()`. The current pattern is:
```python
try:
    var_value = eval(var_name)
    print(f"{var_name}: Found")
except NameError:
    print(f"{var_name}: MISSING!")
```
Replace with:
```python
if var_name in globals():
    print(f"{var_name}: Found")
else:
    print(f"{var_name}: MISSING!")
    missing_vars.append(var_name)
```
Preserve the list of required variable names being checked. Add a `missing_vars = []` list before the loop to track missing variables.

**QUAL-09 — Remove unused self.measurements (Cell 23):**
In the qGAN class `__init__` method, remove the lines (approximately lines 945-949) that populate `self.measurements` with `qml.expval()` objects. These measurement objects are created in __init__ but never referenced — the circuit function builds its own measurements locally. Remove the `self.measurements = [...]` assignment and any related comments. Verify by grep that no other code references `self.measurements`.

**Do NOT change:** Any training logic, model behavior, or cell ordering.
  </action>
  <verify>
    <automated>cd /Users/shawngibford/dev/phd/qGAN && python3 -c "
import json
with open('qgan_pennylane.ipynb') as f:
    nb = json.load(f)
src = ''.join([''.join(c['source']) for c in nb['cells']])
# Check exit() removed (allow 'exit' in strings/comments but not as function call)
import re
exit_calls = re.findall(r'(?<![a-zA-Z_])exit\s*\(\s*\)', src)
assert len(exit_calls) == 0, f'exit() still present: {exit_calls}'
# Check eval() for variable lookup removed
eval_lookups = re.findall(r'eval\s*\(\s*var_name\s*\)', src)
assert len(eval_lookups) == 0, f'eval(var_name) still present'
# Check self.measurements removed
assert 'self.measurements' not in src, 'self.measurements still present'
print('PASS: Unsafe patterns removed')
"</automated>
  </verify>
  <done>exit() call replaced with informative message; eval() replaced with globals()-based lookup; unused self.measurements removed from __init__</done>
</task>

</tasks>

<verification>
After both tasks complete, verify the combined state:
1. No `discriminator` key in checkpoint save/load code
2. `weights_only=True` present in all torch.load calls
3. `nn.Parameter` wrapper used when loading params_pqc
4. No `exit()` calls in notebook
5. No `eval(var_name)` patterns in notebook
6. No `self.measurements` in notebook
</verification>

<success_criteria>
- Checkpoint system saves and loads using 'critic_state' key with timestamped filenames
- torch.load always uses weights_only=True
- params_pqc restored as nn.Parameter with optimizer re-registration on load
- exit(), eval(var_name), and self.measurements are absent from the notebook
- No training behavior or model architecture changes
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-correctness-infrastructure/01-01-SUMMARY.md`
</output>
