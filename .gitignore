# Virtual environments
qgan_env/
qgan_env
venv/
env/
ENV/
.venv/

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Jupyter Notebook
.ipynb_checkpoints
.vscode/

# PyTorch
*.pth
*.pt

# Data files (optional - uncomment if you want to ignore data)
# *.csv
# *.h5
# *.pkl

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Logs
*.log

# Sweep runtime lockfiles (advisory flock guard — not an artifact)
results/**/.status.lock


#docs
pennylane_docs/
xlstm-main copy/
notes.txt
archive/
results/
plan.md

# Phase 14 plan 14-13: track headline checkpoint in-repo (~6 MB)
!checkpoints/best_checkpoint.pt

# Pitfall-4 remediation (D-14-22): the provenance backbone MUST ship in the v2.0-revision tag archive
!results/
!results/**/*.json

# Phase 14 plan 14-19: pre-freeze working-tree hygiene — junk that must NOT
# enter the v2.0-revision tag archive (Agent 6 L-2 freeze-readiness review).
.claude/
amp
circuit_diagram.png
datasets.zip
datasets/
qgan_pennylane copy.ipynb
QGAN_Review_Response_Plan.md.pdf
Final Results from 2000 epochs - IQP:SEL circuit/

# Phase 14 plan 14-19: classical-baseline run binaries are large and excluded
# from the tag (D-14-21 / Agent 6 HIGH-1 / M10). Only the per-seed
# metrics.json + config.yaml are tracked (force-staged with git add -f).
results/baselines/runs/**/*.npy
results/baselines/runs/**/*.npz
results/baselines/runs/**/*.pt

# LaTeX build artifacts at repo root (regenerable from .tex + bib.bib)
*.aux
*.bbl
*.blg
*.out
*.synctex.gz
/main (4) copy.pdf
/supp_material.pdf

# LaTeX build transients under paper/
paper/main.aux
paper/main.log
paper/main.fls
paper/main.fdb_latexmk
paper/main.blg
paper/main.out
paper/main.toc
paper/main.synctex.gz

# GSD project planning (local-only)
.planning/
