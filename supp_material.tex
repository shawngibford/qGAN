\newpage
\setcounter{figure}{0}
\setcounter{table}{0}
\setcounter{equation}{0}
\renewcommand{\thefigure}{A\arabic{figure}}
\renewcommand{\thetable}{A\arabic{table}}
\renewcommand{\theequation}{A\arabic{equation}}
% Disambiguate hyperref destinations in the appendix from the main text
% (avoids duplicate-destination warnings on equation.1, equation.2, etc.).
\makeatletter
\renewcommand{\theHfigure}{appendix.\thefigure}
\renewcommand{\theHtable}{appendix.\thetable}
\renewcommand{\theHequation}{appendix.\theequation}
\makeatother

\section{Supplementary Information}

Supplementary Information: Quantum Synthetic Data Generation for Industrial Bioprocess Monitoring

\textbf{Table of Contents}\\
A.1 Comparison of Synthetic Data Generation Approaches\\
A.2 Extended Background on GANs and Quantum Computing\\
A.3 Hybrid-GAN Framework for Future Work\\
A.4 Additional Validation Metrics Discussion\\
A.5 Code and Data Availability\\
A.6 Additional Figures\\
A.7 Data Transformation Details\\
A.8 Quantum Circuit Introspection\\
A.9 Per-Model Training Loss Diagnostics\\
A.10 Per-Model Reconstruction Overlays\\
A.11 Per-Model Log-Return Distribution Diagnostics\\
A.12 Per-Model DTW Alignment\\
A.13 Critic Network Architecture Details\\
A.14 Quantum Hardware Robustness Sensitivity

\subsection{Comparison of Synthetic Data Generation Approaches}

\begin{table*}
\footnotesize
  \caption{Approaches for Bioprocess Synthetic Data Generation}
  \label{tbl:bioprocess_methods}
  \begin{tabularx}{\textwidth}{@{}lXXX@{}}
    \toprule
    \textbf{Approach} & \textbf{Description} & \textbf{Advantages} & \textbf{Limitations}\\
    \midrule
    Mechanistic Model-Based & 
    First-principles models simulate bioprocess dynamics under various conditions \cite{vidossich2021role, banga2025mechanistic} & 
    Ensures physical consistency & 
    Limited by model accuracy, understanding of pathways/interactions, and ability to capture process variability \\
    \midrule
    Statistical & 
    Gaussian Process regression and multivariate statistical models generate data based on estimated probability distributions \cite{kim2023, miletic2025utility} & 
    Excel at reproducing statistical properties & 
    Struggle with complex temporal dependencies and rare events \\
    \midrule
    Data-Driven & 
    Machine learning models (VAEs, GANs) learn directly from available data \cite{goodfellow2014generativeadversarialnetworks, akkem2024comprehensive} & 
    Flexible generation without requiring mechanistic understanding & 
    Performance depends heavily on training data quality and quantity \\
    \midrule
    Hybrid & 
    Combine mechanistic knowledge with data-driven techniques \cite{albino2024hybridmodel} & 
    Incorporates physical constraints and biological relationships & 
    Complexity in implementation and model integration \\
    \bottomrule
  \end{tabularx}
\end{table*}

Table~\ref{tbl:bioprocess_methods} summarizes the principal approaches to synthetic bioprocess data generation
discussed in Section~1.2 of the main text. The table highlights the trade-offs between
physical consistency, flexibility, and data requirements across mechanistic, statistical,
data-driven, and hybrid approaches.

\subsection{Extended Background on GANs and Quantum Computing}

 \subsubsection{Classical GAN Architecture}

\begin{figure*}
    \centering
    \includegraphics[width=1\linewidth]{classicalgan.png}
    \caption{Illustration of classical GAN architecture.}
    \label{fig:classical_gan}
\end{figure*}

The original GAN formulation by Goodfellow et al. (2014) introduced several key innovations \cite{goodfellow2014generativeadversarialnetworks}: Adversarial training paradigm, Non-cooperative game theory framework, Gradient-based optimization for both networks

\textbf{Training Challenges:}

As discussed in Section~2.1, classical GANs face training instability, mode collapse,
and difficulty with complex multimodal distributions when applied to bioprocess data
\cite{Arjovsky2017, gulrajani2017improved}. These challenges motivate the quantum-enhanced
approach developed in this work.

Specific challenges include:
\begin{itemize}[nosep, leftmargin=*]
  \item \textbf{Mode collapse:} Generator produces limited diversity~\cite{Arjovsky2017}
  \item \textbf{Vanishing gradients:} Discriminator becomes too strong~\cite{goodfellow2014generativeadversarialnetworks}
  \item \textbf{Hyperparameter sensitivity:} Learning rates, batch sizes critically affect convergence~\cite{gulrajani2017improved}
\end{itemize}

\subsubsection{Wasserstein Distance Mathematical Derivation}

The Wasserstein distance, also known as the Earth Mover's Distance (EMD), provides a principled way to measure the dissimilarity between two probability distributions. \cite{villani2008optimal} For two probability measures $\mu $ and $\nu$ on a metric space M with distance function d:
\begin{equation}
    W_p(\mu, \nu) = \left(\inf_{\gamma \in \Gamma(\mu,\nu)} \int_{M \times M} d(x,y)^p d\gamma(x,y)\right)^{1/p}
\end{equation}


where $\Gamma(\mu, \nu)$ denotes the set of all joint probability measures on $M \times M$ with marginals $\mu$ and $\nu$.

\textbf{Kantorovich-Rubinstein Duality:}

For $p=1$, the Wasserstein distance can be expressed as:
\begin{equation}
W_1(\mu, \nu) = \sup_{\|f\|_L \leq 1} \left|\int f d\mu - \int f d\nu\right|
\end{equation}
where the supremum is over all 1-Lipschitz functions.

This formulation allows WGAN to use a neural network (the critic) to approximate the supremum, leading to the practical WGAN objective. \cite{arjovsky17a} The Wasserstein GAN (WGAN) was introduced to address training instabilities by using the Earth Mover's distance to provide a more meaningful and stable loss metric. \cite{arjovsky17a}  The WGAN-GP further refined this approach by replacing weight clipping with a soft gradient constraint. \cite{gulrajani2017improved}

\subsubsection{Quantum Computing Fundamentals for QGANs}

\textbf{Quantum State Representation:}

A quantum state of n qubits is represented by a vector in a $2^n$-dimensional Hilbert space:

$$|\psi\rangle = \sum_{i=0}^{2^n-1} \alpha_i |i\rangle$$

where $\sum_i |\alpha_i|^2 = 1$ (normalization condition).



\textbf{Key Quantum Gates Used:}
\begin{itemize}[nosep, leftmargin=*]
  \item \textbf{Hadamard ($H$):} Creates superposition: $H\ket{0} = \frac{1}{\sqrt{2}}(\ket{0} + \ket{1})$
  \item \textbf{Rotation gates ($R_x$, $R_y$, $R_z$):} Parameterized single-qubit rotations
  \item \textbf{CNOT:} Two-qubit entangling gate
\end{itemize}

\textbf{Quantum Advantage for Generative Models:}

QGANs leverage the inherent properties of quantum systems to provide potential advantages over classical generative models. \cite{Dallaire_Demers_2018, Lloyd_2018, Zoufal_2019}

Specific advantages include:
\begin{itemize}[nosep, leftmargin=*]
  \item \textbf{Exponential state space:} $n$ qubits represent $2^n$ amplitudes, enabling more efficient representation of complex probability distributions \cite{zoufal2021generativequantummachinelearning}
  \item \textbf{Quantum interference:} Constructive/destructive interference explores solution space more efficiently
  \item \textbf{Entanglement:} Creates correlations impossible in classical probability, potentially affecting convergence behaviour; we do not claim a mode-collapse advantage and did not measure one  \cite{romero2019variationalquantumgeneratorsgenerative, Amin_2018}
\end{itemize}

Quantum interference effects can enhance the model's ability to capture subtle correlations and nonlinear relationships present in real-world data, making them particularly suited for complex temporal dependencies found in bioprocess systems. Practical scalability nonetheless remains constrained by current NISQ-device noise and limited circuit depth \cite{Cerezo_2021}.

Recent experimental implementations have demonstrated proof-of-concept QGANs on noisy intermediate-scale quantum (NISQ) devices.  \cite{rudolph2022generationhighresolutionhandwrittendigits, Huang_2021} Rudolph et al. demonstrated that QGANs can generate high-resolution images using an ion trap quantum computer, highlighting the potential of quantum models to exceed classical limitations in certain generative tasks. \cite{rudolph2022generationhighresolutionhandwrittendigits}

\subsection{Proposed Extension (Outlook): Hybrid-GAN-Mechanistic Framework}

\subsubsection{Proposed Mathematical Formulation (not implemented in this study)}

\emph{The following Hybrid-GAN-mechanistic structure is a proposed extension and was not implemented, trained, or evaluated in this work; it is included to motivate future research and is presented as a formulation only.} The proposed Hybrid-GAN-mechanistic structure would integrate generative adversarial networks with physics-informed mechanistic components to enhance predictive modeling in data-scarce environments. This approach complements hybrid machine learning approaches prevalent in chemical and biological process modeling applications, particularly when data availability is limited.  \cite{mansouri2025models, nielsen2020hybrid,nazemzadeh2021integration, ehtesham2025dynamics}

\begin{figure*}
    \centering
    \includegraphics[width=1\linewidth]{hybridgan.png}
    \caption{QGAN and synthetic data implementation within a hybrid-model approach to process monitoring.}\label{fig:qgan_hybrid_approach}
\end{figure*}

Hybrid modeling typically combines parametric models derived from first-principles knowledge such as reaction kinetics or mass balances with nonparametric, data-driven techniques like neural networks to mitigate the challenges of sparse datasets. \cite{sharma2022hybrid} For example, hybrid models augment mechanistic simulations with machine learning to improve accuracy in bioprocessing, where experimental data are often costly and scarce due to lack of process analytical technology (PAT) or regulatory restrictions (especially in biopharmaceutical production processes), enabling reliable predictions for process optimization and control.\cite{O'Brien2021A}

The Hybrid-GAN structure integrates GANs with physics-informed mechanistic components:

\begin{equation}
\begin{split}
\min_G \max_D \bigl[ & \mathbb{E}_{x\sim p_{data}}[\log D(x)] \\
& + \mathbb{E}_{z\sim p_z}[\log(1-D(G(z)))] \\
& + \lambda\mathbb{E}_{z\sim p_z}\biggl[\biggl\|\frac{d}{dt}M(G(z)) \\
& \qquad - f(M(G(z)), t, \theta)\biggr\|^2\biggr] \bigr]
\end{split}
\end{equation}

\paragraph{Relationship to the trained objective.} The objective above is
written in the original (log-GAN / Jensen--Shannon) form,
$\mathbb{E}[\log D(x)] + \mathbb{E}[\log(1-D(G(z)))]$, augmented with a
mechanistic-residual penalty. This is deliberately distinct from the objective
actually trained in this study, which is the Wasserstein GAN with gradient
penalty (Eq.~\ref{eq:wgangp}, Earth-Mover formulation). The log-GAN form is
retained here only because it most transparently exposes where a mechanistic
residual term would attach; any future implementation of this proposed
extension would substitute the Wasserstein--gradient-penalty critic used
throughout the present work (Section~3) for the $\log D$ discriminator, so that
the Hybrid-GAN inherits the training stability documented for the
QWGAN-GP evaluated in this study rather than the mode-collapse and
vanishing-gradient pathologies of the original GAN objective.

\textbf{Components:}
\begin{itemize}[nosep, leftmargin=*]
  \item $G$: Generator mapping latent variables $z$ to generated process variables
  \item $D$: Discriminator assessing authenticity
  \item $M$: Mechanistic operator projecting generated data onto physical states
  \item $f$: Governing dynamics (e.g., ODEs describing bioprocess kinetics)
  \item $\lambda$: Weighting hyperparameter balancing data fidelity and physical consistency
  \item $\theta$: Model parameters from constitutive equations (reaction kinetics, particle size distribution, growth rates, etc.)
\end{itemize}

This optimization balances statistical fidelity from empirical data distributions ($p_{data}$) with physical consistency, minimizing residuals from time derivatives.

\subsubsection{Integration with Process Models}


\begin{figure}[ht]
  \centering
  \includegraphics[width=\columnwidth]{mech_rep.png}
  \caption{Mechanistic representation of (bio)chemical processes. Reprinted with permission from Elsevier. \cite{mansouri2025models}}
  \label{fig:mech_rep}
\end{figure}

The mechanistic representation involves several key equation types:

\textbf{Balance Equations:}
\begin{equation}
  D = f(x, u, d, \theta, t)
  \label{eq:balance}
\end{equation}

\textbf{Constraint Equations:}
\begin{align}
  0 &= g_1(x, y) \label{eq:constraint1} \\
  0 &= g_2(x, y, d) - u \label{eq:constraint2}
\end{align}

\textbf{Constitutive Equations:}
\begin{equation}
  0 = g_3(x, y, c) - \theta
  \label{eq:constitutive}
\end{equation}

\noindent where:
\begin{itemize}[nosep, leftmargin=*]
  \item $c$: vector of model parameters
  \item $d$: vector of specified variables
  \item $t$: independent variable (time)
  \item $u$: vector of design variables
  \item $x$: vector of process variables
  \item $y$: vector of measured variables
  \item $\theta$: vector of physico-chemical properties
\end{itemize}

This structure extends physics-informed neural networks (PINNs) paradigm by incorporating adversarial training for synthetic data augmentation in data-limited scenarios. This connection underscores the Hybrid-GAN's utility in addressing small data challenges, where pure data-driven models fail due to insufficient training samples, by infusing prior scientific knowledge to produce robust, generalizable predictive models.

\subsubsection{Comparison with Other Modeling Approaches}

\begin{table}
\small
\caption{\emph{Qualitative, aspirational} comparison of modelling approaches in data-scarce environments. The ratings are conceptual expectations from the literature, \emph{not} measured outcomes of this study; in particular the ``Hybrid-GAN (Proposed)'' row describes a proposed architecture that was not implemented or evaluated here.}
\label{tbl:various_approaches}
\resizebox{\columnwidth}{!}{%
\begin{tabular}{p{2cm}p{2cm}p{2cm}p{2cm}p{2.55cm}p{2cm}}
\hline
\textbf{Method} & \textbf{Data Efficiency} & \textbf{Physical Consistency} & \textbf{Generative Capability} & \textbf{Interpretability} & \textbf{Robust to Small Data}\\
\hline
Pure Data-Driven Models & \textbf{\ding{55}}& \textbf{\ding{55}}& \textbf{\ding{51}}& \textbf{\ding{55}}& \textbf{\ding{55}} \\
\midrule
Mechanistic Models & \textbf{\ding{51}}& \textbf{\ding{51}}& \textbf{\ding{55}}& \textbf{\ding{51}}& \textbf{\ding{51}}\\
\midrule
Hybrid Models (Mech + ML) & \textbf{\ding{51}}& \textbf{\ding{51}}& \textbf{\ding{55}}& \textbf{\ding{51}}& \textbf{\ding{51}}\\
\midrule
PINNs & \textbf{\ding{51}}& \textbf{\ding{51}}& \textbf{\ding{55}}& \textbf{\ding{51}}& \textbf{\ding{51}}\\
\midrule
Standard GANs & \textbf{\ding{55}}& \textbf{\ding{55}}& \textbf{\ding{51}}& \textbf{\ding{55}}& \textbf{\ding{55}}\\
\midrule
Hybrid-GAN (Proposed) & \textbf{\ding{51}}& \textbf{\ding{51}}& \textbf{\ding{51}}& \textbf{\ding{51}}& \textbf{\ding{51}} \\
\bottomrule
\end{tabular}}
%}
\end{table}

This table is an \emph{aspirational} qualitative comparison: the ``Hybrid-GAN (Proposed)'' row reflects expected properties of the proposed extension and was \emph{not} experimentally validated in this study. It is provided to motivate future work, not as a demonstrated result. If a measured comparison cannot be provided, the row (or the table) may be removed at the editor's discretion.

\subsubsection{Expected Regularization Properties}

In principle, the physics term $M(G(z))$ would impose mechanistic constraints that reduce overfitting \cite{rice2020overfittingadversariallyrobustdeep}, while sensor measurements ($y$) would constrain the latent space to physically feasible dynamics \cite{hermann2025hybrid}. These properties remain to be validated empirically.


\subsection{Additional Validation Metrics Discussion} 

\subsubsection{Dynamic Time Warping (DTW) Implementation}

Dynamic Time Warping (DTW) represents a powerful technique for measuring similarity between temporal sequences that may exhibit varying lengths or temporal misalignments. \cite{dimoudis2023utilizing} The DTW algorithm operates by identifying an optimal warping path that minimizes the cumulative distance between corresponding elements of two time series while respecting monotonicity and boundary constraints.

\begin{algorithm}[ht]
  \caption{Dynamic Time Warping}
  \label{alg:dtw}
  \begin{algorithmic}[1]
    \Require Time series $S$ of length $n$, time series $T$ of length $m$
    \Ensure DTW distance between $S$ and $T$
    \State $\mathbf{DTW} \gets (n+1) \times (m+1)$ matrix
    \State $\mathbf{DTW}[0,\,0] \gets 0$
    \For{$i = 1$ to $n$}
      \State $\mathbf{DTW}[i,\,0] \gets \infty$
    \EndFor
    \For{$j = 1$ to $m$}
      \State $\mathbf{DTW}[0,\,j] \gets \infty$
    \EndFor
    \For{$i = 1$ to $n$}
      \For{$j = 1$ to $m$}
        \State $\text{cost} \gets |S[i] - T[j]|$
        \State $\mathbf{DTW}[i,\,j] \gets \text{cost} + \min\begin{cases}
          \mathbf{DTW}[i-1,\,j]   & \text{insertion} \\
          \mathbf{DTW}[i,\,j-1]   & \text{deletion} \\
          \mathbf{DTW}[i-1,\,j-1] & \text{match}
        \end{cases}$
      \EndFor
    \EndFor
    \State \Return $\mathbf{DTW}[n,\,m]$
  \end{algorithmic}
\end{algorithm}

\textbf{Interpretation:}
Lower DTW scores indicate better temporal alignment. Under the matched parameter budget and matched 2000-epoch training contract, the OD-scale DTW distance for the 55-parameter IQP:SEL quantum generator is approximately 0.30 (mean over 5 seeds). A frozen pre-v1.0 best-case checkpoint reported a DTW score of 0.6843; this value is not re-emitted under the matched-budget contract and is retained here only as a historical reference. DTW is robust to phase shifts and speed variations in time series.

\paragraph{Reconciliation with the pre-v1.0 DTW reference.}
An earlier frozen pre-v1.0 best-case checkpoint reported an OD-scale DTW distance of 0.6843. That value was computed under a legacy preprocessing pipeline (Pipeline~A; not the matched-budget Pipeline~B), under a single-seed best-case selection, and at a different epoch budget. Under the matched-budget contract (Pipeline~B, 5-seed mean, 2000 epochs) reported in Section~4 of the main text, the corresponding OD-scale DTW for the 55-parameter IQP:SEL quantum generator is $0.302 \pm 0.030$; the 0.6843 figure is retained only for historical traceability and is not directly comparable to the matched-budget results.

The selection of DTW for temporal sequence comparison stems from its ability to accommodate local temporal distortions while preserving the overall sequential structure. Unlike rigid distance metrics, DTW provides robust similarity measurements for time-dependent data by dynamically adjusting for speed variations, length differences, and localized temporal shifts that commonly occur in real-world temporal patterns.

\paragraph{Per-seed LR-DTW dominance margin.}
The cross-model comparison in main-text Section~4.1 reports a per-seed
dominance result on LR-DTW: every quantum variant beats every classical
adversarial baseline on every seed (a $4 \times 3 \times 5 = 60$-cell grid
with zero counterexamples). Table~\ref{tbl:per_seed_dtw_dominance} makes
this directly auditable by listing, for each seed, the worst-case quantum
variant's LR-DTW and the best-case classical adversarial baseline's
LR-DTW. The worst-case quantum variant is V3 (75 parameters) at every
seed; the best-case classical baseline is wgan\_lstm (78 parameters) at
every seed. The reported gap is the minimum per-seed margin between any
quantum cell and any classical adversarial cell in the cross-grid; all
60 cells satisfy quantum $<$ classical.

% source: revision/results/matched2000_dualscale.json#rows filtered to metric_name=dtw_mean, scale=log_return, model_kind in {V3, wgan_lstm}, seed in {42,43,44,45,46}
\begin{table*}[!htbp]
\centering
\caption{Per-seed LR-DTW dominance margin between the worst-case quantum
variant (V3, 75 parameters) and the best-case classical WGAN adversarial
baseline (wgan\_lstm, 78 parameters) at the matched 2000-epoch budget.
For every seed the worst quantum LR-DTW is below the best classical WGAN
LR-DTW; the gap column gives the absolute margin. The tightest margin
is at seed~45 ($4.91$, $\approx 35\%$ of the classical value). This table
makes the ``no quantum--WGAN seed overlap on LR-DTW'' claim in
\S4.1 and \S4.2 of the main text directly verifiable. Source:
\protect\path{revision/results/matched2000_dualscale.json}, filtered to
\texttt{metric\_name=dtw\_mean} and \texttt{scale=log\_return}.}
\label{tbl:per_seed_dtw_dominance}
\begin{tabular}{@{}cccc@{}}
\toprule
\textbf{Seed} & \textbf{Worst quantum (V3)} & \textbf{Best classical (wgan\_lstm)} & \textbf{Gap} \\
\midrule
42 & $9.6572$ & $18.6874$ & $9.0303$ \\
43 & $8.1597$ & $19.3005$ & $11.1407$ \\
44 & $11.0691$ & $19.7907$ & $8.7217$ \\
45 & $9.1580$ & $14.0645$ & $4.9065$ \\
46 & $9.3591$ & $19.3106$ & $9.9516$ \\
\bottomrule
\end{tabular}
\end{table*}

\subsubsection{Pairwise Welch t-tests on EMD}

The main-text Section~4.1 reports the OD-EMD null result and the
LR-EMD reversal in aggregate (``Welch $p > 0.36$ on OD'', ``every
classical adversarial baseline outperforms every quantum variant on
LR-EMD''). Tables~\ref{tbl:welch_od_emd} and~\ref{tbl:welch_lr_emd}
make those aggregates auditable cell by cell by listing the full 4
quantum~$\times$ 5 classical-side $=$ 20 pairwise Welch two-sample
$t$-tests at each scale (Welch $t$, two-sided $p$, Cohen's $d$, and a
nonparametric Mann--Whitney~$U$ $p$-value as a backup against the small
$n=5$ Welch approximation). Sign convention: positive Cohen's $d$ means
$\mathrm{mean}_q > \mathrm{mean}_c$ on EMD, i.e., the quantum variant
has \emph{higher} (worse) EMD than the comparator. Significance markers:
$^{*}\,p<0.05$, $^{**}\,p<0.01$, $^{***}\,p<0.001$.

\begin{table*}[!htbp]
\centering
\caption{Pairwise Welch $t$-tests on \textbf{OD-scale EMD} ($n=5$ seeds
per group, Pipeline~B, matched 2000-epoch budget). Seven of the 12
quantum-vs-WGAN pairs reach $p<0.05$ (cluster-floor $p \approx 0.019$
on the IQP:SEL--, V1--, V2--vs-WGAN-MLP pairings), with the quantum
variant below the WGAN comparator in every one of the 12 quantum-vs-WGAN
pairs (negative Cohen's $d$). The non-significant pairs are
quantum-vs-WGAN-CNN ($p\approx 0.31$, where the WGAN-CNN seed-42 outlier
inflates the comparator's standard deviation) and quantum-vs-VAE/AR(2)
on the OD marginal, where the non-adversarial baselines are not the
focus of the cluster-dominance claim. The two one-sided test (TOST)
for equivalence is not satisfied at any defensible margin under the
$n=5$ seed budget, consistent with the cluster-dominance reading rather
than a parametric-equivalence reading.}
\label{tbl:welch_od_emd}
\scriptsize
\setlength{\tabcolsep}{4pt}
\begin{tabular}{@{}llrrrrr@{}}
\toprule
\textbf{Quantum} & \textbf{Classical} & \textbf{Mean}$_q$ & \textbf{Mean}$_c$ & \textbf{Welch $p$} & \textbf{Cohen's $d$} & \textbf{MWU $p$} \\
\midrule
IQP:SEL & WGAN-MLP & $0.0282$ & $0.0769$ & $0.019\,^{*}$ & $-2.34$ & $0.008$ \\
IQP:SEL & WGAN-CNN & $0.0282$ & $0.7989$ & $0.307$ & $-0.74$ & $0.008$ \\
IQP:SEL & WGAN-LSTM & $0.0282$ & $0.1177$ & $0.047\,^{*}$ & $-1.79$ & $0.032$ \\
IQP:SEL & VAE & $0.0282$ & $0.0257$ & $0.539$ & $+0.41$ & $0.421$ \\
IQP:SEL & AR(2) & $0.0282$ & $0.0291$ & $0.750$ & $-0.21$ & $0.841$ \\
V1 & WGAN-MLP & $0.0283$ & $0.0769$ & $0.019\,^{*}$ & $-2.32$ & $0.008$ \\
V1 & WGAN-CNN & $0.0283$ & $0.7989$ & $0.307$ & $-0.74$ & $0.008$ \\
V1 & WGAN-LSTM & $0.0283$ & $0.1177$ & $0.047\,^{*}$ & $-1.79$ & $0.032$ \\
V1 & VAE & $0.0283$ & $0.0257$ & $0.536$ & $+0.41$ & $0.421$ \\
V1 & AR(2) & $0.0283$ & $0.0291$ & $0.816$ & $-0.15$ & $0.690$ \\
V2 & WGAN-MLP & $0.0279$ & $0.0769$ & $0.019\,^{*}$ & $-2.34$ & $0.008$ \\
V2 & WGAN-CNN & $0.0279$ & $0.7989$ & $0.307$ & $-0.74$ & $0.008$ \\
V2 & WGAN-LSTM & $0.0279$ & $0.1177$ & $0.046\,^{*}$ & $-1.79$ & $0.032$ \\
V2 & VAE & $0.0279$ & $0.0257$ & $0.597$ & $+0.35$ & $0.548$ \\
V2 & AR(2) & $0.0279$ & $0.0291$ & $0.711$ & $-0.24$ & $0.690$ \\
V3 & WGAN-MLP & $0.0308$ & $0.0769$ & $0.023\,^{*}$ & $-2.19$ & $0.008$ \\
V3 & WGAN-CNN & $0.0308$ & $0.7989$ & $0.309$ & $-0.74$ & $0.008$ \\
V3 & WGAN-LSTM & $0.0308$ & $0.1177$ & $0.051$ & $-1.74$ & $0.032$ \\
V3 & VAE & $0.0308$ & $0.0257$ & $0.243$ & $+0.80$ & $0.222$ \\
V3 & AR(2) & $0.0308$ & $0.0291$ & $0.598$ & $+0.35$ & $0.548$ \\
\bottomrule
\end{tabular}
\end{table*}

\begin{table*}[!htbp]
\centering
\caption{Pairwise Welch $t$-tests on \textbf{log-return EMD (LR-EMD)}
($n=5$ seeds per group, Pipeline~B, matched 2000-epoch budget). Of
the 12 quantum-vs-WGAN pairs, all 12 reach $p<0.05$ with negative
Cohen's $d$ (quantum variant has \emph{lower} LR-EMD than the WGAN
comparator), and $|d|$ ranges from $\approx 2.8$ (vs.\ WGAN-CNN, where
the WGAN-CNN seed-42 outlier inflates the comparator standard
deviation) to $\approx 7.7$ (vs.\ WGAN-MLP). Quantum-vs-VAE pairs
show large negative $d$ values (the VAE is in the marginal-distribution
cluster while being degenerate on temporal-structure metrics --- see
\S4.1 ``VAE characterization'' in the main text). Quantum-vs-AR(2)
pairs go in the opposite direction: AR(2)'s closed-form Yule--Walker
fit is structurally near-optimal for a Gaussian-like marginal log-return
distribution, so quantum is only marginally above AR(2) (small positive
$d$). The pattern is highly statistically significant in the
quantum-leads direction against the WGAN cluster.}
\label{tbl:welch_lr_emd}
\scriptsize
\setlength{\tabcolsep}{4pt}
\begin{tabular}{@{}llrrrrr@{}}
\toprule
\textbf{Quantum} & \textbf{Classical} & \textbf{Mean}$_q$ & \textbf{Mean}$_c$ & \textbf{Welch $p$} & \textbf{Cohen's $d$} & \textbf{MWU $p$} \\
\midrule
IQP:SEL & WGAN-MLP & $0.0040$ & $0.0444$ & $2.2\!\times\!10^{-4}\,^{***}$ & $-7.65$ & $0.008$ \\
IQP:SEL & WGAN-CNN & $0.0040$ & $0.1286$ & $0.011\,^{*}$ & $-2.81$ & $0.008$ \\
IQP:SEL & WGAN-LSTM & $0.0040$ & $0.0244$ & $0.001\,^{**}$ & $-5.10$ & $0.008$ \\
IQP:SEL & VAE & $0.0040$ & $0.0158$ & $9.4\!\times\!10^{-6}\,^{***}$ & $-17.78$ & $0.008$ \\
IQP:SEL & AR(2) & $0.0040$ & $0.0029$ & $0.060$ & $+1.63$ & $0.056$ \\
V1 & WGAN-MLP & $0.0041$ & $0.0444$ & $2.5\!\times\!10^{-4}\,^{***}$ & $-7.68$ & $0.008$ \\
V1 & WGAN-CNN & $0.0041$ & $0.1286$ & $0.011\,^{*}$ & $-2.81$ & $0.008$ \\
V1 & WGAN-LSTM & $0.0041$ & $0.0244$ & $0.001\,^{**}$ & $-5.13$ & $0.008$ \\
V1 & VAE & $0.0041$ & $0.0158$ & $7.0\!\times\!10^{-7}\,^{***}$ & $-33.77$ & $0.008$ \\
V1 & AR(2) & $0.0041$ & $0.0029$ & $0.005\,^{**}$ & $+3.31$ & $0.008$ \\
V2 & WGAN-MLP & $0.0044$ & $0.0444$ & $2.7\!\times\!10^{-4}\,^{***}$ & $-7.64$ & $0.008$ \\
V2 & WGAN-CNN & $0.0044$ & $0.1286$ & $0.011\,^{*}$ & $-2.81$ & $0.008$ \\
V2 & WGAN-LSTM & $0.0044$ & $0.0244$ & $0.001\,^{**}$ & $-5.08$ & $0.008$ \\
V2 & VAE & $0.0044$ & $0.0158$ & $5.5\!\times\!10^{-13}\,^{***}$ & $-267.45$ & $0.008$ \\
V2 & AR(2) & $0.0044$ & $0.0029$ & $2.6\!\times\!10^{-7}\,^{***}$ & $+16.42$ & $0.008$ \\
V3 & WGAN-MLP & $0.0050$ & $0.0444$ & $1.8\!\times\!10^{-4}\,^{***}$ & $-7.33$ & $0.008$ \\
V3 & WGAN-CNN & $0.0050$ & $0.1286$ & $0.012\,^{*}$ & $-2.79$ & $0.008$ \\
V3 & WGAN-LSTM & $0.0050$ & $0.0244$ & $8.7\!\times\!10^{-4}\,^{***}$ & $-4.70$ & $0.008$ \\
V3 & VAE & $0.0050$ & $0.0158$ & $1.5\!\times\!10^{-4}\,^{***}$ & $-8.93$ & $0.008$ \\
V3 & AR(2) & $0.0050$ & $0.0029$ & $0.053$ & $+1.71$ & $0.016$ \\
\bottomrule
\end{tabular}
\end{table*}

\subsubsection{TSTR Cross-Model Utility}

Reviewer R1-M2 requested a Train-on-Synthetic, Test-on-Real (TSTR)
evaluation as the downstream-utility complement to the distributional
metrics. Figure~\ref{fig:tstr_crossmodel_matched2000} reports TSTR
$R^2$ for a 1-step-ahead OD soft sensor (1-layer LSTM,
hidden$=32$) trained on synthetic windows from each of the 9
matched-budget generators (Pipeline~B, 2000 epochs, 3 init seeds
$\{40, 41, 42\}$).

\begin{figure}[!htbp]
  \centering
  \includegraphics[width=\columnwidth]{revision/results/figures/tstr_crossmodel_matched2000.pdf}
  \caption{TSTR cross-model utility evaluation: 1-step-ahead OD soft
  sensor (1-layer LSTM, hidden$=32$) trained on 19200 synthetic
  windows from each generator (Pipeline~B, 2000 epochs, 3 init seeds
  $\{40, 41, 42\}$), evaluated on $n=320$ held-out real windows. All
  9 matched-budget generators converge to $R^2 \approx 0.99$,
  dramatically above the real-only baseline of $R^2 = -13.35$
  ($n=65$ real training windows; the small real set fails to support
  soft-sensor training). The cross-generator convergence is
  \emph{structural} (caveat): the cumulative-sum back-transform from
  log-returns to OD mathematically encodes near-perfect lag-1
  autocorrelation, so a soft sensor trained on Pipeline-B-derived
  synthetic OD essentially learns the persistence forecast, which is
  near-optimal on real OD regardless of which generator produced
  the underlying log-returns. The Pipeline-B TSTR therefore does
  \emph{not} discriminate among generators (R1-M2 disclosure);
  meaningful TSTR discrimination would require multivariate process
  data where soft-sensor utility depends on cross-channel
  conditional structure. Source:
  \protect\path{revision/results/figures/tstr_crossmodel_matched2000.json};
  underlying data
  \protect\path{revision/results/tstr_matched2000.json}.}
  \label{fig:tstr_crossmodel_matched2000}
\end{figure}

\subsection{Code and Data Availability}
Complete code repository: \url{https://github.com/shawngibford/qgan}

Archived snapshot of the tagged release: Zenodo, DOI: \texttt{ZENODO-DOI-PLACEHOLDER} (to be assigned at release-freeze time).

The complete code, trained models, and datasets have been released for transparency, independent verification, and acceleration of subsequent advances in quantum-enhanced chemical and biochemical engineering.


\textbf{Quantum Circuit Specifications:}

The quantum circuit employs a dual-stage encoding strategy as described in Section~3 of the main text:
\begin{itemize}[nosep, leftmargin=*]
  \item 5 qubits arranged in three layers
  \item IQP (Instantaneous Quantum Polynomial) encoding
  \item Strongly entangling layers with $\text{Rot}(\phi, \theta, \omega)$ gates
  \item Range-based CNOT gates: $r = (\text{layer} \bmod (n_{\text{qubits}} - 1)) + 1$
  \item Enhanced Pauli measurements ($X$ and $Z$ expectation values)
  \item Total parameters: 55 (5 for IQP encoding, 45 for the three strongly entangling layers, 5 for measurement-preparation rotations)
\end{itemize}

\subsection{Additional Figures}

\textbf{Quantum Circuit Family: Headline Generator and Ablation Variants}

The headline quantum generator (\texttt{iqp\_sel\_55}) was selected from a
four-member ansatz family that varies layer depth, parameter count, and
entangler topology while holding the qubit count (5) and the IQP-encoding
plus strongly-entangling-layer (SEL) backbone fixed. The three additional
variants (V1, V2, V3) are the ablation comparators reported in the
main-text expressibility-versus-trainability analysis (§3.2 of the main
text). Figure~\ref{fig:quantum_circuit} shows the rendered gate sequence
for each of the four circuits; per-circuit specifications follow below
the figure.

\begin{figure*}[!htbp]
  \centering
  \begin{subfigure}{\linewidth}
    \centering
    \includegraphics[width=\linewidth]{revision/results/figures/circuits/iqp_sel_55.pdf}
    \caption{IQP:SEL --- 55 parameters, depth 3, range entangler (headline circuit).}
  \end{subfigure}

  \vspace{6pt}

  \begin{subfigure}{\linewidth}
    \centering
    \includegraphics[width=\linewidth]{revision/results/figures/circuits/V1.pdf}
    \caption{V1 --- 75 parameters, depth 4, range entangler.}
  \end{subfigure}

  \vspace{6pt}

  \begin{subfigure}{\linewidth}
    \centering
    \includegraphics[width=\linewidth]{revision/results/figures/circuits/V2.pdf}
    \caption{V2 --- 135 parameters, depth 8, range entangler.}
  \end{subfigure}

  \vspace{6pt}

  \begin{subfigure}{\linewidth}
    \centering
    \includegraphics[width=\linewidth]{revision/results/figures/circuits/V3.pdf}
    \caption{V3 --- 75 parameters, depth 4, linear entangler.}
  \end{subfigure}

  \caption{Rendered gate sequences for the four 5-qubit PQC generators
  evaluated in this study. All variants share an IQP encoding stage
  followed by strongly-entangling layers (SEL) with
  $\text{Rot}(\phi, \theta, \omega)$ rotations and CNOT entanglers; they
  differ in layer depth, total trainable-parameter count, and entangler
  connectivity (range versus linear). The headline circuit (a, IQP:SEL,
  55 parameters, depth 3, range topology) is the smallest in the family.
  Variants V1--V3 span the expressibility-versus-trainability trade-off
  explored in the main-text §3.2 ablation. Rendered with PennyLane's
  \texttt{qml.draw\_mpl} from the locked configurations at
  \protect\path{revision/results/figures/circuits/<name>.json}.}
  \label{fig:quantum_circuit}
\end{figure*}

\paragraph{IQP:SEL (55 parameters, depth 3, range entangler) --- headline.}
The reported generator. Hadamard gates create uniform superposition; two
IQP layers (the first with $R_z$ rotations parameterised by the latent
noise vector as the input encoding, the second with trainable $R_z$
rotations) embed the latent vector through quadratic phase encoding;
three strongly-entangling layers with $\text{Rot}(\phi, \theta, \omega)$
gates and range-based CNOTs implement the trainable transformation; final
$R_x$ and $R_y$ rotations prepare the measurement bases. The 10-dimensional
output vector is read out as Pauli-X and Pauli-Z expectation values, one
of each per qubit.

\paragraph{V1 (75 parameters, depth 4, range entangler).}
Same IQP encoding and SEL stack as IQP:SEL, with one additional entangling
layer (depth 4 versus 3) and the corresponding +20 trainable parameters.
Tests whether a modest depth increase at fixed entangler topology yields
a fidelity improvement.

\paragraph{V2 (135 parameters, depth 8, range entangler).}
The deepest range-entangled variant; doubles V1's depth and roughly
triples IQP:SEL's parameter count. Tests whether the additional
expressibility is recoverable under the matched-budget training contract
(2000 epochs, $n = 5$ seeds, fixed hyperparameters).

\paragraph{V3 (75 parameters, depth 4, linear entangler).}
Parameter- and depth-matched to V1 (both 75 parameters, depth 4) but with
linear CNOT connectivity ($q_i \to q_{i+1}$) in place of the range-based
pattern $r = (\text{layer} \bmod (n_{\text{qubits}} - 1)) + 1$ used by the
other three variants. Isolates the contribution of entangler topology
from depth or parameter count.

The main-text §3.2 result is that, under the matched-budget contract, the
smaller 55-parameter IQP:SEL circuit was not outperformed in fidelity by
any of V1--V3, motivating its selection as the reported configuration
(see the headline LR-DTW dominance reported in main-text §4.2).
\cite{romero2019variationalquantumgeneratorsgenerative, Amin_2018}


\begin{figure*}
    \centering
    \includegraphics[width=0.75\linewidth]{bpm_qgan.drawio.png}
    \caption{Decision-tree triage schematic for bioprocess monitoring, outlined as future work in §4.5 Outlook of the main text. Not an empirical contribution of the present study.}\label{fig:qgan_schematic}
\end{figure*}
This figure illustrates the decision-tree triage workflow outlined as future work in Section~4.5 (Outlook) of the main text, sketching how sensor-feasibility, mechanistic, data-driven, and quantum-synthetic options could be organised under varying data-availability constraints. It is presented as an organising concept and is not evaluated empirically in this paper.
\begin{figure*}
    \centering
    \includegraphics[width=1\linewidth]{lucy_diagram.jpg}
    \caption{Schematic of the 20\,L LUCY\textregistered{} photobioreactor used in this study, with its sensors and actuators.}
    \label{fig:lucy}
\end{figure*}

This schematic depicts the photobioreactor experimental setup described in Section~3.2, showing the sensor and actuator configuration used for data acquisition.

\subsection{Data Transformation Details}

\begin{figure}[h]
  \centering
  \includegraphics[width=\columnwidth]{revision/results/figures/preprocessing_pipeline_4panel.pdf}
  \caption{Four-stage preprocessing pipeline (Pipeline~B, native): raw OD ($n=778$),
  log-returns $r_t = \ln \mathrm{OD}_t - \ln \mathrm{OD}_{t-1}$ ($n=777$),
  standardized ($\mu=0$, $\sigma=1$), and linearly rescaled to $[-1, 1]$ using
  the global min and max of the standardized log-return series. The trained
  generators operate on the final-stage rescaled log-return windows; all
  matched-budget evaluation results in Section~4 of the main text are reported
  under this pipeline. Source:
  \protect\path{revision/results/figures/preprocessing_pipeline_4panel.json}.}
  \label{fig:preprocessing_pipeline}
\end{figure}

The transformation from optical density measurements to log differences is motivated by the bioprocess itself: for an exponentially growing culture the optical density follows $\mathrm{OD}_t \approx \mathrm{OD}_0\,e^{\mu t}$, so the log difference between successive samples is a direct discrete estimate of the instantaneous specific growth rate. For optical density measurements $OD_t$ at time $t$:

$$r_t = \ln(\text{OD}_t) - \ln(\text{OD}_{t-1}) \;\approx\; \mu_t\,\Delta t$$

where $\mu_t$ is the local specific growth rate over the sampling interval $\Delta t$. Working in $r_t$ rather than raw OD has a direct bioprocess interpretation and three practical benefits for this growth-rate signal:
\begin{itemize}[nosep, leftmargin=*]
  \item \textbf{Growth-rate interpretability:} each $r_t$ is (up to the sampling interval) the specific growth rate, the quantity of physiological interest in cultivation monitoring, rather than an arbitrary attenuation reading
  \item \textbf{Scale/stage invariance:} the log difference is invariant to the absolute OD level, so early-exponential and high-density phases are placed on a comparable scale, which stabilises learning across the growth curve
  \item \textbf{Additivity:} growth rates over consecutive intervals sum to the net log-growth over the total span, matching how cumulative biomass increase is reported
\end{itemize}

The finance literature uses the same transform for unrelated reasons; here the justification is physiological (growth-rate estimation), not financial.

\textbf{Normalization:}
\begin{equation}
  r_t^{\,\text{NORM}} = \frac{r_t - \mu}{\sigma}
  \label{eq:normalization_s9}
\end{equation}

This brings all values to a consistent scale with mean zero and standard deviation one, which is essential for quantum circuit encoding.

\textbf{Rescale to $[-1, 1]$:} The standardized log-returns are linearly
rescaled to the closed interval $[-1, 1]$ using the global minimum and maximum
of the standardized series,
\begin{equation}
  r_t^{\,[-1,1]} = -1 + 2\,
    \frac{r_t^{\,\text{NORM}} - r_\text{min}^{\,\text{NORM}}}
         {r_\text{max}^{\,\text{NORM}} - r_\text{min}^{\,\text{NORM}}},
  \label{eq:rescale_pm1_s9}
\end{equation}
to match the bounded output range of the quantum circuit observables.

\paragraph{Preprocessing ablation: why no Lambert~$W$ transform.}
An earlier version of this pipeline (Pipeline~C in our terminology) applied
an inverse Lambert~$W$ heavy-tail correction~\cite{goerg2015lambert} between
the standardization and rescaling steps. A 5-seed ablation at 1000 epochs
(Figure~\ref{fig:preprocessing_ablation}) compared three preprocessing
chains: \emph{Pipeline~A} (raw min-max-normalized OD; control),
\emph{Pipeline~B} (log-returns standardized $+$ rescaled, as above), and
\emph{Pipeline~C} (log-return $\to$ standardize $\to$ inverse
Lambert~$W$ $\to$ rescale to $[-1, 1]$; i.e., Pipeline~B with an
inverse Lambert~$W$ step inserted between standardization and
rescaling). The three
pipelines were trained under identical 5-qubit, 3-layer, 55-parameter
IQP:SEL generators. Pipeline~A trains successfully but produces clearly
worse OD-scale fidelity (EMD $1.0516 \pm 0.0007$ vs $0.0276$ for B and
$0.0261$ for C; OD-ACF lag-1 $-0.094$ vs real-data reference $0.456$).
Pipelines~B and~C are within sampling noise of each other on every
OD-scale metric (EMD $0.0276$ vs $0.0261$; OD-ACF lag-1 $0.696$ vs
$0.697$; OD-DTW mean $0.30$ vs $0.41$; TSTR-lite $R^2$ $0.994$ vs
$0.994$). Per decision D-10-05 we therefore select Pipeline~B for the
matched-budget runs reported in the main text, dropping the Lambert~$W$
step on two grounds: (i) it does not improve OD-scale fidelity over
Pipeline~B; (ii) it directly addresses reviewer concern R1-M3, which
flagged that the Lambert~$W$ step over-Gaussianizes the input
distribution and could trivialise the generative task. Full per-seed
metrics and the ablation summary are in
\path{revision/results/transform_ablation/metrics.csv} and
\texttt{summary.md}.

\begin{figure}[h]
  \centering
  \includegraphics[width=\columnwidth]{revision/results/figures/preprocessing_ablation_comparison.pdf}
  \caption{Preprocessing ablation across three pipelines (A: raw min-max OD;
  B: log-returns standardized $+$ rescaled to $[-1, 1]$, selected;
  C: log-return $\to$ standardize $\to$ inverse Lambert~$W$ $\to$
  rescale to $[-1, 1]$, dropped). Bars are
  5-seed mean $\pm$ sample standard deviation under identical 5-qubit,
  3-layer, 55-parameter IQP:SEL generators trained at 1000 epochs.
  Pipelines~B and~C are within sampling noise of each other on every
  OD-scale metric; Pipeline~B was selected per decision D-10-05.
  Source: OD-scale panels from
  \protect\path{revision/results/transform_ablation/metrics.csv}; TSTR-lite
  $R^2$ panel from
  \protect\path{revision/results/transform_ablation/tstr_lite.json}.}
  \label{fig:preprocessing_ablation}
\end{figure}

\paragraph{WGAN sample-space convention and inverse-pipeline scaling.}
WGAN-trained generators in this study (quantum IQP:SEL plus V1/V2/V3,
classical wgan\_mlp/cnn/lstm) are trained and saved with their
$[-1, +1]$ generator outputs multiplied by $0.1$ at the
sample-export site. This convention is inherited from an earlier
reference notebook
(\protect\path{archive/qgan_pennylane_SEL.py}; the relevant scaling
block carries the explicit code comment ``Real data is roughly in
$[-0.08, 0.09]$, so scale by $\sim 0.1$''), where it served to
magnitude-match the quantum generator's $[-1, +1]$ output to
unstandardized log-returns. The same $\times 0.1$ is retained at the
WGAN training/sample-export sites in the present pipeline so that the
on-disk \texttt{samples.npy} bundles under
\protect\path{revision/results/matched2000/runs/} remain byte-stable
across runs and consistent with every saved checkpoint; modifying the
training-side scaling would invalidate every checkpoint and break
reproducibility against the released artifact set. The on-disk
WGAN-trained samples are therefore stored in $[-0.1, +0.1]$ space.

The downstream inverse formula
$r_{\text{norm}} = ((s + 1)/2)\,(r_{\max} - r_{\min}) + r_{\min}$
assumes $s \in [-1, +1]$, so every paper-cited consumer applies a
$\times 10$ correction at the \texttt{samples.npy} load boundary
via a shared helper module \protect\path{revision/_wgan_unscale.py}
before invoking the inverse formula. The correction is inference-only
and is gated by \texttt{model\_kind in \_WGAN\_KINDS}, where
\texttt{\_WGAN\_KINDS} is the set of the seven WGAN-trained generators
plus the headline-row labels emitted by the canonical headline path.

\emph{VAE and AR(2) are deliberately excluded from this set.} Per the
``Pitfall~3'' design note at \protect\path{revision/run_baselines.py:28},
the VAE and AR(2) generators are trained without the $\times 0.1$
post-scaling and their on-disk samples are already in the canonical
$[-1, +1]$ window space; passing them through the helper would be
incorrect. Every matched-budget JSON
emitted by the consumer set (matched2000\_dualscale, welch\_pairwise,
distribution\_emd, tstr\_matched2000,
predictive\_discriminative\_matched2000, ansatz\_comparison,
headline\_canonical, cross\_model\_emd, cross\_model\_dtw\_dualscale,
cross\_model\_acf\_overlay) carries differential confirmation against
this gate: every WGAN-kind row reflects the $\times 10$-corrected
inverse pipeline, every VAE and AR(2) row is unaffected.

A residual generator mean-bias in standardized space persists for the
quantum cluster (quantum mean $\approx 0$ vs.\ real Pipeline-B mean
$\approx -0.03$) and contributes a $\sim 2\times$ OD drift over 777
contiguous integration steps in the long-horizon overlays of \S A.10;
this is a training-side artifact not addressable at the inference
boundary and is disclosed for transparency. All matched-budget
headline metrics reported in main \S4 and supp \S A.10--A.13 are
computed under the $\times 10$-corrected inverse pipeline described
above.

\subsubsection{Rolling Window Implementation}

Using the rolling window technique~\cite{dimoudis2023utilizing}, we generated overlapping subsequences of the original data to capture temporal patterns and dependencies. This depends on two parameters:
\begin{itemize}[nosep, leftmargin=*]
  \item \textbf{Stride ($s$):} The gap between the starts of subsequences (set to 2)
  \item \textbf{Subsequence length ($m$):} The length of each window (set to 10)
\end{itemize}

The window length ($m$) is set longer than ($s$) to guarantee enough overlap. According to Orlandi et al.~\cite{orlandi2024enhancing}, these values optimize for balanced overlap and computational efficiency. This approach is essential for capturing the temporal dependencies that characterize bioprocess dynamics, where events at one time point influence future states.

\subsection{Quantum Circuit Introspection}
\label{sec:quantum_introspection}

This section addresses reviewer comment R2-6 (``what is the quantum
circuit actually learning?'') with three introspection diagnostics
produced under Phase~13 of the revision. The three diagnostics are
reported on a single representative trajectory (seed~42, Pipeline~B,
snapshot epochs $\{0, 250, 500, 750, 1000\}$); the matched-budget
quantitative evaluation in main \S4.1 is over the full 5-seed
2000-epoch protocol.

\begin{figure*}[!htbp]
  \centering
  \includegraphics[width=\linewidth]{revision/results/figures/training_progression.pdf}
  \caption{Generated log-return distribution snapshots across training:
  4 generators (quantum IQP:SEL 55p; wgan\_mlp 74p; wgan\_cnn 73p;
  wgan\_lstm 78p) at snapshot epochs $\{0, 250, 500, 750, 1000\}$. Each
  cell overlays the synthetic distribution against the real reference.
  The quantum row qualitatively tracks the real reference more closely
  than the classical rows by epoch 1000, anticipating the LR-DTW +
  lag-1 ACF dominance observed at the matched-budget 2000-epoch
  evaluation reported in main \S4.1. Single seed 42, Pipeline~B. Phase
  13 INTRO-01 deliverable. Source:
  \protect\path{revision/results/figures/training_progression.json}.}
  \label{fig:training_progression}
\end{figure*}

\begin{figure}[!htbp]
  \centering
  \includegraphics[width=\columnwidth]{revision/results/figures/entanglement_trajectory.pdf}
  \caption{Entanglement entropy trajectory of the quantum generator
  during training (variant V1, 4 layers, range topology, seed~42).
  Bipartition $\{0,1\}\,|\,\{2,3,4\}$. Top: Von Neumann entanglement
  entropy across snapshot epochs $\{0, 250, 500, 750, 1000\}$, values
  in $[1.17, 1.25]$ against the reference bound $\ln 4 \approx 1.386$
  for maximal entanglement on this bipartition. Bottom: state purity
  $\mathrm{Tr}(\rho^2) \in [0.32, 0.35]$ against the reference bound
  $0.25$ for the maximally mixed state. The trajectory shows the
  generator maintains a highly entangled state throughout training
  without collapsing to a product state, providing the quantum-resource
  signal R2-6 requested. Phase 13 INTRO-03 deliverable. Source:
  \protect\path{revision/results/figures/entanglement_trajectory.json}.}
  \label{fig:entanglement_trajectory}
\end{figure}

\begin{figure}[!htbp]
  \centering
  \includegraphics[width=\columnwidth]{revision/results/figures/param_trajectory.pdf}
  \caption{PQC parameter trajectory during training (variant V1,
  4 layers, range topology, 75 parameters, seed~42). Top: $L_2$-norm
  of the parameter vector across snapshot epochs
  $\{0, 250, 500, 750, 1000\}$; values stably bounded between
  $\approx 4.40$ and $4.41$ (no divergence, no collapse to zero).
  Bottom: distribution of all 75 individual rotation angles at each
  snapshot epoch, with the angle diversity maintained throughout
  training rather than collapsing into a narrow band. Together the
  panels show the optimizer explores the parameter space without
  pathological behavior. Phase 13 INTRO-02 deliverable. Source:
  \protect\path{revision/results/figures/param_trajectory.json}.}
  \label{fig:param_trajectory}
\end{figure}

\subsection{Per-Model Training Loss Diagnostics}
\label{sec:per_model_loss}

Complementing the in-loop OD-EMD convergence figure in main \S3.1
(Figure~\ref{fig:training_convergence_all_models}),
Figure~\ref{fig:per_model_loss_grid} reports the raw per-model training
losses (single representative seed~42, 2000 epochs, Pipeline~B). The
panels surface three qualitatively distinct training regimes.

\textbf{WGAN-GP critic stability across the cohort.} For all 7 WGAN-GP
generators (4 quantum + 3 classical adversarial), the critic loss
descends from its initialization value of $\sim\!+2$ to a stable
small-magnitude negative band within $\sim\!100$ eval steps. The
generator loss correspondingly rises from $\sim\!0$ and oscillates within
a bounded range, with no NaN, no divergence, and no critic saturation
across any of the 35 individual runs (7 models $\times$ 5 seeds).
The IQP:SEL panel (Figure~\ref{fig:per_model_loss_grid}a) is
representative: critic loss settles around $-0.65$, generator loss
oscillates between $0.1$ and $1.2$ --- the dynamic
equilibrium characteristic of healthy WGAN-GP training.

\textbf{WGAN-CNN training instability (quantum--classical contrast).}
The WGAN-CNN panel (Figure~\ref{fig:per_model_loss_grid}f) shows a
qualitatively different signature: the critic loss bottoms out near
$-3.0$ by eval step $\sim\!50$ but then drifts monotonically back
toward $-1.2$ over the remainder of training, indicating the
critic progressively loses discriminative power against the WGAN-CNN
generator. The 4 quantum and the other 2 classical adversarial
generators do not exhibit this drift. This training-side instability
is consistent with the seed-42 OD-EMD outlier reported in main \S4.1
(Section~4.1, paragraph beginning ``On the optical-density single-step
marginal'') and with the dip visible in
Figure~\ref{fig:training_convergence_all_models}: WGAN-CNN at the
matched-budget contract is intermittently fragile on this dataset
while the quantum-cluster training is stable across all 5 seeds.

\textbf{VAE convergence pattern (regularization collapse).} The VAE
panel (Figure~\ref{fig:per_model_loss_grid}h) shows ELBO and
reconstruction terms collapsing to $\sim\!0.058$ within the first
$\sim\!50$ eval steps and remaining flat thereafter, while the KL
divergence term collapses essentially to zero in the same window
and never recovers. This regularization-term collapse is the
training-side signature of the degenerate generation regime
characterized in main \S4.1: the encoder finds a near-zero-KL
solution (posterior matched to the prior) and the decoder learns a
representation that produces real-matching one-step marginals while
losing the temporal structure that the lag-1 ACF $-0.648$ exposes
at evaluation time.

\textbf{AR(2): closed-form fit.} AR(2) is not included in the loss
grid because it is fit in closed form via \texttt{numpy.linalg.lstsq}
on the training log-return windows (residual variance
$\hat{\sigma}^2 = 0.057$, sample mean $-0.003$, sample standard
deviation $0.242$) and therefore has no epoch-wise loss trajectory.
The fit-summary statistics are recorded in
\protect\path{revision/results/figures/loss_ar.json#fit_summary}.

\textbf{No training pathology detected at the matched-budget contract.}
Across all 9 generators (or 35 individual WGAN-GP runs), no run
diverged, NaN-ed, or required intervention; the WGAN-CNN critic-drift
is the only training-side anomaly and is consistent with that model's
known fragility at the matched-budget parameter count of 73.

\begin{figure*}[!htbp]
  \centering
  \begin{subfigure}{0.24\linewidth}
    \includegraphics[width=\linewidth]{revision/results/figures/loss_iqp_sel_55_repro.pdf}
    \caption{IQP:SEL (55p, Q)}
  \end{subfigure}\hfill
  \begin{subfigure}{0.24\linewidth}
    \includegraphics[width=\linewidth]{revision/results/figures/loss_V1.pdf}
    \caption{V1 (75p, Q)}
  \end{subfigure}\hfill
  \begin{subfigure}{0.24\linewidth}
    \includegraphics[width=\linewidth]{revision/results/figures/loss_V2.pdf}
    \caption{V2 (135p, Q)}
  \end{subfigure}\hfill
  \begin{subfigure}{0.24\linewidth}
    \includegraphics[width=\linewidth]{revision/results/figures/loss_V3.pdf}
    \caption{V3 (75p, Q)}
  \end{subfigure}

  \vspace{4pt}

  \begin{subfigure}{0.24\linewidth}
    \includegraphics[width=\linewidth]{revision/results/figures/loss_wgan_mlp.pdf}
    \caption{WGAN-MLP (74p, C)}
  \end{subfigure}\hfill
  \begin{subfigure}{0.24\linewidth}
    \includegraphics[width=\linewidth]{revision/results/figures/loss_wgan_cnn.pdf}
    \caption{WGAN-CNN (73p, C)}
  \end{subfigure}\hfill
  \begin{subfigure}{0.24\linewidth}
    \includegraphics[width=\linewidth]{revision/results/figures/loss_wgan_lstm.pdf}
    \caption{WGAN-LSTM (78p, C)}
  \end{subfigure}\hfill
  \begin{subfigure}{0.24\linewidth}
    \includegraphics[width=\linewidth]{revision/results/figures/loss_vae.pdf}
    \caption{VAE (562p, ELBO+recon+KLD)}
  \end{subfigure}
  \caption{Per-model training loss trajectories at the matched-budget
  contract (single representative seed 42, 2000 epochs, Pipeline~B).
  (a)--(d): 4 quantum WGAN-GP variants. (e)--(g): 3 classical
  adversarial WGAN-GP baselines. (h): VAE (single-Adam ELBO loop;
  the only non-adversarial model with a per-epoch trajectory). Q $=$
  quantum, C $=$ classical. AR(2) is fit in closed form via
  \texttt{lstsq} and has no epoch-wise loss trajectory ($\hat\sigma^2 =
  0.057$ reported in text). Source: per-model trajectories from
  \protect\path{revision/results/matched2000/runs/<model>/42/metrics.json}
  via
  \protect\path{revision/results/figures/loss_<model>.json}.}
  \label{fig:per_model_loss_grid}
\end{figure*}

\subsection{Per-Model Reconstruction Overlays}

Figures~\ref{fig:reconstruction_iqp_sel_55_repro}--\ref{fig:reconstruction_ar}
provide a per-model time-series view of generation fidelity, complementing
the per-model loss diagnostics of \S A.9 and the cross-model distributional
metrics of main-text \S4. For each of the nine matched-budget generators
(seed 42), three vertically stacked panels overlay the generated trajectory
on the corresponding real series across the full data extent (777
log-deltas + 778 OD points):
\begin{itemize}[nosep, leftmargin=*]
  \item \textbf{Top:} Log-delta comparison (generated vs. real log-returns,
    777 points).
  \item \textbf{Middle:} OD reconstruction on linear scale (778 points).
  \item \textbf{Bottom:} OD reconstruction on log scale (778 points).
\end{itemize}

\textbf{Visualization protocol (differs from the fidelity-metric protocol
of main-text \S4.2).} The generated 777-point log-delta sequence is built
by concatenating the first 78 rows of each model's $(3840, 10)$
\texttt{samples.npy} bundle (each row an independent 10-step draw in
$[-1, 1]$ rescaled space), un-rescaling to standardized log-returns via
the saved $r_{\min}, r_{\max}$ bounds, and un-standardizing to raw
log-deltas via the saved $\mu, \sigma$. The reconstructed OD trajectory is
then anchored at $\text{OD}_{\text{real}}[0]$ (rather than the canonical
matched-budget evaluation protocol's random per-window $od_{\text{start}}$
draw seeded from the run seed) and integrated via
$\texttt{inverse\_logreturns}$ to produce a length-778 OD series directly
overlayable on real OD. The synthetic log-delta sequence used in these figures is the same
sequence consumed by the matched-budget evaluation in main \S4 under
the $\times 10$ WGAN inverse correction described in \S A.7; no
additional figure-side post-processing is applied. A residual
generator mean-bias in standardized space persists for the quantum
cluster (quantum mean $\approx 0$ vs.\ real Pipeline-B mean
$\approx -0.03$) and, under the long-horizon overlay protocol used
here (777 contiguous integration steps anchored at
$\text{OD}_{\text{real}}[0]$), compounds into a $\sim 2\times$ OD
drift visible in the bottom panel of each figure as a slow
positive trend on the synthetic line relative to real. This residual
drift is a training-side artifact not addressable at inference; it does
not affect the matched-budget fidelity metrics in main \S4, which are
computed under the canonical per-window-anchor protocol over short
windows.

\textbf{What the figures show.} Each model's log-delta amplitude relative
to real (top panel) is the primary visual signal: a generator that
captures real log-delta variance produces an overlay with matched
oscillation amplitude (cf.\ AR(2),
Figure~\ref{fig:reconstruction_ar}); a generator with attenuated outputs
produces a near-flat orange trace (cf.\ the four quantum variants).
Reconstructed OD smoothness in the middle/bottom panels follows directly:
attenuated log-delta amplitude integrates to a smoother OD trajectory,
consistent with the main-text \S4.1/\S4.2 finding that the
matched-budget quantum entrants exceed classical adversarials on
temporal alignment (LR-DTW) while falling short on single-step
log-return marginals (LR-EMD). AR(2) and the WGAN-LSTM/MLP entrants
exhibit the closest amplitude match to real, also consistent with the
main-text finding that classical adversarials lead on LR-EMD.

\begin{figure*}[!htbp]
  \centering
  \includegraphics[width=\linewidth]{revision/results/figures/reconstruction_iqp_sel_55_repro.pdf}
  \caption{Reconstruction overlay --- IQP:SEL 55p (2000ep repro), seed 42.
  Top: log-delta comparison (777 points). Middle: OD linear scale (778
  points). Bottom: OD log scale (778 points). Real series in blue;
  generated/reconstructed in orange. Source:
  \protect\path{revision/results/figures/reconstruction_iqp_sel_55_repro.json}.}
  \label{fig:reconstruction_iqp_sel_55_repro}
\end{figure*}

\begin{figure*}[!htbp]
  \centering
  \includegraphics[width=\linewidth]{revision/results/figures/reconstruction_V1.pdf}
  \caption{Reconstruction overlay --- Quantum V1 (75p, depth 4, range
  entangler), seed 42. Panels as in
  Figure~\ref{fig:reconstruction_iqp_sel_55_repro}. Source:
  \protect\path{revision/results/figures/reconstruction_V1.json}.}
  \label{fig:reconstruction_V1}
\end{figure*}

\begin{figure*}[!htbp]
  \centering
  \includegraphics[width=\linewidth]{revision/results/figures/reconstruction_V2.pdf}
  \caption{Reconstruction overlay --- Quantum V2 (135p, depth 8, range
  entangler), seed 42. Panels as in
  Figure~\ref{fig:reconstruction_iqp_sel_55_repro}. Source:
  \protect\path{revision/results/figures/reconstruction_V2.json}.}
  \label{fig:reconstruction_V2}
\end{figure*}

\begin{figure*}[!htbp]
  \centering
  \includegraphics[width=\linewidth]{revision/results/figures/reconstruction_V3.pdf}
  \caption{Reconstruction overlay --- Quantum V3 (75p, depth 4, linear
  entangler), seed 42. Panels as in
  Figure~\ref{fig:reconstruction_iqp_sel_55_repro}. Source:
  \protect\path{revision/results/figures/reconstruction_V3.json}.}
  \label{fig:reconstruction_V3}
\end{figure*}

\begin{figure*}[!htbp]
  \centering
  \includegraphics[width=\linewidth]{revision/results/figures/reconstruction_wgan_mlp.pdf}
  \caption{Reconstruction overlay --- WGAN-GP (MLP), 74p, seed 42.
  Panels as in Figure~\ref{fig:reconstruction_iqp_sel_55_repro}. Source:
  \protect\path{revision/results/figures/reconstruction_wgan_mlp.json}.}
  \label{fig:reconstruction_wgan_mlp}
\end{figure*}

\begin{figure*}[!htbp]
  \centering
  \includegraphics[width=\linewidth]{revision/results/figures/reconstruction_wgan_cnn.pdf}
  \caption{Reconstruction overlay --- WGAN-GP (CNN), 73p, seed 42.
  The seed-42 LR-EMD outlier
  (OD-EMD~$=\!0.1587$ vs.\ the other four seeds in 0.020--0.034,
  disclosed at face value in main-text \S4.1) is the run plotted here.
  Panels as in Figure~\ref{fig:reconstruction_iqp_sel_55_repro}. Source:
  \protect\path{revision/results/figures/reconstruction_wgan_cnn.json}.}
  \label{fig:reconstruction_wgan_cnn}
\end{figure*}

\begin{figure*}[!htbp]
  \centering
  \includegraphics[width=\linewidth]{revision/results/figures/reconstruction_wgan_lstm.pdf}
  \caption{Reconstruction overlay --- WGAN-GP (LSTM), 78p, seed 42.
  Panels as in Figure~\ref{fig:reconstruction_iqp_sel_55_repro}. Source:
  \protect\path{revision/results/figures/reconstruction_wgan_lstm.json}.}
  \label{fig:reconstruction_wgan_lstm}
\end{figure*}

\begin{figure*}[!htbp]
  \centering
  \includegraphics[width=\linewidth]{revision/results/figures/reconstruction_vae.pdf}
  \caption{Reconstruction overlay --- VAE (562p, degenerate generation
  regime; see main-text \S4.1), seed 42. Panels as in
  Figure~\ref{fig:reconstruction_iqp_sel_55_repro}. Source:
  \protect\path{revision/results/figures/reconstruction_vae.json}.}
  \label{fig:reconstruction_vae}
\end{figure*}

\begin{figure*}[!htbp]
  \centering
  \includegraphics[width=\linewidth]{revision/results/figures/reconstruction_ar.pdf}
  \caption{Reconstruction overlay --- AR(2) closed-form baseline, seed
  42. Panels as in Figure~\ref{fig:reconstruction_iqp_sel_55_repro}.
  Note the visibly tighter amplitude match in the top panel, consistent
  with AR(2)'s leading LR-EMD ranking under the matched-budget contract
  (main-text \S4.2). Source:
  \protect\path{revision/results/figures/reconstruction_ar.json}.}
  \label{fig:reconstruction_ar}
\end{figure*}

\subsection{Per-Model Log-Return Distribution Diagnostics}

Figures~\ref{fig:stat_grid_iqp_sel_55_repro}--\ref{fig:stat_grid_ar}
present, for each of the nine matched-budget generators (seed~42), a
six-panel statistical-comparison grid on the log-return scale. Inputs
are the full real log-delta series (777 points from
\protect\path{data.csv} via \protect\path{revision/core/data.py::compute_log_delta})
and the synthetic log-delta series (777 points extracted from each
model's \protect\path{samples.npy} via the saved inverse transform with
the $\times 10$ WGAN sample-space correction from \S A.7 applied at the
load boundary; the same construction as \S A.10). The
panels reflect the generator output distribution including any
residual mean bias.

The panel layout is identical across all nine figures:
\begin{itemize}[nosep, leftmargin=*]
  \item Top-left: probability density histograms (50 bins, overlaid).
  \item Top-middle: cumulative density histograms (100 bins, overlaid).
  \item Top-right: Q-Q plot vs.\ the standard normal distribution
    (\texttt{scipy.stats.probplot(dist="norm")} called for each series;
    deviation from the red reference line indicates non-Gaussianity).
  \item Bottom-left: statistical moments bar chart (mean, std,
    skewness, Fisher excess kurtosis; std computed with
    \texttt{ddof=0} and kurtosis as \texttt{scipy.stats.kurtosis}'s
    default to match \protect\path{revision/core/eval.py::compute_moments}).
  \item Bottom-middle: distance metrics bar chart with numerical
    labels --- EMD via \texttt{scipy.stats.wasserstein\_distance} on
    raw samples (\protect\path{revision/core/eval.py::compute_emd});
    Jensen-Shannon divergence on a shared-bin histogram
    (\protect\path{revision/core/eval.py::compute_jsd}, default 100
    bins); and the absolute difference of the two Shannon entropies.
  \item Bottom-right: Shannon entropy comparison (in nats), computed
    on shared 50-bin histograms with a small floor
    ($+10^{-12}$ per bin) to avoid $\log 0$.
\end{itemize}

\textbf{Scope.} These are diagnostic visualizations for distributional
fidelity at the log-return scale; they are not a restatement of the
canonical fidelity metrics reported in main-text \S 4.2 (which are
computed under the matched-budget per-window evaluation protocol with
different sample-construction details and aggregation across the five
seeds). Each panel grid is single-seed (seed~42) and uses the
777-point contiguous reconstruction sequence.

\begin{figure*}[!htbp]
  \centering
  \includegraphics[width=\linewidth]{revision/results/figures/stat_grid_iqp_sel_55_repro.pdf}
  \caption{Log-return distribution diagnostics --- IQP:SEL 55p (2000ep
  repro), seed~42. Six-panel layout per \S A.11 intro. Real (blue) vs.\
  generated (orange). Source:
  \protect\path{revision/results/figures/stat_grid_iqp_sel_55_repro.json}.}
  \label{fig:stat_grid_iqp_sel_55_repro}
\end{figure*}

\begin{figure*}[!htbp]
  \centering
  \includegraphics[width=\linewidth]{revision/results/figures/stat_grid_V1.pdf}
  \caption{Log-return distribution diagnostics --- Quantum V1 (75p),
  seed~42. Panels as in
  Figure~\ref{fig:stat_grid_iqp_sel_55_repro}. Source:
  \protect\path{revision/results/figures/stat_grid_V1.json}.}
  \label{fig:stat_grid_V1}
\end{figure*}

\begin{figure*}[!htbp]
  \centering
  \includegraphics[width=\linewidth]{revision/results/figures/stat_grid_V2.pdf}
  \caption{Log-return distribution diagnostics --- Quantum V2 (135p),
  seed~42. Panels as in
  Figure~\ref{fig:stat_grid_iqp_sel_55_repro}. Source:
  \protect\path{revision/results/figures/stat_grid_V2.json}.}
  \label{fig:stat_grid_V2}
\end{figure*}

\begin{figure*}[!htbp]
  \centering
  \includegraphics[width=\linewidth]{revision/results/figures/stat_grid_V3.pdf}
  \caption{Log-return distribution diagnostics --- Quantum V3 (75p,
  linear entangler), seed~42. Panels as in
  Figure~\ref{fig:stat_grid_iqp_sel_55_repro}. Source:
  \protect\path{revision/results/figures/stat_grid_V3.json}.}
  \label{fig:stat_grid_V3}
\end{figure*}

\begin{figure*}[!htbp]
  \centering
  \includegraphics[width=\linewidth]{revision/results/figures/stat_grid_wgan_mlp.pdf}
  \caption{Log-return distribution diagnostics --- WGAN-GP (MLP, 74p),
  seed~42. Panels as in
  Figure~\ref{fig:stat_grid_iqp_sel_55_repro}. Source:
  \protect\path{revision/results/figures/stat_grid_wgan_mlp.json}.}
  \label{fig:stat_grid_wgan_mlp}
\end{figure*}

\begin{figure*}[!htbp]
  \centering
  \includegraphics[width=\linewidth]{revision/results/figures/stat_grid_wgan_cnn.pdf}
  \caption{Log-return distribution diagnostics --- WGAN-GP (CNN, 73p),
  seed~42 (the LR-EMD outlier seed disclosed in main-text \S 4.1).
  Panels as in Figure~\ref{fig:stat_grid_iqp_sel_55_repro}. Source:
  \protect\path{revision/results/figures/stat_grid_wgan_cnn.json}.}
  \label{fig:stat_grid_wgan_cnn}
\end{figure*}

\begin{figure*}[!htbp]
  \centering
  \includegraphics[width=\linewidth]{revision/results/figures/stat_grid_wgan_lstm.pdf}
  \caption{Log-return distribution diagnostics --- WGAN-GP (LSTM, 78p),
  seed~42. Panels as in
  Figure~\ref{fig:stat_grid_iqp_sel_55_repro}. Source:
  \protect\path{revision/results/figures/stat_grid_wgan_lstm.json}.}
  \label{fig:stat_grid_wgan_lstm}
\end{figure*}

\begin{figure*}[!htbp]
  \centering
  \includegraphics[width=\linewidth]{revision/results/figures/stat_grid_vae.pdf}
  \caption{Log-return distribution diagnostics --- VAE (562p,
  degenerate generation regime; see main-text \S 4.1), seed~42.
  Panels as in Figure~\ref{fig:stat_grid_iqp_sel_55_repro}. Source:
  \protect\path{revision/results/figures/stat_grid_vae.json}.}
  \label{fig:stat_grid_vae}
\end{figure*}

\begin{figure*}[!htbp]
  \centering
  \includegraphics[width=\linewidth]{revision/results/figures/stat_grid_ar.pdf}
  \caption{Log-return distribution diagnostics --- AR(2) closed-form
  baseline, seed~42. Panels as in
  Figure~\ref{fig:stat_grid_iqp_sel_55_repro}. The visibly tightest
  amplitude/moment match in this collection is consistent with AR(2)'s
  leading LR-EMD ranking under the matched-budget contract reported
  in main-text \S 4.2. Source:
  \protect\path{revision/results/figures/stat_grid_ar.json}.}
  \label{fig:stat_grid_ar}
\end{figure*}

\subsection{Per-Model DTW Alignment}

Figures~\ref{fig:dtw_alignment_iqp_sel_55_repro}--\ref{fig:dtw_alignment_ar}
provide per-model DTW alignment visualizations: each plot shows the
real log-delta sequence on the top axis (777 points), the generated
log-delta sequence on the left axis (777 points, rotated), and the
accumulated DTW warping-cost matrix as a heatmap in the centre with the
optimal warping path drawn in red. The Sakoe-Chiba band is visible as
the diagonal corridor; cells outside the band are excluded from the
search.

\textbf{Visualization protocol (differs from main-text \S 4.2).} The
cost matrix and warping path are computed via
\texttt{dtaidistance.dtw.warping\_paths(real, gen, window=500, psi=2)}
followed by \texttt{dtaidistance.dtw.best\_path}, then rendered via
\texttt{dtaidistance.dtw\_visualisation.plot\_warpingpaths}. The
displayed numerical DTW distance is therefore a
visualization-protocol value: it uses a different DTW implementation
and band/skip-edge protocol than the canonical fastdtw-based LR-DTW
reported in main-text \S 4.2 (which is computed under the matched-budget
windowed-evaluation protocol per
\protect\path{revision/core/eval.py::compute_dtw}). Numerical DTW
distances in these figures are NOT directly comparable to the headline
LR-DTW values; the figures are intended to illustrate alignment
quality (warping-path shape, band utilization, anchor cost) rather
than to re-derive the headline metric.

\begin{figure*}[!htbp]
  \centering
  \includegraphics[width=0.85\linewidth]{revision/results/figures/dtw_alignment_iqp_sel_55_repro.pdf}
  \caption{DTW alignment --- IQP:SEL 55p (2000ep repro), seed~42.
  Top: real log-delta series. Left: generated log-delta series.
  Centre: accumulated DTW cost matrix (\texttt{dtaidistance},
  \texttt{window=500}, \texttt{psi=2}). Optimal warping path in red.
  Distance value at top-left is a visualization-protocol value; see
  \S A.12 intro. Source:
  \protect\path{revision/results/figures/dtw_alignment_iqp_sel_55_repro.json}.}
  \label{fig:dtw_alignment_iqp_sel_55_repro}
\end{figure*}

\begin{figure*}[!htbp]
  \centering
  \includegraphics[width=0.85\linewidth]{revision/results/figures/dtw_alignment_V1.pdf}
  \caption{DTW alignment --- Quantum V1 (75p), seed~42. Panels as in
  Figure~\ref{fig:dtw_alignment_iqp_sel_55_repro}. Source:
  \protect\path{revision/results/figures/dtw_alignment_V1.json}.}
  \label{fig:dtw_alignment_V1}
\end{figure*}

\begin{figure*}[!htbp]
  \centering
  \includegraphics[width=0.85\linewidth]{revision/results/figures/dtw_alignment_V2.pdf}
  \caption{DTW alignment --- Quantum V2 (135p), seed~42. Panels as in
  Figure~\ref{fig:dtw_alignment_iqp_sel_55_repro}. Source:
  \protect\path{revision/results/figures/dtw_alignment_V2.json}.}
  \label{fig:dtw_alignment_V2}
\end{figure*}

\begin{figure*}[!htbp]
  \centering
  \includegraphics[width=0.85\linewidth]{revision/results/figures/dtw_alignment_V3.pdf}
  \caption{DTW alignment --- Quantum V3 (75p, linear entangler),
  seed~42. Panels as in
  Figure~\ref{fig:dtw_alignment_iqp_sel_55_repro}. Source:
  \protect\path{revision/results/figures/dtw_alignment_V3.json}.}
  \label{fig:dtw_alignment_V3}
\end{figure*}

\begin{figure*}[!htbp]
  \centering
  \includegraphics[width=0.85\linewidth]{revision/results/figures/dtw_alignment_wgan_mlp.pdf}
  \caption{DTW alignment --- WGAN-GP (MLP, 74p), seed~42. Panels as in
  Figure~\ref{fig:dtw_alignment_iqp_sel_55_repro}. Source:
  \protect\path{revision/results/figures/dtw_alignment_wgan_mlp.json}.}
  \label{fig:dtw_alignment_wgan_mlp}
\end{figure*}

\begin{figure*}[!htbp]
  \centering
  \includegraphics[width=0.85\linewidth]{revision/results/figures/dtw_alignment_wgan_cnn.pdf}
  \caption{DTW alignment --- WGAN-GP (CNN, 73p), seed~42 (the LR-EMD
  outlier seed disclosed in main-text \S 4.1). Panels as in
  Figure~\ref{fig:dtw_alignment_iqp_sel_55_repro}. Source:
  \protect\path{revision/results/figures/dtw_alignment_wgan_cnn.json}.}
  \label{fig:dtw_alignment_wgan_cnn}
\end{figure*}

\begin{figure*}[!htbp]
  \centering
  \includegraphics[width=0.85\linewidth]{revision/results/figures/dtw_alignment_wgan_lstm.pdf}
  \caption{DTW alignment --- WGAN-GP (LSTM, 78p), seed~42. Panels as in
  Figure~\ref{fig:dtw_alignment_iqp_sel_55_repro}. Source:
  \protect\path{revision/results/figures/dtw_alignment_wgan_lstm.json}.}
  \label{fig:dtw_alignment_wgan_lstm}
\end{figure*}

\begin{figure*}[!htbp]
  \centering
  \includegraphics[width=0.85\linewidth]{revision/results/figures/dtw_alignment_vae.pdf}
  \caption{DTW alignment --- VAE (562p, degenerate generation regime;
  see main-text \S 4.1), seed~42. Panels as in
  Figure~\ref{fig:dtw_alignment_iqp_sel_55_repro}. Source:
  \protect\path{revision/results/figures/dtw_alignment_vae.json}.}
  \label{fig:dtw_alignment_vae}
\end{figure*}

\begin{figure*}[!htbp]
  \centering
  \includegraphics[width=0.85\linewidth]{revision/results/figures/dtw_alignment_ar.pdf}
  \caption{DTW alignment --- AR(2) closed-form baseline, seed~42.
  Panels as in Figure~\ref{fig:dtw_alignment_iqp_sel_55_repro}. Source:
  \protect\path{revision/results/figures/dtw_alignment_ar.json}.}
  \label{fig:dtw_alignment_ar}
\end{figure*}

\subsection{Critic Network Architecture Details}

The Critic model is a convolutional neural network that processes sequential data to distinguish between real and generated data from the quantum generator. The network follows the WGAN-GP architecture~\cite{gulrajani2017improved} with specific adaptations for time series data.

% source: revision/results/total_adversarial_param_budget.json#shared_critic_n_params (=250881)
% source: revision/results/classical_architectures.json#models.shared_critic.total_params (=250881)
The critic network has a total of 250881 trainable parameters. This same
critic was used for all WGAN-GP generators (quantum and classical) under the
matched-budget protocol defined in main-text Section~3.1; the only component
that varies between the quantum and classical entrants is the generator,
which isolates the effect of the quantum circuit on the generated
distribution.

\textbf{Architecture Specifications:}

\textbf{Convolutional Layers:}
\begin{itemize}[nosep, leftmargin=*]
  \item Layer 1: 64 filters, kernel size 3, stride 1
  \item Layer 2: 128 filters, kernel size 3, stride 1
  \item Layer 3: 128 filters, kernel size 3, stride 1
\end{itemize}

The increasing filter count allows extraction of more complex features and produces detailed feature maps. The first convolutional layer is designed to handle input data shaped as $(10, 1)$, where the dimension of 10 represents the length of subsequences created through the rolling window approach~\cite{dimoudis2023utilizing}, ensuring the network properly processes the temporal structure of the input data.

\textbf{Activation Functions:}

Each convolutional layer is followed by a LeakyReLU activation function ($\alpha = 0.1$), which addresses the vanishing gradient issue that can cause neurons to become inactive and enables the network to learn more complex patterns through its nonlinear properties~\cite{gulrajani2017improved}.

\textbf{Dense Layers:}

Following the three convolutional layers, the data undergoes flattening to convert the multidimensional feature maps into a one-dimensional vector suitable for dense layer processing. A fully connected layer with 32 neurons is then applied with LeakyReLU activation to capture complex data relationships.

\textbf{Regularization:}

To improve generalization and reduce overfitting, a dropout layer randomly deactivates 20\% of neurons during training.

\textbf{Output Layer:}

The final output layer contains a single neuron that produces an unbounded scalar value, where higher values indicate the input resembles real data and lower values suggest generated data. This unbounded output design aligns with the use of Wasserstein distance as the cost function~\cite{arjovsky17a}, providing more meaningful gradients during training compared to the original GAN formulation~\cite{goodfellow2014generativeadversarialnetworks}.

\subsection{Quantum Hardware Robustness Sensitivity}
\label{sec:robustness_sensitivity}

Reviewer R1-M5 requested an assessment of how the matched-budget
finding would degrade under realistic quantum-hardware conditions
(shot noise from finite-shot measurement; gate noise from
depolarizing and amplitude-damping channels). Phase~12 of the
revision provides two sensitivity studies addressed here:
shot-noise sensitivity (1024 and 8192 shots vs.\ statevector
analytic baseline) and noise-model sensitivity (depolarizing
$p \in \{0, 0.001, 0.01, 0.05\}$ and amplitude-damping
$\gamma \in \{0, 0.001, 0.01, 0.05\}$, per-layer channel insertion).

\begin{figure}[!htbp]
  \centering
  \includegraphics[width=\columnwidth]{revision/results/figures/shot_noise_robustness.pdf}
  \caption{Shot-noise sensitivity of the quantum generator
  (IQP:SEL 55p) under finite-shot measurement: analytic statevector
  baseline vs.\ 8192 shots vs.\ 1024 shots. OD-EMD reported as
  3-seed mean $\pm$ sample-std (seeds $\{42, 43, 44\}$) on Pipelines~A
  and~B. The 1024-shot and 8192-shot curves remain within sampling
  noise of the analytic baseline, indicating the matched-budget
  finding is robust to the shot-noise levels accessible on
  near-term hardware. Phase~12 SENS-01 deliverable.
  Source:
  \protect\path{revision/results/figures/shot_noise_robustness.json};
  underlying data
  \protect\path{revision/results/shot_noise_sensitivity.json}.}
  \label{fig:shot_noise_robustness}
\end{figure}

\begin{figure}[!htbp]
  \centering
  \includegraphics[width=\columnwidth]{revision/results/figures/noise_robustness_quantum.pdf}
  \caption{Noise-model sensitivity of the quantum generator
  (IQP:SEL 55p) under per-layer depolarizing and amplitude-damping
  channels (inserted after each entangling block). OD-EMD as 3-seed
  mean $\pm$ sample-std (seeds $\{42, 43, 44\}$) at noise levels
  $\{0, 0.001, 0.01, 0.05\}$ per channel type. Pipelines~A and~B
  shown as separate curves. Both channels show graceful degradation:
  the matched-budget finding is preserved at the 0.001 level, begins
  to degrade at 0.01, and is degraded at 0.05 — consistent with the
  noise tolerance reported for similar PQC variational circuits
  in the literature. Phase~12 SENS-02 deliverable.
  Source:
  \protect\path{revision/results/figures/noise_robustness_quantum.json};
  underlying data
  \protect\path{revision/results/noise_model_sensitivity.json}.}
  \label{fig:noise_robustness_quantum}
\end{figure}



